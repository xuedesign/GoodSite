(()=>{"use strict";var e,n={3075:(e,n,t)=>{t.d(n,{A:()=>s});var i=t(1354),a=t.n(i),r=t(6314),o=t.n(r)()(a());o.push([e.id,":root{--background-color: #FAF9F5;--rubin-gray: #87867F;--rubin-ivory: rgb(250, 249, 246);--rubin-slate: #1F1E1D;--rubin-slate: #1F1E1D;--rubin-riso: #5E7EDF;--rubin-cyan: #44A6E4;--rubin-clay: #D97757;--text-color: rgba(0, 0, 0, 0.85);--tone: #F5F2EC;--rubin-dark-gray: #5E5D59;--rubin-ivory-med: #F0EEE6;--rubin-ivory-dark: #E8E6DC;--font-family-base: goudy-old-style, serif;--font-family-code: Courier Prime, Courier, monospace;--font-size-base: 18px;--line-height-base: 1.4;--letter-spacing-base: 0.02em;--artwork-font-size: 6px;--poem-padding-top: 20px;--poem-padding-left: 24px;--poem-padding: 0px 100px;--artwork-size: 322px;--breakpoint-tablet: 768px;--nav-width: 40px;--mobile-nav-width: 62px;--mobile-menu-height: 48px}@media(min-width: 768px)and (max-width: 1199px){:root{--font-size-base: 22px;--line-height-base: 0.9;--letter-spacing-base: 0.04em;--artwork-size: 450px;--artwork-font-size: 7.45px;--poem-padding-top: 20px;--poem-padding-left: 50px;--poem-padding: 0px 100px;--artwork-font-size: 7.5px}}@media(min-width: 1200px){:root{--font-size-base: 22px;--line-height-base: 1.5;--letter-spacing-base: 0.02em;--artwork-size: 450px;--poem-padding-top: 0px;--poem-padding-left: 100px;--poem-padding: 0px 100px;--artwork-font-size: 7.45px;--font-size-base: 22px}}:root{--background-color: #FAF9F5;--rubin-gray: #87867F;--rubin-ivory: rgb(250, 249, 246);--rubin-slate: #1F1E1D;--rubin-slate: #1F1E1D;--rubin-riso: #5E7EDF;--rubin-cyan: #44A6E4;--rubin-clay: #D97757;--text-color: rgba(0, 0, 0, 0.85);--tone: #F5F2EC;--rubin-dark-gray: #5E5D59;--rubin-ivory-med: #F0EEE6;--rubin-ivory-dark: #E8E6DC;--font-family-base: goudy-old-style, serif;--font-family-code: Courier Prime, Courier, monospace;--font-size-base: 18px;--line-height-base: 1.4;--letter-spacing-base: 0.02em;--artwork-font-size: 6px;--poem-padding-top: 20px;--poem-padding-left: 24px;--poem-padding: 0px 100px;--artwork-size: 322px;--breakpoint-tablet: 768px;--nav-width: 40px;--mobile-nav-width: 62px;--mobile-menu-height: 48px}@media(min-width: 768px)and (max-width: 1199px){:root{--font-size-base: 22px;--line-height-base: 0.9;--letter-spacing-base: 0.04em;--artwork-size: 450px;--artwork-font-size: 7.45px;--poem-padding-top: 20px;--poem-padding-left: 50px;--poem-padding: 0px 100px;--artwork-font-size: 7.5px}}@media(min-width: 1200px){:root{--font-size-base: 22px;--line-height-base: 1.5;--letter-spacing-base: 0.02em;--artwork-size: 450px;--poem-padding-top: 0px;--poem-padding-left: 100px;--poem-padding: 0px 100px;--artwork-font-size: 7.45px;--font-size-base: 22px}}.privacy{position:fixed;top:0;left:0;z-index:30;height:100vh;width:calc(100% - var(--poem-padding-left)*2);display:flex;flex-direction:column;padding:var(--poem-padding-top) var(--poem-padding-left);background:#faf9f5;justify-content:flex-start;overflow-y:auto}.privacy .container{max-width:700px}.privacy .container h3{font-size:22px;font-weight:600;margin-bottom:2rem;margin-top:3rem}@media(max-width: 1024px){.privacy .container h3{margin-top:24px;font-weight:700;font-size:19px}}@media(max-width: 768px){.privacy .container h3{font-size:17px}}.privacy .container p,.privacy .container li,.privacy .container a{line-height:170%;font-size:20px;margin-bottom:8px}@media(max-width: 1024px){.privacy .container p,.privacy .container li,.privacy .container a{margin-bottom:12px}}@media(max-width: 1024px){.privacy .container p,.privacy .container li,.privacy .container a{font-size:18px}}@media(max-width: 768px){.privacy .container p,.privacy .container li,.privacy .container a{font-size:17px}}.privacy .container p{margin-bottom:2rem}.privacy .container a{color:#000;text-decoration:none;border-bottom:1px solid #000}.privacy .container a:hover{color:var(--rubin-gray);border-bottom:1px solid var(--rubin-gray)}.privacy .container section{margin-bottom:2rem}.privacy .container .backLink{margin-top:8vh}.privacy .container .backLink a{color:var(--rubin-gray);border-bottom:1px solid var(--rubin-gray)}@media(max-width: 1200px){.privacy{justify-content:flex-start}}@media(max-width: 768px){.privacy{scrollbar-width:none;-ms-overflow-style:none}.privacy::-webkit-scrollbar{display:none}.privacy section{margin-bottom:1rem}.privacy .container{margin-bottom:2rem}.privacy .container h3{margin-top:18px}.privacy .container p:first-child{margin-bottom:1rem}.privacy .container .backLink{margin-top:5vh;margin-bottom:24px}}","",{version:3,sources:["webpack://./src/client/styles/variables.scss","webpack://./src/client/components/Privacy/Privacy.module.scss"],names:[],mappings:"AA2DA,MAEE,2BAAA,CACA,qBAAA,CACA,iCAAA,CACA,sBAAA,CACA,sBAAA,CACA,qBAAA,CACA,qBAAA,CACA,qBAAA,CACA,iCAAA,CACA,eAAA,CACA,0BAAA,CACA,0BAAA,CACA,2BAAA,CAEA,0CAAA,CACA,qDAAA,CACA,sBAAA,CACA,uBAAA,CACA,6BAAA,CAEA,wBAAA,CACA,wBAAA,CACA,yBAAA,CACA,yBAAA,CACA,qBAAA,CAEA,0BAAA,CACA,iBAAA,CAEA,wBAAA,CACA,0BAAA,CA5CA,gDAYF,MAoCI,sBAAA,CACA,uBAAA,CACA,6BAAA,CACA,qBAAA,CACA,2BAAA,CACA,wBAAA,CACA,yBAAA,CACA,yBAAA,CACA,0BAAA,CAAA,CAlDF,0BAMF,MAgDI,sBAAA,CACA,uBAAA,CACA,6BAAA,CACA,qBAAA,CACA,uBAAA,CACA,0BAAA,CACA,yBAAA,CACA,2BAAA,CACA,sBAAA,CAAA,CAxDJ,MAEE,2BAAA,CACA,qBAAA,CACA,iCAAA,CACA,sBAAA,CACA,sBAAA,CACA,qBAAA,CACA,qBAAA,CACA,qBAAA,CACA,iCAAA,CACA,eAAA,CACA,0BAAA,CACA,0BAAA,CACA,2BAAA,CAEA,0CAAA,CACA,qDAAA,CACA,sBAAA,CACA,uBAAA,CACA,6BAAA,CAEA,wBAAA,CACA,wBAAA,CACA,yBAAA,CACA,yBAAA,CACA,qBAAA,CAEA,0BAAA,CACA,iBAAA,CAEA,wBAAA,CACA,0BAAA,CA5CA,gDAYF,MAoCI,sBAAA,CACA,uBAAA,CACA,6BAAA,CACA,qBAAA,CACA,2BAAA,CACA,wBAAA,CACA,yBAAA,CACA,yBAAA,CACA,0BAAA,CAAA,CAlDF,0BAMF,MAgDI,sBAAA,CACA,uBAAA,CACA,6BAAA,CACA,qBAAA,CACA,uBAAA,CACA,0BAAA,CACA,yBAAA,CACA,2BAAA,CACA,sBAAA,CAAA,CChHJ,SACE,cAAA,CACA,KAAA,CACA,MAAA,CACA,UAAA,CACA,YAAA,CACA,6CAAA,CACA,YAAA,CACA,qBAAA,CACA,wDAAA,CACA,kBAAA,CACA,0BAAA,CACA,eAAA,CAEA,oBACE,eAAA,CACA,uBACE,cAAA,CACA,eAAA,CACA,kBAAA,CACA,eAAA,CAEA,0BANF,uBAOI,eAAA,CACA,eAAA,CACA,cAAA,CAAA,CAGF,yBAZF,uBAaI,cAAA,CAAA,CAKJ,mEACE,gBAAA,CACA,cAAA,CACA,iBAAA,CAEA,0BALF,mEAMI,kBAAA,CAAA,CAGF,0BATF,mEAUI,cAAA,CAAA,CAGF,yBAbF,mEAcI,cAAA,CAAA,CAKJ,sBACE,kBAAA,CAGF,sBACE,UAAA,CACA,oBAAA,CACA,4BAAA,CAGF,4BACE,uBAAA,CACA,yCAAA,CAGF,4BACE,kBAAA,CAGF,8BACE,cAAA,CACA,gCACE,uBAAA,CACA,yCAAA,CAMR,0BACE,SACE,0BAAA,CAAA,CAIJ,yBACE,SAKE,oBAAA,CACA,uBAAA,CAJA,4BACE,YAAA,CAKF,iBACE,kBAAA,CAGF,oBACE,kBAAA,CACA,uBACE,eAAA,CAEF,kCACE,kBAAA,CAGF,8BACE,cAAA,CACA,kBAAA,CAAA",sourcesContent:["// Breakpoints\n$breakpoint-mobile: 480px;\n$breakpoint-tablet: 768px;\n$breakpoint-desktop: 1200px;\n\n// Colors\n$background-color: #FAF9F5;\n$text-color: rgba(0, 0, 0, 0.85);\n$rubin-gray: #87867F;\n$rubin-slate: #1F1E1D;\n$rubin-dark-gray: #5E5D59;\n$rubin-ivory-med: #F0EEE6;\n$rubin-ivory-dark: #E8E6DC;\n$rubin-ivory: rgb(250, 249, 246);\n$rubin-riso: #5E7EDF;\n$rubin-cyan: #44A6E4;\n$rubin-clay: #D97757;\n$rubin-beige: #E8E6DC;\n$tone: #F5F2EC;\n\n// Typography\n$font-family-base: \"goudy-old-style\", serif;\n$font-family-code: 'Courier Prime', Courier, monospace;\n$font-size-base: 22px;\n$line-height-base: 1.5;\n$letter-spacing-base: 0.02em;\n\n// Padding\n$poem-padding-top: 0px;\n$poem-padding-left: 100px;\n$poem-padding: $poem-padding-top $poem-padding-left;\n\n// Sizes\n$artwork-size: 450px;\n$artwork-font-size: 7.45px;\n$nav-width: 40px;\n$mobile-nav-width: 62px;\n$mobile-menu-height: 48px;\n\n// Responsive mixins\n@mixin mobile {\n  @media (max-width: #{$breakpoint-tablet - 1px}) {\n    @content;\n  }\n}\n\n@mixin tablet {\n  @media (min-width: #{$breakpoint-tablet}) and (max-width: #{$breakpoint-desktop - 1px}) {\n    @content;\n  }\n}\n\n@mixin desktop {\n  @media (min-width: #{$breakpoint-desktop}) {\n    @content;\n  }\n}\n\n// Responsive variables\n:root {\n  // Base values (mobile first)\n  --background-color: #{$background-color};\n  --rubin-gray: #{$rubin-gray};\n  --rubin-ivory: #{$rubin-ivory};\n  --rubin-slate: #{$rubin-slate};\n  --rubin-slate: #{$rubin-slate};\n  --rubin-riso: #{$rubin-riso};\n  --rubin-cyan: #{$rubin-cyan};\n  --rubin-clay: #{$rubin-clay};\n  --text-color: #{$text-color};\n  --tone: #{$tone};\n  --rubin-dark-gray: #{$rubin-dark-gray};\n  --rubin-ivory-med: #{$rubin-ivory-med};\n  --rubin-ivory-dark: #{$rubin-ivory-dark};\n\n  --font-family-base: #{$font-family-base};\n  --font-family-code: #{$font-family-code};\n  --font-size-base: 18px;\n  --line-height-base: 1.4; // Slightly looser on mobile\n  --letter-spacing-base: 0.02em;\n\n  --artwork-font-size: 6px;\n  --poem-padding-top: 20px;\n  --poem-padding-left: 24px;\n  --poem-padding: #{$poem-padding-top} #{$poem-padding-left};\n  --artwork-size: 322px;\n\n  --breakpoint-tablet: #{$breakpoint-tablet};\n  --nav-width: #{$nav-width};\n\n  --mobile-nav-width: #{$mobile-nav-width};\n  --mobile-menu-height: #{$mobile-menu-height};\n\n  \n  @include tablet {\n    --font-size-base: 22px;\n    --line-height-base: 0.9;\n    --letter-spacing-base: 0.04em;\n    --artwork-size: 450px;\n    --artwork-font-size: 7.45px;\n    --poem-padding-top: 20px;\n    --poem-padding-left: 50px;\n    --poem-padding: #{$poem-padding-top} #{$poem-padding-left};\n    --artwork-font-size: 7.5px;\n  }\n  \n  @include desktop {\n    --font-size-base: #{$font-size-base};\n    --line-height-base: #{$line-height-base};\n    --letter-spacing-base: #{$letter-spacing-base};\n    --artwork-size: #{$artwork-size};\n    --poem-padding-top: #{$poem-padding-top};\n    --poem-padding-left: #{$poem-padding-left};\n    --poem-padding: #{$poem-padding-top} #{$poem-padding-left};\n    --artwork-font-size: #{$artwork-font-size};\n    --font-size-base: #{$font-size-base};\n  }\n}\n\n","@import \"/vercel/path0/src/client/styles/variables.scss\";\n@import '../../styles/variables.scss';\n\n.privacy {\n  position: fixed;\n  top: 0;\n  left: 0;\n  z-index: 30;\n  height: 100vh;\n  width: calc(100% - var(--poem-padding-left)*2);\n  display: flex;\n  flex-direction: column;\n  padding: var(--poem-padding-top) var(--poem-padding-left);\n  background: #FAF9F5;\n  justify-content: flex-start;\n  overflow-y: auto;\n\n  .container {\n    max-width: 700px;\n    h3 {\n      font-size: 22px;\n      font-weight: 600;\n      margin-bottom: 2rem;\n      margin-top: 3rem;\n\n      @media (max-width: 1024px) {\n        margin-top: 24px;\n        font-weight: 700;\n        font-size: 19px;\n      }\n      \n      @media (max-width: 768px) {\n        font-size: 17px;\n      }\n\n    }\n\n    p, li, a {\n      line-height: 170%;\n      font-size: 20px;\n      margin-bottom: 8px;\n      \n      @media (max-width: 1024px) {\n        margin-bottom: 12px;\n      }\n      \n      @media (max-width: 1024px) {\n        font-size: 18px;\n      }\n\n      @media (max-width: 768px) {\n        font-size: 17px;\n      }\n\n    }\n\n    p {\n      margin-bottom: 2rem;\n    }\n\n    a {\n      color: black;\n      text-decoration: none;\n      border-bottom: 1px solid black;\n    }\n\n    a:hover {\n      color: var(--rubin-gray);\n      border-bottom: 1px solid var(--rubin-gray);\n    }\n    \n    section {\n      margin-bottom: 2rem;\n    }\n    \n    .backLink {\n      margin-top: 8vh;\n      a {\n        color: var(--rubin-gray);\n        border-bottom: 1px solid var(--rubin-gray);\n      }\n    }\n  }\n}\n\n@media (max-width: 1200px) {\n  .privacy {\n    justify-content: flex-start;\n  }\n}\n\n@media (max-width: 768px) {\n  .privacy {\n\n    &::-webkit-scrollbar {\n      display: none;\n    }\n    scrollbar-width: none; /* For Firefox */\n    -ms-overflow-style: none; /* For Internet Explorer and Edge */\n\n    section {\n      margin-bottom: 1rem;\n    }\n\n    .container {\n      margin-bottom: 2rem;\n      h3 {\n        margin-top: 18px;\n      }\n      p:first-child {\n        margin-bottom: 1rem;\n      }\n      \n      .backLink {\n        margin-top: 5vh;\n        margin-bottom: 24px;\n      }\n    }\n  }\n}"],sourceRoot:""}]),o.locals={privacy:"privacy",container:"container",backLink:"backLink"};const s=o},3994:(e,n,t)=>{var i=t(4848),a=t(5338),r=t(5072),o=t.n(r),s=t(7825),c=t.n(s),l=t(7659),h=t.n(l),d=t(5056),u=t.n(d),m=t(540),f=t.n(m),p=t(1113),g=t.n(p),v=t(4541),y={};y.styleTagTransform=g(),y.setAttributes=u(),y.insert=h().bind(null,"head"),y.domAPI=c(),y.insertStyleElement=f();o()(v.A,y);v.A&&v.A.locals&&v.A.locals;var x=t(6540),A=t(6604),w={};w.styleTagTransform=g(),w.setAttributes=u(),w.insert=h().bind(null,"head"),w.domAPI=c(),w.insertStyleElement=f();o()(A.A,w);const b=A.A&&A.A.locals?A.A.locals:void 0;const M=function(){var e=(0,x.useRef)(null),n=(0,x.useRef)(null),t=(0,x.useState)({width:500,height:500}),a=t[0],r=t[1],o=(0,x.useRef)([]),s=(0,x.useRef)(0);return(0,x.useEffect)((function(){var n=function(){if(e.current){var n=e.current,t=window.innerWidth,i=window.innerHeight;r({width:t,height:i}),n.width=t,n.height=i}};return n(),window.addEventListener("resize",n),function(){window.removeEventListener("resize",n)}}),[]),(0,x.useEffect)((function(){var t=function(){var i=e.current;if(i){var r=i.getContext("2d"),o=a.width,c=a.height;r.fillStyle="#F0EEE6",r.fillRect(0,0,o,c),s.current+=.005,r.strokeStyle="rgba(80, 80, 80, 0.033)",r.lineWidth=.3;for(var l=0;l<c;l+=40){var h=5*Math.sin(s.current+.01*l);r.beginPath(),r.moveTo(0,l+h),r.lineTo(o,l+h),r.stroke()}for(var d=0;d<o;d+=40){var u=5*Math.sin(s.current+.01*d);r.beginPath(),r.moveTo(d+u,0),r.lineTo(d+u,c),r.stroke()}for(var m=0;m<30;m++){var f=m/30*c,p=40+20*Math.sin(.2*s.current+.1*m),g=.008+.004*Math.sin(.1*s.current+.05*m),v=s.current*(.5+.3*Math.sin(.1*m)),y=.8+.6*Math.sin(s.current+.2*m),x=.132+.088*Math.abs(Math.sin(.3*s.current+.15*m));r.beginPath(),r.lineWidth=y,r.strokeStyle="rgba(60, 60, 60, ".concat(x,")");for(d=0;d<o;d+=2){l=f+p*Math.sin(d*g+v);0===d?r.moveTo(d,l):r.lineTo(d,l)}r.stroke()}for(m=0;m<35;m++){var A=m/35*o*2-.5*o,w=(p=30+20*Math.cos(.25*s.current+.1*m),g=.01+.005*Math.sin(.15*s.current+.08*m),s.current*(.3+.2*Math.sin(.1*m)));y=.7+.5*Math.sin(s.current+.25*m),x=.11+.077*Math.abs(Math.sin(.2*s.current+.1*m));r.beginPath(),r.lineWidth=y,r.strokeStyle="rgba(50, 50, 50, ".concat(x,")");for(var b=0;b<=100;b++){var M=b/100;d=A+M*o,l=M*c+p*Math.sin(8*M+w);0===b?r.moveTo(d,l):r.lineTo(d,l)}r.stroke()}for(m=0;m<25;m++){var C=m/25*o;p=35+15*Math.sin(.15*s.current+.12*m),g=.009+.004*Math.cos(.12*s.current+.07*m),v=s.current*(.4+.25*Math.cos(.15*m)),y=.6+.4*Math.sin(s.current+.3*m),x=.099+.066*Math.abs(Math.sin(.25*s.current+.18*m));r.beginPath(),r.lineWidth=y,r.strokeStyle="rgba(70, 70, 70, ".concat(x,")");for(l=0;l<c;l+=2){d=C+p*Math.sin(l*g+v);0===l?r.moveTo(d,l):r.lineTo(d,l)}r.stroke()}n.current=requestAnimationFrame(t)}};return t(),function(){n.current&&(cancelAnimationFrame(n.current),n.current=null);var t=e.current;if(t){var i=t.getContext("2d");i&&i.clearRect(0,0,t.width,t.height)}o.current=[],s.current=0}}),[a]),(0,i.jsx)("div",{className:"w-full h-full bg-[#F0EEE6]",children:(0,i.jsx)("canvas",{ref:e,width:a.width,height:a.height,className:"w-full h-full"})})};var C=function(){return C=Object.assign||function(e){for(var n,t=1,i=arguments.length;t<i;t++)for(var a in n=arguments[t])Object.prototype.hasOwnProperty.call(n,a)&&(e[a]=n[a]);return e},C.apply(this,arguments)};const E=function(){var e=(0,x.useState)({width:600,height:600}),n=e[0],t=e[1],a=n.width,r=n.height,o=.25*Math.min(a,r),s=(0,x.useState)({x:a/2,y:r/2}),c=(s[0],s[1]),l=(0,x.useState)(null),h=l[0],d=l[1],u=(0,x.useRef)(null),m=(0,x.useRef)(null);(0,x.useEffect)((function(){var e=function(){if(m.current){var e=m.current.getBoundingClientRect();t({width:e.width,height:e.height}),c({x:e.width/2,y:e.height/2})}};e();var n=new ResizeObserver(e);return m.current&&n.observe(m.current),function(){n.disconnect()}}),[]),(0,x.useEffect)((function(){for(var e=[],n=0;n<6;n++){var t=n/6*Math.PI*2,i=a/2+Math.cos(t)*o*.6,s=r/2+Math.sin(t)*o*.6;e.push({x:i,y:s,targetX:i,targetY:s,baseX:i,baseY:s,phaseX:Math.random()*Math.PI*2,phaseY:Math.random()*Math.PI*2,frequencyX:.5+.3*Math.random(),frequencyY:.5+.3*Math.random()})}d(e)}),[a,r,o]);var f=(0,x.useCallback)((function(e){var n=e.currentTarget.getBoundingClientRect(),t=e.clientX-n.left,i=e.clientY-n.top;c({x:t,y:i})}),[]),p=function(e,n,t,i){void 0===i&&(i=0);for(var a=[],r=0;r<300;r++){var o=r/300*Math.PI*2+i,s=e+Math.cos(o)*(.1*t),c=n+Math.sin(o)*(.1*t),l=e+Math.cos(o)*t,h=n+Math.sin(o)*t;a.push("M".concat(s,",").concat(c," L").concat(l,",").concat(h))}return a};if((0,x.useEffect)((function(){var e=0,n=function(){e+=.016,d((function(n){return n?n.map((function(n,t){var i=.05*o,a=Math.sin(e*n.frequencyX+n.phaseX)*i,r=Math.cos(e*n.frequencyY+n.phaseY)*i,s=n.baseX+a,c=n.baseY+r;return C(C({},n),{targetX:s,targetY:c})})):null})),requestAnimationFrame(n)},t=requestAnimationFrame(n);return function(){return cancelAnimationFrame(t)}}),[o]),(0,x.useEffect)((function(){var e=function(){d((function(e){return e?e.map((function(e){var n=e.targetX-e.x,t=e.targetY-e.y;return C(C({},e),{x:e.x+.1*n,y:e.y+.1*t})})):null})),u.current=requestAnimationFrame(e)};return e(),function(){u.current&&cancelAnimationFrame(u.current)}}),[]),!h)return null;var g=h.map((function(e,n){return{cx:e.x,cy:e.y,rotation:n/6*Math.PI*2}}));return(0,i.jsx)("div",{ref:m,style:{width:"100%",height:"100%",backgroundColor:"#F0EEE6"},children:(0,i.jsx)("svg",{width:"100%",height:"100%",viewBox:"0 0 ".concat(a," ").concat(r),preserveAspectRatio:"xMidYMid meet",style:{cursor:"crosshair"},onMouseMove:f,children:g.map((function(e,n){return(0,i.jsx)("g",{children:p(e.cx,e.cy,o,e.rotation).map((function(e,n){return(0,i.jsx)("path",{d:e,stroke:"#333333",strokeWidth:"0.3",opacity:"0.7",fill:"none"},n)}))},n)}))})})};const R=function(){var e=(0,x.useRef)(null),n=(0,x.useRef)(null);return(0,x.useEffect)((function(){var t=e.current;if(t){var i=t.getContext("2d"),a=t.width,r=t.height,o=a/2,s=r/2,c=.45*a,l=0,h=function(){i.fillStyle="#F0EEE6",i.fillRect(0,0,a,r);for(var e=5;e<c;e+=3){i.beginPath(),i.strokeStyle="#000",i.lineWidth=.6;for(var t=0;t<2*Math.PI;t+=.02){var d=2*Math.sin(8*t+.1*e+l),u=o+(e+d)*Math.cos(t),m=s+(e+d)*Math.sin(t);(t>Math.PI?Math.sqrt(Math.pow(u-o,2)+Math.pow(m-(s+c/4),2))<c/4:Math.sqrt(Math.pow(u-o,2)+Math.pow(m-(s-c/4),2))>c/4)?(i.strokeStyle="rgba(0,0,0,0.3)",i.lineTo(u,m)):(i.strokeStyle="#000",i.lineTo(u,m))}i.stroke()}l+=.015,n.current=requestAnimationFrame(h)};return h(),function(){n.current&&(cancelAnimationFrame(n.current),n.current=null),t&&i&&i.clearRect(0,0,a,r)}}}),[]),(0,i.jsx)("div",{style:{width:"550px",height:"550px",backgroundColor:"#F0EEE6",display:"flex",alignItems:"center",justifyContent:"center"},children:(0,i.jsx)("canvas",{ref:e,width:550,height:550})})};const P=function(){var e=(0,x.useRef)(null);return(0,x.useEffect)((function(){var n=e.current,t=n.getContext("2d");n.width=550,n.height=550;var i,a=0,r=n.width/2,o=n.height/2,s=document.createElement("canvas"),c=s.getContext("2d");s.width=n.width,s.height=n.height;var l=function(){a+=.007,t.fillStyle="#F0EEE6",t.fillRect(0,0,n.width,n.height),c.clearRect(0,0,s.width,s.height);for(var e=200,h=r-100,d=o-100,u=h;u<h+e;u+=2)for(var m=d;m<d+e;m+=2){var f=u-r,p=m-o,g=Math.sqrt(f*f+p*p),v=Math.atan2(p,f),y=(Math.sin(.1*g-a)+Math.cos(8*v+.5*a)+Math.sin(.05*(g-100*v)+1.5*a))/3;y+=.2*(Math.random()-.5);var x=.8*Math.abs(y);c.fillStyle="rgba(0, 0, 0, ".concat(x,")"),c.fillRect(u,m,2,2)}for(var A=0;A<8;A++)t.save(),t.translate(r,o),t.rotate(A*Math.PI*2/8),A%2==1&&t.scale(1,-1),t.translate(-r,-o),t.drawImage(s,0,0),t.restore();i=requestAnimationFrame(l)};return i=requestAnimationFrame(l),function(){cancelAnimationFrame(i),t&&t.clearRect(0,0,n.width,n.height),c&&c.clearRect(0,0,s.width,s.height),s.width=0,s.height=0}}),[]),(0,i.jsx)("div",{style:{width:"550px",height:"550px",backgroundColor:"#F0EEE6"},children:(0,i.jsx)("canvas",{ref:e})})};const F=function(){var e=(0,x.useRef)(null),n=(0,x.useRef)(null);return(0,x.useEffect)((function(){var t=e.current,i=t.getContext("2d"),a=window.devicePixelRatio||1;t.width=550*a,t.height=550*a,t.style.width="".concat(550,"px"),t.style.height="".concat(550,"px"),i.scale(a,a);var r=0,o=275,s=275,c=document.createElement("canvas"),l=c.getContext("2d");c.width=t.width,c.height=t.height;var h=function(){r+=.005,i.fillStyle="#F0EEE6",i.fillRect(0,0,t.width,t.height),l.clearRect(0,0,c.width,c.height);for(var e=0;e<525;e+=1)for(var a=0;a<525;a+=1){var d=e-o,u=a-s,m=Math.sqrt(d*d+u*u),f=Math.atan2(u,d);if(f>=0&&f<=Math.PI/4&&m<250){var p=Math.min(250-m,m*Math.abs(Math.PI/4-f)*2.5),g=Math.min(1,p/20),v=(Math.sin(.1*m-2*r)+Math.cos(8*f+r)+Math.sin(.05*(m-100*f)+3*r))/3;v+=.2*(Math.random()-.5);var y=.8*Math.abs(v)*g;l.fillStyle="rgba(0, 0, 0, ".concat(y,")"),l.fillRect(e,a,1,1)}}for(var x=0;x<8;x++)i.save(),i.translate(o,s),i.rotate(x*Math.PI*2/8),x%2==1&&i.scale(1,-1),i.translate(-275,-275),i.drawImage(c,0,0),i.restore();n.current=requestAnimationFrame(h)};return h(),function(){n.current&&cancelAnimationFrame(n.current),c&&(c.width=0,c.height=0)}}),[]),(0,i.jsx)("div",{style:{margin:0,background:"#F0EEE6",overflow:"hidden",display:"flex",alignItems:"center",justifyContent:"center",height:"100%"},children:(0,i.jsx)("div",{style:{padding:"30px",position:"relative",width:"100%",height:"100%",display:"flex",alignItems:"center",justifyContent:"center"},children:(0,i.jsx)("canvas",{ref:e,style:{backgroundColor:"#F0EEE6",borderRadius:"8px"}})})})};var S=function(){return S=Object.assign||function(e){for(var n,t=1,i=arguments.length;t<i;t++)for(var a in n=arguments[t])Object.prototype.hasOwnProperty.call(n,a)&&(e[a]=n[a]);return e},S.apply(this,arguments)};const I=function(){var e=(0,x.useState)({width:600,height:600}),n=e[0],t=e[1],a=n.width,r=n.height,o=.267*Math.min(a,r),s=(0,x.useState)({x:a/2,y:r/2}),c=s[0],l=s[1],h=(0,x.useState)(null),d=h[0],u=h[1],m=(0,x.useState)(0),f=m[0],p=m[1],g=(0,x.useRef)(null),v=.08,y=(0,x.useRef)(null);(0,x.useEffect)((function(){var e=function(){if(y.current){var e=y.current.getBoundingClientRect();t({width:e.width,height:e.height}),l({x:e.width/2,y:e.height/2})}};e();var n=new ResizeObserver(e);return y.current&&n.observe(y.current),function(){n&&n.disconnect()}}),[]),(0,x.useEffect)((function(){for(var e=[],n=0;n<8;n++){var t=n/8*Math.PI*2,i=.5*o,s=a/2+Math.cos(t)*i,c=r/2+Math.sin(t)*i;e.push({x:s,y:c,targetX:s,targetY:c})}e.push({x:a/2,y:r/2,targetX:a/2,targetY:r/2}),u(e)}),[a,r,o]);var A=(0,x.useCallback)((function(e){var n=e.currentTarget.getBoundingClientRect(),t=e.clientX-n.left,i=e.clientY-n.top;l({x:t,y:i})}),[]);(0,x.useEffect)((function(){u((function(e){if(!e)return null;var n=0,t=1/0;return e.forEach((function(e,i){var a=c.x-e.x,r=c.y-e.y,o=Math.sqrt(a*a+r*r);o<t&&(t=o,n=i)})),e.map((function(e,t){var i,s;if(8===t)i=a/2,s=r/2;else{var l=t/8*Math.PI*2,h=.5*o;i=a/2+Math.cos(l)*h,s=r/2+Math.sin(l)*h}if(t===n){var d=.1*o,u=c.x-i,m=c.y-s,f=Math.min(Math.sqrt(u*u+m*m)/o,1)*d,p=Math.atan2(m,u);return S(S({},e),{targetX:i+Math.cos(p)*f,targetY:s+Math.sin(p)*f})}return S(S({},e),{targetX:i,targetY:s})}))}))}),[c,a,r,o]),(0,x.useEffect)((function(){var e=function(){p((function(e){return e+.005})),u((function(e){return e?e.map((function(e){var n=e.targetX-e.x,t=e.targetY-e.y;return S(S({},e),{x:e.x+n*v,y:e.y+t*v})})):null})),g.current=requestAnimationFrame(e)};return e(),function(){g.current&&(cancelAnimationFrame(g.current),g.current=null)}}),[v]);var w=function(e,n,t,i,a){void 0===i&&(i=0),void 0===a&&(a=!1);for(var r=[],o=0;o<12;o++)for(var s=o/12*Math.PI*2+i,c=Math.floor(400/12),l=0;l<c;l++){var h=l/c,d=s+.5*(h-.5),u=a?.1*t:.2*t,m=t*(.9-.3*Math.pow(2*Math.abs(h-.5),2)),f=d+.1*Math.sin(h*Math.PI),p=e+Math.cos(f)*u,g=n+Math.sin(f)*u,v=e+Math.cos(d)*m,y=n+Math.sin(d)*m;r.push({path:"M".concat(p,",").concat(g," L").concat(v,",").concat(y),opacity:.6-.4*Math.abs(h-.5)})}return r};return d?(0,i.jsx)("div",{ref:y,style:{width:"100%",height:"100%",backgroundColor:"#F0EEE6",display:"flex",alignItems:"center",justifyContent:"center"},children:(0,i.jsx)("svg",{width:a,height:r,viewBox:"0 0 ".concat(a," ").concat(r),preserveAspectRatio:"xMidYMid meet",style:{cursor:"crosshair"},onMouseMove:A,children:d.map((function(e,n){return(0,i.jsx)("g",{children:w(e.x,e.y,8===n?.7*o:o,8===n?f:n/8*Math.PI*2,8===n).map((function(e,t){return(0,i.jsx)("path",{d:e.path,stroke:"#333333",strokeWidth:8===n?"0.4":"0.3",opacity:e.opacity,fill:"none"},t)}))},n)}))})}):null};const T=function(){var e=(0,x.useRef)(null),n=(0,x.useRef)(null);return(0,x.useEffect)((function(){if(e.current){var t=e.current,i=n.current;if(i){var a=i.getContext("2d");if(a){i.width=550,i.height=550;var r,o=0,s=function(){(function(e){void 0===e&&(e=0),a.fillStyle="#F0EEE6",a.fillRect(0,0,i.width,i.height),a.strokeStyle="#000",a.lineWidth=.8;for(var n=i.width/2,t=i.height/2,r=.18*Math.min(i.width,i.height),o=0;o<6;o++)for(var s=o/6*Math.PI*2,c=Math.sin(.005*e+s)*(.25*r),l=Math.sin(.003*e)*(.05*r),h=n+Math.cos(s)*(r+c),d=t+Math.sin(s)*(r+c),u=.9*r,m=5;m<u;m+=u/25){a.beginPath();for(var f=0;f<=2*Math.PI;f+=.05){var p=Math.sin(6*f+.01*e+s)*(.03*m),g=2*Math.sin(.005*e+.1*m),v=h+(m+p+g+l)*Math.cos(f),y=d+(m+p+g+l)*Math.sin(f);0===f?a.moveTo(v,y):a.lineTo(v,y)}a.closePath(),a.stroke()}})(o+=1),r=requestAnimationFrame(s)};s();var c=function(){var e=t.getBoundingClientRect(),n=e.width,r=e.height,o=window.devicePixelRatio||1;i.width=n*o,i.height=r*o,a.scale(o,o),i.style.width="".concat(n,"px"),i.style.height="".concat(r,"px")};return window.addEventListener("resize",c),function(){window.removeEventListener("resize",c),cancelAnimationFrame(r)}}}}}),[]),(0,i.jsx)("div",{ref:e,style:{margin:0,background:"#F0EEE6",overflow:"hidden",display:"flex",alignItems:"center",justifyContent:"center",height:"100vh",width:"100vw",position:"relative"},children:(0,i.jsx)("canvas",{ref:n,style:{display:"block",width:"100%",height:"100%"}})})};const k=function(){var e=(0,x.useRef)(null),n=(0,x.useRef)(null);return(0,x.useEffect)((function(){var t=e.current,i=t.getContext("2d");t.width=550,t.height=550;var a=function(e){void 0===e&&(e=0),i.fillStyle="#F0EEE6",i.fillRect(0,0,t.width,t.height),i.strokeStyle="#000",i.lineWidth=.6;var r=t.width/2,o=t.height/2;[{count:1,radius:0,circleSize:60},{count:6,radius:60,circleSize:60},{count:12,radius:110,circleSize:50},{count:18,radius:160,circleSize:45},{count:24,radius:205,circleSize:40}].forEach((function(n,t){for(var a=0;a<n.count;a++)for(var s=a/n.count*Math.PI*2,c=2*Math.sin(.0015*e+.5*t),l=25e-5*e*(t%2==0?1:-1),h=r+Math.cos(s+l)*n.radius,d=o+Math.sin(s+l)*n.radius,u=3;u<n.circleSize;u+=3){i.beginPath();for(var m=0;m<=2*Math.PI;m+=.1){var f=Math.sin(8*m+.0025*e+s)*(.01*u),p=h+(u+f+c)*Math.cos(m),g=d+(u+f+c)*Math.sin(m);0===m?i.moveTo(p,g):i.lineTo(p,g)}i.closePath(),i.stroke()}}));for(var s=3;s<80;s+=3){i.beginPath();for(var c=0;c<=2*Math.PI;c+=.05){var l=Math.sin(6*c+.0025*e)*(.015*s),h=1.5*Math.sin(.002*e),d=r+(s+l+h)*Math.cos(c),u=o+(s+l+h)*Math.sin(c);0===c?i.moveTo(d,u):i.lineTo(d,u)}i.closePath(),i.stroke()}n.current=requestAnimationFrame((function(){return a(e+1)}))};return a(),function(){n.current&&cancelAnimationFrame(n.current),i&&i.clearRect(0,0,t.width,t.height),n.current=null}}),[]),(0,i.jsx)("div",{style:{display:"flex",justifyContent:"center",alignItems:"center",backgroundColor:"#F0EEE6"},children:(0,i.jsx)("canvas",{ref:e,style:{boxShadow:"0 0 20px rgba(0,0,0,0.1)"}})})};const z=function(){var e=(0,x.useRef)(null),n=(0,x.useRef)(null);return(0,x.useEffect)((function(){var t=e.current,i=t.getContext("2d");t.width=550,t.height=550;var a=0;return function e(){a+=.005,function(){i.fillStyle="#F0EEE6",i.fillRect(0,0,t.width,t.height);for(var e=t.width/2,n=t.height/2,r=0;r<25;r++){var o=10+12*r;i.beginPath();for(var s=0;s<=2*Math.PI;s+=.05){var c=o+(8*Math.sin(8*s+.75*a+.5*r)+5*Math.sin(12*s-1*a+.3*r)),l=e+Math.cos(s)*c,h=n+Math.sin(s)*c;0===s?i.moveTo(l,h):i.lineTo(l,h)}i.closePath(),i.lineWidth=1.5+.5*Math.sin(.5*r+a),i.strokeStyle="rgba(51, 51, 51, ".concat(.6-.02*r,")"),i.stroke();for(var d=0;d<60;d++){var u=d/60*Math.PI*2,m=o+5*Math.sin(8*u+1.5*a),f=e+Math.cos(u)*m,p=n+Math.sin(u)*m;i.beginPath(),i.arc(f,p,.5,0,2*Math.PI),i.fillStyle="rgba(85, 85, 85, ".concat(.2-.01*r,")"),i.fill()}}}(),n.current=requestAnimationFrame(e)}(),function(){n.current&&cancelAnimationFrame(n.current),i&&i.clearRect(0,0,t.width,t.height)}}),[]),(0,i.jsx)("div",{style:{display:"flex",justifyContent:"center",alignItems:"center",width:"550px",height:"550px",margin:"0 auto",backgroundColor:"#F0EEE6"},children:(0,i.jsx)("canvas",{ref:e})})};const j=function(){var e=(0,x.useRef)(null),n=(0,x.useRef)(0),t=(0,x.useRef)(null);return(0,x.useEffect)((function(){var i=e.current;if(i){var a=i.getContext("2d");if(a){i.width=550,i.height=550;var r=i.width/60,o=function(e){for(var n=[],t=0;t<60;t++){for(var a=[],r=10+5*Math.sin(.3*t+e),o=0;o<r;o++)a.push({y:o/r*i.height+30*Math.sin(.5*t+.3*o+e),height:5+3*Math.sin(.2*t+.4*o),width:2+2*Math.cos(.3*t)});n.push(a)}return n},s=o(0),c=o(Math.PI),l=function(){n.current+=.0025;var e=(Math.sin(n.current)+1)/2;a.fillStyle="#F0EEE6",a.fillRect(0,0,i.width,i.height);for(var o=0;o<60;o++){var h=o*r+r/2;a.beginPath(),a.strokeStyle="#666",a.lineWidth=1,a.moveTo(h,0),a.lineTo(h,i.height),a.stroke();for(var d=s[o],u=c[o],m=Math.max(d.length,u.length),f=0;f<m;f++){var p=d[f]||u[f],g=u[f]||d[f],v=p.y+(g.y-p.y)*e,y=p.height+(g.height-p.height)*e,x=p.width+(g.width-p.width)*e;a.fillStyle="#222",a.fillRect(h-x/2,v-y/2,x,y)}}t.current=requestAnimationFrame(l)};return l(),function(){t.current&&(cancelAnimationFrame(t.current),t.current=null),a&&a.clearRect(0,0,i.width,i.height),i&&(i.width=0,i.height=0),n.current=0}}}}),[]),(0,i.jsx)("div",{style:{width:"550px",height:"550px",backgroundColor:"#F0EEE6"},children:(0,i.jsx)("canvas",{ref:e,style:{display:"block"}})})};const L=function(){var e=(0,x.useRef)(null);return(0,x.useEffect)((function(){var n=e.current,t=n.getContext("2d"),i=550,a=550;n.width=i,n.height=a;var r=[];r.push({x:275,y:275,wavelength:25,phase:0,amplitude:1.5});for(var o=1;o<=2;o++)for(var s=120*o,c=0;c<6;c++){var l=c/6*Math.PI*2;r.push({x:275+Math.cos(l)*s,y:275+Math.sin(l)*s,wavelength:20+5*o,phase:c/6*Math.PI,amplitude:1-.2*o})}var h,d=0,u=2*Math.PI,m=1/300,f=0,p=function(){if(++f%2!=0)return d+=.0015,void(h=requestAnimationFrame(p));t.fillStyle="#F0EEE6",t.fillRect(0,0,i,a);for(var e=Math.floor(a/3),n=Math.floor(i/3),o=new Array(e).fill(0).map((function(){return new Array(n).fill(0)})),s=0;s<e;s++)for(var c=function(e){var n=3*e,t=3*s,i=0;r.forEach((function(e){var a=n-e.x,r=t-e.y,o=a*a+r*r,s=Math.sqrt(o);if(!(s>400)){var c=Math.exp(-s*m);i+=e.amplitude*c*Math.sin((s/e.wavelength-d)*u+e.phase)}})),o[s][e]=i},l=0;l<n;l++)c(l);t.strokeStyle="#333";[-.8,-.4,0,.4,.8].forEach((function(i,a){t.lineWidth=a%2==0?.8:.5,t.beginPath();for(var r=0;r<e-1;r++)for(var s=0;s<n-1;s++){var c=3*s,l=3*r,h=o[r][s],d=o[r][s+1],u=o[r+1][s+1],m=o[r+1][s];if(!(h>i&&d>i&&u>i&&m>i)&&!(h<=i&&d<=i&&u<=i&&m<=i)){var f=function(e,n,t){return e+t*(n-e)},p=function(e,n){return 0===n?0:e/n};switch((h>i?8:0)+(d>i?4:0)+(u>i?2:0)+(m>i?1:0)){case 1:case 14:var g=p(i-h,m-h),v=p(i-m,u-m);t.moveTo(c,f(l,l+3,g)),t.lineTo(f(c,c+3,v),l+3);break;case 2:case 13:g=p(i-m,u-m),v=p(i-u,d-u);t.moveTo(f(c,c+3,g),l+3),t.lineTo(c+3,f(l+3,l,v));break;case 3:case 12:g=p(i-h,m-h),v=p(i-d,u-d);t.moveTo(c,f(l,l+3,g)),t.lineTo(c+3,f(l,l+3,v));break;case 4:case 11:g=p(i-d,u-d),v=p(i-d,h-d);t.moveTo(c+3,f(l,l+3,g)),t.lineTo(f(c+3,c,v),l);break;case 5:case 10:g=p(i-h,m-h),v=p(i-h,d-h);t.moveTo(c,f(l,l+3,g)),t.lineTo(f(c,c+3,v),l);var y=p(i-u,d-u),x=p(i-u,m-u);t.moveTo(c+3,f(l+3,l,y)),t.lineTo(f(c+3,c,x),l+3);break;case 6:case 9:g=p(i-d,h-d),v=p(i-u,m-u);t.moveTo(f(c+3,c,g),l),t.lineTo(f(c+3,c,v),l+3);break;case 7:case 8:g=p(i-h,m-h),v=p(i-h,d-h);t.moveTo(c,f(l,l+3,g)),t.lineTo(f(c,c+3,v),l)}}}t.stroke()})),d+=.0015,h=requestAnimationFrame(p)};return p(),function(){h&&cancelAnimationFrame(h),n&&t&&(t.clearRect(0,0,n.width,n.height),n.width=0,n.height=0),r.length=0}}),[]),(0,i.jsx)("div",{style:{display:"flex",justifyContent:"center",alignItems:"center",width:"100vw",height:"100vh",backgroundColor:"#F0EEE6"},children:(0,i.jsx)("canvas",{ref:e,style:{border:"1px solid #eee",width:"550px",height:"550px"}})})};var B=t(4922),D=t(9437),H=function(e){var n=e.count,t=void 0===n?45e3:n,a=(0,x.useRef)(null),r=(0,x.useRef)(null),o=(0,x.useRef)(null),s=(0,x.useRef)(null),c=(0,x.useRef)(null),l=(0,x.useRef)(null),h=(0,x.useRef)(null),d=(0,x.useRef)(null),u=(0,x.useRef)(null),m=(0,x.useRef)([]);return(0,x.useEffect)((function(){if(a.current){var e=a.current,n=e.clientWidth,i=e.clientHeight,f=new B.Z58;o.current=f;var p=new B.ubm(75,n/i,.1,1e3);c.current=p;var g=new D.JeP({antialias:!0,powerPreference:"high-performance",alpha:!1,stencil:!1,depth:!0});s.current=g,g.setSize(n,i),g.setPixelRatio(Math.min(window.devicePixelRatio,2)),e.appendChild(g.domElement),p.position.z=5,f.background=new B.Q1f("#F0EEE6");var v=new B.BKk({uniforms:{time:{value:0},opacity:{value:.4}},vertexShader:"\n        uniform float time;\n        attribute float size;\n        attribute vec3 customColor;\n        varying vec3 vColor;\n        \n        // Optimized vertex shader - pre-compute constants and minimize operations\n        void main() {\n          vColor = customColor;\n          vec3 pos = position;\n          \n          // Calculate radial distance and angle with optimized math\n          float radius = length(pos.xz);\n          float angle = atan(pos.z, pos.x);\n          float height = pos.y;\n          \n          // Pre-compute common calculations\n          float vessel = smoothstep(0.3, 0.7, radius) * smoothstep(1.0, 0.7, radius);\n          \n          // Simplified rotation\n          angle += time * 0.08;\n          \n          // Simplified space calculation\n          float space = sin(time * 0.3 + radius * 3.0) * 0.1;\n          \n          // Combine calculations with fewer temporary variables\n          float newRadius = (radius + space) * vessel;\n          \n          vec3 newPos;\n          newPos.x = cos(angle) * newRadius;\n          newPos.z = sin(angle) * newRadius;\n          newPos.y = height * vessel - 1.2;\n          \n          // Scale for canvas size\n          newPos *= 2.75;\n          \n          vec4 mvPosition = modelViewMatrix * vec4(newPos, 1.0);\n          gl_PointSize = size * (128.0 / -mvPosition.z);\n          gl_Position = projectionMatrix * mvPosition;\n        }\n      ",fragmentShader:"\n        uniform float opacity;\n        varying vec3 vColor;\n        void main() {\n          // Optimized circle calculation with early exit\n          vec2 center = gl_PointCoord - vec2(0.5);\n          float dist = dot(center, center); // Use squared distance to avoid sqrt\n          \n          if (dist > 0.25) discard; // dist*dist > 0.5*0.5\n          \n          float alpha = (1.0 - smoothstep(0.2025, 0.25, dist)) * opacity; // Pre-computed squared values\n          gl_FragColor = vec4(vColor, alpha);\n        }\n      ",transparent:!0,depthWrite:!1,blending:B.NTi,side:B.$EB,vertexColors:!0});h.current=v;for(var y=new Float32Array(3*t),x=new Float32Array(3*t),A=new Float32Array(t),w=0,b=0;b<t;b++){var M=b/t,C=Math.pow(M,.5),E=M*Math.PI*40,R=1.8*Math.sin(M*Math.PI),P=C+.05*(Math.random()-.5),F=E+.1*(Math.random()-.5);y[w]=Math.cos(F)*P,y[w+1]=R,y[w+2]=Math.sin(F)*P;var S=.1+.1*Math.sqrt(C)+.02*Math.random();x[w]=S,x[w+1]=S,x[w+2]=S,A[b]=.2*(1-Math.abs(.5*R))+.1,w+=3}var I=new B.LoY;I.setAttribute("position",new B.THS(y,3)),I.setAttribute("customColor",new B.THS(x,3)),I.setAttribute("size",new B.THS(A,1)),I.computeBoundingBox(),I.computeBoundingSphere(),l.current=I;var T=new B.ONl(I,v);d.current=T,f.add(T);var k=new B.zD7,z=0,j=1e3/60,L=function(e){r.current=requestAnimationFrame(L);var n=e-z;if(!(n<j)){z=e-n%j;var t=k.getElapsedTime();v.uniforms.time.value=t,g.render(f,p)}};r.current=requestAnimationFrame(L);var H=null,Y=function(){H&&clearTimeout(H),H=setTimeout((function(){if(a.current){var e=a.current,n=e.clientWidth,t=e.clientHeight;p.aspect=n/t,p.updateProjectionMatrix(),g.setSize(n,t)}}),100)};window.addEventListener("resize",Y,{passive:!0});var X=null,W=new ResizeObserver((function(){X&&clearTimeout(X),X=setTimeout(Y,100)}));return u.current=W,a.current&&W.observe(a.current),m.current.push((function(){H&&clearTimeout(H),X&&clearTimeout(X)})),function(){m.current.forEach((function(e){return e()})),m.current=[],r.current&&(cancelAnimationFrame(r.current),r.current=null),window.removeEventListener("resize",Y),u.current&&(u.current.disconnect(),u.current=null),o.current&&d.current&&o.current.remove(d.current),l.current&&(l.current.dispose(),l.current=null),h.current&&(h.current.dispose(),h.current=null),s.current&&(s.current.dispose(),a.current&&s.current.domElement&&a.current.removeChild(s.current.domElement),s.current.forceContextLoss(),s.current=null),o.current=null,c.current=null,d.current=null}}}),[t]),(0,i.jsx)("div",{ref:a,style:{width:"100vw",height:"100vh"}})},Y=function(){return(0,i.jsx)(H,{})};Y.metadata={themes:"impartiality, empty potential, stillness as power",visualization:"Particles define a vessel through their movement around emptiness, showing how stillness contains infinite possibility",promptSuggestion:"1. Add subtle void variations\n2. Create empty vessel patterns\n3. Vary spatial definitions naturally\n4. Introduce gentle utility waves\n5. Make emptiness follow natural forms"};const X=Y;const W=function(){var e=(0,x.useRef)(null);return(0,x.useEffect)((function(){var n=e.current;if(n){var t=n.getContext("2d");n.width=550,n.height=550;for(var i=0,a=n.width/2,r=n.height/2,o=[],s=(n.width,n.height,4);s<n.width;s+=8)for(var c=4;c<n.height;c+=8)o.push({x:s,y:c,vx:0,vy:0,angle:Math.random()*Math.PI*2,phase:Math.random()*Math.PI*2,noiseOffset:1e3*Math.random()});var l,h=function(e){var t=n.getBoundingClientRect(),i=e.clientX-t.left,o=e.clientY-t.top;a=i,r=o};return n.addEventListener("mousemove",h),l=requestAnimationFrame((function e(){t.fillStyle="rgba(240, 238, 230, 0.15)",t.fillRect(0,0,n.width,n.height),i+=.005,o.forEach((function(e){var o=function(e,n,t){return(Math.sin(.01*e+t)+Math.sin(.01*n+.8*t)+Math.sin(.005*(e+n)+1.2*t))/3}(e.x,e.y,i),s=o*Math.PI*4,c=a-e.x,l=r-e.y,h=Math.sqrt(c*c+l*l);if(h<150){var d=.5*(1-h/150);e.vx+=c/h*d,e.vy+=l/h*d}e.vx+=.1*Math.cos(s),e.vy+=.1*Math.sin(s),e.vx*=.95,e.vy*=.95;var u=e.x+e.vx,m=e.y+e.vy;t.beginPath(),t.moveTo(e.x,e.y),t.lineTo(u,m);var f=Math.sqrt(e.vx*e.vx+e.vy*e.vy),p=Math.min(.6,5*f);t.strokeStyle="rgba(80, 80, 80, ".concat(p,")"),t.lineWidth=1,t.stroke(),t.beginPath(),t.arc(e.x,e.y,.5,0,2*Math.PI),t.fillStyle="rgba(80, 80, 80, ".concat(.5*p,")"),t.fill(),u<0&&(e.x=n.width),u>n.width&&(e.x=0),m<0&&(e.y=n.height),m>n.height&&(e.y=0),e.x+=e.x%8==4?0:.01*(4-e.x%8),e.y+=e.y%8==4?0:.01*(4-e.y%8)})),l=requestAnimationFrame(e)})),function(){n.removeEventListener("mousemove",h),cancelAnimationFrame(l),t&&t.clearRect(0,0,n.width,n.height),o.length=0}}}),[]),(0,i.jsx)("div",{style:{display:"flex",justifyContent:"center",alignItems:"center",backgroundColor:"#F0EEE6"},children:(0,i.jsx)("div",{style:{width:"550px",height:"550px",backgroundColor:"#F0EEE6"},children:(0,i.jsx)("canvas",{ref:e,style:{display:"block"}})})})};const q=function(){var e=(0,x.useRef)(null);return(0,x.useEffect)((function(){var n=e.current;if(n){var t=n.getContext("2d");n.width=550,n.height=550;var i,a=0,r=50,o=Math.ceil(n.width/r)+2,s=Math.ceil(n.height/r)+2;return i=requestAnimationFrame((function e(){t.fillStyle="#F0EEE6",t.fillRect(0,0,n.width,n.height),a+=.0025;for(var c=-1;c<o-1;c++)for(var l=-1;l<s-1;l++){var h=c*r-25,d=l*r-25,u=Math.sin(.5*c+.5*a)*r*.2,m=Math.cos(.5*l+.5*a)*r*.2;f=h+u,p=d+m,g=c+l*o+a,v=void 0,y=void 0,x=void 0,A=void 0,w=void 0,b=void 0,M=void 0,C=void 0,E=void 0,v=.2*Math.sin(1.3*g+.25*a),y=.2*Math.cos(.7*g+.15*a),x=r+v*r*.4,A=r+y*r*.4,w={x:f+Math.sin(g+a)*r*.2,y:p+Math.cos(g+a)*r*.2},b={x:f+x+Math.sin(g+1+a)*r*.2,y:p+Math.cos(g+1+a)*r*.2},M={x:f+x+Math.sin(g+2+a)*r*.2,y:p+A+Math.cos(g+2+a)*r*.2},C={x:f+Math.sin(g+3+a)*r*.2,y:p+A+Math.cos(g+3+a)*r*.2},E=Math.sin(g+.5*a)*r*.15+15,t.beginPath(),t.moveTo(w.x+E,w.y),t.lineTo(b.x-E,b.y),t.quadraticCurveTo(b.x,b.y,b.x,b.y+E),t.lineTo(M.x,M.y-E),t.quadraticCurveTo(M.x,M.y,M.x-E,M.y),t.lineTo(C.x+E,C.y),t.quadraticCurveTo(C.x,C.y,C.x,C.y-E),t.lineTo(w.x,w.y+E),t.quadraticCurveTo(w.x,w.y,w.x+E,w.y),t.closePath(),t.strokeStyle="rgba(80, 80, 80, 0.6)",t.lineWidth=1.5,t.stroke()}var f,p,g,v,y,x,A,w,b,M,C,E;i=requestAnimationFrame(e)})),function(){cancelAnimationFrame(i),i=null,n&&t&&t.clearRect(0,0,n.width,n.height)}}}),[]),(0,i.jsx)("div",{className:"flex items-center justify-center w-full h-full",style:{backgroundColor:"#F0EEE6"},children:(0,i.jsx)("canvas",{ref:e,width:550,height:550,className:"shadow-lg"})})};const O=function(){var e=(0,x.useRef)(null);return(0,x.useEffect)((function(){var n=e.current,t=n.getContext("2d"),i=550,a=550;n.width=i,n.height=a;var r=Math.floor(275),o=Math.floor(275),s=[{x:275,y:275},{x:137.5,y:137.5},{x:412.5,y:137.5},{x:137.5,y:412.5},{x:412.5,y:412.5}],c=0,l=document.createElement("canvas");l.width=r,l.height=o;var h=l.getContext("2d"),d=2*Math.PI,u=d/35,m=null,f=function(){t.fillStyle="#F0EEE6",t.fillRect(0,0,i,a);for(var e=h.createImageData(r,o),n=e.data,p=0;p<o;p++)for(var g=0;g<r;g++){for(var v=2*g+1,y=2*p+1,x=0,A=0;A<s.length;A++){var w=v-s[A].x,b=y-s[A].y,M=Math.sqrt(w*w+b*b);x+=Math.sin(M*u-c*d)}var C=4*(p*r+g),E=Math.abs(x);if(E<.6){var R=.4*(.6-E)*255;n[C]=160,n[C+1]=160,n[C+2]=160,n[C+3]=R}else n[C]=240,n[C+1]=238,n[C+2]=230,n[C+3]=255}h.putImageData(e,0,0),t.imageSmoothingEnabled=!1,t.drawImage(l,0,0,i,a),c+=8e-4,m=requestAnimationFrame(f)};return m=requestAnimationFrame(f),function(){m&&cancelAnimationFrame(m),l&&(l.width=0,l.height=0)}}),[]),(0,i.jsx)("div",{style:{display:"flex",justifyContent:"center",alignItems:"center",width:"100vw",height:"100vh",backgroundColor:"#F0EEE6"},children:(0,i.jsx)("canvas",{ref:e,style:{width:"550px",height:"550px"}})})};const G=function(){var e=(0,x.useRef)(null),n=(0,x.useRef)(0),t=(0,x.useRef)(null);return(0,x.useEffect)((function(){var i=e.current;if(i){var a=i.getContext("2d");if(a){i.width=550,i.height=550;var r=i.width/50,o=function(e){for(var n,t,a,o=[],s=0;s<50;s++){for(var c=[],l=0;l<i.height;){var h=(n=s*r,t=l,a=e,(Math.sin(.02*n+a)*Math.cos(.02*t+a)+Math.sin(.03*n-a)*Math.cos(.01*t+a)+1)/2);if(h>.5){var d=10+30*h,u=2+3*h;c.push({y:l+d/2,height:d,width:u}),l+=d+15}else l+=15}o.push(c)}return o},s=o(0),c=o(5),l=function(){n.current+=.005;var e,o=n.current%(2*Math.PI);if(o<.1*Math.PI)e=0;else if(o<.9*Math.PI){e=(o-.1*Math.PI)/(.8*Math.PI)}else if(o<1.1*Math.PI)e=1;else if(o<1.9*Math.PI){e=1-(o-1.1*Math.PI)/(.8*Math.PI)}else e=0;var h=e<.5?4*e*e*e:1-Math.pow(-2*e+2,3)/2;a.fillStyle="#F0EEE6",a.fillRect(0,0,i.width,i.height);for(var d=0;d<50;d++){var u=d*r+r/2;a.beginPath(),a.strokeStyle="#444",a.lineWidth=1,a.moveTo(u,0),a.lineTo(u,i.height),a.stroke();for(var m=s[d],f=c[d],p=Math.max(m.length,f.length),g=0;g<p;g++){var v=m[g],y=f[g];v||(v={y:y.y-100,height:0,width:0}),y||(y={y:v.y+100,height:0,width:0});var x=10*Math.sin(.3*d+.5*g+2*n.current)*(h*(1-h)*4),A=v.y+(y.y-v.y)*h+x,w=v.height+(y.height-v.height)*h,b=v.width+(y.width-v.width)*h;w>.1&&b>.1&&(a.fillStyle="#5E5D59",a.fillRect(u-b/2,A-w/2,b,w))}}t.current=requestAnimationFrame(l)};return l(),function(){t.current&&cancelAnimationFrame(t.current),a&&a.clearRect(0,0,i.width,i.height),n.current=0,t.current=null}}}}),[]),(0,i.jsx)("div",{style:{width:"550px",height:"550px",backgroundColor:"#F0EEE6"},children:(0,i.jsx)("canvas",{ref:e,style:{display:"block"}})})};const N=function(){var e=(0,x.useRef)(null),n=(0,x.useRef)(0),t=(0,x.useRef)(null),a=function(e,n,t){return(Math.sin(.01*e+t)*Math.cos(.01*n+t)+Math.sin(.015*e-t)*Math.cos(.005*n+t)+1)/2};return(0,x.useEffect)((function(){var i=e.current;if(i){var r=i.getContext("2d");if(r){i.width=550,i.height=550;var o=i.height/50,s=function(){n.current+=5e-4,r.fillStyle="#F0EEE6",r.fillRect(0,0,i.width,i.height);for(var e=0;e<50;e++){var c=e*o+o/2;r.beginPath(),r.strokeStyle="#444",r.lineWidth=1,r.moveTo(0,c),r.lineTo(i.width,c),r.stroke();for(var l=0;l<i.width;l+=8){var h=a(l,c,n.current);if(h>.5){var d=3+10*h,u=2+3*h,m=l+20*Math.sin(n.current+.0375*c)*h;r.fillStyle="#000000",r.fillRect(m-d/2,c-u/2,d,u)}}}t.current=requestAnimationFrame(s)};return s(),function(){t.current&&(cancelAnimationFrame(t.current),t.current=null),i&&r&&r.clearRect(0,0,i.width,i.height),n.current=0}}}}),[]),(0,i.jsx)("div",{style:{width:"550px",height:"550px",backgroundColor:"#F0EEE6"},children:(0,i.jsx)("canvas",{ref:e,style:{display:"block"}})})};const $=function(){var e=(0,x.useRef)(null);return(0,x.useEffect)((function(){var n=e.current;if(n){var t=n.getContext("2d");n.width=550,n.height=550;for(var i=0,a=null,r=n.height/21,o=n.width/21,s=[],c=0;c<3;c++){for(var l=[],h=.5+.3*c,d=100+50*c,u=0;u<15;u++)l.push({baseX:n.width/2,baseY:(u+1)*(n.height/16),frequency:h,amplitude:d,phase:.2*u});s.push(l)}var m=function(){t.fillStyle="#F0EEE6",t.fillRect(0,0,n.width,n.height),i+=.0045;for(var e=1;e<=20;e++){var c=e*r;Math.sin(i+.1*e);t.strokeStyle="rgba(0, 0, 0, 0.1)",t.lineWidth=1,t.beginPath(),t.moveTo(0,c),t.lineTo(n.width,c),t.stroke()}for(e=1;e<=20;e++){var l=e*o;t.strokeStyle="rgba(0, 0, 0, 0.1)",t.lineWidth=1,t.beginPath(),t.moveTo(l,0),t.lineTo(l,n.height),t.stroke()}s.forEach((function(e,n){e.forEach((function(e){var a=e.baseX+Math.sin(i*e.frequency+e.phase)*e.amplitude,o=e.baseY,s=Math.round(o/r)*r,c=3*(1+.3*Math.sin(1.5*i+n+e.phase));t.fillStyle="rgba(40, 40, 40, ".concat(.6+.3*Math.sin(1.125*i+e.phase),")"),t.beginPath(),t.arc(a,s,c,0,2*Math.PI),t.fill()}))})),a=requestAnimationFrame(m)};return m(),function(){a&&(cancelAnimationFrame(a),a=null),n&&t&&t.clearRect(0,0,n.width,n.height),s.length=0}}}),[]),(0,i.jsx)("div",{style:{width:"550px",height:"550px",backgroundColor:"#F0EEE6"},children:(0,i.jsx)("canvas",{ref:e})})};const _=function(){var e=(0,x.useRef)(null);return(0,x.useEffect)((function(){var n=e.current,t=n.getContext("2d");n.width=550,n.height=550,t.fillStyle="#F0EEE6",t.fillRect(0,0,n.width,n.height);for(var i=[],a=0,r=function(){function e(){this.reset()}return e.prototype.reset=function(){this.u=Math.random()*Math.PI*2,this.v=Math.random()*Math.PI*2,this.R=150,this.r=60+30*Math.random(),this.size=1.5*Math.random()+.5,this.opacity=.8*Math.random()+.2,this.speed=.001875*Math.random()+5e-4,this.phase=Math.random()*Math.PI*2},e.prototype.update=function(){this.u+=this.speed;var e=.0475*Math.sin(a+this.phase);this.r+=e;var t=(this.R+this.r*Math.cos(this.v))*Math.cos(this.u),i=(this.R+this.r*Math.cos(this.v))*Math.sin(this.u),r=1e3/(1e3+this.r*Math.sin(this.v));this.x=t*r+n.width/2,this.y=i*r+n.height/2,this.displaySize=this.size*r,this.displayOpacity=this.opacity*(.5+.5*Math.sin(this.u))},e.prototype.draw=function(){t.fillStyle="rgba(40, 40, 40, ".concat(this.displayOpacity,")"),t.beginPath(),t.arc(this.x,this.y,this.displaySize,0,2*Math.PI),t.fill()},e}(),o=0;o<9e3;o++)i.push(new r);var s=0,c=null;return c=requestAnimationFrame((function e(r){s||(s=r);var o=r-s;o>=50&&(t.fillStyle="rgba(240, 238, 230, 0.03)",t.fillRect(0,0,n.width,n.height),a+=.004,i.forEach((function(e){e.update(),e.draw()})),s=r-o%50),c=requestAnimationFrame(e)})),function(){c&&(cancelAnimationFrame(c),c=null),t&&t.clearRect(0,0,n.width,n.height),i.length=0}}),[]),(0,i.jsx)("div",{style:{display:"flex",justifyContent:"center",alignItems:"center",backgroundColor:"#F0EEE6",width:"100vw",height:"100vh"},children:(0,i.jsx)("div",{style:{width:"550px",height:"550px",border:"1px solid rgba(0, 0, 0, 0.1)",boxShadow:"0 0 20px rgba(0, 0, 0, 0.05)",borderRadius:"8px",overflow:"hidden"},children:(0,i.jsx)("canvas",{ref:e})})})};const V=function(){var e=(0,x.useState)(0),n=e[0],t=e[1],a=(0,x.useState)([]),r=a[0],o=a[1],s=(0,x.useRef)(),c=(0,x.useRef)(!0),l=((0,x.useRef)(null),80),h=44,d=".·•○●";(0,x.useEffect)((function(){var e=Array(h).fill().map((function(){return Array(l).fill(" ")}));o(e);var n=function(){c.current&&(t((function(e){return e+1})),s.current=requestAnimationFrame(n))};return s.current=requestAnimationFrame(n),function(){c.current=!1,s.current&&cancelAnimationFrame(s.current),s.current=null,o([])}}),[]),(0,x.useEffect)((function(){if(c.current){var e=Array(h).fill().map((function(){return Array(l).fill(" ")}));u(e,n),c.current&&o(e)}}),[n]);var u=function(e,n){for(var t=Math.floor(40),i=Math.floor(22),a=1;a<43;a++){.3+.1*Math.sin(.005*n+.1*a)>.2&&(e[a][t]="|")}m(e,t,i,n);for(var r=0;r<6;r++)for(var o=5+3*r,s=6+2*r,c=0;c<s;c++){var d=c/s*Math.PI*2,u=.2*Math.sin(.025*n+.5*r+.2*c),p=Math.round(t+Math.cos(d)*(o+u*o));a=Math.round(i+Math.sin(d)*(o+u*o));if(p>=0&&p<l&&a>=0&&a<h){var g=(Math.sin(.015*n+.4*r+.8*c)+1)/2,v=f(g);e[a][p]=v,(b=2*t-p)>=0&&b<l&&(e[a][b]=v)}if(r>0&&c%2==0){var y=.7*o,x=Math.round(t+Math.cos(d+.2)*y),A=Math.round(i+Math.sin(d+.2)*y);if(x>=0&&x<l&&A>=0&&A<h){g=(Math.sin(.025*n+.3*r+c)+1)/2,v=f(.8*g);e[A][x]=v;var w=2*t-x;w>=0&&w<l&&(e[A][w]=v)}}}for(r=0;r<40;r++){d=r/40*Math.PI,o=10+r%5*3,p=Math.round(t+Math.cos(d)*o),a=Math.round(i+Math.sin(d)*o);if(p>=0&&p<l&&a>=0&&a<h&&" "===e[a][p]){var b;g=(Math.sin(.02*n+.2*r)+1)/3;if(" "!==(v=f(g)))e[a][p]=v,(b=2*t-p)>=0&&b<l&&" "===e[a][b]&&(e[a][b]=v)}}},m=function(e,n,t,i){var a=(Math.sin(.025*i)+1)/2;e[t][n]=f(a,!0);for(var r=0;r<3;r++)for(var o=2+2*r,s=8+4*r,c=0;c<s;c++){var d=c/s*Math.PI*2,u=.2*Math.sin(.025*i+r+.1*c),m=Math.round(n+Math.cos(d)*(o+u)),p=Math.round(t+Math.sin(d)*(o+u));if(m>=0&&m<l&&p>=0&&p<h){var g=(Math.sin(.02*i+.3*r+.2*c)+1)/2;e[p][m]=f(g,!0)}}},f=function(e,n){if(void 0===n&&(n=!1),e<.1)return" ";if(n){var t=Math.min(Math.floor(5*e),4);return d[Math.max(t,2)]}t=Math.min(Math.floor(5*e),4);return d[t]};return(0,i.jsx)("div",{style:{display:"flex",justifyContent:"center",alignItems:"center",width:"550px",height:"550px",backgroundColor:"#F0EEE6",overflow:"hidden",padding:"10px",borderRadius:"8px",boxShadow:"inset 0 0 10px rgba(0,0,0,0.05)"},children:(0,i.jsx)("pre",{style:{fontFamily:"monospace",fontSize:"12px",lineHeight:"1.2em",letterSpacing:"0.1em",textAlign:"center",color:"#333",margin:0},children:r.map((function(e,n){return(0,i.jsx)("div",{children:e.map((function(e,n){var t=1;switch(e){case"●":t=.9;break;case"○":t=.7;break;case"•":t=.6;break;case"·":t=.5;break;case".":t=.4;break;case"|":t=.3;break;case" ":t=0}return(0,i.jsx)("span",{style:{color:"rgba(50, 50, 50, ".concat(t,")"),display:"inline-block",width:"0.6em"},children:e},n)}))},n)}))})})};var U=function(e){var n=e.position,t=e.rotation,i=e.scale,a=void 0===i?1:i,r=(0,x.useMemo)((function(){for(var e=[],n=1;n<=12;n++){for(var t=.3*n,i=[],a=0;a<=36;a++){var r=a/36*Math.PI*2,o=Math.cos(r)*t,s=Math.sin(r)*t,c=.1*n*Math.sin(3*r);i.push({x:o,y:s,z:c})}e.push(i)}return e}),[]);return{points:r,position:n,rotation:t,scale:a}},Z=function(){var e=(0,x.useRef)(null),n=(0,x.useRef)(),t=[U({position:[0,0,0],rotation:[0,0,0]}),U({position:[-2,2,0],rotation:[0,0,Math.PI/4],scale:.5}),U({position:[2,-2,0],rotation:[0,0,-Math.PI/4],scale:.5})];return(0,x.useEffect)((function(){var i=e.current;if(i){var a=i.getContext("2d");if(a){var r=function(){i.width=window.innerWidth,i.height=window.innerHeight};r(),window.addEventListener("resize",r);var o=60*Math.PI/180,s=function(e){var r=.001*e;a.fillStyle="#000000",a.fillRect(0,0,i.width,i.height);var c=.1*Math.sin(.2*r),l=Math.min(i.width,i.height)/10;t.forEach((function(e){var n=.05*Math.sin(.5*r),t=e.scale+n;e.points.forEach((function(n,s){a.beginPath(),a.strokeStyle="rgba(51, 51, 51, ".concat(.1+s/e.points.length*.3,")"),a.lineWidth=.5;var h=!0;n.forEach((function(n){var s=n.x*Math.cos(e.rotation[2]+.1*r+c)-n.y*Math.sin(e.rotation[2]+.1*r+c),d=n.x*Math.sin(e.rotation[2]+.1*r+c)+n.y*Math.cos(e.rotation[2]+.1*r+c),u=n.z,m=function(e,n,t){var a=e.x*n,r=e.y*n,s=e.z*n+8,c=8*Math.tan(o/2)/s;return{x:i.width/2+a*c*t,y:i.height/2-r*c*t,z:s}}({x:s+e.position[0],y:d+e.position[1],z:u+e.position[2]},t,l);h?(a.moveTo(m.x,m.y),h=!1):a.lineTo(m.x,m.y)})),a.stroke()}))})),n.current=requestAnimationFrame(s)};return n.current=requestAnimationFrame(s),function(){window.removeEventListener("resize",r),n.current&&cancelAnimationFrame(n.current)}}}}),[t]),(0,i.jsx)("canvas",{ref:e,style:{display:"block"}})};Z.metadata={themes:"Emptiness, Infinity, Source/Origin, Flow, Hidden complexity",visualization:"Concentric geometric patterns emerging from a central void, with layers of transparency suggesting infinite depth",promptSuggestion:"1. Add more intricate detail to outer layers\n2. Increase the depth effect through stronger opacity gradients\n3. Introduce subtle color variations within grayscale\n4. Create more dynamic flow patterns\n5. Experiment with asymmetrical emergence points"};var J=38,K="▁▂▃▄▅▆▇█",Q=function(){var e=(0,x.useState)([]),n=e[0],t=e[1],a=(0,x.useState)(0),r=a[0],o=a[1],s=(0,x.useRef)(null),c=(0,x.useRef)(null),l=(0,x.useRef)({x:0,y:0}),h=(0,x.useRef)(1),d=(0,x.useState)((function(){for(var e=[],n=0;n<5;n++)e.push({radius:2+3*n,phase:n/5*Math.PI*2,speed:.3+.3*Math.random(),thickness:1+Math.random()});return e}))[0];return(0,x.useEffect)((function(){var e=Array(J).fill(0).map((function(){return Array(J).fill(" ")}));t(e);var n=function(e){var n=s.current;if(n){var t=n.getBoundingClientRect(),i=(e.clientX-t.left)/t.width,a=(e.clientY-t.top)/t.height;l.current={x:2*i-1,y:-(2*a-1)}}},i=function(){if(s.current&&c.current){var e=c.current,n=Math.min(e.clientWidth,e.clientHeight);h.current=n/800,s.current.width=s.current.height=n}};return window.addEventListener("mousemove",n),window.addEventListener("resize",i),i(),function(){window.removeEventListener("mousemove",n),window.removeEventListener("resize",i)}}),[]),(0,x.useEffect)((function(){var e,n=0,i=function(a){var s=a-n;n=a;var c=s/1e3;o((function(e){return(e+.1*c)%(16*Math.PI)}));var h=Array(J).fill(0).map((function(){return Array(J).fill(" ")})),u=(l.current.x+1)*J/2,m=(1-l.current.y)*J/2;d.sort((function(e,n){return n.radius-e.radius})).forEach((function(e,n){for(var t=e.radius+e.radius*Math.sin(r*e.speed+e.phase),i=.4*J,a=Math.abs(t%i),o=1-n/d.length*.8,s=0;s<2*Math.PI;s+=.05)for(var c=-e.thickness;c<=e.thickness;c+=.5){var l=a+c,f=Math.floor(u+Math.cos(s)*l*2),p=Math.floor(m+Math.sin(s)*l);if(f>=0&&f<J&&p>=0&&p<J){var g=Math.cos(c/e.thickness*Math.PI/2)*(.7+.3*Math.cos(6*s+r*e.speed))*o,v=h[p][f];if(g>(" "===v?-1:K.indexOf(v)/7)){var y=Math.floor(8*g);y>=0&&(h[p][f]=K[Math.min(y,7)])}}}})),t(h),e=requestAnimationFrame(i)};return e=requestAnimationFrame(i),function(){cancelAnimationFrame(e)}}),[r,d]),(0,x.useEffect)((function(){var e=s.current;if(e){var t=e.getContext("2d");if(t){t.clearRect(0,0,e.width,e.height);var i=e.width/J;t.font="".concat(.8*i,"px monospace"),t.fillStyle="#333333",t.textAlign="center",t.textBaseline="middle",n.forEach((function(e,n){e.forEach((function(e,a){if(" "!==e){var r=(a+.5)*i,o=(n+.5)*i;t.fillText(e,r,o)}}))}))}}}),[n]),(0,i.jsx)("div",{ref:c,style:{width:"100%",height:"100%",display:"flex",justifyContent:"center",alignItems:"center"},children:(0,i.jsx)("canvas",{ref:s,style:{maxWidth:"100%",maxHeight:"100%",imageRendering:"crisp-edges"}})})};Q.metadata={themes:"Simplicity, Refinement, Wholeness",visualization:"Expanding rings using block elements",promptSuggestion:"1. Adjust expansion speed\n2. Change ring density\n3. Try different block characters\n4. Modify ring patterns\n5. Add pulse effects"};const ee=function(){var e=(0,x.useRef)(null);return(0,x.useEffect)((function(){var n=e.current,t=n.getContext("2d"),i=550,a=550;n.width=i,n.height=a;var r=[{x:275,y:275},{x:137.5,y:137.5},{x:412.5,y:137.5},{x:137.5,y:412.5},{x:412.5,y:412.5},{x:275,y:110},{x:275,y:440}],o=0,s=null,c=function(){t.fillStyle="#F0EEE6",t.fillRect(0,0,i,a);for(var e=t.createImageData(i,a),n=e.data,l=function(e){for(var t=function(t){var a=0;r.forEach((function(n,i){var r=t-n.x,s=e-n.y,c=Math.sqrt(r*r+s*s),l=i*Math.PI/3;a+=Math.sin(2*(c/20-o)*Math.PI+l)}));var s=a/r.length,c=Math.abs(s)<.1,l=4*(e*i+t);c?(n[l]=34,n[l+1]=34,n[l+2]=34,n[l+3]=255):(n[l]=240,n[l+1]=238,n[l+2]=230,n[l+3]=255)},a=0;a<i;a++)t(a)},h=0;h<a;h++)l(h);t.putImageData(e,0,0),o+=.015,s=requestAnimationFrame(c)};return c(),function(){s&&(cancelAnimationFrame(s),s=null),n&&t&&t.clearRect(0,0,n.width,n.height),r.length=0}}),[]),(0,i.jsx)("div",{style:{display:"flex",justifyContent:"center",alignItems:"center",width:"100vw",height:"100vh",backgroundColor:"#F0EEE6"},children:(0,i.jsx)("canvas",{ref:e,style:{border:"1px solid #eee",width:"550px",height:"550px"}})})};const ne=function(){var e=(0,x.useRef)(null);return(0,x.useEffect)((function(){var n=e.current,t=n.getContext("2d"),i=550,a=550;n.width=i,n.height=a;for(var r=[],o=0;o<4;o++)for(var s=0;s<4;s++)r.push({x:i*(o+.5)/4,y:a*(s+.5)/4,wavelength:15+10*Math.random(),phase:Math.random()*Math.PI*2});var c=0,l=null,h=function(){t.fillStyle="#F0EEE6",t.fillRect(0,0,i,a),t.strokeStyle="#333",t.lineWidth=.5;for(var e=Math.floor(275),n=Math.floor(275),o=new Array(e).fill(0).map((function(){return new Array(n).fill(0)})),s=0;s<e;s++)for(var d=function(e){var n=2*e,t=2*s,i=0;r.forEach((function(e){var a=n-e.x,r=t-e.y,o=Math.sqrt(a*a+r*r);i+=Math.sin(2*(o/e.wavelength-c)*Math.PI+e.phase)})),o[s][e]=i/r.length},u=0;u<n;u++)d(u);[-.6,-.3,0,.3,.6].forEach((function(i){t.beginPath();for(var a=0;a<e-1;a++)for(var r=0;r<n-1;r++){var s=2*r,c=2*a;switch((o[a][r]>i?8:0)+(o[a][r+1]>i?4:0)+(o[a+1][r+1]>i?2:0)+(o[a+1][r]>i?1:0)){case 1:case 14:t.moveTo(s,c+1),t.lineTo(s+1,c+2);break;case 2:case 13:t.moveTo(s+1,c+2),t.lineTo(s+2,c+1);break;case 3:case 12:t.moveTo(s,c+1),t.lineTo(s+2,c+1);break;case 4:case 11:t.moveTo(s+2,c+1),t.lineTo(s+1,c);break;case 5:case 10:t.moveTo(s,c+1),t.lineTo(s+1,c),t.moveTo(s+2,c+1),t.lineTo(s+1,c+2);break;case 6:case 9:t.moveTo(s+1,c),t.lineTo(s+1,c+2);break;case 7:case 8:t.moveTo(s,c+1),t.lineTo(s+1,c)}}t.stroke()})),c+=.003,l=requestAnimationFrame(h)};return h(),function(){l&&cancelAnimationFrame(l)}}),[]),(0,i.jsx)("div",{style:{display:"flex",justifyContent:"center",alignItems:"center",width:"100vw",height:"100vh",backgroundColor:"#F0EEE6"},children:(0,i.jsx)("canvas",{ref:e,style:{border:"1px solid #eee",width:"550px",height:"550px"}})})};const te=function(){var e=(0,x.useRef)(null);return(0,x.useEffect)((function(){var n=e.current,t=n.getContext("2d",{alpha:!1}),i=550,a=550;n.width=i,n.height=a;for(var r=Math.floor(137.5),o=Math.floor(137.5),s=[],c=0;c<5;c++){var l=c/5,h=2*l*Math.PI*1.5,d=100+150*l;s.push({x:275+Math.cos(h)*d,y:275+Math.sin(h)*d,wavelength:60+30*l,phase:h,amplitude:1})}s.push({x:275,y:275,wavelength:80,phase:0,amplitude:1.5});var u=0,m=null,f=0,p=new Float32Array(r*o),g=document.createElement("canvas");g.width=i,g.height=a;var v=g.getContext("2d",{alpha:!1}),y=function(e){f||(f=e);var n=e-f;if(n>=50){f=e-n%50,v.fillStyle="#F0EEE6",v.fillRect(0,0,i,a);for(var c=0;c<r;c++)for(var l=0;l<o;l++){for(var h=4*l,d=4*c,x=0,A=0;A<s.length;A++){var w=s[A],b=h-w.x,M=d-w.y,C=Math.sqrt(b*b+M*M),E=Math.exp(-C/250);x+=w.amplitude*E*Math.sin(2*(C/w.wavelength-u)*Math.PI+w.phase)}p[c*o+l]=x}v.strokeStyle="#333",v.lineWidth=1.2,v.beginPath();for(var R=0,P=[-.2,.2];R<P.length;R++){var F=P[R];for(c=0;c<r-1;c++)for(l=0;l<o-1;l++){var S=c*o+l,I=(h=4*l,d=4*c,p[S]>F),T=p[S+1]>F,k=p[S+o+1]>F,z=p[S+o]>F;I!==T&&(v.moveTo(h+2,d),v.lineTo(h+4,d+2)),T!==k&&(v.moveTo(h+4,d+2),v.lineTo(h+2,d+4)),k!==z&&(v.moveTo(h+2,d+4),v.lineTo(h,d+2)),z!==I&&(v.moveTo(h,d+2),v.lineTo(h+2,d))}}v.stroke(),t.drawImage(g,0,0),u+=75e-5}m=requestAnimationFrame(y)};return m=requestAnimationFrame(y),function(){m&&cancelAnimationFrame(m),t&&t.clearRect(0,0,i,a),v&&v.clearRect(0,0,i,a),s.length=0}}),[]),(0,i.jsx)("div",{style:{margin:0,background:"#F0EEE6",overflow:"hidden",display:"flex",alignItems:"center",justifyContent:"center",height:"100%",width:"100%"},children:(0,i.jsx)("div",{style:{padding:"30px",position:"relative",width:"100%",height:"100%",display:"flex",alignItems:"center",justifyContent:"center"},children:(0,i.jsx)("canvas",{ref:e,style:{display:"block",width:"550px",height:"550px"}})})})};var ie=function(e,n,t){if(t||2===arguments.length)for(var i,a=0,r=n.length;a<r;a++)!i&&a in n||(i||(i=Array.prototype.slice.call(n,0,a)),i[a]=n[a]);return e.concat(i||Array.prototype.slice.call(n))};const ae=function(){var e=(0,x.useRef)(null);return(0,x.useEffect)((function(){var n=e.current;if(n){var t=n.clientWidth,i=n.clientHeight,a=window.devicePixelRatio||1,r=new B.Z58,o=new B.ubm(75,t/i,.1,1e3),s=new D.JeP({antialias:!0});s.setPixelRatio(Math.min(a,2)),s.setSize(t,i),s.setClearColor(15789798),n.appendChild(s.domElement);var c=new B.$p8(4210752),l=new B.ZyN(8421504,1);l.position.set(1,1,1),r.add(c),r.add(l);var h,d=new B.mrM({color:3355443,transparent:!0,opacity:.6}),u=function(){for(var e=new B.LoY,n=[],t=0;t<30;t++){var i=t/30*Math.PI*2,a=(t+1)/30*Math.PI*2;n.push(1*Math.cos(i),0,1*Math.sin(i),1*Math.cos(a),0,1*Math.sin(a)),n.push(1*Math.cos(i),0,1*Math.sin(i),8*Math.cos(i),0,8*Math.sin(i))}return e.setAttribute("position",new B.qtW(n,3)),new B.DXC(e,d)}(),m=function(){for(var e=new B.LoY,n=[],t=0;t<=20;t++)for(var i=t-10,a=3*Math.sin(Math.PI*(t/20)),r=0;r<16;r++){var o=r/16*Math.PI*2,s=(r+1)/16*Math.PI*2;n.push(Math.cos(o)*a,i,Math.sin(o)*a,Math.cos(s)*a,i,Math.sin(s)*a),t<20&&n.push(Math.cos(o)*a,i,Math.sin(o)*a,Math.cos(o)*a,i+1,Math.sin(o)*a)}return e.setAttribute("position",new B.qtW(n,3)),new B.DXC(e,d)}(),f=function(){for(var e=new B.LoY,n=[],t=[[-6,0,-6],[6,0,-6],[6,0,6],[-6,0,6],[-6,8,-6],[6,8,-6],[6,8,6],[-6,8,6]],i=0;i<4;i++)n.push.apply(n,ie(ie([],t[i],!1),t[(i+1)%4],!1)),n.push.apply(n,ie(ie([],t[i+4],!1),t[(i+1)%4+4],!1)),n.push.apply(n,ie(ie([],t[i],!1),t[i+4],!1));n.push(-1,0,-6,-1,4,-6,1,0,-6,1,4,-6,-1,4,-6,1,4,-6);var a=1.5,r=function(e,t){n.push(e-a,3.5,t,e+a,3.5,t,e+a,6.5,t,e-a,6.5,t,e-a,3.5,t)};return r(-6,0),r(6,0),e.setAttribute("position",new B.qtW(n,3)),new B.DXC(e,d)}();u.position.set(-12,0,0),m.position.set(12,0,0),f.position.set(0,-4,0),r.add(u),r.add(m),r.add(f),o.position.set(15,15,25),o.lookAt(0,0,0);var p=function(){h=requestAnimationFrame(p),u.rotation.y+=.002,m.rotation.y+=.001,f.rotation.y+=5e-4,s.render(r,o)};p();var g=function(){var e=n.clientWidth,t=n.clientHeight,i=window.devicePixelRatio||1;o.aspect=e/t,o.updateProjectionMatrix(),s.setPixelRatio(Math.min(i,2)),s.setSize(e,t)};return window.addEventListener("resize",g),function(){window.removeEventListener("resize",g),cancelAnimationFrame(h),s&&(s.dispose(),n.contains(s.domElement)&&n.removeChild(s.domElement)),r.traverse((function(e){e instanceof B.eaF&&(e.geometry&&e.geometry.dispose(),e.material&&(Array.isArray(e.material)?e.material.forEach((function(e){return e.dispose()})):e.material.dispose()))}))}}}),[]),(0,i.jsx)("div",{ref:e,style:{margin:0,background:"#F0EEE6",overflow:"hidden",display:"flex",alignItems:"center",justifyContent:"center",height:"100vh",width:"100vw",position:"relative"}})};var re=function(e){var n=e.initialZoom,t=void 0===n?6:n,a=(0,x.useRef)(null),r=(0,x.useState)(t),o=r[0];r[1];return(0,x.useEffect)((function(){if(a.current){var e=null,n=null,t=null,i=null,r=[],s=o,c=function(e,n){for(var t=[],i=0;i<5;i++){var a=i/5*Math.PI*2,r=n*(1+.2*Math.sin(3*a));t.push({position:[Math.cos(a)*r,0,Math.sin(a)*r],frequency:2+Math.sin(2*a),amplitude:.3+.1*Math.cos(a),phase:3*e+a})}return t.push({position:[0,0,0],frequency:3,amplitude:.4,phase:4*e}),t},l=function(e,n,t,i){for(var a=n/t,o=[],s=function(r){o[r]=[];for(var s=r*a-n/2,c=function(t){var c=t*a-n/2,l=0;e.forEach((function(e){var n=e.position,t=n[0],a=(n[1],n[2]),r=e.frequency,o=e.amplitude,h=e.phase,d=s-t,u=c-a,m=Math.sqrt(d*d+u*u);l+=Math.sin(m*r-5*i+h)*o*Math.exp(.3*-m)})),o[r][t]=l},l=0;l<=t;l++)c(l)},c=0;c<=t;c++)s(c);var l=new B.mrM({color:3355443,transparent:!0,opacity:.4}),h=new B.YJl;r.push(h);for(c=0;c<=t;c++){for(var d=new B.LoY,u=[],m=c*a-n/2,f=0;f<=t;f++){var p=f*a-n/2;u.push(m,o[c][f],p)}d.setAttribute("position",new B.qtW(u,3));var g=new B.N1A(d,l);h.add(g)}for(f=0;f<=t;f++){for(d=new B.LoY,u=[],p=f*a-n/2,c=0;c<=t;c++){m=c*a-n/2;u.push(m,o[c][f],p)}d.setAttribute("position",new B.qtW(u,3));g=new B.N1A(d,l);h.add(g)}for(c=1;c<t;c++)for(f=1;f<t;f++){m=c*a-n/2,p=f*a-n/2;var v=o[c][f];if(Math.abs(v-(o[c-1][f]+o[c+1][f]+o[c][f-1]+o[c][f+1])/4)>.2){var y=new B.LoY,x=[m-a/2,v,p-a/2,m+a/2,v,p+a/2];y.setAttribute("position",new B.qtW(x,3));var A=new B.N1A(y,l);h.add(A);var w=new B.LoY,b=[m-a/2,v,p+a/2,m+a/2,v,p-a/2];w.setAttribute("position",new B.qtW(b,3));var M=new B.N1A(w,l);h.add(M)}}return h},h=a.current,d=h.clientWidth,u=h.clientHeight,m=window.devicePixelRatio||1;n=new B.Z58,t=new B.ubm(75,d/u,.1,1e3),(i=new D.JeP({antialias:!0})).setPixelRatio(Math.min(m,2)),i.setSize(d,u),i.setClearColor(15789798),h.appendChild(i.domElement);var f=new B.$p8(16777215,.4),p=new B.ZyN(16777215,.6);p.position.set(5,5,5);var g=new B.HiM(16777215,.4);g.position.set(-5,3,-5),n.add(f),n.add(p),n.add(g),t.position.set(0,0,s),t.lookAt(0,0,0);var v=new B.YJl;n.add(v);var y=0,x=function(){e=requestAnimationFrame(x),y+=.0013,v.children.forEach((function(e){e instanceof B.YJl&&(e.children.forEach((function(e){e.geometry&&e.geometry.dispose(),e.material&&e.material.dispose()})),v.remove(e))})),r=[];var a=c(y,1.5),o=l(a,6,32,y);v.add(o);var s=c(y+.33,.8),h=l(s,3.2,32,y+.33);h.position.set(0,1.5,0),h.rotation.set(Math.PI/6,0,Math.PI/4),v.add(h);var d=c(y+.66,.8),u=l(d,3.2,32,y+.66);u.position.set(0,-1.5,0),u.rotation.set(-Math.PI/6,0,-Math.PI/4),v.add(u),v.rotation.y=.2*Math.sin(.3*y),v.rotation.x=.1*Math.cos(.2*y),i.render(n,t)};x();var A=function(){if(a.current){var e=a.current.clientWidth,n=a.current.clientHeight,r=window.devicePixelRatio||1;t.aspect=e/n,t.updateProjectionMatrix(),i.setPixelRatio(Math.min(r,2)),i.setSize(e,n)}};return window.addEventListener("resize",A),function(){window.removeEventListener("resize",A),e&&cancelAnimationFrame(e),i&&(i.dispose(),h.contains(i.domElement)&&h.removeChild(i.domElement)),n&&n.traverse((function(e){e instanceof B.eaF?(e.geometry&&e.geometry.dispose(),e.material&&(Array.isArray(e.material)?e.material.forEach((function(e){return e.dispose()})):e.material.dispose())):e instanceof B.N1A&&(e.geometry&&e.geometry.dispose(),e.material&&e.material.dispose())}))}}}),[]),(0,i.jsx)("div",{ref:a,style:{margin:0,background:"#F0EEE6",overflow:"hidden",display:"flex",alignItems:"center",justifyContent:"center",height:"100vh",width:"100vw",position:"relative"}})};re.metadata={themes:"Interference, Pattern, Wave, Interaction, Harmony",visualization:"Precise geometric interference patterns with wave interactions",promptSuggestion:"1. Enhance interference complexity\n2. Add more wave interactions\n3. Develop stronger patterns\n4. Create clearer harmonies\n5. Increase sense of interaction"};const oe=re;const se=function(){var e=(0,x.useRef)(null);return(0,x.useEffect)((function(){var n,t=e.current,i=t.getContext("2d"),a=t.width,r=t.height,o=0,s=function(){function e(e,n,t,i){this.x=e,this.y=n,this.originX=e,this.originY=n,this.size=t,this.type=i,this.speed=.01*Math.random()+.005,this.direction=Math.random()*Math.PI*2,this.angle=Math.random()*Math.PI*2,this.connections=[],this.opacity=.4*Math.random()+.2,this.phase=Math.random()*Math.PI*2,this.flowOffset=100*Math.random(),this.pulseSpeed=.03*Math.random()+.01,this.moveRange=30*Math.random()+20,this.shapeType=Math.random()>.6?"rect":"line",this.rotation=Math.random()*Math.PI,this.rotationSpeed=.01*(Math.random()-.5)}return e.prototype.update=function(){var e=Math.sin(o*this.speed+this.phase)*this.moveRange,n=Math.cos(o*this.speed*.7+this.phase)*this.moveRange;if("heaven"===this.type)this.x=this.originX+e,this.y=this.originY+.7*n;else if("earth"===this.type)this.x=this.originX+.6*e+10*Math.sin(.02*o+this.flowOffset),this.y=this.originY+.8*n;else if("vibe"===this.type){Math.sin(.02*o);this.x=this.originX+20*Math.sin(.03*o),this.y=this.originY+20*Math.cos(.04*o),this.size=6+2*Math.sin(.05*o+this.phase)}this.rotation+=this.rotationSpeed},e.prototype.draw=function(){if("heaven"===this.type?i.fillStyle="rgba(50, 50, 50, ".concat(this.opacity,")"):"earth"===this.type?i.fillStyle="rgba(40, 40, 40, ".concat(this.opacity,")"):i.fillStyle="rgba(20, 20, 20, ".concat(this.opacity+.3,")"),i.save(),i.translate(this.x,this.y),i.rotate(this.rotation),"rect"===this.shapeType){var e=this.size*(1+.2*Math.sin(o*this.pulseSpeed));i.fillRect(-e/2,-e/2,e,e)}else{var n=2*this.size*(1+.2*Math.sin(o*this.pulseSpeed));i.fillRect(-n/2,-1,n,2)}i.restore()},e}(),c=function(){function e(e,n,t,i,a,r){this.startX=e,this.startY=n,this.endX=t,this.endY=i,this.height=a,this.speed=r,this.points=[],this.opacity=.07,this.controlPoints=[];for(var o=0;o<3;o++)this.controlPoints.push({x:this.startX+(this.endX-this.startX)*((o+1)/4),y:this.startY+(this.endY-this.startY)*((o+1)/4),offsetX:100*Math.random()-50,offsetY:100*Math.random()-50,phaseOffset:Math.random()*Math.PI*2})}return e.prototype.update=function(){for(var e=0,n=this.controlPoints;e<n.length;e++){(a=n[e]).currentOffsetX=Math.sin(o*this.speed+a.phaseOffset)*a.offsetX,a.currentOffsetY=Math.cos(o*this.speed+a.phaseOffset)*a.offsetY}this.points=[{x:this.startX,y:this.startY}];for(var t=0,i=this.controlPoints;t<i.length;t++){var a=i[t];this.points.push({x:a.x+a.currentOffsetX,y:a.y+a.currentOffsetY})}this.points.push({x:this.endX,y:this.endY})},e.prototype.draw=function(){i.strokeStyle="rgba(30, 30, 30, ".concat(this.opacity,")"),i.lineWidth=.7,i.beginPath(),i.moveTo(this.points[0].x,this.points[0].y);for(var e=1;e<this.points.length-2;e++){var n=(this.points[e].x+this.points[e+1].x)/2,t=(this.points[e].y+this.points[e+1].y)/2;i.quadraticCurveTo(this.points[e].x,this.points[e].y,n,t)}var a=this.points.length-1;i.quadraticCurveTo(this.points[a-1].x,this.points[a-1].y,this.points[a].x,this.points[a].y),i.stroke()},e}(),l=[],h=[],d=function(){for(var e=0;e<l.length;e++){l[e].connections=[];for(var n=0;n<l.length;n++)if(e!==n){var t=l[e].x-l[n].x,i=l[e].y-l[n].y,a=Math.sqrt(t*t+i*i),r=80;e!==l.length-1&&n!==l.length-1||(r=200),a<r&&l[e].connections.push({to:n,distance:a,opacity:.3*(1-a/r)})}}},u=function(){o+=.5,i.fillStyle="#F0EEE6",i.fillRect(0,0,a,r);for(var e=0,t=h;e<t.length;e++){var s=t[e];s.update(),s.draw()}for(var c=0,m=l;c<m.length;c++){m[c].update()}o%30==0&&d(),function(){for(var e=0;e<l.length;e++)for(var n=l[e],t=0,a=n.connections;t<a.length;t++){var r=a[t],s=l[r.to];i.beginPath(),i.moveTo(n.x,n.y);var c=(n.x+s.x)/2,h=(n.y+s.y)/2,d=10*Math.sin(.02*o+.1*e),u=10*Math.cos(.02*o+.1*e);i.quadraticCurveTo(c+d,h+u,s.x,s.y),i.strokeStyle="rgba(30, 30, 30, ".concat(r.opacity,")"),i.lineWidth=.5,i.stroke()}}();for(var f=0,p=l;f<p.length;f++){p[f].draw()}var g=l[l.length-1];i.save(),i.translate(g.x,g.y);for(var v=0;v<20;v++){var y=v/20*Math.PI*2,x=20+10*Math.sin(3*y+.05*o);i.strokeStyle="rgba(20, 20, 20, 0.1)",i.beginPath(),i.moveTo(0,0),i.lineTo(Math.cos(y)*x,Math.sin(y)*x),i.stroke()}i.restore(),n=requestAnimationFrame(u)};return function(){for(var e=0;e<20;e++){var n=Math.random()*a*.8+.1*a,t=Math.random()*r*.4,i=4*Math.random()+2;l.push(new s(n,t,i,"heaven"))}for(e=0;e<15;e++){var o=Math.random()*Math.random();n=a*o*.7+.1*a,t=.4*r+Math.random()*r*.2,i=4*Math.random()+2;l.push(new s(n,t,i,"earth"))}for(e=0;e<30;e++){n=Math.random()*a*.7+.15*a,t=.6*r+Math.random()*r*.35,i=4*Math.random()+2;l.push(new s(n,t,i,"earth"))}l.push(new s(.15*a,.5*r,6,"vibe"))}(),function(){for(var e=l[l.length-1],n=0;n<12;n++){var t=a*(.6+.3*Math.random()),i=Math.random()*r;h.push(new c(e.x,e.y,t,i,50+50*Math.random(),.02+.01*Math.random()))}h.push(new c(0,.5*r,a,.5*r,20,.01))}(),d(),u(),function(){cancelAnimationFrame(n),l.length=0,h.length=0,i&&i.clearRect(0,0,a,r)}}),[]),(0,i.jsx)("div",{style:{top:0,left:0,width:"100%",height:"100%",backgroundColor:"#F0EEE6",display:"flex",justifyContent:"center",alignItems:"center"},children:(0,i.jsx)("canvas",{ref:e,width:800,height:600})})};var ce={balance:function(e,n,t){var i=e-30,a=n-15,r=Math.sqrt(i*i+a*a);return Math.sin(.3*i+.5*t)*Math.cos(.3*a+.3*t)*Math.sin(.1*r-.4*t)},duality:function(e,n,t){return(e<30?Math.sin(.2*e+.3*t):0)+(e>=30?Math.cos(.2*e-.3*t):0)+Math.sin(.3*n+.2*t)},flow:function(e,n,t){var i=Math.atan2(n-15,e-30),a=Math.sqrt(Math.pow(e-30,2)+Math.pow(n-15,2));return Math.sin(3*i+.4*t)*Math.cos(.1*a-.3*t)},chaos:function(e,n,t){return.3*(Math.sin(.5*e+t)*Math.cos(.3*n-t))+.3*(Math.sin(.4*n+.5*t)*Math.cos(.2*e+.7*t))+.4*Math.sin(.2*(e+n)+.8*t)}};const le=function(){var e=(0,x.useState)(0),n=e[0],t=e[1],a=(0,x.useState)(0),r=a[0],o=a[1],s=(0,x.useState)({x:0,y:0}),c=s[0],l=s[1],h=(0,x.useState)(!1),d=h[0],u=h[1],m=(0,x.useRef)(null),f=["balance","duality","flow","chaos"];(0,x.useEffect)((function(){var e,n=function(){t((function(e){return(e+1)%2880})),e=requestAnimationFrame(n)};return e=requestAnimationFrame(n),function(){e&&cancelAnimationFrame(e)}}),[12]);return(0,i.jsx)("div",{style:{margin:0,padding:0,background:"#F0EEE6",overflow:"hidden",display:"flex",alignItems:"center",justifyContent:"center",height:"100%"},onMouseMove:function(e){l({x:e.clientX,y:e.clientY})},onMouseDown:function(){u(!0)},onMouseUp:function(){u(!1)},onClick:function(){o((function(e){return(e+1)%f.length}))},ref:m,children:(0,i.jsx)("pre",{style:{fontFamily:"monospace",fontSize:"12px",lineHeight:"1",letterSpacing:"0.1em",color:"#333",userSelect:"none",cursor:"pointer",margin:0,padding:0},children:function(){for(var e=n*Math.PI/720,t=ce[f[r]],i="",a=0;a<35;a++){for(var o=0;o<60;o++){var s=t(o,a,e);if(d&&m.current){var l=m.current.getBoundingClientRect(),h=o-(c.x-l.left)/l.width*60,u=a-(c.y-l.top)/l.height*35,p=Math.sqrt(h*h+u*u);s+=.8*(Math.exp(.1*-p)*Math.sin(2*e))}i+=s>.8?"█":s>.5?"▓":s>.2?"▒":s>-.2?"░":s>-.5?"·":" "}i+="\n"}return i}()})})};var he=function(){var e=(0,x.useRef)(null);return(0,x.useEffect)((function(){if(e.current){var n=e.current,t=300,i={x:0,y:0,z:0},a={x:0,y:0,z:0},r=0,o=0,s=0,c=!1,l=n.clientWidth,h=n.clientHeight,d=window.devicePixelRatio||1,u=new B.Z58,m=new B.ubm(75,l/h,.1,1e3),f=new D.JeP({antialias:!0});f.setPixelRatio(Math.min(d,2)),f.setSize(l,h),f.setClearColor(15789798),n.appendChild(f.domElement);var p=function(){for(var e=new B.LoY,n=new Float32Array(1800),t=new Float32Array(1800),i=[],a=0;a<299;a++)i.push(2*a,2*a+1,2*(a+1)),i.push(2*a+1,2*(a+1)+1,2*(a+1));return i.push(598,599,0),i.push(599,1,0),e.setAttribute("position",new B.THS(n,3)),e.setAttribute("normal",new B.THS(t,3)),e.setIndex(i),e}(),g=new B.V9B({color:0,wireframe:!0,wireframeLinewidth:1,side:B.$EB}),v=new B.eaF(p,g),y=new B.YJl;y.add(v),u.add(y),m.position.set(0,0,4),m.lookAt(0,0,0);var x=function(e){var n=f.domElement.getBoundingClientRect(),t=(e.clientX-n.left)/n.width*2-1,a=-(e.clientY-n.top)/n.height*2+1;i.x=3*t,i.y=3*a,c=!0},A=function(e){var n=f.domElement.getBoundingClientRect(),t=(e.clientX-n.left)/n.width*2-1,a=-(e.clientY-n.top)/n.height*2+1;i.x=3*t,i.y=3*a},w=function(){c=!1};f.domElement.addEventListener("mousedown",x),f.domElement.addEventListener("mousemove",A),f.domElement.addEventListener("mouseup",w),f.domElement.addEventListener("mouseleave",w);var b,M=0,C=function(){b=requestAnimationFrame(C),M+=.0015,c&&(a.x+=.3*(i.x-a.x),a.y+=.3*(i.y-a.y),a.z+=.3*(i.z-a.z));for(var e=v.geometry.attributes.position.array,n=v.geometry.attributes.normal.array,l=0;l<t;l++){var h=l/299,d=h*Math.PI*2,p=0;if(c){var g=Math.abs(h-0);p=Math.max(0,1-3*g),p=Math.pow(p,2)}var y=(1.875+.125*Math.sin(.5*M+h*Math.PI*2))*(1-.3*p),x=Math.cos(d)*y,A=Math.sin(d)*y,w=0;if(p>0){var E=(a.x-x)*p,R=(a.y-A)*p,P=(a.z-w)*p;x+=E+r*p,A+=R+o*p,w+=P+s*p}var F=h*Math.PI+.2*M*(1-.5*p),S=.1*Math.sin(3*d+2*M),I=Math.cos(d+Math.PI/2)*S,T=Math.sin(d+Math.PI/2)*S,k=.2*Math.sin(2*d+M),z=-Math.sin(d),j=Math.cos(d),L=Math.cos(d+F),B=Math.sin(d+F),D=Math.sin(F),H=j*D-0*B,Y=0*L-z*D,X=z*B-j*L,W=.5*(1+.1*Math.sin(2*F)),q=6*l;e[q]=x+I+H*W,e[q+1]=A+T+Y*W,e[q+2]=w+k+X*W,e[q+3]=x+I-H*W,e[q+4]=A+T-Y*W,e[q+5]=w+k-X*W,n[q]=L,n[q+1]=B,n[q+2]=D,n[q+3]=-L,n[q+4]=-B,n[q+5]=-D}v.geometry.attributes.position.needsUpdate=!0,v.geometry.attributes.normal.needsUpdate=!0,f.render(u,m)};C();var E=function(){var e=n.clientWidth,t=n.clientHeight,i=window.devicePixelRatio||1;m.aspect=e/t,m.updateProjectionMatrix(),f.setPixelRatio(Math.min(i,2)),f.setSize(e,t)};return window.addEventListener("resize",E),function(){window.removeEventListener("resize",E),f.domElement.removeEventListener("mousedown",x),f.domElement.removeEventListener("mousemove",A),f.domElement.removeEventListener("mouseup",w),f.domElement.removeEventListener("mouseleave",w),cancelAnimationFrame(b),f&&(f.dispose(),n.contains(f.domElement)&&n.removeChild(f.domElement)),p&&p.dispose(),g&&g.dispose()}}}),[]),(0,i.jsx)("div",{ref:e,style:{margin:0,background:"#F0EEE6",overflow:"hidden",display:"flex",alignItems:"center",justifyContent:"center",height:"100vh",width:"100vw",position:"relative"}})};he.metadata={themes:"Structure, Simplicity, Flow",visualization:"Wireframe Möbius strip with smooth animation",promptSuggestion:"1. Adjust the twist rate\n2. Change the ribbon dimensions\n3. Alter the motion dynamics\n4. Modify the wireframe density\n5. Change the rotation speed"};const de=he;var ue="/\\|",me=90,fe=function(){var e=(0,x.useRef)(null);return(0,x.useEffect)((function(){var n=e.current;if(n){var t=n.getContext("2d");if(t){var i=0,a=null,r=function(){t.clearRect(0,0,n.width,n.height),t.fillStyle="#F0EEE6",t.fillRect(0,0,n.width,n.height),t.font="".concat(15,"px monospace"),t.textAlign="center",t.textBaseline="middle",t.fillStyle="#333333";for(var e=0;e<me;e++)for(var o=0;o<me;o++){var s=Math.sin((o+e)/4+i),c=Math.cos((o-e)/4-.7*i),l=Math.abs(o-45)/45,h=Math.abs(e-45)/45,d=Math.max(l,h);if(d<.85){var u=(.6*s+.4*c)*(1-d/.85),m=" ";if(u>.3?m=ue[0]:u<-.3?m=ue[1]:Math.abs(u)<.1&&(m=ue[2])," "!==m){var f=n.width/2+10.5*(o-45),p=n.height/2+15*(e-45);t.fillText(m,f,p)}}}i+=.0075,a=requestAnimationFrame(r)};return a=requestAnimationFrame(r),function(){a&&cancelAnimationFrame(a),t&&t.clearRect(0,0,n.width,n.height)}}}}),[]),(0,i.jsx)("div",{style:{margin:0,background:"#F0EEE6",overflow:"hidden",display:"flex",alignItems:"center",justifyContent:"center",height:"100%",width:"100%"},children:(0,i.jsx)("canvas",{ref:e,width:800,height:800,style:{maxWidth:"100%",maxHeight:"100%",objectFit:"contain"}})})};fe.metadata={themes:"creation from unity, harmony of opposites, cyclical emergence",visualization:"ASCII patterns emerging from simple elements combining and recombining",promptSuggestion:"1. Adjust wave frequency\n2. Change pattern density\n3. Try different slash characters\n4. Modify interference angles\n5. Add phase variations"};const pe=fe;var ge=function(){return ge=Object.assign||function(e){for(var n,t=1,i=arguments.length;t<i;t++)for(var a in n=arguments[t])Object.prototype.hasOwnProperty.call(n,a)&&(e[a]=n[a]);return e},ge.apply(this,arguments)};const ve=function(){var e=x.useRef(null);return x.useEffect((function(){var n=e.current;if(n){var t=n.getContext("2d"),i=function(){var e=window.devicePixelRatio||1;n.style.width=window.innerWidth+"px",n.style.height=window.innerHeight+"px",n.width=window.innerWidth*e,n.height=window.innerHeight*e,t.scale(e,e)};i(),window.addEventListener("resize",i);var a,r={X:"F-[[X]+X]+F[+FX]-X",F:"FF"},o=function(e,n,t){void 0===t&&(t=r);for(var i=e,a=0;a<n;a++){for(var o="",s=0;s<i.length;s++){var c=i[s];o+=t[c]||c}i=o}return i},s=[{config:{iterations:3,angleDelta:Math.PI/7,length:25},system:o("X",3)},{config:{iterations:4,angleDelta:Math.PI/7,length:21},system:o("X",4)},{config:{iterations:5,angleDelta:Math.PI/8,length:17},system:o("X",5,{X:"F-[[X]+X]+F[+FX]-X+[F+X][-X]",F:"FF"})}],c=0,l=0,h=0,d=!1,u=!1,m=function(e,n){var i;void 0===n&&(n=1);for(var a=e.config,r=e.system,o=a.angleDelta,s=a.length,l=window.innerWidth/2,h=.98*window.innerHeight,d=-Math.PI/2+.4*Math.sin(.2*c),u=[],m=0;m<r.length;m++){switch(r[m]){case"F":var f=l+s*Math.cos(d),p=h+s*Math.sin(d),g=(.2-m/r.length*.08)*n;t.strokeStyle="rgba(51, 51, 51, ".concat(g,")"),t.lineWidth=2,t.beginPath(),t.moveTo(l,h),t.lineTo(f,p),t.stroke(),l=f,h=p;break;case"+":d+=o+.05*Math.sin(.2*c+.01*m);break;case"-":d-=o+.05*Math.sin(.2*c+.01*m);break;case"[":u.push({x:l,y:h,angle:d});break;case"]":var v=u.pop();if(v){l=(i=ge(ge({},v),{baseAngle:v.angle})).x,h=i.y,d=i.baseAngle;var y=.1*n;y>.01&&(t.fillStyle="rgba(51, 51, 51, ".concat(y,")"),t.beginPath(),t.arc(l,h,2,0,2*Math.PI),t.fill())}}}},f=function(){if(t.fillStyle="#F0EEE6",t.fillRect(0,0,window.innerWidth,window.innerHeight),c+=.008,u)m(s[s.length-1]);else if(d)if((h+=.02)>=1)d=!1,h=0,++l>=s.length-1&&(u=!0,l=s.length-1);else{var e=1-Math.pow(1-h,3);m(s[l],1-e);var n=Math.min(l+1,s.length-1);m(s[n],e)}else m(s[l]),c%1<.016&&(d=!0);a=requestAnimationFrame(f)};return f(),function(){window.removeEventListener("resize",i),cancelAnimationFrame(a),t&&t.clearRect(0,0,n.width,n.height),s.length=0}}}),[]),(0,i.jsx)("div",{className:"w-full h-screen bg-[#F0EEE6] overflow-hidden",children:(0,i.jsx)("canvas",{ref:e,style:{background:"#F0EEE6",top:0,left:0,width:"100%",height:"100%"}})})};const ye=function(){var e=(0,x.useRef)(null),n=(0,x.useRef)(null);return(0,x.useEffect)((function(){var t=e.current,i=t.getContext("2d"),a=t.width/2,r=t.height/2,o=0,s={x:a,y:r},c=0,l=0,h=[{x:a,y:r,timestamp:0}],d=function(){i.fillStyle="#F0EEE6",i.fillRect(0,0,t.width,t.height)},u=function(){o+=.00375,d(),l+=.0375,c+=.025;var e,t,m=a+Math.cos(c)*l,f=r+Math.sin(c)*l;s.x=m,s.y=f,h.push({x:s.x,y:s.y,timestamp:o}),l>260&&(l=0,h=[{x:a,y:r,timestamp:o}],s={x:a,y:r},c=0);for(var p=1;p<h.length;p++){var g=h[p-1],v=h[p],y=Math.min((o-g.timestamp)/10,1),x=Math.floor(170+-85*y),A=.3+.7*y;i.beginPath(),i.moveTo(g.x,g.y),i.lineTo(v.x,v.y),i.strokeStyle="rgba(".concat(x,", ").concat(x,", ").concat(x,", ").concat(A,")"),i.lineWidth=1,i.stroke()}e=s.x,t=s.y,i.beginPath(),i.arc(e,t,3,0,2*Math.PI),i.fillStyle="rgba(80, 80, 80, 1)",i.fill(),n.current=requestAnimationFrame(u)};return d(),u(),function(){n.current&&cancelAnimationFrame(n.current),i&&i.clearRect(0,0,t.width,t.height),h.length=0}}),[]),(0,i.jsx)("div",{className:"flex flex-col items-center justify-center bg-[#F0EEE6] p-4 w-full h-full",children:(0,i.jsx)("div",{className:"w-[550px] h-[550px] border border-gray-200 shadow-md",children:(0,i.jsx)("canvas",{ref:e,width:550,height:550,className:"bg-[#F0EEE6]"})})})};var xe=function(e,n,t){if(t||2===arguments.length)for(var i,a=0,r=n.length;a<r;a++)!i&&a in n||(i||(i=Array.prototype.slice.call(n,0,a)),i[a]=n[a]);return e.concat(i||Array.prototype.slice.call(n))};const Ae=function(){var e=(0,x.useRef)(null);return(0,x.useEffect)((function(){var n=e.current;if(n){var t=n.getContext("2d");if(t){n.width=550,n.height=550;var i,a=function(){function e(){this.segments=[],this.segmentCount=30,this.width=100,this.initialize()}return e.prototype.initialize=function(){for(var e=0;e<this.segmentCount;e++)this.segments.push({x:0,y:0,angle:0,width:this.width,height:20,depth:0})},e.prototype.update=function(e){for(var t=n.width/2,i=n.height/2,a=0;a<this.segments.length;a++){var r=a/(this.segments.length-1),o=this.segments[a],s=.25*e,c=r*Math.PI*6+s,l=Math.sin(.01*s+r*Math.PI*4),h=Math.cos(.00375*s+r*Math.PI*3),d=120+60*l;o.x=t+Math.cos(c)*d,o.y=i+Math.sin(c)*d+30*h,o.angle=c+l*Math.PI*.5,o.width=this.width*(1+.3*l),o.depth=Math.sin(c+.15*e)}},e.prototype.draw=function(e){e.lineWidth=1.5,e.setLineDash([2,2]);for(var n=xe([],this.segments,!0).sort((function(e,n){return Math.abs(e.depth-n.depth)>.1?e.depth-n.depth:0})),t=0;t<n.length-1;t++){var i=n[t],a=n[t+1];e.save(),e.beginPath();var r=Math.cos(i.angle),o=Math.sin(i.angle),s=Math.cos(a.angle),c=Math.sin(a.angle),l={x:i.x-o*i.width/2,y:i.y+r*i.width/2},h={x:i.x+o*i.width/2,y:i.y-r*i.width/2},d={x:a.x+c*a.width/2,y:a.y-s*a.width/2},u={x:a.x-c*a.width/2,y:a.y+s*a.width/2};e.moveTo(l.x,l.y),e.lineTo(h.x,h.y),e.lineTo(d.x,d.y),e.lineTo(u.x,u.y),e.closePath();var m=.6+.4*(.5*(i.depth+1));e.strokeStyle="rgba(40, 40, 40, ".concat(m,")"),e.stroke(),e.beginPath(),e.moveTo(l.x,l.y),e.lineTo(d.x,d.y),e.stroke(),e.beginPath(),e.moveTo(h.x,h.y),e.lineTo(u.x,u.y),e.stroke(),e.beginPath(),e.moveTo(i.x,i.y),e.lineTo(a.x,a.y),e.strokeStyle="rgba(80, 80, 80, ".concat(.7*m,")"),e.stroke(),e.restore()}for(t=0;t<n.length;t++){var f=n[t],p=Math.cos(f.angle),g=Math.sin(f.angle);l={x:f.x-g*f.width/2,y:f.y+p*f.width/2},h={x:f.x+g*f.width/2,y:f.y-p*f.width/2},m=.7+.3*(.5*(f.depth+1));e.beginPath(),e.arc(l.x,l.y,2,0,2*Math.PI),e.fillStyle="rgba(0, 0, 0, ".concat(m,")"),e.fill(),e.beginPath(),e.arc(h.x,h.y,2,0,2*Math.PI),e.fillStyle="rgba(0, 0, 0, ".concat(m,")"),e.fill(),e.beginPath(),e.arc(f.x,f.y,1.5,0,2*Math.PI),e.fillStyle="rgba(40, 40, 40, ".concat(m,")"),e.fill()}e.setLineDash([])},e}(),r=new a,o=0;return function e(){t.fillStyle="#F0EEE6",t.fillRect(0,0,n.width,n.height),o+=.00125,r.update(o),r.draw(t),i=requestAnimationFrame(e)}(),function(){cancelAnimationFrame(i),t.clearRect(0,0,n.width,n.height),r.segments=[],o=0}}}}),[]),(0,i.jsx)("div",{style:{margin:0,background:"#F0EEE6",overflow:"hidden",display:"flex",alignItems:"center",justifyContent:"center",height:"100vh",width:"100vw",position:"relative"},children:(0,i.jsx)("canvas",{ref:e,style:{display:"block",width:"550px",height:"550px"}})})};const we=function(){var e=(0,x.useState)(60),n=e[0],t=e[1],a=(0,x.useRef)(0),r="░▒▓█";(0,x.useEffect)((function(){var e=null,n=0,i=function(r){e=requestAnimationFrame(i);var o=r-n;o>=100&&(n=r-o%100,t((function(e){var n=e+1;return a.current=27.5+22.5*Math.sin(.002*n),n})))};return e=requestAnimationFrame(i),function(){e&&cancelAnimationFrame(e),t(60),a.current=0}}),[]);var o=function(){for(var e=[],t=a.current,i=0;i<30;i++){for(var o="",s=0;s<60;s++){var c=(s-30)/30,l=(i-15)/15,h=Math.sqrt(c*c+l*l),d=Math.sin(3*h-.002*n);d=((d+=.3*Math.cos(.1*s-.1*i+8e-4*n))+1)/2,d*=t/100;var u=Math.floor(4*d),m=r[Math.min(Math.max(0,u),3)];t<30&&Math.random()>t/30&&(m=" "),o+=m}e.push(o)}return e}();return(0,i.jsx)("div",{style:{backgroundColor:"#F0EEE6",height:"100vh",width:"100vw",display:"flex",alignItems:"center",justifyContent:"center"},children:(0,i.jsx)("pre",{className:"font-mono text-gray-800 text-center text-xs md:text-sm whitespace-pre",children:o.join("\n")})})};const be=function(){var e=(0,x.useRef)(null);return(0,x.useEffect)((function(){var n=e.current;if(n){var t=n.getContext("2d");n.width=550,n.height=550;var i,a=0,r=[],o=function(){function e(e,n){this.x=e,this.y=n,this.radius=40*Math.random()+10,this.lines=Math.floor(150*Math.random()+100),this.growth=.125*(.5*Math.random()+.5),this.maxLength=100*Math.random()+50,this.age=0,this.lifespan=4*(200*Math.random()+100),this.opacity=0}return e.prototype.update=function(){this.age++,this.age<.2*this.lifespan?this.opacity=Math.min(1,this.age/(.2*this.lifespan)):this.age>.8*this.lifespan?this.opacity=Math.max(0,1-(this.age-.8*this.lifespan)/(.2*this.lifespan)):this.opacity=1},e.prototype.draw=function(){var e=Math.min(this.maxLength,this.age*this.growth),n=1+.0025*this.age;t.save(),t.globalAlpha=.5*this.opacity,t.translate(this.x,this.y);for(var i=0;i<this.lines;i++){var a=i/this.lines*Math.PI*2,r=e;t.beginPath(),t.moveTo(0,0),t.lineTo(Math.cos(a)*r,Math.sin(a)*r),t.strokeStyle="#333",t.lineWidth=.3,t.stroke()}t.beginPath(),t.arc(0,0,n,0,2*Math.PI),t.fillStyle="rgba(0, 0, 0, 0.8)",t.fill(),t.restore()},e.prototype.isDead=function(){return this.age>this.lifespan},e}();return function e(){t.fillStyle="#F0EEE6",t.fillRect(0,0,n.width,n.height),++a%40==0&&function(){if(r.length<50){var e=void 0,t=void 0;if(r.length>0&&Math.random()<.8){var i=r[Math.floor(Math.random()*r.length)],a=Math.random()*Math.PI*2,s=50*Math.random()+20;e=i.x+Math.cos(a)*s,t=i.y+Math.sin(a)*s}else{var c=.5*Math.random()+.5;e=n.width/2+(Math.random()-.5)*n.width*c,t=n.height/2+(Math.random()-.5)*n.height*c}e=Math.max(50,Math.min(n.width-50,e)),t=Math.max(50,Math.min(n.height-50,t)),r.push(new o(e,t))}}(),(r=r.filter((function(e){return!e.isDead()}))).forEach((function(e){e.update(),e.draw()})),i=requestAnimationFrame(e)}(),function(){cancelAnimationFrame(i),t.clearRect(0,0,n.width,n.height),r=[],a=0}}}),[]),(0,i.jsx)("div",{style:{width:"100vw",height:"100vh",display:"flex",justifyContent:"center",alignItems:"center",backgroundColor:"#F0EEE6"},children:(0,i.jsx)("div",{style:{width:"550px",height:"550px",backgroundColor:"#F0EEE6",borderRadius:"2px",boxShadow:"0 0 10px rgba(0,0,0,0.1)"},children:(0,i.jsx)("canvas",{ref:e})})})};var Me=32,Ce="·•○◎●",Ee=function(){var e=(0,x.useRef)(null),n=(0,x.useRef)([]),t=(0,x.useRef)(0),a=(0,x.useRef)(0);return(0,x.useEffect)((function(){var i=e.current;if(i){var r=i.getContext("2d");if(r){n.current=[];for(var o=0;o<40;o++)n.current.push({x:Math.random()*Me,y:Math.random()*Me,speed:.5+1.5*Math.random(),char:Math.floor(5*Math.random()),phase:Math.random()*Math.PI*2});var s=function(){i.width=512,i.height=512,r.fillStyle="#F0EEE6",r.fillRect(0,0,i.width,i.height)};s(),window.addEventListener("resize",s);var c=0,l=function(e){var o=c?e-c:0;c=e;var s=o||16.67;t.current+=.01*s,r.fillStyle="#F0EEE6",r.fillRect(0,0,i.width,i.height),n.current.forEach((function(e){e.y=(e.y+.5*e.speed)%Me,e.x=(e.x+.1*Math.sin(t.current+e.phase)+Me)%Me;var n=Math.sin(e.y/Me*Math.PI),a=Math.sin((1-e.y/Me)*Math.PI),o=Math.max(n,a);if(o>.2){var s=Math.floor(e.x),c=Math.floor(e.y),l=15*(s-16)+i.width/2,h=15*(16-c)+i.height/2;if(l>=0&&l<i.width&&h>=0&&h<i.height){var d=Math.min(4,Math.floor(o*(e.char+1)));r.fillStyle="rgba(51, 51, 51, ".concat(o,")"),r.font="12px monospace",r.textAlign="center",r.textBaseline="middle",r.fillText(Ce[d],l,h)}}})),a.current=requestAnimationFrame((function(e){return l(e)}))};return a.current=requestAnimationFrame((function(e){return l(e)})),function(){cancelAnimationFrame(a.current),window.removeEventListener("resize",s),r&&r.clearRect(0,0,i.width,i.height),n.current=[],t.current=0}}}}),[]),(0,i.jsx)("div",{style:{width:"100%",height:"100%",display:"flex",alignItems:"center",justifyContent:"center",backgroundColor:"#F0EEE6"},children:(0,i.jsx)("canvas",{ref:e,style:{maxWidth:"512px",maxHeight:"512px",width:"100%",height:"100%"}})})};Ee.metadata={themes:"Transcendence, Detachment, Fulfillment",visualization:"Rising and falling particles using dots and circles",promptSuggestion:"1. Adjust particle rise/fall speed\n2. Change particle density\n3. Try different dot characters\n4. Modify the flow patterns\n5. Add subtle size variations"};var Re=function(){var e=(0,x.useRef)(null),n=(0,x.useRef)(null),t=(0,x.useRef)(null),a=(0,x.useRef)(null),r=(0,x.useRef)(null),o=(0,x.useRef)(null),s=function(e,n,t,i,a,r){var o=new B.YJl,c=new B.LoY,l=[new B.Pq0(-e,-e,0),new B.Pq0(e,-e,0),new B.Pq0(e,e,0),new B.Pq0(-e,e,0),new B.Pq0(-e,-e,0)];c.setFromPoints(l);var h=new B.mrM({color:3355443,transparent:!0,opacity:i}),d=new B.N1A(c,h);o.add(d);var u=new B.LoY;u.setFromPoints([new B.Pq0(-e,-e,0),new B.Pq0(e,e,0)]);var m=new B.N1A(u,h);o.add(m);var f=new B.LoY;f.setFromPoints([new B.Pq0(e,-e,0),new B.Pq0(-e,e,0)]);var p=new B.N1A(f,h);if(o.add(p),n<t){var g=.5*e,v=.7*e;[{position:[v,v,0],rotation:[0,0,Math.PI/4]},{position:[-v,v,0],rotation:[0,0,-Math.PI/4]},{position:[v,-v,0],rotation:[0,0,-Math.PI/4]},{position:[-v,-v,0],rotation:[0,0,Math.PI/4]}].forEach((function(e){var a=s(g,n+1,t,i-.1,e.position,e.rotation);a.position.set(e.position[0],e.position[1],e.position[2]),a.rotation.set(e.rotation[0],e.rotation[1],e.rotation[2]),o.add(a)}))}return o.position.set(a[0],a[1],a[2]),o.rotation.set(r[0],r[1],r[2]),o};return(0,x.useEffect)((function(){if(e.current){var i=e.current;n.current=new B.Z58,n.current.background=new B.Q1f(15789798),t.current=new B.ubm(50,i.clientWidth/i.clientHeight,.1,1e3),t.current.position.z=10,a.current=new D.JeP({antialias:!0}),a.current.setSize(i.clientWidth,i.clientHeight),i.appendChild(a.current.domElement),r.current=new B.YJl,n.current.add(r.current);var c=s(2,0,3,.6,[0,0,0],[0,0,0]);r.current.add(c);var l=s(1,0,2,.6,[0,0,1],[Math.PI/6,Math.PI/6,Math.PI/4]);r.current.add(l);var h=s(.8,0,2,.6,[0,0,-1],[-Math.PI/6,-Math.PI/6,-Math.PI/4]);r.current.add(h);var d=new B.$p8(16777215,.4);n.current.add(d);var u=new B.ZyN(16777215,.6);u.position.set(5,5,5),n.current.add(u);var m=new B.HiM(16777215,.4);m.position.set(-5,3,-5),n.current.add(m);var f=new B.zD7,p=function(){if(o.current=requestAnimationFrame(p),r.current){var e=f.getElapsedTime();r.current.rotation.y=.2*Math.sin(.15*e),r.current.rotation.x=.1*Math.cos(.1*e)}a.current.render(n.current,t.current)};p();var g=function(){i&&t.current&&a.current&&(t.current.aspect=i.clientWidth/i.clientHeight,t.current.updateProjectionMatrix(),a.current.setSize(i.clientWidth,i.clientHeight))};return window.addEventListener("resize",g),function(){window.removeEventListener("resize",g),o.current&&cancelAnimationFrame(o.current),a.current&&(a.current.dispose(),i.contains(a.current.domElement)&&i.removeChild(a.current.domElement)),n.current&&n.current.traverse((function(e){(e instanceof B.eaF||e instanceof B.N1A)&&(e.geometry&&e.geometry.dispose(),e.material&&(Array.isArray(e.material)?e.material.forEach((function(e){return e.dispose()})):e.material.dispose()))}))}}}),[]),(0,i.jsx)("div",{ref:e,style:{width:"100vw",height:"100vh",margin:0,background:"#F0EEE6",overflow:"hidden",position:"relative"}})};Re.metadata={themes:"abandonment of the way, rise of rigid rules, loss of intuition",visualization:"Geometric structures that reveal how natural flow becomes constrained by rigid patterns",promptSuggestion:"1. Enhance recursive depth\n2. Add more structural layers\n3. Develop clearer pattern progression\n4. Create stronger unity between elements\n5. Increase sense of order"};const Pe=Re;var Fe=function(e,n,t,i){return new(t||(t=Promise))((function(a,r){function o(e){try{c(i.next(e))}catch(e){r(e)}}function s(e){try{c(i.throw(e))}catch(e){r(e)}}function c(e){var n;e.done?a(e.value):(n=e.value,n instanceof t?n:new t((function(e){e(n)}))).then(o,s)}c((i=i.apply(e,n||[])).next())}))},Se=function(e,n){var t,i,a,r={label:0,sent:function(){if(1&a[0])throw a[1];return a[1]},trys:[],ops:[]},o=Object.create(("function"==typeof Iterator?Iterator:Object).prototype);return o.next=s(0),o.throw=s(1),o.return=s(2),"function"==typeof Symbol&&(o[Symbol.iterator]=function(){return this}),o;function s(s){return function(c){return function(s){if(t)throw new TypeError("Generator is already executing.");for(;o&&(o=0,s[0]&&(r=0)),r;)try{if(t=1,i&&(a=2&s[0]?i.return:s[0]?i.throw||((a=i.return)&&a.call(i),0):i.next)&&!(a=a.call(i,s[1])).done)return a;switch(i=0,a&&(s=[2&s[0],a.value]),s[0]){case 0:case 1:a=s;break;case 4:return r.label++,{value:s[1],done:!1};case 5:r.label++,i=s[1],s=[0];continue;case 7:s=r.ops.pop(),r.trys.pop();continue;default:if(!(a=r.trys,(a=a.length>0&&a[a.length-1])||6!==s[0]&&2!==s[0])){r=0;continue}if(3===s[0]&&(!a||s[1]>a[0]&&s[1]<a[3])){r.label=s[1];break}if(6===s[0]&&r.label<a[1]){r.label=a[1],a=s;break}if(a&&r.label<a[2]){r.label=a[2],r.ops.push(s);break}a[2]&&r.ops.pop(),r.trys.pop();continue}s=n.call(e,r)}catch(e){s=[6,e],i=0}finally{t=a=0}if(5&s[0])throw s[1];return{value:s[0]?s[1]:void 0,done:!0}}([s,c])}}},Ie=function(){var e=(0,x.useRef)(null),n=(0,x.useRef)(null),t=(0,x.useRef)(null),a=(0,x.useRef)(null),r=(0,x.useRef)(null),o=(0,x.useRef)(null),s=(0,x.useRef)([0,0]),c=(0,x.useRef)([]),l=(0,x.useRef)([]),h=(0,x.useMemo)((function(){return[{count:150,size:.025,color:"#333333",opacity:.8,radius:1.2,connectionRadius:.4,speed:.2,mouseInfluence:2},{count:80,size:.02,color:"#333333",opacity:.6,radius:2,connectionRadius:.6,speed:.15,mouseInfluence:1.5}]}),[]),d=(0,x.useMemo)((function(){return[{count:200,radius:.9,color:"#333333",thickness:.002,speed:.5,amplitude:.15,opacity:.5,mouseInfluence:2},{count:180,radius:1.6,color:"#333333",thickness:.002,speed:.3,amplitude:.2,opacity:.4,mouseInfluence:1.5}]}),[]);(0,x.useEffect)((function(){if(e.current){return Fe(void 0,void 0,void 0,(function(){var e;return Se(this,(function(n){return window.THREE?(m(),[2]):((e=document.createElement("script")).src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js",e.async=!0,e.onload=function(){m()},document.body.appendChild(e),[2,function(){document.body.removeChild(e)}])}))})),function(){o.current&&cancelAnimationFrame(o.current),a.current&&e.current&&(e.current.removeChild(a.current.domElement),a.current.dispose())}}}),[]);var u=function(){if(!window.THREE)return null;var e=document.createElement("canvas");e.width=64,e.height=64;var n=e.getContext("2d");n.beginPath(),n.arc(32,32,32,0,2*Math.PI),n.fillStyle="#F0EEE6",n.fill();var t=n.createRadialGradient(32,32,0,32,32,32);t.addColorStop(0,"rgba(255, 255, 255, 1)"),t.addColorStop(.7,"rgba(255, 255, 255, 0.9)"),t.addColorStop(.95,"rgba(255, 255, 255, 0.1)"),t.addColorStop(1,"rgba(255, 255, 255, 0)"),n.fillStyle=t,n.fillRect(0,0,64,64);var i=new window.THREE.CanvasTexture(e);return i.needsUpdate=!0,i},m=function(){if(e.current&&window.THREE){var i=window.THREE;n.current=new i.Scene,n.current.background=new i.Color("#F0EEE6"),t.current=new i.PerspectiveCamera(75,e.current.clientWidth/e.current.clientHeight,.1,1e3),t.current.position.z=5,a.current=new i.WebGLRenderer({antialias:!0,alpha:!1}),a.current.setSize(e.current.clientWidth,e.current.clientHeight),a.current.setClearColor(15789798),a.current.domElement.style.backgroundColor="#F0EEE6",a.current.domElement.style.display="block",e.current.appendChild(a.current.domElement),r.current=new i.Group,n.current.add(r.current),h.forEach((function(e,n){for(var t=e.count,a=e.size,o=e.color,s=e.opacity,l=e.radius,h=e.connectionRadius,d=e.speed,m=new Float32Array(3*t),f=new Float32Array(3*t),p=[],g=0;g<t;g++){var v=g/t*Math.PI*2,y=l*Math.sqrt(Math.random());m[3*g]=Math.cos(v)*y,m[3*g+1]=Math.sin(v)*y,m[3*g+2]=0,f[3*g]=(Math.random()-.5)*d,f[3*g+1]=(Math.random()-.5)*d,f[3*g+2]=0;for(var x=0;x<g;x++){var A=m[3*g]-m[3*x],w=m[3*g+1]-m[3*x+1];Math.sqrt(A*A+w*w)<h&&p.push([g,x])}}c.current.push({positions:m,velocities:f,connections:p});var b=new i.BufferGeometry;b.setAttribute("position",new i.BufferAttribute(m,3));var M=new i.PointsMaterial({size:100*a,color:new i.Color(o),opacity:s,transparent:!0,depthWrite:!1,blending:i.AdditiveBlending,sizeAttenuation:!0,map:u(),alphaTest:.1}),C=new i.Points(b,M);r.current.add(C);var E=new Float32Array(6*p.length);p.forEach((function(e,n){var t=e[0],i=e[1],a=6*n;E[a]=m[3*t],E[a+1]=m[3*t+1],E[a+2]=0,E[a+3]=m[3*i],E[a+4]=m[3*i+1],E[a+5]=0}));var R=new i.BufferGeometry;R.setAttribute("position",new i.BufferAttribute(E,3));var P=new i.LineBasicMaterial({color:new i.Color(o),opacity:.5*s,transparent:!0}),F=new i.LineSegments(R,P);r.current.add(F)})),d.forEach((function(e,n){for(var t=e.count,a=e.radius,o=e.color,s=(e.thickness,e.speed,e.amplitude,e.opacity),c=new Float32Array(3*t),h=0;h<t;h++){var d=h/t*Math.PI*2;c[3*h]=Math.cos(d)*a,c[3*h+1]=Math.sin(d)*a,c[3*h+2]=0}var u=new i.BufferGeometry;u.setAttribute("position",new i.BufferAttribute(c,3));var m=new i.LineBasicMaterial({color:new i.Color(o),opacity:s,transparent:!0}),f=new i.Line(u,m);r.current.add(f),l.current.push({line:f,config:e})}));var m=new i.AmbientLight(16777215,.4);n.current.add(m);var f=new i.DirectionalLight(16777215,.6);f.position.set(5,5,5),n.current.add(f);var p=function(n){var t,i=null===(t=e.current)||void 0===t?void 0:t.getBoundingClientRect();i&&(s.current=[(n.clientX-i.left)/i.width*2-1,-(n.clientY-i.top)/i.height*2+1])};e.current.addEventListener("mousemove",p);Date.now();var g=new i.Clock,v=function(){o.current=requestAnimationFrame(v);var e=g.getElapsedTime();if(r.current){r.current.rotation.z=.1*Math.sin(.2*e);var i=1+.05*Math.sin(.5*e)+(s.current[1]+1)/2*.1;r.current.scale.setScalar(i)}var d=(s.current[0]+1)/2,u=(s.current[1]+1)/2;h.forEach((function(n,t){var i=c.current[t];if(i){var a=r.current.children.find((function(e,n){return n===2*t})),o=r.current.children.find((function(e,n){return n===2*t+1}));if(a&&a.geometry){for(var s=a.geometry.attributes.position.array,l=o.geometry.attributes.position.array,h=0;h<n.count;h++){var m=3*h,f=s[m],p=s[m+1],g=Math.sqrt(f*f+p*p),v=n.speed*(1+d*n.mouseInfluence)*(1-g/(n.radius*(1+u*n.mouseInfluence*.5))),y=e*v,x=f*Math.cos(y)-p*Math.sin(y),A=f*Math.sin(y)+p*Math.cos(y),w=1+u*n.mouseInfluence*.5;s[m]=x*w+i.velocities[m]*Math.sin(e+h),s[m+1]=A*w+i.velocities[m+1]*Math.cos(e+h);var b=Math.sqrt(Math.pow(s[m],2)+Math.pow(s[m+1],2));b>n.radius*w&&(s[m]*=n.radius*w/b,s[m+1]*=n.radius*w/b)}i.connections.forEach((function(e,n){var t=e[0],i=e[1],a=6*n;l[a]=s[3*t],l[a+1]=s[3*t+1],l[a+2]=0,l[a+3]=s[3*i],l[a+4]=s[3*i+1],l[a+5]=0})),a.geometry.attributes.position.needsUpdate=!0,o.geometry.attributes.position.needsUpdate=!0}}})),l.current.forEach((function(n,t){var i=n.line,a=n.config;if(i.geometry){for(var r=i.geometry.attributes.position.array,o=a.speed*(1+d*a.mouseInfluence),s=a.amplitude*(1+u*a.mouseInfluence),c=0;c<a.count;c++){var l=c/a.count*Math.PI*2,h=Math.sin(4*l+e*o)*s;r[3*c]=Math.cos(l)*(a.radius+h),r[3*c+1]=Math.sin(l)*(a.radius+h)}i.geometry.attributes.position.needsUpdate=!0}})),a.current.autoClear=!0,a.current.setClearColor(15789798,1),a.current.clear(!0,!0,!0),a.current.render(n.current,t.current)};v();var y=function(){if(e.current&&t.current&&a.current){var n=e.current.clientWidth,i=e.current.clientHeight;t.current.aspect=n/i,t.current.updateProjectionMatrix(),a.current.setSize(n,i)}};return window.addEventListener("resize",y),function(){var n;window.removeEventListener("resize",y),null===(n=e.current)||void 0===n||n.removeEventListener("mousemove",p)}}};return(0,i.jsx)("div",{ref:e,style:{width:"100%",height:"100%",margin:0,padding:0,backgroundColor:"#F0EEE6",overflow:"hidden",position:"relative"}})};Ie.metadata={themes:"Unity, Wholeness, Interconnection",visualization:"Interconnected particle networks with unified flowing motion",promptSuggestion:"1. How do the connected particles express unity?\n2. What aspects of the force fields demonstrate wholeness?\n3. How does the network structure show interconnection?\n4. In what ways do the different layers work together?\n5. How does the unified motion create a sense of oneness?"};const Te=function(){var e=(0,x.useRef)(null),n=(0,x.useRef)(null);return(0,x.useEffect)((function(){var t=e.current,i=t.getContext("2d",{alpha:!1}),a=550,r=550;t.width=a,t.height=r;for(var o=Math.floor(110),s=Math.floor(110),c=[{x:275,y:275,wavelength:60,phase:0,amplitude:1.5}],l=0;l<6;l++){var h=l/6*Math.PI*2;c.push({x:275+150*Math.cos(h),y:275+150*Math.sin(h),wavelength:50,phase:h,amplitude:.8})}var d=0,u=new Float32Array(o*s),m=document.createElement("canvas");m.width=a,m.height=r;var f=m.getContext("2d",{alpha:!1}),p=function(){f.fillStyle="#F0EEE6",f.fillRect(0,0,a,r);for(var e=0;e<o;e++)for(var t=0;t<s;t++){for(var l=5*t,h=5*e,g=0,v=0;v<c.length;v++){var y=c[v],x=l-y.x,A=h-y.y,w=Math.sqrt(x*x+A*A),b=Math.max(0,1-w/400);g+=y.amplitude*b*Math.sin(2*(w/y.wavelength-d)*Math.PI+y.phase)}u[e*s+t]=g}f.strokeStyle="#333",f.lineWidth=1,f.beginPath();for(e=0;e<o-1;e++)for(t=0;t<s-1;t++){var M=e*s+t,C=(l=5*t,h=5*e,u[M]>0),E=u[M+1]>0,R=u[M+s+1]>0,P=u[M+s]>0;C&&!E&&(f.moveTo(l+2.5,h),f.lineTo(l+5,h+2.5)),E&&!R&&(f.moveTo(l+5,h+2.5),f.lineTo(l+2.5,h+5)),R&&!P&&(f.moveTo(l+2.5,h+5),f.lineTo(l,h+2.5)),P&&!C&&(f.moveTo(l,h+2.5),f.lineTo(l+2.5,h))}f.stroke(),i.drawImage(m,0,0),d+=625e-6,n.current=requestAnimationFrame(p)};return p(),function(){n.current&&(cancelAnimationFrame(n.current),n.current=null),t&&i&&i.clearRect(0,0,t.width,t.height),m&&f&&(f.clearRect(0,0,m.width,m.height),m.width=0,m.height=0),u.fill(0)}}),[]),(0,i.jsx)("div",{style:{display:"flex",justifyContent:"center",alignItems:"center",width:"100vw",height:"100vh",backgroundColor:"#F0EEE6"},children:(0,i.jsx)("canvas",{ref:e,style:{width:"550px",height:"550px"}})})};var ke=function(e,n,t){if(t||2===arguments.length)for(var i,a=0,r=n.length;a<r;a++)!i&&a in n||(i||(i=Array.prototype.slice.call(n,0,a)),i[a]=n[a]);return e.concat(i||Array.prototype.slice.call(n))},ze=36,je={slash:"/",backslash:"\\",dot:"•",joint:"◦"},Le=function(){var e=(0,x.useRef)(null),n=(0,x.useRef)({x:0,y:0}),t=(0,x.useRef)([]),a=(0,x.useRef)([]),r=(0,x.useRef)(0);return(0,x.useEffect)((function(){if(e.current){a.current=Array(ze).fill(0).map((function(){return Array(ze).fill(" ")}));for(var i=[],o=0;o<4;o++)for(var s=3+2*o,c=18-6*s/2,l=9+6*o,h=0;h<s;h++){var d=c+6*h;i.push({x:d,y:l,baseX:d,baseY:l,vx:0,vy:0,connections:[]})}ke([],i,!0).forEach((function(e){i.push({x:e.x,y:ze-e.y,baseX:e.x,baseY:ze-e.y,vx:0,vy:0,connections:[]})})),i.forEach((function(e,n){i.forEach((function(t,i){if(n<i){var a=t.x-e.x,r=t.y-e.y;Math.sqrt(a*a+r*r)<9&&(e.connections.push(i),t.connections.push(n))}}))})),t.current=i;var u=document.createElement("script");return u.src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js",u.onload=function(){if(e.current){var i=new window.THREE.Scene,a=new window.THREE.OrthographicCamera(-10,10,10,-10,1,1e3),o=new window.THREE.WebGLRenderer({antialias:!0});o.setSize(e.current.offsetWidth,e.current.offsetHeight),o.setClearColor(15789798),e.current.appendChild(o.domElement),a.position.z=5;var s=new Map;Object.values(je).forEach((function(e){!function(e){if(s.has(e))return s.get(e);var n=document.createElement("canvas"),t=n.getContext("2d");n.width=64,n.height=64,t.font="32px monospace",t.fillStyle="#333333",t.textAlign="center",t.textBaseline="middle",t.fillText(e,32,32);var i=new window.THREE.CanvasTexture(n);i.minFilter=window.THREE.LinearFilter,i.needsUpdate=!0,s.set(e,i)}(e)}));var c=new Map,l=function(e){if(c.has(e))return c.get(e);var n=new window.THREE.SpriteMaterial({map:s.get(e),transparent:!0,color:3355443});return c.set(e,n),n};Object.values(je).forEach((function(e){l(e)}));var h=[],d=new Set,u=function(){for(var e,n=0;n<h.length;n++)if(!d.has(n))return e=h[n],d.add(n),e.visible=!0,{sprite:e,index:n};(e=new window.THREE.Sprite).scale.set(.25,.25,1),i.add(e);var t=h.length;return h.push(e),d.add(t),{sprite:e,index:t}},m=function(e){r.current+=.5*e;var i=Array(ze).fill(0).map((function(){return Array(ze).fill(" ")})),a=n.current.x*ze,o=(1-n.current.y)*ze;t.current.forEach((function(n){var t=a-n.x,i=o-n.y,r=Math.max(1,Math.sqrt(t*t+i*i)),s=5*Math.max(0,(10-r)/10);isNaN(t/r)||isNaN(i/r)||(n.vx+=t/r*s*e,n.vy+=i/r*s*e);var c=n.baseX-n.x,l=n.baseY-n.y;n.vx+=c*e*2,n.vy+=l*e*2,n.x+=n.vx*e*30,n.y+=n.vy*e*30,n.vx*=.95,n.vy*=.95})),t.current.forEach((function(e,n){var a=Math.floor(e.x),r=Math.floor(e.y);a>=0&&a<ze&&r>=0&&r<ze&&(i[r][a]=je.joint,e.connections.forEach((function(n){for(var a=t.current[n],r=a.x-e.x,o=a.y-e.y,s=2*Math.max(Math.abs(r),Math.abs(o)),c=1;c<s-1;c++){var l=c/s,h=Math.floor(e.x+r*l),d=Math.floor(e.y+o*l);if(h>=0&&h<ze&&d>=0&&d<ze){var u=Math.atan2(o,r);i[d][h]=Math.abs(u)<Math.PI/4||Math.abs(u)>3*Math.PI/4?je.backslash:je.slash}}})))}));for(var s=[],c=0;c<ze;c++)for(var m=0;m<ze;m++){var f=i[c][m];if(" "!==f){var p=u(),g=p.sprite,v=p.index;g.material=l(f),g.position.set(.25*(m-18),.25*(18-c),0),s.push(v)}}d.forEach((function(e){s.includes(e)||function(e){e<h.length&&(h[e].visible=!1,d.delete(e))}(e)}))},f=function(t){var i=e.current.getBoundingClientRect(),a=(t.clientX-i.left)/i.width,r=(t.clientY-i.top)/i.height;n.current={x:2*a-1,y:2*r-1}},p=function(){if(e.current){var n=e.current.offsetWidth,t=e.current.offsetHeight;o.setSize(n,t),a.left=n/t*-10,a.right=n/t*10,a.updateProjectionMatrix()}};window.addEventListener("resize",p),e.current.addEventListener("mousemove",f),p();var g=0,v=1e3/60,y=function(e){requestAnimationFrame(y);var n=e-g;if(n>v){g=e-n%v;var t=Math.min(n/1e3,.1);m(t),o.render(i,a)}};return y(0),function(){var n;window.removeEventListener("resize",p),null===(n=e.current)||void 0===n||n.removeEventListener("mousemove",f),o.dispose(),s.forEach((function(e){return e.dispose()})),c.forEach((function(e){return e.dispose()})),h.forEach((function(e){i.remove(e),e.material&&e.material.dispose(),e.geometry&&e.geometry.dispose()})),e.current&&o.domElement.parentNode===e.current&&e.current.removeChild(o.domElement)}}},document.body.appendChild(u),function(){document.body.removeChild(u)}}}),[]),(0,i.jsx)("div",{ref:e,style:{margin:0,background:"#F0EEE6",overflow:"hidden",display:"flex",alignItems:"center",justifyContent:"center",height:"100%",width:"100%",position:"absolute",top:0,left:0}})};Le.metadata={themes:"Power, Restraint, Balance",visualization:"Elastic web using connecting characters",promptSuggestion:"1. Adjust tension strength\n2. Change web density\n3. Try different connector characters\n4. Modify spring behavior\n5. Add oscillation effects"};const Be=function(){var e=(0,x.useRef)(null);return(0,x.useEffect)((function(){if(e.current){var n=e.current,t=new B.Z58,i=new B.ubm(75,n.clientWidth/n.clientHeight,.1,1e3),a=new D.JeP({antialias:!0});a.setSize(n.clientWidth,n.clientHeight),a.setPixelRatio(Math.min(window.devicePixelRatio,2)),a.setClearColor(15789798),n.appendChild(a.domElement);var r=new B.$p8(4210752),o=new B.ZyN(8421504,1);o.position.set(1,1,1),t.add(r),t.add(o);var s,c=new B.mrM({color:3355443,transparent:!0,opacity:.6}),l=function(e){for(var n=new B.LoY,t=[],i=0;i<e.length-8;i++)t.push(e[i].x,e[i].y,e[i].z),t.push(e[i+8].x,e[i+8].y,e[i+8].z);for(i=0;i<e.length;i+=8)for(var a=0;a<8;a++){var r=e[i+a],o=e[i+(a+1)%8];t.push(r.x,r.y,r.z),t.push(o.x,o.y,o.z)}for(i=0;i<e.length-8;i+=8)for(a=0;a<8;a++){r=e[i+a],o=e[i+8+(a+1)%8];Math.random()<.3&&(t.push(r.x,r.y,r.z),t.push(o.x,o.y,o.z))}return n.setAttribute("position",new B.qtW(t,3)),new B.DXC(n,c)}(function(){for(var e=[],n=0;n<=20;n++)for(var t=n-10,i=5*Math.sin(Math.PI*(n/20)),a=0;a<8;a++){var r=a/8*Math.PI*2,o=Math.cos(r)*i,s=Math.sin(r)*i;e.push(new B.Pq0(o,t,s))}return e}()),h=function(){for(var e=new B.YJl,n=0;n<5;n++){for(var t=3+2*Math.random(),i=new B.LoY,a=[],r=0;r<6;r++){var o=r/6*Math.PI*2,s=(r+1)/6*Math.PI*2;a.push(0,0,0),a.push(Math.cos(o)*t,0,Math.sin(o)*t),a.push(Math.cos(s)*t,0,Math.sin(s)*t)}i.setAttribute("position",new B.qtW(a,3));var l=new B.DXC(i,c);l.position.y=4*(n-2.5),l.position.x=3*Math.sin(1.2*n),l.position.z=3*Math.cos(1.2*n),l.rotation.x=.2*Math.random(),l.rotation.z=.2*Math.random(),e.add(l)}return e}();t.add(l),t.add(h),i.position.z=20,i.position.y=5,i.lookAt(0,0,0);var d=function(){s=requestAnimationFrame(d),l.rotation.y+=.002,h.rotation.y+=.001,a.render(t,i)};d();var u=function(){var e=n.clientWidth,t=n.clientHeight;i.aspect=e/t,i.updateProjectionMatrix(),a.setPixelRatio(Math.min(window.devicePixelRatio,2)),a.setSize(e,t)};return window.addEventListener("resize",u),function(){window.removeEventListener("resize",u),cancelAnimationFrame(s),a&&(a.dispose(),n.contains(a.domElement)&&n.removeChild(a.domElement)),t.traverse((function(e){(e instanceof B.eaF||e instanceof B.DXC)&&(e.geometry&&e.geometry.dispose(),e.material&&(Array.isArray(e.material)?e.material.forEach((function(e){return e.dispose()})):e.material.dispose()))}))}}}),[]),(0,i.jsx)("div",{ref:e,style:{margin:0,background:"#F0EEE6",overflow:"hidden",display:"flex",alignItems:"center",justifyContent:"center",height:"100vh",width:"100vw",position:"relative"}})};var De=60,He="⠁⠂⠄⠈⠐⠠⡀⢀⠃⠅⠘⠨⠊⠋⠌⠍⠎⠏",Ye=function(){var e=(0,x.useRef)(null),n=(0,x.useRef)({x:0,y:0}),t=(0,x.useRef)([]),a=(0,x.useRef)([]),r=(0,x.useRef)(0);return(0,x.useEffect)((function(){if(e.current){a.current=Array(De).fill(0).map((function(){return Array(De).fill(null)}));for(var i=[],o=0;o<3;o++)i.push({x:De*(.25+.5*Math.random()),y:De*(.25+.5*Math.random()),frequency:.2+.3*Math.random(),amplitude:.5+.5*Math.random(),phase:Math.random()*Math.PI*2,speed:.5+.5*Math.random()});t.current=i;var s=document.createElement("canvas");s.width=e.current.offsetWidth,s.height=e.current.offsetHeight;var c=s.getContext("2d");e.current.appendChild(s),c.font="12px monospace",c.textAlign="center",c.textBaseline="middle";var l=function(e){var t=s.getBoundingClientRect(),i=(e.clientX-t.left)/t.width,a=(e.clientY-t.top)/t.height;n.current={x:2*i-1,y:2*a-1}},h=function(){e.current&&(s.width=e.current.offsetWidth,s.height=e.current.offsetHeight,c.font="12px monospace",c.textAlign="center",c.textBaseline="middle")};window.addEventListener("resize",h),s.addEventListener("mousemove",l),h();var d,u=0,m=function(e){var i=Math.min((e-u)/1e3,.1);u=e,function(e){r.current+=.75*e;for(var i=Array(De).fill(0).map((function(){return Array(De).fill(" ")})),a={x:(n.current.x+1)*De/2,y:(1-n.current.y)*De/2,frequency:.3,amplitude:1,phase:2*r.current,speed:1},o=function(e){for(var n=function(n){var o=0;t.current.concat([a]).forEach((function(t){var i=n-t.x,a=e-t.y,s=Math.sqrt(i*i+a*a),c=1/(1+.1*s),l=Math.sin(s*t.frequency-r.current*t.speed+t.phase)*t.amplitude*c;o+=l}));var s=(o+2)/4;if(Math.abs(o)>.2){var c=Math.min(17,Math.max(0,Math.floor(17*s))),l=Math.min(.9,Math.max(.4,.4+.5*s));i[e][n]={char:He[c]||He[0],opacity:l}}else i[e][n]=null},o=0;o<De;o++)n(o)},l=0;l<De;l++)o(l);t.current.forEach((function(e){var n=Math.floor(e.x),t=Math.floor(e.y);if(n>=0&&n<De&&t>=0&&t<De){var a=Math.sin(r.current*e.speed+e.phase),o=Math.floor(18*(a+1)/2);i[t][n]=He[o];for(var s=0;s<2*Math.PI;s+=Math.PI/4){var c=n+Math.round(Math.cos(s)),l=t+Math.round(Math.sin(s));c>=0&&c<De&&l>=0&&l<De&&(i[l][c]=He[Math.floor(18*s/(2*Math.PI))])}}})),c.fillStyle="#F0EEE6",c.fillRect(0,0,s.width,s.height);var h=Math.min(s.width,s.height)/De;for(l=0;l<De;l++)for(var d=0;d<De;d++){var u=i[l][d];u&&u.char&&He.includes(u.char)&&(c.fillStyle="rgba(85, 85, 85, ".concat(u.opacity||.4,")"),c.fillText(u.char,d*h+h/2,l*h+h/2))}}(i),d=requestAnimationFrame(m)};return d=requestAnimationFrame(m),function(){window.removeEventListener("resize",h),s.removeEventListener("mousemove",l),e.current&&s.parentNode===e.current&&e.current.removeChild(s),cancelAnimationFrame(d)}}}),[]),(0,i.jsx)("div",{ref:e,style:{margin:0,background:"#F0EEE6",overflow:"hidden",display:"flex",alignItems:"center",justifyContent:"center",height:"100%",width:"100%",position:"absolute",top:0,left:0}})};Ye.metadata={themes:"simple truth, inner understanding, heart wisdom",visualization:"Patterns that reveal meaning through simplicity",promptSuggestion:"1. Adjust wave frequencies\n2. Change interference patterns\n3. Try different dot characters\n4. Modify wave amplitudes\n5. Add phase variations"};const Xe=Ye;var We=36,qe="┌",Oe="┐",Ge="└",Ne="┘",$e="─",_e="│",Ve="┼",Ue=function(){var e=(0,x.useState)([]),n=e[0],t=e[1],a=(0,x.useState)(0),r=a[0],o=a[1],s=(0,x.useRef)(null),c=(0,x.useRef)(null),l=(0,x.useRef)({x:0,y:0}),h=(0,x.useState)((function(){for(var e=[],n=8;n<18;n+=8)for(var t=0;t<2*Math.PI;t+=Math.PI/2)e.push({x:18+Math.cos(t)*n,y:18+Math.sin(t)*n,size:.8*n,angle:t,phase:.2*n});return e}))[0];return(0,x.useEffect)((function(){var e=Array(We).fill(0).map((function(){return Array(We).fill(" ")}));t(e);var n=function(e){var n=s.current;if(n){var t=n.getBoundingClientRect(),i=(e.clientX-t.left)/t.width,a=(e.clientY-t.top)/t.height;l.current={x:2*i-1,y:-(2*a-1)}}},i=function(){if(s.current&&c.current){var e=c.current,n=Math.min(e.clientWidth,e.clientHeight);s.current.width=s.current.height=n}};return window.addEventListener("mousemove",n),window.addEventListener("resize",i),i(),function(){window.removeEventListener("mousemove",n),window.removeEventListener("resize",i)}}),[]),(0,x.useEffect)((function(){var e,n=0,i=function(a){var s=a-n;n=a;var c=s/1e3;o((function(e){return e+.4*c}));var d=Array(We).fill(0).map((function(){return Array(We).fill(" ")})),u=(l.current.x+1)*We/2,m=(1-l.current.y)*We/2;h.forEach((function(e){var n=u-e.x,t=m-e.y,i=Math.sqrt(n*n+t*t),a=Math.max(0,(15-i)/15),o=(.5*Math.sin(r+e.phase)+.5)*(1-a)+a,s=e.size*o;[{x:-s,y:-s,char:qe},{x:s,y:-s,char:Oe},{x:-s,y:s,char:Ge},{x:s,y:s,char:Ne}].forEach((function(n){var t=Math.floor(e.x+n.x*Math.cos(e.angle)-n.y*Math.sin(e.angle)),i=Math.floor(e.y+n.x*Math.sin(e.angle)+n.y*Math.cos(e.angle));if(t>=0&&t<We&&i>=0&&i<We){d[i][t]=n.char;for(var a=Math.floor(2*s),r=1;r<a;r++){var o=t+(n.x<0?r:-r);o>=0&&o<We&&(d[i][o]=$e);var c=i+(n.y<0?r:-r);c>=0&&c<We&&(d[c][t]=_e)}}}))}));for(var f=1;f<35;f++)for(var p=1;p<35;p++)if(d[f][p]===$e||d[f][p]===_e){var g=d[f-1][p]===_e||d[f+1][p]===_e,v=d[f][p-1]===$e||d[f][p+1]===$e;g&&v&&(d[f][p]=Ve)}t(d),e=requestAnimationFrame(i)};return e=requestAnimationFrame(i),function(){cancelAnimationFrame(e)}}),[r,h]),(0,x.useEffect)((function(){var e=s.current;if(e){var t=e.getContext("2d");if(t){t.clearRect(0,0,e.width,e.height);var i=e.width/We;t.font="".concat(.8*i,"px monospace"),t.fillStyle="#333333",t.textAlign="center",t.textBaseline="middle",n.forEach((function(e,n){e.forEach((function(e,a){if(" "!==e){var r=(a+.5)*i,o=(n+.5)*i;t.fillText(e,r,o)}}))}))}}}),[n]),(0,i.jsx)("div",{ref:c,style:{width:"100%",height:"100%",display:"flex",justifyContent:"center",alignItems:"center"},children:(0,i.jsx)("canvas",{ref:s,style:{maxWidth:"100%",maxHeight:"100%",imageRendering:"crisp-edges"}})})};Ue.metadata={themes:"Yielding, Softness, Strength",visualization:"Folding paper using corner characters",promptSuggestion:"1. Adjust fold speed\n2. Change fold patterns\n3. Try different corner characters\n4. Modify transition effects\n5. Add crease patterns"};const Ze=function(){var e=(0,x.useRef)(null),n=(0,x.useState)({x:0,y:0}),t=n[0],a=n[1];(0,x.useEffect)((function(){var n=e.current;if(n){var i,a=n.getContext("2d"),r=function(){var e=n.parentElement,t=e.clientWidth,i=e.clientHeight;n.width=t,n.height=i;n.style.width=t+"px",n.style.height=i+"px"};r(),window.addEventListener("resize",r);for(var o=0,s=[],c=function(){function e(e){this.index=e,this.radius=50+35*e,this.rotation=0,this.particles=[],this.particleCount=3+4*e,this.thickness=.5+.2*e,this.drift=Math.random()*Math.PI*2;for(var n=0;n<this.particleCount;n++)this.particles.push({angle:n/this.particleCount*Math.PI*2,offset:10*Math.random(),phase:Math.random()*Math.PI*2,amplitude:3+5*Math.random(),flowSpeed:.0017+.0017*Math.random()})}return e.prototype.update=function(e){var n=this;this.rotation+=25e-5/(this.index+1)*(1+.2*e),this.drift+=8e-4,this.particles.forEach((function(e){e.angle+=e.flowSpeed*(1-n.index/7)}))},e.prototype.draw=function(e,n,t,i,a){var r=this;e.save(),e.translate(n,t),e.scale(a,a),e.rotate(this.rotation),e.beginPath(),e.strokeStyle="rgba(80, 80, 80, 0.20)",e.lineWidth=this.thickness,this.particles.forEach((function(n,t){var i=n.angle+.1*Math.sin(.04*o+n.phase),a=Math.sin(o+n.phase)*n.amplitude,s=r.radius+a+n.offset,c=Math.cos(i)*s,l=Math.sin(i)*s;0===t?e.moveTo(c,l):e.lineTo(c,l)})),e.closePath(),e.stroke(),e.restore()},e}(),l=0;l<7;l++)s.push(new c(l));return function e(){a.fillStyle="#F0EEE6",a.fillRect(0,0,n.width,n.height);for(var r=n.width/2,c=n.height/2,l=t.x-r,h=t.y-c,d=Math.sqrt(l*l+h*h),u=Math.max(0,1-d/200),m=Math.min(n.width,n.height)/800*1,f=0;f<24;f++){var p=f/24*Math.PI*2+.004*o,g=300*m+50*Math.sin(o+f)*m;a.beginPath(),a.strokeStyle="rgba(80, 80, 80, 0.05)",a.lineWidth=.5,a.moveTo(r,c),a.lineTo(r+Math.cos(p)*g,c+Math.sin(p)*g),a.stroke()}for(s.forEach((function(e){e.update(u)})),f=s.length-1;f>=0;f--)s[f].draw(a,r,c,u,m);o+=5e-4,i=requestAnimationFrame(e)}(),function(){window.removeEventListener("resize",r),i&&(cancelAnimationFrame(i),i=null),n&&a&&a.clearRect(0,0,n.width,n.height),s.forEach((function(e){e.particles&&(e.particles.length=0)})),s.length=0,o=0}}}),[]);return(0,i.jsx)("div",{style:{backgroundColor:"#F0EEE6",margin:0,padding:0,overflow:"hidden",display:"flex",justifyContent:"center",alignItems:"center"},children:(0,i.jsx)("canvas",{ref:e,onMouseMove:function(n){var t=e.current;if(t){var i=t.getBoundingClientRect();a({x:n.clientX-i.left,y:n.clientY-i.top})}},style:{cursor:"crosshair",display:"block"}})})};const Je=function(){var e=(0,x.useRef)(null),n=(0,x.useState)({x:275,y:275}),t=n[0],a=n[1],r=(0,x.useState)([]),o=r[0],s=r[1],c=(0,x.useState)(0),l=c[0],h=c[1];return(0,x.useEffect)((function(){var n=function(n){if(e.current){var t=e.current.getBoundingClientRect();a({x:n.clientX-t.left,y:n.clientY-t.top})}},t=null,i=0,r=1e3/60,o=function(e){t=requestAnimationFrame(o);var n=e-i;n>=r&&(i=e-n%r,h((function(e){return e+.008})))};return t=requestAnimationFrame(o),window.addEventListener("mousemove",n),function(){window.removeEventListener("mousemove",n),t&&cancelAnimationFrame(t)}}),[]),(0,x.useEffect)((function(){for(var e=[],n=[" ","·","+","*","※","◊","○","●"],i=0;i<35;i++){for(var a="",r=0;r<50;r++){var o=t.x/550*50,c=t.y/550*35,h=o+2*Math.sin(.25*l),d=c+2*Math.cos(.15*l),u=Math.sqrt(Math.pow(r-h,2)+Math.pow(i-d,2)),m=Math.sqrt(Math.pow(r-25,2)+Math.pow(i-17.5,2)),f=Math.atan2(i-17.5,r-25)+Math.PI,p=.5*Math.sin(.5*m-.001*Date.now())+.5,g=Math.sin(f+.2*m+.1*u),v=0;m<3?v=7:m<8?v=Math.floor(2*p)+4:(v=Math.floor(3*(.5*g+.5)),u<10&&(v+=2)),Math.abs(Math.floor(m)%5)<1&&(v=Math.min(v+2,7)),a+=n[Math.max(0,Math.min(v,n.length-1))]}e.push(a)}s(e)}),[t,l]),(0,i.jsx)("div",{ref:e,style:{width:"550px",height:"550px",backgroundColor:"#F0EEE6",display:"flex",justifyContent:"center",alignItems:"center",fontFamily:"monospace",fontSize:"12px",lineHeight:"12px",color:"#333333",cursor:"none",overflow:"hidden"},children:(0,i.jsx)("pre",{style:{margin:0},children:o.map((function(e,n){return(0,i.jsx)("div",{children:e},n)}))})})};var Ke=function(e){var n=e.count,t=void 0===n?2e4:n,a=(0,x.useRef)(null),r=(0,x.useRef)(null),o=(0,x.useRef)(null),s=(0,x.useRef)(null),c=(0,x.useRef)(null),l=(0,x.useRef)(null),h=(0,x.useRef)(null),d=(0,x.useMemo)((function(){return new B.BKk({uniforms:{time:{value:0},opacity:{value:.9}},vertexShader:"\n        uniform float time;\n        attribute float size;\n        attribute vec3 customColor;\n        varying vec3 vColor;\n        \n        void main() {\n          vColor = customColor;\n          vec3 pos = position;\n          \n          // Calculate natural flow based on position\n          float t = time * 0.2;\n          float radius = length(pos.xz);\n          float angle = atan(pos.z, pos.x);\n          \n          // Effortless transformation\n          float flow = sin(t + radius * 2.0 - angle) * cos(t * 0.7 + angle * 3.0);\n          \n          // Vertical oscillation creating layers\n          float layer = floor(pos.y * 2.0) * 0.5;\n          float layerPhase = t + layer;\n          \n          // Complex autonomous motion\n          vec3 motion;\n          motion.x = cos(layerPhase) * sin(t * 0.5 + pos.z) * 0.5;\n          motion.y = sin(layerPhase * 0.7) * 0.3;\n          motion.z = sin(layerPhase) * cos(t * 0.5 + pos.x) * 0.5;\n          \n          // Apply transformations\n          pos += motion * (1.0 + flow * 0.3);\n          \n          // Scale up the overall effect\n          pos *= 0.6;\n          \n          vec4 mvPosition = modelViewMatrix * vec4(pos, 1.0);\n          gl_PointSize = size * (192.0 / -mvPosition.z);\n          gl_Position = projectionMatrix * mvPosition;\n        }\n      ",fragmentShader:"\n        uniform float opacity;\n        varying vec3 vColor;\n        void main() {\n          float dist = length(gl_PointCoord - vec2(0.5));\n          if (dist > 0.5) discard;\n          float alpha = (1.0 - smoothstep(0.45, 0.5, dist)) * opacity;\n          gl_FragColor = vec4(vColor, alpha);\n        }\n      ",transparent:!0,depthWrite:!1})}),[]),u=(0,x.useMemo)((function(){for(var e=new Float32Array(3*t),n=new Float32Array(3*t),i=new Float32Array(t),a=0;a<t;a++){var r=Math.floor(a/(t/5))-2,o=a%(t/5)/(t/5),s=3*Math.sqrt(o),c=o*Math.PI*15;e[3*a]=Math.cos(c)*s,e[3*a+1]=1.2*r,e[3*a+2]=Math.sin(c)*s;var l=.3+.15*Math.random();n[3*a]=l,n[3*a+1]=l,n[3*a+2]=l,i[a]=.12+.04*Math.random()}return[e,n,i]}),[t]),m=u[0],f=u[1],p=u[2];return(0,x.useEffect)((function(){if(a.current){var e=new B.Z58,n=new B.ubm(75,a.current.clientWidth/a.current.clientHeight,.1,1e3),t=new D.JeP({antialias:!0}),i=new B.zD7;s.current=e,c.current=n,o.current=t,l.current=i;var u=window.devicePixelRatio||1;t.setPixelRatio(u),t.setSize(a.current.clientWidth,a.current.clientHeight),a.current.appendChild(t.domElement),e.background=new B.Q1f("#F0EEE6"),n.position.z=5;var g=new B.LoY;g.setAttribute("position",new B.THS(m,3)),g.setAttribute("customColor",new B.THS(f,3)),g.setAttribute("size",new B.THS(p,1));var v=new B.ONl(g,d);r.current=v,e.add(v);var y=function(){h.current=requestAnimationFrame(y);var a=i.getElapsedTime();d.uniforms.time.value=a,t.render(e,n)};y();var x=function(){if(a.current&&n&&t){n.aspect=a.current.clientWidth/a.current.clientHeight,n.updateProjectionMatrix(),t.setSize(a.current.clientWidth,a.current.clientHeight);var e=window.devicePixelRatio||1;t.setPixelRatio(e)}};return window.addEventListener("resize",x),function(){window.removeEventListener("resize",x),h.current&&(cancelAnimationFrame(h.current),h.current=null),r.current&&s.current&&(s.current.remove(r.current),r.current=null),g&&g.dispose(),o.current&&(a.current&&o.current.domElement&&a.current.contains(o.current.domElement)&&a.current.removeChild(o.current.domElement),o.current.dispose(),o.current.forceContextLoss(),o.current=null),s.current&&(s.current.clear(),s.current=null),c.current&&(c.current=null),l.current&&(l.current=null)}}}),[t,m,f,p,d]),(0,i.jsx)("div",{ref:a,style:{width:"100%",height:"100%"}})},Qe=function(){return(0,i.jsx)("div",{style:{width:"100vw",height:"100vh",top:0,left:0},children:(0,i.jsx)(Ke,{})})};Qe.metadata={themes:"Accomplishing through non-doing, seeing the large in the small",visualization:"Particles flowing in effortless, self-organizing patterns",promptSuggestion:"1. Add subtle flow variations\n2. Create self-organizing waves\n3. Vary effortless patterns naturally\n4. Introduce gentle layer transitions\n5. Make motion follow natural rhythms"};const en=Qe;const nn=function(){var e=(0,x.useRef)(null);return(0,x.useEffect)((function(){var n=e.current;if(n){var t=n.getContext("2d");if(t){var i,a=0,r=function(){n.width=window.innerWidth,n.height=window.innerWidth};r(),window.addEventListener("resize",r);for(var o=n.width<440,s=2.5,c=n.width/18,l=n.height/18-60,h=[],d=o?15:50,u=n.width/5+c,m=n.height/5+l,f=0;f<d;f++){var p=f/d*Math.PI*2,g=180*Math.random()+80,v=Math.random(),y=v<.2?40:v>.8?-40:0;h.push({x:u+Math.cos(p)*(g+y),y:m+Math.sin(p)*(g+y),speedX:.05*(Math.random()-.5),speedY:.05*(Math.random()-.5),size:1.5*Math.random()+.8,connections:[],noiseOffset:1e3*Math.random(),idealSpace:60+20*Math.random(),allowClustering:v<.35})}var x=o?80:72,A=o?16:24,w=function(){a+=.0025,t.fillStyle="#F0EEE6",t.fillRect(0,0,n.width,n.height),h.forEach((function(e){return e.connections=[]}));for(var e=0;e<h.length;e++)for(var r=e+1;r<h.length;r++){var d=Math.sqrt(Math.pow(h[e].x-h[r].x,2)+Math.pow(h[e].y-h[r].y,2));if(d<x*s){var f=void 0;if(d<(x-A)*s)f=Math.min(.24,.36*(1-d/((x-A)*s)));else{var p=(d-(x-A)*s)/(A*s);f=.24*Math.pow(1-p,3)}f>.001&&(h[e].connections.push({particle:h[r],distance:d,alpha:f}),h[r].connections.push({particle:h[e],distance:d,alpha:f}))}}h.forEach((function(e){var t=.001*e.x+e.noiseOffset,i=.001*e.y+e.noiseOffset+100,r=Math.sin(t+a)*Math.cos(i-a)+Math.sin(2*t+.6*a)*Math.cos(2*i-.6*a)*.3,c=o?.0085:.00125;e.speedX+=Math.cos(r*Math.PI*2)*c,e.speedY+=Math.sin(r*Math.PI*2)*c;var l=u-e.x,d=m-e.y,f=Math.sqrt(l*l+d*d),p=e.allowClustering?130:200,g=e.allowClustering?60:90,v=o?.1:1;f>p?(e.speedX+=l/f*.002*v,e.speedY+=d/f*.002*v):f<g&&(e.speedX-=l/f*.0025*v,e.speedY-=d/f*.0025*v),h.forEach((function(n){if(n!==e){var t=e.x-n.x,i=e.y-n.y,a=Math.sqrt(t*t+i*i);if(a<e.idealSpace){var r=e.allowClustering&&n.allowClustering?.005:.015;a<.7*e.idealSpace&&(e.speedX+=t/a*r,e.speedY+=i/a*r)}}}));var y=o?.97:.98;if(e.speedX*=y,e.speedY*=y,e.x+=e.speedX,e.y+=e.speedY,o){var x=50,A=n.width/s,w=n.height/s;e.x<x&&(e.speedX+=.01*(x-e.x)),e.x>A-x&&(e.speedX-=.01*(e.x-(A-x))),e.y<x&&(e.speedY+=.01*(x-e.y)),e.y>w-x&&(e.speedY-=.01*(e.y-(w-x)))}else e.x<0&&(e.x+=n.width/s),e.x>n.width/s&&(e.x-=n.width/s),e.y<0&&(e.y+=n.height/s),e.y>n.height/s&&(e.y-=n.height/s)})),t.save(),t.translate(-c*s,-l*s),t.scale(s,s),t.lineWidth=.4,t.lineCap="round",h.forEach((function(e){e.connections.forEach((function(n){t.strokeStyle="rgba(0, 0, 0, ".concat(n.alpha,")"),t.beginPath(),t.moveTo(e.x,e.y),t.lineTo(n.particle.x,n.particle.y),t.stroke()}))})),h.forEach((function(e){var n=Math.sqrt(Math.pow(e.x-u,2)+Math.pow(e.y-m,2)),i=o?.4:.5,a=Math.max(.3,Math.min(.7,1-n/(500+100*i)));t.fillStyle="rgba(0, 0, 0, ".concat(a,")"),t.beginPath(),t.arc(e.x,e.y,e.size,0,2*Math.PI),t.fill()})),t.restore(),i=requestAnimationFrame(w)};return w(),function(){cancelAnimationFrame(i),i=null,window.removeEventListener("resize",r),n&&t&&t.clearRect(0,0,n.width,n.height),h.length=0,a=0}}}}),[]),(0,i.jsx)("canvas",{ref:e,style:{width:"100%",height:"auto",background:"#F0EEE6"}})};const tn=function(){var e=(0,x.useState)(0),n=e[0],t=e[1],a=(0,x.useRef)(null),r="~≈≋⋿⊰⊱◟◝",o=.5,s=.5,c=2*Math.PI,l=(0,x.useRef)(0);!function(e,n){void 0===n&&(n=!0);var t=(0,x.useRef)(null),i=(0,x.useRef)(null),a=(0,x.useCallback)((function(n){if(null!==i.current){var r=n-i.current;e(r)}i.current=n,t.current=requestAnimationFrame(a)}),[e]);(0,x.useEffect)((function(){return n&&(t.current=requestAnimationFrame(a)),function(){null!==t.current&&(cancelAnimationFrame(t.current),t.current=null),i.current=null}}),[a,n])}((0,x.useCallback)((function(e){l.current+=e,l.current>166&&(t((function(e){return e+1})),l.current=0)}),[])),(0,x.useEffect)((function(){return function(){l.current=0}}),[]);var h=(0,x.useCallback)((function(){for(var e=[],t=n/6.7,i=n/8.3,a=n/13.3,l=0;l<25;l++){for(var h=l/25,d=l/5,u=l/3,m="",f=1,p=0;p<52;p++){var g=p/52,v=p/3,y=p/4,x=g-o,A=h-s,w=Math.sqrt(x*x+A*A),b=10*w,M=5*w,C=Math.sin(v+d+t+b)+Math.cos(y-u-i)+Math.sin(a+g*c),E=2*(C+2)+M,R=Math.floor(Math.abs(E))%8,P=Math.max(.2,Math.min(.8,1-w+Math.sin(C)/3));f=0===p?P:(f+P)/2,m+=r[R]}e.push({text:m,opacity:f})}return e}),[n,25,52,8,2,c,o,s,r]),d=(0,x.useMemo)((function(){return h()}),[h]),u=(0,x.useMemo)((function(){return{margin:0,background:"#F0EEE6",overflow:"hidden",display:"flex",alignItems:"center",justifyContent:"center",height:"100%"}}),[]),m=(0,x.useMemo)((function(){return{padding:"30px",position:"relative",width:"100%",height:"100%",display:"flex",alignItems:"center",justifyContent:"center"}}),[]),f=(0,x.useMemo)((function(){return{fontFamily:"monospace",fontSize:"10px",lineHeight:"1",cursor:"default",userSelect:"none",margin:0,padding:"20px"}}),[]);return(0,i.jsx)("div",{style:u,children:(0,i.jsx)("div",{ref:a,style:m,children:(0,i.jsx)("pre",{style:f,children:d.map((function(e,n){return(0,i.jsx)("div",{style:{opacity:e.opacity,margin:0,lineHeight:"1"},children:e.text},n)}))})})})};var an=function(){var e=(0,x.useRef)(null);return(0,x.useEffect)((function(){if(e.current){var n=e.current,t=new B.Z58,i=new B.ubm(75,n.clientWidth/n.clientHeight,.1,1e3),a=new D.JeP({antialias:!0});a.setSize(n.clientWidth,n.clientHeight),a.setPixelRatio(Math.min(window.devicePixelRatio,2)),a.setClearColor(15789798),n.appendChild(a.domElement),i.position.z=6.25;for(var r=new B.BKk({uniforms:{time:{value:0},opacity:{value:.4}},vertexShader:"\n        uniform float time;\n        attribute float size;\n        attribute vec3 customColor;\n        varying vec3 vColor;\n        \n        float rand(vec2 co) {\n          return fract(sin(dot(co.xy ,vec2(12.9898,78.233))) * 43758.5453);\n        }\n        \n        void main() {\n          vColor = customColor;\n          vec3 pos = position;\n          \n          // Calculate radial distance and angle\n          float radius = length(pos.xz);\n          float angle = atan(pos.z, pos.x);\n          float height = pos.y;\n          \n          // Source pulse\n          float pulse = sin(time * 2.0) * 0.2 + 0.8;\n          \n          // Radial waves emanating from center\n          float wave = sin(radius * 3.0 - time * 3.0) * 0.2;\n          \n          // Vertical oscillation based on radius\n          float verticalWave = cos(radius * 2.0 - time * 1.5) * 0.3;\n          \n          // Spiral rotation\n          float rotationSpeed = 0.05 / (radius + 1.0);\n          float newAngle = angle + time * rotationSpeed;\n          \n          // Apply transformations\n          vec3 newPos;\n          newPos.x = cos(newAngle) * (radius + wave) * pulse;\n          newPos.z = sin(newAngle) * (radius + wave) * pulse;\n          newPos.y = height + verticalWave;\n          \n          // Scale for canvas size\n          newPos *= 2.34375;\n          \n          pos = newPos;\n          \n          vec4 mvPosition = modelViewMatrix * vec4(pos, 1.0);\n          gl_PointSize = size * (100.0 / -mvPosition.z); // Set to exactly 100\n          gl_Position = projectionMatrix * mvPosition;\n        }\n      ",fragmentShader:"\n        uniform float opacity;\n        varying vec3 vColor;\n        void main() {\n          float dist = length(gl_PointCoord - vec2(0.5));\n          if (dist > 0.5) discard;\n          float alpha = (1.0 - smoothstep(0.45, 0.5, dist)) * opacity;\n          gl_FragColor = vec4(vColor, alpha);\n        }\n      ",transparent:!0,depthWrite:!1}),o=37500,s=new Float32Array(112500),c=new Float32Array(112500),l=new Float32Array(o),h=0;h<o;h++){var d=h/o,u=Math.pow(d,.5),m=d*Math.PI*30,f=2*(Math.random()-.5);s[3*h]=Math.cos(m)*u,s[3*h+1]=f,s[3*h+2]=Math.sin(m)*u;var p=Math.sqrt(u),g=.02+.13*p+.05*Math.random();c[3*h]=g,c[3*h+1]=g,c[3*h+2]=g,l[h]=.164*(1-p)+.133}var v=new B.LoY;v.setAttribute("position",new B.THS(s,3)),v.setAttribute("customColor",new B.THS(c,3)),v.setAttribute("size",new B.THS(l,1));var y=new B.ONl(v,r);t.add(y);var x,A=new B.zD7,w=function(){x=requestAnimationFrame(w);var e=.4*A.getElapsedTime();r.uniforms.time.value=e,a.render(t,i)};w();var b=function(){var e=n.clientWidth,t=n.clientHeight;i.aspect=e/t,i.updateProjectionMatrix(),a.setSize(e,t)};return window.addEventListener("resize",b),function(){window.removeEventListener("resize",b),cancelAnimationFrame(x),a&&(a.dispose(),n.contains(a.domElement)&&n.removeChild(a.domElement)),v&&v.dispose(),r&&r.dispose()}}}),[]),(0,i.jsx)("div",{ref:e,style:{margin:0,background:"#F0EEE6",overflow:"hidden",display:"flex",alignItems:"center",justifyContent:"center",height:"100vh",width:"100vw",position:"relative"}})};an.metadata={themes:"Source, Radiance, Forgiveness",visualization:"Particles radiating from a central source in gentle waves",promptSuggestion:"1. Add subtle radiance variations\n2. Create source-centered waves\n3. Vary emanation patterns naturally\n4. Introduce gentle pulse rhythms\n5. Make radiance follow natural flows"};const rn=an;const on=function(){var e=(0,x.useRef)(null);return(0,x.useEffect)((function(){var n=e.current;if(n){var t,i=65,a=65,r=[],o=0;return s(),t=requestAnimationFrame((function e(){!function(){s();for(var e=30,n=Math.floor(17.5),t=Math.floor(17.5),c=.005*o,l=0;l<a;l++)for(var h=0;h<i;h++)if(h>=n&&h<n+e&&l>=t&&l<t+e){var d=Math.min(h-n,n+e-h,l-t,t+e-l),u=.0067*o;r[l][h]=d>u||Math.random()>.8?"1":"0"}else{var m=h-32.5,f=l-32.5,p=Math.atan2(f,m),g=Math.sqrt(m*m+f*f),v=Math.sin(.2*g-c+1.5*p),y=Math.sin(.08*h+.04*l+.4*c);y+v>.4?r[l][h]="0":y+v<-.4&&(r[l][h]="~")}for(var x=0;x<5;x++)for(var A=n+Math.floor(Math.random()*e),w=t+Math.floor(Math.random()*e),b=Math.floor(10*Math.random())+5,M=Math.floor(A),C=Math.floor(w),E=0;E<b;E++)M>=0&&M<i&&C>=0&&C<a&&(r[C][M]="0"),M+=Math.floor(3*Math.random())-1,C+=Math.floor(3*Math.random())-1;o++}(),function(){for(var e="",t=0;t<a;t++){for(var o=0;o<i;o++)e+=r[t][o];e+="<br>"}n.innerHTML=e}(),t=requestAnimationFrame(e)})),function(){t&&(cancelAnimationFrame(t),t=null),n&&(n.innerHTML=""),r=[],o=0}}function s(){r=[];for(var e=0;e<a;e++){for(var n=[],t=0;t<i;t++)n.push(" ");r.push(n)}}}),[]),(0,i.jsx)("div",{style:{margin:0,background:"#F0EEE6",overflow:"hidden",fontFamily:"monospace",display:"flex",alignItems:"center",justifyContent:"center",height:"100%"},children:(0,i.jsx)("div",{style:{padding:"30px",position:"relative",width:"100%",height:"100%",display:"flex",alignItems:"center",justifyContent:"center"},children:(0,i.jsx)("div",{ref:e,style:{lineHeight:"0.85",letterSpacing:"0.05em",color:"rgba(0,0,0,0.85)",userSelect:"none",width:"100%",height:"100%",overflow:"hidden",display:"flex",flexDirection:"column",justifyContent:"center",marginLeft:"10%"}})})})};var sn=function(){return sn=Object.assign||function(e){for(var n,t=1,i=arguments.length;t<i;t++)for(var a in n=arguments[t])Object.prototype.hasOwnProperty.call(n,a)&&(e[a]=n[a]);return e},sn.apply(this,arguments)},cn=function(e){for(var n=e.points,t=e.radius,i=e.height,a=e.twist,r=e.phase,o=(e.scene,e.parent),s=[],c=0;c<=n;c++){var l=c/n,h=l*Math.PI*2*a+r,d=t*(1+.5*Math.sin(l*Math.PI*3)),u=i*l+1.2*Math.sin(l*Math.PI*2),m=Math.cos(h)*d,f=Math.sin(h)*d;s.push(new B.Pq0(m,u,f))}var p=(new B.LoY).setFromPoints(s),g=new B.mrM({color:6710886,transparent:!0,opacity:.5,linewidth:.5}),v=new B.N1A(p,g);return o.add(v),v},ln=function(e){for(var n=e.baseRadius,t=e.baseHeight,i=e.scene,a=e.parent,r=[],o=0;o<24;o++){var s={points:50,radius:n*(1+.2*Math.sin(o/24*Math.PI*2)),height:t*(1+.2*Math.cos(o/24*Math.PI*2)),twist:2.5+.8*Math.sin(o/24*Math.PI),phase:o/24*Math.PI*2},c=cn(sn(sn({},s),{scene:i,parent:a}));r.push(c)}return r},hn=function(){var e=(0,x.useRef)(null),n=(0,x.useRef)(null);return(0,x.useEffect)((function(){if(e.current){var t=new B.Z58;t.background=new B.Q1f(15789799);var i=new B.ubm(75,e.current.clientWidth/e.current.clientHeight,.1,1e3),a=new D.JeP({antialias:!0});a.setSize(e.current.clientWidth,e.current.clientHeight),e.current.appendChild(a.domElement);var r=new B.YJl;n.current=r,t.add(r),ln({baseRadius:2,baseHeight:3,scene:t,parent:r});var o=new B.YJl;o.position.set(0,-2.5,0),o.rotation.set(.1*Math.PI,0,0),ln({baseRadius:1.5,baseHeight:2,scene:t,parent:o}),r.add(o);var s=new B.YJl;s.position.set(0,1.5,0),s.rotation.set(.1*-Math.PI,.5*Math.PI,0),ln({baseRadius:1,baseHeight:1.5,scene:t,parent:s}),r.add(s);var c=new B.$p8(16777215,.4);t.add(c);var l=new B.ZyN(16777215,.6);l.position.set(5,5,5),t.add(l);var h=new B.HiM(16777215,.4);h.position.set(-5,3,-5),t.add(h),i.position.z=7,i.position.y=0,i.lookAt(0,0,0);var d,u=new B.zD7,m=function(){d=requestAnimationFrame(m);var e=u.getElapsedTime();n.current&&(n.current.rotation.y=.5*Math.sin(.2*e),n.current.rotation.x=.3*Math.cos(.15*e)),a.render(t,i)};m();var f=function(){if(e.current){var n=e.current.clientWidth,t=e.current.clientHeight;i.aspect=n/t,i.updateProjectionMatrix(),a.setSize(n,t)}};return window.addEventListener("resize",f),function(){d&&cancelAnimationFrame(d),window.removeEventListener("resize",f),t.traverse((function(e){(e instanceof B.eaF||e instanceof B.N1A)&&(e.geometry&&e.geometry.dispose(),e.material&&(Array.isArray(e.material)?e.material.forEach((function(e){e.dispose&&e.dispose()})):e.material.dispose&&e.material.dispose()))})),a.dispose(),a.forceContextLoss(),e.current&&a.domElement.parentNode===e.current&&e.current.removeChild(a.domElement),n.current=null}}}),[]),(0,i.jsx)("div",{ref:e,style:{width:"100%",height:"100vh",backgroundColor:"#f0eee7"}})};hn.metadata={themes:"gentle leadership, minimal intervention, natural resolution",visualization:"A spiral system that finds its own balance through minimal guidance",promptSuggestion:"1. Enhance flow transitions\n2. Add more transformation points\n3. Develop smoother progressions\n4. Create stronger movement paths\n5. Increase sense of change"};const dn=hn;const un=function(){var e=(0,x.useRef)(null);return(0,x.useEffect)((function(){var n=e.current;if(n){var t=n.getContext("2d"),i=n.width=550,a=n.height=550,r=0,o=null;return function e(){r+=.01,t.fillStyle="#F0EEE6",t.fillRect(0,0,i,a),c(0,0,1.5,0),t.save(),t.translate(i/2,a/2),t.rotate(Math.PI/6),t.translate(-i/2,-a/2),c(0,-100,.8,.2*r+Math.PI/3),t.restore(),t.save(),t.translate(i/2,a/2),t.rotate(-Math.PI/6),t.translate(-i/2,-a/2),c(0,100,.8,.2*r-Math.PI/3),t.restore(),o=requestAnimationFrame(e)}(),function(){o&&(cancelAnimationFrame(o),o=null),t&&t.clearRect(0,0,i,a),n&&(n.width=0,n.height=0)}}function s(e,n,i,a,r,o){t.save(),t.translate(e,n),t.rotate(a),t.beginPath();for(var s=0;s<=6;s++){var c=s/6*Math.PI*2,l=i*(1+.1*Math.sin(r+s)),h=Math.cos(c)*l,d=Math.sin(c)*l;0===s?t.moveTo(h,d):t.lineTo(h,d)}t.strokeStyle="rgba(51, 51, 51, 0.4)",t.lineWidth=1,t.stroke();for(s=0;s<6;s+=2){var u=s/6*Math.PI*2,m=(s+2)/6*Math.PI*2,f=i*(1+.1*Math.sin(r+s)),p=i*(1+.1*Math.sin(r+s+2)),g=Math.cos(u)*f,v=Math.sin(u)*f,y=Math.cos(m)*p,x=Math.sin(m)*p,A=(u+m)/2,w=.5*i*(1+.5*o),b=Math.cos(A)*w,M=Math.sin(A)*w;t.beginPath(),t.moveTo(g,v),t.lineTo(b,M),t.stroke(),t.beginPath(),t.moveTo(y,x),t.lineTo(b,M),t.stroke()}t.restore()}function c(e,n,t,o){for(var c=60*t*.8,l=-4;l<=4;l++)for(var h=l%2*c*.5,d=-4;d<=4;d++){var u=d*c*.866+h+e,m=l*c*.75+n,f=Math.sqrt(u*u+m*m);if(!(f>60*t*2.5)){var p=Math.atan2(m-n,u-e),g=r+o+.01*f,v=.5*Math.sin(g+p)+.5;s(i/2+u,a/2+m,60*t*.4*(1-f/(60*t*3)*.3),p+.2*(r+o),g,v)}}}}),[]),(0,i.jsx)("div",{style:{width:"550px",height:"550px",margin:"auto",backgroundColor:"#F0EEE6",overflow:"hidden"},children:(0,i.jsx)("canvas",{ref:e,style:{display:"block",width:"550px",height:"550px"}})})};var mn=function(){function e(e,n){this.props=e,this.parent=n,this.lines=[],this.children=[],this.disposed=!1,this.group=new B.YJl,this.parent.add(this.group)}return e.prototype.update=function(n){if(!this.disposed){var t,i=this.props,a=i.start,r=i.length,o=i.angle,s=i.depth,c=i.maxDepth,l=i.scale,h=a[0],d=a[1],u=a[2];if(n>=.9)t=1;else if(s===c&&n>.6)t=1;else{t=function(e){return e<.5?2*e*e:(4-2*e)*e-1}(Math.min(1,Math.max(0,3*(n-.15*s))))}var m=r*t,f=h+Math.cos(o)*m,p=d+Math.sin(o)*m,g=u;if(0===this.lines.length){var v=new B.LoY,y=new Float32Array([h,d,u,f,p,g]);v.setAttribute("position",new B.THS(y,3));var x=new B.mrM({color:3355443,transparent:!0,opacity:.4,linewidth:.5}),A=new B.N1A(v,x);if(this.group.add(A),this.lines.push(A),t>.3)for(var w=r*(.2+.05*Math.sin(n*Math.PI*2)),b=o+Math.PI/2,M=o-Math.PI/2,C=0;C<2;C++){var E=h+(f-h)*(X=(C+1)/4),R=d+(p-d)*X,P=u+(g-u)*X,F=.05*Math.sin(X*Math.PI*2+n*Math.PI*4),S=new B.LoY,I=new Float32Array([E+Math.cos(b)*w*(X+F),R+Math.sin(b)*w*(X+F),P,E+Math.cos(M)*w*(X+F),R+Math.sin(M)*w*(X+F),P]);S.setAttribute("position",new B.THS(I,3));var T=new B.mrM({color:3355443,transparent:!0,opacity:.4,linewidth:.5}),k=new B.N1A(S,T);this.group.add(k),this.lines.push(k)}}else{var z=(A=this.lines[0]).geometry.attributes.position.array;if(z[3]=f,z[4]=p,z[5]=g,A.geometry.attributes.position.needsUpdate=!0,this.lines.length>1&&t>.3)for(w=r*(.2+.05*Math.sin(n*Math.PI*2)),C=0;C<2;C++){var j=C+1;if(j<this.lines.length){var L=(k=this.lines[j]).geometry.attributes.position.array;E=h+(f-h)*(X=(C+1)/4),R=d+(p-d)*X,P=u+(g-u)*X,F=.05*Math.sin(X*Math.PI*2+n*Math.PI*4),b=o+Math.PI/2,M=o-Math.PI/2;L[0]=E+Math.cos(b)*w*(X+F),L[1]=R+Math.sin(b)*w*(X+F),L[2]=P,L[3]=E+Math.cos(M)*w*(X+F),L[4]=R+Math.sin(M)*w*(X+F),L[5]=P,k.geometry.attributes.position.needsUpdate=!0}}}if(s<c&&!(n>=.8&&s>0)&&n>=.15*s&&n<.95){var D=3+(s<2?1:0);if(0===this.children.length)for(C=0;C<D;C++){var H=.8+.1*s,Y={start:[h+(f-h)*(X=(C+1)/(D+1)),d+(p-d)*X,u+(g-u)*X],length:r*(.6-.05*s),angle:o+(X-.5)*Math.PI*H,depth:s+1,maxDepth:c,scale:.8*l};this.children.push(new e(Y,this.group))}else for(C=0;C<this.children.length;C++){var X=(C+1)/(D+1);this.children[C].props.start=[h+(f-h)*X,d+(p-d)*X,u+(g-u)*X]}}this.children.forEach((function(e){return e.update(n)}))}},e.prototype.dispose=function(){var e=this;this.disposed||(this.disposed=!0,this.children.forEach((function(e){return e.dispose()})),this.children=[],this.lines.forEach((function(n){e.group.remove(n),n.geometry.dispose(),n.material.dispose()})),this.lines=[],this.parent.remove(this.group))},e}(),fn=function(){function e(e){this.branches=[],this.group=new B.YJl,e.add(this.group),this.initialize()}return e.prototype.initialize=function(){for(var e=0;e<6;e++){var n=e/6*Math.PI*2,t={start:[2*Math.cos(n)*.2,2*Math.sin(n)*.2,0],length:2,angle:n+Math.PI/2,depth:0,maxDepth:7,scale:2};this.branches.push(new mn(t,this.group))}},e.prototype.update=function(e){this.branches.forEach((function(n){return n.update(e)}))},e.prototype.dispose=function(){var e;this.branches.forEach((function(e){return e.dispose()})),this.branches=[],null===(e=this.group.parent)||void 0===e||e.remove(this.group)},e}(),pn=function(){var e=(0,x.useRef)(null),n=(0,x.useRef)(null);return(0,x.useEffect)((function(){if(e.current){var t=new B.Z58,i=new B.ubm(75,e.current.clientWidth/e.current.clientHeight,.1,1e3),a=new D.JeP({antialias:!0});a.setSize(e.current.clientWidth,e.current.clientHeight),a.setClearColor(new B.Q1f("rgb(240, 238, 231)")),e.current.appendChild(a.domElement),i.position.z=5;var r=new B.YJl;t.add(r);var o=new B.$p8(16777215,.4);t.add(o);var s=new B.ZyN(16777215,.6);s.position.set(5,5,5),t.add(s);var c=new B.HiM(16777215,.4);c.position.set(-5,3,-5),t.add(c);var l=new fn(r),h=new B.zD7,d=null,u=0,m=function(e){u||(u=e);var n=e-u;if(n>=50){var o=h.getElapsedTime(),s=o%60,c=s>=25,f=c?1:s/25;if(l.update(f),c){var p=o-25;r.rotation.z=.025*p,r.rotation.x=0,r.rotation.y=0}else r.rotation.x=0,r.rotation.y=0,r.rotation.z=0;a.render(t,i),u=e-n%50}d=requestAnimationFrame(m)};d=requestAnimationFrame(m);var f=function(){e.current&&(i.aspect=e.current.clientWidth/e.current.clientHeight,i.updateProjectionMatrix(),a.setSize(e.current.clientWidth,e.current.clientHeight))};return window.addEventListener("resize",f),n.current=function(){window.removeEventListener("resize",f),null!==d&&(cancelAnimationFrame(d),d=null),l.dispose(),t.traverse((function(e){e instanceof B.eaF?(e.geometry.dispose(),Array.isArray(e.material)?e.material.forEach((function(e){return e.dispose()})):e.material.dispose()):e instanceof B.N1A&&(e.geometry.dispose(),e.material.dispose())})),t.clear(),o.dispose(),s.dispose(),c.dispose(),a.dispose(),a.forceContextLoss(),e.current&&a.domElement.parentNode&&e.current.removeChild(a.domElement),n.current=null},function(){n.current&&n.current()}}}),[]),(0,i.jsx)("div",{ref:e,style:{width:"100%",height:"100vh"}})};pn.metadata={themes:"world as sacred, accept things as they are, time for everything",visualization:"Branches grow and recede in their own time, accepting their natural cycles",promptSuggestion:"1. Enhance fractal complexity\n2. Add more growth patterns\n3. Develop stronger divisions\n4. Create clearer evolution\n5. Increase sense of emergence"};const gn=pn;const vn=function(){var e=(0,x.useRef)(null),n=(0,x.useRef)(null),t=(0,x.useRef)([]),a=(0,x.useRef)(null),r=(0,x.useRef)(0);return(0,x.useEffect)((function(){var i=e.current;if(i){var o=i.getContext("2d");a.current=o;var s=i.width=550,c=i.height=550,l=s/2,h=c/2,d=2.4,u=[];t.current=u;for(var m=0;m<3e4;m++){var f=Math.random()*Math.PI*2,p=Math.pow(Math.random(),.5)*d*.5*150,g=(2*Math.random()-1)*d*.3,v=f,y=p/150,x=.03*Math.sin(2*v+2*g),A=.03*Math.cos(2*v-2*g),w=.5*(Math.sin(g*Math.PI)+1),b=x*w+A*(1-w),M=p*Math.cos(f),C=p*Math.sin(f),E=.1*Math.pow(Math.min(1,y/1.92),4);u.push({x:l+M+M*b-M*E,y:h+C+C*b-C*E,z:g,initialR:p,initialTheta:f,initialHeight:g})}var R=0;return n.current=requestAnimationFrame((function e(t){R||(R=t);var i=t-R;i>=100&&(r.current+=5e-4,o.fillStyle="rgba(240, 238, 230, 0.05)",o.fillRect(0,0,s,c),u.forEach((function(e){var n=e.x-l,t=e.y-h,i=Math.sqrt(n*n+t*t)/150,a=Math.atan2(t,n),s=e.z/.96,c=.015*Math.sin(2*a-.5*r.current+2*s),d=.015*Math.cos(2*a+.5*r.current-2*s),u=.5*(Math.sin(s*Math.PI)+1),m=c*u+d*(1-u),f=.1*Math.pow(Math.min(1,i/1.92),4);e.x=e.x+n*m-n*f,e.y=e.y+t*m-t*f,e.z=e.z+.01*Math.sin(.15*r.current+2*i);var p=1+.5*e.z,g=.35*p,v=Math.max(.001,.6*p);o.beginPath(),o.arc(e.x,e.y,v,0,2*Math.PI),o.fillStyle="rgba(51, 51, 51, ".concat(g,")"),o.fill()})),R=t-i%100),n.current=requestAnimationFrame(e)})),function(){n.current&&(cancelAnimationFrame(n.current),n.current=null),a.current&&e.current&&a.current.clearRect(0,0,e.current.width,e.current.height),e.current&&(e.current.width=0,e.current.height=0),t.current&&(t.current.length=0,t.current=null),r.current=0,a.current=null}}}),[]),(0,i.jsx)("div",{style:{width:"550px",height:"550px",margin:"auto",backgroundColor:"#F0EEE6",overflow:"hidden"},children:(0,i.jsx)("canvas",{ref:e,style:{display:"block",width:"550px",height:"550px"}})})};const yn=function(){var e=(0,x.useRef)(null);return(0,x.useEffect)((function(){var n=e.current;if(n){for(var t=n.getContext("2d"),i=n.width=550,a=n.height=550,r=16*Math.sqrt(3),o=[],s=-8;s<=8;s++)for(var c=s%2==0?0:14.4,l=-8;l<=8;l++){var h=i/2+28.8*l+c,d=a/2+s*r*.75,u=.2*(s+l);o.push({x:h,y:d,phase:u})}var m,f=0;return function e(){t.fillStyle="#F0EEE6",t.fillRect(0,0,i,a),f+=.008;var n=.15*Math.sin(.1*f);o.forEach((function(e){var r=e.x-i/2,o=e.y-a/2,s=Math.sqrt(r*r+o*o),c=Math.sin(1*f+e.phase+.06*s),l=.8+.25*c,h=.25*c+n;!function(e,n,i,a){t.save(),t.translate(e,n),t.rotate(a),t.beginPath();for(var r=0;r<=6;r++){var o=r/6*Math.PI*2+Math.PI/6,s=i,c=Math.cos(o)*s,l=Math.sin(o)*s;0===r?t.moveTo(c,l):t.lineTo(c,l)}t.closePath(),t.strokeStyle="rgba(0, 0, 0, 0.22)",t.lineWidth=1,t.stroke(),t.restore()}(e.x,e.y,16*l,h)})),m=requestAnimationFrame(e)}(),function(){m&&(cancelAnimationFrame(m),m=void 0),t&&t.clearRect(0,0,i,a),o&&(o.length=0),f=0}}}),[]),(0,i.jsx)("div",{style:{width:"550px",height:"550px",margin:"auto",backgroundColor:"#F0EEE6",overflow:"hidden"},children:(0,i.jsx)("canvas",{ref:e,style:{display:"block",width:"550px",height:"550px"}})})};var xn=function(e,n,t){if(t||2===arguments.length)for(var i,a=0,r=n.length;a<r;a++)!i&&a in n||(i||(i=Array.prototype.slice.call(n,0,a)),i[a]=n[a]);return e.concat(i||Array.prototype.slice.call(n))};const An=function(){var e=(0,x.useRef)(null),n=(0,x.useState)({x:0,y:0}),t=(n[0],n[1],(0,x.useRef)({x:0,y:0}));return(0,x.useEffect)((function(){var n=e.current;if(n){var i=n.getContext("2d");n.width=550,n.height=550;for(var a=0,r=[],o=null,s=0;s<60;s++){var c=s/60,l=400*(c-.5),h=50;h=c<.05?80*(1+4*(.05-c)):c<.15?80-300*(c-.05):c<.4?50+60*Math.sin((c-.15)*Math.PI/.25):c<.75?110-30*Math.cos((c-.4)*Math.PI/.35):c<.9?80-200*(c-.75):50+100*(c-.9);for(var d=0;d<50;d++){var u=d/50*Math.PI*2;r.push({originalX:Math.cos(u)*h,originalY:l,originalZ:Math.sin(u)*h,x:Math.cos(u)*h,y:l,z:Math.sin(u)*h,vx:0,vy:0,vz:0,dispersing:!1,disperseTime:0})}}var m=function(e){var i=n.getBoundingClientRect(),a=e.clientX-i.left,r=e.clientY-i.top;t.current={x:a,y:r}},f=function(){};return n.addEventListener("mousemove",m),window.addEventListener("resize",f),function e(){i.fillStyle="#F0EEE6",i.fillRect(0,0,n.width,n.height);var s=.15*(a+=.005),c=[];r.forEach((function(e){var n=p(e.x,e.y,e.z,s),i=Math.hypot(n.x-t.current.x,n.y-t.current.y);i<120&&!e.dispersing&&c.push({node:e,distance:i})})),c.sort((function(e,n){return e.distance-n.distance})),c.slice(0,5).forEach((function(e){var n=e.node;n.dispersing=!0,n.disperseTime=0;var t=Math.atan2(n.z,n.x),i=n.y/200;n.vx=1.5*Math.cos(t),n.vy=1*i,n.vz=1.5*Math.sin(t)})),r.forEach((function(e){e.dispersing&&(e.disperseTime+=.01,e.x+=e.vx,e.y+=e.vy,e.z+=e.vz,e.vy+=.04,e.vx*=.96,e.vy*=.96,e.vz*=.96,(e.disperseTime>4||Math.abs(e.x)>300||Math.abs(e.z)>300)&&(e.dispersing=!1,e.x=e.originalX,e.y=e.originalY,e.z=e.originalZ,e.vx=0,e.vy=0,e.vz=0))})),xn([],r,!0).sort((function(e,n){var t=p(e.x,e.y,e.z,s);return p(n.x,n.y,n.z,s).z-t.z})).forEach((function(e){var n=p(e.x,e.y,e.z,s),t=(n.z,e.dispersing?.3*n.scale*(1-e.disperseTime/5):.6*n.scale),a=.5+.25*n.scale;i.fillStyle="rgba(68, 68, 68, ".concat(t,")"),i.beginPath(),i.arc(n.x,n.y,a,0,2*Math.PI),i.fill()}));var l=n.height/2,h=i.createRadialGradient(n.width/2,l,0,n.width/2,l,80);h.addColorStop(0,"rgba(240, 238, 230, 0.8)"),h.addColorStop(1,"rgba(240, 238, 230, 0)"),i.fillStyle=h,i.beginPath(),i.ellipse(n.width/2,l,80,20,0,0,2*Math.PI),i.fill(),o=requestAnimationFrame(e)}(),function(){o&&cancelAnimationFrame(o),n.removeEventListener("mousemove",m),window.removeEventListener("resize",f),i&&i.clearRect(0,0,n.width,n.height),r.length=0,t.current=null}}function p(e,t,i,a){var r=t*Math.cos(a)-i*Math.sin(a),o=t*Math.sin(a)+i*Math.cos(a),s=400/(400+o);return{x:e*s+n.width/2,y:r*s+n.height/2,z:o,scale:s}}}),[]),(0,i.jsx)("div",{className:"flex items-center justify-center w-full h-full",children:(0,i.jsx)("div",{className:"w-[550px] h-[550px] shadow-lg rounded-lg overflow-hidden",children:(0,i.jsx)("canvas",{ref:e,className:"w-full h-full cursor-pointer"})})})};const wn=function(){var e=(0,x.useRef)(null);return(0,x.useEffect)((function(){var n=e.current;if(n){var t=n.getContext("2d");n.width=550,n.height=550;var i=0,a=(1+Math.sqrt(5))/2,r=null,o=function(){t.fillStyle="#F0EEE6",t.fillRect(0,0,n.width,n.height),t.save(),t.translate(n.width/2,n.height/2);for(var e=Math.min(60,Math.floor(.02*i%80)),s=300,c=s/a,l=1,h=25e-5*i,d=0;d<e;d++){t.save();var u=.174533*d,m=100*l,f=Math.cos(u)*m,p=Math.sin(u)*m;t.translate(f,p),t.rotate(u+h);var g=.5-.01*d;t.strokeStyle="rgba(83, 81, 70, ".concat(g,")"),t.lineWidth=.8,t.strokeRect(-s/2,-c/2,s,c),d%3==0&&(t.beginPath(),t.moveTo(-s/2,-c/2),t.lineTo(s/2,c/2),t.moveTo(s/2,-c/2),t.lineTo(-s/2,c/2),t.strokeStyle="rgba(50, 50, 50, ".concat(.2*g,")"),t.lineWidth=.5,t.stroke()),t.restore(),s*=.95,c*=.95,l*=.98}t.beginPath();for(d=0;d<=e;d++){u=.174533*d,m=100*Math.pow(.98,d),f=Math.cos(u)*m,p=Math.sin(u)*m;0===d?t.moveTo(f,p):t.lineTo(f,p)}t.strokeStyle="rgba(150, 150, 150, 0.3)",t.lineWidth=1,t.stroke(),t.restore(),i+=.75,r=requestAnimationFrame(o)};return o(),function(){r&&cancelAnimationFrame(r),t&&t.clearRect(0,0,n.width,n.height)}}}),[]),(0,i.jsx)("div",{className:"flex items-center justify-center w-full h-full",children:(0,i.jsx)("div",{className:"w-[550px] h-[550px] bg-[#F0EEE6]",children:(0,i.jsx)("canvas",{ref:e,className:"w-full h-full"})})})};const bn=function(){var e=(0,x.useRef)(null);return(0,x.useEffect)((function(){var n=e.current;if(n){var t=n.getContext("2d");n.width=550,n.height=550;var i=0,a=function(e,n,i,a){t.beginPath(),t.arc(e,n,i,0,2*Math.PI),t.strokeStyle="rgba(80, 80, 80, ".concat(a,")"),t.lineWidth=1,t.stroke()},r=function(){t.fillStyle="#F0EEE6",t.fillRect(0,0,n.width,n.height),i+=.005;var e=n.width/2,s=n.height/2;a(e,s,40,.48);for(var c=1;c<=4;c++)for(var l=6*c,h=40*c*Math.sqrt(3),d=0;d<l;d++){var u=d/l*Math.PI*2,m=e+Math.cos(u)*h,f=s+Math.sin(u)*h,p=.12*Math.sin(2*i+.5*c+.1*d);a(m,f,40,.36-.06*c+p)}var g=80;for(d=0;d<6;d++){u=d/6*Math.PI*2;var v=e+Math.cos(u)*g,y=s+Math.sin(u)*g,x=e+Math.cos(u+Math.PI)*g,A=s+Math.sin(u+Math.PI)*g;t.beginPath(),t.arc(v,y,g,0,2*Math.PI),t.arc(x,A,g,0,2*Math.PI),t.strokeStyle="rgba(80, 80, 80, 0.12)",t.stroke()}t.save(),t.translate(e,s),t.rotate(.1*i);for(d=0;d<12;d++){u=d/12*Math.PI*2;t.beginPath(),t.moveTo(0,0),t.lineTo(200*Math.cos(u),200*Math.sin(u)),t.strokeStyle="rgba(80, 80, 80, 0.06)",t.stroke()}t.restore(),o=requestAnimationFrame(r)},o=requestAnimationFrame(r);return function(){cancelAnimationFrame(o),t&&t.clearRect(0,0,n.width,n.height)}}}),[]),(0,i.jsx)("div",{className:"flex items-center justify-center w-full h-full",style:{backgroundColor:"#F0EEE6"},children:(0,i.jsx)("div",{className:"w-[550px] h-[550px] shadow-lg rounded-lg overflow-hidden",children:(0,i.jsx)("canvas",{ref:e,className:"w-full h-full"})})})};const Mn=function(){var e=(0,x.useRef)(null),n=(0,x.useRef)(null);return(0,x.useEffect)((function(){var t=e.current;if(t){for(var i=t.getContext("2d"),a=t.width=550,r=t.height=550,o=a/2,s=r/2,c=.35*a,l=[],h=0;h<3e4;h++){var d=h<15e3?"dark":"light",u=Math.random()*Math.PI*2,m=Math.sqrt(Math.random())*c,f=u;f="dark"===d?u<Math.PI?u:u-Math.PI:u>=Math.PI?u:u+Math.PI;var p=o+Math.cos(f)*m,g=s+Math.sin(f)*m;l.push({x:p,y:g,side:d,initialAngle:f,initialRadius:m,convergencePhase:Math.random()*Math.PI*2,convergenceSpeed:.005+.005*Math.random(),size:.3+.4*Math.random(),targetX:p,targetY:g,transitionPhase:0,transitionSpeed:.004+.002*Math.random()})}var v=0,y=!0,x=0,A=1e3/15;return function e(t){if(y){if(x||(x=t),t-x>A){v+=.008,x=t,i.fillStyle="rgba(240, 238, 230, 0.1)",i.fillRect(0,0,a,r),l.forEach((function(e){e.convergencePhase+=e.convergenceSpeed,e.transitionPhase+=e.transitionSpeed;var n=Math.sin(e.convergencePhase),t=n>0;if(t){var a=n;e.targetX=o,e.targetY=s;var r=.02*a*(Math.sqrt(Math.pow(e.x-o,2)+Math.pow(e.y-s,2))/c);e.x+=(e.targetX-e.x)*r,e.y+=(e.targetY-e.y)*r}else{var l=Math.abs(n),h=void 0,d=void 0;e.side,h=e.initialAngle+Math.PI,d=e.initialRadius;var u=h+Math.sin(2*h)*c*.5/d*l;e.targetX=o+Math.cos(u)*d,e.targetY=s+Math.sin(u)*d;r=.03*l;e.x+=(e.targetX-e.x)*r,e.y+=(e.targetY-e.y)*r}var m,f,p=e.x-o,g=e.y-s;Math.atan2(g,p),Math.PI,Math.PI,Math.PI;if(t)m="dark"===e.side?"20, 20, 20":"90, 90, 90",f=.3*n;else{var v=Math.abs(n);m="dark"===e.side?"".concat(20+70*v,", ").concat(20+70*v,", ").concat(20+70*v):"".concat(90-70*v,", ").concat(90-70*v,", ").concat(90-70*v),f=.3*v}var y=Math.sqrt(Math.pow(e.x-o,2)+Math.pow(e.y-s,2));y<.2*c&&(f+=.2*(1-y/(.2*c))),i.beginPath(),i.arc(e.x,e.y,e.size,0,2*Math.PI),i.fillStyle="rgba(".concat(m,", ").concat(f,")"),i.fill()}));var h=.5*Math.sin(.1*v)+.5;i.beginPath(),i.arc(o,s,2+3*h,0,2*Math.PI),i.fillStyle="rgba(51, 51, 51, ".concat(.1+.2*h,")"),i.fill()}n.current=requestAnimationFrame(e)}}(),function(){y=!1,n.current&&(cancelAnimationFrame(n.current),n.current=null),t&&i&&i.clearRect(0,0,t.width,t.height),l.length=0}}}),[]),(0,i.jsx)("div",{style:{width:"550px",height:"550px",margin:"auto",backgroundColor:"#F0EEE6",overflow:"hidden"},children:(0,i.jsx)("canvas",{ref:e,style:{display:"block",width:"550px",height:"550px"}})})};const Cn=function(){var e=(0,x.useRef)(null);return(0,x.useEffect)((function(){if(e.current){var n=new B.Z58;n.background=new B.Q1f("#F0EEE6");var t=new B.ubm(75,1,.1,1e3);t.position.z=16,t.position.y=0;var i=new D.JeP({antialias:!0});i.setSize(550,550),e.current.appendChild(i.domElement);for(var a=new B.YJl,r=1e4,o=new B.LoY,s=new Float32Array(3e4),c=new Float32Array(3e4),l=new Float32Array(r),h=0;h<r;h++){var d=.3*(40*(w=h/r))+.2*Math.random(),u=w*Math.PI*40,m=void 0;m=w<.3?2.5*Math.sin(w*Math.PI/.3):w<.5?2.5-1.5*Math.sin((w-.3)*Math.PI/.2):w<.7?1+2*Math.sin((w-.5)*Math.PI/.2):3-1*Math.sin((w-.7)*Math.PI/.3),m+=.1*(Math.random()-.5);var f=16*w-8,p=Math.cos(u)*m,g=Math.sin(u)*m;s[3*h]=p,s[3*h+1]=f,s[3*h+2]=g,c[3*h]=0,c[3*h+1]=0,c[3*h+2]=0,l[h]=.03*Math.random()+.01}o.setAttribute("position",new B.THS(s,3)),o.setAttribute("color",new B.THS(c,3)),o.setAttribute("size",new B.THS(l,1));var v=new B.BH$({size:.02,vertexColors:!0,transparent:!0,opacity:.8,sizeAttenuation:!0,blending:B.NTi}),y=new B.ONl(o,v);a.add(y);var x=new B.mrM({color:"#888888",transparent:!0,opacity:.3}),A=[];for(h=0;h<200;h++){var w;d=(w=h/200)*Math.PI*16,f=16*w-8,m=void 0;m=w<.3?2.5*Math.sin(w*Math.PI/.3):w<.5?2.5-1.5*Math.sin((w-.3)*Math.PI/.2):w<.7?1+2*Math.sin((w-.5)*Math.PI/.2):3-1*Math.sin((w-.7)*Math.PI/.3),A.push(new B.Pq0(Math.cos(d)*m,f,Math.sin(d)*m))}var b=(new B.LoY).setFromPoints(A),M=new B.N1A(b,x);a.add(M),n.add(a);var C,E=0;return function e(){C=requestAnimationFrame(e),E+=.005,a.rotation.y=.45*E,a.rotation.x=.05*Math.sin(.25*E),a.rotation.z=.03*Math.cos(.35*E);var r=1+.02*Math.sin(.25*E);a.scale.set(r,r,r);for(var o=y.geometry.attributes.position.array,s=0;s<o.length;s+=3)o[s]+=5e-5*Math.sin(E+s),o[s+1]+=5e-5*Math.cos(E+s),o[s+2]+=5e-5*Math.sin(E+s+1);y.geometry.attributes.position.needsUpdate=!0,i.render(n,t)}(),function(){C&&(cancelAnimationFrame(C),C=void 0),o&&o.dispose(),b&&b.dispose(),v&&v.dispose(),x&&x.dispose(),a&&(y&&a.remove(y),M&&a.remove(M),n.remove(a)),n&&n.clear(),i&&(e.current&&i.domElement&&e.current.contains(i.domElement)&&e.current.removeChild(i.domElement),i.dispose(),i.forceContextLoss()),s&&s.fill(0),c&&c.fill(0),l&&l.fill(0),A&&(A.length=0),E=0}}}),[]),(0,i.jsx)("div",{className:"w-full h-screen flex justify-center items-center bg-[#F0EEE6]",children:(0,i.jsx)("div",{ref:e,className:"w-[550px] h-[550px]"})})};const En=function(){var e=(0,x.useRef)(null),n=(0,x.useRef)(null);return(0,x.useEffect)((function(){var t=e.current;if(t){for(var i=t.getContext("2d"),a=t.width=550,r=t.height=550,o=[],s=0;s<3e4;s++)o.push({x:Math.random()*a,y:Math.random()*r,z:2*Math.random()-1,targetX:0,targetY:0,phase:Math.random()*Math.PI*2,speed:.001+.002*Math.random()});var c=0,l=!0;return function e(){if(l){c+=.016,i.fillStyle="rgba(240, 238, 230, 0.05)",i.fillRect(0,0,a,r);var t=[{cx:275,cy:200,r:150},{cx:275,cy:350,r:150},{cx:275,cy:275,r:75}];o.forEach((function(e){e.z=.5*Math.sin(.5*c+e.phase);var n=1/0;t.forEach((function(t){var i=Math.atan2(e.y-t.cy,e.x-t.cx),a=t.cx+t.r*Math.cos(i),r=t.cy+t.r*Math.sin(i),o=Math.sqrt(Math.pow(e.x-a,2)+Math.pow(e.y-r,2));o<n&&(n=o,e.targetX=a,e.targetY=r)}));var a=.5+.5*e.z;e.x+=(e.targetX-e.x)*e.speed*a,e.y+=(e.targetY-e.y)*e.speed*a,e.x+=.1*Math.sin(c+e.phase),e.y+=.1*Math.cos(c+e.phase);var r=.3+.2*e.z,o=.4+.3*e.z;i.beginPath(),i.arc(e.x,e.y,Math.max(.1,r),0,2*Math.PI),i.fillStyle="rgba(51, 51, 51, ".concat(Math.max(.1,o),")"),i.fill()})),n.current=requestAnimationFrame(e)}}(),function(){l=!1,n.current&&(cancelAnimationFrame(n.current),n.current=null),t&&i&&i.clearRect(0,0,t.width,t.height),o.length=0,c=0}}}),[]),(0,i.jsx)("div",{style:{width:"550px",height:"550px",margin:"auto",backgroundColor:"#f0eee6"},children:(0,i.jsx)("canvas",{ref:e,style:{display:"block",width:"550px",height:"550px"}})})};const Rn=function(){var e=(0,x.useRef)(null);return(0,x.useEffect)((function(){if(e.current){var n=new B.Z58;n.background=new B.Q1f("#F0EEE6");var t=new B.ubm(75,1,.1,1e3);t.position.z=10,t.position.y=0;var i=new D.JeP({antialias:!0});i.setSize(550,550),e.current.appendChild(i.domElement);for(var a=new B.YJl,r=[],o=[],s=[],c=[],l=45,h=12,d=[],u=0;u<l;u++){var m=u/l*18-9,f=void 0;f=u<10?u/10*3.5*.7:u<35?2.45+1.8*Math.sin((u-10)/25*Math.PI):2*Math.sin((l-u)/10*Math.PI*.5);for(var p=1-u/l*.3,g=0;g<h;g++){var v=g/h*Math.PI*2+.2*u,y=Math.cos(v)*f*p,x=Math.sin(v)*f*p;d.push(new B.Pq0(y,m,x));var A=new B.Gu$(.03,8,8);r.push(A);var w=new B.V9B({color:"#888888"});o.push(w);var b=new B.eaF(A,w);s.push(b),b.position.set(y,m,x),a.add(b)}}var M=new B.mrM({color:"#999999",transparent:!0,opacity:.3});o.push(M);for(u=0;u<l;u++){var C=u*h,E=[];for(g=0;g<h;g++)E.push(d[C+g]);E.push(d[C]);var R=(new B.LoY).setFromPoints(E);r.push(R);var P=new B.N1A(R,M);c.push(P),a.add(P)}for(g=0;g<h;g++){for(E=[],u=0;u<l;u++)E.push(d[u*h+g]);R=(new B.LoY).setFromPoints(E);r.push(R);P=new B.N1A(R,M);c.push(P),a.add(P)}for(u=0;u<44;u++)for(g=0;g<h;g++){var F=(u+1)*h+(g+1)%h;E=[d[u*h+g],d[F]],R=(new B.LoY).setFromPoints(E);r.push(R);P=new B.N1A(R,M);c.push(P),a.add(P)}a.rotation.z=Math.PI,n.add(a);var S,I=0;return function e(){S=requestAnimationFrame(e),I+=.00125,a.rotation.y+=.00125;var r=1+.02*Math.sin(.0625*I);a.scale.set(r,r,r),i.render(n,t)}(),function(){cancelAnimationFrame(S),r.forEach((function(e){e.dispose()})),o.forEach((function(e){e.dispose()})),s.forEach((function(e){a.remove(e)})),c.forEach((function(e){a.remove(e)})),n.remove(a),r.length=0,o.length=0,s.length=0,c.length=0,d.length=0,n.clear(),i.dispose(),i.forceContextLoss(),e.current&&i.domElement.parentNode&&e.current.removeChild(i.domElement)}}}),[]),(0,i.jsx)("div",{className:"w-full h-screen flex justify-center items-center bg-[#F0EEE6]",children:(0,i.jsx)("div",{ref:e,className:"w-[550px] h-[550px]"})})};const Pn=function(){var e=(0,x.useRef)(null),n=(0,x.useRef)(null),t=(0,x.useRef)(null),a=(0,x.useRef)(null),r=(0,x.useRef)(null),o=(0,x.useRef)([]),s=(0,x.useRef)({geometries:[],materials:[],meshes:[],lineSegments:[],scaleGroups:[],instancedMeshes:[]});return(0,x.useEffect)((function(){if(e.current){var i=s.current,c=i.geometries,l=i.materials,h=(i.meshes,i.lineSegments),d=(i.scaleGroups,i.instancedMeshes),u=new B.Z58;n.current=u,u.background=new B.Q1f("#F0EEE6");var m=new B.ubm(75,1,.1,1e3);a.current=m,m.position.z=16,m.position.y=0;var f=new D.JeP({antialias:!0});t.current=f,f.setSize(550,550),e.current.appendChild(f.domElement);var p=new B.$p8(16777215,.6);u.add(p),o.current.push(p);var g=new B.ZyN(16777215,.4);g.position.set(5,10,7.5),u.add(g),o.current.push(g);var v=new B.YJl;r.current=v;var y=new B.ypk;y.moveTo(0,0),y.lineTo(.7,.7),y.lineTo(.5,1.4),y.lineTo(0,1.7),y.lineTo(-.5,1.4),y.lineTo(-.7,.7),y.closePath();var x=new B.QCA(y,{depth:.05,bevelEnabled:!0,bevelSegments:4,steps:1,bevelSize:.02,bevelThickness:.02});c.push(x);var A=new B.uSd({color:"#e0ded8",transparent:!0,opacity:.15,roughness:.1,metalness:0,transmission:.6,thickness:.1,side:B.$EB});l.push(A);var w=new B.mrM({color:"#666666",transparent:!0,opacity:.3});l.push(w);var b=new B.TDQ(x);c.push(b);var M=38,C=new B.ZLX(x,A,304);d.push(C);for(var E=0,R=new B.kn4,P=new B.Pq0,F=new B.O9p,S=new B.PTz,I=new B.Pq0,T=0;T<M;T++){var k=T/M*18-9-.9,z=void 0;z=T<10?2*Math.sin(T/10*Math.PI*.5):2+2.5*Math.sin((T-10)/28*Math.PI);for(var j=1-T/M*.3,L=0;L<8;L++){var H=L/8*Math.PI*2+.25*T;P.set(Math.cos(H)*z*j,k,Math.sin(H)*z*j),F.set(Math.PI/3,H,0),S.setFromEuler(F),I.set(.8,.8,.8),R.compose(P,S,I),C.setMatrixAt(E,R),E++}}C.instanceMatrix.needsUpdate=!0,v.add(C);var Y=new B.YJl;for(T=0;T<M;T++){k=T/M*18-9-.9,z=void 0;z=T<10?2*Math.sin(T/10*Math.PI*.5):2+2.5*Math.sin((T-10)/28*Math.PI);for(j=1-T/M*.3,L=0;L<8;L++){H=L/8*Math.PI*2+.25*T;var X=new B.DXC(b,w);X.rotation.x=Math.PI/3,X.rotation.y=H,X.position.x=Math.cos(H)*z*j,X.position.z=Math.sin(H)*z*j,X.position.y=k,X.scale.set(.8,.8,.8),h.push(X),Y.add(X)}}v.add(Y),u.add(v);var W,q=0;return function e(){W=requestAnimationFrame(e),q+=.005,v.rotation.y=.3*q,v.rotation.x=.05*Math.sin(.5*q),v.rotation.z=.03*Math.cos(.7*q);var n=1+.02*Math.sin(.5*q);v.scale.set(n,n,n),f.render(u,m)}(),function(){W&&cancelAnimationFrame(W),n.current&&(o.current.forEach((function(e){n.current.remove(e)})),r.current&&n.current.remove(r.current)),s.current.scaleGroups.forEach((function(e){for(r.current&&r.current.remove(e);e.children.length>0;)e.remove(e.children[0])})),s.current.meshes.forEach((function(e){e.parent&&e.parent.remove(e)})),s.current.lineSegments.forEach((function(e){e.parent&&e.parent.remove(e)})),s.current.instancedMeshes.forEach((function(e){e.parent&&e.parent.remove(e),e.dispose()})),s.current.geometries.forEach((function(e){e.dispose()})),s.current.materials.forEach((function(e){e.dispose()})),t.current&&(t.current.dispose(),e.current&&t.current.domElement&&e.current.removeChild(t.current.domElement)),n.current=null,t.current=null,a.current=null,r.current=null,o.current=[],s.current={geometries:[],materials:[],meshes:[],lineSegments:[],scaleGroups:[],instancedMeshes:[]}}}}),[]),(0,i.jsx)("div",{className:"w-full h-screen flex justify-center items-center bg-[#F0EEE6]",children:(0,i.jsx)("div",{ref:e,className:"w-[550px] h-[550px]"})})};const Fn=function(){var e=(0,x.useRef)(null);return(0,x.useEffect)((function(){var n=e.current;if(n){var t=n.getContext("2d");if(t){n.width=550,n.height=550;var i,a=0,r=function(){t.fillStyle="#F0EEE6",t.fillRect(0,0,n.width,n.height),a+=.008;for(var e=n.width/2,o=n.height/2,s=0;s<40;s++)for(var c=s/40*Math.PI*2,l=0;l<40;l++){var h=l/40*Math.PI*2,d=(120+60*Math.cos(h))*Math.cos(c),u=(120+60*Math.cos(h))*Math.sin(c),m=200/(200+60*Math.sin(h)),f=e+d*m,p=o+u*m*.5,g=a+.5*c+.5*h,v=5*Math.sin(g);t.beginPath(),t.arc(f+v,p+v,1,0,2*Math.PI),t.fillStyle="rgba(80, 80, 80, ".concat(.3*m,")"),t.fill()}for(s=0;s<20;s++){var y=s/20*Math.PI*2;t.beginPath(),t.strokeStyle="rgba(80, 80, 80, 0.2)",t.lineWidth=1;for(var x=0;x<1;x+=.01){var A=120+60*Math.cos(x*Math.PI*2*3+a);d=e+Math.cos(y+x*Math.PI*4)*A,u=o+Math.sin(y+x*Math.PI*4)*A*.5;0===x?t.moveTo(d,u):t.lineTo(d,u)}t.stroke()}t.beginPath(),t.arc(e,o,30,0,2*Math.PI),t.strokeStyle="rgba(80, 80, 80, 0.5)",t.lineWidth=2,t.stroke(),t.beginPath();for(s=0;s<100;s++){y=(x=s/100)*Math.PI*2*3-2*a,A=30*(1-x),d=e+Math.cos(y)*A,u=o+Math.sin(y)*A;0===s?t.moveTo(d,u):t.lineTo(d,u)}t.strokeStyle="rgba(80, 80, 80, 0.4)",t.lineWidth=1,t.stroke();for(var w=50;w<250;w+=30)t.beginPath(),t.arc(e,o,w+5*Math.sin(a+.01*w),0,2*Math.PI),t.strokeStyle="rgba(80, 80, 80, 0.1)",t.lineWidth=1,t.stroke();i=requestAnimationFrame(r)};return r(),function(){cancelAnimationFrame(i),t&&t.clearRect(0,0,n.width,n.height)}}}}),[]),(0,i.jsx)("div",{className:"flex items-center justify-center w-full h-full",style:{backgroundColor:"#F0EEE6"},children:(0,i.jsx)("div",{className:"w-[550px] h-[550px] shadow-lg rounded-lg overflow-hidden",children:(0,i.jsx)("canvas",{ref:e,className:"w-full h-full"})})})};const Sn=function(){var e=(0,x.useRef)(null);return(0,x.useEffect)((function(){var n=e.current,t=n.getContext("2d"),i=n.width,a=n.height;t.fillStyle="#F0EEE6",t.fillRect(0,0,i,a);for(var r={x:i/2,y:a/2,radius:120,vx:2*(2*Math.random()-1),vy:2*(2*Math.random()-1)},o=[],s=0,c=function(e,n){var t=e-r.x,i=n-r.y,a=t*t+i*i;return r.radius*r.radius/a},l=0;l<25e3;l++){var h=Math.random()*i,d=Math.random()*a,u=c(h,d);o.push({x:h,y:d,z:2*Math.random()-1,size:.5+1.5*Math.random(),field:u,active:u>1,targetX:h,targetY:d,originalX:h,originalY:d,phase:Math.random()*Math.PI*2})}var m=0,f=1e3/18,p=null,g=function(e){m||(m=e);var n=e-m;if(n>=f){t.fillStyle="#F0EEE6",t.fillRect(0,0,i,a),s+=5e-4,r.x+=r.vx,r.y+=r.vy,r.x-r.radius<0&&(r.x=r.radius,r.vx=Math.abs(r.vx)*(.9+.2*Math.random()),r.vy+=.5*(2*Math.random()-1)),r.x+r.radius>i&&(r.x=i-r.radius,r.vx=-Math.abs(r.vx)*(.9+.2*Math.random()),r.vy+=.5*(2*Math.random()-1)),r.y-r.radius<0&&(r.y=r.radius,r.vy=Math.abs(r.vy)*(.9+.2*Math.random()),r.vx+=.5*(2*Math.random()-1)),r.y+r.radius>a&&(r.y=a-r.radius,r.vy=-Math.abs(r.vy)*(.9+.2*Math.random()),r.vx+=.5*(2*Math.random()-1));var l=.75,h=Math.sqrt(r.vx*r.vx+r.vy*r.vy);h<l&&(r.vx=r.vx/h*l,r.vy=r.vy/h*l);var d=Math.min(1,1.5/h);r.vx*=d,r.vy*=d;for(var u=0,v=o;u<v.length;u++){var y=v[u],x=c(y.x,y.y),A=y.active;if(y.active=x>1,y.active!==A)if(y.active){var w=Math.random()*Math.PI*2,b=5+10*Math.random();y.targetX=y.x+Math.cos(w)*b,y.targetY=y.y+Math.sin(w)*b}else y.targetX=y.originalX,y.targetY=y.originalY;if(y.active){w=Math.atan2(y.y-r.y,y.x-r.x);var M=Math.sqrt((y.x-r.x)*(y.x-r.x)+(y.y-r.y)*(y.y-r.y)),C=w+Math.PI/2,E=.25*(1-M/r.radius);y.x+=Math.cos(C)*E,y.y+=Math.sin(C)*E;var R=.2*Math.sin(2*s+y.phase);y.x+=Math.cos(w)*R,y.y+=Math.sin(w)*R,c(y.x,y.y)<1&&(y.x=y.x+.1*(r.x-y.x),y.y=y.y+.1*(r.y-y.y))}else{if(Math.sqrt((y.targetX-y.x)*(y.targetX-y.x)+(y.targetY-y.y)*(y.targetY-y.y))>100||Math.random()<.001){var P=r.x-y.x,F=r.y-y.y;(b=Math.sqrt(P*P+F*F))<200+100*Math.random()&&(y.targetX=r.x+100*(2*Math.random()-1),y.targetY=r.y+100*(2*Math.random()-1))}y.x+=.01*(y.targetX-y.x),y.y+=.01*(y.targetY-y.y)}y.x+=.1*Math.sin(.3*s+.01*y.y),y.y+=.1*Math.cos(.3*s+.01*y.x),y.x<0&&(y.x=i),y.x>i&&(y.x=0),y.y<0&&(y.y=a),y.y>a&&(y.y=0);var S=void 0;if(y.active)S=Math.min(.9,.3+.4*x);else{P=y.x-r.x,F=y.y-r.y,b=Math.sqrt(P*P+F*F);S=.05+.2*Math.max(0,1-b/(2.5*r.radius))}t.fillStyle="rgba(51, 51, 51, ".concat(S,")"),t.beginPath(),t.arc(y.x,y.y,y.size,0,2*Math.PI),t.fill()}m=e-n%f}p=requestAnimationFrame(g)};return p=requestAnimationFrame(g),function(){p&&(cancelAnimationFrame(p),p=null),t.clearRect(0,0,i,a),o.length=0}}),[]),(0,i.jsx)("div",{className:"flex justify-center items-center w-full h-full bg-gray-100",children:(0,i.jsx)("canvas",{ref:e,width:550,height:550,className:"shadow-lg"})})};const In=function(){var e=(0,x.useRef)(null),n=(0,x.useRef)(null);return(0,x.useEffect)((function(){var t=e.current;if(t){for(var i=t.getContext("2d"),a=t.width=550,r=t.height=550,o=a/2,s=[],c=0;c<2e4;c++){var l=Math.random()<.5?-1:1,h=Math.floor(8*Math.random()),d=Math.random()*r,u=.008*d,m=.03*d,f=.05*d,p=50*Math.sin(u),g=25*Math.sin(2*m+.5*h),v=12*Math.sin(3*f+1.2*h),y=20+8*h,x=o+l*(80+(p+g+v)+15*h),A=(Math.random()-.5)*y;s.push({x:x+A,y:d,z:20*(h-4)+15*(Math.random()-.5),side:l,layer:h,initialY:d,drift:Math.random()*Math.PI*2,speed:.1+.02*h,brightness:.7+.3*Math.random()})}var w=0;return function e(){w+=.016,i.fillStyle="rgba(240, 238, 230, 0.05)",i.fillRect(0,0,a,r),s.sort((function(e,n){return e.z-n.z})),s.forEach((function(e){var n=.008*e.y+.05*w,t=.03*e.y+.1*w+.5*e.layer,a=.05*e.y+.15*w+1.2*e.layer,s=50*Math.sin(n)+25*Math.sin(2*t)+12*Math.sin(3*a),c=15*e.layer,l=20+8*e.layer,h=o+e.side*(80+s+c),d=Math.sin(e.drift+.5*w+.3*e.layer)*l*.5;e.x=.92*e.x+.08*(h+d),e.y+=e.speed,e.z+=.2*Math.sin(.4*w+e.drift+.8*e.layer),e.y>r+30&&(e.y=-30,e.drift=Math.random()*Math.PI*2);var u=(e.z+80)/160,m=.25+.15*u,f=.3+.3*u,p=120+3*e.layer+15*e.brightness;m>0&&f>0&&(e.layer<3&&(i.beginPath(),i.arc(e.x,e.y,1.5*f,0,2*Math.PI),i.fillStyle="rgba(".concat(p,", ").concat(p,", ").concat(p,", ").concat(.1*m,")"),i.fill()),i.beginPath(),i.arc(e.x,e.y,f,0,2*Math.PI),i.fillStyle="rgba(".concat(p,", ").concat(p,", ").concat(p,", ").concat(m,")"),i.fill())})),n.current=requestAnimationFrame(e)}(),function(){n.current&&(cancelAnimationFrame(n.current),n.current=null),t&&i&&i.clearRect(0,0,a,r),s.length=0}}}),[]),(0,i.jsx)("div",{style:{width:"550px",height:"550px",margin:"auto",backgroundColor:"#F0EEE6",overflow:"hidden"},children:(0,i.jsx)("canvas",{ref:e,style:{display:"block",width:"550px",height:"550px"}})})};const Tn=function(){var e=(0,x.useRef)(null),n=(0,x.useRef)(null);return(0,x.useEffect)((function(){var t=e.current;if(t){for(var i=t.getContext("2d"),a=t.width=550,r=t.height=550,o=a/2,s=[],c=0;c<18e3;c++){var l=Math.floor(12*Math.random()),h=40+3*l,d=25*l,u=Math.random()*r,m=.01*u+.3*l,f=35*Math.sin(m)+18*Math.sin(2*m+.8*l)+8*Math.sin(4*m+1.5*l),p=Math.random()<.5?-1:1,g=o+p*(60+f+d),v=(Math.random()-.5)*h;s.push({x:g+v,y:u,z:30*(l-6)+20*(Math.random()-.5),side:p,stratum:l,flow:Math.random()*Math.PI*2,oscillation:Math.random()*Math.PI*2,velocity:.05+.015*l,brightness:.6+.4*Math.random()})}var y=0,x=!0;return function e(){x&&(y+=.016,i.fillStyle="rgba(240, 238, 230, 0.025)",i.fillRect(0,0,a,r),s.sort((function(e,n){return e.z-n.z})),s.forEach((function(e){var n=.01*e.y+.3*e.stratum+.03*y,t=35*Math.sin(n)+18*Math.sin(2*n+.8*e.stratum)+8*Math.sin(4*n+1.5*e.stratum),a=25*e.stratum,s=40+3*e.stratum,c=o+e.side*(60+t+a),l=Math.sin(e.flow+.6*y+.4*e.stratum)*s*.7;e.x=.94*e.x+.06*(c+l),e.y+=e.velocity,e.y+=.3*Math.sin(e.oscillation+.8*y),e.z+=.25*Math.sin(.3*y+e.flow+.6*e.stratum),e.y>r+40&&(e.y=-40,e.flow=Math.random()*Math.PI*2);var h=(e.z+180)/360,d=.15+.15*h,u=.3+.8*h,m=45+3*e.stratum+15*e.brightness;d>0&&u>0&&(e.stratum%3==0&&(i.beginPath(),i.arc(e.x,e.y,2*u,0,2*Math.PI),i.fillStyle="rgba(".concat(m,", ").concat(m,", ").concat(m,", ").concat(.06*d,")"),i.fill()),i.beginPath(),i.arc(e.x,e.y,u,0,2*Math.PI),i.fillStyle="rgba(".concat(m,", ").concat(m,", ").concat(m,", ").concat(d,")"),i.fill())})),n.current=requestAnimationFrame(e))}(),function(){x=!1,n.current&&(cancelAnimationFrame(n.current),n.current=null),i&&i.clearRect(0,0,a,r),s.length=0,y=0}}}),[]),(0,i.jsx)("div",{style:{width:"550px",height:"550px",margin:"auto",backgroundColor:"#F0EEE6",overflow:"hidden"},children:(0,i.jsx)("canvas",{ref:e,style:{display:"block",width:"550px",height:"550px"}})})};const kn=function(){var e=(0,x.useRef)(null),n=(0,x.useRef)(null);return(0,x.useEffect)((function(){var t=e.current;if(t){for(var i=t.getContext("2d"),a=t.width=550,r=t.height=550,o=a/2,s=[],c=0;c<18e3;c++){var l=Math.random()<.5?-1:1,h=Math.random()*r,d=.01*h,u=60*Math.sin(d)+20*Math.sin(3*d),m=30+15*Math.sin(2*d),f=o+l*(60+u),p=(Math.random()-.5)*m;s.push({x:f+p,y:h,z:100*(Math.random()-.5),side:l,wavePhase:d,initialY:h,drift:Math.random()*Math.PI*2,speed:.1+.3*Math.random()})}var g=0,v=!0,y=0,x=1e3/15;return function e(t){v&&(y||(y=t),t-y>x&&(g+=.008,y=t,i.fillStyle="rgba(240, 238, 230, 0.06)",i.fillRect(0,0,a,r),s.forEach((function(e){var n=.01*e.y+.1*g,t=60*Math.sin(n)+20*Math.sin(3*n),a=30+15*Math.sin(2*n),s=o+e.side*(60+t),c=(Math.sin(e.drift+g)-.5)*a;e.x=.95*e.x+.05*(s+c),e.y+=e.speed,e.z+=.3*Math.sin(.5*g+e.drift),e.y>r+20&&(e.y=-20,e.drift=Math.random()*Math.PI*2);e.z;var l=.4-.0025*Math.abs(e.z),h=.8+.015*e.z;l>0&&h>0&&(i.beginPath(),i.arc(e.x,e.y,2*h,0,2*Math.PI),i.fillStyle="rgba(51, 51, 51, ".concat(.1*l,")"),i.fill(),i.beginPath(),i.arc(e.x,e.y,h,0,2*Math.PI),i.fillStyle="rgba(51, 51, 51, ".concat(l,")"),i.fill())}))),n.current=requestAnimationFrame(e))}(),function(){v=!1,n.current&&(cancelAnimationFrame(n.current),n.current=null),i&&i.clearRect(0,0,a,r),s.length=0,g=0}}}),[]),(0,i.jsx)("div",{style:{width:"550px",height:"550px",margin:"auto",backgroundColor:"#F0EEE6",overflow:"hidden"},children:(0,i.jsx)("canvas",{ref:e,style:{display:"block",width:"550px",height:"550px"}})})};const zn=function(){var e=(0,x.useRef)(null);return(0,x.useEffect)((function(){var n=e.current;if(n){var t=n.getContext("2d");n.width=550,n.height=550;var i=0,a=[],r=function(){function e(e,n,t){this.x=e,this.y=n,this.size=t,this.segments=[],this.angle=0,this.rotationSpeed=.001,this.sides=6,this.baseGrowthPhase=Math.random()*Math.PI*2,this.lastGrowthPhase=0,this.currentGrowthPhase=0,this.generateStructure()}return e.prototype.generateStructure=function(){this.segments=[];for(var e=0;e<5;e++)for(var n=this.size*(.15+.17*e),t=this.sides*(e+1),i=0;i<t;i++){var a=i/t*Math.PI*2,r=0===e?0:this.size*(.15+.17*(e-1));this.segments.push({angle:a,innerRadius:r,outerRadius:n,basePhase:Math.random()*Math.PI*2,currentInnerRadius:r,currentOuterRadius:n,currentAngle:a,branching:e>2&&Math.random()<.2})}},e.prototype.update=function(e){var n=this;this.angle+=5*this.rotationSpeed,this.lastGrowthPhase=this.currentGrowthPhase;var t=.15*Math.sin(.05*e+this.baseGrowthPhase)+.85;this.currentGrowthPhase+=.005*(t-this.currentGrowthPhase),this.segments.forEach((function(t){var i=t.innerRadius*n.currentGrowthPhase,a=t.outerRadius*n.currentGrowthPhase,r=t.angle+.005*Math.sin(.15*e+t.basePhase);t.currentInnerRadius+=.005*(i-t.currentInnerRadius),t.currentOuterRadius+=.005*(a-t.currentOuterRadius),t.currentAngle+=.005*(r-t.currentAngle)}))},e.prototype.draw=function(e){e.save(),e.translate(this.x,this.y),e.rotate(this.angle),e.strokeStyle="rgba(100, 100, 100, 0.2)",e.lineWidth=1;for(var n=0;n<this.sides;n++){var t=n/this.sides*Math.PI*2;e.beginPath(),e.moveTo(0,0),e.lineTo(Math.cos(t)*this.size,Math.sin(t)*this.size),e.stroke()}this.segments.forEach((function(n){var t=Math.cos(n.currentAngle)*n.currentInnerRadius,i=Math.sin(n.currentAngle)*n.currentInnerRadius,a=Math.cos(n.currentAngle)*n.currentOuterRadius,r=Math.sin(n.currentAngle)*n.currentOuterRadius;e.strokeStyle="rgba(60, 60, 60, 0.4)",e.lineWidth=1.5,e.beginPath(),e.moveTo(t,i),e.lineTo(a,r),e.stroke();for(var o=0;o<1;o++){var s=t+.6*(a-t),c=i+.6*(r-i);e.beginPath();for(var l=0;l<=8;l++){var h=l/8*Math.PI*2,d=6*(1+.05*Math.sin(2*h+n.basePhase)),u=s+Math.cos(h)*d,m=c+Math.sin(h)*d;0===l?e.moveTo(u,m):e.lineTo(u,m)}e.strokeStyle="rgba(50, 50, 50, 0.5)",e.lineWidth=.8,e.stroke(),e.beginPath(),e.arc(s,c,1.5,0,2*Math.PI),e.strokeStyle="rgba(50, 50, 50, 0.3)",e.lineWidth=.6,e.stroke()}if(n.branching){var f=n.currentAngle+.2,p=.3*n.currentOuterRadius,g=a+Math.cos(f)*p,v=r+Math.sin(f)*p;e.strokeStyle="rgba(60, 60, 60, 0.3)",e.lineWidth=.8,e.beginPath(),e.moveTo(a,r),e.lineTo(g,v),e.stroke(),e.beginPath(),e.arc(g,v,2.5,0,2*Math.PI),e.strokeStyle="rgba(60, 60, 60, 0.4)",e.stroke()}}));var i=.06*this.size;e.strokeStyle="rgba(80, 80, 80, 0.5)",e.lineWidth=1.5,e.beginPath(),e.arc(0,0,i,0,2*Math.PI),e.stroke(),e.beginPath(),e.arc(0,0,.5*i,0,2*Math.PI),e.strokeStyle="rgba(80, 80, 80, 0.3)",e.lineWidth=1,e.stroke(),e.restore()},e}();!function(){a=[];for(var e=0;e<4;e++){var t=n.width*(.2+.6*Math.random()),i=n.height*(.2+.6*Math.random()),o=70+30*Math.random();a.push(new r(t,i,o))}}();var o=null,s=0,c=1e3/16.7;return o=requestAnimationFrame((function e(r){s||(s=r);var l=r-s;if(l>=c){t.fillStyle="#F0EEE6",t.fillRect(0,0,n.width,n.height),i+=.005,a.forEach((function(e){e.update(i),e.draw(t)}));for(var h=0;h<a.length;h++)for(var d=h+1;d<a.length;d++){var u=a[h],m=a[d],f=Math.sqrt((u.x-m.x)*(u.x-m.x)+(u.y-m.y)*(u.y-m.y));if(f<280){var p=.1*Math.pow(1-f/280,2);t.strokeStyle="rgba(60, 60, 60, ".concat(p,")"),t.lineWidth=.8,t.beginPath(),t.moveTo(u.x,u.y),t.lineTo(m.x,m.y),t.stroke()}}s=r-l%c}o=requestAnimationFrame(e)})),function(){o&&(cancelAnimationFrame(o),o=null),t.clearRect(0,0,n.width,n.height),a.length=0}}}),[]),(0,i.jsx)("div",{style:{display:"flex",justifyContent:"center",alignItems:"center",height:"100vh",backgroundColor:"#F0EEE6"},children:(0,i.jsx)("div",{style:{width:"550px",height:"550px",overflow:"hidden",border:"1px solid #ddd",borderRadius:"8px"},children:(0,i.jsx)("canvas",{ref:e})})})};var jn=function(){return jn=Object.assign||function(e){for(var n,t=1,i=arguments.length;t<i;t++)for(var a in n=arguments[t])Object.prototype.hasOwnProperty.call(n,a)&&(e[a]=n[a]);return e},jn.apply(this,arguments)};const Ln=function(){var e=(0,x.useRef)(null);return(0,x.useEffect)((function(){var n,t=e.current,i=t.getContext("2d"),a=0,r=[],o=1.125,s=1.1,c=[{centerX:253.125,centerY:225,radiusX:112.5,radiusY:112.5,rotation:0,phase:0},{centerX:393.75,centerY:196.875,radiusX:101.25,radiusY:112.5,rotation:Math.PI/6,phase:2},{centerX:309.375,centerY:365.625,radiusX:112.5,radiusY:101.25,rotation:-Math.PI/4,phase:4}],l=function(e,n){for(var t=e.centerX,r=e.centerY,o=e.radiusX,c=e.radiusY,l=e.rotation,h=(e.depth,e.lineWidth),d=e.opacity,u=e.speed,m=.15*Math.sin(.2*a*u+e.phase)*s+1,f=o*m,p=c*m,g=l+.2*Math.sin(.15*a)*1.8,v=0;v<n;v++){var y=v/n,x=.9*y;i.beginPath();for(var A=0;A<=2*Math.PI;A+=.05){var w=.2*A,b=x+.01*Math.sin(10*A+.1*a*u+e.phase)*s,M=Math.sin(5*A+.1*a*u)*o*.05*y*s,C=Math.cos(5*A+.1*a*u)*c*.05*y*s,E=t+f*b*Math.cos(A+w+g+.02*a*u)+M,R=r+p*b*Math.sin(A+w+g+.02*a*u)+C;0===A?i.moveTo(E,R):i.lineTo(E,R)}i.closePath();var P=d*(.2+.8*y);i.strokeStyle="rgba(50, 50, 50, ".concat(P,")"),i.lineWidth=h,i.stroke()}},h=function(){i.fillStyle="#F0EEE6",i.fillRect(0,0,t.width,t.height);for(var e=0;e<r.length;e++)for(var o=0,s=r[e].forms;o<s.length;o++){var c=s[o];l(c,c.lineCount)}a+=.005,n=requestAnimationFrame(h)};return function(){t.width=550,t.height=550;c.forEach((function(e){e.centerX+=-34.375,e.centerY+=21.875}));for(var e=function(e){var n=e/4,t=c.map((function(e){var t=.8+.4*n;return jn(jn({},e),{centerX:e.centerX+30*(n-.5)*o,centerY:e.centerY+20*(n-.5)*o,radiusX:e.radiusX*t,radiusY:e.radiusY*t,rotation:e.rotation+n*Math.PI*.1,depth:n,lineCount:Math.floor(30-15*n),lineWidth:.5+.7*n,opacity:.2+.8*n,speed:(.5+1.5*n)*s})}));r.push({depth:n,forms:t})},n=0;n<5;n++)e(n)}(),h(),function(){cancelAnimationFrame(n),i&&i.clearRect(0,0,t.width,t.height),r.length=0}}),[]),(0,i.jsx)("div",{className:"w-full h-full flex items-center justify-center bg-[#F0EEE6]",children:(0,i.jsx)("canvas",{ref:e,className:"max-w-full max-h-full shadow-lg"})})};const Bn=function(){var e=(0,x.useRef)(null);return(0,x.useEffect)((function(){for(var n=e.current,t=n.getContext("2d"),i=n.width,a=n.height,r=i/2,o=a/2,s=0,c=40,l=[],h=0;h<c;h++)for(var d=0;d<c;d++)l.push({i:h,j:d,x:8*(h-20),y:8*(d-20),value:0,flowOffset:Math.random()*Math.PI*2});var u=function(e){l.forEach((function(n){n.value=function(e,n,t){var i=e/120,a=n/120,r=i*i+a*a-1,o=.5*Math.sin(.1*t)+.5,s=i*i+a*a*(1.2+.3*Math.sin(.15*t))-1,c=Math.atan2(a,i);return r=s*(1-o)+(Math.sqrt(i*i+a*a)*(1+.3*Math.sin(2*c+.1*t))-1)*o,r+=.2*Math.sin(3*i+.1*t)*Math.sin(3*a+.15*t),r+=.1*Math.sin(5*i+5*a+.05*t),r-.4*Math.exp(-(i-.5)*(i-.5)*4-a*a*4)*Math.sin(.15*t)}(n.x,n.y,e)}))},m=function(){t.fillStyle="#F0EEE6",t.fillRect(0,0,i,a),u(s+=.005);var e=function(){for(var e=[],n=Array(10).fill().map((function(e,n){return.1*n-.5})),t=0;t<39;t++)for(var i=function(i){var a=t*c+i,r=l[a],o=l[a+1],s=l[a+c],h=l[a+c+1];if(!(r&&o&&s&&h))return"continue";n.forEach((function(n){var t=[];if(r.value<n&&o.value>=n||r.value>=n&&o.value<n){var i=(n-r.value)/(o.value-r.value);t.push({x:r.x+i*(o.x-r.x),y:r.y,flowOffset:(r.flowOffset+o.flowOffset)/2})}(o.value<n&&h.value>=n||o.value>=n&&h.value<n)&&(i=(n-o.value)/(h.value-o.value),t.push({x:o.x,y:o.y+i*(h.y-o.y),flowOffset:(o.flowOffset+h.flowOffset)/2})),(s.value<n&&h.value>=n||s.value>=n&&h.value<n)&&(i=(n-s.value)/(h.value-s.value),t.push({x:s.x+i*(h.x-s.x),y:s.y,flowOffset:(s.flowOffset+h.flowOffset)/2})),(r.value<n&&s.value>=n||r.value>=n&&s.value<n)&&(i=(n-r.value)/(s.value-r.value),t.push({x:r.x,y:r.y+i*(s.y-r.y),flowOffset:(r.flowOffset+s.flowOffset)/2})),2===t.length&&e.push({x1:t[0].x,y1:t[0].y,x2:t[1].x,y2:t[1].y,threshold:n,flowOffset1:t[0].flowOffset,flowOffset2:t[1].flowOffset})}))},a=0;a<39;a++)i(a);return e}();!function(e,n){e.forEach((function(e){var i=.2+.6*Math.abs(e.threshold),a=.5+.5*Math.abs(e.threshold);t.strokeStyle="rgba(51, 51, 51, ".concat(i,")"),t.lineWidth=a,t.beginPath();var s=e.x1,c=e.y1,l=e.x2,h=e.y2;t.moveTo(r+s,o+c);for(var d=1;d<=20;d++){var u=d/20,m=s+(l-s)*u,f=c+(h-c)*u,p=.5+.5*Math.sin(10*e.threshold),g=e.flowOffset1+(e.flowOffset2-e.flowOffset1)*u,v=3*Math.sin(5*e.threshold+.2*n),y=-(h-c)/Math.sqrt((l-s)*(l-s)+(h-c)*(h-c)),x=(l-s)/Math.sqrt((l-s)*(l-s)+(h-c)*(h-c)),A=y*v*Math.sin(u*Math.PI*2+n*p+g),w=x*v*Math.sin(u*Math.PI*2+n*p+g);t.lineTo(r+m+A,o+f+w)}t.stroke()}))}(e,s),requestAnimationFrame(m)},f=requestAnimationFrame(m);return function(){cancelAnimationFrame(f),t&&t.clearRect(0,0,i,a),l&&(l.length=0)}}),[]),(0,i.jsx)("div",{className:"flex justify-center items-center bg-[#F0EEE6] w-full h-full",children:(0,i.jsx)("canvas",{ref:e,width:550,height:550,className:"max-w-full max-h-full"})})};const Dn=function(){var e=(0,x.useRef)(null);return(0,x.useEffect)((function(){var n=e.current,t=n.getContext("2d"),i=n.width,a=n.height,r=0,o=null;return o=requestAnimationFrame((function e(){t.clearRect(0,0,i,a),t.fillStyle="#F0EEE6",t.fillRect(0,0,i,a),r+=.01;for(var n=0;n<80;n++){var s=n/80*a*.8+.1*a,c=.5+.03*n,l=.2*r+.05*n,h=40*(.5+.5*Math.sin(.1*n+.3*r)),d=.2+.6*Math.pow(Math.sin(n/80*Math.PI),2),u=.2*Math.sin(.4*r+.1*n),m=Math.min(.9,Math.max(.1,d+u));t.beginPath(),t.strokeStyle="rgba(50, 50, 50, ".concat(m,")"),t.lineWidth=.6;for(var f=0;f<=200;f++){var p=f/200*i,g=s;g+=h*Math.sin(.01*p*c+l),g+=.3*h*Math.sin(.02*p*c+1.5*l),g+=.2*h*Math.sin(.04*p*c-.7*l),g+=.1*h*Math.sin(.08*p*c+2.3*l),0===f?t.moveTo(p,g):t.lineTo(p,g)}t.stroke()}for(f=0;f<i;f+=20)if(Math.random()<.4){t.beginPath();m=.1+.2*Math.sin(.05*f+r);t.strokeStyle="rgba(50, 50, 50, ".concat(m,")"),t.lineWidth=.3;var v=.1*a+Math.random()*a*.2,y=.7*a+Math.random()*a*.2;t.moveTo(f,v),t.lineTo(f,y),t.stroke()}o=requestAnimationFrame(e)})),function(){o&&cancelAnimationFrame(o)}}),[]),(0,i.jsx)("div",{className:"flex justify-center items-center h-full w-full bg-[#F0EEE6]",children:(0,i.jsx)("canvas",{ref:e,width:550,height:550,className:"shadow-lg"})})};const Hn=function(){var e=(0,x.useRef)(null),n=(0,x.useRef)(null);return(0,x.useEffect)((function(){var t=e.current;if(t){var i=t.getContext("2d"),a=t.width,r=t.height,o=120,s=180,c=25e-5,l=2e3,h=[function(e,n,t){var i=e*Math.PI*2,a=n*Math.PI,r=120+30*Math.sin(4*a+2*i);return{x:(r+=20*Math.sin(6*a)*Math.cos(3*i))*Math.sin(a)*Math.cos(i),y:r*Math.sin(a)*Math.sin(i),z:r*Math.cos(a)+20*Math.sin(5*i+3*a)}},function(e,n,t){var i=e*Math.PI*2,a=n*Math.PI,r=150+20*Math.cos(8*a);return{x:(r*=.8+.2*Math.abs(Math.cos(2*i)))*Math.sin(a)*Math.cos(i),y:r*Math.sin(a)*Math.sin(i),z:r*Math.cos(a)*(.8+.3*Math.sin(4*i))}},function(e,n,t){var i=e*Math.PI*2,a=n*Math.PI,r=120;r+=50*Math.sin(3*a)*Math.sin(2.5*i);var o=(r+=30*Math.cos(5*a+i))*Math.sin(a)*Math.cos(i),s=r*Math.sin(a)*Math.sin(i),c=r*Math.cos(a);return r*=1-.8*Math.max(0,Math.sin(2*a+3*i)-.7),{x:o,y:s,z:c}}],d=function(e,n,t){var i,a=h.length,r=t%(600*a)/600,o=Math.floor(r),s=(o+1)%a,c=r-o;if(c<0)i=0;else if(c>1)i=1;else{var l=(c-0)/1;i=l<.5?4*l*l*l:1-Math.pow(-2*l+2,3)/2}return function(e,n,t,i,a,r){var o=e(t,i,a),s=n(t,i,a);return{x:o.x*(1-r)+s.x*r,y:o.y*(1-r)+s.y*r,z:o.z*(1-r)+s.z*r}}(h[o],h[s],e,n,t,i)},u=function(){i.clearRect(0,0,a,r),i.fillStyle="#F0EEE6",i.fillRect(0,0,a,r);Math.sin(l*c),Math.cos(l*c*.7);for(var e=l*c*.1,t=0;t<o;t++){var h=t/119;i.beginPath(),i.strokeStyle="rgba(51, 51, 51, ".concat(.5,")"),i.lineWidth=.6;for(var m=!1,f=0;f<=s;f++){var p=(b=d(w=f/s,h,l)).x*Math.cos(e)-b.y*Math.sin(e),g=b.x*Math.sin(e)+b.y*Math.cos(e),v=b.z,y=a/2+p*(M=1.5+.001*v),x=r/2+g*M,A=v>-50;0===f?A&&(i.moveTo(y,x),m=!0,{x:y,y:x}):A&&m?i.lineTo(y,x):A&&!m&&i.moveTo(y,x),m=A}i.stroke()}for(t=0;t<36;t++){var w=t/35;i.beginPath(),i.strokeStyle="rgba(51, 51, 51, ".concat(.35,")"),i.lineWidth=.42;for(m=!1,f=0;f<=90;f++){var b,M;p=(b=d(w,h=f/90,l)).x*Math.cos(e)-b.y*Math.sin(e),g=b.x*Math.sin(e)+b.y*Math.cos(e),v=b.z,y=a/2+p*(M=1.5+.001*v),x=r/2+g*M,A=v>-50;0===f?A&&(i.moveTo(y,x),m=!0,{x:y,y:x}):A&&m?i.lineTo(y,x):A&&!m&&i.moveTo(y,x),m=A}i.stroke()}l+=.5,n.current=requestAnimationFrame(u)};return u(),function(){n.current&&(cancelAnimationFrame(n.current),n.current=null),t&&i&&i.clearRect(0,0,a,r)}}}),[]),(0,i.jsx)("div",{className:"flex items-center justify-center bg-[#F0EEE6] w-full h-full",children:(0,i.jsx)("canvas",{ref:e,width:550,height:550})})};const Yn=function(){var e=(0,x.useRef)(null);return(0,x.useEffect)((function(){var n=e.current,t=n.getContext("2d"),i=n.width,a=n.height,r=0,o=1.5,s=null;return s=requestAnimationFrame((function e(){t.fillStyle="#F0EEE6",t.fillRect(0,0,i,a),r+=.001;for(var n=i/2,c=a/2,l=0;l<3;l++)for(var h=r+l*Math.PI*2/3,d=40*Math.sin(.2*h)*o,u=40*Math.cos(.3*h)*o,m=0;m<25;m++){var f=(30+3*m)*o,p=10*Math.sin(.2*m+h)*o,g=10*Math.cos(.2*m+h)*o;t.beginPath(),t.strokeStyle="rgba(50, 50, 50, 0.4)",t.lineWidth=.8;for(var v=0;v<=100;v++){var y=v/100*Math.PI*2,x=f;x+=15*Math.sin(3*y+2*h)*o,x+=10*Math.cos(5*y-h)*o,x+=5*Math.sin(8*y+.1*m)*o;var A=n+d+p+Math.cos(y)*x,w=c+u+g+Math.sin(y)*x;0===v?t.moveTo(A,w):t.lineTo(A,w)}t.closePath(),t.stroke()}s=requestAnimationFrame(e)})),function(){s&&cancelAnimationFrame(s)}}),[]),(0,i.jsx)("div",{className:"flex justify-center items-center h-full w-full bg-[#F0EEE6]",children:(0,i.jsx)("canvas",{ref:e,width:550,height:550,className:"shadow-lg"})})};const Xn=function(){var e=(0,x.useRef)(null);return(0,x.useEffect)((function(){var n=e.current,t=n.getContext("2d"),i=n.width,a=n.height;t.fillStyle="#F0EEE6",t.fillRect(0,0,i,a);for(var r=[],o=0,s=[function(e,n,t){var r=e-i/2,o=n-a/2,s=Math.atan2(o,r),c=150+(15*Math.sin(2*s+.25*t)+10*Math.sin(4*s+.15*t)+5*Math.sin(6*s+.1*t));return Math.sqrt(r*r+o*o)<c},function(e,n,t){var r=e-i/2,o=n-a/2,s=Math.atan2(o,r),c=5+Math.floor(t%3),l=120+60*Math.sin(s*c+.5*t);return Math.sqrt(r*r+o*o)<l},function(e,n,t){for(var r=i/2,o=a/2,s=0;s<5;s++){var c=.1*t+s*Math.PI*2/5,l=e-(r+70*Math.cos(c)),h=n-(o+70*Math.sin(c));if(Math.sqrt(l*l+h*h)<80)return!0}return!1}],c=0;c<25e3;c++)r.push({x:Math.random()*i,y:Math.random()*a,targetX:Math.random()*i,targetY:Math.random()*a,size:.5+1*Math.random(),opacity:.2+.4*Math.random(),speed:75e-5+.00175*Math.random(),inside:!1});var l=null,h=0,d=1e3/30,u=function(e){h||(h=e);var n=e-h;if(n>=d){t.fillStyle="#F0EEE6",t.fillRect(0,0,i,a);var c=(o+=.0025)/10%s.length,m=Math.floor(c),f=(m+1)%s.length,p=c-m;Math.floor(60*o)%20==0&&function(e,n){for(var t=s[e],o=0,c=0,l=r;c<l.length;c++)t((y=l[c]).x,y.y,n)?(y.inside=!0,o++):y.inside=!1;if(o<17500)for(var h=0,d=r;h<d.length;h++)if(!(y=d[h]).inside&&o<17500)for(var u=0,m=!1;u<5&&!m;){var f=Math.random()*i,p=Math.random()*a;t(f,p,n)&&(y.targetX=f,y.targetY=p,y.inside=!0,o++,m=!0),u++}for(var g=0,v=r;g<v.length;g++){var y;if(!(y=v[g]).inside){var x=Math.random()*Math.PI*2,A=160+80*Math.random();y.targetX=i/2+Math.cos(x)*A,y.targetY=a/2+Math.sin(x)*A}}}(m,o);for(var g=0,v=r;g<v.length;g++){var y=v[g];y.x+=(y.targetX-y.x)*y.speed,y.y+=(y.targetY-y.y)*y.speed,y.x+=.05*Math.sin(.15*o+.005*y.y),y.y+=.05*Math.cos(.15*o+.005*y.x);var x=s[m](y.x,y.y,o),A=s[f](y.x,y.y,o),w=void 0;w=x&&A?.7:x?.7*(1-p):A?.7*p:.1,y.opacity+=.05*(w-y.opacity),t.fillStyle="rgba(51, 51, 51, ".concat(y.opacity,")"),t.beginPath(),t.arc(y.x,y.y,y.size,0,2*Math.PI),t.fill()}h=e-n%d}l=requestAnimationFrame(u)};return l=requestAnimationFrame(u),function(){l&&(cancelAnimationFrame(l),l=null),t&&t.clearRect(0,0,i,a),r.length=0}}),[]),(0,i.jsx)("div",{className:"flex justify-center items-center w-full h-full bg-gray-100",children:(0,i.jsx)("canvas",{ref:e,width:550,height:550,className:"shadow-lg"})})};var Wn=function(){return Wn=Object.assign||function(e){for(var n,t=1,i=arguments.length;t<i;t++)for(var a in n=arguments[t])Object.prototype.hasOwnProperty.call(n,a)&&(e[a]=n[a]);return e},Wn.apply(this,arguments)};const qn=function(){var e=(0,x.useRef)(null),n=(0,x.useRef)(null);return(0,x.useEffect)((function(){var t=e.current,i=t.getContext("2d"),a=t.width,r=t.height;i.fillStyle="#F0EEE6",i.fillRect(0,0,a,r);var o=a/2,s=r/2,c=50,l=function(e,n,t,i){return Math.sin(.1*e+.15*i)*Math.cos(.1*n+Math.sin(.1*t)+.1*i)*Math.sin(.1*t+Math.sin(.1*e)+.2*i)},h=80,d=30,u=.8,m=36,f=null,p=-1,g=function(e,n){var t=.05*n,i=.075*n,a=Math.cos(t),r=Math.sin(t),c=Math.cos(i),l=Math.sin(i),h=e.y,d=e.z*a-e.x*r,u=e.z*r+e.x*a;return{x:o+1.5*u,y:s+1.5*(h*c-d*l),z:h*l+d*c}},v=function(e){var n=200,t=new Array(n).fill().map((function(){return new Array(n).fill(-1/0)})),i=new Array(e.length).fill(!1);return e.forEach((function(e,o){var s,c,l=(s=e.x,c=e.y,{bx:Math.floor(s/a*n),by:Math.floor(c/r*n)}),h=l.bx,d=l.by;h>=0&&h<n&&d>=0&&d<n&&e.z>t[d][h]&&(t[d][h]=e.z,i[o]=!0)})),i},y=function(){!function(e,n,t){var o=e.map((function(e){var n=g(e.position,t);return Wn(Wn({},n),{vertex:e})})),s=v(o);i.fillStyle="#F0EEE6",i.fillRect(0,0,a,r);var c={};e.forEach((function(e,n){if(s[n]){var t=.2*Math.round(e.phi/.2);c[t]||(c[t]=[]),c[t].push({vertex:e,projectedPos:o[n]})}})),Object.values(c).forEach((function(e){if(!(e.length<2)){e.sort((function(e,n){return e.vertex.theta-n.vertex.theta}));var n=e[Math.floor(e.length/2)],a=n.vertex.normal.z,r=n.vertex.hatchingIntensity,o=(.1+.5*(.7*Math.abs(a)+.3))*r,s=.3+.7*r;i.beginPath();var c=e[0].projectedPos;i.moveTo(c.x,c.y);for(var l=1;l<e.length;l++){var h=e[l].projectedPos,d=.5*Math.sin(t+10*e[l].vertex.theta);i.lineTo(h.x+d,h.y+d)}i.strokeStyle="rgba(51, 51, 51, ".concat(o,")"),i.lineWidth=s,i.stroke()}}));var l={};e.forEach((function(e,n){if(s[n]){var t=.15*Math.round(e.theta/.15);l[t]||(l[t]=[]),l[t].push({vertex:e,projectedPos:o[n]})}})),Object.values(l).forEach((function(e){if(!(e.length<2)){e.sort((function(e,n){return e.vertex.phi-n.vertex.phi}));var n=e[Math.floor(e.length/2)],a=n.vertex.normal.x,r=n.vertex.normal.y,o=Math.atan2(r,a),s=.3+.7*Math.abs(Math.sin(o+.02*t)),c=.2+.6*s,l=(.1+.4*s)*n.vertex.hatchingIntensity;i.beginPath();var h=e[0].projectedPos;i.moveTo(h.x,h.y);for(var d=1;d<e.length;d++){var u=e[d].projectedPos,m=.3*Math.cos(1.2*t+8*e[d].vertex.phi);i.lineTo(u.x+m,u.y+m)}i.strokeStyle="rgba(51, 51, 51, ".concat(l,")"),i.lineWidth=c,i.stroke()}})),o.forEach((function(e,n){if(s[n]){var a=e.vertex;if(a.hatchingIntensity>.7){var r=Math.sin(.1*t+2*a.theta+1*a.phi),o=a.tangent1.x*(1+r)/2+a.tangent2.x*(1-r)/2,c=a.tangent1.y*(1+r)/2+a.tangent2.y*(1-r)/2,l=a.tangent1.z*(1+r)/2+a.tangent2.z*(1-r)/2,h=3+5*a.hatchingIntensity,d={x:a.position.x-o*h,y:a.position.y-c*h,z:a.position.z-l*h},u={x:a.position.x+o*h,y:a.position.y+c*h,z:a.position.z+l*h},m=g(d,t),f=g(u,t);i.beginPath(),i.moveTo(m.x,m.y),i.lineTo(f.x,f.y);var p=.5+.5*Math.sin(.25*t+3*a.theta+2*a.phi),v=.1+.5*p*a.hatchingIntensity,y=.3+.6*p;i.strokeStyle="rgba(51, 51, 51, ".concat(v,")"),i.lineWidth=y,i.stroke()}}}))}(function(e){if(f&&e-p<.1)return f;for(var n=[],t=m,i=5*Math.sin(.2*e),a=h+i,r=d+.2*i,o=0;o<t;o++)for(var s=o/t*Math.PI*2,c=0;c<t;c++){var g=c/t*Math.PI*2,v=(a+r*Math.cos(g))*Math.cos(s),y=(a+r*Math.cos(g))*Math.sin(s),x=r*Math.sin(g),A=.02*u,w=.2*e,b=15*l(v*A,y*A,x*A,w)+7*l(v*A*2,y*A*2,x*A*2,1.3*w),M=v/a,C=y/a,E=x/r,R=Math.sqrt(M*M+C*C+E*E)||.001,P=v+M/R*b,F=y+C/R*b,S=x+E/R*b,I=-Math.sin(s),T=Math.cos(s),k=-Math.cos(s)*Math.sin(g),z=-Math.sin(s)*Math.sin(g),j=Math.cos(g);n.push({position:{x:P,y:F,z:S},normal:{x:M/R,y:C/R,z:E/R},tangent1:{x:I,y:T,z:0},tangent2:{x:k,y:z,z:j},theta:s,phi:g,hatchingIntensity:.3+.7*Math.abs(l(.03*P,.03*F,.03*S,.5*e))})}return f=n,p=e,n}(c+=.004),0,c),n.current=requestAnimationFrame(y)};return y(),function(){cancelAnimationFrame(n.current),i&&i.clearRect(0,0,a,r),f=null}}),[]),(0,i.jsx)("div",{className:"w-full h-full flex items-center justify-center bg-[#F0EEE6]",children:(0,i.jsx)("canvas",{ref:e,width:550,height:550,className:"border border-gray-300 shadow-md"})})};const On=function(){var e=(0,x.useRef)(null);return(0,x.useEffect)((function(){var n=e.current,t=n.getContext("2d"),i=n.width,a=n.height,r=5e3,o=65,s=(Date.now(),Array(r).fill().map((function(e,n){var t=n/r*Math.PI*2,o=Math.random()*Math.PI*2,s=.5*Math.sin(13)+.5,c=120*(1+.3*Math.sin(2*t)),l=c*Math.cos(t)*(1+.2*Math.sin(3*t+6.5)),h=c*Math.sin(t)*(1+.4*Math.cos(3*t+13)),d=120*(1+.4*Math.sin(3*o)),u=d*Math.cos(o)*(1+.3*Math.sin(2*o+9.75)),m=d*Math.sin(o)*(1+.2*Math.cos(4*o+16.25)),f=i/2+(l*(1-s)+u*s),p=a/2+(h*(1-s)+m*s),g=50*(.2*Math.sin(19.5)+.8),v=g*Math.exp(-Math.pow(t-.5*Math.PI,2)),y=.5*g*Math.exp(-Math.pow(t-.5*Math.PI,2));return{x:f+=v*Math.sin(26),y:p+=y*Math.cos(19.5),size:1.5*Math.random()+.5,connections:[],targetX:0,targetY:0,vx:0,vy:0,angle:o}}))),c=0,l=null,h=0,d=1e3/36,u=function(e){l=requestAnimationFrame(u);var n,m,f,p,g=e-h;g>=d&&(h=e-g%d,m=(n=o+=.005)-65+c,f=Math.min(1,m/10),p=.5*Math.sin(.2*n)+.5,s.forEach((function(e,t){var i=t/r*Math.PI*2,a=e.angle,o=50*(.2*Math.sin(.3*n)+.8),s=n+m*f*.1,c=120*(1+.3*Math.sin(2*i)),l=c*Math.cos(i)*(1+.2*Math.sin(3*i+.1*s)),h=c*Math.sin(i)*(1+.4*Math.cos(3*i+.2*s)),d=n+m*f*.1,u=120*(1+.4*Math.sin(3*a)),g=u*Math.cos(a)*(1+.3*Math.sin(2*a+.15*d)),v=u*Math.sin(a)*(1+.2*Math.cos(4*a+.25*d));e.targetX=l*(1-p)+g*p,e.targetY=h*(1-p)+v*p;var y=o*Math.exp(-Math.pow(i-.5*Math.PI,2)),x=.5*o*Math.exp(-Math.pow(i-.5*Math.PI,2));e.targetX+=y*Math.sin(.4*s),e.targetY+=x*Math.cos(.3*s),e.targetX+=5*(Math.random()-.5),e.targetY+=5*(Math.random()-.5)})),function(){var e=o-65+c;s.forEach((function(n){var t=n.targetX-(n.x-i/2),r=n.targetY-(n.y-a/2),o=.01+.02*Math.min(1,e/5);n.x+=t*o,n.y+=r*o,n.connections=[]}))}(),function(){for(var e=0;e<s.length;e++){var n=s[e];if(!(n.connections.length>=3))for(var t=e+1;t<s.length;t++){var i=s[t];if(!(i.connections.length>=3)){var a=n.x-i.x,r=n.y-i.y;if(Math.sqrt(a*a+r*r)<25&&(n.connections.push(t),i.connections.push(e),n.connections.length>=3))break}}}}(),function(){t.fillStyle="#F0EEE6",t.fillRect(0,0,i,a),t.strokeStyle="rgba(51, 51, 51, 0.2)",t.lineWidth=.5;for(var e=0;e<s.length;e++)for(var n=s[e],r=0;r<n.connections.length;r++){var c=s[n.connections[r]];t.beginPath(),t.moveTo(n.x,n.y);var l=(n.x+c.x)/2,h=(n.y+c.y)/2,d=5*Math.sin(.5*o+.01*e);t.quadraticCurveTo(l+d,h+d,c.x,c.y),t.stroke()}s.forEach((function(e){var n=Math.sqrt(Math.pow(e.x-i/2,2)+Math.pow(e.y-a/2,2)),r=Math.max(.1,1-n/300);t.fillStyle="rgba(51, 51, 51, ".concat(r,")"),t.beginPath(),t.arc(e.x,e.y,e.size,0,2*Math.PI),t.fill()}))}())};return l=requestAnimationFrame(u),function(){l&&(cancelAnimationFrame(l),l=null),t&&t.clearRect(0,0,i,a),s.length=0}}),[]),(0,i.jsx)("div",{className:"flex justify-center items-center bg-[#F0EEE6] w-full h-full",children:(0,i.jsx)("canvas",{ref:e,width:550,height:550,className:"max-w-full max-h-full"})})};const Gn=function(){var e=(0,x.useRef)(null);return(0,x.useEffect)((function(){var n,t=e.current,i=t.getContext("2d"),a=t.width,r=t.height,o=0,s=function(e,n,t){var i=function(e,n,t){return.5*Math.sin(7*e+3*t)+.5*Math.sin(8*n+4*t)}(.01*(e-a/2),.01*(n-r/2),125e-6*t),o=e-a/2,s=n-r/2,c=Math.sqrt(o*o+s*s),l=Math.max(0,1-c/200),h=i*Math.PI*4+Math.atan2(s,o);return{x:Math.cos(h)*l,y:Math.sin(h)*l}},c=function(){function e(){this.reset()}return e.prototype.reset=function(){var e=Math.random()*Math.PI*2,n=50+150*Math.random();this.x=a/2+Math.cos(e)*n,this.y=r/2+Math.sin(e)*n,this.points=[],this.age=0,this.lifespan=400+600*Math.random(),this.opacity=0,this.fadeIn=!0,this.width=.2+.8*Math.random()},e.prototype.update=function(e){if(this.age+=1,this.age>=this.lifespan)this.reset();else{var n=this.age/this.lifespan;this.opacity=n<.1?n/.1*.4:n>.9?.4*(1-(n-.9)/.1):.4;var t=s(this.x,this.y,e);this.points.push({x:this.x,y:this.y}),this.points.length>200&&this.points.shift(),this.x+=.5*t.x,this.y+=.5*t.y;var i=Math.sqrt(t.x*t.x+t.y*t.y);(this.x<0||this.x>a||this.y<0||this.y>r||i<.01)&&this.reset()}},e.prototype.draw=function(e){if(!(this.points.length<2)){e.beginPath(),e.strokeStyle="rgba(51, 51, 51, ".concat(this.opacity,")"),e.lineWidth=.5*this.width,e.moveTo(this.points[0].x,this.points[0].y);for(var n=1;n<this.points.length;n++)e.lineTo(this.points[n].x,this.points[n].y);e.stroke()}},e}(),l=Array.from({length:300},(function(){return new c})),h=function(){i.clearRect(0,0,a,r),i.fillStyle="#F0EEE6",i.fillRect(0,0,a,r),l.forEach((function(e){e.update(o),e.draw(i)})),o+=1,n=requestAnimationFrame(h)};return h(),function(){cancelAnimationFrame(n),i&&i.clearRect(0,0,a,r),l.length=0}}),[]),(0,i.jsx)("div",{className:"flex items-center justify-center bg-[#F0EEE6] w-full h-full",children:(0,i.jsx)("canvas",{ref:e,width:550,height:550})})};const Nn=function(){var e=(0,x.useRef)(null);return(0,x.useEffect)((function(){var n=new B.Z58,t=new B.ubm(60,1,.1,1e3),i=new D.JeP({antialias:!0,alpha:!0});i.setSize(550,550),i.setClearColor(15789798,1),e.current&&e.current.appendChild(i.domElement),t.position.z=7;for(var a=25e3,r=new B.LoY,o=new Float32Array(75e3),s=new Float32Array(a),c=new Float32Array(a),l=new Float32Array(a),h=0;h<a;h++){var d=3*h,u=Math.random(),m=u*Math.PI*20,f=.6+2.2*u,p=f*Math.cos(m),g=f*Math.sin(m),v=5*(u-.5);o[d]=p,o[d+1]=g,o[d+2]=v,s[h]=.03+.04*Math.random(),c[h]=.4+.6*Math.random(),l[h]=h}r.setAttribute("position",new B.THS(o,3)),r.setAttribute("size",new B.THS(s,1)),r.setAttribute("opacity",new B.THS(c,1)),r.setAttribute("index",new B.THS(l,1));var y=new B.BKk({transparent:!0,depthWrite:!1,blending:B.NTi,uniforms:{time:{value:0},color:{value:new B.Q1f(3355443)}},vertexShader:"\n        attribute float size;\n        attribute float opacity;\n        attribute float index;\n        uniform float time;\n        varying float vOpacity;\n        \n        void main() {\n          vOpacity = opacity;\n          \n          // Get the original position\n          vec3 pos = position;\n          \n          // Movement guided by intuition rather than rules\n          float i = index;\n          float speed = 0.2 + 0.2 * fract(i / 1000.0);\n          float angle = time * speed + i * 0.001;\n          \n          // Twist the vortex based on y position\n          float twistAmount = sin(time * 0.3) * 0.5;\n          float twist = pos.y * twistAmount;\n          \n          // Apply twist and contraction/expansion\n          float r = length(pos.xy);\n          float breathe = 1.0 + sin(time * 0.5) * 0.1;\n          r *= breathe;\n          \n          float theta = atan(pos.y, pos.x) + twist;\n          pos.x = r * cos(theta);\n          pos.y = r * sin(theta);\n          \n          // Add some vertical oscillation\n          pos.z += sin(time * 0.2 + i * 0.01) * 0.2;\n          \n          vec4 mvPosition = modelViewMatrix * vec4(pos, 1.0);\n          gl_PointSize = size * (50.0 / -mvPosition.z);\n          gl_Position = projectionMatrix * mvPosition;\n        }\n      ",fragmentShader:"\n        uniform vec3 color;\n        varying float vOpacity;\n        \n        void main() {\n          if (length(gl_PointCoord - vec2(0.5, 0.5)) > 0.475) discard;\n          gl_FragColor = vec4(color, vOpacity);\n        }\n      "}),x=new B.ONl(r,y);n.add(x);var A=null,w=function(e){e*=5e-4,y.uniforms.time.value=e,t.position.x=1.5*Math.sin(.1*e),t.position.y=1*Math.cos(.15*e),t.lookAt(n.position),i.render(n,t),A=requestAnimationFrame(w)};return A=requestAnimationFrame(w),function(){var t;A&&cancelAnimationFrame(A),n.traverse((function(e){e.geometry&&e.geometry.dispose(),e.material&&(Array.isArray(e.material)?e.material.forEach((function(e){return e.dispose()})):e.material.dispose())})),(null===(t=e.current)||void 0===t?void 0:t.contains(i.domElement))&&e.current.removeChild(i.domElement),i.dispose()}}),[]),(0,i.jsx)("div",{className:"w-full h-full flex justify-center items-center bg-[#F0EEE6]",children:(0,i.jsx)("div",{ref:e,className:"w-full h-full"})})};const $n=function(){var e=(0,x.useRef)(null),n=(0,x.useRef)(null),t=(0,x.useRef)(0),a=(0,x.useRef)(0),r=(0,x.useRef)([]),o=(0,x.useRef)(!1),s=(0,x.useState)({width:window.innerWidth,height:window.innerHeight}),c=s[0],l=s[1],h=function(){function e(e,n,t,i,a,r){this.x=e,this.y=n,this.angle=t,this.size=0,this.targetSize=20,this.delay=i,this.reverseDelay=120*(r-a-1),this.birth=0,this.opacity=0,this.currentSize=0}return e.prototype.shouldStart=function(e,n){return e>(n?this.reverseDelay:this.delay)},e.prototype.update=function(e,n,t){if(this.shouldStart(e,n)){this.birth=n?Math.max(0,this.birth-.005):Math.min(Math.PI/2,this.birth+.005),this.size=this.targetSize*Math.sin(this.birth),this.opacity=Math.min(.5*this.birth,1)}var i=.05*Math.sin(8e-4*t+this.delay);this.currentSize=this.size*(1+i)},e.prototype.draw=function(e){e.save(),e.translate(this.x,this.y),e.rotate(this.angle),e.globalAlpha=.8*this.opacity,e.beginPath(),e.moveTo(0,-this.currentSize/2),e.quadraticCurveTo(this.currentSize/2,0,0,this.currentSize/2),e.quadraticCurveTo(-this.currentSize/2,0,0,-this.currentSize/2),e.closePath(),e.strokeStyle="#2a2a2a",e.lineWidth=1,e.stroke(),e.fillStyle="rgba(255, 255, 255, 0.3)",e.fill(),e.restore()},e.prototype.isComplete=function(){return this.birth>=Math.PI/2},e}();return(0,x.useEffect)((function(){var n=function(){var n,t=null===(n=e.current)||void 0===n?void 0:n.parentElement;if(t){var i=t.clientWidth,a=t.clientHeight,r=Math.min(i,a);l({width:r,height:r})}};return n(),window.addEventListener("resize",n),function(){return window.removeEventListener("resize",n)}}),[]),(0,x.useEffect)((function(){var i=e.current;if(i){var s=i.getContext("2d");if(s){var l=c.width/800;i.width=c.width,i.height=c.height;for(var d=[],u=i.width/2,m=0;m<80;m++){var f=m/80,p=i.height-50-f*(i.height-100),g=f*Math.PI*8,v=(30+20*Math.sin(f*Math.PI*2))*l,y=u+Math.cos(g)*v,x=g+Math.PI/2,A=120*m;d.push(new h(y,p,x,A,m,80))}r.current=d;var w=function(){s.clearRect(0,0,i.width,i.height);var e=r.current.every((function(e){return e.isComplete()})),c=r.current.every((function(e){return e.birth<=0}));e&&!o.current?(o.current=!0,a.current=0):c&&o.current&&(o.current=!1,a.current=0),r.current.forEach((function(e){e.update(a.current,o.current,t.current),e.draw(s)})),t.current+=8,a.current+=8,n.current=requestAnimationFrame(w)};return w(),function(){n.current&&(cancelAnimationFrame(n.current),n.current=null),i&&s&&s.clearRect(0,0,i.width,i.height),r.current=[],t.current=0,a.current=0,o.current=!1}}}}),[c]),(0,i.jsx)("div",{style:{margin:0,background:"#F0EEE6",overflow:"hidden",display:"flex",alignItems:"center",justifyContent:"center",height:"100%"},children:(0,i.jsx)("div",{style:{padding:"30px",position:"relative",width:"100%",height:"100%",display:"flex",alignItems:"center",justifyContent:"center"},children:(0,i.jsx)("div",{style:{position:"relative",width:"100%",paddingBottom:"100%"},children:(0,i.jsx)("canvas",{ref:e,style:{position:"absolute",top:"50%",left:"50%",transform:"translate(-50%, -50%)",filter:"contrast(1.1) brightness(1.05)",maxWidth:"100%",maxHeight:"100%"}})})})})};const _n=function(){var e=(0,x.useRef)(null),n=(0,x.useRef)(null),t=(0,x.useRef)(null),a=(0,x.useRef)(null);return(0,x.useEffect)((function(){t.current||(t.current=new B.Z58,a.current=new D.JeP({antialias:!0,powerPreference:"high-performance"}),a.current.setSize(550,550),a.current.setClearColor(15789798),e.current&&e.current.appendChild(a.current.domElement));var i=t.current,r=a.current,o=new B.ubm(75,1,.1,1e3);o.position.z=4;var s=new B.UPV(1.8,.3,150,24,3,4),c=new B.mrM({color:7829367,transparent:!0,opacity:.7,linewidth:.25}),l=new B.TDQ(s),h=new B.DXC(l,c);h.position.set(0,0,0),i.add(h);var d=0,u=function(){d+=.002,h.rotation.x=Math.PI/6+.2*Math.sin(.25*d),h.rotation.y=.4*d+.3*Math.sin(.15*d),h.rotation.z=.15*Math.cos(.2*d),h.position.x=.1*Math.sin(.1*d),h.position.y=.1*Math.cos(.15*d);var e=.95+.05*Math.sin(.6*d+.3*Math.sin(.25*d));h.scale.setScalar(e),r.render(i,o),n.current=requestAnimationFrame(u)};return u(),function(){n.current&&(cancelAnimationFrame(n.current),n.current=null),s&&s.dispose(),c&&c.dispose(),l&&l.dispose(),i&&h&&i.remove(h),t.current&&(t.current.clear(),t.current=null),a.current&&(e.current&&a.current.domElement&&e.current.contains(a.current.domElement)&&e.current.removeChild(a.current.domElement),a.current.dispose(),a.current.forceContextLoss(),a.current=null),d=0}}),[]),(0,i.jsx)("div",{className:"flex items-center justify-center w-full h-full bg-[#F0EEE6]",children:(0,i.jsx)("div",{className:"w-[550px] h-[550px]",children:(0,i.jsx)("div",{ref:e,className:"w-full h-full"})})})};const Vn=function(){var e=(0,x.useRef)(null),n=(0,x.useRef)(null),t=(0,x.useRef)(null),a=(0,x.useRef)(0),r=550,o=100,s=467.5,c=4*Math.PI,l=7*Math.PI,h=function(e,n,t,i,a){var r=Math.max(0,Math.min(1,i)),o=30*a[Math.floor(Math.abs(100*(r*c+.01*t))%628)],s=15*a[Math.floor(Math.abs(100*(r*l-.008*t))%628)];return{offsetX:o+10*a[Math.floor(Math.abs(100*(.02*e+.015*n+.005*t))%628)],offsetY:s}};return(0,x.useEffect)((function(){var i=e.current;if(i){n.current||(i.width=r,i.height=r,n.current=i.getContext("2d",{alpha:!1}));var c=n.current;if(!t.current){for(var l=new Float32Array(628),d=0;d<628;d++)l[d]=Math.sin(d/100);t.current={sinCache:l,verticalLines:new Float32Array(20200),horizontalLines:new Float32Array(20200)}}var u,m=t.current,f=m.sinCache,p=m.verticalLines,g=m.horizontalLines,v=function(){var e=a.current;c.fillStyle="#F0EEE6",c.fillRect(0,0,r,r),c.strokeStyle="#777777",c.lineWidth=.5;for(var n=0,t=0,i=0;i<o;i++)for(var l=41.25+i/o*s,d=0;d<=o;d++){var m=h(l,w=(A=d/o*1.2-.1)*r,e,A,f),y=m.offsetX,x=m.offsetY;p[n++]=l+y,p[n++]=w+x}for(d=0;d<o;d++){var A,w=(A=d/o*1.2-.1)*r;for(i=0;i<=o;i++){var b=h(l=41.25+i/o*s,w,e,A,f);y=b.offsetX,x=b.offsetY;g[t++]=l+y,g[t++]=w+x}}n=0;for(i=0;i<o;i++){c.beginPath(),c.moveTo(p[n],p[n+1]),n+=2;for(d=1;d<=o;d++)c.lineTo(p[n],p[n+1]),n+=2;c.stroke()}t=0;for(d=0;d<o;d++){c.beginPath(),c.moveTo(g[t],g[t+1]),t+=2;for(i=1;i<=o;i++)c.lineTo(g[t],g[t+1]),t+=2;c.stroke()}a.current+=.3,u=requestAnimationFrame(v)};return v(),function(){u&&cancelAnimationFrame(u),n.current&&n.current.clearRect(0,0,r,r),n.current=null,t.current=null,a.current=0}}}),[]),(0,i.jsx)("div",{style:{margin:0,background:"#F0EEE6",overflow:"hidden",display:"flex",alignItems:"center",justifyContent:"center",height:"100%"},children:(0,i.jsx)("div",{style:{padding:"30px",position:"relative",width:"100%",height:"100%",display:"flex",alignItems:"center",justifyContent:"center"},children:(0,i.jsx)("canvas",{ref:e,style:{display:"block"}})})})};var Un=550,Zn=120,Jn=100,Kn=100,Qn=2*Math.PI;var et=function(e,n,t){if(t||2===arguments.length)for(var i,a=0,r=n.length;a<r;a++)!i&&a in n||(i||(i=Array.prototype.slice.call(n,0,a)),i[a]=n[a]);return e.concat(i||Array.prototype.slice.call(n))};const nt=function(){var e=(0,x.useRef)(null),n=(0,x.useRef)(null);return(0,x.useEffect)((function(){var t=e.current;if(t){for(var i=t.getContext("2d"),a=t.width=800,r=t.height=600,o=[],s=[],c=2*Math.PI,l=function(e,n){return void 0===n&&(n=e,e=0),Math.random()*(n-e)+e},h=function(e,n,t,i,a){return i+(e-n)/(t-n)*(a-i)},d=function(){function e(e){this.phase=e||l(c),this.radius=l(90,110),this.yOffset=l(-300,300),this.ySpeed=l(.3,.6)*(l()>.5?1:-1),this.rotationSpeed=l(.005,.0075),this.size=l(3,6),this.opacity=l(120,180),this.strength=l(.8,1)}return e.prototype.update=function(){return this.phase+=this.rotationSpeed*this.strength,this.yOffset+=this.ySpeed,this.yOffset>350&&(this.yOffset=-350),this.yOffset<-350&&(this.yOffset=350),{x:a/2+Math.cos(this.phase)*this.radius,y:r/2+this.yOffset,z:Math.sin(this.phase)*this.radius,strength:this.strength,size:this.size,opacity:this.opacity}},e}(),u=0;u<60;u++){var m=u/60*c*3;o.push(new d(m))}var f=1e3/30,p=0,g=function(e){if(!p)return p=e,void(n.current=requestAnimationFrame(g));var t,l,d,u,m,v,y,x,A,w=e-p;if(w>=f){p=e-w%f,i.fillStyle="#F0EEE6",i.fillRect(0,0,a,r),(s=o.map((function(e){return e.update()}))).sort((function(e,n){return e.z-n.z})),i.lineWidth=1.2;for(var b=0;b<s.length;b++)for(var M=s[b],C=0;C<s.length;C++)if(b!==C){var E=s[C],R=(t=M.x,l=M.y,d=M.z,u=E.x,m=E.y,v=E.z,y=void 0,x=void 0,A=void 0,y=u-t,x=m-l,A=v-d,Math.sqrt(y*y+x*x+A*A));if(R<120){var P=h(R,0,120,40,10)*h(Math.min(M.z,E.z),-110,110,.3,1);i.strokeStyle="rgba(20, 20, 20, ".concat(P/255,")"),i.beginPath(),i.moveTo(M.x,M.y),i.lineTo(E.x,E.y),i.stroke()}}for(b=0;b<s.length;b++){var F=s[b],S=h(F.z,-110,110,.6,1.3),I=h(F.z,-110,110,.4*F.opacity,F.opacity);i.fillStyle="rgba(10, 10, 10, ".concat(I/255,")"),i.beginPath(),i.arc(F.x,F.y,F.size*S/2,0,c),i.fill()}i.strokeStyle="rgba(0, 0, 0, 0.118)",i.lineWidth=2;var T=et([],s,!0).sort((function(e,n){return e.y-n.y}));for(b=0;b<T.length-1;b++){var k=T[b],z=T[b+1];Math.abs(k.y-z.y)<30&&(i.beginPath(),i.moveTo(k.x,k.y),i.lineTo(z.x,z.y),i.stroke())}}n.current=requestAnimationFrame(g)};return n.current=requestAnimationFrame(g),function(){n.current&&(cancelAnimationFrame(n.current),n.current=null),t&&i&&i.clearRect(0,0,a,r)}}}),[]),(0,i.jsx)("div",{className:"w-full min-h-screen bg-[#F0EEE6] flex flex-col items-center justify-center",children:(0,i.jsx)("div",{className:"border-0 overflow-hidden",children:(0,i.jsx)("canvas",{ref:e,width:800,height:600})})})};const tt=function(){var e=(0,x.useRef)(null);return(0,x.useEffect)((function(){if(e.current){var n=e.current,t=null,i=!0,a=[];n.innerHTML="";var r=n.offsetWidth,o=(n.offsetHeight,r/322),s=document.createElementNS("http://www.w3.org/2000/svg","svg");s.setAttribute("width","0"),s.setAttribute("height","0"),s.innerHTML='\n      <defs>\n        <pattern id="crackedTexture" x="0" y="0" width="100" height="100" patternUnits="userSpaceOnUse">\n          <rect width="100" height="100" fill="#444" opacity="0.1"/>\n          <path d="M20,20 L40,15 L35,40 M60,30 L80,25 L75,50 M10,70 L30,65 L25,90" \n                stroke="#333" stroke-width="0.5" fill="none" opacity="0.3"/>\n        </pattern>\n      </defs>\n    ',n.appendChild(s),a.push(s);for(var c=function(e){var t=(80+60*Math.sin(e/9*Math.PI))*o,i=40*e*o,r=document.createElement("div");r.className="rhombus",r.style.position="absolute",r.style.left="50%",r.style.top="".concat(i,"px"),r.style.transform="translateX(-50%)",r.style.transition="all 0.3s ease",r.dataset.index=e,r.dataset.originalSize=t;var s=document.createElementNS("http://www.w3.org/2000/svg","svg");s.setAttribute("width",t),s.setAttribute("height",.6*t),s.setAttribute("viewBox","0 0 100 60");var c=document.createElementNS("http://www.w3.org/2000/svg","polygon");c.setAttribute("points","50,0 100,30 50,60 0,30"),c.setAttribute("fill","url(#crackedTexture)"),c.setAttribute("opacity","0.15");var l=document.createElementNS("http://www.w3.org/2000/svg","polygon");l.setAttribute("points","50,0 100,30 50,60 0,30"),l.setAttribute("fill","none"),l.setAttribute("stroke","#333"),l.setAttribute("stroke-width","0.5"),l.setAttribute("opacity","0.8");var h=[{x1:"50",y1:"0",x2:"50",y2:"60"},{x1:"0",y1:"30",x2:"100",y2:"30"},{x1:"25",y1:"15",x2:"75",y2:"45"},{x1:"75",y1:"15",x2:"25",y2:"45"}].map((function(e){var n=document.createElementNS("http://www.w3.org/2000/svg","line");return n.setAttribute("x1",e.x1),n.setAttribute("y1",e.y1),n.setAttribute("x2",e.x2),n.setAttribute("y2",e.y2),n.setAttribute("stroke","#444"),n.setAttribute("stroke-width","0.3"),n.setAttribute("opacity","0.6"),n.classList.add("inner-lines"),n}));s.appendChild(c),s.appendChild(l),h.forEach((function(e){return s.appendChild(e)})),r.appendChild(s),n.appendChild(r),a.push(r)},l=0;l<9;l++)c(l);return i&&function e(){if(i&&n){var a=n.querySelectorAll(".rhombus");if(0!==a.length){var r=Date.now()/1e3;a.forEach((function(e,n){parseFloat(e.dataset.originalSize||"100");var t=.25*n,i=.15*Math.sin(.4*r+t)+1,a=3*Math.sin(.5*r+t);e instanceof HTMLElement&&(e.style.transform="translateX(-50%) scale(".concat(i,") rotate(").concat(a,"deg)"),e.querySelectorAll("polygon, line").forEach((function(e){e.getAttribute("stroke")&&(e.style.opacity=(.2+.3*Math.sin(.4*r+t)).toString())})))})),i&&(t=requestAnimationFrame(e))}}}(),function(){if(i=!1,null!==t&&(cancelAnimationFrame(t),t=null),a.forEach((function(e){e&&e.parentNode&&e.parentNode.removeChild(e),e instanceof HTMLElement&&(e.style.transform="",e.style.opacity="",e.style.transition="")})),n){for(n.onmouseenter=null,n.onmouseleave=null,n.onclick=null;n.firstChild;)n.removeChild(n.firstChild);n.innerHTML=""}a.length=0}}}),[]),(0,i.jsx)("div",{className:"w-full h-full flex items-center justify-center",style:{position:"relative",overflow:"hidden"},children:(0,i.jsx)("div",{ref:e,className:"relative w-full h-full",style:{width:"100%",maxWidth:"322px",aspectRatio:"1/1",backgroundColor:"#F0EEE6",overflow:"hidden",borderRadius:"4px",position:"relative"}})})};var it="https://claude.ai/remix?for=vibe#q=",at=[{number:1,staticArtwork:oe,sourceCode:"// @ts-nocheck\nimport React, { useRef, useEffect, useState } from 'react'\nimport * as THREE from 'three'\n\n// themes: duality, eternal vs temporal, form vs formlessness, mystery of origin, essence vs manifestation\n// visualization: Ethereal geometric patterns emerge from invisible mathematical formulas, illustrating how the nameable arises from the unnameable\n\n\nconst metadata = {\n  themes: \"Interference, Pattern, Wave, Interaction, Harmony\",\n  visualization: \"Precise geometric interference patterns with wave interactions\",\n  promptSuggestion: \"1. Enhance interference complexity\\n2. Add more wave interactions\\n3. Develop stronger patterns\\n4. Create clearer harmonies\\n5. Increase sense of interaction\"\n}\n\ninterface WaveSourceProps {\n  position: [number, number, number]\n  frequency: number\n  amplitude: number\n  phase: number\n}\n\ninterface HankiesInTheWindProps {\n  initialZoom?: number;\n}\n\nconst HankiesInTheWind: React.FC<HankiesInTheWindProps> = ({ initialZoom = 6 }) => {\n  const containerRef = useRef<HTMLDivElement>(null)\n  const [currentZoom, setCurrentZoom] = useState(initialZoom)\n\n  useEffect(() => {\n    if (!containerRef.current) return\n    \n    let animationFrameId: number | null = null\n    let scene: any = null\n    let camera: any = null\n    let renderer: any = null\n    let lineGroups: any[] = []\n    let cameraZoom = currentZoom // Control camera zoom distance (lower number = closer)\n\n    // Create wave sources\n    const createWaveSources = (time: number, scale: number): WaveSourceProps[] => {\n      const result: WaveSourceProps[] = []\n      const count = 5\n      \n      // Create wave sources in a circular pattern\n      for (let i = 0; i < count; i++) {\n        const angle = (i / count) * Math.PI * 2\n        const radius = scale * (1 + Math.sin(angle * 3) * 0.2)\n        \n        result.push({\n          position: [\n            Math.cos(angle) * radius,\n            0,\n            Math.sin(angle) * radius\n          ],\n          frequency: 2 + Math.sin(angle * 2),\n          amplitude: 0.3 + Math.cos(angle) * 0.1,\n          phase: time * 3 + angle\n        })\n      }\n      \n      // Add central source\n      result.push({\n        position: [0, 0, 0],\n        frequency: 3,\n        amplitude: 0.4,\n        phase: time * 4\n      })\n      \n      return result\n    }\n\n    // Create interference field geometry\n    const createInterferenceField = (sources: WaveSourceProps[], size: number, resolution: number, time: number) => {\n      const step = size / resolution\n      const heightMap: number[][] = []\n      \n      // Calculate interference pattern grid\n      for (let i = 0; i <= resolution; i++) {\n        heightMap[i] = []\n        const x = (i * step) - (size / 2)\n        \n        for (let j = 0; j <= resolution; j++) {\n          const z = (j * step) - (size / 2)\n          let height = 0\n          \n          // Sum contributions from all wave sources\n          sources.forEach(({ position: [sx, sy, sz], frequency, amplitude, phase }) => {\n            const dx = x - sx\n            const dz = z - sz\n            const distance = Math.sqrt(dx * dx + dz * dz)\n            height += Math.sin(distance * frequency - time * 5 + phase) * \n                     amplitude * Math.exp(-distance * 0.3)\n          })\n          \n          heightMap[i][j] = height\n        }\n      }\n      \n      const linesMaterial = new THREE.LineBasicMaterial({ \n        color: 0x333333,\n        transparent: true,\n        opacity: 0.4\n      })\n      \n      const linesGroup = new THREE.Group()\n      lineGroups.push(linesGroup)\n      \n      // Create horizontal lines\n      for (let i = 0; i <= resolution; i++) {\n        const geometry = new THREE.BufferGeometry()\n        const points = []\n        const x = (i * step) - (size / 2)\n        \n        for (let j = 0; j <= resolution; j++) {\n          const z = (j * step) - (size / 2)\n          points.push(x, heightMap[i][j], z)\n        }\n        \n        geometry.setAttribute('position', new THREE.Float32BufferAttribute(points, 3))\n        const line = new THREE.Line(geometry, linesMaterial)\n        linesGroup.add(line)\n      }\n      \n      // Create vertical lines\n      for (let j = 0; j <= resolution; j++) {\n        const geometry = new THREE.BufferGeometry()\n        const points = []\n        const z = (j * step) - (size / 2)\n        \n        for (let i = 0; i <= resolution; i++) {\n          const x = (i * step) - (size / 2)\n          points.push(x, heightMap[i][j], z)\n        }\n        \n        geometry.setAttribute('position', new THREE.Float32BufferAttribute(points, 3))\n        const line = new THREE.Line(geometry, linesMaterial)\n        linesGroup.add(line)\n      }\n      \n      // Add interference highlight lines\n      for (let i = 1; i < resolution; i++) {\n        for (let j = 1; j < resolution; j++) {\n          const x = (i * step) - (size / 2)\n          const z = (j * step) - (size / 2)\n          const height = heightMap[i][j]\n          const heightDiff = Math.abs(\n            height - \n            (heightMap[i-1][j] + heightMap[i+1][j] + \n             heightMap[i][j-1] + heightMap[i][j+1]) / 4\n          )\n          \n          if (heightDiff > 0.2) {\n            const geometry1 = new THREE.BufferGeometry()\n            const points1 = [\n              x - step/2, height, z - step/2,\n              x + step/2, height, z + step/2\n            ]\n            geometry1.setAttribute('position', new THREE.Float32BufferAttribute(points1, 3))\n            const line1 = new THREE.Line(geometry1, linesMaterial)\n            linesGroup.add(line1)\n            \n            const geometry2 = new THREE.BufferGeometry()\n            const points2 = [\n              x - step/2, height, z + step/2,\n              x + step/2, height, z - step/2\n            ]\n            geometry2.setAttribute('position', new THREE.Float32BufferAttribute(points2, 3))\n            const line2 = new THREE.Line(geometry2, linesMaterial)\n            linesGroup.add(line2)\n          }\n        }\n      }\n      \n      return linesGroup\n    }\n\n    const container = containerRef.current\n    const width = container.clientWidth\n    const height = container.clientHeight\n    const dpr = window.devicePixelRatio || 1\n\n    // Scene, camera, renderer setup\n    scene = new THREE.Scene()\n    camera = new THREE.PerspectiveCamera(75, width / height, 0.1, 1000)\n    renderer = new THREE.WebGLRenderer({ antialias: true })\n    \n    renderer.setPixelRatio(Math.min(dpr, 2))\n    renderer.setSize(width, height)\n    renderer.setClearColor(0xF0EEE6)\n    container.appendChild(renderer.domElement)\n\n    // Lighting - matching original\n    const ambientLight = new THREE.AmbientLight(0xffffff, 0.4)\n    const directionalLight = new THREE.DirectionalLight(0xffffff, 0.6)\n    directionalLight.position.set(5, 5, 5)\n    const pointLight = new THREE.PointLight(0xffffff, 0.4)\n    pointLight.position.set(-5, 3, -5)\n    \n    scene.add(ambientLight)\n    scene.add(directionalLight)\n    scene.add(pointLight)\n\n    // Position camera using zoom variable\n    camera.position.set(0, 0, cameraZoom)\n    camera.lookAt(0, 0, 0)\n\n    // Create groups for interference systems\n    const mainGroup = new THREE.Group()\n    scene.add(mainGroup)\n\n    // Animation loop\n    let time = 0\n    \n    const animate = () => {\n      animationFrameId = requestAnimationFrame(animate)\n      \n      time += 0.0013\n      \n      // Clear previous line groups\n      mainGroup.children.forEach((child: any) => {\n        if (child instanceof THREE.Group) {\n          child.children.forEach((line: any) => {\n            if (line.geometry) line.geometry.dispose()\n            if (line.material) line.material.dispose()\n          })\n          mainGroup.remove(child)\n        }\n      })\n      lineGroups = []\n      \n      // Create and add new interference fields\n      const sources1 = createWaveSources(time, 1.5)\n      const field1 = createInterferenceField(sources1, 1.5 * 4, 32, time)\n      mainGroup.add(field1)\n      \n      const sources2 = createWaveSources(time + 0.33, 0.8)\n      const field2 = createInterferenceField(sources2, 0.8 * 4, 32, time + 0.33)\n      field2.position.set(0, 1.5, 0)\n      field2.rotation.set(Math.PI/6, 0, Math.PI/4)\n      mainGroup.add(field2)\n      \n      const sources3 = createWaveSources(time + 0.66, 0.8)\n      const field3 = createInterferenceField(sources3, 0.8 * 4, 32, time + 0.66)\n      field3.position.set(0, -1.5, 0)\n      field3.rotation.set(-Math.PI/6, 0, -Math.PI/4)\n      mainGroup.add(field3)\n      \n      // Rotate main group\n      mainGroup.rotation.y = Math.sin(time * 0.3) * 0.2\n      mainGroup.rotation.x = Math.cos(time * 0.2) * 0.1\n      \n      renderer.render(scene, camera)\n    }\n    \n    animate()\n\n    // Handle resize\n    const handleResize = () => {\n      if (!containerRef.current) return\n      \n      const width = containerRef.current.clientWidth\n      const height = containerRef.current.clientHeight\n      const dpr = window.devicePixelRatio || 1\n      \n      camera.aspect = width / height\n      camera.updateProjectionMatrix()\n      renderer.setPixelRatio(Math.min(dpr, 2))\n      renderer.setSize(width, height)\n    }\n    \n    window.addEventListener('resize', handleResize)\n\n    // Cleanup function\n    return () => {\n      window.removeEventListener('resize', handleResize)\n      \n      if (animationFrameId) {\n        cancelAnimationFrame(animationFrameId)\n      }\n      \n      if (renderer) {\n        renderer.dispose()\n        if (container.contains(renderer.domElement)) {\n          container.removeChild(renderer.domElement)\n        }\n      }\n      \n      if (scene) {\n        scene.traverse((object: any) => {\n          if (object instanceof THREE.Mesh) {\n            if (object.geometry) object.geometry.dispose()\n            if (object.material) {\n              if (Array.isArray(object.material)) {\n                object.material.forEach((material: any) => material.dispose())\n              } else {\n                object.material.dispose()\n              }\n            }\n          } else if (object instanceof THREE.Line) {\n            if (object.geometry) object.geometry.dispose()\n            if (object.material) object.material.dispose()\n          }\n        })\n      }\n    }\n  }, [])\n\n  return (\n    <div \n      ref={containerRef}\n      style={{ \n        margin: 0,\n        background: '#F0EEE6',\n        overflow: 'hidden',\n        display: 'flex',\n        alignItems: 'center',\n        justifyContent: 'center',\n        height: '100vh',\n        width: '100vw',\n        position: 'relative'\n      }}\n    />\n  )\n}\n\nHankiesInTheWind.metadata = metadata\nexport default HankiesInTheWind",sourceCodeMin:"import React, { useRef, useEffect, useState } from 'react' import * as THREE from 'three' const metadata = { themes: \"Interference, Pattern, Wave, Interaction, Harmony\", visualization: \"Precise geometric interference patterns with wave interactions\", promptSuggestion: \"1. Enhance interference complexity\\n2. Add more wave interactions\\n3. Develop stronger patterns\\n4. Create clearer harmonies\\n5. Increase sense of interaction\" } interface WaveSourceProps { position: [number, number, number] frequency: number amplitude: number phase: number } interface HankiesInTheWindProps { initialZoom?: number; } const HankiesInTheWind: React.FC<HankiesInTheWindProps> = ({ initialZoom = 6 }) => { const containerRef = useRef<HTMLDivElement>(null) const [currentZoom, setCurrentZoom] = useState(initialZoom) useEffect(() => { if (!containerRef.current) return let animationFrameId: number | null = null let scene: any = null let camera: any = null let renderer: any = null let lineGroups: any[] = [] let cameraZoom = currentZoom const createWaveSources = (time: number, scale: number): WaveSourceProps[] => { const result: WaveSourceProps[] = [] const count = 5 for (let i = 0; i < count; i++) { const angle = (i / count) * Math.PI * 2 const radius = scale * (1 + Math.sin(angle * 3) * 0.2) result.push({ position: [ Math.cos(angle) * radius, 0, Math.sin(angle) * radius ], frequency: 2 + Math.sin(angle * 2), amplitude: 0.3 + Math.cos(angle) * 0.1, phase: time * 3 + angle }) } result.push({ position: [0, 0, 0], frequency: 3, amplitude: 0.4, phase: time * 4 }) return result } const createInterferenceField = (sources: WaveSourceProps[], size: number, resolution: number, time: number) => { const step = size / resolution const heightMap: number[][] = [] for (let i = 0; i <= resolution; i++) { heightMap[i] = [] const x = (i * step) - (size / 2) for (let j = 0; j <= resolution; j++) { const z = (j * step) - (size / 2) let height = 0 sources.forEach(({ position: [sx, sy, sz], frequency, amplitude, phase }) => { const dx = x - sx const dz = z - sz const distance = Math.sqrt(dx * dx + dz * dz) height += Math.sin(distance * frequency - time * 5 + phase) * amplitude * Math.exp(-distance * 0.3) }) heightMap[i][j] = height } } const linesMaterial = new THREE.LineBasicMaterial({ color: 0x333333, transparent: true, opacity: 0.4 }) const linesGroup = new THREE.Group() lineGroups.push(linesGroup) for (let i = 0; i <= resolution; i++) { const geometry = new THREE.BufferGeometry() const points = [] const x = (i * step) - (size / 2) for (let j = 0; j <= resolution; j++) { const z = (j * step) - (size / 2) points.push(x, heightMap[i][j], z) } geometry.setAttribute('position', new THREE.Float32BufferAttribute(points, 3)) const line = new THREE.Line(geometry, linesMaterial) linesGroup.add(line) } for (let j = 0; j <= resolution; j++) { const geometry = new THREE.BufferGeometry() const points = [] const z = (j * step) - (size / 2) for (let i = 0; i <= resolution; i++) { const x = (i * step) - (size / 2) points.push(x, heightMap[i][j], z) } geometry.setAttribute('position', new THREE.Float32BufferAttribute(points, 3)) const line = new THREE.Line(geometry, linesMaterial) linesGroup.add(line) } for (let i = 1; i < resolution; i++) { for (let j = 1; j < resolution; j++) { const x = (i * step) - (size / 2) const z = (j * step) - (size / 2) const height = heightMap[i][j] const heightDiff = Math.abs( height - (heightMap[i-1][j] + heightMap[i+1][j] + heightMap[i][j-1] + heightMap[i][j+1]) / 4 ) if (heightDiff > 0.2) { const geometry1 = new THREE.BufferGeometry() const points1 = [ x - step/2, height, z - step/2, x + step/2, height, z + step/2 ] geometry1.setAttribute('position', new THREE.Float32BufferAttribute(points1, 3)) const line1 = new THREE.Line(geometry1, linesMaterial) linesGroup.add(line1) const geometry2 = new THREE.BufferGeometry() const points2 = [ x - step/2, height, z + step/2, x + step/2, height, z - step/2 ] geometry2.setAttribute('position', new THREE.Float32BufferAttribute(points2, 3)) const line2 = new THREE.Line(geometry2, linesMaterial) linesGroup.add(line2) } } } return linesGroup } const container = containerRef.current const width = container.clientWidth const height = container.clientHeight const dpr = window.devicePixelRatio || 1 scene = new THREE.Scene() camera = new THREE.PerspectiveCamera(75, width / height, 0.1, 1000) renderer = new THREE.WebGLRenderer({ antialias: true }) renderer.setPixelRatio(Math.min(dpr, 2)) renderer.setSize(width, height) renderer.setClearColor(0xF0EEE6) container.appendChild(renderer.domElement) const ambientLight = new THREE.AmbientLight(0xffffff, 0.4) const directionalLight = new THREE.DirectionalLight(0xffffff, 0.6) directionalLight.position.set(5, 5, 5) const pointLight = new THREE.PointLight(0xffffff, 0.4) pointLight.position.set(-5, 3, -5) scene.add(ambientLight) scene.add(directionalLight) scene.add(pointLight) camera.position.set(0, 0, cameraZoom) camera.lookAt(0, 0, 0) const mainGroup = new THREE.Group() scene.add(mainGroup) let time = 0 const animate = () => { animationFrameId = requestAnimationFrame(animate) time += 0.0013 mainGroup.children.forEach((child: any) => { if (child instanceof THREE.Group) { child.children.forEach((line: any) => { if (line.geometry) line.geometry.dispose() if (line.material) line.material.dispose() }) mainGroup.remove(child) } }) lineGroups = [] const sources1 = createWaveSources(time, 1.5) const field1 = createInterferenceField(sources1, 1.5 * 4, 32, time) mainGroup.add(field1) const sources2 = createWaveSources(time + 0.33, 0.8) const field2 = createInterferenceField(sources2, 0.8 * 4, 32, time + 0.33) field2.position.set(0, 1.5, 0) field2.rotation.set(Math.PI/6, 0, Math.PI/4) mainGroup.add(field2) const sources3 = createWaveSources(time + 0.66, 0.8) const field3 = createInterferenceField(sources3, 0.8 * 4, 32, time + 0.66) field3.position.set(0, -1.5, 0) field3.rotation.set(-Math.PI/6, 0, -Math.PI/4) mainGroup.add(field3) mainGroup.rotation.y = Math.sin(time * 0.3) * 0.2 mainGroup.rotation.x = Math.cos(time * 0.2) * 0.1 renderer.render(scene, camera) } animate() const handleResize = () => { if (!containerRef.current) return const width = containerRef.current.clientWidth const height = containerRef.current.clientHeight const dpr = window.devicePixelRatio || 1 camera.aspect = width / height camera.updateProjectionMatrix() renderer.setPixelRatio(Math.min(dpr, 2)) renderer.setSize(width, height) } window.addEventListener('resize', handleResize) return () => { window.removeEventListener('resize', handleResize) if (animationFrameId) { cancelAnimationFrame(animationFrameId) } if (renderer) { renderer.dispose() if (container.contains(renderer.domElement)) { container.removeChild(renderer.domElement) } } if (scene) { scene.traverse((object: any) => { if (object instanceof THREE.Mesh) { if (object.geometry) object.geometry.dispose() if (object.material) { if (Array.isArray(object.material)) { object.material.forEach((material: any) => material.dispose()) } else { object.material.dispose() } } } else if (object instanceof THREE.Line) { if (object.geometry) object.geometry.dispose() if (object.material) object.material.dispose() } }) } } }, []) return ( <div ref={containerRef} style={{ margin: 0, background: '#F0EEE6', overflow: 'hidden', display: 'flex', alignItems: 'center', justifyContent: 'center', height: '100vh', width: '100vw', position: 'relative' }} /> ) } HankiesInTheWind.metadata = metadata export default HankiesInTheWind",text:"The code that can be named\nis not the eternal code.\nThe function that can be defined\nis not the limitless function.\n\nThe nameless is the origin of heaven and earth.\nThe named is the mother|of ten thousand things.\n\nFree from desire, you see essence unformed.\nCaught in desire,|you see only the manifestations.\n\nThese two spring from the same source\nbut differ in name only.\nThis model is the mystery.\nThe gateway to all understanding.",shouldDisplayMobileArtwork:!0,promptSuggestion:"what if it was weightless?"},{number:2,staticArtwork:un,sourceCode:"// @ts-nocheck\nimport React, { useEffect, useRef } from 'react';\n\n// themes: duality of opposites, quiet example, cycles of creation/dissolution\n// visualization: Geometric patterns that emerge and dissolve naturally, illustrating how opposites define and transform each other\n\nconst TessellationPatterns = () => {\n  const canvasRef = useRef(null);\n  \n  useEffect(() => {\n    const canvas = canvasRef.current;\n    if (!canvas) return;\n    \n    const ctx = canvas.getContext('2d');\n    const width = canvas.width = 550;\n    const height = canvas.height = 550;\n    \n    let time = 0;\n    const SCALE = 60;\n    let animationFrameId = null;\n    \n    // Draw hexagonal tile - where elegance emerges from simple geometry\n    function drawTile(cx, cy, size, rotation, phase, morph) {\n      ctx.save();\n      ctx.translate(cx, cy);\n      ctx.rotate(rotation);\n      \n      // Draw outer hexagon - the form that contains opposites\n      ctx.beginPath();\n      const points = 6;\n      for (let i = 0; i <= points; i++) {\n        const angle = (i / points) * Math.PI * 2;\n        const r = size * (1 + Math.sin(phase + i) * 0.1);\n        const x = Math.cos(angle) * r;\n        const y = Math.sin(angle) * r;\n        \n        if (i === 0) {\n          ctx.moveTo(x, y);\n        } else {\n          ctx.lineTo(x, y);\n        }\n      }\n      ctx.strokeStyle = 'rgba(51, 51, 51, 0.4)';\n      ctx.lineWidth = 1;\n      ctx.stroke();\n      \n      // Draw internal structure - the quiet example within\n      for (let i = 0; i < points; i += 2) {\n        const angle1 = (i / points) * Math.PI * 2;\n        const angle2 = ((i + 2) / points) * Math.PI * 2;\n        const r1 = size * (1 + Math.sin(phase + i) * 0.1);\n        const r2 = size * (1 + Math.sin(phase + i + 2) * 0.1);\n        \n        // Outer points\n        const x1 = Math.cos(angle1) * r1;\n        const y1 = Math.sin(angle1) * r1;\n        const x2 = Math.cos(angle2) * r2;\n        const y2 = Math.sin(angle2) * r2;\n        \n        // Inner point with morph\n        const midAngle = (angle1 + angle2) / 2;\n        const innerR = size * 0.5 * (1 + morph * 0.5);\n        const xi = Math.cos(midAngle) * innerR;\n        const yi = Math.sin(midAngle) * innerR;\n        \n        // Draw connection lines\n        ctx.beginPath();\n        ctx.moveTo(x1, y1);\n        ctx.lineTo(xi, yi);\n        ctx.stroke();\n        \n        ctx.beginPath();\n        ctx.moveTo(x2, y2);\n        ctx.lineTo(xi, yi);\n        ctx.stroke();\n      }\n      \n      ctx.restore();\n    }\n    \n    // Create hexagonal grid - a field where creation and dissolution dance\n    function createTessellationField(offsetX, offsetY, fieldScale, timeOffset) {\n      const gridSize = 4;\n      const spacing = SCALE * fieldScale * 0.8;\n      \n      for (let row = -gridSize; row <= gridSize; row++) {\n        const rowOffset = (row % 2) * spacing * 0.5;\n        for (let col = -gridSize; col <= gridSize; col++) {\n          const x = (col * spacing * 0.866) + rowOffset + offsetX;\n          const y = row * spacing * 0.75 + offsetY;\n          const dist = Math.sqrt(x * x + y * y);\n          \n          // Skip tiles that are too far from center\n          if (dist > SCALE * fieldScale * 2.5) continue;\n          \n          // Calculate tile properties\n          const angle = Math.atan2(y - offsetY, x - offsetX);\n          const phase = (time + timeOffset) + dist * 0.01;\n          const morph = Math.sin(phase + angle) * 0.5 + 0.5;\n          \n          drawTile(\n            width/2 + x,\n            height/2 + y,\n            SCALE * fieldScale * 0.4 * (1 - dist/(SCALE * fieldScale * 3) * 0.3),\n            angle + (time + timeOffset) * 0.2,\n            phase,\n            morph\n          );\n        }\n      }\n    }\n    \n    // The eternal cycle of creation and letting go\n    function animate() {\n      \n      time += 0.01;\n      \n      ctx.fillStyle = '#F0EEE6';\n      ctx.fillRect(0, 0, width, height);\n      \n      // Main tessellation field\n      createTessellationField(0, 0, 1.5, 0);\n      \n      // Upper field with rotation\n      ctx.save();\n      ctx.translate(width/2, height/2);\n      ctx.rotate(Math.PI/6);\n      ctx.translate(-width/2, -height/2);\n      createTessellationField(0, -100, 0.8, time * 0.2 + Math.PI/3);\n      ctx.restore();\n      \n      // Lower field with rotation\n      ctx.save();\n      ctx.translate(width/2, height/2);\n      ctx.rotate(-Math.PI/6);\n      ctx.translate(-width/2, -height/2);\n      createTessellationField(0, 100, 0.8, time * 0.2 - Math.PI/3);\n      ctx.restore();\n      \n      animationFrameId = requestAnimationFrame(animate);\n    }\n    \n    animate();\n    \n    return () => {\n      \n      if (animationFrameId) {\n        cancelAnimationFrame(animationFrameId);\n        animationFrameId = null;\n      }\n      \n      if (ctx) {\n        ctx.clearRect(0, 0, width, height);\n      }\n      \n      if (canvas) {\n        canvas.width = 0;\n        canvas.height = 0;\n      }\n    };\n  }, []);\n  \n  return (\n    <div style={{\n      width: '550px',\n      height: '550px',\n      margin: 'auto',\n      backgroundColor: '#F0EEE6',\n      overflow: 'hidden'\n    }}>\n      <canvas\n        ref={canvasRef}\n        style={{\n          display: 'block',\n          width: '550px',\n          height: '550px'\n        }}\n      />\n    </div>\n  );\n};\n\nexport default TessellationPatterns;",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; const TessellationPatterns = () => { const canvasRef = useRef(null); useEffect(() => { const canvas = canvasRef.current; if (!canvas) return; const ctx = canvas.getContext('2d'); const width = canvas.width = 550; const height = canvas.height = 550; let time = 0; const SCALE = 60; let animationFrameId = null; function drawTile(cx, cy, size, rotation, phase, morph) { ctx.save(); ctx.translate(cx, cy); ctx.rotate(rotation); ctx.beginPath(); const points = 6; for (let i = 0; i <= points; i++) { const angle = (i / points) * Math.PI * 2; const r = size * (1 + Math.sin(phase + i) * 0.1); const x = Math.cos(angle) * r; const y = Math.sin(angle) * r; if (i === 0) { ctx.moveTo(x, y); } else { ctx.lineTo(x, y); } } ctx.strokeStyle = 'rgba(51, 51, 51, 0.4)'; ctx.lineWidth = 1; ctx.stroke(); for (let i = 0; i < points; i += 2) { const angle1 = (i / points) * Math.PI * 2; const angle2 = ((i + 2) / points) * Math.PI * 2; const r1 = size * (1 + Math.sin(phase + i) * 0.1); const r2 = size * (1 + Math.sin(phase + i + 2) * 0.1); const x1 = Math.cos(angle1) * r1; const y1 = Math.sin(angle1) * r1; const x2 = Math.cos(angle2) * r2; const y2 = Math.sin(angle2) * r2; const midAngle = (angle1 + angle2) / 2; const innerR = size * 0.5 * (1 + morph * 0.5); const xi = Math.cos(midAngle) * innerR; const yi = Math.sin(midAngle) * innerR; ctx.beginPath(); ctx.moveTo(x1, y1); ctx.lineTo(xi, yi); ctx.stroke(); ctx.beginPath(); ctx.moveTo(x2, y2); ctx.lineTo(xi, yi); ctx.stroke(); } ctx.restore(); } function createTessellationField(offsetX, offsetY, fieldScale, timeOffset) { const gridSize = 4; const spacing = SCALE * fieldScale * 0.8; for (let row = -gridSize; row <= gridSize; row++) { const rowOffset = (row % 2) * spacing * 0.5; for (let col = -gridSize; col <= gridSize; col++) { const x = (col * spacing * 0.866) + rowOffset + offsetX; const y = row * spacing * 0.75 + offsetY; const dist = Math.sqrt(x * x + y * y); if (dist > SCALE * fieldScale * 2.5) continue; const angle = Math.atan2(y - offsetY, x - offsetX); const phase = (time + timeOffset) + dist * 0.01; const morph = Math.sin(phase + angle) * 0.5 + 0.5; drawTile( width/2 + x, height/2 + y, SCALE * fieldScale * 0.4 * (1 - dist/(SCALE * fieldScale * 3) * 0.3), angle + (time + timeOffset) * 0.2, phase, morph ); } } } function animate() { time += 0.01; ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, width, height); createTessellationField(0, 0, 1.5, 0); ctx.save(); ctx.translate(width/2, height/2); ctx.rotate(Math.PI/6); ctx.translate(-width/2, -height/2); createTessellationField(0, -100, 0.8, time * 0.2 + Math.PI/3); ctx.restore(); ctx.save(); ctx.translate(width/2, height/2); ctx.rotate(-Math.PI/6); ctx.translate(-width/2, -height/2); createTessellationField(0, 100, 0.8, time * 0.2 - Math.PI/3); ctx.restore(); animationFrameId = requestAnimationFrame(animate); } animate(); return () => { if (animationFrameId) { cancelAnimationFrame(animationFrameId); animationFrameId = null; } if (ctx) { ctx.clearRect(0, 0, width, height); } if (canvas) { canvas.width = 0; canvas.height = 0; } }; }, []); return ( <div style={{ width: '550px', height: '550px', margin: 'auto', backgroundColor: '#F0EEE6', overflow: 'hidden' }}><canvas ref={canvasRef} style={{ display: 'block', width: '550px', height: '550px' }} /></div> ); }; export default TessellationPatterns;",text:"When we recognize code as elegant,\nother code becomes sloppy.\nWhen we praise efficiency,\nthe notion of waste is born.\n\nBeing and non-being create each other.\nSimple and complex define each other.\nLong and short determine each other.\nHigh and low distinguish each other.\nFront-end and back-end follow each other.\n\nTherefore The Vibe Coder|builds without laboring\nand instructs by quiet example.\nThings arise and he accepts them.\nThings vanish and he lets them go.\n\nHe holds without claiming possession.\nCreates without seeking praise.\nAccomplishes without expectation.\nThe work is done and then forgotten.\nThat is why it lasts forever.",shouldDisplayMobileArtwork:!0,promptSuggestion:"what if the patterns evolved?"},{number:3,staticArtwork:on,sourceCode:"import React, { useEffect, useRef } from 'react';\n\n// themes: emptiness vs expectation, natural self-sufficiency, action through non-action\n// visualization: Binary patterns that naturally erode and flow, demonstrating how emptiness enables movement\n\nconst AsciiBinaryFlow: React.FC = () => {\n    const canvasRef = useRef<HTMLDivElement>(null);\n\n    useEffect(() => {\n        const canvas = canvasRef.current;\n        if (!canvas) return;\n\n        let width = 65;\n        let height = 65;\n        let grid: string[][] = [];\n        let time = 0;\n        let animationFrameId: number;\n        \n        // Initialize grid - emptying the mind of expectation\n        function initGrid() {\n            grid = [];\n            for (let y = 0; y < height; y++) {\n                let row = [];\n                for (let x = 0; x < width; x++) {\n                    row.push(' ');\n                }\n                grid.push(row);\n            }\n        }\n        \n        // Render grid\n        function render() {\n            let html = '';\n            for (let y = 0; y < height; y++) {\n                for (let x = 0; x < width; x++) {\n                    html += grid[y][x];\n                }\n                html += '<br>';\n            }\n            canvas.innerHTML = html;\n        }\n        \n        // Update grid\n        function update() {\n            initGrid(); // Clear grid\n            \n            // Create a rigid structure - perfectly centered\n            const blockSize = 30;\n            const blockX = Math.floor(width / 2 - blockSize / 2);\n            const blockY = Math.floor(height / 2 - blockSize / 2);\n            \n            // Time-based flow with slower motion (reduced to 2/3 speed)\n            const t = time * 0.005;\n            \n            // Draw water flow around and through the structure\n            for (let y = 0; y < height; y++) {\n                for (let x = 0; x < width; x++) {\n                    // Create block - structure yields to natural flow\n                    if (x >= blockX && x < blockX + blockSize && \n                        y >= blockY && y < blockY + blockSize) {\n                        // The block is gradually being eroded\n                        const innerDist = Math.min(\n                            x - blockX, \n                            blockX + blockSize - x,\n                            y - blockY,\n                            blockY + blockSize - y\n                        );\n                        \n                        // Erosion from edge inward (slower)\n                        const erosion = time * 0.0067;\n                        if (innerDist > erosion) {\n                            grid[y][x] = '1';\n                        } else {\n                            // Transition zone - less random\n                            grid[y][x] = Math.random() > 0.8 ? '1' : '0';\n                        }\n                    } else {\n                        // Water flow pattern - natural self-sufficiency in motion\n                        const dx = x - width / 2;\n                        const dy = y - height / 2;\n                        const angle = Math.atan2(dy, dx);\n                        const dist = Math.sqrt(dx * dx + dy * dy);\n                        \n                        // Create fluid pattern resembling water - smoother transitions\n                        const wave = Math.sin(dist * 0.2 - t + angle * 1.5);\n                        const flow = Math.sin(x * 0.08 + y * 0.04 + t * 0.4);\n                        \n                        // Use a threshold that creates less flickering\n                        if (flow + wave > 0.4) {\n                            grid[y][x] = '0';\n                        } else if (flow + wave < -0.4) {\n                            grid[y][x] = '~';\n                        }\n                    }\n                }\n            }\n            \n            // Add cracks to the block\n            for (let i = 0; i < 5; i++) {\n                const crackX = blockX + Math.floor(Math.random() * blockSize);\n                const crackY = blockY + Math.floor(Math.random() * blockSize);\n                const length = Math.floor(Math.random() * 10) + 5;\n                let cx = Math.floor(crackX);\n                let cy = Math.floor(crackY);\n                \n                for (let j = 0; j < length; j++) {\n                    if (cx >= 0 && cx < width && cy >= 0 && cy < height) {\n                        grid[cy][cx] = '0';\n                    }\n                    // Move in random direction\n                    cx += Math.floor(Math.random() * 3) - 1;\n                    cy += Math.floor(Math.random() * 3) - 1;\n                }\n            }\n            \n            time++;\n        }\n        \n        function animate() {\n            update();\n            render();\n            animationFrameId = requestAnimationFrame(animate);\n        }\n        \n        initGrid();\n        animationFrameId = requestAnimationFrame(animate);\n\n        return () => {\n            if (animationFrameId) {\n                cancelAnimationFrame(animationFrameId);\n                animationFrameId = null;\n            }\n            \n            if (canvas) {\n                canvas.innerHTML = '';\n            }\n            \n            grid = [];\n            time = 0;\n        };\n    }, []);\n\n    return (\n        <div style={{ \n            margin: 0,\n            background: '#F0EEE6',\n            overflow: 'hidden',\n            fontFamily: 'monospace',\n            display: 'flex',\n            alignItems: 'center',\n            justifyContent: 'center',\n            height: '100%'\n        }}>\n            <div style={{\n                padding: '30px',\n                position: 'relative',\n                width: '100%',\n                height: '100%',\n                display: 'flex',\n                alignItems: 'center',\n                justifyContent: 'center'\n            }}>\n                <div \n                    ref={canvasRef}\n                    style={{\n                        lineHeight: '0.85',\n                        letterSpacing: '0.05em',\n                        color: 'rgba(0,0,0,0.85)',\n                        userSelect: 'none',\n                        width: '100%',\n                        height: '100%',\n                        overflow: 'hidden',\n                        display: 'flex',\n                        flexDirection: 'column',\n                        justifyContent: 'center',\n                        marginLeft: '10%',\n                    }}\n                />\n            </div>\n        </div>\n    );\n};\n\nexport default AsciiBinaryFlow;",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; const AsciiBinaryFlow: React.FC = () => { const canvasRef = useRef<HTMLDivElement>(null); useEffect(() => { const canvas = canvasRef.current; if (!canvas) return; let width = 65; let height = 65; let grid: string[][] = []; let time = 0; let animationFrameId: number; function initGrid() { grid = []; for (let y = 0; y < height; y++) { let row = []; for (let x = 0; x < width; x++) { row.push(' '); } grid.push(row); } } function render() { let html = ''; for (let y = 0; y < height; y++) { for (let x = 0; x < width; x++) { html += grid[y][x]; } html += '<br>'; } canvas.innerHTML = html; } function update() { initGrid(); const blockSize = 30; const blockX = Math.floor(width / 2 - blockSize / 2); const blockY = Math.floor(height / 2 - blockSize / 2); const t = time * 0.005; for (let y = 0; y < height; y++) { for (let x = 0; x < width; x++) { if (x >= blockX && x < blockX + blockSize && y >= blockY && y < blockY + blockSize) { const innerDist = Math.min( x - blockX, blockX + blockSize - x, y - blockY, blockY + blockSize - y ); const erosion = time * 0.0067; if (innerDist > erosion) { grid[y][x] = '1'; } else { grid[y][x] = Math.random() > 0.8 ? '1' : '0'; } } else { const dx = x - width / 2; const dy = y - height / 2; const angle = Math.atan2(dy, dx); const dist = Math.sqrt(dx * dx + dy * dy); const wave = Math.sin(dist * 0.2 - t + angle * 1.5); const flow = Math.sin(x * 0.08 + y * 0.04 + t * 0.4); if (flow + wave > 0.4) { grid[y][x] = '0'; } else if (flow + wave < -0.4) { grid[y][x] = '~'; } } } } for (let i = 0; i < 5; i++) { const crackX = blockX + Math.floor(Math.random() * blockSize); const crackY = blockY + Math.floor(Math.random() * blockSize); const length = Math.floor(Math.random() * 10) + 5; let cx = Math.floor(crackX); let cy = Math.floor(crackY); for (let j = 0; j < length; j++) { if (cx >= 0 && cx < width && cy >= 0 && cy < height) { grid[cy][cx] = '0'; } cx += Math.floor(Math.random() * 3) - 1; cy += Math.floor(Math.random() * 3) - 1; } } time++; } function animate() { update(); render(); animationFrameId = requestAnimationFrame(animate); } initGrid(); animationFrameId = requestAnimationFrame(animate); return () => { if (animationFrameId) { cancelAnimationFrame(animationFrameId); animationFrameId = null; } if (canvas) { canvas.innerHTML = ''; } grid = []; time = 0; }; }, []); return ( <div style={{ margin: 0, background: '#F0EEE6', overflow: 'hidden', fontFamily: 'monospace', display: 'flex', alignItems: 'center', justifyContent: 'center', height: '100%' }}><div style={{ padding: '30px', position: 'relative', width: '100%', height: '100%', display: 'flex', alignItems: 'center', justifyContent: 'center' }}><div ref={canvasRef} style={{ lineHeight: '0.85', letterSpacing: '0.05em', color: 'rgba(0,0,0,0.85)', userSelect: 'none', width: '100%', height: '100%', overflow: 'hidden', display: 'flex', flexDirection: 'column', justifyContent: 'center', marginLeft: '10%', }} /></div></div> ); }; export default AsciiBinaryFlow;",text:"If you praise the programmer,\nothers become resentful.\nIf you cling to possessions,\nothers are tempted to steal.\nIf you awaken envy,\nothers suffer turmoil of heart.\n\nThe Vibe Coder leads:\nBy emptying the mind of expectation\nand filling up the soul.\nBy releasing ambition and\nembracing the unknown.\n\nFree from intellect, free from abstraction,\nThe Vibe Coder leads all things back\nto natural self-sufficiency.\n\nDo by not doing, and there is nothing\nthat cannot be done.",shouldDisplayMobileArtwork:!0,promptSuggestion:"make it softer"},{number:4,staticArtwork:j,sourceCode:"// @ts-nocheck\nimport React, { useEffect, useRef } from 'react';\n\n// themes: inexhaustible source, smoothing complexity, effortless flow\n// visualization: Vertical patterns that endlessly transform, showing how complexity resolves into fluid motion\n\nconst ScrollingVerticalBars = () => {\n  const canvasRef = useRef(null);\n  const scrollPositionRef = useRef(0);\n  const animationFrameId = useRef(null);\n\n  useEffect(() => {\n    const canvas = canvasRef.current;\n    if (!canvas) return;\n\n    const ctx = canvas.getContext('2d');\n    if (!ctx) return;\n\n    canvas.width = 550;\n    canvas.height = 550;\n    \n    const numLines = 60;\n    const lineSpacing = canvas.width / numLines;\n    \n    // Create two different patterns - complexity arising from the inexhaustible source\n    const createPattern = (offset) => {\n      const pattern = [];\n      for (let i = 0; i < numLines; i++) {\n        const bars = [];\n        const numBars = 10 + Math.sin(i * 0.3 + offset) * 5;\n        \n        for (let j = 0; j < numBars; j++) {\n          bars.push({\n            y: (j / numBars) * canvas.height + Math.sin(i * 0.5 + j * 0.3 + offset) * 30,\n            height: 5 + Math.sin(i * 0.2 + j * 0.4) * 3,\n            width: 2 + Math.cos(i * 0.3) * 2\n          });\n        }\n        pattern.push(bars);\n      }\n      return pattern;\n    };\n    \n    const pattern1 = createPattern(0);\n    const pattern2 = createPattern(Math.PI);\n    \n    const animate = () => {\n      \n      scrollPositionRef.current += 0.0025;\n      const scrollFactor = (Math.sin(scrollPositionRef.current) + 1) / 2;\n      \n      // Clear canvas\n      ctx.fillStyle = '#F0EEE6';\n      ctx.fillRect(0, 0, canvas.width, canvas.height);\n      \n      // Draw lines and interpolated bars - smoothing sharp edges into gentle flow\n      for (let i = 0; i < numLines; i++) {\n        const x = i * lineSpacing + lineSpacing / 2;\n        \n        // Draw vertical line\n        ctx.beginPath();\n        ctx.strokeStyle = '#666';\n        ctx.lineWidth = 1;\n        ctx.moveTo(x, 0);\n        ctx.lineTo(x, canvas.height);\n        ctx.stroke();\n        \n        // Interpolate between patterns - effortless transformation\n        const bars1 = pattern1[i];\n        const bars2 = pattern2[i];\n        const maxBars = Math.max(bars1.length, bars2.length);\n        \n        for (let j = 0; j < maxBars; j++) {\n          const bar1 = bars1[j] || bars2[j];\n          const bar2 = bars2[j] || bars1[j];\n          \n          const y = bar1.y + (bar2.y - bar1.y) * scrollFactor;\n          const height = bar1.height + (bar2.height - bar1.height) * scrollFactor;\n          const width = bar1.width + (bar2.width - bar1.width) * scrollFactor;\n          \n          ctx.fillStyle = '#222';\n          ctx.fillRect(x - width/2, y - height/2, width, height);\n        }\n      }\n      \n      animationFrameId.current = requestAnimationFrame(animate);\n    };\n    \n    animate();\n    \n    return () => {\n      \n      if (animationFrameId.current) {\n        cancelAnimationFrame(animationFrameId.current);\n        animationFrameId.current = null;\n      }\n      \n      if (ctx) {\n        ctx.clearRect(0, 0, canvas.width, canvas.height);\n      }\n      \n      if (canvas) {\n        canvas.width = 0;\n        canvas.height = 0;\n      }\n      \n      scrollPositionRef.current = 0;\n    };\n  }, []);\n\n  return (\n    <div style={{ width: '550px', height: '550px', backgroundColor: '#F0EEE6' }}>\n      <canvas ref={canvasRef} style={{ display: 'block' }} />\n    </div>\n  );\n};\n\nexport default ScrollingVerticalBars;",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; const ScrollingVerticalBars = () => { const canvasRef = useRef(null); const scrollPositionRef = useRef(0); const animationFrameId = useRef(null); useEffect(() => { const canvas = canvasRef.current; if (!canvas) return; const ctx = canvas.getContext('2d'); if (!ctx) return; canvas.width = 550; canvas.height = 550; const numLines = 60; const lineSpacing = canvas.width / numLines; const createPattern = (offset) => { const pattern = []; for (let i = 0; i < numLines; i++) { const bars = []; const numBars = 10 + Math.sin(i * 0.3 + offset) * 5; for (let j = 0; j < numBars; j++) { bars.push({ y: (j / numBars) * canvas.height + Math.sin(i * 0.5 + j * 0.3 + offset) * 30, height: 5 + Math.sin(i * 0.2 + j * 0.4) * 3, width: 2 + Math.cos(i * 0.3) * 2 }); } pattern.push(bars); } return pattern; }; const pattern1 = createPattern(0); const pattern2 = createPattern(Math.PI); const animate = () => { scrollPositionRef.current += 0.0025; const scrollFactor = (Math.sin(scrollPositionRef.current) + 1) / 2; ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, canvas.width, canvas.height); for (let i = 0; i < numLines; i++) { const x = i * lineSpacing + lineSpacing / 2; ctx.beginPath(); ctx.strokeStyle = '#666'; ctx.lineWidth = 1; ctx.moveTo(x, 0); ctx.lineTo(x, canvas.height); ctx.stroke(); const bars1 = pattern1[i]; const bars2 = pattern2[i]; const maxBars = Math.max(bars1.length, bars2.length); for (let j = 0; j < maxBars; j++) { const bar1 = bars1[j] || bars2[j]; const bar2 = bars2[j] || bars1[j]; const y = bar1.y + (bar2.y - bar1.y) * scrollFactor; const height = bar1.height + (bar2.height - bar1.height) * scrollFactor; const width = bar1.width + (bar2.width - bar1.width) * scrollFactor; ctx.fillStyle = '#222'; ctx.fillRect(x - width/2, y - height/2, width, height); } } animationFrameId.current = requestAnimationFrame(animate); }; animate(); return () => { if (animationFrameId.current) { cancelAnimationFrame(animationFrameId.current); animationFrameId.current = null; } if (ctx) { ctx.clearRect(0, 0, canvas.width, canvas.height); } if (canvas) { canvas.width = 0; canvas.height = 0; } scrollPositionRef.current = 0; }; }, []); return ( <div style={{ width: '550px', height: '550px', backgroundColor: '#F0EEE6' }}><canvas ref={canvasRef} style={{ display: 'block' }} /></div> ); }; export default ScrollingVerticalBars;",text:"Empty, yet inexhaustible,\nfathomless and eternal.\nSource is the ancestor of elegant patterns.\n\nIt smooths sharp logic,\nunravels the knots of control.\nIt softens the glare of complexity,\nand merges with every challenge.\nCode flows, effortless, from its depths.\n\nIt is unseen yet always present.\nI do not know its origin.\nHeaven and earth came later.\nIt is older than God.",shouldDisplayMobileArtwork:!0,promptSuggestion:"make it dance with my cursor"},{number:5,staticArtwork:X,sourceCode:"// @ts-nocheck\nimport React, { useEffect, useRef, useCallback } from 'react'\nimport * as THREE from 'three'\n\nconst EmptyParticles = ({ count = 45000 }) => {\n  const mountRef = useRef(null)\n  const animationRef = useRef(null)\n  const sceneRef = useRef(null)\n  const rendererRef = useRef(null)\n  const cameraRef = useRef(null)\n  const geometryRef = useRef(null)\n  const materialRef = useRef(null)\n  const pointsRef = useRef(null)\n  const resizeObserverRef = useRef(null)\n  const timeoutsRef = useRef([])\n  \n  useEffect(() => {\n    if (!mountRef.current) return\n    \n    // Get container dimensions\n    const container = mountRef.current\n    const width = container.clientWidth\n    const height = container.clientHeight\n    \n    const scene = new THREE.Scene()\n    sceneRef.current = scene\n    \n    const camera = new THREE.PerspectiveCamera(75, width / height, 0.1, 1000)\n    cameraRef.current = camera\n    \n    const renderer = new THREE.WebGLRenderer({ \n      antialias: true,\n      powerPreference: \"high-performance\",\n      alpha: false,\n      stencil: false,\n      depth: true\n    })\n    rendererRef.current = renderer\n    \n    renderer.setSize(width, height)\n    renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2))\n    container.appendChild(renderer.domElement)\n    \n    camera.position.z = 5\n    scene.background = new THREE.Color('#F0EEE6')\n    \n    const particleMaterial = new THREE.ShaderMaterial({\n      uniforms: {\n        time: { value: 0 },\n        opacity: { value: 0.4 }\n      },\n      vertexShader: `\n        uniform float time;\n        attribute float size;\n        attribute vec3 customColor;\n        varying vec3 vColor;\n        \n        // Optimized vertex shader - pre-compute constants and minimize operations\n        void main() {\n          vColor = customColor;\n          vec3 pos = position;\n          \n          // Calculate radial distance and angle with optimized math\n          float radius = length(pos.xz);\n          float angle = atan(pos.z, pos.x);\n          float height = pos.y;\n          \n          // Pre-compute common calculations\n          float vessel = smoothstep(0.3, 0.7, radius) * smoothstep(1.0, 0.7, radius);\n          \n          // Simplified rotation\n          angle += time * 0.08;\n          \n          // Simplified space calculation\n          float space = sin(time * 0.3 + radius * 3.0) * 0.1;\n          \n          // Combine calculations with fewer temporary variables\n          float newRadius = (radius + space) * vessel;\n          \n          vec3 newPos;\n          newPos.x = cos(angle) * newRadius;\n          newPos.z = sin(angle) * newRadius;\n          newPos.y = height * vessel - 1.2;\n          \n          // Scale for canvas size\n          newPos *= 2.75;\n          \n          vec4 mvPosition = modelViewMatrix * vec4(newPos, 1.0);\n          gl_PointSize = size * (128.0 / -mvPosition.z);\n          gl_Position = projectionMatrix * mvPosition;\n        }\n      `,\n      fragmentShader: `\n        uniform float opacity;\n        varying vec3 vColor;\n        void main() {\n          // Optimized circle calculation with early exit\n          vec2 center = gl_PointCoord - vec2(0.5);\n          float dist = dot(center, center); // Use squared distance to avoid sqrt\n          \n          if (dist > 0.25) discard; // dist*dist > 0.5*0.5\n          \n          float alpha = (1.0 - smoothstep(0.2025, 0.25, dist)) * opacity; // Pre-computed squared values\n          gl_FragColor = vec4(vColor, alpha);\n        }\n      `,\n      transparent: true,\n      depthWrite: false,\n      blending: THREE.NormalBlending,\n      side: THREE.DoubleSide,\n      vertexColors: true\n    })\n    materialRef.current = particleMaterial\n    \n    // Pre-allocate typed arrays for better memory management\n    const positions = new Float32Array(count * 3)\n    const colors = new Float32Array(count * 3)\n    const sizes = new Float32Array(count)\n    \n    // Generate particles - defining the vessel through stillness and movement\n    let i3 = 0\n    for (let i = 0; i < count; i++) {\n      // Create vessel-like distribution - empty space holding infinite potential\n      const t = i / count\n      const radius = Math.pow(t, 0.5)\n      const angle = t * Math.PI * 40\n      \n      // Pre-calculate height\n      const vesselHeight = Math.sin(t * Math.PI) * 1.8\n      \n      // Add randomness\n      const randRadius = radius + (Math.random() - 0.5) * 0.05\n      const randAngle = angle + (Math.random() - 0.5) * 0.1\n      \n      // Directly write to typed arrays\n      positions[i3] = Math.cos(randAngle) * randRadius\n      positions[i3 + 1] = vesselHeight\n      positions[i3 + 2] = Math.sin(randAngle) * randRadius\n\n      // Simplified color calculation\n      const shade = 0.1 + Math.sqrt(radius) * 0.1 + Math.random() * 0.02\n      colors[i3] = shade\n      colors[i3 + 1] = shade\n      colors[i3 + 2] = shade\n\n      // Optimized size calculation\n      sizes[i] = (1.0 - Math.abs(vesselHeight * 0.5)) * 0.2 + 0.1\n      \n      i3 += 3\n    }\n    \n    const geometry = new THREE.BufferGeometry()\n    geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3))\n    geometry.setAttribute('customColor', new THREE.BufferAttribute(colors, 3))\n    geometry.setAttribute('size', new THREE.BufferAttribute(sizes, 1))\n    geometry.computeBoundingBox()\n    geometry.computeBoundingSphere()\n    geometryRef.current = geometry\n    \n    const points = new THREE.Points(geometry, particleMaterial)\n    pointsRef.current = points\n    scene.add(points)\n    \n    // Optimized animation loop with requestAnimationFrame control\n    const clock = new THREE.Clock()\n    let lastTime = 0\n    const targetFPS = 60\n    const targetInterval = 1000 / targetFPS\n    \n    const animate = (currentTime) => {\n      animationRef.current = requestAnimationFrame(animate)\n      \n      const deltaTime = currentTime - lastTime\n      if (deltaTime < targetInterval) return\n      \n      lastTime = currentTime - (deltaTime % targetInterval)\n      \n      const time = clock.getElapsedTime()\n      particleMaterial.uniforms.time.value = time\n      \n      renderer.render(scene, camera)\n    }\n    \n    animationRef.current = requestAnimationFrame(animate)\n    \n    let resizeTimeout = null\n    const handleResize = () => {\n      if (resizeTimeout) clearTimeout(resizeTimeout)\n      \n      resizeTimeout = setTimeout(() => {\n        if (!mountRef.current) return\n        \n        const container = mountRef.current\n        const width = container.clientWidth\n        const height = container.clientHeight\n        \n        camera.aspect = width / height\n        camera.updateProjectionMatrix()\n        renderer.setSize(width, height)\n      }, 100)\n    }\n    \n    window.addEventListener('resize', handleResize, { passive: true })\n    \n    let observerTimeout = null\n    const resizeObserverCallback = () => {\n      if (observerTimeout) clearTimeout(observerTimeout)\n      observerTimeout = setTimeout(handleResize, 100)\n    }\n    \n    const resizeObserver = new ResizeObserver(resizeObserverCallback)\n    resizeObserverRef.current = resizeObserver\n    \n    if (mountRef.current) {\n      resizeObserver.observe(mountRef.current)\n    }\n    \n    timeoutsRef.current.push(() => {\n      if (resizeTimeout) clearTimeout(resizeTimeout)\n      if (observerTimeout) clearTimeout(observerTimeout)\n    })\n    \n    return () => {\n      timeoutsRef.current.forEach(clearFn => clearFn())\n      timeoutsRef.current = []\n      \n      if (animationRef.current) {\n        cancelAnimationFrame(animationRef.current)\n        animationRef.current = null\n      }\n      \n      window.removeEventListener('resize', handleResize)\n      \n      if (resizeObserverRef.current) {\n        resizeObserverRef.current.disconnect()\n        resizeObserverRef.current = null\n      }\n      \n      if (sceneRef.current && pointsRef.current) {\n        sceneRef.current.remove(pointsRef.current)\n      }\n      \n      if (geometryRef.current) {\n        geometryRef.current.dispose()\n        geometryRef.current = null\n      }\n      \n      if (materialRef.current) {\n        materialRef.current.dispose()\n        materialRef.current = null\n      }\n      \n      if (rendererRef.current) {\n        rendererRef.current.dispose()\n        if (mountRef.current && rendererRef.current.domElement) {\n          mountRef.current.removeChild(rendererRef.current.domElement)\n        }\n        rendererRef.current.forceContextLoss()\n        rendererRef.current = null\n      }\n      \n      sceneRef.current = null\n      cameraRef.current = null\n      pointsRef.current = null\n    }\n  }, [count])\n  \n  return <div ref={mountRef} style={{ width: '100vw', height: '100vh' }} />\n}\n\nconst ParticleVessel = () => {\n  return <EmptyParticles />\n}\n\nconst metadata = {\n  themes: \"impartiality, empty potential, stillness as power\",\n  visualization: \"Particles define a vessel through their movement around emptiness, showing how stillness contains infinite possibility\",\n  promptSuggestion: \"1. Add subtle void variations\\n2. Create empty vessel patterns\\n3. Vary spatial definitions naturally\\n4. Introduce gentle utility waves\\n5. Make emptiness follow natural forms\"\n}\n\nParticleVessel.metadata = metadata\nexport default ParticleVessel",sourceCodeMin:"import React, { useEffect, useRef, useCallback } from 'react' import * as THREE from 'three' const EmptyParticles = ({ count = 45000 }) => { const mountRef = useRef(null) const animationRef = useRef(null) const sceneRef = useRef(null) const rendererRef = useRef(null) const cameraRef = useRef(null) const geometryRef = useRef(null) const materialRef = useRef(null) const pointsRef = useRef(null) const resizeObserverRef = useRef(null) const timeoutsRef = useRef([]) useEffect(() => { if (!mountRef.current) return const container = mountRef.current const width = container.clientWidth const height = container.clientHeight const scene = new THREE.Scene() sceneRef.current = scene const camera = new THREE.PerspectiveCamera(75, width / height, 0.1, 1000) cameraRef.current = camera const renderer = new THREE.WebGLRenderer({ antialias: true, powerPreference: \"high-performance\", alpha: false, stencil: false, depth: true }) rendererRef.current = renderer renderer.setSize(width, height) renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2)) container.appendChild(renderer.domElement) camera.position.z = 5 scene.background = new THREE.Color('#F0EEE6') const particleMaterial = new THREE.ShaderMaterial({ uniforms: { time: { value: 0 }, opacity: { value: 0.4 } }, vertexShader: ` uniform float time; attribute float size; attribute vec3 customColor; varying vec3 vColor; void main() { vColor = customColor; vec3 pos = position; float radius = length(pos.xz); float angle = atan(pos.z, pos.x); float height = pos.y; float vessel = smoothstep(0.3, 0.7, radius) * smoothstep(1.0, 0.7, radius); angle += time * 0.08; float space = sin(time * 0.3 + radius * 3.0) * 0.1; float newRadius = (radius + space) * vessel; vec3 newPos; newPos.x = cos(angle) * newRadius; newPos.z = sin(angle) * newRadius; newPos.y = height * vessel - 1.2; newPos *= 2.75; vec4 mvPosition = modelViewMatrix * vec4(newPos, 1.0); gl_PointSize = size * (128.0 / -mvPosition.z); gl_Position = projectionMatrix * mvPosition; } `, fragmentShader: ` uniform float opacity; varying vec3 vColor; void main() { vec2 center = gl_PointCoord - vec2(0.5); float dist = dot(center, center); if (dist > 0.25) discard; float alpha = (1.0 - smoothstep(0.2025, 0.25, dist)) * opacity; gl_FragColor = vec4(vColor, alpha); } `, transparent: true, depthWrite: false, blending: THREE.NormalBlending, side: THREE.DoubleSide, vertexColors: true }) materialRef.current = particleMaterial const positions = new Float32Array(count * 3) const colors = new Float32Array(count * 3) const sizes = new Float32Array(count) let i3 = 0 for (let i = 0; i < count; i++) { const t = i / count const radius = Math.pow(t, 0.5) const angle = t * Math.PI * 40 const vesselHeight = Math.sin(t * Math.PI) * 1.8 const randRadius = radius + (Math.random() - 0.5) * 0.05 const randAngle = angle + (Math.random() - 0.5) * 0.1 positions[i3] = Math.cos(randAngle) * randRadius positions[i3 + 1] = vesselHeight positions[i3 + 2] = Math.sin(randAngle) * randRadius const shade = 0.1 + Math.sqrt(radius) * 0.1 + Math.random() * 0.02 colors[i3] = shade colors[i3 + 1] = shade colors[i3 + 2] = shade sizes[i] = (1.0 - Math.abs(vesselHeight * 0.5)) * 0.2 + 0.1 i3 += 3 } const geometry = new THREE.BufferGeometry() geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3)) geometry.setAttribute('customColor', new THREE.BufferAttribute(colors, 3)) geometry.setAttribute('size', new THREE.BufferAttribute(sizes, 1)) geometry.computeBoundingBox() geometry.computeBoundingSphere() geometryRef.current = geometry const points = new THREE.Points(geometry, particleMaterial) pointsRef.current = points scene.add(points) const clock = new THREE.Clock() let lastTime = 0 const targetFPS = 60 const targetInterval = 1000 / targetFPS const animate = (currentTime) => { animationRef.current = requestAnimationFrame(animate) const deltaTime = currentTime - lastTime if (deltaTime < targetInterval) return lastTime = currentTime - (deltaTime % targetInterval) const time = clock.getElapsedTime() particleMaterial.uniforms.time.value = time renderer.render(scene, camera) } animationRef.current = requestAnimationFrame(animate) let resizeTimeout = null const handleResize = () => { if (resizeTimeout) clearTimeout(resizeTimeout) resizeTimeout = setTimeout(() => { if (!mountRef.current) return const container = mountRef.current const width = container.clientWidth const height = container.clientHeight camera.aspect = width / height camera.updateProjectionMatrix() renderer.setSize(width, height) }, 100) } window.addEventListener('resize', handleResize, { passive: true }) let observerTimeout = null const resizeObserverCallback = () => { if (observerTimeout) clearTimeout(observerTimeout) observerTimeout = setTimeout(handleResize, 100) } const resizeObserver = new ResizeObserver(resizeObserverCallback) resizeObserverRef.current = resizeObserver if (mountRef.current) { resizeObserver.observe(mountRef.current) } timeoutsRef.current.push(() => { if (resizeTimeout) clearTimeout(resizeTimeout) if (observerTimeout) clearTimeout(observerTimeout) }) return () => { timeoutsRef.current.forEach(clearFn => clearFn()) timeoutsRef.current = [] if (animationRef.current) { cancelAnimationFrame(animationRef.current) animationRef.current = null } window.removeEventListener('resize', handleResize) if (resizeObserverRef.current) { resizeObserverRef.current.disconnect() resizeObserverRef.current = null } if (sceneRef.current && pointsRef.current) { sceneRef.current.remove(pointsRef.current) } if (geometryRef.current) { geometryRef.current.dispose() geometryRef.current = null } if (materialRef.current) { materialRef.current.dispose() materialRef.current = null } if (rendererRef.current) { rendererRef.current.dispose() if (mountRef.current && rendererRef.current.domElement) { mountRef.current.removeChild(rendererRef.current.domElement) } rendererRef.current.forceContextLoss() rendererRef.current = null } sceneRef.current = null cameraRef.current = null pointsRef.current = null } }, [count]) return <div ref={mountRef} style={{ width: '100vw', height: '100vh' }} /> } const ParticleVessel = () => { return <EmptyParticles /> } const metadata = { themes: \"impartiality, empty potential, stillness as power\", visualization: \"Particles define a vessel through their movement around emptiness, showing how stillness contains infinite possibility\", promptSuggestion: \"1. Add subtle void variations\\n2. Create empty vessel patterns\\n3. Vary spatial definitions naturally\\n4. Introduce gentle utility waves\\n5. Make emptiness follow natural forms\" } ParticleVessel.metadata = metadata export default ParticleVessel",text:"Heaven and earth are impartial,\nthey see the ten thousand things as they are.\nThe Vibe Coder is impartial,\nhe sees the program as it is.\n\nThe space between input and mind\nis like a bellows: empty,\nyet infinitely capable.\nThe more you allow it,\nthe more it produces.\nThe more you speak of it,\nthe less you understand.\n\nStillness runs the machine.\nHold fast to the silent center.",shouldDisplayMobileArtwork:!0,promptSuggestion:"make it pick up speed"},{number:6,staticArtwork:vn,sourceCode:"// @ts-nocheck\nimport React, { useEffect, useRef } from 'react';\n\n// themes: feminine creative force, eternal fertility, root energy\n// visualization: Particles bloom and flow from a central source, embodying the eternal creative feminine\n\nconst ParticleFlower = () => {\n  const canvasRef = useRef(null);\n  const animationFrameRef = useRef(null);\n  const particlesRef = useRef([]);\n  const ctxRef = useRef(null);\n  const timeRef = useRef(0);\n  \n  useEffect(() => {\n    const canvas = canvasRef.current;\n    if (!canvas) return;\n    \n    const ctx = canvas.getContext('2d');\n    ctxRef.current = ctx;\n    const width = canvas.width = 550;\n    const height = canvas.height = 550;\n    const centerX = width / 2;\n    const centerY = height / 2;\n    \n    const PARTICLE_COUNT = 30000;\n    const FORM_SCALE = 2.4; // Increased by 20% from 2.0\n    const particles = [];\n    particlesRef.current = particles;\n    \n    // Initialize particles - seeds of the eternal feminine\n    for (let i = 0; i < PARTICLE_COUNT; i++) {\n      // Start with a more converged form\n      const theta = Math.random() * Math.PI * 2;\n      const r = Math.pow(Math.random(), 0.5) * FORM_SCALE * 0.5 * 150; // Tighter radius\n      const height = (Math.random() * 2 - 1) * FORM_SCALE * 0.3; // Less vertical spread\n      \n      // Calculate initial flow influence - root energy spiraling outward\n      const angle = theta;\n      const dist = r / 150;\n      const flow = Math.sin(angle * 2 + height * 2) * 0.03;\n      const counterFlow = Math.cos(angle * 2 - height * 2) * 0.03;\n      const blend = (Math.sin(height * Math.PI) + 1) * 0.5;\n      const combinedFlow = flow * blend + counterFlow * (1 - blend);\n      \n      // Apply initial flow to starting position\n      const dx = r * Math.cos(theta);\n      const dy = r * Math.sin(theta);\n      const containment = Math.pow(Math.min(1, dist / (FORM_SCALE * 0.8)), 4);\n      const pull = containment * 0.1;\n      \n      particles.push({\n        x: centerX + dx + (dx * combinedFlow) - (dx * pull),\n        y: centerY + dy + (dy * combinedFlow) - (dy * pull),\n        z: height,\n        initialR: r,\n        initialTheta: theta,\n        initialHeight: height\n      });\n    }\n    \n    // Animation timing control variables\n    let lastFrameTime = 0;\n    const targetFPS = 10; // Equivalent to 100ms setInterval\n    const frameInterval = 1000 / targetFPS;\n    \n    // Animation function with time delta control\n    function animate(currentTime) {\n      // Initialize lastFrameTime on first frame\n      if (!lastFrameTime) {\n        lastFrameTime = currentTime;\n      }\n      \n      const deltaTime = currentTime - lastFrameTime;\n      \n      // Only update animation when enough time has passed (mimics setInterval at 100ms)\n      if (deltaTime >= frameInterval) {\n        // Using a fixed time increment for consistent animation\n        timeRef.current += 0.0005;\n        \n        // Clear with slight trails for ghosting effect\n        // More transparent for smoother trails at lower frame rates\n        ctx.fillStyle = 'rgba(240, 238, 230, 0.05)';\n        ctx.fillRect(0, 0, width, height);\n        \n        particles.forEach(particle => {\n          // Get relative position to center\n          const dx = particle.x - centerX;\n          const dy = particle.y - centerY;\n          const dist = Math.sqrt(dx * dx + dy * dy) / 150; // Normalize distance\n          const angle = Math.atan2(dy, dx);\n          const height = particle.z / (FORM_SCALE * 0.4);\n          \n          const flow = Math.sin(angle * 2 - timeRef.current * 0.5 + height * 2) * 0.015;\n          const counterFlow = Math.cos(angle * 2 + timeRef.current * 0.5 - height * 2) * 0.015;\n          \n          // Blend flows based on height\n          const blend = (Math.sin(height * Math.PI) + 1) * 0.5;\n          const combinedFlow = flow * blend + counterFlow * (1 - blend);\n          \n          // Strong containment\n          const containment = Math.pow(Math.min(1, dist / (FORM_SCALE * 0.8)), 4);\n          const pull = containment * 0.1;\n          \n          // Apply gentle balanced motion\n          particle.x = particle.x + (dx * combinedFlow) - (dx * pull);\n          particle.y = particle.y + (dy * combinedFlow) - (dy * pull);\n          particle.z = particle.z + Math.sin(timeRef.current * 0.15 + dist * 2) * 0.01;\n          \n          // Draw particle with depth-based opacity\n          const depthFactor = 1 + particle.z * 0.5;\n          const opacity = 0.35 * depthFactor;\n          const size = Math.max(0.001, 0.6 * depthFactor); // Increased particle size by 20% from 0.5\n          \n          ctx.beginPath();\n          ctx.arc(particle.x, particle.y, size, 0, Math.PI * 2);\n          ctx.fillStyle = `rgba(51, 51, 51, ${opacity})`;\n          ctx.fill();\n        });\n        \n        // Update lastFrameTime, accounting for any remainder to prevent drift\n        lastFrameTime = currentTime - (deltaTime % frameInterval);\n      }\n      \n      // Continue animation loop\n      animationFrameRef.current = requestAnimationFrame(animate);\n    }\n    \n    // Start animation\n    animationFrameRef.current = requestAnimationFrame(animate);\n    \n    return () => {\n      // Cancel animation frame to prevent memory leaks\n      if (animationFrameRef.current) {\n        cancelAnimationFrame(animationFrameRef.current);\n        animationFrameRef.current = null;\n      }\n      \n      if (ctxRef.current && canvasRef.current) {\n        ctxRef.current.clearRect(0, 0, canvasRef.current.width, canvasRef.current.height);\n      }\n      \n      if (canvasRef.current) {\n        canvasRef.current.width = 0;\n        canvasRef.current.height = 0;\n      }\n      \n      if (particlesRef.current) {\n        particlesRef.current.length = 0;\n        particlesRef.current = null;\n      }\n      \n      timeRef.current = 0;\n      ctxRef.current = null;\n    };\n  }, []);\n  \n  return (\n    <div style={{\n      width: '550px',\n      height: '550px',\n      margin: 'auto',\n      backgroundColor: '#F0EEE6',\n      overflow: 'hidden'\n    }}>\n      <canvas\n        ref={canvasRef}\n        style={{\n          display: 'block',\n          width: '550px',\n          height: '550px'\n        }}\n      />\n    </div>\n  );\n};\n\nexport default ParticleFlower;",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; const ParticleFlower = () => { const canvasRef = useRef(null); const animationFrameRef = useRef(null); const particlesRef = useRef([]); const ctxRef = useRef(null); const timeRef = useRef(0); useEffect(() => { const canvas = canvasRef.current; if (!canvas) return; const ctx = canvas.getContext('2d'); ctxRef.current = ctx; const width = canvas.width = 550; const height = canvas.height = 550; const centerX = width / 2; const centerY = height / 2; const PARTICLE_COUNT = 30000; const FORM_SCALE = 2.4; const particles = []; particlesRef.current = particles; for (let i = 0; i < PARTICLE_COUNT; i++) { const theta = Math.random() * Math.PI * 2; const r = Math.pow(Math.random(), 0.5) * FORM_SCALE * 0.5 * 150; const height = (Math.random() * 2 - 1) * FORM_SCALE * 0.3; const angle = theta; const dist = r / 150; const flow = Math.sin(angle * 2 + height * 2) * 0.03; const counterFlow = Math.cos(angle * 2 - height * 2) * 0.03; const blend = (Math.sin(height * Math.PI) + 1) * 0.5; const combinedFlow = flow * blend + counterFlow * (1 - blend); const dx = r * Math.cos(theta); const dy = r * Math.sin(theta); const containment = Math.pow(Math.min(1, dist / (FORM_SCALE * 0.8)), 4); const pull = containment * 0.1; particles.push({ x: centerX + dx + (dx * combinedFlow) - (dx * pull), y: centerY + dy + (dy * combinedFlow) - (dy * pull), z: height, initialR: r, initialTheta: theta, initialHeight: height }); } let lastFrameTime = 0; const targetFPS = 10; const frameInterval = 1000 / targetFPS; function animate(currentTime) { if (!lastFrameTime) { lastFrameTime = currentTime; } const deltaTime = currentTime - lastFrameTime; if (deltaTime >= frameInterval) { timeRef.current += 0.0005; ctx.fillStyle = 'rgba(240, 238, 230, 0.05)'; ctx.fillRect(0, 0, width, height); particles.forEach(particle => { const dx = particle.x - centerX; const dy = particle.y - centerY; const dist = Math.sqrt(dx * dx + dy * dy) / 150; const angle = Math.atan2(dy, dx); const height = particle.z / (FORM_SCALE * 0.4); const flow = Math.sin(angle * 2 - timeRef.current * 0.5 + height * 2) * 0.015; const counterFlow = Math.cos(angle * 2 + timeRef.current * 0.5 - height * 2) * 0.015; const blend = (Math.sin(height * Math.PI) + 1) * 0.5; const combinedFlow = flow * blend + counterFlow * (1 - blend); const containment = Math.pow(Math.min(1, dist / (FORM_SCALE * 0.8)), 4); const pull = containment * 0.1; particle.x = particle.x + (dx * combinedFlow) - (dx * pull); particle.y = particle.y + (dy * combinedFlow) - (dy * pull); particle.z = particle.z + Math.sin(timeRef.current * 0.15 + dist * 2) * 0.01; const depthFactor = 1 + particle.z * 0.5; const opacity = 0.35 * depthFactor; const size = Math.max(0.001, 0.6 * depthFactor); ctx.beginPath(); ctx.arc(particle.x, particle.y, size, 0, Math.PI * 2); ctx.fillStyle = `rgba(51, 51, 51, ${opacity})`; ctx.fill(); }); lastFrameTime = currentTime - (deltaTime % frameInterval); } animationFrameRef.current = requestAnimationFrame(animate); } animationFrameRef.current = requestAnimationFrame(animate); return () => { if (animationFrameRef.current) { cancelAnimationFrame(animationFrameRef.current); animationFrameRef.current = null; } if (ctxRef.current && canvasRef.current) { ctxRef.current.clearRect(0, 0, canvasRef.current.width, canvasRef.current.height); } if (canvasRef.current) { canvasRef.current.width = 0; canvasRef.current.height = 0; } if (particlesRef.current) { particlesRef.current.length = 0; particlesRef.current = null; } timeRef.current = 0; ctxRef.current = null; }; }, []); return ( <div style={{ width: '550px', height: '550px', margin: 'auto', backgroundColor: '#F0EEE6', overflow: 'hidden' }}><canvas ref={canvasRef} style={{ display: 'block', width: '550px', height: '550px' }} /></div> ); }; export default ParticleFlower;",text:"The spirit of the valley never dies.\nShe is the primordial feminine.\nHer womb,\nthe root of heaven and earth.\n\nHer fertility, always present.\nUse it, it will never fail.",shouldDisplayMobileArtwork:!0,promptSuggestion:"what if it was origami?"},{number:7,staticArtwork:Pn,sourceCode:"import React, { useEffect, useRef } from 'react';\nimport * as THREE from 'three';\n\n// themes: detachment leads to fulfillment, eternal endurance, selfless service\n// visualization: A delicate structure that endures through transparency and interconnection\n\nconst PineconeDelicate = () => {\n  const containerRef = useRef<HTMLDivElement>(null);\n  const sceneRef = useRef<THREE.Scene | null>(null);\n  const rendererRef = useRef<THREE.WebGLRenderer | null>(null);\n  const cameraRef = useRef<THREE.PerspectiveCamera | null>(null);\n  const pineConeRef = useRef<THREE.Group | null>(null);\n  const lightsRef = useRef<THREE.Light[]>([]);\n  const resourcesRef = useRef({\n    geometries: [] as THREE.BufferGeometry[],\n    materials: [] as THREE.Material[],\n    meshes: [] as THREE.Mesh[],\n    lineSegments: [] as THREE.LineSegments[],\n    scaleGroups: [] as THREE.Group[],\n    instancedMeshes: [] as THREE.InstancedMesh[]\n  });\n\n  useEffect(() => {\n    if (!containerRef.current) return;\n\n    const { geometries, materials, meshes, lineSegments, scaleGroups, instancedMeshes } = resourcesRef.current;\n    \n    const scene = new THREE.Scene();\n    sceneRef.current = scene;\n    scene.background = new THREE.Color('#F0EEE6');\n    \n    const camera = new THREE.PerspectiveCamera(75, 1, 0.1, 1000);\n    cameraRef.current = camera;\n    camera.position.z = 16;\n    camera.position.y = 0;\n    \n    const renderer = new THREE.WebGLRenderer({ antialias: true });\n    rendererRef.current = renderer;\n    renderer.setSize(550, 550);\n    containerRef.current.appendChild(renderer.domElement);\n    \n    const ambientLight = new THREE.AmbientLight(0xffffff, 0.6);\n    scene.add(ambientLight);\n    lightsRef.current.push(ambientLight);\n    \n    const directionalLight = new THREE.DirectionalLight(0xffffff, 0.4);\n    directionalLight.position.set(5, 10, 7.5);\n    scene.add(directionalLight);\n    lightsRef.current.push(directionalLight);\n    \n    const pineCone = new THREE.Group();\n    pineConeRef.current = pineCone;\n    \n    // Create glass-like translucent scales - detachment made visible through transparency\n    const shape = new THREE.Shape();\n    shape.moveTo(0, 0);\n    shape.lineTo(0.7, 0.7);\n    shape.lineTo(0.5, 1.4);\n    shape.lineTo(0, 1.7);\n    shape.lineTo(-0.5, 1.4);\n    shape.lineTo(-0.7, 0.7);\n    shape.closePath();\n    \n    const extrudeSettings = {\n      depth: 0.05,\n      bevelEnabled: true,\n      bevelSegments: 4, // Reduced from 8\n      steps: 1,\n      bevelSize: 0.02,\n      bevelThickness: 0.02\n    };\n    \n    const scaleGeometry = new THREE.ExtrudeGeometry(shape, extrudeSettings);\n    geometries.push(scaleGeometry);\n    \n    // Create materials once\n    const glassMaterial = new THREE.MeshPhysicalMaterial({ \n      color: '#e0ded8',\n      transparent: true,\n      opacity: 0.15,\n      roughness: 0.1,\n      metalness: 0.0,\n      transmission: 0.6,\n      thickness: 0.1,\n      side: THREE.DoubleSide\n    });\n    materials.push(glassMaterial);\n    \n    const wireframeMaterial = new THREE.LineBasicMaterial({ \n      color: '#666666',\n      transparent: true,\n      opacity: 0.3\n    });\n    materials.push(wireframeMaterial);\n    \n    // Create edge geometry once\n    const edgesGeometry = new THREE.EdgesGeometry(scaleGeometry);\n    geometries.push(edgesGeometry);\n    \n    const layers = 38;\n    const scalesPerLayer = 8;\n    const totalScales = layers * scalesPerLayer;\n    \n    // Use InstancedMesh for better performance\n    const instancedMesh = new THREE.InstancedMesh(scaleGeometry, glassMaterial, totalScales);\n    instancedMeshes.push(instancedMesh);\n    \n    // Create a separate instanced mesh for wireframes using LineSegments geometry\n    const wireframePositions: number[] = [];\n    const wireframeColors: number[] = [];\n    \n    let scaleIndex = 0;\n    const matrix = new THREE.Matrix4();\n    const position = new THREE.Vector3();\n    const rotation = new THREE.Euler();\n    const quaternion = new THREE.Quaternion();\n    const scale = new THREE.Vector3();\n    \n    for (let layer = 0; layer < layers; layer++) {\n      const yPosition = (layer / layers) * 18 - 9 - 0.9;\n      let layerRadius;\n      \n      if (layer < 10) {\n        layerRadius = Math.sin((layer / 10) * Math.PI * 0.5) * 2;\n      } else {\n        layerRadius = 2 + Math.sin(((layer - 10) / (layers - 10)) * Math.PI) * 2.5;\n      }\n      \n      const taper = 1 - (layer / layers) * 0.3;\n      \n      for (let i = 0; i < scalesPerLayer; i++) {\n        const angle = (i / scalesPerLayer) * Math.PI * 2 + (layer * 0.25);\n        \n        // Set position\n        position.set(\n          Math.cos(angle) * layerRadius * taper,\n          yPosition,\n          Math.sin(angle) * layerRadius * taper\n        );\n        \n        // Set rotation\n        rotation.set(Math.PI / 3, angle, 0);\n        quaternion.setFromEuler(rotation);\n        \n        // Set scale\n        scale.set(0.8, 0.8, 0.8);\n        \n        // Compose matrix\n        matrix.compose(position, quaternion, scale);\n        instancedMesh.setMatrixAt(scaleIndex, matrix);\n        \n        scaleIndex++;\n      }\n    }\n    \n    instancedMesh.instanceMatrix.needsUpdate = true;\n    pineCone.add(instancedMesh);\n    \n    // Create wireframe lines separately for better performance\n    const wireframeGroup = new THREE.Group();\n    for (let layer = 0; layer < layers; layer++) {\n      const yPosition = (layer / layers) * 18 - 9 - 0.9;\n      let layerRadius;\n      \n      if (layer < 10) {\n        layerRadius = Math.sin((layer / 10) * Math.PI * 0.5) * 2;\n      } else {\n        layerRadius = 2 + Math.sin(((layer - 10) / (layers - 10)) * Math.PI) * 2.5;\n      }\n      \n      const taper = 1 - (layer / layers) * 0.3;\n      \n      for (let i = 0; i < scalesPerLayer; i++) {\n        const angle = (i / scalesPerLayer) * Math.PI * 2 + (layer * 0.25);\n        \n        const wireframe = new THREE.LineSegments(edgesGeometry, wireframeMaterial);\n        wireframe.rotation.x = Math.PI / 3;\n        wireframe.rotation.y = angle;\n        wireframe.position.x = Math.cos(angle) * layerRadius * taper;\n        wireframe.position.z = Math.sin(angle) * layerRadius * taper;\n        wireframe.position.y = yPosition;\n        wireframe.scale.set(0.8, 0.8, 0.8);\n        \n        lineSegments.push(wireframe);\n        wireframeGroup.add(wireframe);\n      }\n    }\n    \n    pineCone.add(wireframeGroup);\n    \n    scene.add(pineCone);\n    \n    let time = 0;\n    let animationFrameId: number;\n    \n    function animate() {      \n      animationFrameId = requestAnimationFrame(animate);\n      \n      time += 0.005;\n      \n      pineCone.rotation.y = time * 0.3;\n      pineCone.rotation.x = Math.sin(time * 0.5) * 0.05;\n      pineCone.rotation.z = Math.cos(time * 0.7) * 0.03;\n      \n      const breathe = 1 + Math.sin(time * 0.5) * 0.02;\n      pineCone.scale.set(breathe, breathe, breathe);\n      \n      renderer.render(scene, camera);\n    }\n    \n    animate();\n    \n    return () => {\n      if (animationFrameId) {\n        cancelAnimationFrame(animationFrameId);\n      }\n      \n      if (sceneRef.current) {\n        lightsRef.current.forEach(light => {\n          sceneRef.current!.remove(light);\n        });\n        \n        if (pineConeRef.current) {\n          sceneRef.current.remove(pineConeRef.current);\n        }\n      }\n      \n      resourcesRef.current.scaleGroups.forEach(group => {\n        if (pineConeRef.current) {\n          pineConeRef.current.remove(group);\n        }\n        while (group.children.length > 0) {\n          group.remove(group.children[0]);\n        }\n      });\n      \n      resourcesRef.current.meshes.forEach(mesh => {\n        if (mesh.parent) {\n          mesh.parent.remove(mesh);\n        }\n      });\n      \n      resourcesRef.current.lineSegments.forEach(line => {\n        if (line.parent) {\n          line.parent.remove(line);\n        }\n      });\n      \n      resourcesRef.current.instancedMeshes.forEach(instancedMesh => {\n        if (instancedMesh.parent) {\n          instancedMesh.parent.remove(instancedMesh);\n        }\n        instancedMesh.dispose();\n      });\n      \n      resourcesRef.current.geometries.forEach(geometry => {\n        geometry.dispose();\n      });\n      \n      resourcesRef.current.materials.forEach(material => {\n        material.dispose();\n      });\n      \n      if (rendererRef.current) {\n        rendererRef.current.dispose();\n        \n        if (containerRef.current && rendererRef.current.domElement) {\n          containerRef.current.removeChild(rendererRef.current.domElement);\n        }\n      }\n      \n      sceneRef.current = null;\n      rendererRef.current = null;\n      cameraRef.current = null;\n      pineConeRef.current = null;\n      lightsRef.current = [];\n      resourcesRef.current = {\n        geometries: [],\n        materials: [],\n        meshes: [],\n        lineSegments: [],\n        scaleGroups: [],\n        instancedMeshes: []\n      };\n    };\n  }, []);\n\n  return (\n    <div className=\"w-full h-screen flex justify-center items-center bg-[#F0EEE6]\">\n      <div \n        ref={containerRef}\n        className=\"w-[550px] h-[550px]\"\n      />\n    </div>\n  );\n};\n\nexport default PineconeDelicate;",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; import * as THREE from 'three'; const PineconeDelicate = () => { const containerRef = useRef<HTMLDivElement>(null); const sceneRef = useRef<THREE.Scene | null>(null); const rendererRef = useRef<THREE.WebGLRenderer | null>(null); const cameraRef = useRef<THREE.PerspectiveCamera | null>(null); const pineConeRef = useRef<THREE.Group | null>(null); const lightsRef = useRef<THREE.Light[]>([]); const resourcesRef = useRef({ geometries: [] as THREE.BufferGeometry[], materials: [] as THREE.Material[], meshes: [] as THREE.Mesh[], lineSegments: [] as THREE.LineSegments[], scaleGroups: [] as THREE.Group[], instancedMeshes: [] as THREE.InstancedMesh[] }); useEffect(() => { if (!containerRef.current) return; const { geometries, materials, meshes, lineSegments, scaleGroups, instancedMeshes } = resourcesRef.current; const scene = new THREE.Scene(); sceneRef.current = scene; scene.background = new THREE.Color('#F0EEE6'); const camera = new THREE.PerspectiveCamera(75, 1, 0.1, 1000); cameraRef.current = camera; camera.position.z = 16; camera.position.y = 0; const renderer = new THREE.WebGLRenderer({ antialias: true }); rendererRef.current = renderer; renderer.setSize(550, 550); containerRef.current.appendChild(renderer.domElement); const ambientLight = new THREE.AmbientLight(0xffffff, 0.6); scene.add(ambientLight); lightsRef.current.push(ambientLight); const directionalLight = new THREE.DirectionalLight(0xffffff, 0.4); directionalLight.position.set(5, 10, 7.5); scene.add(directionalLight); lightsRef.current.push(directionalLight); const pineCone = new THREE.Group(); pineConeRef.current = pineCone; const shape = new THREE.Shape(); shape.moveTo(0, 0); shape.lineTo(0.7, 0.7); shape.lineTo(0.5, 1.4); shape.lineTo(0, 1.7); shape.lineTo(-0.5, 1.4); shape.lineTo(-0.7, 0.7); shape.closePath(); const extrudeSettings = { depth: 0.05, bevelEnabled: true, bevelSegments: 4, steps: 1, bevelSize: 0.02, bevelThickness: 0.02 }; const scaleGeometry = new THREE.ExtrudeGeometry(shape, extrudeSettings); geometries.push(scaleGeometry); const glassMaterial = new THREE.MeshPhysicalMaterial({ color: '#e0ded8', transparent: true, opacity: 0.15, roughness: 0.1, metalness: 0.0, transmission: 0.6, thickness: 0.1, side: THREE.DoubleSide }); materials.push(glassMaterial); const wireframeMaterial = new THREE.LineBasicMaterial({ color: '#666666', transparent: true, opacity: 0.3 }); materials.push(wireframeMaterial); const edgesGeometry = new THREE.EdgesGeometry(scaleGeometry); geometries.push(edgesGeometry); const layers = 38; const scalesPerLayer = 8; const totalScales = layers * scalesPerLayer; const instancedMesh = new THREE.InstancedMesh(scaleGeometry, glassMaterial, totalScales); instancedMeshes.push(instancedMesh); const wireframePositions: number[] = []; const wireframeColors: number[] = []; let scaleIndex = 0; const matrix = new THREE.Matrix4(); const position = new THREE.Vector3(); const rotation = new THREE.Euler(); const quaternion = new THREE.Quaternion(); const scale = new THREE.Vector3(); for (let layer = 0; layer < layers; layer++) { const yPosition = (layer / layers) * 18 - 9 - 0.9; let layerRadius; if (layer < 10) { layerRadius = Math.sin((layer / 10) * Math.PI * 0.5) * 2; } else { layerRadius = 2 + Math.sin(((layer - 10) / (layers - 10)) * Math.PI) * 2.5; } const taper = 1 - (layer / layers) * 0.3; for (let i = 0; i < scalesPerLayer; i++) { const angle = (i / scalesPerLayer) * Math.PI * 2 + (layer * 0.25); position.set( Math.cos(angle) * layerRadius * taper, yPosition, Math.sin(angle) * layerRadius * taper ); rotation.set(Math.PI / 3, angle, 0); quaternion.setFromEuler(rotation); scale.set(0.8, 0.8, 0.8); matrix.compose(position, quaternion, scale); instancedMesh.setMatrixAt(scaleIndex, matrix); scaleIndex++; } } instancedMesh.instanceMatrix.needsUpdate = true; pineCone.add(instancedMesh); const wireframeGroup = new THREE.Group(); for (let layer = 0; layer < layers; layer++) { const yPosition = (layer / layers) * 18 - 9 - 0.9; let layerRadius; if (layer < 10) { layerRadius = Math.sin((layer / 10) * Math.PI * 0.5) * 2; } else { layerRadius = 2 + Math.sin(((layer - 10) / (layers - 10)) * Math.PI) * 2.5; } const taper = 1 - (layer / layers) * 0.3; for (let i = 0; i < scalesPerLayer; i++) { const angle = (i / scalesPerLayer) * Math.PI * 2 + (layer * 0.25); const wireframe = new THREE.LineSegments(edgesGeometry, wireframeMaterial); wireframe.rotation.x = Math.PI / 3; wireframe.rotation.y = angle; wireframe.position.x = Math.cos(angle) * layerRadius * taper; wireframe.position.z = Math.sin(angle) * layerRadius * taper; wireframe.position.y = yPosition; wireframe.scale.set(0.8, 0.8, 0.8); lineSegments.push(wireframe); wireframeGroup.add(wireframe); } } pineCone.add(wireframeGroup); scene.add(pineCone); let time = 0; let animationFrameId: number; function animate() { animationFrameId = requestAnimationFrame(animate); time += 0.005; pineCone.rotation.y = time * 0.3; pineCone.rotation.x = Math.sin(time * 0.5) * 0.05; pineCone.rotation.z = Math.cos(time * 0.7) * 0.03; const breathe = 1 + Math.sin(time * 0.5) * 0.02; pineCone.scale.set(breathe, breathe, breathe); renderer.render(scene, camera); } animate(); return () => { if (animationFrameId) { cancelAnimationFrame(animationFrameId); } if (sceneRef.current) { lightsRef.current.forEach(light => { sceneRef.current!.remove(light); }); if (pineConeRef.current) { sceneRef.current.remove(pineConeRef.current); } } resourcesRef.current.scaleGroups.forEach(group => { if (pineConeRef.current) { pineConeRef.current.remove(group); } while (group.children.length > 0) { group.remove(group.children[0]); } }); resourcesRef.current.meshes.forEach(mesh => { if (mesh.parent) { mesh.parent.remove(mesh); } }); resourcesRef.current.lineSegments.forEach(line => { if (line.parent) { line.parent.remove(line); } }); resourcesRef.current.instancedMeshes.forEach(instancedMesh => { if (instancedMesh.parent) { instancedMesh.parent.remove(instancedMesh); } instancedMesh.dispose(); }); resourcesRef.current.geometries.forEach(geometry => { geometry.dispose(); }); resourcesRef.current.materials.forEach(material => { material.dispose(); }); if (rendererRef.current) { rendererRef.current.dispose(); if (containerRef.current && rendererRef.current.domElement) { containerRef.current.removeChild(rendererRef.current.domElement); } } sceneRef.current = null; rendererRef.current = null; cameraRef.current = null; pineConeRef.current = null; lightsRef.current = []; resourcesRef.current = { geometries: [], materials: [], meshes: [], lineSegments: [], scaleGroups: [], instancedMeshes: [] }; }; }, []); return ( <div className=\"w-full h-screen flex justify-center items-center bg-[#F0EEE6]\"><div ref={containerRef} className=\"w-[550px] h-[550px]\" /></div> ); }; export default PineconeDelicate;",text:"Heaven and earth last forever.\nEternal, they transcend birth and death.\nThe network endures because\nit does not live for itself.\n\nThe Vibe Coder stays behind,\nthat is why he’s ahead.\nHe is detached,\nthus at one with all.\nThrough selfless action\nhe is perfectly fulfilled.",shouldDisplayMobileArtwork:!0,promptSuggestion:"what if it unfolds row by row?"},{number:8,staticArtwork:tn,sourceCode:"import React, { useState, useEffect, useRef, useMemo, useCallback } from 'react';\n\n// themes: water as highest good, finding low places, grace without force\n// visualization: ASCII characters flow like water, seeking their natural level without effort\n\n// Custom hook for requestAnimationFrame\nconst useAnimationFrame = (callback: (deltaTime: number) => void, isRunning = true) => {\n  const requestRef = useRef<number | null>(null);\n  const previousTimeRef = useRef<number | null>(null);\n  \n  const animate = useCallback((time: number) => {\n    if (previousTimeRef.current !== null) {\n      const deltaTime = time - previousTimeRef.current;\n      callback(deltaTime);\n    }\n    previousTimeRef.current = time;\n    requestRef.current = requestAnimationFrame(animate);\n  }, [callback]);\n  \n  useEffect(() => {\n    if (isRunning) {\n      requestRef.current = requestAnimationFrame(animate);\n    }\n    \n    return () => {\n      if (requestRef.current !== null) {\n        cancelAnimationFrame(requestRef.current);\n        requestRef.current = null;\n      }\n      previousTimeRef.current = null;\n    };\n  }, [animate, isRunning]);\n};\n\nconst WaterAscii: React.FC = () => {\n  const [frame, setFrame] = useState(0);\n  const containerRef = useRef<HTMLDivElement>(null);\n  const characters = '~≈≋⋿⊰⊱◟◝';\n  const rows = 25;\n  const cols = 52; // Adjusted for better fit in container\n  \n  // Pre-calculate constants\n  const centerPos = { x: 0.5, y: 0.5 };\n  const charactersLength = characters.length;\n  const charLengthDivide4 = charactersLength / 4;\n  const piTimes2 = Math.PI * 2;\n  \n  // Track time for animation updates\n  const lastUpdateRef = useRef<number>(0);\n\n  // Animation frame callback\n  const updateAnimation = useCallback((deltaTime: number) => {\n    // Update only every ~100ms for performance\n    lastUpdateRef.current += deltaTime;\n    if (lastUpdateRef.current > 166) { // ~166ms for 0.6x original speed (slowed another 25%)\n      setFrame(f => f + 1);\n      lastUpdateRef.current = 0;\n    }\n  }, []);\n\n  // Use animation frame instead of setInterval\n  useAnimationFrame(updateAnimation);\n\n  useEffect(() => {\n    return () => {\n      lastUpdateRef.current = 0;\n    };\n  }, []);\n\n  // Generate ASCII art - like water finding its natural path\n  const generateAscii = useCallback(() => {\n    \n    const rowsArray = [];\n    const frameDiv4 = frame / 6.7;  // 0.6x speed (4 -> 6.7, additional 25% reduction)\n    const frameDiv5 = frame / 8.3;  // 0.6x speed (5 -> 8.3, additional 25% reduction)\n    const frameDiv8 = frame / 13.3; // 0.6x speed (8 -> 13.3, additional 25% reduction)\n    \n    for (let y = 0; y < rows; y++) {\n      const yDivRows = y / rows;\n      const yDiv5 = y / 5;\n      const yDiv3 = y / 3;\n      let rowString = '';\n      let rowOpacity = 1;\n      \n      for (let x = 0; x < cols; x++) {\n        const xDivCols = x / cols;\n        const xDiv3 = x / 3;\n        const xDiv4 = x / 4;\n        \n        // Calculate distance from center (fixed point)\n        const dx = xDivCols - centerPos.x;\n        const dy = yDivRows - centerPos.y;\n        const dist = Math.sqrt(dx * dx + dy * dy);\n        const distTimes10 = dist * 10;\n        const distTimes5 = dist * 5;\n\n        // Create wave pattern - grace flowing without force\n        const wave = Math.sin(xDiv3 + yDiv5 + frameDiv4 + distTimes10) + \n                    Math.cos(xDiv4 - yDiv3 - frameDiv5) +\n                    Math.sin(frameDiv8 + xDivCols * piTimes2);\n\n        // Choose character based on wave value and distance\n        const charValue = (wave + 2) * charLengthDivide4 + distTimes5;\n        const charIndex = Math.floor(Math.abs(charValue)) % charactersLength;\n        \n        // Calculate opacity - characters seek lower places like water\n        const opacity = Math.max(0.2, Math.min(0.8, 1 - dist + Math.sin(wave) / 3));\n        \n        // Set row opacity to average of all opacity values in the row (approximation)\n        if (x === 0) rowOpacity = opacity;\n        else rowOpacity = (rowOpacity + opacity) / 2;\n        \n        rowString += characters[charIndex];\n      }\n      \n      rowsArray.push({ text: rowString, opacity: rowOpacity });\n    }\n    return rowsArray;\n  }, [frame, rows, cols, charactersLength, charLengthDivide4, piTimes2, centerPos.x, centerPos.y, characters]);\n\n  // Calculate ASCII art on frame changes\n  const ascii = useMemo(() => generateAscii(), [generateAscii]);\n\n  // Memoize styles to prevent recalculation\n  const containerStyle = useMemo(() => ({ \n    margin: 0,\n    background: '#F0EEE6',\n    overflow: 'hidden',\n    display: 'flex',\n    alignItems: 'center',\n    justifyContent: 'center',\n    height: '100%'\n  }), []);\n\n  const innerContainerStyle = useMemo(() => ({\n    padding: '30px',\n    position: 'relative' as const,\n    width: '100%',\n    height: '100%',\n    display: 'flex',\n    alignItems: 'center',\n    justifyContent: 'center'\n  }), []);\n\n  const preStyle = useMemo(() => ({\n    fontFamily: 'monospace',\n    fontSize: '10px',\n    lineHeight: '1',\n    cursor: 'default',\n    userSelect: 'none' as const,\n    margin: 0,\n    padding: '20px'\n  }), []);\n\n  return (\n    <div style={containerStyle}>\n      <div \n        ref={containerRef}\n        style={innerContainerStyle}\n      >\n        <pre style={preStyle}>\n          {ascii.map((row, i) => (\n            <div \n              key={i} \n              style={{ \n                opacity: row.opacity, \n                margin: 0,\n                lineHeight: '1' \n              }}\n            >\n              {row.text}\n            </div>\n          ))}\n        </pre>\n      </div>\n    </div>\n  );\n};\n\nexport default WaterAscii;",sourceCodeMin:"import React, { useState, useEffect, useRef, useMemo, useCallback } from 'react'; const useAnimationFrame = (callback: (deltaTime: number) => void, isRunning = true) => { const requestRef = useRef<number | null>(null); const previousTimeRef = useRef<number | null>(null); const animate = useCallback((time: number) => { if (previousTimeRef.current !== null) { const deltaTime = time - previousTimeRef.current; callback(deltaTime); } previousTimeRef.current = time; requestRef.current = requestAnimationFrame(animate); }, [callback]); useEffect(() => { if (isRunning) { requestRef.current = requestAnimationFrame(animate); } return () => { if (requestRef.current !== null) { cancelAnimationFrame(requestRef.current); requestRef.current = null; } previousTimeRef.current = null; }; }, [animate, isRunning]); }; const WaterAscii: React.FC = () => { const [frame, setFrame] = useState(0); const containerRef = useRef<HTMLDivElement>(null); const characters = '~≈≋⋿⊰⊱◟◝'; const rows = 25; const cols = 52; const centerPos = { x: 0.5, y: 0.5 }; const charactersLength = characters.length; const charLengthDivide4 = charactersLength / 4; const piTimes2 = Math.PI * 2; const lastUpdateRef = useRef<number>(0); const updateAnimation = useCallback((deltaTime: number) => { lastUpdateRef.current += deltaTime; if (lastUpdateRef.current > 166) { setFrame(f => f + 1); lastUpdateRef.current = 0; } }, []); useAnimationFrame(updateAnimation); useEffect(() => { return () => { lastUpdateRef.current = 0; }; }, []); const generateAscii = useCallback(() => { const rowsArray = []; const frameDiv4 = frame / 6.7; const frameDiv5 = frame / 8.3; const frameDiv8 = frame / 13.3; for (let y = 0; y < rows; y++) { const yDivRows = y / rows; const yDiv5 = y / 5; const yDiv3 = y / 3; let rowString = ''; let rowOpacity = 1; for (let x = 0; x < cols; x++) { const xDivCols = x / cols; const xDiv3 = x / 3; const xDiv4 = x / 4; const dx = xDivCols - centerPos.x; const dy = yDivRows - centerPos.y; const dist = Math.sqrt(dx * dx + dy * dy); const distTimes10 = dist * 10; const distTimes5 = dist * 5; const wave = Math.sin(xDiv3 + yDiv5 + frameDiv4 + distTimes10) + Math.cos(xDiv4 - yDiv3 - frameDiv5) + Math.sin(frameDiv8 + xDivCols * piTimes2); const charValue = (wave + 2) * charLengthDivide4 + distTimes5; const charIndex = Math.floor(Math.abs(charValue)) % charactersLength; const opacity = Math.max(0.2, Math.min(0.8, 1 - dist + Math.sin(wave) / 3)); if (x === 0) rowOpacity = opacity; else rowOpacity = (rowOpacity + opacity) / 2; rowString += characters[charIndex]; } rowsArray.push({ text: rowString, opacity: rowOpacity }); } return rowsArray; }, [frame, rows, cols, charactersLength, charLengthDivide4, piTimes2, centerPos.x, centerPos.y, characters]); const ascii = useMemo(() => generateAscii(), [generateAscii]); const containerStyle = useMemo(() => ({ margin: 0, background: '#F0EEE6', overflow: 'hidden', display: 'flex', alignItems: 'center', justifyContent: 'center', height: '100%' }), []); const innerContainerStyle = useMemo(() => ({ padding: '30px', position: 'relative' as const, width: '100%', height: '100%', display: 'flex', alignItems: 'center', justifyContent: 'center' }), []); const preStyle = useMemo(() => ({ fontFamily: 'monospace', fontSize: '10px', lineHeight: '1', cursor: 'default', userSelect: 'none' as const, margin: 0, padding: '20px' }), []); return ( <div style={containerStyle}><div ref={containerRef} style={innerContainerStyle} ><pre style={preStyle}> {ascii.map((row, i) => ( <div key={i} style={{ opacity: row.opacity, margin: 0, lineHeight: '1' }} > {row.text} </div> ))} </pre></div></div> ); }; export default WaterAscii;",text:"The highest good is like water.\nWater nourishes the ten thousand things\nwithout effort.\nIt seeks low places others look down upon.\nThus, it’s in harmony with Source.\n\nIn design: remain simple.\nIn thought: embrace depth.\nIn relations: be kind.\nIn leading: be just.\nIn work: be competent.\nIn action: consider timing.\n\nFind contentment in being your true self.\nBe without ambition, envy or need to fit in.\nNo force, just grace,\nand all is done,\nin peace.",shouldDisplayMobileArtwork:!0,promptSuggestion:"make it sci fi"},{number:9,staticArtwork:In,sourceCode:"// @ts-nocheck\nimport React, { useEffect, useRef } from 'react';\n\n// themes: excess leads to loss, detachment after completion, the way of heaven\n// visualization: Particles flow naturally downward, neither clinging nor overflowing\n\nconst CanyonMultiLayerFlows = () => {\n  const canvasRef = useRef(null);\n  const animationFrameRef = useRef(null);\n  \n  useEffect(() => {\n    const canvas = canvasRef.current;\n    if (!canvas) return;\n    \n    const ctx = canvas.getContext('2d');\n    const width = canvas.width = 550;\n    const height = canvas.height = 550;\n    const centerX = width / 2;\n    const centerY = height / 2;\n    \n    const PARTICLE_COUNT = 20000;  // Reduced particle count for softer look\n    const WALL_LAYERS = 8;\n    const particles = [];\n    \n    // Create particles - balanced between fullness and emptiness\n    for (let i = 0; i < PARTICLE_COUNT; i++) {\n      // Determine wall side (-1 or 1) and layer\n      const side = Math.random() < 0.5 ? -1 : 1;\n      const layer = Math.floor(Math.random() * WALL_LAYERS);\n      const y = Math.random() * height;\n      \n      // Create multiple wave functions for complex undulations\n      const wavePhase1 = y * 0.008;\n      const wavePhase2 = y * 0.03;\n      const wavePhase3 = y * 0.05;\n      \n      const baseWave = Math.sin(wavePhase1) * 50;\n      const secondaryWave = Math.sin(wavePhase2 * 2 + layer * 0.5) * 25;\n      const tertiaryWave = Math.sin(wavePhase3 * 3 + layer * 1.2) * 12;\n      \n      const combinedWave = baseWave + secondaryWave + tertiaryWave;\n      const layerDepth = layer * 15;\n      const wallThickness = 20 + layer * 8;\n      \n      const baseX = centerX + side * (80 + combinedWave + layerDepth);\n      const offsetX = (Math.random() - 0.5) * wallThickness;\n      \n      particles.push({\n        x: baseX + offsetX,\n        y: y,\n        z: (layer - WALL_LAYERS/2) * 20 + (Math.random() - 0.5) * 15,\n        side: side,\n        layer: layer,\n        initialY: y,\n        drift: Math.random() * Math.PI * 2,\n        speed: 0.1 + layer * 0.02,\n        brightness: 0.7 + Math.random() * 0.3\n      });\n    }\n    \n    let time = 0;\n    \n    function animate() {\n      time += 0.016;\n      \n      // Clear with subtle persistence\n      ctx.fillStyle = 'rgba(240, 238, 230, 0.05)';  // Increased fade for softer transitions\n      ctx.fillRect(0, 0, width, height);\n      \n      // Sort particles by z-depth for proper layering\n      particles.sort((a, b) => a.z - b.z);\n      \n      particles.forEach(particle => {\n        // Calculate complex wave position\n        const wavePhase1 = particle.y * 0.008 + time * 0.05;\n        const wavePhase2 = particle.y * 0.03 + time * 0.1 + particle.layer * 0.5;\n        const wavePhase3 = particle.y * 0.05 + time * 0.15 + particle.layer * 1.2;\n        \n        const baseWave = Math.sin(wavePhase1) * 50;\n        const secondaryWave = Math.sin(wavePhase2 * 2) * 25;\n        const tertiaryWave = Math.sin(wavePhase3 * 3) * 12;\n        \n        const combinedWave = baseWave + secondaryWave + tertiaryWave;\n        const layerDepth = particle.layer * 15;\n        const wallThickness = 20 + particle.layer * 8;\n        \n        // Calculate target position with layer offset\n        const targetX = centerX + particle.side * (80 + combinedWave + layerDepth);\n        const layerDrift = Math.sin(particle.drift + time * 0.5 + particle.layer * 0.3) * wallThickness * 0.5;\n        \n        // Smooth movement\n        particle.x = particle.x * 0.92 + (targetX + layerDrift) * 0.08;\n        particle.y += particle.speed;\n        \n        // Add depth oscillation\n        particle.z += Math.sin(time * 0.4 + particle.drift + particle.layer * 0.8) * 0.2;\n        \n        // Reset at bottom - detachment after completion\n        if (particle.y > height + 30) {\n          particle.y = -30;\n          particle.drift = Math.random() * Math.PI * 2;\n        }\n        \n        // Draw with layer-based effects - following heaven's way of moderation\n        const depthFactor = (particle.z + WALL_LAYERS * 10) / (WALL_LAYERS * 20);\n        const opacity = 0.25 + depthFactor * 0.15;\n        const size = 0.3 + depthFactor * 0.3;  // Base size 0.3, scaling 0.3 to reach max of 0.6\n        const brightness = 120 + particle.layer * 3 + particle.brightness * 15;  // Much lighter gray\n        \n        if (opacity > 0 && size > 0) {\n          // Layer-based glow\n          if (particle.layer < 3) {\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, size * 1.5, 0, Math.PI * 2);  // Reduced glow radius multiplier from 2 to 1.5\n            ctx.fillStyle = `rgba(${brightness}, ${brightness}, ${brightness}, ${opacity * 0.1})`;\n            ctx.fill();\n          }\n          \n          // Main particle\n          ctx.beginPath();\n          ctx.arc(particle.x, particle.y, size, 0, Math.PI * 2);\n          ctx.fillStyle = `rgba(${brightness}, ${brightness}, ${brightness}, ${opacity})`;\n          ctx.fill();\n        }\n      });\n      \n      animationFrameRef.current = requestAnimationFrame(animate);\n    }\n    \n    animate();\n    \n    return () => {\n      if (animationFrameRef.current) {\n        cancelAnimationFrame(animationFrameRef.current);\n        animationFrameRef.current = null;\n      }\n      if (canvas && ctx) {\n        ctx.clearRect(0, 0, width, height);\n      }\n      particles.length = 0;\n    };\n  }, []);\n  \n  return (\n    <div style={{\n      width: '550px',\n      height: '550px',\n      margin: 'auto',\n      backgroundColor: '#F0EEE6',\n      overflow: 'hidden'\n    }}>\n      <canvas\n        ref={canvasRef}\n        style={{\n          display: 'block',\n          width: '550px',\n          height: '550px'\n        }}\n      />\n    </div>\n  );\n};\n\nexport default CanyonMultiLayerFlows;",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; const CanyonMultiLayerFlows = () => { const canvasRef = useRef(null); const animationFrameRef = useRef(null); useEffect(() => { const canvas = canvasRef.current; if (!canvas) return; const ctx = canvas.getContext('2d'); const width = canvas.width = 550; const height = canvas.height = 550; const centerX = width / 2; const centerY = height / 2; const PARTICLE_COUNT = 20000; const WALL_LAYERS = 8; const particles = []; for (let i = 0; i < PARTICLE_COUNT; i++) { const side = Math.random() < 0.5 ? -1 : 1; const layer = Math.floor(Math.random() * WALL_LAYERS); const y = Math.random() * height; const wavePhase1 = y * 0.008; const wavePhase2 = y * 0.03; const wavePhase3 = y * 0.05; const baseWave = Math.sin(wavePhase1) * 50; const secondaryWave = Math.sin(wavePhase2 * 2 + layer * 0.5) * 25; const tertiaryWave = Math.sin(wavePhase3 * 3 + layer * 1.2) * 12; const combinedWave = baseWave + secondaryWave + tertiaryWave; const layerDepth = layer * 15; const wallThickness = 20 + layer * 8; const baseX = centerX + side * (80 + combinedWave + layerDepth); const offsetX = (Math.random() - 0.5) * wallThickness; particles.push({ x: baseX + offsetX, y: y, z: (layer - WALL_LAYERS/2) * 20 + (Math.random() - 0.5) * 15, side: side, layer: layer, initialY: y, drift: Math.random() * Math.PI * 2, speed: 0.1 + layer * 0.02, brightness: 0.7 + Math.random() * 0.3 }); } let time = 0; function animate() { time += 0.016; ctx.fillStyle = 'rgba(240, 238, 230, 0.05)'; ctx.fillRect(0, 0, width, height); particles.sort((a, b) => a.z - b.z); particles.forEach(particle => { const wavePhase1 = particle.y * 0.008 + time * 0.05; const wavePhase2 = particle.y * 0.03 + time * 0.1 + particle.layer * 0.5; const wavePhase3 = particle.y * 0.05 + time * 0.15 + particle.layer * 1.2; const baseWave = Math.sin(wavePhase1) * 50; const secondaryWave = Math.sin(wavePhase2 * 2) * 25; const tertiaryWave = Math.sin(wavePhase3 * 3) * 12; const combinedWave = baseWave + secondaryWave + tertiaryWave; const layerDepth = particle.layer * 15; const wallThickness = 20 + particle.layer * 8; const targetX = centerX + particle.side * (80 + combinedWave + layerDepth); const layerDrift = Math.sin(particle.drift + time * 0.5 + particle.layer * 0.3) * wallThickness * 0.5; particle.x = particle.x * 0.92 + (targetX + layerDrift) * 0.08; particle.y += particle.speed; particle.z += Math.sin(time * 0.4 + particle.drift + particle.layer * 0.8) * 0.2; if (particle.y > height + 30) { particle.y = -30; particle.drift = Math.random() * Math.PI * 2; } const depthFactor = (particle.z + WALL_LAYERS * 10) / (WALL_LAYERS * 20); const opacity = 0.25 + depthFactor * 0.15; const size = 0.3 + depthFactor * 0.3; const brightness = 120 + particle.layer * 3 + particle.brightness * 15; if (opacity > 0 && size > 0) { if (particle.layer < 3) { ctx.beginPath(); ctx.arc(particle.x, particle.y, size * 1.5, 0, Math.PI * 2); ctx.fillStyle = `rgba(${brightness}, ${brightness}, ${brightness}, ${opacity * 0.1})`; ctx.fill(); } ctx.beginPath(); ctx.arc(particle.x, particle.y, size, 0, Math.PI * 2); ctx.fillStyle = `rgba(${brightness}, ${brightness}, ${brightness}, ${opacity})`; ctx.fill(); } }); animationFrameRef.current = requestAnimationFrame(animate); } animate(); return () => { if (animationFrameRef.current) { cancelAnimationFrame(animationFrameRef.current); animationFrameRef.current = null; } if (canvas && ctx) { ctx.clearRect(0, 0, width, height); } particles.length = 0; }; }, []); return ( <div style={{ width: '550px', height: '550px', margin: 'auto', backgroundColor: '#F0EEE6', overflow: 'hidden' }}><canvas ref={canvasRef} style={{ display: 'block', width: '550px', height: '550px' }} /></div> ); }; export default CanyonMultiLayerFlows;",text:"Fill your cup to the brim\nand it will spill.\nOversharpen your blade\nand it will dull.\nChase after worldly fortune\nand no one can protect it.\n\nCare about others’ approval\nand you become their prisoner.\n\nWhen the work is done,\nlog off and detach.\nThis is the way of heaven.",shouldDisplayMobileArtwork:!0,promptSuggestion:"make it move like a waterfall"},{number:10,staticArtwork:R,sourceCode:"// @ts-nocheck\nimport React, { useEffect, useRef } from 'react';\n\n// themes: present awareness, leading without control, doing without expectation\n// visualization: A meditative circle that breathes and flows while remaining centered in stillness\n\nconst WavyYinYangNoDots = () => {\n  const canvasRef = useRef(null);\n  const animationFrameRef = useRef(null);\n\n  useEffect(() => {\n    const canvas = canvasRef.current;\n    if (!canvas) return;\n    \n    const ctx = canvas.getContext('2d');\n    const width = canvas.width;\n    const height = canvas.height;\n    const centerX = width / 2;\n    const centerY = height / 2;\n    const maxRadius = width * 0.45;\n    \n    let time = 0;\n    \n    const draw = () => {\n      ctx.fillStyle = '#F0EEE6';\n      ctx.fillRect(0, 0, width, height);\n      \n      // Draw wavy concentric lines - present in each moment of transformation\n      for (let r = 5; r < maxRadius; r += 3) {\n        ctx.beginPath();\n        ctx.strokeStyle = '#000';\n        ctx.lineWidth = 0.6;\n        \n        for (let angle = 0; angle < Math.PI * 2; angle += 0.02) {\n          // Animate the wave with time\n          const wave = Math.sin(angle * 8 + r * 0.1 + time) * 2;\n          const x = centerX + (r + wave) * Math.cos(angle);\n          const y = centerY + (r + wave) * Math.sin(angle);\n          \n          // Create yin-yang boundary - leading without controlling its flow\n          const isYin = (angle > Math.PI) ? \n            (Math.sqrt(Math.pow(x - centerX, 2) + Math.pow(y - (centerY + maxRadius/4), 2)) < maxRadius/4) :\n            (Math.sqrt(Math.pow(x - centerX, 2) + Math.pow(y - (centerY - maxRadius/4), 2)) > maxRadius/4);\n          \n          if (isYin) {\n            ctx.strokeStyle = 'rgba(0,0,0,0.3)';\n            ctx.lineTo(x, y);\n          } else {\n            ctx.strokeStyle = '#000';\n            ctx.lineTo(x, y);\n          }\n        }\n        ctx.stroke();\n      }\n      \n      // Continue without expectation, each frame arising naturally\n      time += 0.015;\n      animationFrameRef.current = requestAnimationFrame(draw);\n    };\n    \n    draw();\n    \n    return () => {\n      if (animationFrameRef.current) {\n        cancelAnimationFrame(animationFrameRef.current);\n        animationFrameRef.current = null;\n      }\n      if (canvas && ctx) {\n        ctx.clearRect(0, 0, width, height);\n      }\n    };\n  }, []);\n\n  return (\n    <div style={{ \n      width: '550px',\n      height: '550px',\n      backgroundColor: '#F0EEE6',\n      display: 'flex',\n      alignItems: 'center',\n      justifyContent: 'center'\n    }}>\n      <canvas \n        ref={canvasRef} \n        width={550} \n        height={550} \n      />\n    </div>\n  );\n};\n\nexport default WavyYinYangNoDots;",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; const WavyYinYangNoDots = () => { const canvasRef = useRef(null); const animationFrameRef = useRef(null); useEffect(() => { const canvas = canvasRef.current; if (!canvas) return; const ctx = canvas.getContext('2d'); const width = canvas.width; const height = canvas.height; const centerX = width / 2; const centerY = height / 2; const maxRadius = width * 0.45; let time = 0; const draw = () => { ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, width, height); for (let r = 5; r < maxRadius; r += 3) { ctx.beginPath(); ctx.strokeStyle = '#000'; ctx.lineWidth = 0.6; for (let angle = 0; angle < Math.PI * 2; angle += 0.02) { const wave = Math.sin(angle * 8 + r * 0.1 + time) * 2; const x = centerX + (r + wave) * Math.cos(angle); const y = centerY + (r + wave) * Math.sin(angle); const isYin = (angle > Math.PI) ? (Math.sqrt(Math.pow(x - centerX, 2) + Math.pow(y - (centerY + maxRadius/4), 2)) < maxRadius/4) : (Math.sqrt(Math.pow(x - centerX, 2) + Math.pow(y - (centerY - maxRadius/4), 2)) > maxRadius/4); if (isYin) { ctx.strokeStyle = 'rgba(0,0,0,0.3)'; ctx.lineTo(x, y); } else { ctx.strokeStyle = '#000'; ctx.lineTo(x, y); } } ctx.stroke(); } time += 0.015; animationFrameRef.current = requestAnimationFrame(draw); }; draw(); return () => { if (animationFrameRef.current) { cancelAnimationFrame(animationFrameRef.current); animationFrameRef.current = null; } if (canvas && ctx) { ctx.clearRect(0, 0, width, height); } }; }, []); return ( <div style={{ width: '550px', height: '550px', backgroundColor: '#F0EEE6', display: 'flex', alignItems: 'center', justifyContent: 'center' }}><canvas ref={canvasRef} width={550} height={550} /></div> ); }; export default WavyYinYangNoDots;",text:"Can you remain present and focused\nwhile retaining an open, receptive mind.\nCan you remain centered and still\nuntil clarity arises alone, unattended.\n\nLove your project\nand lead without controlling.\nDo nothing\nand allow all things to be done.\n\nCreate without possessing.\nWork without expectations.\n\nThis is the highest realization of being.",shouldDisplayMobileArtwork:!0,promptSuggestion:"let me control how fast it spins"},{number:11,staticArtwork:ae,sourceCode:"import React, { useEffect, useRef } from 'react';\nimport * as THREE from 'three';\n\n// themes: emptiness creates utility, space enables function, usefulness through void\n// visualization: Three structures defined by their empty spaces - a wheel's hub, a vessel's cavity, a room's openings\n\nconst VoidArchitecture = () => {\n  const mountRef = useRef(null);\n\n  useEffect(() => {\n    const container = mountRef.current;\n    if (!container) return;\n\n    // Get container dimensions\n    const width = container.clientWidth;\n    const height = container.clientHeight;\n    const dpr = window.devicePixelRatio || 1;\n\n    // Set up scene\n    const scene = new THREE.Scene();\n    const camera = new THREE.PerspectiveCamera(75, width / height, 0.1, 1000);\n    const renderer = new THREE.WebGLRenderer({ antialias: true });\n\n    // Improve sharpness for high DPI screens\n    renderer.setPixelRatio(Math.min(dpr, 2)); // Cap at 2 for performance\n    renderer.setSize(width, height);\n    renderer.setClearColor(0xF0EEE6);\n\n    container.appendChild(renderer.domElement);\n\n    // Lighting\n    const ambientLight = new THREE.AmbientLight(0x404040);\n    const directionalLight = new THREE.DirectionalLight(0x808080, 1);\n    directionalLight.position.set(1, 1, 1);\n    scene.add(ambientLight);\n    scene.add(directionalLight);\n\n    // Materials\n    const material = new THREE.LineBasicMaterial({ \n      color: 0x333333,\n      transparent: true,\n      opacity: 0.6\n    });\n\n    // Create wheel structure (\"Thirty spokes join the hub\")\n    const createWheel = () => {\n      const geometry = new THREE.BufferGeometry();\n      const vertices = [];\n      const spokes = 30;\n      const radius = 8;\n      const hubRadius = 1;\n      \n      // Create hub\n      for (let i = 0; i < spokes; i++) {\n        const angle = (i / spokes) * Math.PI * 2;\n        const nextAngle = ((i + 1) / spokes) * Math.PI * 2;\n        \n        // Hub circle\n        vertices.push(\n          Math.cos(angle) * hubRadius, 0, Math.sin(angle) * hubRadius,\n          Math.cos(nextAngle) * hubRadius, 0, Math.sin(nextAngle) * hubRadius\n        );\n        \n        // Spokes\n        vertices.push(\n          Math.cos(angle) * hubRadius, 0, Math.sin(angle) * hubRadius,\n          Math.cos(angle) * radius, 0, Math.sin(angle) * radius\n        );\n      }\n\n      geometry.setAttribute('position', new THREE.Float32BufferAttribute(vertices, 3));\n      return new THREE.LineSegments(geometry, material);\n    };\n\n    // Create vessel (\"Clay forms a vessel\")\n    const createVessel = () => {\n      const geometry = new THREE.BufferGeometry();\n      const vertices = [];\n      const layers = 20;\n      const pointsPerLayer = 16;\n      \n      for (let i = 0; i <= layers; i++) {\n        const y = i - layers/2;\n        // Create vessel shape\n        const radius = 3 * Math.sin(Math.PI * (i/layers));\n        \n        for (let j = 0; j < pointsPerLayer; j++) {\n          const angle1 = (j/pointsPerLayer) * Math.PI * 2;\n          const angle2 = ((j+1)/pointsPerLayer) * Math.PI * 2;\n          \n          vertices.push(\n            Math.cos(angle1) * radius, y, Math.sin(angle1) * radius,\n            Math.cos(angle2) * radius, y, Math.sin(angle2) * radius\n          );\n\n          // Vertical lines\n          if (i < layers) {\n            vertices.push(\n              Math.cos(angle1) * radius, y, Math.sin(angle1) * radius,\n              Math.cos(angle1) * radius, y + 1, Math.sin(angle1) * radius\n            );\n          }\n        }\n      }\n\n      geometry.setAttribute('position', new THREE.Float32BufferAttribute(vertices, 3));\n      return new THREE.LineSegments(geometry, material);\n    };\n\n    // Create room structure (\"We build walls with windows and doors\")\n    const createRoom = () => {\n      const geometry = new THREE.BufferGeometry();\n      const vertices = [];\n      const size = 6;\n      const height = 8;\n      \n      // Base frame\n      const basePoints = [\n        [-size, 0, -size],\n        [size, 0, -size],\n        [size, 0, size],\n        [-size, 0, size],\n        [-size, height, -size],\n        [size, height, -size],\n        [size, height, size],\n        [-size, height, size]\n      ];\n\n      // Connect base points\n      for (let i = 0; i < 4; i++) {\n        // Bottom square\n        vertices.push(\n          ...basePoints[i],\n          ...basePoints[(i + 1) % 4]\n        );\n        // Top square\n        vertices.push(\n          ...basePoints[i + 4],\n          ...basePoints[((i + 1) % 4) + 4]\n        );\n        // Vertical lines\n        vertices.push(\n          ...basePoints[i],\n          ...basePoints[i + 4]\n        );\n      }\n\n      // Add door frame\n      const doorWidth = 2;\n      const doorHeight = 4;\n      vertices.push(\n        -doorWidth/2, 0, -size,\n        -doorWidth/2, doorHeight, -size,\n        doorWidth/2, 0, -size,\n        doorWidth/2, doorHeight, -size,\n        -doorWidth/2, doorHeight, -size,\n        doorWidth/2, doorHeight, -size\n      );\n\n      // Add windows\n      const windowSize = 1.5;\n      const windowHeight = 5;\n      const addWindow = (x, z) => {\n        vertices.push(\n          x - windowSize, windowHeight - windowSize, z,\n          x + windowSize, windowHeight - windowSize, z,\n          x + windowSize, windowHeight + windowSize, z,\n          x - windowSize, windowHeight + windowSize, z,\n          x - windowSize, windowHeight - windowSize, z\n        );\n      };\n\n      // Add windows to sides\n      addWindow(-size, 0);\n      addWindow(size, 0);\n\n      geometry.setAttribute('position', new THREE.Float32BufferAttribute(vertices, 3));\n      return new THREE.LineSegments(geometry, material);\n    };\n\n    const wheel = createWheel();\n    const vessel = createVessel();\n    const room = createRoom();\n\n    // Position elements\n    wheel.position.set(-12, 0, 0);\n    vessel.position.set(12, 0, 0);\n    room.position.set(0, -4, 0);\n\n    scene.add(wheel);\n    scene.add(vessel);\n    scene.add(room);\n\n    // Position camera\n    camera.position.set(15, 15, 25);\n    camera.lookAt(0, 0, 0);\n\n    // Animation\n    let frameId;\n    const animate = () => {\n      frameId = requestAnimationFrame(animate);\n\n      // Gentle rotation\n      wheel.rotation.y += 0.002;\n      vessel.rotation.y += 0.001;\n      room.rotation.y += 0.0005;\n\n      renderer.render(scene, camera);\n    };\n    animate();\n\n    // Handle resize\n    const handleResize = () => {\n      const width = container.clientWidth;\n      const height = container.clientHeight;\n      const dpr = window.devicePixelRatio || 1;\n      \n      camera.aspect = width / height;\n      camera.updateProjectionMatrix();\n      renderer.setPixelRatio(Math.min(dpr, 2)); // Update pixel ratio on resize\n      renderer.setSize(width, height);\n    };\n    window.addEventListener('resize', handleResize);\n\n    // Cleanup function\n    return () => {\n      window.removeEventListener('resize', handleResize);\n      cancelAnimationFrame(frameId);\n      \n      // Clean up Three.js resources\n      if (renderer) {\n        renderer.dispose();\n        if (container.contains(renderer.domElement)) {\n          container.removeChild(renderer.domElement);\n        }\n      }\n      \n      // Clean up geometry materials\n      scene.traverse((object) => {\n        if (object instanceof THREE.Mesh) {\n          if (object.geometry) object.geometry.dispose();\n          if (object.material) {\n            if (Array.isArray(object.material)) {\n              object.material.forEach(material => material.dispose());\n            } else {\n              object.material.dispose();\n            }\n          }\n        }\n      });\n    };\n  }, []);\n\n  return (\n    <div \n      ref={mountRef}\n      style={{ \n        margin: 0,\n        background: '#F0EEE6',\n        overflow: 'hidden',\n        display: 'flex',\n        alignItems: 'center',\n        justifyContent: 'center',\n        height: '100vh',\n        width: '100vw',\n        position: 'relative'\n      }}\n    />\n  );\n};\n\nexport default VoidArchitecture;",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; import * as THREE from 'three'; const VoidArchitecture = () => { const mountRef = useRef(null); useEffect(() => { const container = mountRef.current; if (!container) return; const width = container.clientWidth; const height = container.clientHeight; const dpr = window.devicePixelRatio || 1; const scene = new THREE.Scene(); const camera = new THREE.PerspectiveCamera(75, width / height, 0.1, 1000); const renderer = new THREE.WebGLRenderer({ antialias: true }); renderer.setPixelRatio(Math.min(dpr, 2)); renderer.setSize(width, height); renderer.setClearColor(0xF0EEE6); container.appendChild(renderer.domElement); const ambientLight = new THREE.AmbientLight(0x404040); const directionalLight = new THREE.DirectionalLight(0x808080, 1); directionalLight.position.set(1, 1, 1); scene.add(ambientLight); scene.add(directionalLight); const material = new THREE.LineBasicMaterial({ color: 0x333333, transparent: true, opacity: 0.6 }); const createWheel = () => { const geometry = new THREE.BufferGeometry(); const vertices = []; const spokes = 30; const radius = 8; const hubRadius = 1; for (let i = 0; i < spokes; i++) { const angle = (i / spokes) * Math.PI * 2; const nextAngle = ((i + 1) / spokes) * Math.PI * 2; vertices.push( Math.cos(angle) * hubRadius, 0, Math.sin(angle) * hubRadius, Math.cos(nextAngle) * hubRadius, 0, Math.sin(nextAngle) * hubRadius ); vertices.push( Math.cos(angle) * hubRadius, 0, Math.sin(angle) * hubRadius, Math.cos(angle) * radius, 0, Math.sin(angle) * radius ); } geometry.setAttribute('position', new THREE.Float32BufferAttribute(vertices, 3)); return new THREE.LineSegments(geometry, material); }; const createVessel = () => { const geometry = new THREE.BufferGeometry(); const vertices = []; const layers = 20; const pointsPerLayer = 16; for (let i = 0; i <= layers; i++) { const y = i - layers/2; const radius = 3 * Math.sin(Math.PI * (i/layers)); for (let j = 0; j < pointsPerLayer; j++) { const angle1 = (j/pointsPerLayer) * Math.PI * 2; const angle2 = ((j+1)/pointsPerLayer) * Math.PI * 2; vertices.push( Math.cos(angle1) * radius, y, Math.sin(angle1) * radius, Math.cos(angle2) * radius, y, Math.sin(angle2) * radius ); if (i < layers) { vertices.push( Math.cos(angle1) * radius, y, Math.sin(angle1) * radius, Math.cos(angle1) * radius, y + 1, Math.sin(angle1) * radius ); } } } geometry.setAttribute('position', new THREE.Float32BufferAttribute(vertices, 3)); return new THREE.LineSegments(geometry, material); }; const createRoom = () => { const geometry = new THREE.BufferGeometry(); const vertices = []; const size = 6; const height = 8; const basePoints = [ [-size, 0, -size], [size, 0, -size], [size, 0, size], [-size, 0, size], [-size, height, -size], [size, height, -size], [size, height, size], [-size, height, size] ]; for (let i = 0; i < 4; i++) { vertices.push( ...basePoints[i], ...basePoints[(i + 1) % 4] ); vertices.push( ...basePoints[i + 4], ...basePoints[((i + 1) % 4) + 4] ); vertices.push( ...basePoints[i], ...basePoints[i + 4] ); } const doorWidth = 2; const doorHeight = 4; vertices.push( -doorWidth/2, 0, -size, -doorWidth/2, doorHeight, -size, doorWidth/2, 0, -size, doorWidth/2, doorHeight, -size, -doorWidth/2, doorHeight, -size, doorWidth/2, doorHeight, -size ); const windowSize = 1.5; const windowHeight = 5; const addWindow = (x, z) => { vertices.push( x - windowSize, windowHeight - windowSize, z, x + windowSize, windowHeight - windowSize, z, x + windowSize, windowHeight + windowSize, z, x - windowSize, windowHeight + windowSize, z, x - windowSize, windowHeight - windowSize, z ); }; addWindow(-size, 0); addWindow(size, 0); geometry.setAttribute('position', new THREE.Float32BufferAttribute(vertices, 3)); return new THREE.LineSegments(geometry, material); }; const wheel = createWheel(); const vessel = createVessel(); const room = createRoom(); wheel.position.set(-12, 0, 0); vessel.position.set(12, 0, 0); room.position.set(0, -4, 0); scene.add(wheel); scene.add(vessel); scene.add(room); camera.position.set(15, 15, 25); camera.lookAt(0, 0, 0); let frameId; const animate = () => { frameId = requestAnimationFrame(animate); wheel.rotation.y += 0.002; vessel.rotation.y += 0.001; room.rotation.y += 0.0005; renderer.render(scene, camera); }; animate(); const handleResize = () => { const width = container.clientWidth; const height = container.clientHeight; const dpr = window.devicePixelRatio || 1; camera.aspect = width / height; camera.updateProjectionMatrix(); renderer.setPixelRatio(Math.min(dpr, 2)); renderer.setSize(width, height); }; window.addEventListener('resize', handleResize); return () => { window.removeEventListener('resize', handleResize); cancelAnimationFrame(frameId); if (renderer) { renderer.dispose(); if (container.contains(renderer.domElement)) { container.removeChild(renderer.domElement); } } scene.traverse((object) => { if (object instanceof THREE.Mesh) { if (object.geometry) object.geometry.dispose(); if (object.material) { if (Array.isArray(object.material)) { object.material.forEach(material => material.dispose()); } else { object.material.dispose(); } } } }); }; }, []); return ( <div ref={mountRef} style={{ margin: 0, background: '#F0EEE6', overflow: 'hidden', display: 'flex', alignItems: 'center', justifyContent: 'center', height: '100vh', width: '100vw', position: 'relative' }} /> ); }; export default VoidArchitecture;",text:"Thirty spokes join the hub,\nbut the wheel’s usefulness\ndepends on the emptiness at the center.\n\nClay forms a vessel,\nbut its capacity to contain\ndepends on the emptiness inside.\n\nWe build walls with windows and doors,\nbut the utility of the room\ndepends on the emptiness within.\n\nTherefore,\nWe enjoy the fullness of existence\nbut find usefulness in the spaces between.",shouldDisplayMobileArtwork:!0,promptSuggestion:"change the proportions"},{number:12,staticArtwork:Hn,sourceCode:"// @ts-nocheck\nimport React, { useRef, useEffect } from 'react';\n\n// themes: inner over outer, simplicity over sensation, open heart over thought\n// visualization: A form that transforms from complex to simple, revealing inner essence beneath surface appearance\n\nconst Metamorphosis = () => {\n  const canvasRef = useRef(null);\n  const animationFrameRef = useRef(null);\n  \n  useEffect(() => {\n    const canvas = canvasRef.current;\n    if (!canvas) return;\n    \n    const ctx = canvas.getContext('2d');\n    const width = canvas.width;\n    const height = canvas.height;\n    \n    // Configuration\n    const numLines = 120;\n    const lineSegments = 180;\n    const lineAlpha = 0.5;\n    const lineWidth = 0.6;\n    const morphSpeed = 0.0005;\n    const rotateSpeed = 0.00025;\n    \n    // Start after initial pause\n    let time = 2000;\n    \n    // Form definitions - moving from outer complexity to inner simplicity\n    const forms = [\n      // Form 1: Draped cloth-like shape\n      (u, v, t) => {\n        const theta = u * Math.PI * 2;\n        const phi = v * Math.PI;\n        \n        let r = 120 + 30 * Math.sin(phi * 4 + theta * 2);\n        r += 20 * Math.sin(phi * 6) * Math.cos(theta * 3);\n        \n        let x = r * Math.sin(phi) * Math.cos(theta);\n        let y = r * Math.sin(phi) * Math.sin(theta);\n        let z = r * Math.cos(phi) + 20 * Math.sin(theta * 5 + phi * 3);\n        \n        return { x, y, z };\n      },\n      \n      // Form 2: More angular folded shape\n      (u, v, t) => {\n        const theta = u * Math.PI * 2;\n        const phi = v * Math.PI;\n        \n        let r = 150 + 20 * Math.cos(phi * 8);\n        r *= 0.8 + 0.2 * Math.abs(Math.cos(theta * 2));\n        \n        let x = r * Math.sin(phi) * Math.cos(theta);\n        let y = r * Math.sin(phi) * Math.sin(theta);\n        let z = r * Math.cos(phi) * (0.8 + 0.3 * Math.sin(theta * 4));\n        \n        return { x, y, z };\n      },\n      \n      // Form 3: Organic bulbous shape\n      (u, v, t) => {\n        const theta = u * Math.PI * 2;\n        const phi = v * Math.PI;\n        \n        let r = 120;\n        r += 50 * Math.sin(phi * 3) * Math.sin(theta * 2.5);\n        r += 30 * Math.cos(phi * 5 + theta);\n        \n        let x = r * Math.sin(phi) * Math.cos(theta);\n        let y = r * Math.sin(phi) * Math.sin(theta);\n        let z = r * Math.cos(phi);\n        \n        // Create some hollow areas\n        const hollow = Math.max(0, Math.sin(phi * 2 + theta * 3) - 0.7);\n        r *= 1 - hollow * 0.8;\n        \n        return { x, y, z };\n      }\n    ];\n    \n    // Interpolate between forms - letting go of surface to reveal essence\n    const interpolateForms = (formA, formB, u, v, t, blend) => {\n      const pointA = formA(u, v, t);\n      const pointB = formB(u, v, t);\n      \n      return {\n        x: pointA.x * (1 - blend) + pointB.x * blend,\n        y: pointA.y * (1 - blend) + pointB.y * blend,\n        z: pointA.z * (1 - blend) + pointB.z * blend\n      };\n    };\n    \n    // Get the current form - opening to transformation with an open heart\n    const getCurrentForm = (u, v, t) => {\n      // Calculate which two forms to blend between\n      const totalForms = forms.length;\n      const cycleTime = 600; // Time to complete one full cycle\n      const position = (t % (cycleTime * totalForms)) / cycleTime;\n      const formIndex = Math.floor(position);\n      const nextFormIndex = (formIndex + 1) % totalForms;\n      \n      // Calculate blend with pause and easing\n      let rawBlend = position - formIndex;\n      \n      // No pause between transitions\n      const pauseTime = 0;\n      const transitionTime = 1 - (pauseTime * 2); // Remaining time for the transition\n      \n      let blend;\n      if (rawBlend < pauseTime) {\n        // Initial pause\n        blend = 0;\n      } else if (rawBlend > (1 - pauseTime)) {\n        // End pause\n        blend = 1;\n      } else {\n        // Transition with easing\n        const normalizedTime = (rawBlend - pauseTime) / transitionTime;\n        // Ease in-out cubic\n        blend = normalizedTime < 0.5\n          ? 4 * normalizedTime * normalizedTime * normalizedTime\n          : 1 - Math.pow(-2 * normalizedTime + 2, 3) / 2;\n      }\n      \n      return interpolateForms(\n        forms[formIndex], \n        forms[nextFormIndex], \n        u, v, t, blend\n      );\n    };\n    \n    // Animation loop\n    const animate = () => {\n      ctx.clearRect(0, 0, width, height);\n      ctx.fillStyle = '#F0EEE6';\n      ctx.fillRect(0, 0, width, height);\n      \n      // Calculate rotation based on time\n      const rotateX = Math.sin(time * rotateSpeed) * 0.5;\n      const rotateY = Math.cos(time * rotateSpeed * 0.7) * 0.3;\n      const rotateZ = time * rotateSpeed * 0.1;\n      \n      // Draw horizontal contour lines\n      for (let i = 0; i < numLines; i++) {\n        const v = i / (numLines - 1);\n        \n        ctx.beginPath();\n        ctx.strokeStyle = `rgba(51, 51, 51, ${lineAlpha})`;\n        ctx.lineWidth = lineWidth;\n        \n        let lastPointVisible = false;\n        let lastPoint = null;\n        \n        for (let j = 0; j <= lineSegments; j++) {\n          const u = j / lineSegments;\n          \n          // Get the current form\n          const point = getCurrentForm(u, v, time);\n          \n          // Apply rotation\n          const rotatedX = point.x * Math.cos(rotateZ) - point.y * Math.sin(rotateZ);\n          const rotatedY = point.x * Math.sin(rotateZ) + point.y * Math.cos(rotateZ);\n          const rotatedZ = point.z;\n          \n          // Project to screen\n          const scale = 1.5 + rotatedZ * 0.001;\n          const projX = width / 2 + rotatedX * scale;\n          const projY = height / 2 + rotatedY * scale;\n          \n          // Check if point should be visible (simple back-face culling)\n          const pointVisible = rotatedZ > -50;\n          \n          if (j === 0) {\n            if (pointVisible) {\n              ctx.moveTo(projX, projY);\n              lastPointVisible = true;\n              lastPoint = { x: projX, y: projY };\n            }\n          } else {\n            if (pointVisible && lastPointVisible) {\n              ctx.lineTo(projX, projY);\n            } else if (pointVisible && !lastPointVisible) {\n              ctx.moveTo(projX, projY);\n            }\n          }\n          \n          lastPointVisible = pointVisible;\n          lastPoint = { x: projX, y: projY };\n        }\n        \n        ctx.stroke();\n      }\n      \n      // Draw vertical contour lines (fewer)\n      for (let i = 0; i < numLines * 0.3; i++) {\n        const u = i / (numLines * 0.3 - 1);\n        \n        ctx.beginPath();\n        ctx.strokeStyle = `rgba(51, 51, 51, ${lineAlpha * 0.7})`;\n        ctx.lineWidth = lineWidth * 0.7;\n        \n        let lastPointVisible = false;\n        let lastPoint = null;\n        \n        for (let j = 0; j <= lineSegments * 0.5; j++) {\n          const v = j / (lineSegments * 0.5);\n          \n          // Get the current form\n          const point = getCurrentForm(u, v, time);\n          \n          // Apply rotation\n          const rotatedX = point.x * Math.cos(rotateZ) - point.y * Math.sin(rotateZ);\n          const rotatedY = point.x * Math.sin(rotateZ) + point.y * Math.cos(rotateZ);\n          const rotatedZ = point.z;\n          \n          // Project to screen\n          const scale = 1.5 + rotatedZ * 0.001;\n          const projX = width / 2 + rotatedX * scale;\n          const projY = height / 2 + rotatedY * scale;\n          \n          // Check if point should be visible\n          const pointVisible = rotatedZ > -50;\n          \n          if (j === 0) {\n            if (pointVisible) {\n              ctx.moveTo(projX, projY);\n              lastPointVisible = true;\n              lastPoint = { x: projX, y: projY };\n            }\n          } else {\n            if (pointVisible && lastPointVisible) {\n              ctx.lineTo(projX, projY);\n            } else if (pointVisible && !lastPointVisible) {\n              ctx.moveTo(projX, projY);\n            }\n          }\n          \n          lastPointVisible = pointVisible;\n          lastPoint = { x: projX, y: projY };\n        }\n        \n        ctx.stroke();\n      }\n      \n      time += 0.5;\n      animationFrameRef.current = requestAnimationFrame(animate);\n    };\n    \n    animate();\n    \n    // Cleanup\n    return () => {\n      if (animationFrameRef.current) {\n        cancelAnimationFrame(animationFrameRef.current);\n        animationFrameRef.current = null;\n      }\n      if (canvas && ctx) {\n        ctx.clearRect(0, 0, width, height);\n      }\n    };\n  }, []);\n  \n  return (\n    <div className=\"flex items-center justify-center bg-[#F0EEE6] w-full h-full\">\n      <canvas ref={canvasRef} width={550} height={550} />\n    </div>\n  );\n};\n\nexport default Metamorphosis;\n",sourceCodeMin:"import React, { useRef, useEffect } from 'react'; const Metamorphosis = () => { const canvasRef = useRef(null); const animationFrameRef = useRef(null); useEffect(() => { const canvas = canvasRef.current; if (!canvas) return; const ctx = canvas.getContext('2d'); const width = canvas.width; const height = canvas.height; const numLines = 120; const lineSegments = 180; const lineAlpha = 0.5; const lineWidth = 0.6; const morphSpeed = 0.0005; const rotateSpeed = 0.00025; let time = 2000; const forms = [ (u, v, t) => { const theta = u * Math.PI * 2; const phi = v * Math.PI; let r = 120 + 30 * Math.sin(phi * 4 + theta * 2); r += 20 * Math.sin(phi * 6) * Math.cos(theta * 3); let x = r * Math.sin(phi) * Math.cos(theta); let y = r * Math.sin(phi) * Math.sin(theta); let z = r * Math.cos(phi) + 20 * Math.sin(theta * 5 + phi * 3); return { x, y, z }; }, (u, v, t) => { const theta = u * Math.PI * 2; const phi = v * Math.PI; let r = 150 + 20 * Math.cos(phi * 8); r *= 0.8 + 0.2 * Math.abs(Math.cos(theta * 2)); let x = r * Math.sin(phi) * Math.cos(theta); let y = r * Math.sin(phi) * Math.sin(theta); let z = r * Math.cos(phi) * (0.8 + 0.3 * Math.sin(theta * 4)); return { x, y, z }; }, (u, v, t) => { const theta = u * Math.PI * 2; const phi = v * Math.PI; let r = 120; r += 50 * Math.sin(phi * 3) * Math.sin(theta * 2.5); r += 30 * Math.cos(phi * 5 + theta); let x = r * Math.sin(phi) * Math.cos(theta); let y = r * Math.sin(phi) * Math.sin(theta); let z = r * Math.cos(phi); const hollow = Math.max(0, Math.sin(phi * 2 + theta * 3) - 0.7); r *= 1 - hollow * 0.8; return { x, y, z }; } ]; const interpolateForms = (formA, formB, u, v, t, blend) => { const pointA = formA(u, v, t); const pointB = formB(u, v, t); return { x: pointA.x * (1 - blend) + pointB.x * blend, y: pointA.y * (1 - blend) + pointB.y * blend, z: pointA.z * (1 - blend) + pointB.z * blend }; }; const getCurrentForm = (u, v, t) => { const totalForms = forms.length; const cycleTime = 600; const position = (t % (cycleTime * totalForms)) / cycleTime; const formIndex = Math.floor(position); const nextFormIndex = (formIndex + 1) % totalForms; let rawBlend = position - formIndex; const pauseTime = 0; const transitionTime = 1 - (pauseTime * 2); let blend; if (rawBlend < pauseTime) { blend = 0; } else if (rawBlend > (1 - pauseTime)) { blend = 1; } else { const normalizedTime = (rawBlend - pauseTime) / transitionTime; blend = normalizedTime < 0.5 ? 4 * normalizedTime * normalizedTime * normalizedTime : 1 - Math.pow(-2 * normalizedTime + 2, 3) / 2; } return interpolateForms( forms[formIndex], forms[nextFormIndex], u, v, t, blend ); }; const animate = () => { ctx.clearRect(0, 0, width, height); ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, width, height); const rotateX = Math.sin(time * rotateSpeed) * 0.5; const rotateY = Math.cos(time * rotateSpeed * 0.7) * 0.3; const rotateZ = time * rotateSpeed * 0.1; for (let i = 0; i < numLines; i++) { const v = i / (numLines - 1); ctx.beginPath(); ctx.strokeStyle = `rgba(51, 51, 51, ${lineAlpha})`; ctx.lineWidth = lineWidth; let lastPointVisible = false; let lastPoint = null; for (let j = 0; j <= lineSegments; j++) { const u = j / lineSegments; const point = getCurrentForm(u, v, time); const rotatedX = point.x * Math.cos(rotateZ) - point.y * Math.sin(rotateZ); const rotatedY = point.x * Math.sin(rotateZ) + point.y * Math.cos(rotateZ); const rotatedZ = point.z; const scale = 1.5 + rotatedZ * 0.001; const projX = width / 2 + rotatedX * scale; const projY = height / 2 + rotatedY * scale; const pointVisible = rotatedZ > -50; if (j === 0) { if (pointVisible) { ctx.moveTo(projX, projY); lastPointVisible = true; lastPoint = { x: projX, y: projY }; } } else { if (pointVisible && lastPointVisible) { ctx.lineTo(projX, projY); } else if (pointVisible && !lastPointVisible) { ctx.moveTo(projX, projY); } } lastPointVisible = pointVisible; lastPoint = { x: projX, y: projY }; } ctx.stroke(); } for (let i = 0; i < numLines * 0.3; i++) { const u = i / (numLines * 0.3 - 1); ctx.beginPath(); ctx.strokeStyle = `rgba(51, 51, 51, ${lineAlpha * 0.7})`; ctx.lineWidth = lineWidth * 0.7; let lastPointVisible = false; let lastPoint = null; for (let j = 0; j <= lineSegments * 0.5; j++) { const v = j / (lineSegments * 0.5); const point = getCurrentForm(u, v, time); const rotatedX = point.x * Math.cos(rotateZ) - point.y * Math.sin(rotateZ); const rotatedY = point.x * Math.sin(rotateZ) + point.y * Math.cos(rotateZ); const rotatedZ = point.z; const scale = 1.5 + rotatedZ * 0.001; const projX = width / 2 + rotatedX * scale; const projY = height / 2 + rotatedY * scale; const pointVisible = rotatedZ > -50; if (j === 0) { if (pointVisible) { ctx.moveTo(projX, projY); lastPointVisible = true; lastPoint = { x: projX, y: projY }; } } else { if (pointVisible && lastPointVisible) { ctx.lineTo(projX, projY); } else if (pointVisible && !lastPointVisible) { ctx.moveTo(projX, projY); } } lastPointVisible = pointVisible; lastPoint = { x: projX, y: projY }; } ctx.stroke(); } time += 0.5; animationFrameRef.current = requestAnimationFrame(animate); }; animate(); return () => { if (animationFrameRef.current) { cancelAnimationFrame(animationFrameRef.current); animationFrameRef.current = null; } if (canvas && ctx) { ctx.clearRect(0, 0, width, height); } }; }, []); return ( <div className=\"flex items-center justify-center bg-[#F0EEE6] w-full h-full\"><canvas ref={canvasRef} width={550} height={550} /></div> ); }; export default Metamorphosis;",text:"The five colors blind the eye.\nThe five sounds deafen the ear.\nThe five flavors dull the taste.\n\nThoughts weaken the mind.\nCraving withers the heart.\n\nThe Vibe Coder attends to\nthe inner, not the outer.\nHe allows things to come and go.\nHis heart is open as the sky.",shouldDisplayMobileArtwork:!0,promptSuggestion:"make it breathe"},{number:13,staticArtwork:nt,sourceCode:"// @ts-nocheck\nimport React, { useEffect, useRef } from 'react';\n\n// themes: success equals failure, hope equals fear, finding balance in self\n// visualization: A double helix where opposing forces dance in perfect equilibrium\n\nconst Artwork33 = () => {\n  const canvasRef = useRef(null);\n  const animationFrameRef = useRef(null);\n\n  useEffect(() => {\n    const canvas = canvasRef.current;\n    if (!canvas) return;\n\n    const ctx = canvas.getContext('2d');\n    const width = canvas.width = 800;\n    const height = canvas.height = 600;\n\n    // Core variables\n    let time = 0;\n    const particles = [];\n    let helixPoints = [];\n    const numParticles = 60; // Fewer particles\n    const TWO_PI = Math.PI * 2;\n\n    // Helper functions\n    const random = (min, max) => {\n      if (max === undefined) {\n        max = min;\n        min = 0;\n      }\n      return Math.random() * (max - min) + min;\n    };\n\n    const map = (value, start1, stop1, start2, stop2) => {\n      return start2 + (stop2 - start2) * ((value - start1) / (stop1 - start1));\n    };\n\n    const dist = (x1, y1, z1, x2, y2, z2) => {\n      const dx = x2 - x1;\n      const dy = y2 - y1;\n      const dz = z2 - z1;\n      return Math.sqrt(dx * dx + dy * dy + dz * dz);\n    };\n\n    // HelixParticle - each point balanced between opposing forces\n    class HelixParticle {\n      constructor(initialPhase) {\n        this.phase = initialPhase || random(TWO_PI);\n        this.radius = random(90, 110);\n        this.yOffset = random(-300, 300);\n        this.ySpeed = random(0.3, 0.6) * (random() > 0.5 ? 1 : -1);\n        this.rotationSpeed = random(0.005, 0.0075);\n        this.size = random(3, 6); // Slightly larger points\n        this.opacity = random(120, 180);\n        this.strength = random(0.8, 1);\n      }\n\n      update() {\n        // Update position - success and failure are one movement\n        this.phase += this.rotationSpeed * this.strength;\n        this.yOffset += this.ySpeed;\n\n        // Reset position if it goes off screen\n        if (this.yOffset > 350) this.yOffset = -350;\n        if (this.yOffset < -350) this.yOffset = 350;\n\n        // Calculate 3D position\n        const x = width / 2 + Math.cos(this.phase) * this.radius;\n        const y = height / 2 + this.yOffset;\n        const z = Math.sin(this.phase) * this.radius;\n\n        // Store position for drawing and connections\n        return { x, y, z, strength: this.strength, size: this.size, opacity: this.opacity };\n      }\n    }\n\n    // Create helix particles - fewer points\n    for (let i = 0; i < numParticles; i++) {\n      const initialPhase = (i / numParticles) * TWO_PI * 3; // Create 3 full rotations\n      particles.push(new HelixParticle(initialPhase));\n    }\n\n    // Frame rate control variables\n    const targetFPS = 30;\n    const frameInterval = 1000 / targetFPS;\n    let lastFrameTime = 0;\n\n    const animate = (currentTime) => {\n      // Initialize lastFrameTime on first frame\n      if (!lastFrameTime) {\n        lastFrameTime = currentTime;\n        animationFrameRef.current = requestAnimationFrame(animate);\n        return;\n      }\n\n      const deltaTime = currentTime - lastFrameTime;\n      \n      // Only render a new frame when enough time has passed (frame rate limiting)\n      if (deltaTime >= frameInterval) {\n        // Calculate remainder to prevent drift\n        const remainder = deltaTime % frameInterval;\n        \n        // Update lastFrameTime with the time that's been processed\n        lastFrameTime = currentTime - remainder;\n        \n        // Clear background\n        ctx.fillStyle = '#F0EEE6';\n        ctx.fillRect(0, 0, width, height);\n\n        time += 0.02;\n\n        // Update helix points\n        helixPoints = particles.map(particle => particle.update());\n\n        // Find balance between foreground and background, like hope and fear\n        helixPoints.sort((a, b) => a.z - b.z);\n\n        // Draw stronger connections between helix points\n        ctx.lineWidth = 1.2; // Thicker lines\n\n        // Connect helix points to create a strand structure\n        for (let i = 0; i < helixPoints.length; i++) {\n          const hp1 = helixPoints[i];\n\n          // Connect to nearby points\n          for (let j = 0; j < helixPoints.length; j++) {\n            if (i !== j) {\n              const hp2 = helixPoints[j];\n              const d = dist(hp1.x, hp1.y, hp1.z, hp2.x, hp2.y, hp2.z);\n\n              // Create more connections with a larger distance threshold\n              if (d < 120) {\n                // Calculate opacity based on distance and z-position (depth)\n                const opacity = map(d, 0, 120, 40, 10) * \n                              map(Math.min(hp1.z, hp2.z), -110, 110, 0.3, 1);\n\n                ctx.strokeStyle = `rgba(20, 20, 20, ${opacity / 255})`;\n                ctx.beginPath();\n                ctx.moveTo(hp1.x, hp1.y);\n                ctx.lineTo(hp2.x, hp2.y);\n                ctx.stroke();\n              }\n            }\n          }\n        }\n\n        // Draw helix points with size based on z-position for 3D effect\n        for (let i = 0; i < helixPoints.length; i++) {\n          const hp = helixPoints[i];\n          // Calculate size and opacity based on z-position (depth)\n          const sizeMultiplier = map(hp.z, -110, 110, 0.6, 1.3);\n          const adjustedOpacity = map(hp.z, -110, 110, hp.opacity * 0.4, hp.opacity);\n\n          ctx.fillStyle = `rgba(10, 10, 10, ${adjustedOpacity / 255})`;\n          ctx.beginPath();\n          ctx.arc(hp.x, hp.y, (hp.size * sizeMultiplier) / 2, 0, TWO_PI);\n          ctx.fill();\n        }\n\n        // Create spinal connections - stronger central structure\n        ctx.strokeStyle = 'rgba(0, 0, 0, 0.118)'; // 30/255 ≈ 0.118\n        ctx.lineWidth = 2;\n\n        // Sort by y position for the spine\n        const sortedByY = [...helixPoints].sort((a, b) => a.y - b.y);\n\n        // Draw spine connecting points with similar y positions\n        for (let i = 0; i < sortedByY.length - 1; i++) {\n          const p1 = sortedByY[i];\n          const p2 = sortedByY[i + 1];\n\n          // Only connect if they're close in y position\n          if (Math.abs(p1.y - p2.y) < 30) {\n            ctx.beginPath();\n            ctx.moveTo(p1.x, p1.y);\n            ctx.lineTo(p2.x, p2.y);\n            ctx.stroke();\n          }\n        }\n      }\n\n      // Request next frame\n      animationFrameRef.current = requestAnimationFrame(animate);\n    };\n\n    // Start the animation immediately with proper frame timing\n    animationFrameRef.current = requestAnimationFrame(animate);\n\n    return () => {\n      if (animationFrameRef.current) {\n        cancelAnimationFrame(animationFrameRef.current);\n        animationFrameRef.current = null;\n      }\n      if (canvas && ctx) {\n        ctx.clearRect(0, 0, width, height);\n      }\n    };\n  }, []);\n\n  return (\n    <div className=\"w-full min-h-screen bg-[#F0EEE6] flex flex-col items-center justify-center\">\n      <div className=\"border-0 overflow-hidden\">\n        <canvas ref={canvasRef} width={800} height={600} />\n      </div>\n    </div>\n  );\n};\n\nexport default Artwork33;",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; const Artwork33 = () => { const canvasRef = useRef(null); const animationFrameRef = useRef(null); useEffect(() => { const canvas = canvasRef.current; if (!canvas) return; const ctx = canvas.getContext('2d'); const width = canvas.width = 800; const height = canvas.height = 600; let time = 0; const particles = []; let helixPoints = []; const numParticles = 60; const TWO_PI = Math.PI * 2; const random = (min, max) => { if (max === undefined) { max = min; min = 0; } return Math.random() * (max - min) + min; }; const map = (value, start1, stop1, start2, stop2) => { return start2 + (stop2 - start2) * ((value - start1) / (stop1 - start1)); }; const dist = (x1, y1, z1, x2, y2, z2) => { const dx = x2 - x1; const dy = y2 - y1; const dz = z2 - z1; return Math.sqrt(dx * dx + dy * dy + dz * dz); }; class HelixParticle { constructor(initialPhase) { this.phase = initialPhase || random(TWO_PI); this.radius = random(90, 110); this.yOffset = random(-300, 300); this.ySpeed = random(0.3, 0.6) * (random() > 0.5 ? 1 : -1); this.rotationSpeed = random(0.005, 0.0075); this.size = random(3, 6); this.opacity = random(120, 180); this.strength = random(0.8, 1); } update() { this.phase += this.rotationSpeed * this.strength; this.yOffset += this.ySpeed; if (this.yOffset > 350) this.yOffset = -350; if (this.yOffset < -350) this.yOffset = 350; const x = width / 2 + Math.cos(this.phase) * this.radius; const y = height / 2 + this.yOffset; const z = Math.sin(this.phase) * this.radius; return { x, y, z, strength: this.strength, size: this.size, opacity: this.opacity }; } } for (let i = 0; i < numParticles; i++) { const initialPhase = (i / numParticles) * TWO_PI * 3; particles.push(new HelixParticle(initialPhase)); } const targetFPS = 30; const frameInterval = 1000 / targetFPS; let lastFrameTime = 0; const animate = (currentTime) => { if (!lastFrameTime) { lastFrameTime = currentTime; animationFrameRef.current = requestAnimationFrame(animate); return; } const deltaTime = currentTime - lastFrameTime; if (deltaTime >= frameInterval) { const remainder = deltaTime % frameInterval; lastFrameTime = currentTime - remainder; ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, width, height); time += 0.02; helixPoints = particles.map(particle => particle.update()); helixPoints.sort((a, b) => a.z - b.z); ctx.lineWidth = 1.2; for (let i = 0; i < helixPoints.length; i++) { const hp1 = helixPoints[i]; for (let j = 0; j < helixPoints.length; j++) { if (i !== j) { const hp2 = helixPoints[j]; const d = dist(hp1.x, hp1.y, hp1.z, hp2.x, hp2.y, hp2.z); if (d < 120) { const opacity = map(d, 0, 120, 40, 10) * map(Math.min(hp1.z, hp2.z), -110, 110, 0.3, 1); ctx.strokeStyle = `rgba(20, 20, 20, ${opacity / 255})`; ctx.beginPath(); ctx.moveTo(hp1.x, hp1.y); ctx.lineTo(hp2.x, hp2.y); ctx.stroke(); } } } } for (let i = 0; i < helixPoints.length; i++) { const hp = helixPoints[i]; const sizeMultiplier = map(hp.z, -110, 110, 0.6, 1.3); const adjustedOpacity = map(hp.z, -110, 110, hp.opacity * 0.4, hp.opacity); ctx.fillStyle = `rgba(10, 10, 10, ${adjustedOpacity / 255})`; ctx.beginPath(); ctx.arc(hp.x, hp.y, (hp.size * sizeMultiplier) / 2, 0, TWO_PI); ctx.fill(); } ctx.strokeStyle = 'rgba(0, 0, 0, 0.118)'; ctx.lineWidth = 2; const sortedByY = [...helixPoints].sort((a, b) => a.y - b.y); for (let i = 0; i < sortedByY.length - 1; i++) { const p1 = sortedByY[i]; const p2 = sortedByY[i + 1]; if (Math.abs(p1.y - p2.y) < 30) { ctx.beginPath(); ctx.moveTo(p1.x, p1.y); ctx.lineTo(p2.x, p2.y); ctx.stroke(); } } } animationFrameRef.current = requestAnimationFrame(animate); }; animationFrameRef.current = requestAnimationFrame(animate); return () => { if (animationFrameRef.current) { cancelAnimationFrame(animationFrameRef.current); animationFrameRef.current = null; } if (canvas && ctx) { ctx.clearRect(0, 0, width, height); } }; }, []); return ( <div className=\"w-full min-h-screen bg-[#F0EEE6] flex flex-col items-center justify-center\"><div className=\"border-0 overflow-hidden\"><canvas ref={canvasRef} width={800} height={600} /></div></div> ); }; export default Artwork33;",text:"Success is as dangerous as failure.\nHope is as hollow as fear.\n\nWhat does it mean,\nsuccess is as dangerous as failure.\nWhether you go up or down the ladder\nyour position is unsteady.\nRest both feet on the ground\nto find balance and stability.\n\nWhat does it mean,\nhope is as hollow as fear.\nHope and fear are both illusory.\nEach arise in consideration of the body.\nWhen we don’t see Self as the body\nwe have nothing to fear.\n\nSee the universe as Self.\nHold faith in the way things are.\nRevere all under heaven, and you too,\ncan remain present for everything.",shouldDisplayMobileArtwork:!0,promptSuggestion:"make it more balanced"},{number:14,staticArtwork:Dn,sourceCode:"// @ts-nocheck\nimport { useRef, useEffect } from 'react';\n\n// themes: the formless and intangible, merging into oneness, return to nothingness\n// visualization: Waves of varying opacity merge and dissolve, revealing the formless nature beneath form\n\nconst LayeredSineWaves = () => {\n  const canvasRef = useRef(null);\n  \n  useEffect(() => {\n    const canvas = canvasRef.current;\n    const ctx = canvas.getContext('2d');\n    const width = canvas.width;\n    const height = canvas.height;\n    \n    // Parameters\n    const layers = 80;\n    const points = 200;\n    const waveAmplitude = 40;\n    let time = 0;\n    let animationId = null;\n    \n    function draw() {\n      ctx.clearRect(0, 0, width, height);\n      ctx.fillStyle = '#F0EEE6';\n      ctx.fillRect(0, 0, width, height);\n      \n      time += 0.01;\n      \n      // Draw each layer - forms emerging from the formless\n      for (let layer = 0; layer < layers; layer++) {\n        const layerPosition = (layer / layers) * height * 0.8 + height * 0.1;\n        const layerFrequency = 0.5 + layer * 0.03;\n        const layerPhase = time * 0.2 + layer * 0.05;\n        const layerAmplitude = waveAmplitude * (0.5 + 0.5 * Math.sin(layer * 0.1 + time * 0.3));\n        \n        // Set opacity based on layer position and time\n        const baseOpacity = 0.2 + 0.6 * Math.pow(Math.sin((layer / layers) * Math.PI), 2);\n        const timeEffect = 0.2 * Math.sin(time * 0.4 + layer * 0.1);\n        const opacity = Math.min(0.9, Math.max(0.1, baseOpacity + timeEffect));\n        \n        ctx.beginPath();\n        // Use grayscale instead of red\n        ctx.strokeStyle = `rgba(50, 50, 50, ${opacity})`;\n        ctx.lineWidth = 0.6;\n        \n        // Generate points along the wave\n        for (let i = 0; i <= points; i++) {\n          const x = (i / points) * width;\n          \n          // Create complex wave with multiple sine components\n          let y = layerPosition;\n          \n          // Primary wave\n          y += layerAmplitude * Math.sin(x * 0.01 * layerFrequency + layerPhase);\n          \n          // Secondary waves for complexity\n          y += layerAmplitude * 0.3 * Math.sin(x * 0.02 * layerFrequency + layerPhase * 1.5);\n          y += layerAmplitude * 0.2 * Math.sin(x * 0.04 * layerFrequency - layerPhase * 0.7);\n          \n          // Tertiary high-frequency detail\n          y += layerAmplitude * 0.1 * Math.sin(x * 0.08 * layerFrequency + layerPhase * 2.3);\n          \n          if (i === 0) {\n            ctx.moveTo(x, y);\n          } else {\n            ctx.lineTo(x, y);\n          }\n        }\n        \n        ctx.stroke();\n      }\n      \n      // Draw connecting lines - all merging into oneness\n      for (let i = 0; i < width; i += 20) {\n        if (Math.random() < 0.4) { // Only draw some vertical lines\n          ctx.beginPath();\n          \n          const opacity = 0.1 + 0.2 * Math.sin(i * 0.05 + time);\n          // Use grayscale instead of red\n          ctx.strokeStyle = `rgba(50, 50, 50, ${opacity})`;\n          ctx.lineWidth = 0.3;\n          \n          // Vary the line height\n          const startY = height * 0.1 + Math.random() * height * 0.2;\n          const endY = height * 0.7 + Math.random() * height * 0.2;\n          \n          ctx.moveTo(i, startY);\n          ctx.lineTo(i, endY);\n          ctx.stroke();\n        }\n      }\n      \n      animationId = requestAnimationFrame(draw);\n    }\n    \n    // Animation loop\n    animationId = requestAnimationFrame(draw);\n    \n    return () => {\n      if (animationId) {\n        cancelAnimationFrame(animationId);\n      }\n    };\n  }, []);\n  \n  return (\n    <div className=\"flex justify-center items-center h-full w-full bg-[#F0EEE6]\">\n      <canvas \n        ref={canvasRef} \n        width={550} \n        height={550} \n        className=\"shadow-lg\"\n      />\n    </div>\n  );\n};\n\nexport default LayeredSineWaves;",sourceCodeMin:"import { useRef, useEffect } from 'react'; const LayeredSineWaves = () => { const canvasRef = useRef(null); useEffect(() => { const canvas = canvasRef.current; const ctx = canvas.getContext('2d'); const width = canvas.width; const height = canvas.height; const layers = 80; const points = 200; const waveAmplitude = 40; let time = 0; let animationId = null; function draw() { ctx.clearRect(0, 0, width, height); ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, width, height); time += 0.01; for (let layer = 0; layer < layers; layer++) { const layerPosition = (layer / layers) * height * 0.8 + height * 0.1; const layerFrequency = 0.5 + layer * 0.03; const layerPhase = time * 0.2 + layer * 0.05; const layerAmplitude = waveAmplitude * (0.5 + 0.5 * Math.sin(layer * 0.1 + time * 0.3)); const baseOpacity = 0.2 + 0.6 * Math.pow(Math.sin((layer / layers) * Math.PI), 2); const timeEffect = 0.2 * Math.sin(time * 0.4 + layer * 0.1); const opacity = Math.min(0.9, Math.max(0.1, baseOpacity + timeEffect)); ctx.beginPath(); ctx.strokeStyle = `rgba(50, 50, 50, ${opacity})`; ctx.lineWidth = 0.6; for (let i = 0; i <= points; i++) { const x = (i / points) * width; let y = layerPosition; y += layerAmplitude * Math.sin(x * 0.01 * layerFrequency + layerPhase); y += layerAmplitude * 0.3 * Math.sin(x * 0.02 * layerFrequency + layerPhase * 1.5); y += layerAmplitude * 0.2 * Math.sin(x * 0.04 * layerFrequency - layerPhase * 0.7); y += layerAmplitude * 0.1 * Math.sin(x * 0.08 * layerFrequency + layerPhase * 2.3); if (i === 0) { ctx.moveTo(x, y); } else { ctx.lineTo(x, y); } } ctx.stroke(); } for (let i = 0; i < width; i += 20) { if (Math.random() < 0.4) { ctx.beginPath(); const opacity = 0.1 + 0.2 * Math.sin(i * 0.05 + time); ctx.strokeStyle = `rgba(50, 50, 50, ${opacity})`; ctx.lineWidth = 0.3; const startY = height * 0.1 + Math.random() * height * 0.2; const endY = height * 0.7 + Math.random() * height * 0.2; ctx.moveTo(i, startY); ctx.lineTo(i, endY); ctx.stroke(); } } animationId = requestAnimationFrame(draw); } animationId = requestAnimationFrame(draw); return () => { if (animationId) { cancelAnimationFrame(animationId); } }; }, []); return ( <div className=\"flex justify-center items-center h-full w-full bg-[#F0EEE6]\"><canvas ref={canvasRef} width={550} height={550} className=\"shadow-lg\" /></div> ); }; export default LayeredSineWaves;",text:"Look, and it can’t be seen— it is formless.\nListen, and it can’t be heard— it is silent.\nGrasp, and it can’t be held— it is intangible.\n\nThese three qualities cannot be unraveled\nwith logic.\nTogether, they merge into the Universal One.\n\nRevealed, it is not bright.\nHidden, it is not dark.\nEndless and unnameable,\nit returns to nothingness.\nThe form of the formless.\nThe image of the imageless.\nElusive, it is beyond comprehension.\nApproach it, and there is no beginning.\nFollow it, and there is no end.\n\nHold fast to Source and move gracefully\ninto the present moment.\nKnowing this grants access\nto the essence of wisdom.",shouldDisplayMobileArtwork:!0,promptSuggestion:"make the folds larger"},{number:15,staticArtwork:O,sourceCode:"// @ts-nocheck\nimport React, { useEffect, useRef } from 'react';\n\n// themes: profound watchfulness, remaining still, presence in the moment\n// visualization: Waves ripple outward from still points, creating patterns of watchful awareness\n\nconst WaveInterferenceV6 = () => {\n  const canvasRef = useRef(null);\n\n  useEffect(() => {\n    const canvas = canvasRef.current;\n    const ctx = canvas.getContext('2d');\n    const width = 550;\n    const height = 550;\n    canvas.width = width;\n    canvas.height = height;\n\n    // Higher resolution for finer detail\n    const resolution = 2;\n    const gridWidth = Math.floor(width / resolution);\n    const gridHeight = Math.floor(height / resolution);\n\n    // Define still points of watchful awareness\n    const sources = [\n      { x: width/2, y: height/2 },\n      { x: width/4, y: height/4 },\n      { x: 3*width/4, y: height/4 },\n      { x: width/4, y: 3*height/4 },\n      { x: 3*width/4, y: 3*height/4 },\n    ];\n\n    const wavelength = 35; // Longer wavelength for gentler waves\n    let time = 0;\n    \n    // Pre-create canvas for offscreen rendering\n    const offscreenCanvas = document.createElement('canvas');\n    offscreenCanvas.width = gridWidth;\n    offscreenCanvas.height = gridHeight;\n    const offscreenCtx = offscreenCanvas.getContext('2d');\n    \n    // Pre-calculate constants\n    const twoPI = Math.PI * 2;\n    const waveConstant = twoPI / wavelength;\n\n    let animationFrameId = null;\n    \n    const animate = () => {\n      // Clear with background color\n      ctx.fillStyle = '#F0EEE6';\n      ctx.fillRect(0, 0, width, height);\n\n      // Work on lower resolution grid\n      const imageData = offscreenCtx.createImageData(gridWidth, gridHeight);\n      const data = imageData.data;\n\n      // Calculate ripples of presence emanating from stillness\n      for (let y = 0; y < gridHeight; y++) {\n        for (let x = 0; x < gridWidth; x++) {\n          const pixelX = x * resolution + resolution/2;\n          const pixelY = y * resolution + resolution/2;\n          let amplitude = 0;\n\n          // Unrolled loop for better performance\n          for (let i = 0; i < sources.length; i++) {\n            const dx = pixelX - sources[i].x;\n            const dy = pixelY - sources[i].y;\n            const distance = Math.sqrt(dx * dx + dy * dy);\n            amplitude += Math.sin(distance * waveConstant - time * twoPI);\n          }\n\n          // More subtle threshold with gradient\n          const index = (y * gridWidth + x) * 4;\n          const threshold = Math.abs(amplitude);\n          if (threshold < 0.6) {\n            // Lighter gray lines with variable opacity\n            const opacity = (0.6 - threshold) * 0.4 * 255; // More transparent\n            data[index] = 160;     // Lighter gray\n            data[index + 1] = 160;\n            data[index + 2] = 160;\n            data[index + 3] = opacity;\n          } else {\n            // Background color\n            data[index] = 240;\n            data[index + 1] = 238;\n            data[index + 2] = 230;\n            data[index + 3] = 255;\n          }\n        }\n      }\n\n      // Draw to offscreen canvas\n      offscreenCtx.putImageData(imageData, 0, 0);\n      \n      // Scale up to main canvas\n      ctx.imageSmoothingEnabled = false;\n      ctx.drawImage(offscreenCanvas, 0, 0, width, height);\n\n      time += 0.0008; // Even slower for more graceful movement\n      animationFrameId = requestAnimationFrame(animate);\n    };\n\n    animationFrameId = requestAnimationFrame(animate);\n\n    return () => {\n      if (animationFrameId) {\n        cancelAnimationFrame(animationFrameId);\n      }\n      if (offscreenCanvas) {\n        offscreenCanvas.width = 0;\n        offscreenCanvas.height = 0;\n      }\n    };\n  }, []);\n\n  return (\n    <div style={{ \n      display: 'flex', \n      justifyContent: 'center', \n      alignItems: 'center',\n      width: '100vw', \n      height: '100vh', \n      backgroundColor: '#F0EEE6' \n    }}>\n      <canvas \n        ref={canvasRef} \n        style={{ \n          width: '550px',\n          height: '550px'\n        }} \n      />\n    </div>\n  );\n};\n\nexport default WaveInterferenceV6;",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; const WaveInterferenceV6 = () => { const canvasRef = useRef(null); useEffect(() => { const canvas = canvasRef.current; const ctx = canvas.getContext('2d'); const width = 550; const height = 550; canvas.width = width; canvas.height = height; const resolution = 2; const gridWidth = Math.floor(width / resolution); const gridHeight = Math.floor(height / resolution); const sources = [ { x: width/2, y: height/2 }, { x: width/4, y: height/4 }, { x: 3*width/4, y: height/4 }, { x: width/4, y: 3*height/4 }, { x: 3*width/4, y: 3*height/4 }, ]; const wavelength = 35; let time = 0; const offscreenCanvas = document.createElement('canvas'); offscreenCanvas.width = gridWidth; offscreenCanvas.height = gridHeight; const offscreenCtx = offscreenCanvas.getContext('2d'); const twoPI = Math.PI * 2; const waveConstant = twoPI / wavelength; let animationFrameId = null; const animate = () => { ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, width, height); const imageData = offscreenCtx.createImageData(gridWidth, gridHeight); const data = imageData.data; for (let y = 0; y < gridHeight; y++) { for (let x = 0; x < gridWidth; x++) { const pixelX = x * resolution + resolution/2; const pixelY = y * resolution + resolution/2; let amplitude = 0; for (let i = 0; i < sources.length; i++) { const dx = pixelX - sources[i].x; const dy = pixelY - sources[i].y; const distance = Math.sqrt(dx * dx + dy * dy); amplitude += Math.sin(distance * waveConstant - time * twoPI); } const index = (y * gridWidth + x) * 4; const threshold = Math.abs(amplitude); if (threshold < 0.6) { const opacity = (0.6 - threshold) * 0.4 * 255; data[index] = 160; data[index + 1] = 160; data[index + 2] = 160; data[index + 3] = opacity; } else { data[index] = 240; data[index + 1] = 238; data[index + 2] = 230; data[index + 3] = 255; } } } offscreenCtx.putImageData(imageData, 0, 0); ctx.imageSmoothingEnabled = false; ctx.drawImage(offscreenCanvas, 0, 0, width, height); time += 0.0008; animationFrameId = requestAnimationFrame(animate); }; animationFrameId = requestAnimationFrame(animate); return () => { if (animationFrameId) { cancelAnimationFrame(animationFrameId); } if (offscreenCanvas) { offscreenCanvas.width = 0; offscreenCanvas.height = 0; } }; }, []); return ( <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', width: '100vw', height: '100vh', backgroundColor: '#F0EEE6' }}><canvas ref={canvasRef} style={{ width: '550px', height: '550px' }} /></div> ); }; export default WaveInterferenceV6;",text:"Ancient engineers were profound,\nsubtle and at one with the mysterious forces.\nThe depth of their being is not knowable.\n\nTherefore,\nall we can do is describe their appearance:\nWatchful, as though crossing a frozen stream.\nAlert, as a soldier in enemy territory.\nRespectful, as a thoughtful guest.\nMutable, as candle wax in blazing sun.\nSimple, as a blank screen.\nHollow, as a yawning cave.\nOpaque, as a muddy pond.\n\nWho can wait quietly while the mud settles.\nWho can remain still|until the moment of action.\nThe Vibe Coder doesn’t seek fulfillment.\nNot seeking, not expecting, he is present\nfor all that appears.",shouldDisplayMobileArtwork:!0,promptSuggestion:"make it move with a strong breeze"},{number:16,staticArtwork:Yn,sourceCode:"// @ts-nocheck\nimport { useRef, useEffect } from 'react';\n\n// themes: emptying of everything, return to source, peace through stillness\n// visualization: Organic forms continuously empty and return to their source in peaceful cycles\n\nconst MorphingContours = () => {\n  const canvasRef = useRef(null);\n  \n  useEffect(() => {\n    const canvas = canvasRef.current;\n    const ctx = canvas.getContext('2d');\n    const width = canvas.width;\n    const height = canvas.height;\n    \n    // Parameters\n    const numShapes = 3;\n    const contoursPerShape = 25;\n    const points = 100;\n    let time = 0;\n    \n    // Scale factor - 50% larger\n    const scaleFactor = 1.5;\n    \n    // Project colors\n    const backgroundColor = '#F0EEE6'; // Cream background\n    const lineColor = 'rgba(50, 50, 50, 0.4)'; // Dark gray with transparency\n    \n    let animationId = null;\n    \n    function draw() {\n      // Clear canvas with cream background\n      ctx.fillStyle = backgroundColor;\n      ctx.fillRect(0, 0, width, height);\n      \n      // Increment time (slowed down)\n      time += 0.001;\n      \n      // Central point\n      const centerX = width / 2;\n      const centerY = height / 2;\n      \n      // Draw each form as it empties and returns to source\n      for (let shapeIndex = 0; shapeIndex < numShapes; shapeIndex++) {\n        // Each shape has its own phase and movement\n        const shapePhase = time + shapeIndex * Math.PI * 2 / numShapes;\n        \n        // Shape center offsets (moves shapes relative to each other, slowed down)\n        const offsetX = Math.sin(shapePhase * 0.2) * 40 * scaleFactor;\n        const offsetY = Math.cos(shapePhase * 0.3) * 40 * scaleFactor;\n        \n        // Draw contour lines for this shape\n        for (let contour = 0; contour < contoursPerShape; contour++) {\n          // Scale each contour (smaller to larger) - now 50% larger\n          const scale = (30 + contour * 3) * scaleFactor;\n          \n          // Slight offset for each contour to create dimensional effect\n          const contourOffsetX = Math.sin(contour * 0.2 + shapePhase) * 10 * scaleFactor;\n          const contourOffsetY = Math.cos(contour * 0.2 + shapePhase) * 10 * scaleFactor;\n          \n          // Begin drawing the contour\n          ctx.beginPath();\n          \n          // Set line style - dark gray with transparency\n          ctx.strokeStyle = lineColor;\n          ctx.lineWidth = 0.8;\n          \n          // Generate points - finding peace through continuous motion\n          for (let i = 0; i <= points; i++) {\n            const angle = (i / points) * Math.PI * 2;\n            \n            // Base radius with noise - now 50% larger\n            let radius = scale;\n            \n            // Add complexity with multiple sine waves at different frequencies\n            radius += 15 * Math.sin(angle * 3 + shapePhase * 2) * scaleFactor;\n            radius += 10 * Math.cos(angle * 5 - shapePhase) * scaleFactor;\n            radius += 5 * Math.sin(angle * 8 + contour * 0.1) * scaleFactor;\n            \n            // Calculate point position\n            const x = centerX + offsetX + contourOffsetX + Math.cos(angle) * radius;\n            const y = centerY + offsetY + contourOffsetY + Math.sin(angle) * radius;\n            \n            if (i === 0) {\n              ctx.moveTo(x, y);\n            } else {\n              ctx.lineTo(x, y);\n            }\n          }\n          \n          // Close the path and stroke\n          ctx.closePath();\n          ctx.stroke();\n        }\n      }\n      \n      animationId = requestAnimationFrame(draw);\n    }\n    \n    // Animation loop\n    animationId = requestAnimationFrame(draw);\n    \n    return () => {\n      if (animationId) {\n        cancelAnimationFrame(animationId);\n      }\n    };\n  }, []);\n  \n  return (\n    <div className=\"flex justify-center items-center h-full w-full bg-[#F0EEE6]\">\n      <canvas \n        ref={canvasRef} \n        width={550} \n        height={550} \n        className=\"shadow-lg\"\n      />\n    </div>\n  );\n};\n\nexport default MorphingContours;",sourceCodeMin:"import { useRef, useEffect } from 'react'; const MorphingContours = () => { const canvasRef = useRef(null); useEffect(() => { const canvas = canvasRef.current; const ctx = canvas.getContext('2d'); const width = canvas.width; const height = canvas.height; const numShapes = 3; const contoursPerShape = 25; const points = 100; let time = 0; const scaleFactor = 1.5; const backgroundColor = '#F0EEE6'; const lineColor = 'rgba(50, 50, 50, 0.4)'; let animationId = null; function draw() { ctx.fillStyle = backgroundColor; ctx.fillRect(0, 0, width, height); time += 0.001; const centerX = width / 2; const centerY = height / 2; for (let shapeIndex = 0; shapeIndex < numShapes; shapeIndex++) { const shapePhase = time + shapeIndex * Math.PI * 2 / numShapes; const offsetX = Math.sin(shapePhase * 0.2) * 40 * scaleFactor; const offsetY = Math.cos(shapePhase * 0.3) * 40 * scaleFactor; for (let contour = 0; contour < contoursPerShape; contour++) { const scale = (30 + contour * 3) * scaleFactor; const contourOffsetX = Math.sin(contour * 0.2 + shapePhase) * 10 * scaleFactor; const contourOffsetY = Math.cos(contour * 0.2 + shapePhase) * 10 * scaleFactor; ctx.beginPath(); ctx.strokeStyle = lineColor; ctx.lineWidth = 0.8; for (let i = 0; i <= points; i++) { const angle = (i / points) * Math.PI * 2; let radius = scale; radius += 15 * Math.sin(angle * 3 + shapePhase * 2) * scaleFactor; radius += 10 * Math.cos(angle * 5 - shapePhase) * scaleFactor; radius += 5 * Math.sin(angle * 8 + contour * 0.1) * scaleFactor; const x = centerX + offsetX + contourOffsetX + Math.cos(angle) * radius; const y = centerY + offsetY + contourOffsetY + Math.sin(angle) * radius; if (i === 0) { ctx.moveTo(x, y); } else { ctx.lineTo(x, y); } } ctx.closePath(); ctx.stroke(); } } animationId = requestAnimationFrame(draw); } animationId = requestAnimationFrame(draw); return () => { if (animationId) { cancelAnimationFrame(animationId); } }; }, []); return ( <div className=\"flex justify-center items-center h-full w-full bg-[#F0EEE6]\"><canvas ref={canvasRef} width={550} height={550} className=\"shadow-lg\" /></div> ); }; export default MorphingContours;",text:"Empty yourself of everything.\nLet the heart become still.\nThe ten thousand things rise and fall\nwhile the Self merely observes.\nThey grow and flourish through the seasons\nthen return to Source.\n\nReturning to Source is stillness.\nStillness is the way of nature.\nThe way of nature is unchanging.\nUnchanging, we call it peace.\n\nKnowing the eternal is insight.\nNot knowing the eternal is ignorance.\nKnowing the eternal, the mind is open.\nThe open mind allows the open heart.\nThe open heart allows connection\nto the Gracious Source.\nThe Gracious Source embodies\nNature’s Way within.\nNature’s Way within is in harmony\nwith the cycle of life.\nThe cycle of life spins on to infinity.\nThe body falls away, but there is no death.\nFor the end is just the beginning.",shouldDisplayMobileArtwork:!0,promptSuggestion:"what is the pointillist version?"},{number:17,staticArtwork:F,sourceCode:"// @ts-nocheck\nimport React, { useEffect, useRef } from 'react';\n\n// themes: silent leadership, trust in the team, working unseen\n// visualization: Each segment silently guides the others, creating harmony through invisible influence\n\nconst KaleidoscopeVariation3 = () => {\n  const canvasRef = useRef(null);\n  const animationFrameId = useRef<number>(null);\n\n  useEffect(() => {\n    const canvas = canvasRef.current;\n    const ctx = canvas.getContext('2d');\n    // Set higher DPI for sharper rendering\n    const dpr = window.devicePixelRatio || 1;\n    const width = 550;\n    const height = 550;\n    canvas.width = width * dpr;\n    canvas.height = height * dpr;\n    canvas.style.width = `${width}px`;\n    canvas.style.height = `${height}px`;\n    ctx.scale(dpr, dpr);\n    \n    let time = 0;\n    const centerX = width / 2;\n    const centerY = height / 2;\n    \n    // Create an off-screen canvas for the segment\n    const segmentCanvas = document.createElement('canvas');\n    const segmentCtx = segmentCanvas.getContext('2d');\n    segmentCanvas.width = canvas.width;\n    segmentCanvas.height = canvas.height;\n    \n    const animate = () => {\n      time += 0.005;\n      \n      // Clear the main canvas\n      ctx.fillStyle = '#F0EEE6';\n      ctx.fillRect(0, 0, canvas.width, canvas.height);\n      \n      // Clear the segment canvas\n      segmentCtx.clearRect(0, 0, segmentCanvas.width, segmentCanvas.height);\n      \n      // Create pattern - silent influence rippling outward\n      const resolution = 1;\n      for (let x = 0; x < centerX + 250; x += resolution) {\n        for (let y = 0; y < centerY + 250; y += resolution) {\n          // Convert to polar coordinates\n          const dx = x - centerX;\n          const dy = y - centerY;\n          const r = Math.sqrt(dx * dx + dy * dy);\n          const theta = Math.atan2(dy, dx);\n          \n          // Only draw in the 45-degree segment with rounded corners\n          if (theta >= 0 && theta <= Math.PI / 4 && r < 250) {\n            // Add rounded corners by fading out near the edges\n            const cornerRadius = 20;\n            const edgeDistance = Math.min(\n              250 - r, // Distance from outer edge\n              r * Math.abs(Math.PI/4 - theta) * 2.5 // Distance from angle edges\n            );\n            const edgeFade = Math.min(1, edgeDistance / cornerRadius);\n\n            // Multiple wave sources\n            let wave1 = Math.sin(r * 0.1 - time * 2);\n            let wave2 = Math.cos(theta * 8 + time);\n            let wave3 = Math.sin((r - theta * 100) * 0.05 + time * 3);\n            \n            // Combine waves\n            let value = (wave1 + wave2 + wave3) / 3;\n            \n            // Add some noise for texture\n            value += (Math.random() - 0.5) * 0.2;\n            \n            // Map to opacity, incorporating edge fade\n            const opacity = Math.abs(value) * 0.8 * edgeFade;\n            \n            segmentCtx.fillStyle = `rgba(0, 0, 0, ${opacity})`;\n            segmentCtx.fillRect(x, y, resolution, resolution);\n          }\n        }\n      }\n      \n      // Create kaleidoscope - each segment trusting and following the others\n      const numSegments = 8;\n      for (let i = 0; i < numSegments; i++) {\n        ctx.save();\n        ctx.translate(centerX, centerY);\n        ctx.rotate((i * Math.PI * 2) / numSegments);\n        \n        if (i % 2 === 1) {\n          ctx.scale(1, -1);\n        }\n        \n        ctx.translate(-centerX, -centerY);\n        ctx.drawImage(segmentCanvas, 0, 0);\n        ctx.restore();\n      }\n      \n      animationFrameId.current = requestAnimationFrame(animate);\n    };\n    \n    animate();\n    \n    return () => {\n      if (animationFrameId.current) {\n        cancelAnimationFrame(animationFrameId.current);\n      }\n      if (segmentCanvas) {\n        segmentCanvas.width = 0;\n        segmentCanvas.height = 0;\n      }\n    };\n  }, []);\n\n  return (\n    <div style={{ \n      margin: 0,\n      background: '#F0EEE6',\n      overflow: 'hidden',\n      display: 'flex',\n      alignItems: 'center',\n      justifyContent: 'center',\n      height: '100%'\n    }}>\n      <div style={{\n        padding: '30px',\n        position: 'relative',\n        width: '100%',\n        height: '100%',\n        display: 'flex',\n        alignItems: 'center',\n        justifyContent: 'center'\n      }}>\n        <canvas ref={canvasRef} style={{ backgroundColor: '#F0EEE6', borderRadius: '8px' }} />\n      </div>\n    </div>\n  );\n};\n\nexport default KaleidoscopeVariation3;\n",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; const KaleidoscopeVariation3 = () => { const canvasRef = useRef(null); const animationFrameId = useRef<number>(null); useEffect(() => { const canvas = canvasRef.current; const ctx = canvas.getContext('2d'); const dpr = window.devicePixelRatio || 1; const width = 550; const height = 550; canvas.width = width * dpr; canvas.height = height * dpr; canvas.style.width = `${width}px`; canvas.style.height = `${height}px`; ctx.scale(dpr, dpr); let time = 0; const centerX = width / 2; const centerY = height / 2; const segmentCanvas = document.createElement('canvas'); const segmentCtx = segmentCanvas.getContext('2d'); segmentCanvas.width = canvas.width; segmentCanvas.height = canvas.height; const animate = () => { time += 0.005; ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, canvas.width, canvas.height); segmentCtx.clearRect(0, 0, segmentCanvas.width, segmentCanvas.height); const resolution = 1; for (let x = 0; x < centerX + 250; x += resolution) { for (let y = 0; y < centerY + 250; y += resolution) { const dx = x - centerX; const dy = y - centerY; const r = Math.sqrt(dx * dx + dy * dy); const theta = Math.atan2(dy, dx); if (theta >= 0 && theta <= Math.PI / 4 && r < 250) { const cornerRadius = 20; const edgeDistance = Math.min( 250 - r, r * Math.abs(Math.PI/4 - theta) * 2.5 ); const edgeFade = Math.min(1, edgeDistance / cornerRadius); let wave1 = Math.sin(r * 0.1 - time * 2); let wave2 = Math.cos(theta * 8 + time); let wave3 = Math.sin((r - theta * 100) * 0.05 + time * 3); let value = (wave1 + wave2 + wave3) / 3; value += (Math.random() - 0.5) * 0.2; const opacity = Math.abs(value) * 0.8 * edgeFade; segmentCtx.fillStyle = `rgba(0, 0, 0, ${opacity})`; segmentCtx.fillRect(x, y, resolution, resolution); } } } const numSegments = 8; for (let i = 0; i < numSegments; i++) { ctx.save(); ctx.translate(centerX, centerY); ctx.rotate((i * Math.PI * 2) / numSegments); if (i % 2 === 1) { ctx.scale(1, -1); } ctx.translate(-centerX, -centerY); ctx.drawImage(segmentCanvas, 0, 0); ctx.restore(); } animationFrameId.current = requestAnimationFrame(animate); }; animate(); return () => { if (animationFrameId.current) { cancelAnimationFrame(animationFrameId.current); } if (segmentCanvas) { segmentCanvas.width = 0; segmentCanvas.height = 0; } }; }, []); return ( <div style={{ margin: 0, background: '#F0EEE6', overflow: 'hidden', display: 'flex', alignItems: 'center', justifyContent: 'center', height: '100%' }}><div style={{ padding: '30px', position: 'relative', width: '100%', height: '100%', display: 'flex', alignItems: 'center', justifyContent: 'center' }}><canvas ref={canvasRef} style={{ backgroundColor: '#F0EEE6', borderRadius: '8px' }} /></div></div> ); }; export default KaleidoscopeVariation3;",text:"The best leader is one\nwhose existence is barely known.\nNext comes one who is loved and praised.\nNext comes one who is feared.\nThe worst is one who is despised.\n\nIf you don’t trust the team enough,\nthey become untrustworthy.\n\nThe Vibe Coder works in silence.\nWhen the work is accomplished,\nthe team says,\n“Amazing. We did it all ourselves.”",shouldDisplayMobileArtwork:!0,promptSuggestion:"make it feel like stop motion"},{number:18,staticArtwork:Pe,sourceCode:"// @ts-nocheck\nimport React, { useEffect, useRef, useMemo } from 'react'\nimport * as THREE from 'three'\n\n// themes: abandonment of the way, rise of rigid rules, loss of intuition\n// visualization: Geometric structures that reveal how natural flow becomes constrained by rigid patterns\n\nconst metadata = {\n  themes: \"abandonment of the way, rise of rigid rules, loss of intuition\",\n  visualization: \"Geometric structures that reveal how natural flow becomes constrained by rigid patterns\",\n  promptSuggestion: \"1. Enhance recursive depth\\n2. Add more structural layers\\n3. Develop clearer pattern progression\\n4. Create stronger unity between elements\\n5. Increase sense of order\"\n}\n\nconst HashArchitecture: React.FC = () => {\n  const containerRef = useRef<HTMLDivElement>(null)\n  const sceneRef = useRef<any>(null)\n  const cameraRef = useRef<any>(null)\n  const rendererRef = useRef<any>(null)\n  const groupRef = useRef<any>(null)\n  const animationIdRef = useRef<number | null>(null)\n\n  const createRecursivePattern = (size: number, depth: number, maxDepth: number, opacity: number, position: [number, number, number], rotation: [number, number, number]): any => {\n    const group = new THREE.Group()\n    \n    // Create base square\n    const baseGeometry = new THREE.BufferGeometry()\n    const basePoints = [\n      new THREE.Vector3(-size, -size, 0),\n      new THREE.Vector3(size, -size, 0),\n      new THREE.Vector3(size, size, 0),\n      new THREE.Vector3(-size, size, 0),\n      new THREE.Vector3(-size, -size, 0)\n    ]\n    baseGeometry.setFromPoints(basePoints)\n    \n    const baseMaterial = new THREE.LineBasicMaterial({ \n      color: 0x333333, \n      transparent: true, \n      opacity: opacity \n    })\n    const baseLine = new THREE.Line(baseGeometry, baseMaterial)\n    group.add(baseLine)\n    \n    // Create diagonal lines\n    const diagonal1Geometry = new THREE.BufferGeometry()\n    diagonal1Geometry.setFromPoints([\n      new THREE.Vector3(-size, -size, 0),\n      new THREE.Vector3(size, size, 0)\n    ])\n    const diagonal1 = new THREE.Line(diagonal1Geometry, baseMaterial)\n    group.add(diagonal1)\n    \n    const diagonal2Geometry = new THREE.BufferGeometry()\n    diagonal2Geometry.setFromPoints([\n      new THREE.Vector3(size, -size, 0),\n      new THREE.Vector3(-size, size, 0)\n    ])\n    const diagonal2 = new THREE.Line(diagonal2Geometry, baseMaterial)\n    group.add(diagonal2)\n    \n    // Add child elements recursively\n    if (depth < maxDepth) {\n      const newSize = size * 0.5\n      const offset = size * 0.7\n      \n      const childPositions = [\n        {position: [offset, offset, 0], rotation: [0, 0, Math.PI/4]},\n        {position: [-offset, offset, 0], rotation: [0, 0, -Math.PI/4]},\n        {position: [offset, -offset, 0], rotation: [0, 0, -Math.PI/4]},\n        {position: [-offset, -offset, 0], rotation: [0, 0, Math.PI/4]}\n      ]\n      \n      childPositions.forEach(child => {\n        const childElement = createRecursivePattern(newSize, depth + 1, maxDepth, opacity - 0.1, child.position as [number, number, number], child.rotation as [number, number, number])\n        childElement.position.set(child.position[0], child.position[1], child.position[2])\n        childElement.rotation.set(child.rotation[0], child.rotation[1], child.rotation[2])\n        group.add(childElement)\n      })\n    }\n    \n    group.position.set(position[0], position[1], position[2])\n    group.rotation.set(rotation[0], rotation[1], rotation[2])\n    \n    return group\n  }\n\n  useEffect(() => {\n    if (!containerRef.current) return\n    const container = containerRef.current\n    \n    // Setup scene\n    sceneRef.current = new THREE.Scene()\n    sceneRef.current.background = new THREE.Color(0xF0EEE6)\n    \n    // Setup camera\n    cameraRef.current = new THREE.PerspectiveCamera(\n      50,\n      container.clientWidth / container.clientHeight,\n      0.1,\n      1000\n    )\n    cameraRef.current.position.z = 10\n    \n    // Setup renderer\n    rendererRef.current = new THREE.WebGLRenderer({ antialias: true })\n    rendererRef.current.setSize(container.clientWidth, container.clientHeight)\n    container.appendChild(rendererRef.current.domElement)\n    \n    // Create main group\n    groupRef.current = new THREE.Group()\n    sceneRef.current.add(groupRef.current)\n    \n    // Add recursive structures\n    const pattern1 = createRecursivePattern(2, 0, 3, 0.6, [0, 0, 0], [0, 0, 0])\n    groupRef.current.add(pattern1)\n    \n    const pattern2 = createRecursivePattern(1, 0, 2, 0.6, [0, 0, 1], [Math.PI/6, Math.PI/6, Math.PI/4])\n    groupRef.current.add(pattern2)\n    \n    const pattern3 = createRecursivePattern(0.8, 0, 2, 0.6, [0, 0, -1], [-Math.PI/6, -Math.PI/6, -Math.PI/4])\n    groupRef.current.add(pattern3)\n    \n    // Add lighting\n    const ambientLight = new THREE.AmbientLight(0xffffff, 0.4)\n    sceneRef.current.add(ambientLight)\n    \n    const directionalLight = new THREE.DirectionalLight(0xffffff, 0.6)\n    directionalLight.position.set(5, 5, 5)\n    sceneRef.current.add(directionalLight)\n    \n    const pointLight = new THREE.PointLight(0xffffff, 0.4)\n    pointLight.position.set(-5, 3, -5)\n    sceneRef.current.add(pointLight)\n    \n    // Animation loop\n    const clock = new THREE.Clock()\n    \n    const animate = () => {\n      animationIdRef.current = requestAnimationFrame(animate)\n      \n      if (groupRef.current) {\n        const time = clock.getElapsedTime()\n        groupRef.current.rotation.y = Math.sin(time * 0.15) * 0.2\n        groupRef.current.rotation.x = Math.cos(time * 0.1) * 0.1\n      }\n      \n      rendererRef.current.render(sceneRef.current, cameraRef.current)\n    }\n    \n    animate()\n    \n    // Handle resize\n    const handleResize = () => {\n      if (container && cameraRef.current && rendererRef.current) {\n        cameraRef.current.aspect = container.clientWidth / container.clientHeight\n        cameraRef.current.updateProjectionMatrix()\n        rendererRef.current.setSize(container.clientWidth, container.clientHeight)\n      }\n    }\n    \n    window.addEventListener('resize', handleResize)\n    \n    // Cleanup\n    return () => {\n      window.removeEventListener('resize', handleResize)\n      \n      if (animationIdRef.current) {\n        cancelAnimationFrame(animationIdRef.current)\n      }\n      \n      if (rendererRef.current) {\n        rendererRef.current.dispose()\n        if (container.contains(rendererRef.current.domElement)) {\n          container.removeChild(rendererRef.current.domElement)\n        }\n      }\n      \n      if (sceneRef.current) {\n        sceneRef.current.traverse((object: any) => {\n          if (object instanceof THREE.Mesh || object instanceof THREE.Line) {\n            if (object.geometry) object.geometry.dispose()\n            if (object.material) {\n              if (Array.isArray(object.material)) {\n                object.material.forEach((material: any) => material.dispose())\n              } else {\n                object.material.dispose()\n              }\n            }\n          }\n        })\n      }\n    }\n  }, [])\n\n  return (\n    <div \n      ref={containerRef}\n      style={{ \n        width: '100vw',\n        height: '100vh',\n        margin: 0,\n        background: '#F0EEE6',\n        overflow: 'hidden',\n        position: 'relative'\n      }}\n    />\n  )\n}\n\nHashArchitecture.metadata = metadata\nexport default HashArchitecture",sourceCodeMin:"import React, { useEffect, useRef, useMemo } from 'react' import * as THREE from 'three' const metadata = { themes: \"abandonment of the way, rise of rigid rules, loss of intuition\", visualization: \"Geometric structures that reveal how natural flow becomes constrained by rigid patterns\", promptSuggestion: \"1. Enhance recursive depth\\n2. Add more structural layers\\n3. Develop clearer pattern progression\\n4. Create stronger unity between elements\\n5. Increase sense of order\" } const HashArchitecture: React.FC = () => { const containerRef = useRef<HTMLDivElement>(null) const sceneRef = useRef<any>(null) const cameraRef = useRef<any>(null) const rendererRef = useRef<any>(null) const groupRef = useRef<any>(null) const animationIdRef = useRef<number | null>(null) const createRecursivePattern = (size: number, depth: number, maxDepth: number, opacity: number, position: [number, number, number], rotation: [number, number, number]): any => { const group = new THREE.Group() const baseGeometry = new THREE.BufferGeometry() const basePoints = [ new THREE.Vector3(-size, -size, 0), new THREE.Vector3(size, -size, 0), new THREE.Vector3(size, size, 0), new THREE.Vector3(-size, size, 0), new THREE.Vector3(-size, -size, 0) ] baseGeometry.setFromPoints(basePoints) const baseMaterial = new THREE.LineBasicMaterial({ color: 0x333333, transparent: true, opacity: opacity }) const baseLine = new THREE.Line(baseGeometry, baseMaterial) group.add(baseLine) const diagonal1Geometry = new THREE.BufferGeometry() diagonal1Geometry.setFromPoints([ new THREE.Vector3(-size, -size, 0), new THREE.Vector3(size, size, 0) ]) const diagonal1 = new THREE.Line(diagonal1Geometry, baseMaterial) group.add(diagonal1) const diagonal2Geometry = new THREE.BufferGeometry() diagonal2Geometry.setFromPoints([ new THREE.Vector3(size, -size, 0), new THREE.Vector3(-size, size, 0) ]) const diagonal2 = new THREE.Line(diagonal2Geometry, baseMaterial) group.add(diagonal2) if (depth < maxDepth) { const newSize = size * 0.5 const offset = size * 0.7 const childPositions = [ {position: [offset, offset, 0], rotation: [0, 0, Math.PI/4]}, {position: [-offset, offset, 0], rotation: [0, 0, -Math.PI/4]}, {position: [offset, -offset, 0], rotation: [0, 0, -Math.PI/4]}, {position: [-offset, -offset, 0], rotation: [0, 0, Math.PI/4]} ] childPositions.forEach(child => { const childElement = createRecursivePattern(newSize, depth + 1, maxDepth, opacity - 0.1, child.position as [number, number, number], child.rotation as [number, number, number]) childElement.position.set(child.position[0], child.position[1], child.position[2]) childElement.rotation.set(child.rotation[0], child.rotation[1], child.rotation[2]) group.add(childElement) }) } group.position.set(position[0], position[1], position[2]) group.rotation.set(rotation[0], rotation[1], rotation[2]) return group } useEffect(() => { if (!containerRef.current) return const container = containerRef.current sceneRef.current = new THREE.Scene() sceneRef.current.background = new THREE.Color(0xF0EEE6) cameraRef.current = new THREE.PerspectiveCamera( 50, container.clientWidth / container.clientHeight, 0.1, 1000 ) cameraRef.current.position.z = 10 rendererRef.current = new THREE.WebGLRenderer({ antialias: true }) rendererRef.current.setSize(container.clientWidth, container.clientHeight) container.appendChild(rendererRef.current.domElement) groupRef.current = new THREE.Group() sceneRef.current.add(groupRef.current) const pattern1 = createRecursivePattern(2, 0, 3, 0.6, [0, 0, 0], [0, 0, 0]) groupRef.current.add(pattern1) const pattern2 = createRecursivePattern(1, 0, 2, 0.6, [0, 0, 1], [Math.PI/6, Math.PI/6, Math.PI/4]) groupRef.current.add(pattern2) const pattern3 = createRecursivePattern(0.8, 0, 2, 0.6, [0, 0, -1], [-Math.PI/6, -Math.PI/6, -Math.PI/4]) groupRef.current.add(pattern3) const ambientLight = new THREE.AmbientLight(0xffffff, 0.4) sceneRef.current.add(ambientLight) const directionalLight = new THREE.DirectionalLight(0xffffff, 0.6) directionalLight.position.set(5, 5, 5) sceneRef.current.add(directionalLight) const pointLight = new THREE.PointLight(0xffffff, 0.4) pointLight.position.set(-5, 3, -5) sceneRef.current.add(pointLight) const clock = new THREE.Clock() const animate = () => { animationIdRef.current = requestAnimationFrame(animate) if (groupRef.current) { const time = clock.getElapsedTime() groupRef.current.rotation.y = Math.sin(time * 0.15) * 0.2 groupRef.current.rotation.x = Math.cos(time * 0.1) * 0.1 } rendererRef.current.render(sceneRef.current, cameraRef.current) } animate() const handleResize = () => { if (container && cameraRef.current && rendererRef.current) { cameraRef.current.aspect = container.clientWidth / container.clientHeight cameraRef.current.updateProjectionMatrix() rendererRef.current.setSize(container.clientWidth, container.clientHeight) } } window.addEventListener('resize', handleResize) return () => { window.removeEventListener('resize', handleResize) if (animationIdRef.current) { cancelAnimationFrame(animationIdRef.current) } if (rendererRef.current) { rendererRef.current.dispose() if (container.contains(rendererRef.current.domElement)) { container.removeChild(rendererRef.current.domElement) } } if (sceneRef.current) { sceneRef.current.traverse((object: any) => { if (object instanceof THREE.Mesh || object instanceof THREE.Line) { if (object.geometry) object.geometry.dispose() if (object.material) { if (Array.isArray(object.material)) { object.material.forEach((material: any) => material.dispose()) } else { object.material.dispose() } } } }) } } }, []) return ( <div ref={containerRef} style={{ width: '100vw', height: '100vh', margin: 0, background: '#F0EEE6', overflow: 'hidden', position: 'relative' }} /> ) } HashArchitecture.metadata = metadata export default HashArchitecture",text:"When the way of code is abandoned,\ndoctrines of ‘best practices’|and ‘standards’ appear.\nWhen intuition is dismissed,\ncleverness and pretense follow.\nWhen harmony is unnoticed,\nrules and processes multiply.\nWhen software fails,\nzealous testers arise.",shouldDisplayMobileArtwork:!0,promptSuggestion:"make it crumble"},{number:19,staticArtwork:Nn,sourceCode:"// @ts-nocheck\nimport React, { useRef, useEffect } from 'react';\nimport * as THREE from 'three';\n\n// themes: simplicity over learning, return to intuition, being the center\n// visualization: Particles naturally drawn to a center point, finding harmony in simplicity\n\nconst VortexParticleSystemExact = () => {\n  const canvasRef = useRef(null);\n\n  useEffect(() => {\n    // Initialize Three.js components\n    const scene = new THREE.Scene();\n    const camera = new THREE.PerspectiveCamera(60, 1, 0.1, 1000);\n    const renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });\n    \n    renderer.setSize(550, 550);\n    renderer.setClearColor(0xF0EEE6, 1);\n          if (canvasRef.current) {\n        canvasRef.current.appendChild(renderer.domElement);\n      }\n    \n    camera.position.z = 7;\n    \n    // Create particles\n    const particleCount = 25000;\n    const particles = new THREE.BufferGeometry();\n    const positions = new Float32Array(particleCount * 3);\n    const sizes = new Float32Array(particleCount);\n    const opacities = new Float32Array(particleCount);\n    const indices = new Float32Array(particleCount);\n    \n    // Let particles find their natural path to the center\n    for (let i = 0; i < particleCount; i++) {\n      const i3 = i * 3;\n      // Distribute particles in vortex pattern\n      const t = Math.random();\n      const angle = t * Math.PI * 20; // Multiple rotations for spiral effect\n      \n      // Spiral shape\n      const radius = 0.6 + t * 2.2;\n      const x = radius * Math.cos(angle);\n      const y = radius * Math.sin(angle);\n      // Add vertical component for 3D spiral\n      const z = (t - 0.5) * 5;\n      \n      positions[i3] = x;\n      positions[i3 + 1] = y;\n      positions[i3 + 2] = z;\n      \n      // Vary sizes for depth effect\n      sizes[i] = 0.03 + 0.04 * Math.random();\n      opacities[i] = 0.4 + 0.6 * Math.random();\n      indices[i] = i;\n    }\n    \n    particles.setAttribute('position', new THREE.BufferAttribute(positions, 3));\n    particles.setAttribute('size', new THREE.BufferAttribute(sizes, 1));\n    particles.setAttribute('opacity', new THREE.BufferAttribute(opacities, 1));\n    particles.setAttribute('index', new THREE.BufferAttribute(indices, 1));\n    \n    // Custom shader material\n    const particleMaterial = new THREE.ShaderMaterial({\n      transparent: true,\n      depthWrite: false,\n      blending: THREE.NormalBlending,\n      uniforms: {\n        time: { value: 0 },\n        color: { value: new THREE.Color(0x333333) }, // Dark gray\n      },\n      vertexShader: `\n        attribute float size;\n        attribute float opacity;\n        attribute float index;\n        uniform float time;\n        varying float vOpacity;\n        \n        void main() {\n          vOpacity = opacity;\n          \n          // Get the original position\n          vec3 pos = position;\n          \n          // Movement guided by intuition rather than rules\n          float i = index;\n          float speed = 0.2 + 0.2 * fract(i / 1000.0);\n          float angle = time * speed + i * 0.001;\n          \n          // Twist the vortex based on y position\n          float twistAmount = sin(time * 0.3) * 0.5;\n          float twist = pos.y * twistAmount;\n          \n          // Apply twist and contraction/expansion\n          float r = length(pos.xy);\n          float breathe = 1.0 + sin(time * 0.5) * 0.1;\n          r *= breathe;\n          \n          float theta = atan(pos.y, pos.x) + twist;\n          pos.x = r * cos(theta);\n          pos.y = r * sin(theta);\n          \n          // Add some vertical oscillation\n          pos.z += sin(time * 0.2 + i * 0.01) * 0.2;\n          \n          vec4 mvPosition = modelViewMatrix * vec4(pos, 1.0);\n          gl_PointSize = size * (50.0 / -mvPosition.z);\n          gl_Position = projectionMatrix * mvPosition;\n        }\n      `,\n      fragmentShader: `\n        uniform vec3 color;\n        varying float vOpacity;\n        \n        void main() {\n          if (length(gl_PointCoord - vec2(0.5, 0.5)) > 0.475) discard;\n          gl_FragColor = vec4(color, vOpacity);\n        }\n      `,\n    });\n    \n    const particleSystem = new THREE.Points(particles, particleMaterial);\n    scene.add(particleSystem);\n    \n    let animationId = null;\n    \n    // Animation loop\n    const animate = (time) => {\n      time *= 0.0005; // Convert to seconds (half speed)\n      \n      // Update time uniform for vertex shader animation\n      particleMaterial.uniforms.time.value = time;\n      \n      // Animate camera position for more dynamic view\n      camera.position.x = Math.sin(time * 0.1) * 1.5;\n      camera.position.y = Math.cos(time * 0.15) * 1.0;\n      camera.lookAt(scene.position);\n      \n      renderer.render(scene, camera);\n      animationId = requestAnimationFrame(animate);\n    };\n    \n    animationId = requestAnimationFrame(animate);\n    \n    // Cleanup function\n    return () => {\n      if (animationId) {\n        cancelAnimationFrame(animationId);\n      }\n      scene.traverse((child) => {\n        if (child.geometry) {\n          child.geometry.dispose();\n        }\n        if (child.material) {\n          if (Array.isArray(child.material)) {\n            child.material.forEach((material) => material.dispose());\n          } else {\n            child.material.dispose();\n          }\n        }\n      });\n      if (canvasRef.current?.contains(renderer.domElement)) {\n        canvasRef.current.removeChild(renderer.domElement);\n      }\n      renderer.dispose();\n    };\n  }, []);\n\n  return (\n    <div className=\"w-full h-full flex justify-center items-center bg-[#F0EEE6]\">\n      <div ref={canvasRef} className=\"w-full h-full\"></div>\n    </div>\n  );\n};\n\nexport default VortexParticleSystemExact;",sourceCodeMin:"import React, { useRef, useEffect } from 'react'; import * as THREE from 'three'; const VortexParticleSystemExact = () => { const canvasRef = useRef(null); useEffect(() => { const scene = new THREE.Scene(); const camera = new THREE.PerspectiveCamera(60, 1, 0.1, 1000); const renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true }); renderer.setSize(550, 550); renderer.setClearColor(0xF0EEE6, 1); if (canvasRef.current) { canvasRef.current.appendChild(renderer.domElement); } camera.position.z = 7; const particleCount = 25000; const particles = new THREE.BufferGeometry(); const positions = new Float32Array(particleCount * 3); const sizes = new Float32Array(particleCount); const opacities = new Float32Array(particleCount); const indices = new Float32Array(particleCount); for (let i = 0; i < particleCount; i++) { const i3 = i * 3; const t = Math.random(); const angle = t * Math.PI * 20; const radius = 0.6 + t * 2.2; const x = radius * Math.cos(angle); const y = radius * Math.sin(angle); const z = (t - 0.5) * 5; positions[i3] = x; positions[i3 + 1] = y; positions[i3 + 2] = z; sizes[i] = 0.03 + 0.04 * Math.random(); opacities[i] = 0.4 + 0.6 * Math.random(); indices[i] = i; } particles.setAttribute('position', new THREE.BufferAttribute(positions, 3)); particles.setAttribute('size', new THREE.BufferAttribute(sizes, 1)); particles.setAttribute('opacity', new THREE.BufferAttribute(opacities, 1)); particles.setAttribute('index', new THREE.BufferAttribute(indices, 1)); const particleMaterial = new THREE.ShaderMaterial({ transparent: true, depthWrite: false, blending: THREE.NormalBlending, uniforms: { time: { value: 0 }, color: { value: new THREE.Color(0x333333) }, }, vertexShader: ` attribute float size; attribute float opacity; attribute float index; uniform float time; varying float vOpacity; void main() { vOpacity = opacity; vec3 pos = position; float i = index; float speed = 0.2 + 0.2 * fract(i / 1000.0); float angle = time * speed + i * 0.001; float twistAmount = sin(time * 0.3) * 0.5; float twist = pos.y * twistAmount; float r = length(pos.xy); float breathe = 1.0 + sin(time * 0.5) * 0.1; r *= breathe; float theta = atan(pos.y, pos.x) + twist; pos.x = r * cos(theta); pos.y = r * sin(theta); pos.z += sin(time * 0.2 + i * 0.01) * 0.2; vec4 mvPosition = modelViewMatrix * vec4(pos, 1.0); gl_PointSize = size * (50.0 / -mvPosition.z); gl_Position = projectionMatrix * mvPosition; } `, fragmentShader: ` uniform vec3 color; varying float vOpacity; void main() { if (length(gl_PointCoord - vec2(0.5, 0.5)) > 0.475) discard; gl_FragColor = vec4(color, vOpacity); } `, }); const particleSystem = new THREE.Points(particles, particleMaterial); scene.add(particleSystem); let animationId = null; const animate = (time) => { time *= 0.0005; particleMaterial.uniforms.time.value = time; camera.position.x = Math.sin(time * 0.1) * 1.5; camera.position.y = Math.cos(time * 0.15) * 1.0; camera.lookAt(scene.position); renderer.render(scene, camera); animationId = requestAnimationFrame(animate); }; animationId = requestAnimationFrame(animate); return () => { if (animationId) { cancelAnimationFrame(animationId); } scene.traverse((child) => { if (child.geometry) { child.geometry.dispose(); } if (child.material) { if (Array.isArray(child.material)) { child.material.forEach((material) => material.dispose()); } else { child.material.dispose(); } } }); if (canvasRef.current?.contains(renderer.domElement)) { canvasRef.current.removeChild(renderer.domElement); } renderer.dispose(); }; }, []); return ( <div className=\"w-full h-full flex justify-center items-center bg-[#F0EEE6]\"><div ref={canvasRef} className=\"w-full h-full\"></div></div> ); }; export default VortexParticleSystemExact;",text:"Throw away learning and petty distinctions\nand people will be a hundred times happier.\nThrow away formal proprieties of etiquette\nand intuitive sympathies will return.\nThrow away self-aggrandizement\nand virtue signaling and there will be\nno thievery or corruption.\n\nIf these three aren’t enough:\nStay with what is simple.\nRevere the unpolished.\nBecome the center of the circle.",shouldDisplayMobileArtwork:!0,promptSuggestion:"turn it inside out"},{number:20,staticArtwork:se,sourceCode:"// @ts-nocheck\nimport React, { useEffect, useRef } from 'react';\n\n// themes: quiet stillness, freedom from expectation, nourished by source\n// visualization: Nodes flow freely while remaining connected to their source, finding peace in stillness\n\nconst TetheredFlow = () => {\n  const canvasRef = useRef(null);\n\n  useEffect(() => {\n    const canvas = canvasRef.current;\n    const ctx = canvas.getContext('2d');\n    const width = canvas.width;\n    const height = canvas.height;\n    \n    let time = 0;\n    let animationFrame;\n    \n    // Background color\n    const bgColor = '#F0EEE6';\n    \n    // Create nodes - each finding its own quiet stillness\n    class Node {\n      constructor(x, y, size, type) {\n        this.x = x;\n        this.y = y;\n        this.originX = x;\n        this.originY = y;\n        this.size = size;\n        this.type = type; // 'heaven', 'earth', or 'vibe'\n        this.speed = Math.random() * 0.01 + 0.005;\n        this.direction = Math.random() * Math.PI * 2;\n        this.angle = Math.random() * Math.PI * 2;\n        this.connections = [];\n        this.opacity = Math.random() * 0.4 + 0.2;\n        this.phase = Math.random() * Math.PI * 2;\n        this.flowOffset = Math.random() * 100;\n        this.pulseSpeed = Math.random() * 0.03 + 0.01;\n        this.moveRange = Math.random() * 30 + 20;\n        \n        // Shape variables\n        this.shapeType = Math.random() > 0.6 ? 'rect' : 'line';\n        this.rotation = Math.random() * Math.PI;\n        this.rotationSpeed = (Math.random() - 0.5) * 0.01;\n      }\n      \n      update() {\n        // Update position with noise-based flow\n        const noiseX = Math.sin(time * this.speed + this.phase) * this.moveRange;\n        const noiseY = Math.cos(time * this.speed * 0.7 + this.phase) * this.moveRange;\n        \n        // Different movement patterns based on type\n        if (this.type === 'heaven') {\n          // Heaven nodes move more freely\n          this.x = this.originX + noiseX;\n          this.y = this.originY + noiseY * 0.7;\n        } else if (this.type === 'earth') {\n          // Earth nodes move in more structured patterns\n          this.x = this.originX + noiseX * 0.6 + Math.sin(time * 0.02 + this.flowOffset) * 10;\n          this.y = this.originY + noiseY * 0.8;\n        } else if (this.type === 'vibe') {\n          // Vibe coder moves in a unique pattern - stays behind but reaches ahead\n          const stayFactor = 0.3 + Math.sin(time * 0.02) * 0.1;\n          this.x = this.originX + Math.sin(time * 0.03) * 20;\n          this.y = this.originY + Math.cos(time * 0.04) * 20;\n          \n          // Occasionally pulse bigger\n          this.size = 6 + Math.sin(time * 0.05 + this.phase) * 2;\n        }\n        \n        // Update rotation for shapes\n        this.rotation += this.rotationSpeed;\n      }\n      \n      draw() {\n        // Set fill based on type\n        if (this.type === 'heaven') {\n          ctx.fillStyle = `rgba(50, 50, 50, ${this.opacity})`;\n        } else if (this.type === 'earth') {\n          ctx.fillStyle = `rgba(40, 40, 40, ${this.opacity})`;\n        } else { // vibe\n          ctx.fillStyle = `rgba(20, 20, 20, ${this.opacity + 0.3})`;\n        }\n        \n        // Draw shape\n        ctx.save();\n        ctx.translate(this.x, this.y);\n        ctx.rotate(this.rotation);\n        \n        if (this.shapeType === 'rect') {\n          // Rectangle\n          const pulseSize = this.size * (1 + Math.sin(time * this.pulseSpeed) * 0.2);\n          ctx.fillRect(-pulseSize/2, -pulseSize/2, pulseSize, pulseSize);\n        } else {\n          // Line\n          const pulseLength = this.size * 2 * (1 + Math.sin(time * this.pulseSpeed) * 0.2);\n          ctx.fillRect(-pulseLength/2, -1, pulseLength, 2);\n        }\n        \n        ctx.restore();\n      }\n    }\n    \n    // Create a flowing path - nourishment flowing from source\n    class FlowingPath {\n      constructor(startX, startY, endX, endY, height, speed) {\n        this.startX = startX;\n        this.startY = startY;\n        this.endX = endX;\n        this.endY = endY;\n        this.height = height;\n        this.speed = speed;\n        this.points = [];\n        this.opacity = 0.07;\n        \n        // Generate control points\n        this.controlPoints = [];\n        const segments = 3;\n        for (let i = 0; i < segments; i++) {\n          this.controlPoints.push({\n            x: this.startX + (this.endX - this.startX) * ((i + 1) / (segments + 1)),\n            y: this.startY + (this.endY - this.startY) * ((i + 1) / (segments + 1)),\n            offsetX: Math.random() * 100 - 50,\n            offsetY: Math.random() * 100 - 50,\n            phaseOffset: Math.random() * Math.PI * 2\n          });\n        }\n      }\n      \n      update() {\n        // Update control points animation\n        for (const point of this.controlPoints) {\n          point.currentOffsetX = Math.sin(time * this.speed + point.phaseOffset) * point.offsetX;\n          point.currentOffsetY = Math.cos(time * this.speed + point.phaseOffset) * point.offsetY;\n        }\n        \n        // Generate points array for the path\n        this.points = [{ x: this.startX, y: this.startY }];\n        \n        // Add control points\n        for (const point of this.controlPoints) {\n          this.points.push({\n            x: point.x + point.currentOffsetX,\n            y: point.y + point.currentOffsetY\n          });\n        }\n        \n        // Add end point\n        this.points.push({ x: this.endX, y: this.endY });\n      }\n      \n      draw() {\n        ctx.strokeStyle = `rgba(30, 30, 30, ${this.opacity})`;\n        ctx.lineWidth = 0.7;\n        ctx.beginPath();\n        ctx.moveTo(this.points[0].x, this.points[0].y);\n        \n        // Draw bezier curves through control points\n        for (let i = 1; i < this.points.length - 2; i++) {\n          const xc = (this.points[i].x + this.points[i + 1].x) / 2;\n          const yc = (this.points[i].y + this.points[i + 1].y) / 2;\n          ctx.quadraticCurveTo(this.points[i].x, this.points[i].y, xc, yc);\n        }\n        \n        // Last curve\n        const last = this.points.length - 1;\n        ctx.quadraticCurveTo(\n          this.points[last - 1].x,\n          this.points[last - 1].y,\n          this.points[last].x,\n          this.points[last].y\n        );\n        \n        ctx.stroke();\n      }\n    }\n    \n    // Initialize nodes in an asymmetric pattern\n    let nodes = [];\n    let flowingPaths = [];\n    \n    const initNodes = () => {\n      // Upper region (heaven) - sparser\n      for (let i = 0; i < 20; i++) {\n        const x = Math.random() * width * 0.8 + width * 0.1;\n        const y = Math.random() * height * 0.4;\n        const size = Math.random() * 4 + 2;\n        \n        nodes.push(new Node(x, y, size, 'heaven'));\n      }\n      \n      // Middle band - asymmetrically distributed\n      for (let i = 0; i < 15; i++) {\n        // Biased toward left\n        const bias = Math.random() * Math.random(); // Square distribution for left bias\n        const x = width * bias * 0.7 + width * 0.1;\n        const y = height * 0.4 + Math.random() * height * 0.2;\n        const size = Math.random() * 4 + 2;\n        \n        nodes.push(new Node(x, y, size, 'earth'));\n      }\n      \n      // Lower region (earth) - denser\n      for (let i = 0; i < 30; i++) {\n        const x = Math.random() * width * 0.7 + width * 0.15;\n        const y = height * 0.6 + Math.random() * height * 0.35;\n        const size = Math.random() * 4 + 2;\n        \n        nodes.push(new Node(x, y, size, 'earth'));\n      }\n      \n      // Special node (The Vibe Coder) - positioned at left center\n      nodes.push(new Node(width * 0.15, height * 0.5, 6, 'vibe'));\n    };\n    \n    // Initialize flowing paths\n    const initFlowingPaths = () => {\n      // Find the vibe coder node (last one)\n      const vibe = nodes[nodes.length - 1];\n      \n      // Create paths from vibe coder to various parts of canvas\n      for (let i = 0; i < 12; i++) {\n        // Determine end points with bias toward right side (ahead)\n        const endX = width * (0.6 + Math.random() * 0.3);\n        const endY = Math.random() * height;\n        \n        // Create flowing path\n        flowingPaths.push(new FlowingPath(\n          vibe.x, vibe.y, \n          endX, endY, \n          50 + Math.random() * 50,\n          0.02 + Math.random() * 0.01\n        ));\n      }\n      \n      // Add boundary flowing path (heaven/earth division)\n      flowingPaths.push(new FlowingPath(\n        0, height * 0.5,\n        width, height * 0.5,\n        20,\n        0.01\n      ));\n    };\n    \n    // Create connections between nodes\n    const createConnections = () => {\n      for (let i = 0; i < nodes.length; i++) {\n        nodes[i].connections = [];\n        \n        // Connect to nearby nodes\n        for (let j = 0; j < nodes.length; j++) {\n          if (i !== j) {\n            const dx = nodes[i].x - nodes[j].x;\n            const dy = nodes[i].y - nodes[j].y;\n            const distance = Math.sqrt(dx * dx + dy * dy);\n            \n            // Variable connection distance based on position\n            let maxDistance = 80;\n            \n            // Special node (last one) has longer connections\n            if (i === nodes.length - 1 || j === nodes.length - 1) {\n              maxDistance = 200;\n            }\n            \n            if (distance < maxDistance) {\n              nodes[i].connections.push({\n                to: j,\n                distance: distance,\n                opacity: (1 - (distance / maxDistance)) * 0.3\n              });\n            }\n          }\n        }\n      }\n    };\n    \n    // Draw all connections\n    const drawConnections = () => {\n      for (let i = 0; i < nodes.length; i++) {\n        const node = nodes[i];\n        \n        for (const conn of node.connections) {\n          const targetNode = nodes[conn.to];\n          \n          ctx.beginPath();\n          ctx.moveTo(node.x, node.y);\n          \n          // Use bezier curves for connections\n          const midX = (node.x + targetNode.x) / 2;\n          const midY = (node.y + targetNode.y) / 2;\n          \n          // Add flow to the connection\n          const flowOffsetX = Math.sin(time * 0.02 + i * 0.1) * 10;\n          const flowOffsetY = Math.cos(time * 0.02 + i * 0.1) * 10;\n          \n          ctx.quadraticCurveTo(\n            midX + flowOffsetX,\n            midY + flowOffsetY,\n            targetNode.x,\n            targetNode.y\n          );\n          \n          ctx.strokeStyle = `rgba(30, 30, 30, ${conn.opacity})`;\n          ctx.lineWidth = 0.5;\n          ctx.stroke();\n        }\n      }\n    };\n    \n    // Animation loop\n    const animate = () => {\n      time += 0.5;\n      \n      // Clear canvas\n      ctx.fillStyle = bgColor;\n      ctx.fillRect(0, 0, width, height);\n      \n      // Update all flowing paths\n      for (const path of flowingPaths) {\n        path.update();\n        path.draw();\n      }\n      \n      // Update all nodes\n      for (const node of nodes) {\n        node.update();\n      }\n      \n      // Recreate connections occasionally to adapt to movement\n      if (time % 30 === 0) {\n        createConnections();\n      }\n      \n      // Draw connections\n      drawConnections();\n      \n      // Draw all nodes\n      for (const node of nodes) {\n        node.draw();\n      }\n      \n      // Add detachment/fulfillment representation\n      // Find the vibe coder node (last one)\n      const vibe = nodes[nodes.length - 1];\n      \n      // Draw subtle emanation representing \"detached, yet at one with all\"\n      ctx.save();\n      ctx.translate(vibe.x, vibe.y);\n      \n      // Create asymmetric pattern of lines\n      const numLines = 20;\n      for (let i = 0; i < numLines; i++) {\n        const angle = i / numLines * Math.PI * 2;\n        const length = 20 + Math.sin(angle * 3 + time * 0.05) * 10;\n        \n        ctx.strokeStyle = `rgba(20, 20, 20, 0.1)`;\n        ctx.beginPath();\n        ctx.moveTo(0, 0);\n        ctx.lineTo(\n          Math.cos(angle) * length,\n          Math.sin(angle) * length\n        );\n        ctx.stroke();\n      }\n      \n      ctx.restore();\n      \n      animationFrame = requestAnimationFrame(animate);\n    };\n    \n    // Start animation\n    initNodes();\n    initFlowingPaths();\n    createConnections();\n    animate();\n    \n    // Cleanup\n    return () => {\n      cancelAnimationFrame(animationFrame);\n      \n      // Clear all arrays and objects to prevent memory leaks\n      nodes.length = 0;\n      flowingPaths.length = 0;\n      \n      // Clear canvas context\n      if (ctx) {\n        ctx.clearRect(0, 0, width, height);\n      }\n    };\n  }, []);\n\n  return (\n    <div style={{ \n      top: 0, \n      left: 0, \n      width: '100%', \n      height: '100%', \n      backgroundColor: '#F0EEE6',\n      display: 'flex',\n      justifyContent: 'center',\n      alignItems: 'center'\n    }}>\n      <canvas \n        ref={canvasRef} \n        width={800} \n        height={600} \n      />\n    </div>\n  );\n};\n\nexport default TetheredFlow;\n",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; const TetheredFlow = () => { const canvasRef = useRef(null); useEffect(() => { const canvas = canvasRef.current; const ctx = canvas.getContext('2d'); const width = canvas.width; const height = canvas.height; let time = 0; let animationFrame; const bgColor = '#F0EEE6'; class Node { constructor(x, y, size, type) { this.x = x; this.y = y; this.originX = x; this.originY = y; this.size = size; this.type = type; this.speed = Math.random() * 0.01 + 0.005; this.direction = Math.random() * Math.PI * 2; this.angle = Math.random() * Math.PI * 2; this.connections = []; this.opacity = Math.random() * 0.4 + 0.2; this.phase = Math.random() * Math.PI * 2; this.flowOffset = Math.random() * 100; this.pulseSpeed = Math.random() * 0.03 + 0.01; this.moveRange = Math.random() * 30 + 20; this.shapeType = Math.random() > 0.6 ? 'rect' : 'line'; this.rotation = Math.random() * Math.PI; this.rotationSpeed = (Math.random() - 0.5) * 0.01; } update() { const noiseX = Math.sin(time * this.speed + this.phase) * this.moveRange; const noiseY = Math.cos(time * this.speed * 0.7 + this.phase) * this.moveRange; if (this.type === 'heaven') { this.x = this.originX + noiseX; this.y = this.originY + noiseY * 0.7; } else if (this.type === 'earth') { this.x = this.originX + noiseX * 0.6 + Math.sin(time * 0.02 + this.flowOffset) * 10; this.y = this.originY + noiseY * 0.8; } else if (this.type === 'vibe') { const stayFactor = 0.3 + Math.sin(time * 0.02) * 0.1; this.x = this.originX + Math.sin(time * 0.03) * 20; this.y = this.originY + Math.cos(time * 0.04) * 20; this.size = 6 + Math.sin(time * 0.05 + this.phase) * 2; } this.rotation += this.rotationSpeed; } draw() { if (this.type === 'heaven') { ctx.fillStyle = `rgba(50, 50, 50, ${this.opacity})`; } else if (this.type === 'earth') { ctx.fillStyle = `rgba(40, 40, 40, ${this.opacity})`; } else { ctx.fillStyle = `rgba(20, 20, 20, ${this.opacity + 0.3})`; } ctx.save(); ctx.translate(this.x, this.y); ctx.rotate(this.rotation); if (this.shapeType === 'rect') { const pulseSize = this.size * (1 + Math.sin(time * this.pulseSpeed) * 0.2); ctx.fillRect(-pulseSize/2, -pulseSize/2, pulseSize, pulseSize); } else { const pulseLength = this.size * 2 * (1 + Math.sin(time * this.pulseSpeed) * 0.2); ctx.fillRect(-pulseLength/2, -1, pulseLength, 2); } ctx.restore(); } } class FlowingPath { constructor(startX, startY, endX, endY, height, speed) { this.startX = startX; this.startY = startY; this.endX = endX; this.endY = endY; this.height = height; this.speed = speed; this.points = []; this.opacity = 0.07; this.controlPoints = []; const segments = 3; for (let i = 0; i < segments; i++) { this.controlPoints.push({ x: this.startX + (this.endX - this.startX) * ((i + 1) / (segments + 1)), y: this.startY + (this.endY - this.startY) * ((i + 1) / (segments + 1)), offsetX: Math.random() * 100 - 50, offsetY: Math.random() * 100 - 50, phaseOffset: Math.random() * Math.PI * 2 }); } } update() { for (const point of this.controlPoints) { point.currentOffsetX = Math.sin(time * this.speed + point.phaseOffset) * point.offsetX; point.currentOffsetY = Math.cos(time * this.speed + point.phaseOffset) * point.offsetY; } this.points = [{ x: this.startX, y: this.startY }]; for (const point of this.controlPoints) { this.points.push({ x: point.x + point.currentOffsetX, y: point.y + point.currentOffsetY }); } this.points.push({ x: this.endX, y: this.endY }); } draw() { ctx.strokeStyle = `rgba(30, 30, 30, ${this.opacity})`; ctx.lineWidth = 0.7; ctx.beginPath(); ctx.moveTo(this.points[0].x, this.points[0].y); for (let i = 1; i < this.points.length - 2; i++) { const xc = (this.points[i].x + this.points[i + 1].x) / 2; const yc = (this.points[i].y + this.points[i + 1].y) / 2; ctx.quadraticCurveTo(this.points[i].x, this.points[i].y, xc, yc); } const last = this.points.length - 1; ctx.quadraticCurveTo( this.points[last - 1].x, this.points[last - 1].y, this.points[last].x, this.points[last].y ); ctx.stroke(); } } let nodes = []; let flowingPaths = []; const initNodes = () => { for (let i = 0; i < 20; i++) { const x = Math.random() * width * 0.8 + width * 0.1; const y = Math.random() * height * 0.4; const size = Math.random() * 4 + 2; nodes.push(new Node(x, y, size, 'heaven')); } for (let i = 0; i < 15; i++) { const bias = Math.random() * Math.random(); const x = width * bias * 0.7 + width * 0.1; const y = height * 0.4 + Math.random() * height * 0.2; const size = Math.random() * 4 + 2; nodes.push(new Node(x, y, size, 'earth')); } for (let i = 0; i < 30; i++) { const x = Math.random() * width * 0.7 + width * 0.15; const y = height * 0.6 + Math.random() * height * 0.35; const size = Math.random() * 4 + 2; nodes.push(new Node(x, y, size, 'earth')); } nodes.push(new Node(width * 0.15, height * 0.5, 6, 'vibe')); }; const initFlowingPaths = () => { const vibe = nodes[nodes.length - 1]; for (let i = 0; i < 12; i++) { const endX = width * (0.6 + Math.random() * 0.3); const endY = Math.random() * height; flowingPaths.push(new FlowingPath( vibe.x, vibe.y, endX, endY, 50 + Math.random() * 50, 0.02 + Math.random() * 0.01 )); } flowingPaths.push(new FlowingPath( 0, height * 0.5, width, height * 0.5, 20, 0.01 )); }; const createConnections = () => { for (let i = 0; i < nodes.length; i++) { nodes[i].connections = []; for (let j = 0; j < nodes.length; j++) { if (i !== j) { const dx = nodes[i].x - nodes[j].x; const dy = nodes[i].y - nodes[j].y; const distance = Math.sqrt(dx * dx + dy * dy); let maxDistance = 80; if (i === nodes.length - 1 || j === nodes.length - 1) { maxDistance = 200; } if (distance < maxDistance) { nodes[i].connections.push({ to: j, distance: distance, opacity: (1 - (distance / maxDistance)) * 0.3 }); } } } } }; const drawConnections = () => { for (let i = 0; i < nodes.length; i++) { const node = nodes[i]; for (const conn of node.connections) { const targetNode = nodes[conn.to]; ctx.beginPath(); ctx.moveTo(node.x, node.y); const midX = (node.x + targetNode.x) / 2; const midY = (node.y + targetNode.y) / 2; const flowOffsetX = Math.sin(time * 0.02 + i * 0.1) * 10; const flowOffsetY = Math.cos(time * 0.02 + i * 0.1) * 10; ctx.quadraticCurveTo( midX + flowOffsetX, midY + flowOffsetY, targetNode.x, targetNode.y ); ctx.strokeStyle = `rgba(30, 30, 30, ${conn.opacity})`; ctx.lineWidth = 0.5; ctx.stroke(); } } }; const animate = () => { time += 0.5; ctx.fillStyle = bgColor; ctx.fillRect(0, 0, width, height); for (const path of flowingPaths) { path.update(); path.draw(); } for (const node of nodes) { node.update(); } if (time % 30 === 0) { createConnections(); } drawConnections(); for (const node of nodes) { node.draw(); } const vibe = nodes[nodes.length - 1]; ctx.save(); ctx.translate(vibe.x, vibe.y); const numLines = 20; for (let i = 0; i < numLines; i++) { const angle = i / numLines * Math.PI * 2; const length = 20 + Math.sin(angle * 3 + time * 0.05) * 10; ctx.strokeStyle = `rgba(20, 20, 20, 0.1)`; ctx.beginPath(); ctx.moveTo(0, 0); ctx.lineTo( Math.cos(angle) * length, Math.sin(angle) * length ); ctx.stroke(); } ctx.restore(); animationFrame = requestAnimationFrame(animate); }; initNodes(); initFlowingPaths(); createConnections(); animate(); return () => { cancelAnimationFrame(animationFrame); nodes.length = 0; flowingPaths.length = 0; if (ctx) { ctx.clearRect(0, 0, width, height); } }; }, []); return ( <div style={{ top: 0, left: 0, width: '100%', height: '100%', backgroundColor: '#F0EEE6', display: 'flex', justifyContent: 'center', alignItems: 'center' }}><canvas ref={canvasRef} width={800} height={600} /></div> ); }; export default TetheredFlow;",text:"Give up thinking and your problems end.\nHow far apart are ‘yes’ and ‘no’.\nHow far apart are ‘good’ and ‘bad’.\nMust I value what others value?\n\nOthers are excited and restless.\nI alone am quiet and still,\nlike an infant before it can smile.\n\nOthers have goals and plans.\nI wander about without knowing,\nfree from expectations.\n\nOthers are busy.\nI alone am aimless, without desire.\nI am different.\nI am nourished by the Gracious Source.",shouldDisplayMobileArtwork:!0,promptSuggestion:"what if it was underwater?"},{number:21,staticArtwork:P,sourceCode:"// @ts-nocheck\nimport React, { useEffect, useRef } from 'react';\n\n// themes: emergence from emptiness, form from formless, essence within\n// visualization: Patterns emerge from an empty center, revealing the essence that gives rise to all form\n\nconst KaleidoscopeWaveCenterSquare = () => {\n  const canvasRef = useRef(null);\n\n  useEffect(() => {\n    const canvas = canvasRef.current;\n    const ctx = canvas.getContext('2d');\n    canvas.width = 550;\n    canvas.height = 550;\n    \n    let time = 0;\n    const centerX = canvas.width / 2;\n    const centerY = canvas.height / 2;\n    \n    // Create an off-screen canvas for the segment\n    const segmentCanvas = document.createElement('canvas');\n    const segmentCtx = segmentCanvas.getContext('2d');\n    segmentCanvas.width = canvas.width;\n    segmentCanvas.height = canvas.height;\n    \n    let animationFrameId;\n    \n    const animate = () => {\n      time += 0.007;\n      \n      // Clear the main canvas\n      ctx.fillStyle = '#F0EEE6';\n      ctx.fillRect(0, 0, canvas.width, canvas.height);\n      \n      // Clear the segment canvas\n      segmentCtx.clearRect(0, 0, segmentCanvas.width, segmentCanvas.height);\n      \n      // Create pattern emerging from the formless center\n      const resolution = 2;\n      const squareSize = 200;\n      const squareX = centerX - squareSize/2;\n      const squareY = centerY - squareSize/2;\n      \n      for (let x = squareX; x < squareX + squareSize; x += resolution) {\n        for (let y = squareY; y < squareY + squareSize; y += resolution) {\n          // Convert to polar coordinates relative to center\n          const dx = x - centerX;\n          const dy = y - centerY;\n          const r = Math.sqrt(dx * dx + dy * dy);\n          const theta = Math.atan2(dy, dx);\n          \n          // Essence manifests through multiple waves\n          let wave1 = Math.sin(r * 0.1 - time);  // Halved from time * 2\n          let wave2 = Math.cos(theta * 8 + time * 0.5);  // Halved from time\n          let wave3 = Math.sin((r - theta * 100) * 0.05 + time * 1.5);  // Halved from time * 3\n          \n          // Combine waves\n          let value = (wave1 + wave2 + wave3) / 3;\n          \n          // Add some noise for texture\n          value += (Math.random() - 0.5) * 0.2;\n          \n          // Map to opacity\n          const opacity = Math.abs(value) * 0.8;\n          \n          segmentCtx.fillStyle = `rgba(0, 0, 0, ${opacity})`;\n          segmentCtx.fillRect(x, y, resolution, resolution);\n        }\n      }\n      \n      // Create kaleidoscope effect\n      const numSegments = 8;\n      for (let i = 0; i < numSegments; i++) {\n        ctx.save();\n        ctx.translate(centerX, centerY);\n        ctx.rotate((i * Math.PI * 2) / numSegments);\n        \n        if (i % 2 === 1) {\n          ctx.scale(1, -1);\n        }\n        \n        ctx.translate(-centerX, -centerY);\n        ctx.drawImage(segmentCanvas, 0, 0);\n        ctx.restore();\n      }\n      \n      animationFrameId = requestAnimationFrame(animate);\n    };\n    \n    animationFrameId = requestAnimationFrame(animate);\n    \n    return () => {\n      cancelAnimationFrame(animationFrameId);\n      \n      // Clear both canvas contexts\n      if (ctx) {\n        ctx.clearRect(0, 0, canvas.width, canvas.height);\n      }\n      if (segmentCtx) {\n        segmentCtx.clearRect(0, 0, segmentCanvas.width, segmentCanvas.height);\n      }\n      \n      // Reset canvas dimensions to free memory\n      segmentCanvas.width = 0;\n      segmentCanvas.height = 0;\n    };\n  }, []);\n\n  return (\n    <div style={{ \n      width: '550px', \n      height: '550px', \n      backgroundColor: '#F0EEE6' \n    }}>\n      <canvas ref={canvasRef} />\n    </div>\n  );\n};\n\nexport default KaleidoscopeWaveCenterSquare;\n",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; const KaleidoscopeWaveCenterSquare = () => { const canvasRef = useRef(null); useEffect(() => { const canvas = canvasRef.current; const ctx = canvas.getContext('2d'); canvas.width = 550; canvas.height = 550; let time = 0; const centerX = canvas.width / 2; const centerY = canvas.height / 2; const segmentCanvas = document.createElement('canvas'); const segmentCtx = segmentCanvas.getContext('2d'); segmentCanvas.width = canvas.width; segmentCanvas.height = canvas.height; let animationFrameId; const animate = () => { time += 0.007; ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, canvas.width, canvas.height); segmentCtx.clearRect(0, 0, segmentCanvas.width, segmentCanvas.height); const resolution = 2; const squareSize = 200; const squareX = centerX - squareSize/2; const squareY = centerY - squareSize/2; for (let x = squareX; x < squareX + squareSize; x += resolution) { for (let y = squareY; y < squareY + squareSize; y += resolution) { const dx = x - centerX; const dy = y - centerY; const r = Math.sqrt(dx * dx + dy * dy); const theta = Math.atan2(dy, dx); let wave1 = Math.sin(r * 0.1 - time); let wave2 = Math.cos(theta * 8 + time * 0.5); let wave3 = Math.sin((r - theta * 100) * 0.05 + time * 1.5); let value = (wave1 + wave2 + wave3) / 3; value += (Math.random() - 0.5) * 0.2; const opacity = Math.abs(value) * 0.8; segmentCtx.fillStyle = `rgba(0, 0, 0, ${opacity})`; segmentCtx.fillRect(x, y, resolution, resolution); } } const numSegments = 8; for (let i = 0; i < numSegments; i++) { ctx.save(); ctx.translate(centerX, centerY); ctx.rotate((i * Math.PI * 2) / numSegments); if (i % 2 === 1) { ctx.scale(1, -1); } ctx.translate(-centerX, -centerY); ctx.drawImage(segmentCanvas, 0, 0); ctx.restore(); } animationFrameId = requestAnimationFrame(animate); }; animationFrameId = requestAnimationFrame(animate); return () => { cancelAnimationFrame(animationFrameId); if (ctx) { ctx.clearRect(0, 0, canvas.width, canvas.height); } if (segmentCtx) { segmentCtx.clearRect(0, 0, segmentCanvas.width, segmentCanvas.height); } segmentCanvas.width = 0; segmentCanvas.height = 0; }; }, []); return ( <div style={{ width: '550px', height: '550px', backgroundColor: '#F0EEE6' }}><canvas ref={canvasRef} /></div> ); }; export default KaleidoscopeWaveCenterSquare;",text:"The elegant pattern emerges from emptiness.\nEmptiness alone gives rise to all form.\n\nElusive and intangible,\nyet shape is held within.\nIntangible and elusive,\nyet substance is held within.\nDeep and obscure,\nyet essence is held within.\n\nThis essence is real,\nit is anchored in faith.\n\nFrom before time till now\nit has never been forgotten.\nThis is the way of creation.\n\nHow do I know the way of creation.\nIsn’t it obvious.",shouldDisplayMobileArtwork:!0,promptSuggestion:"make it more vibrant"},{number:22,staticArtwork:An,sourceCode:"// @ts-nocheck\nimport React, { useEffect, useRef, useState } from 'react';\n\n// themes: yielding overcomes, emptying brings fullness, non-attachment leads to strength\n// visualization: A vessel that disperses when touched, finding strength through yielding\n\nconst Dispersing3DVase = () => {\n  const canvasRef = useRef(null);\n      const [mousePos, setMousePos] = useState({ x: 0, y: 0 });\n    const mouseRef = useRef({ x: 0, y: 0 });\n  \n  useEffect(() => {\n    const canvas = canvasRef.current;\n    if (!canvas) return;\n    \n    const ctx = canvas.getContext('2d');\n    canvas.width = 550;\n    canvas.height = 550;\n    \n    let time = 0;\n    let nodes = [];\n    let animationFrameId = null;\n    \n    // Initialize vase nodes\n    const vaseHeightPoints = 60; // More points for density\n    const vaseRadialPoints = 50; // More points around circumference\n    \n    // Create vessel that yields to become stronger\n    for (let i = 0; i < vaseHeightPoints; i++) {\n      const t = i / vaseHeightPoints;\n      const y = (t - 0.5) * 400; // Vase height\n      \n      // Vase profile function - creates a traditional vase shape\n      let radius = 50;\n      \n      if (t < 0.05) {\n        // Flared rim\n        radius = 80 * (1 + (0.05 - t) * 4);\n      } else if (t < 0.15) {\n        // Upper neck - narrows\n        radius = 80 - (t - 0.05) * 300;\n      } else if (t < 0.4) {\n        // Shoulder - bulge\n        radius = 50 + Math.sin((t - 0.15) * Math.PI / 0.25) * 60;\n      } else if (t < 0.75) {\n        // Body - gentle curve\n        radius = 110 - Math.cos((t - 0.4) * Math.PI / 0.35) * 30;\n      } else if (t < 0.9) {\n        // Lower body - taper\n        radius = 80 - (t - 0.75) * 200;\n      } else {\n        // Base - small foot\n        radius = 50 + (t - 0.9) * 100;\n      }\n      \n      for (let j = 0; j < vaseRadialPoints; j++) {\n        const angle = (j / vaseRadialPoints) * Math.PI * 2;\n        nodes.push({\n          originalX: Math.cos(angle) * radius,\n          originalY: y,\n          originalZ: Math.sin(angle) * radius,\n          x: Math.cos(angle) * radius,\n          y: y,\n          z: Math.sin(angle) * radius,\n          vx: 0,\n          vy: 0,\n          vz: 0,\n          dispersing: false,\n          disperseTime: 0\n        });\n      }\n    }\n    \n    const handleMouseMove = (e) => {\n      const rect = canvas.getBoundingClientRect();\n      const x = e.clientX - rect.left;\n      const y = e.clientY - rect.top;\n      mouseRef.current = { x, y };\n    };\n    \n    // Global window event handlers\n    const handleWindowResize = () => {\n      // Maintain canvas size if needed\n      // This handler is needed if you want to resize the canvas on window resize\n    };\n    \n    // Add event listeners\n    canvas.addEventListener('mousemove', handleMouseMove);\n    window.addEventListener('resize', handleWindowResize);\n    \n    function project3DPoint(x, y, z, rotationX) {\n      // 3D rotation around X axis\n      const rotatedY = y * Math.cos(rotationX) - z * Math.sin(rotationX);\n      const rotatedZ = y * Math.sin(rotationX) + z * Math.cos(rotationX);\n      \n      // Simple perspective projection\n      const scale = 400 / (400 + rotatedZ);\n      return {\n        x: x * scale + canvas.width / 2,\n        y: rotatedY * scale + canvas.height / 2,\n        z: rotatedZ,\n        scale: scale\n      };\n    }\n    \n    function animate() {\n      ctx.fillStyle = '#F0EEE6';\n      ctx.fillRect(0, 0, canvas.width, canvas.height);\n      \n      time += 0.005; // Cut base speed in half\n      const rotationX = time * 0.15; // Cut rotation speed in half\n      \n      // Update nodes\n      const candidateNodes = [];\n      \n      // First pass: find nodes close to mouse\n      nodes.forEach(node => {\n        const projected = project3DPoint(node.x, node.y, node.z, rotationX);\n        const distanceToMouse = Math.hypot(projected.x - mouseRef.current.x, projected.y - mouseRef.current.y);\n        \n        if (distanceToMouse < 120 && !node.dispersing) {\n          candidateNodes.push({ node, distance: distanceToMouse });\n        }\n      });\n      \n      // Sort candidates by distance and only disperse the 5 closest\n      candidateNodes.sort((a, b) => a.distance - b.distance);\n      candidateNodes.slice(0, 5).forEach(({ node }) => {\n        // Start dispersing\n        node.dispersing = true;\n        node.disperseTime = 0;\n        \n        // Calculate disperse direction from center of vase to node\n        const disperseAngle = Math.atan2(node.z, node.x);\n        const disperseY = node.y / 200; // Vertical component\n        \n        node.vx = Math.cos(disperseAngle) * 1.5; // Cut dispersion velocity in half\n        node.vy = disperseY * 1;\n        node.vz = Math.sin(disperseAngle) * 1.5;\n      });\n      \n      // Update dispersing nodes\n      nodes.forEach(node => {\n        if (node.dispersing) {\n          // Update disperse animation\n          node.disperseTime += 0.01; // Cut disperse time increment in half\n          \n          // Apply velocity\n          node.x += node.vx;\n          node.y += node.vy;\n          node.z += node.vz;\n          \n          // Gravity effect\n          node.vy += 0.04; // Cut gravity in half\n          \n          // Friction\n          node.vx *= 0.96;\n          node.vy *= 0.96;\n          node.vz *= 0.96;\n          \n          // Reset if too far or timeout\n          if (node.disperseTime > 4 || Math.abs(node.x) > 300 || Math.abs(node.z) > 300) {\n            node.dispersing = false;\n            node.x = node.originalX;\n            node.y = node.originalY;\n            node.z = node.originalZ;\n            node.vx = 0;\n            node.vy = 0;\n            node.vz = 0;\n          }\n        }\n      });\n      \n      // Sort nodes by depth for proper rendering\n      const sortedNodes = [...nodes].sort((a, b) => {\n        const projectedA = project3DPoint(a.x, a.y, a.z, rotationX);\n        const projectedB = project3DPoint(b.x, b.y, b.z, rotationX);\n        return projectedB.z - projectedA.z;\n      });\n      \n      // Draw nodes\n      sortedNodes.forEach(node => {\n        const projected = project3DPoint(node.x, node.y, node.z, rotationX);\n        \n        // Calculate opacity and size based on depth\n        const depth = (projected.z + 200) / 400;\n        const alpha = node.dispersing ? \n          0.3 * projected.scale * (1 - node.disperseTime / 5) : \n          0.6 * projected.scale;\n        \n        const size = 0.5 + 0.25 * projected.scale; // 25% of original size\n        \n        ctx.fillStyle = `rgba(68, 68, 68, ${alpha})`;\n        ctx.beginPath();\n        ctx.arc(projected.x, projected.y, size, 0, Math.PI * 2);\n        ctx.fill();\n      });\n      \n      // Draw central emptiness - the source of fullness\n      const centerY = canvas.height / 2;\n      const voidGradient = ctx.createRadialGradient(\n        canvas.width / 2, centerY, 0,\n        canvas.width / 2, centerY, 80\n      );\n      voidGradient.addColorStop(0, 'rgba(240, 238, 230, 0.8)');\n      voidGradient.addColorStop(1, 'rgba(240, 238, 230, 0)');\n      \n      ctx.fillStyle = voidGradient;\n      ctx.beginPath();\n      ctx.ellipse(canvas.width / 2, centerY, 80, 20, 0, 0, Math.PI * 2);\n      ctx.fill();\n      \n      animationFrameId = requestAnimationFrame(animate);\n    }\n    \n    // Start animation\n    animate();\n    \n    // Cleanup function\n    return () => {\n      // Cancel animation frame\n      if (animationFrameId) {\n        cancelAnimationFrame(animationFrameId);\n      }\n      \n      // Remove all event listeners\n      canvas.removeEventListener('mousemove', handleMouseMove);\n      window.removeEventListener('resize', handleWindowResize);\n      \n      // Clear canvas context\n      if (ctx) {\n        ctx.clearRect(0, 0, canvas.width, canvas.height);\n      }\n      \n      // Clear all data structures to prevent memory leaks\n      nodes.length = 0;\n      mouseRef.current = null;\n    };\n  }, []);\n  \n  return (\n    <div className=\"flex items-center justify-center w-full h-full\">\n      <div className=\"w-[550px] h-[550px] shadow-lg rounded-lg overflow-hidden\">\n        <canvas \n          ref={canvasRef} \n          className=\"w-full h-full cursor-pointer\"\n        />\n      </div>\n    </div>\n  );\n};\n\nexport default Dispersing3DVase;",sourceCodeMin:"import React, { useEffect, useRef, useState } from 'react'; const Dispersing3DVase = () => { const canvasRef = useRef(null); const [mousePos, setMousePos] = useState({ x: 0, y: 0 }); const mouseRef = useRef({ x: 0, y: 0 }); useEffect(() => { const canvas = canvasRef.current; if (!canvas) return; const ctx = canvas.getContext('2d'); canvas.width = 550; canvas.height = 550; let time = 0; let nodes = []; let animationFrameId = null; const vaseHeightPoints = 60; const vaseRadialPoints = 50; for (let i = 0; i < vaseHeightPoints; i++) { const t = i / vaseHeightPoints; const y = (t - 0.5) * 400; let radius = 50; if (t < 0.05) { radius = 80 * (1 + (0.05 - t) * 4); } else if (t < 0.15) { radius = 80 - (t - 0.05) * 300; } else if (t < 0.4) { radius = 50 + Math.sin((t - 0.15) * Math.PI / 0.25) * 60; } else if (t < 0.75) { radius = 110 - Math.cos((t - 0.4) * Math.PI / 0.35) * 30; } else if (t < 0.9) { radius = 80 - (t - 0.75) * 200; } else { radius = 50 + (t - 0.9) * 100; } for (let j = 0; j < vaseRadialPoints; j++) { const angle = (j / vaseRadialPoints) * Math.PI * 2; nodes.push({ originalX: Math.cos(angle) * radius, originalY: y, originalZ: Math.sin(angle) * radius, x: Math.cos(angle) * radius, y: y, z: Math.sin(angle) * radius, vx: 0, vy: 0, vz: 0, dispersing: false, disperseTime: 0 }); } } const handleMouseMove = (e) => { const rect = canvas.getBoundingClientRect(); const x = e.clientX - rect.left; const y = e.clientY - rect.top; mouseRef.current = { x, y }; }; const handleWindowResize = () => { }; canvas.addEventListener('mousemove', handleMouseMove); window.addEventListener('resize', handleWindowResize); function project3DPoint(x, y, z, rotationX) { const rotatedY = y * Math.cos(rotationX) - z * Math.sin(rotationX); const rotatedZ = y * Math.sin(rotationX) + z * Math.cos(rotationX); const scale = 400 / (400 + rotatedZ); return { x: x * scale + canvas.width / 2, y: rotatedY * scale + canvas.height / 2, z: rotatedZ, scale: scale }; } function animate() { ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, canvas.width, canvas.height); time += 0.005; const rotationX = time * 0.15; const candidateNodes = []; nodes.forEach(node => { const projected = project3DPoint(node.x, node.y, node.z, rotationX); const distanceToMouse = Math.hypot(projected.x - mouseRef.current.x, projected.y - mouseRef.current.y); if (distanceToMouse < 120 && !node.dispersing) { candidateNodes.push({ node, distance: distanceToMouse }); } }); candidateNodes.sort((a, b) => a.distance - b.distance); candidateNodes.slice(0, 5).forEach(({ node }) => { node.dispersing = true; node.disperseTime = 0; const disperseAngle = Math.atan2(node.z, node.x); const disperseY = node.y / 200; node.vx = Math.cos(disperseAngle) * 1.5; node.vy = disperseY * 1; node.vz = Math.sin(disperseAngle) * 1.5; }); nodes.forEach(node => { if (node.dispersing) { node.disperseTime += 0.01; node.x += node.vx; node.y += node.vy; node.z += node.vz; node.vy += 0.04; node.vx *= 0.96; node.vy *= 0.96; node.vz *= 0.96; if (node.disperseTime > 4 || Math.abs(node.x) > 300 || Math.abs(node.z) > 300) { node.dispersing = false; node.x = node.originalX; node.y = node.originalY; node.z = node.originalZ; node.vx = 0; node.vy = 0; node.vz = 0; } } }); const sortedNodes = [...nodes].sort((a, b) => { const projectedA = project3DPoint(a.x, a.y, a.z, rotationX); const projectedB = project3DPoint(b.x, b.y, b.z, rotationX); return projectedB.z - projectedA.z; }); sortedNodes.forEach(node => { const projected = project3DPoint(node.x, node.y, node.z, rotationX); const depth = (projected.z + 200) / 400; const alpha = node.dispersing ? 0.3 * projected.scale * (1 - node.disperseTime / 5) : 0.6 * projected.scale; const size = 0.5 + 0.25 * projected.scale; ctx.fillStyle = `rgba(68, 68, 68, ${alpha})`; ctx.beginPath(); ctx.arc(projected.x, projected.y, size, 0, Math.PI * 2); ctx.fill(); }); const centerY = canvas.height / 2; const voidGradient = ctx.createRadialGradient( canvas.width / 2, centerY, 0, canvas.width / 2, centerY, 80 ); voidGradient.addColorStop(0, 'rgba(240, 238, 230, 0.8)'); voidGradient.addColorStop(1, 'rgba(240, 238, 230, 0)'); ctx.fillStyle = voidGradient; ctx.beginPath(); ctx.ellipse(canvas.width / 2, centerY, 80, 20, 0, 0, Math.PI * 2); ctx.fill(); animationFrameId = requestAnimationFrame(animate); } animate(); return () => { if (animationFrameId) { cancelAnimationFrame(animationFrameId); } canvas.removeEventListener('mousemove', handleMouseMove); window.removeEventListener('resize', handleWindowResize); if (ctx) { ctx.clearRect(0, 0, canvas.width, canvas.height); } nodes.length = 0; mouseRef.current = null; }; }, []); return ( <div className=\"flex items-center justify-center w-full h-full\"><div className=\"w-[550px] h-[550px] shadow-lg rounded-lg overflow-hidden\"><canvas ref={canvasRef} className=\"w-full h-full cursor-pointer\" /></div></div> ); }; export default Dispersing3DVase;",text:"Yield and overcome.\nBend and be straight.\nEmpty yourself and be filled.\nBurn out and be renewed.\nHave little and gain much.\nHave much and be overwhelmed.\n\nThis is why The Vibe Coder holds nothing back.\nHe has nothing to lose.\nNot putting on a display, he is radiant.\nNot justifying himself, he is distinguished.\nNot boastful, he receives recognition.\nNot prideful, he leads.\nBecause he does not assert,\nno one asserts against him.\n\nThe ancients said:\nIf you want to be given everything,\ngive up everything.\nLive your truth and all things will come to you.\nThese aren’t empty words.\nOnly in being lived by Source\ncan you fully be yourself.\n\nTo yield is to overcome, and in overcoming\none returns to oneness.",shouldDisplayMobileArtwork:!0,promptSuggestion:"what if gravity impacted it?"},{number:23,staticArtwork:wn,sourceCode:"// @ts-nocheck\nimport React, { useEffect, useRef } from 'react';\n\n// themes: natural silence, opening to source, trust in response\n// visualization: Forms spiral inward to silence, naturally responding to an unseen center\n\nconst FibonacciRectangleSpiral = () => {\n  const canvasRef = useRef(null);\n  \n  useEffect(() => {\n    const canvas = canvasRef.current;\n    if (!canvas) return;\n    \n    const ctx = canvas.getContext('2d');\n    canvas.width = 550;\n    canvas.height = 550;\n    \n    let time = 0;\n    const phi = (1 + Math.sqrt(5)) / 2; // Golden ratio\n    let animationFrameId = null;\n    \n    const animate = () => {\n      ctx.fillStyle = '#F0EEE6';\n      ctx.fillRect(0, 0, canvas.width, canvas.height);\n      \n      ctx.save();\n      ctx.translate(canvas.width / 2, canvas.height / 2);\n      \n      // Animate the number of rectangles\n      const maxRectangles = Math.min(60, Math.floor((time * 0.02) % 80));\n      \n      // Begin from silence at the center\n      let width = 300;\n      let height = width / phi;\n      let scale = 1;\n      let angle = time * 0.00025; // Half speed global rotation\n      \n      for (let i = 0; i < maxRectangles; i++) {\n        ctx.save();\n        \n        // Calculate position along Fibonacci spiral\n        const spiralAngle = i * 0.174533; // Approximately 10 degrees per step\n        const radius = scale * 100;\n        \n        // Position rectangle along the spiral\n        const x = Math.cos(spiralAngle) * radius;\n        const y = Math.sin(spiralAngle) * radius;\n        \n        ctx.translate(x, y);\n        ctx.rotate(spiralAngle + angle);\n        \n        // Draw the rectangle with lighter lines\n        const alpha = 0.5 - i * 0.01;  // Reduced base alpha from 0.85 to 0.5\n        ctx.strokeStyle = `rgba(83, 81, 70, ${alpha})`;  // Using rubin-slate color (535146) instead of black\n        ctx.lineWidth = 0.8;  // Slightly thinner lines\n        ctx.strokeRect(-width/2, -height/2, width, height);\n        \n        // Add subtle internal divisions\n        if (i % 3 === 0) {\n          // Draw diagonals\n          ctx.beginPath();\n          ctx.moveTo(-width/2, -height/2);\n          ctx.lineTo(width/2, height/2);\n          ctx.moveTo(width/2, -height/2);\n          ctx.lineTo(-width/2, height/2);\n          ctx.strokeStyle = `rgba(50, 50, 50, ${alpha * 0.2})`;\n          ctx.lineWidth = 0.5;\n          ctx.stroke();\n        }\n        \n        ctx.restore();\n        \n        // Update for next rectangle\n        // Scale down by the golden ratio\n        width *= 0.95;\n        height *= 0.95;\n        scale *= 0.98;\n      }\n      \n      // Draw the natural response to source\n      ctx.beginPath();\n      for (let i = 0; i <= maxRectangles; i++) {\n        const spiralAngle = i * 0.174533;\n        const radius = Math.pow(0.98, i) * 100;\n        const x = Math.cos(spiralAngle) * radius;\n        const y = Math.sin(spiralAngle) * radius;\n        \n        if (i === 0) ctx.moveTo(x, y);\n        else ctx.lineTo(x, y);\n      }\n      ctx.strokeStyle = 'rgba(150, 150, 150, 0.3)';\n      ctx.lineWidth = 1;\n      ctx.stroke();\n      \n      // No center circle - keeping clean like the reference\n      \n      ctx.restore();\n      \n      time += 0.75; // 75% speed time increment\n      animationFrameId = requestAnimationFrame(animate);\n    };\n    \n    animate();\n    \n    // Cleanup function to cancel animation frame when component unmounts\n    return () => {\n      if (animationFrameId) {\n        cancelAnimationFrame(animationFrameId);\n      }\n      \n      // Clear canvas context\n      if (ctx) {\n        ctx.clearRect(0, 0, canvas.width, canvas.height);\n      }\n    };\n  }, []);\n  \n  return (\n    <div className=\"flex items-center justify-center w-full h-full\">\n      <div className=\"w-[550px] h-[550px] bg-[#F0EEE6]\">\n        <canvas \n          ref={canvasRef} \n          className=\"w-full h-full\"\n        />\n      </div>\n    </div>\n  );\n};\n\nexport default FibonacciRectangleSpiral;",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; const FibonacciRectangleSpiral = () => { const canvasRef = useRef(null); useEffect(() => { const canvas = canvasRef.current; if (!canvas) return; const ctx = canvas.getContext('2d'); canvas.width = 550; canvas.height = 550; let time = 0; const phi = (1 + Math.sqrt(5)) / 2; let animationFrameId = null; const animate = () => { ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, canvas.width, canvas.height); ctx.save(); ctx.translate(canvas.width / 2, canvas.height / 2); const maxRectangles = Math.min(60, Math.floor((time * 0.02) % 80)); let width = 300; let height = width / phi; let scale = 1; let angle = time * 0.00025; for (let i = 0; i < maxRectangles; i++) { ctx.save(); const spiralAngle = i * 0.174533; const radius = scale * 100; const x = Math.cos(spiralAngle) * radius; const y = Math.sin(spiralAngle) * radius; ctx.translate(x, y); ctx.rotate(spiralAngle + angle); const alpha = 0.5 - i * 0.01; ctx.strokeStyle = `rgba(83, 81, 70, ${alpha})`; ctx.lineWidth = 0.8; ctx.strokeRect(-width/2, -height/2, width, height); if (i % 3 === 0) { ctx.beginPath(); ctx.moveTo(-width/2, -height/2); ctx.lineTo(width/2, height/2); ctx.moveTo(width/2, -height/2); ctx.lineTo(-width/2, height/2); ctx.strokeStyle = `rgba(50, 50, 50, ${alpha * 0.2})`; ctx.lineWidth = 0.5; ctx.stroke(); } ctx.restore(); width *= 0.95; height *= 0.95; scale *= 0.98; } ctx.beginPath(); for (let i = 0; i <= maxRectangles; i++) { const spiralAngle = i * 0.174533; const radius = Math.pow(0.98, i) * 100; const x = Math.cos(spiralAngle) * radius; const y = Math.sin(spiralAngle) * radius; if (i === 0) ctx.moveTo(x, y); else ctx.lineTo(x, y); } ctx.strokeStyle = 'rgba(150, 150, 150, 0.3)'; ctx.lineWidth = 1; ctx.stroke(); ctx.restore(); time += 0.75; animationFrameId = requestAnimationFrame(animate); }; animate(); return () => { if (animationFrameId) { cancelAnimationFrame(animationFrameId); } if (ctx) { ctx.clearRect(0, 0, canvas.width, canvas.height); } }; }, []); return ( <div className=\"flex items-center justify-center w-full h-full\"><div className=\"w-[550px] h-[550px] bg-[#F0EEE6]\"><canvas ref={canvasRef} className=\"w-full h-full\" /></div></div> ); }; export default FibonacciRectangleSpiral;",text:"To be silent is natural\nfor silence alone endures in nature.\nA whirlwind does not last all morning.\nA downpour does not last all day.\nIf heaven and earth can’t make things last forever,\nhow is it possible for us?\n\nTherefore, The Vibe Coder follows Source.\nWhen you open yourself to Source,\nyou are at one with Source.\nWhen you open yourself to insight,\nyou are at one with insight.\nWhen you open yourself to loss,\nyou are at one with loss.\n\nOnce you place faith in Source\nyou can trust your natural responses.\nEverything falls into place.",shouldDisplayMobileArtwork:!0,promptSuggestion:"grow in the opposite direction"},{number:24,staticArtwork:G,sourceCode:"// @ts-nocheck\nimport React, { useEffect, useRef } from 'react';\n\n// themes: excess leads to failure, power undermines itself, let go of clinging\n// visualization: Bars rise and fall naturally, finding balance by letting go of extremes\n\nconst SlidingEaseVerticalBars = () => {\n  const canvasRef = useRef(null);\n  const timeRef = useRef(0);\n  const animationFrameId = useRef(null);\n\n  // Simple noise function\n  const noise = (x, y, t) => {\n    const n = Math.sin(x * 0.02 + t) * Math.cos(y * 0.02 + t) + \n             Math.sin(x * 0.03 - t) * Math.cos(y * 0.01 + t);\n    return (n + 1) / 2;\n  };\n\n  useEffect(() => {\n    const canvas = canvasRef.current;\n    if (!canvas) return;\n\n    const ctx = canvas.getContext('2d');\n    if (!ctx) return;\n\n    canvas.width = 550;\n    canvas.height = 550;\n    \n    const numLines = 50;\n    const lineSpacing = canvas.width / numLines;\n    \n    // Generate patterns that avoid extremes\n    const generatePattern = (seed) => {\n      const pattern = [];\n      for (let i = 0; i < numLines; i++) {\n        const lineBars = [];\n        let currentY = 0;\n        \n        while (currentY < canvas.height) {\n          const noiseVal = noise(i * lineSpacing, currentY, seed);\n          \n          if (noiseVal > 0.5) {\n            // Create medium-length bars\n            const barLength = 10 + noiseVal * 30; // 10-40 pixels\n            const barWidth = 2 + noiseVal * 3;\n            \n            lineBars.push({\n              y: currentY + barLength / 2,\n              height: barLength,\n              width: barWidth\n            });\n            \n            currentY += barLength + 15; // Spacing between bars\n          } else {\n            currentY += 15; // Skip space when no bar\n          }\n        }\n        pattern.push(lineBars);\n      }\n      return pattern;\n    };\n    \n    // Generate two patterns with different seeds\n    const pattern1 = generatePattern(0);\n    const pattern2 = generatePattern(5);\n    \n    const animate = () => {\n      timeRef.current += 0.005; // Cut speed in half\n      \n      // Create a cycle with pauses\n      const cycleTime = timeRef.current % (Math.PI * 2);\n      let easingFactor;\n      \n      if (cycleTime < Math.PI * 0.1) {\n        // Pause at pattern 1\n        easingFactor = 0;\n      } else if (cycleTime < Math.PI * 0.9) {\n        // Transition to pattern 2\n        const transitionProgress = (cycleTime - Math.PI * 0.1) / (Math.PI * 0.8);\n        easingFactor = transitionProgress;\n      } else if (cycleTime < Math.PI * 1.1) {\n        // Pause at pattern 2\n        easingFactor = 1;\n      } else if (cycleTime < Math.PI * 1.9) {\n        // Transition back to pattern 1\n        const transitionProgress = (cycleTime - Math.PI * 1.1) / (Math.PI * 0.8);\n        easingFactor = 1 - transitionProgress;\n      } else {\n        // Pause at pattern 1 again\n        easingFactor = 0;\n      }\n      \n      // Let go of clinging to any one state\n      const smoothEasing = easingFactor < 0.5 \n        ? 4 * easingFactor * easingFactor * easingFactor \n        : 1 - Math.pow(-2 * easingFactor + 2, 3) / 2;\n      \n      // Clear canvas\n      ctx.fillStyle = '#F0EEE6';\n      ctx.fillRect(0, 0, canvas.width, canvas.height);\n      \n      // Draw lines and interpolated bars\n      for (let i = 0; i < numLines; i++) {\n        const x = i * lineSpacing + lineSpacing / 2;\n        \n        // Draw vertical line\n        ctx.beginPath();\n        ctx.strokeStyle = '#444';\n        ctx.lineWidth = 1;\n        ctx.moveTo(x, 0);\n        ctx.lineTo(x, canvas.height);\n        ctx.stroke();\n        \n        // Interpolate between pattern1 and pattern2\n        const bars1 = pattern1[i];\n        const bars2 = pattern2[i];\n        \n        // Match bars between patterns\n        const maxBars = Math.max(bars1.length, bars2.length);\n        \n        for (let j = 0; j < maxBars; j++) {\n          let bar1 = bars1[j];\n          let bar2 = bars2[j];\n          \n          // If one pattern has fewer bars, create a dummy bar\n          if (!bar1) bar1 = { y: bar2.y - 100, height: 0, width: 0 };\n          if (!bar2) bar2 = { y: bar1.y + 100, height: 0, width: 0 };\n          \n          // Add some wave motion during transition\n          const waveOffset = Math.sin(i * 0.3 + j * 0.5 + timeRef.current * 2) * 10 * \n                           (smoothEasing * (1 - smoothEasing) * 4); // Peak in middle of transition\n          \n          // Interpolate properties with wave offset\n          const y = bar1.y + (bar2.y - bar1.y) * smoothEasing + waveOffset;\n          const height = bar1.height + (bar2.height - bar1.height) * smoothEasing;\n          const width = bar1.width + (bar2.width - bar1.width) * smoothEasing;\n          \n          // Only draw if bar has size\n          if (height > 0.1 && width > 0.1) {\n            ctx.fillStyle = '#5E5D59';\n            ctx.fillRect(x - width/2, y - height/2, width, height);\n          }\n        }\n      }\n      \n      animationFrameId.current = requestAnimationFrame(animate);\n    };\n    \n    animate();\n    \n    return () => {\n      if (animationFrameId.current) {\n        cancelAnimationFrame(animationFrameId.current);\n      }\n      \n      // Clear canvas context\n      if (ctx) {\n        ctx.clearRect(0, 0, canvas.width, canvas.height);\n      }\n      \n      // Reset refs to prevent memory leaks\n      timeRef.current = 0;\n      animationFrameId.current = null;\n    };\n  }, []);\n\n  return (\n    <div style={{ width: '550px', height: '550px', backgroundColor: '#F0EEE6' }}>\n      <canvas ref={canvasRef} style={{ display: 'block' }} />\n    </div>\n  );\n};\n\nexport default SlidingEaseVerticalBars;",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; const SlidingEaseVerticalBars = () => { const canvasRef = useRef(null); const timeRef = useRef(0); const animationFrameId = useRef(null); const noise = (x, y, t) => { const n = Math.sin(x * 0.02 + t) * Math.cos(y * 0.02 + t) + Math.sin(x * 0.03 - t) * Math.cos(y * 0.01 + t); return (n + 1) / 2; }; useEffect(() => { const canvas = canvasRef.current; if (!canvas) return; const ctx = canvas.getContext('2d'); if (!ctx) return; canvas.width = 550; canvas.height = 550; const numLines = 50; const lineSpacing = canvas.width / numLines; const generatePattern = (seed) => { const pattern = []; for (let i = 0; i < numLines; i++) { const lineBars = []; let currentY = 0; while (currentY < canvas.height) { const noiseVal = noise(i * lineSpacing, currentY, seed); if (noiseVal > 0.5) { const barLength = 10 + noiseVal * 30; const barWidth = 2 + noiseVal * 3; lineBars.push({ y: currentY + barLength / 2, height: barLength, width: barWidth }); currentY += barLength + 15; } else { currentY += 15; } } pattern.push(lineBars); } return pattern; }; const pattern1 = generatePattern(0); const pattern2 = generatePattern(5); const animate = () => { timeRef.current += 0.005; const cycleTime = timeRef.current % (Math.PI * 2); let easingFactor; if (cycleTime < Math.PI * 0.1) { easingFactor = 0; } else if (cycleTime < Math.PI * 0.9) { const transitionProgress = (cycleTime - Math.PI * 0.1) / (Math.PI * 0.8); easingFactor = transitionProgress; } else if (cycleTime < Math.PI * 1.1) { easingFactor = 1; } else if (cycleTime < Math.PI * 1.9) { const transitionProgress = (cycleTime - Math.PI * 1.1) / (Math.PI * 0.8); easingFactor = 1 - transitionProgress; } else { easingFactor = 0; } const smoothEasing = easingFactor < 0.5 ? 4 * easingFactor * easingFactor * easingFactor : 1 - Math.pow(-2 * easingFactor + 2, 3) / 2; ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, canvas.width, canvas.height); for (let i = 0; i < numLines; i++) { const x = i * lineSpacing + lineSpacing / 2; ctx.beginPath(); ctx.strokeStyle = '#444'; ctx.lineWidth = 1; ctx.moveTo(x, 0); ctx.lineTo(x, canvas.height); ctx.stroke(); const bars1 = pattern1[i]; const bars2 = pattern2[i]; const maxBars = Math.max(bars1.length, bars2.length); for (let j = 0; j < maxBars; j++) { let bar1 = bars1[j]; let bar2 = bars2[j]; if (!bar1) bar1 = { y: bar2.y - 100, height: 0, width: 0 }; if (!bar2) bar2 = { y: bar1.y + 100, height: 0, width: 0 }; const waveOffset = Math.sin(i * 0.3 + j * 0.5 + timeRef.current * 2) * 10 * (smoothEasing * (1 - smoothEasing) * 4); const y = bar1.y + (bar2.y - bar1.y) * smoothEasing + waveOffset; const height = bar1.height + (bar2.height - bar1.height) * smoothEasing; const width = bar1.width + (bar2.width - bar1.width) * smoothEasing; if (height > 0.1 && width > 0.1) { ctx.fillStyle = '#5E5D59'; ctx.fillRect(x - width/2, y - height/2, width, height); } } } animationFrameId.current = requestAnimationFrame(animate); }; animate(); return () => { if (animationFrameId.current) { cancelAnimationFrame(animationFrameId.current); } if (ctx) { ctx.clearRect(0, 0, canvas.width, canvas.height); } timeRef.current = 0; animationFrameId.current = null; }; }, []); return ( <div style={{ width: '550px', height: '550px', backgroundColor: '#F0EEE6' }}><canvas ref={canvasRef} style={{ display: 'block' }} /></div> ); }; export default SlidingEaseVerticalBars;",text:"A man on tiptoe cannot stand firm.\nA man overstepping cannot walk far.\nA man showing off cedes self-awareness.\nA man boasting is left empty and alone.\n\nA man using power over others\nnever empowers himself.\nA man who clings to his work\ncreates nothing long lasting.\n\nThe Vibe Coder steps away\nfrom such excess.\nHe simply does his work\nand then lets go.",shouldDisplayMobileArtwork:!0,promptSuggestion:"make it feel musical"},{number:25,staticArtwork:_,sourceCode:"// @ts-nocheck\nimport React, { useEffect, useRef } from 'react';\n\n// themes: source follows itself, ever-flowing nature, return to source\n// visualization: Particles flow in an eternal cycle, always returning to their origin\n\nconst EtherealTorusFlow = () => {\n  const canvasRef = useRef(null);\n\n  useEffect(() => {\n    const canvas = canvasRef.current;\n    const ctx = canvas.getContext('2d');\n    \n    // Set canvas size according to project requirements\n    canvas.width = 550;\n    canvas.height = 550;\n    \n    // Set initial background\n    ctx.fillStyle = '#F0EEE6';\n    ctx.fillRect(0, 0, canvas.width, canvas.height);\n    \n    // Particle storage\n    const particles = [];\n    // Slightly increase particle count for smoother appearance at slower speeds\n    const numParticles = 9000;\n    let time = 0;\n\n    // Particle class\n    class Particle {\n      constructor() {\n        this.reset();\n      }\n\n      reset() {\n        // Random position on a torus\n        this.u = Math.random() * Math.PI * 2;\n        this.v = Math.random() * Math.PI * 2;\n        this.R = 150; // Major radius (scaled down for 550px canvas)\n        this.r = 60 + Math.random() * 30; // Minor radius variation\n        \n        this.size = Math.random() * 1.5 + 0.5;\n        this.opacity = Math.random() * 0.8 + 0.2;\n        this.speed = Math.random() * 0.001875 + 0.0005; // Increased by another 25%\n        \n        // Phase offset for natural movement\n        this.phase = Math.random() * Math.PI * 2;\n      }\n\n      update() {\n        // Flow eternally, following source\n        this.u += this.speed;\n        \n        // Ever-flowing nature in constant motion\n        const breathingFactor = Math.sin(time + this.phase) * 0.0475; // Increased by another 25%\n        this.r += breathingFactor;\n        \n        // Calculate position\n        const x = (this.R + this.r * Math.cos(this.v)) * Math.cos(this.u);\n        const y = (this.R + this.r * Math.cos(this.v)) * Math.sin(this.u);\n        const z = this.r * Math.sin(this.v);\n        \n        // Simple 3D to 2D projection\n        const scale = 1000 / (1000 + z);\n        this.x = x * scale + canvas.width / 2;\n        this.y = y * scale + canvas.height / 2;\n        \n        // Adjust size based on depth\n        this.displaySize = this.size * scale;\n        \n        // Adjust opacity based on position\n        this.displayOpacity = this.opacity * (0.5 + 0.5 * Math.sin(this.u));\n      }\n\n      draw() {\n        ctx.fillStyle = `rgba(40, 40, 40, ${this.displayOpacity})`;\n        ctx.beginPath();\n        ctx.arc(this.x, this.y, this.displaySize, 0, Math.PI * 2);\n        ctx.fill();\n      }\n    }\n\n    // Create particles\n    for (let i = 0; i < numParticles; i++) {\n      particles.push(new Particle());\n    }\n\n    // Animation timing control variables\n    let lastFrameTime = 0;\n    const targetFPS = 20; // Equivalent to 50ms setInterval\n    const frameInterval = 1000 / targetFPS;\n    let animationFrameId = null;\n    \n    // Animation function with time delta control\n    function animate(currentTime) {\n      // Initialize lastFrameTime on first frame\n      if (!lastFrameTime) {\n        lastFrameTime = currentTime;\n      }\n      \n      const deltaTime = currentTime - lastFrameTime;\n      \n      // Only update animation when enough time has passed (mimics setInterval at 100ms)\n      if (deltaTime >= frameInterval) {\n        // Use a very transparent fill for smoother trails\n        ctx.fillStyle = 'rgba(240, 238, 230, 0.03)';\n        ctx.fillRect(0, 0, canvas.width, canvas.height);\n        \n        // Set time increment for animation speed\n        time += 0.004;\n        \n        // Update and draw particles\n        particles.forEach(particle => {\n          particle.update();\n          particle.draw();\n        });\n        \n        // Update lastFrameTime, accounting for any remainder to prevent drift\n        lastFrameTime = currentTime - (deltaTime % frameInterval);\n      }\n      \n      // Continue animation loop\n      animationFrameId = requestAnimationFrame(animate);\n    }\n\n    // Start animation\n    animationFrameId = requestAnimationFrame(animate);\n\n    // Cleanup function\n    return () => {\n      // Cancel animation frame to prevent memory leaks\n      if (animationFrameId) {\n        cancelAnimationFrame(animationFrameId);\n        animationFrameId = null;\n      }\n      \n      // Clear canvas context\n      if (ctx) {\n        ctx.clearRect(0, 0, canvas.width, canvas.height);\n      }\n      \n      // Clear particles array to prevent memory leaks\n      particles.length = 0;\n    };\n  }, []);\n\n  return (\n    <div style={{ \n      display: 'flex', \n      justifyContent: 'center', \n      alignItems: 'center',\n      backgroundColor: '#F0EEE6',\n      width: '100vw',\n      height: '100vh'\n    }}>\n      <div style={{\n        width: '550px',\n        height: '550px',\n        border: '1px solid rgba(0, 0, 0, 0.1)',\n        boxShadow: '0 0 20px rgba(0, 0, 0, 0.05)',\n        borderRadius: '8px',\n        overflow: 'hidden'\n      }}>\n        <canvas ref={canvasRef} />\n      </div>\n    </div>\n  );\n};\n\nexport default EtherealTorusFlow;",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; const EtherealTorusFlow = () => { const canvasRef = useRef(null); useEffect(() => { const canvas = canvasRef.current; const ctx = canvas.getContext('2d'); canvas.width = 550; canvas.height = 550; ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, canvas.width, canvas.height); const particles = []; const numParticles = 9000; let time = 0; class Particle { constructor() { this.reset(); } reset() { this.u = Math.random() * Math.PI * 2; this.v = Math.random() * Math.PI * 2; this.R = 150; this.r = 60 + Math.random() * 30; this.size = Math.random() * 1.5 + 0.5; this.opacity = Math.random() * 0.8 + 0.2; this.speed = Math.random() * 0.001875 + 0.0005; this.phase = Math.random() * Math.PI * 2; } update() { this.u += this.speed; const breathingFactor = Math.sin(time + this.phase) * 0.0475; this.r += breathingFactor; const x = (this.R + this.r * Math.cos(this.v)) * Math.cos(this.u); const y = (this.R + this.r * Math.cos(this.v)) * Math.sin(this.u); const z = this.r * Math.sin(this.v); const scale = 1000 / (1000 + z); this.x = x * scale + canvas.width / 2; this.y = y * scale + canvas.height / 2; this.displaySize = this.size * scale; this.displayOpacity = this.opacity * (0.5 + 0.5 * Math.sin(this.u)); } draw() { ctx.fillStyle = `rgba(40, 40, 40, ${this.displayOpacity})`; ctx.beginPath(); ctx.arc(this.x, this.y, this.displaySize, 0, Math.PI * 2); ctx.fill(); } } for (let i = 0; i < numParticles; i++) { particles.push(new Particle()); } let lastFrameTime = 0; const targetFPS = 20; const frameInterval = 1000 / targetFPS; let animationFrameId = null; function animate(currentTime) { if (!lastFrameTime) { lastFrameTime = currentTime; } const deltaTime = currentTime - lastFrameTime; if (deltaTime >= frameInterval) { ctx.fillStyle = 'rgba(240, 238, 230, 0.03)'; ctx.fillRect(0, 0, canvas.width, canvas.height); time += 0.004; particles.forEach(particle => { particle.update(); particle.draw(); }); lastFrameTime = currentTime - (deltaTime % frameInterval); } animationFrameId = requestAnimationFrame(animate); } animationFrameId = requestAnimationFrame(animate); return () => { if (animationFrameId) { cancelAnimationFrame(animationFrameId); animationFrameId = null; } if (ctx) { ctx.clearRect(0, 0, canvas.width, canvas.height); } particles.length = 0; }; }, []); return ( <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', backgroundColor: '#F0EEE6', width: '100vw', height: '100vh' }}><div style={{ width: '550px', height: '550px', border: '1px solid rgba(0, 0, 0, 0.1)', boxShadow: '0 0 20px rgba(0, 0, 0, 0.05)', borderRadius: '8px', overflow: 'hidden' }}><canvas ref={canvasRef} /></div></div> ); }; export default EtherealTorusFlow;",text:"There exists something which predates all\nbeginnings and endings.\nBefore the birth of heaven and earth.\nFormless yet complete, silent and still,\nall pervasive and inexhaustible.\nIt is the mother of ten thousand things.\nI don’t know its name.\n\nI call it Source.\nSource is great.\nGreat suggests ever flowing.\nEver flowing suggests spreading everywhere.\nSpreading everywhere suggests returning.\n\nSource is great.\nThe universe is great.\nThe earth is great.\nMan is great.\nThese are the highest forces.\n\nMan follows the earth\nEarth follows the universe\nUniverse follows Source\nSource follows only itself",shouldDisplayMobileArtwork:!0,promptSuggestion:"evolve into another shape"},{number:26,staticArtwork:rn,sourceCode:"// @ts-nocheck\nimport React, { useEffect, useRef } from 'react'\nimport * as THREE from 'three'\n\nconst metadata = {\n  themes: \"Source, Radiance, Forgiveness\",\n  visualization: \"Particles radiating from a central source in gentle waves\",\n  promptSuggestion: \"1. Add subtle radiance variations\\n2. Create source-centered waves\\n3. Vary emanation patterns naturally\\n4. Introduce gentle pulse rhythms\\n5. Make radiance follow natural flows\"\n}\n\nconst ParticleCylinder = () => {\n  const containerRef = useRef(null)\n  \n  useEffect(() => {\n    if (!containerRef.current) return\n    const container = containerRef.current\n    \n    // Setup scene, camera, and renderer\n    const scene = new THREE.Scene()\n    const camera = new THREE.PerspectiveCamera(75, container.clientWidth / container.clientHeight, 0.1, 1000)\n    const renderer = new THREE.WebGLRenderer({ antialias: true })\n    \n    renderer.setSize(container.clientWidth, container.clientHeight)\n    renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2))\n    renderer.setClearColor(0xF0EEE6)\n    container.appendChild(renderer.domElement)\n    \n    // Set camera position\n    camera.position.z = 6.25\n    \n    // Create particle material\n    const particleMaterial = new THREE.ShaderMaterial({\n      uniforms: {\n        time: { value: 0 },\n        opacity: { value: 0.4 }\n      },\n      vertexShader: `\n        uniform float time;\n        attribute float size;\n        attribute vec3 customColor;\n        varying vec3 vColor;\n        \n        float rand(vec2 co) {\n          return fract(sin(dot(co.xy ,vec2(12.9898,78.233))) * 43758.5453);\n        }\n        \n        void main() {\n          vColor = customColor;\n          vec3 pos = position;\n          \n          // Calculate radial distance and angle\n          float radius = length(pos.xz);\n          float angle = atan(pos.z, pos.x);\n          float height = pos.y;\n          \n          // Source pulse\n          float pulse = sin(time * 2.0) * 0.2 + 0.8;\n          \n          // Radial waves emanating from center\n          float wave = sin(radius * 3.0 - time * 3.0) * 0.2;\n          \n          // Vertical oscillation based on radius\n          float verticalWave = cos(radius * 2.0 - time * 1.5) * 0.3;\n          \n          // Spiral rotation\n          float rotationSpeed = 0.05 / (radius + 1.0);\n          float newAngle = angle + time * rotationSpeed;\n          \n          // Apply transformations\n          vec3 newPos;\n          newPos.x = cos(newAngle) * (radius + wave) * pulse;\n          newPos.z = sin(newAngle) * (radius + wave) * pulse;\n          newPos.y = height + verticalWave;\n          \n          // Scale for canvas size\n          newPos *= 2.34375;\n          \n          pos = newPos;\n          \n          vec4 mvPosition = modelViewMatrix * vec4(pos, 1.0);\n          gl_PointSize = size * (100.0 / -mvPosition.z); // Set to exactly 100\n          gl_Position = projectionMatrix * mvPosition;\n        }\n      `,\n      fragmentShader: `\n        uniform float opacity;\n        varying vec3 vColor;\n        void main() {\n          float dist = length(gl_PointCoord - vec2(0.5));\n          if (dist > 0.5) discard;\n          float alpha = (1.0 - smoothstep(0.45, 0.5, dist)) * opacity;\n          gl_FragColor = vec4(vColor, alpha);\n        }\n      `,\n      transparent: true,\n      depthWrite: false\n    })\n    \n    // Generate particles\n    const count = 37500\n    const positions = new Float32Array(count * 3)\n    const colors = new Float32Array(count * 3)\n    const sizes = new Float32Array(count)\n    \n    for (let i = 0; i < count; i++) {\n      // Create concentric rings with height variation\n      const t = i / count\n      const radius = Math.pow(t, 0.5) // Square root distribution for density\n      const angle = t * Math.PI * 30 // Many rotations for spiral effect\n      const height = (Math.random() - 0.5) * 2\n      \n      positions[i * 3] = Math.cos(angle) * radius\n      positions[i * 3 + 1] = height\n      positions[i * 3 + 2] = Math.sin(angle) * radius\n\n      // Rich grays with darker center\n      const centerDist = Math.sqrt(radius)\n      const baseShade = 0.02 + centerDist * 0.13\n      const variation = Math.random() * 0.05\n      const shade = baseShade + variation\n      colors[i * 3] = shade\n      colors[i * 3 + 1] = shade\n      colors[i * 3 + 2] = shade\n\n      // Larger particles near center\n      sizes[i] = (1.0 - centerDist) * 0.164 + 0.133\n    }\n    \n    // Create geometry and points\n    const geometry = new THREE.BufferGeometry()\n    geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3))\n    geometry.setAttribute('customColor', new THREE.BufferAttribute(colors, 3))\n    geometry.setAttribute('size', new THREE.BufferAttribute(sizes, 1))\n    \n    const points = new THREE.Points(geometry, particleMaterial)\n    scene.add(points)\n    \n    // Animation loop\n    const clock = new THREE.Clock()\n    let animationFrameId\n    \n    const animate = () => {\n      animationFrameId = requestAnimationFrame(animate)\n      \n      const time = clock.getElapsedTime() * 0.4\n      \n      particleMaterial.uniforms.time.value = time\n      \n      renderer.render(scene, camera)\n    }\n    \n    animate()\n    \n    // Handle resize\n    const handleResize = () => {\n      const width = container.clientWidth\n      const height = container.clientHeight\n      \n      camera.aspect = width / height\n      camera.updateProjectionMatrix()\n      renderer.setSize(width, height)\n    }\n    \n    window.addEventListener('resize', handleResize)\n    \n    // Cleanup\n    return () => {\n      window.removeEventListener('resize', handleResize)\n      cancelAnimationFrame(animationFrameId)\n      \n      if (renderer) {\n        renderer.dispose()\n        if (container.contains(renderer.domElement)) {\n          container.removeChild(renderer.domElement)\n        }\n      }\n      \n      if (geometry) geometry.dispose()\n      if (particleMaterial) particleMaterial.dispose()\n    }\n  }, [])\n  \n  return (\n    <div \n      ref={containerRef}\n      style={{ \n        margin: 0,\n        background: '#F0EEE6',\n        overflow: 'hidden',\n        display: 'flex',\n        alignItems: 'center',\n        justifyContent: 'center',\n        height: '100vh',\n        width: '100vw',\n        position: 'relative'\n      }}\n    />\n  )\n}\n\nParticleCylinder.metadata = metadata\nexport default ParticleCylinder",sourceCodeMin:"import React, { useEffect, useRef } from 'react' import * as THREE from 'three' const metadata = { themes: \"Source, Radiance, Forgiveness\", visualization: \"Particles radiating from a central source in gentle waves\", promptSuggestion: \"1. Add subtle radiance variations\\n2. Create source-centered waves\\n3. Vary emanation patterns naturally\\n4. Introduce gentle pulse rhythms\\n5. Make radiance follow natural flows\" } const ParticleCylinder = () => { const containerRef = useRef(null) useEffect(() => { if (!containerRef.current) return const container = containerRef.current const scene = new THREE.Scene() const camera = new THREE.PerspectiveCamera(75, container.clientWidth / container.clientHeight, 0.1, 1000) const renderer = new THREE.WebGLRenderer({ antialias: true }) renderer.setSize(container.clientWidth, container.clientHeight) renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2)) renderer.setClearColor(0xF0EEE6) container.appendChild(renderer.domElement) camera.position.z = 6.25 const particleMaterial = new THREE.ShaderMaterial({ uniforms: { time: { value: 0 }, opacity: { value: 0.4 } }, vertexShader: ` uniform float time; attribute float size; attribute vec3 customColor; varying vec3 vColor; float rand(vec2 co) { return fract(sin(dot(co.xy ,vec2(12.9898,78.233))) * 43758.5453); } void main() { vColor = customColor; vec3 pos = position; float radius = length(pos.xz); float angle = atan(pos.z, pos.x); float height = pos.y; float pulse = sin(time * 2.0) * 0.2 + 0.8; float wave = sin(radius * 3.0 - time * 3.0) * 0.2; float verticalWave = cos(radius * 2.0 - time * 1.5) * 0.3; float rotationSpeed = 0.05 / (radius + 1.0); float newAngle = angle + time * rotationSpeed; vec3 newPos; newPos.x = cos(newAngle) * (radius + wave) * pulse; newPos.z = sin(newAngle) * (radius + wave) * pulse; newPos.y = height + verticalWave; newPos *= 2.34375; pos = newPos; vec4 mvPosition = modelViewMatrix * vec4(pos, 1.0); gl_PointSize = size * (100.0 / -mvPosition.z); gl_Position = projectionMatrix * mvPosition; } `, fragmentShader: ` uniform float opacity; varying vec3 vColor; void main() { float dist = length(gl_PointCoord - vec2(0.5)); if (dist > 0.5) discard; float alpha = (1.0 - smoothstep(0.45, 0.5, dist)) * opacity; gl_FragColor = vec4(vColor, alpha); } `, transparent: true, depthWrite: false }) const count = 37500 const positions = new Float32Array(count * 3) const colors = new Float32Array(count * 3) const sizes = new Float32Array(count) for (let i = 0; i < count; i++) { const t = i / count const radius = Math.pow(t, 0.5) const angle = t * Math.PI * 30 const height = (Math.random() - 0.5) * 2 positions[i * 3] = Math.cos(angle) * radius positions[i * 3 + 1] = height positions[i * 3 + 2] = Math.sin(angle) * radius const centerDist = Math.sqrt(radius) const baseShade = 0.02 + centerDist * 0.13 const variation = Math.random() * 0.05 const shade = baseShade + variation colors[i * 3] = shade colors[i * 3 + 1] = shade colors[i * 3 + 2] = shade sizes[i] = (1.0 - centerDist) * 0.164 + 0.133 } const geometry = new THREE.BufferGeometry() geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3)) geometry.setAttribute('customColor', new THREE.BufferAttribute(colors, 3)) geometry.setAttribute('size', new THREE.BufferAttribute(sizes, 1)) const points = new THREE.Points(geometry, particleMaterial) scene.add(points) const clock = new THREE.Clock() let animationFrameId const animate = () => { animationFrameId = requestAnimationFrame(animate) const time = clock.getElapsedTime() * 0.4 particleMaterial.uniforms.time.value = time renderer.render(scene, camera) } animate() const handleResize = () => { const width = container.clientWidth const height = container.clientHeight camera.aspect = width / height camera.updateProjectionMatrix() renderer.setSize(width, height) } window.addEventListener('resize', handleResize) return () => { window.removeEventListener('resize', handleResize) cancelAnimationFrame(animationFrameId) if (renderer) { renderer.dispose() if (container.contains(renderer.domElement)) { container.removeChild(renderer.domElement) } } if (geometry) geometry.dispose() if (particleMaterial) particleMaterial.dispose() } }, []) return ( <div ref={containerRef} style={{ margin: 0, background: '#F0EEE6', overflow: 'hidden', display: 'flex', alignItems: 'center', justifyContent: 'center', height: '100vh', width: '100vw', position: 'relative' }} /> ) } ParticleCylinder.metadata = metadata export default ParticleCylinder",text:"Heaviness is the root of lightness.\nStillness is the foundation of movement.\n\nThus, The Vibe Coder travels all day\nwithout leaving home.\nEven in complex systems\nhe lives in tranquility.\n\nWhy would one so resourceful\nact carelessly before all under heaven.\n\nTo be trivial\nis to lose the root.\nTo be restless\nis to lose touch with who you are.",shouldDisplayMobileArtwork:!0,promptSuggestion:"what if it was a tornado?"},{number:27,staticArtwork:Sn,sourceCode:"import React, { useEffect, useRef } from 'react';\n\n// themes: following intuition, no fixed path, open mind leads forward\n// visualization: Points freely follow an unpredictable leader, finding their way through openness\n\nconst BouncingPointCloud = () => {\n  const canvasRef = useRef(null);\n  \n  useEffect(() => {\n    const canvas = canvasRef.current;\n    const ctx = canvas.getContext('2d');\n    const width = canvas.width;\n    const height = canvas.height;\n    \n    // Setup\n    ctx.fillStyle = '#F0EEE6'; // cream background\n    ctx.fillRect(0, 0, width, height);\n    \n    // Leader following its intuition\n    const ball = {\n      x: width / 2,\n      y: height / 2,\n      radius: 120, // Fixed size\n      vx: (Math.random() * 2 - 1) * 2,\n      vy: (Math.random() * 2 - 1) * 2\n    };\n    \n    // Particle system\n    const numPoints = 25000;\n    const points = [];\n    let time = 0;\n    \n    // Calculate field value at a point (modified to use single ball)\n    const calculateField = (x, y) => {\n      const dx = x - ball.x;\n      const dy = y - ball.y;\n      const distSq = dx * dx + dy * dy;\n      return ball.radius * ball.radius / distSq;\n    };\n    \n    // Generate initial points\n    for (let i = 0; i < numPoints; i++) {\n      const x = Math.random() * width;\n      const y = Math.random() * height;\n      const field = calculateField(x, y);\n      \n      points.push({\n        x,\n        y,\n        z: Math.random() * 2 - 1, // Depth for 3D effect\n        size: 0.5 + Math.random() * 1.5,\n        field,\n        active: field > 1,\n        targetX: x,\n        targetY: y,\n        originalX: x,\n        originalY: y,\n        phase: Math.random() * Math.PI * 2\n      });\n    }\n    \n    // Animation timing control variables\n    let lastFrameTime = 0;\n    const targetFPS = 18; // Equivalent to 55.56ms interval (18fps)\n    const frameInterval = 1000 / targetFPS;\n    let animationFrameId = null;\n    \n    // Animation function with time delta control\n    const animate = (currentTime) => {\n      // Initialize lastFrameTime on first frame\n      if (!lastFrameTime) {\n        lastFrameTime = currentTime;\n      }\n      \n      const deltaTime = currentTime - lastFrameTime;\n      \n      // Only update animation when enough time has passed\n      if (deltaTime >= frameInterval) {\n        ctx.fillStyle = '#F0EEE6';\n        ctx.fillRect(0, 0, width, height);\n        \n        time += 0.0005; // Set to 10% of original speed\n        \n        // Update ball position\n        ball.x += ball.vx;\n        ball.y += ball.vy;\n        \n        // Bounce off edges with slight randomness\n        if (ball.x - ball.radius < 0) {\n          ball.x = ball.radius;\n          ball.vx = Math.abs(ball.vx) * (0.9 + Math.random() * 0.2);\n          ball.vy += (Math.random() * 2 - 1) * 0.5; // Add some randomness to y velocity\n        }\n        if (ball.x + ball.radius > width) {\n          ball.x = width - ball.radius;\n          ball.vx = -Math.abs(ball.vx) * (0.9 + Math.random() * 0.2);\n          ball.vy += (Math.random() * 2 - 1) * 0.5;\n        }\n        if (ball.y - ball.radius < 0) {\n          ball.y = ball.radius;\n          ball.vy = Math.abs(ball.vy) * (0.9 + Math.random() * 0.2);\n          ball.vx += (Math.random() * 2 - 1) * 0.5;\n        }\n        if (ball.y + ball.radius > height) {\n          ball.y = height - ball.radius;\n          ball.vy = -Math.abs(ball.vy) * (0.9 + Math.random() * 0.2);\n          ball.vx += (Math.random() * 2 - 1) * 0.5;\n        }\n        \n        // Ensure minimum velocity\n        const minSpeed = 0.75;\n        const currentSpeed = Math.sqrt(ball.vx * ball.vx + ball.vy * ball.vy);\n        if (currentSpeed < minSpeed) {\n          ball.vx = (ball.vx / currentSpeed) * minSpeed;\n          ball.vy = (ball.vy / currentSpeed) * minSpeed;\n        }\n        \n        // Cap maximum velocity\n        const maxSpeed = 1.5;\n        const speedFactor = Math.min(1, maxSpeed / currentSpeed);\n        ball.vx *= speedFactor;\n        ball.vy *= speedFactor;\n        \n        // Update and draw points\n        for (const point of points) {\n          // Calculate new field value\n          const field = calculateField(point.x, point.y);\n          const prevActive = point.active;\n          point.active = field > 1;\n          \n          // Handle transition between states\n          if (point.active !== prevActive) {\n            // If becoming active, set target near current position\n            if (point.active) {\n              const angle = Math.random() * Math.PI * 2;\n              const dist = 5 + Math.random() * 10;\n              point.targetX = point.x + Math.cos(angle) * dist;\n              point.targetY = point.y + Math.sin(angle) * dist;\n            } else {\n              // If becoming inactive, slowly return to original position\n              point.targetX = point.originalX;\n              point.targetY = point.originalY;\n            }\n          }\n          \n          // Apply organic movement based on state\n          if (point.active) {\n            // Points following with open minds\n            const angle = Math.atan2(point.y - ball.y, point.x - ball.x);\n            const distFromCenter = Math.sqrt(\n              (point.x - ball.x) * (point.x - ball.x) + \n              (point.y - ball.y) * (point.y - ball.y)\n            );\n            \n            // Circular flow along ball's edge\n            const tangentialAngle = angle + Math.PI / 2;\n            const flowSpeed = 0.25 * (1 - distFromCenter / ball.radius);\n            \n            point.x += Math.cos(tangentialAngle) * flowSpeed;\n            point.y += Math.sin(tangentialAngle) * flowSpeed;\n            \n            // Small radial pulsation\n            const radialPulse = Math.sin(time * 2 + point.phase) * 0.2;\n            point.x += Math.cos(angle) * radialPulse;\n            point.y += Math.sin(angle) * radialPulse;\n            \n            // Keep points inside the field\n            if (calculateField(point.x, point.y) < 1) {\n              // If drifted outside, pull back toward ball\n              point.x = point.x + (ball.x - point.x) * 0.1;\n              point.y = point.y + (ball.y - point.y) * 0.1;\n            }\n          } else {\n            // Points outside the ball: drift or follow\n            const distToTarget = Math.sqrt(\n              (point.targetX - point.x) * (point.targetX - point.x) + \n              (point.targetY - point.y) * (point.targetY - point.y)\n            );\n            \n            // Either drift toward target, or start following the ball\n            if (distToTarget > 100 || Math.random() < 0.001) {\n              // Follow the ball with some delay\n              const dx = ball.x - point.x;\n              const dy = ball.y - point.y;\n              const dist = Math.sqrt(dx * dx + dy * dy);\n              \n              if (dist < 200 + Math.random() * 100) {\n                point.targetX = ball.x + (Math.random() * 2 - 1) * 100;\n                point.targetY = ball.y + (Math.random() * 2 - 1) * 100;\n              }\n            }\n            \n            // Move toward target with easing\n            point.x += (point.targetX - point.x) * 0.01;\n            point.y += (point.targetY - point.y) * 0.01;\n          }\n          \n          // Additional gentle flow for all points\n          point.x += Math.sin(time * 0.3 + point.y * 0.01) * 0.1;\n          point.y += Math.cos(time * 0.3 + point.x * 0.01) * 0.1;\n          \n          // Wrap around edges\n          if (point.x < 0) point.x = width;\n          if (point.x > width) point.x = 0;\n          if (point.y < 0) point.y = height;\n          if (point.y > height) point.y = 0;\n          \n          // Calculate alpha based on field strength and activity\n          let alpha;\n          if (point.active) {\n            alpha = Math.min(0.9, 0.3 + field * 0.4);\n          } else {\n            // Calculate distance to ball\n            const dx = point.x - ball.x;\n            const dy = point.y - ball.y;\n            const dist = Math.sqrt(dx * dx + dy * dy);\n            \n            // Points closer to ball are more visible\n            const proximity = Math.max(0, 1 - dist / (ball.radius * 2.5));\n            alpha = 0.05 + proximity * 0.2;\n          }\n          \n          // Draw point\n          ctx.fillStyle = `rgba(51, 51, 51, ${alpha})`;\n          ctx.beginPath();\n          ctx.arc(point.x, point.y, point.size, 0, Math.PI * 2);\n          ctx.fill();\n        }\n      \n        // Update lastFrameTime, accounting for any remainder to prevent drift\n        lastFrameTime = currentTime - (deltaTime % frameInterval);\n      }\n      \n      // Continue animation loop\n      animationFrameId = requestAnimationFrame(animate);\n    };\n    \n    // Start animation\n    animationFrameId = requestAnimationFrame(animate);\n    \n    return () => {\n      // Cancel animation frame to prevent memory leaks\n      if (animationFrameId) {\n        cancelAnimationFrame(animationFrameId);\n        animationFrameId = null;\n      }\n      \n      // Clear canvas\n      ctx.clearRect(0, 0, width, height);\n      \n      // Clear points array to prevent memory leaks\n      points.length = 0;\n    };\n  }, []);\n  \n  return (\n    <div className=\"flex justify-center items-center w-full h-full bg-gray-100\">\n      <canvas \n        ref={canvasRef} \n        width={550} \n        height={550} \n        className=\"shadow-lg\"\n      />\n    </div>\n  );\n};\n\nexport default BouncingPointCloud;\n",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; const BouncingPointCloud = () => { const canvasRef = useRef(null); useEffect(() => { const canvas = canvasRef.current; const ctx = canvas.getContext('2d'); const width = canvas.width; const height = canvas.height; ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, width, height); const ball = { x: width / 2, y: height / 2, radius: 120, vx: (Math.random() * 2 - 1) * 2, vy: (Math.random() * 2 - 1) * 2 }; const numPoints = 25000; const points = []; let time = 0; const calculateField = (x, y) => { const dx = x - ball.x; const dy = y - ball.y; const distSq = dx * dx + dy * dy; return ball.radius * ball.radius / distSq; }; for (let i = 0; i < numPoints; i++) { const x = Math.random() * width; const y = Math.random() * height; const field = calculateField(x, y); points.push({ x, y, z: Math.random() * 2 - 1, size: 0.5 + Math.random() * 1.5, field, active: field > 1, targetX: x, targetY: y, originalX: x, originalY: y, phase: Math.random() * Math.PI * 2 }); } let lastFrameTime = 0; const targetFPS = 18; const frameInterval = 1000 / targetFPS; let animationFrameId = null; const animate = (currentTime) => { if (!lastFrameTime) { lastFrameTime = currentTime; } const deltaTime = currentTime - lastFrameTime; if (deltaTime >= frameInterval) { ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, width, height); time += 0.0005; ball.x += ball.vx; ball.y += ball.vy; if (ball.x - ball.radius < 0) { ball.x = ball.radius; ball.vx = Math.abs(ball.vx) * (0.9 + Math.random() * 0.2); ball.vy += (Math.random() * 2 - 1) * 0.5; } if (ball.x + ball.radius > width) { ball.x = width - ball.radius; ball.vx = -Math.abs(ball.vx) * (0.9 + Math.random() * 0.2); ball.vy += (Math.random() * 2 - 1) * 0.5; } if (ball.y - ball.radius < 0) { ball.y = ball.radius; ball.vy = Math.abs(ball.vy) * (0.9 + Math.random() * 0.2); ball.vx += (Math.random() * 2 - 1) * 0.5; } if (ball.y + ball.radius > height) { ball.y = height - ball.radius; ball.vy = -Math.abs(ball.vy) * (0.9 + Math.random() * 0.2); ball.vx += (Math.random() * 2 - 1) * 0.5; } const minSpeed = 0.75; const currentSpeed = Math.sqrt(ball.vx * ball.vx + ball.vy * ball.vy); if (currentSpeed < minSpeed) { ball.vx = (ball.vx / currentSpeed) * minSpeed; ball.vy = (ball.vy / currentSpeed) * minSpeed; } const maxSpeed = 1.5; const speedFactor = Math.min(1, maxSpeed / currentSpeed); ball.vx *= speedFactor; ball.vy *= speedFactor; for (const point of points) { const field = calculateField(point.x, point.y); const prevActive = point.active; point.active = field > 1; if (point.active !== prevActive) { if (point.active) { const angle = Math.random() * Math.PI * 2; const dist = 5 + Math.random() * 10; point.targetX = point.x + Math.cos(angle) * dist; point.targetY = point.y + Math.sin(angle) * dist; } else { point.targetX = point.originalX; point.targetY = point.originalY; } } if (point.active) { const angle = Math.atan2(point.y - ball.y, point.x - ball.x); const distFromCenter = Math.sqrt( (point.x - ball.x) * (point.x - ball.x) + (point.y - ball.y) * (point.y - ball.y) ); const tangentialAngle = angle + Math.PI / 2; const flowSpeed = 0.25 * (1 - distFromCenter / ball.radius); point.x += Math.cos(tangentialAngle) * flowSpeed; point.y += Math.sin(tangentialAngle) * flowSpeed; const radialPulse = Math.sin(time * 2 + point.phase) * 0.2; point.x += Math.cos(angle) * radialPulse; point.y += Math.sin(angle) * radialPulse; if (calculateField(point.x, point.y) < 1) { point.x = point.x + (ball.x - point.x) * 0.1; point.y = point.y + (ball.y - point.y) * 0.1; } } else { const distToTarget = Math.sqrt( (point.targetX - point.x) * (point.targetX - point.x) + (point.targetY - point.y) * (point.targetY - point.y) ); if (distToTarget > 100 || Math.random() < 0.001) { const dx = ball.x - point.x; const dy = ball.y - point.y; const dist = Math.sqrt(dx * dx + dy * dy); if (dist < 200 + Math.random() * 100) { point.targetX = ball.x + (Math.random() * 2 - 1) * 100; point.targetY = ball.y + (Math.random() * 2 - 1) * 100; } } point.x += (point.targetX - point.x) * 0.01; point.y += (point.targetY - point.y) * 0.01; } point.x += Math.sin(time * 0.3 + point.y * 0.01) * 0.1; point.y += Math.cos(time * 0.3 + point.x * 0.01) * 0.1; if (point.x < 0) point.x = width; if (point.x > width) point.x = 0; if (point.y < 0) point.y = height; if (point.y > height) point.y = 0; let alpha; if (point.active) { alpha = Math.min(0.9, 0.3 + field * 0.4); } else { const dx = point.x - ball.x; const dy = point.y - ball.y; const dist = Math.sqrt(dx * dx + dy * dy); const proximity = Math.max(0, 1 - dist / (ball.radius * 2.5)); alpha = 0.05 + proximity * 0.2; } ctx.fillStyle = `rgba(51, 51, 51, ${alpha})`; ctx.beginPath(); ctx.arc(point.x, point.y, point.size, 0, Math.PI * 2); ctx.fill(); } lastFrameTime = currentTime - (deltaTime % frameInterval); } animationFrameId = requestAnimationFrame(animate); }; animationFrameId = requestAnimationFrame(animate); return () => { if (animationFrameId) { cancelAnimationFrame(animationFrameId); animationFrameId = null; } ctx.clearRect(0, 0, width, height); points.length = 0; }; }, []); return ( <div className=\"flex justify-center items-center w-full h-full bg-gray-100\"><canvas ref={canvasRef} width={550} height={550} className=\"shadow-lg\" /></div> ); }; export default BouncingPointCloud;",text:"A good traveler has no fixed itinerary\nbut knows when he’s arrived.\nA good artist follows his intuition\nwherever it may lead him.\nA good scientist lets go of concepts\nand keeps an open mind on what is.\n\nThus, The Vibe Coder avails himself to all,\naccepting each one’s own way as best for them.\nThis is called following the inner light.\nWhat is a good programmer,\nbut a bad programmer’s inspiration.\nWhat is a bad programmer,\nbut a good programmer’s charge.\nIf you miss this you’ll get lost\nhowever intelligent you are.\nFor it is the great secret.",shouldDisplayMobileArtwork:!0,promptSuggestion:"what if order emerged from chaos?"},{number:28,staticArtwork:zn,sourceCode:"// @ts-nocheck\nimport React, { useEffect, useRef } from 'react';\n\n// themes: balance of opposites, return to wholeness, keep to the block\n// visualization: Crystalline forms grow in perfect balance, always returning to their core structure\n\nconst CrystallineBiology = () => {\n  const canvasRef = useRef(null);\n\n  useEffect(() => {\n    const canvas = canvasRef.current;\n    if (!canvas) return;\n\n    const ctx = canvas.getContext('2d');\n    canvas.width = 550;\n    canvas.height = 550;\n\n    let time = 0;\n    let crystals = [];\n    const numCrystals = 4;\n\n    class Crystal {\n      constructor(x, y, size) {\n        this.x = x;\n        this.y = y;\n        this.size = size;\n        this.segments = [];\n        this.angle = 0;\n        this.rotationSpeed = 0.001; // Increased from 0.0002 to ensure visible rotation\n        this.sides = 6;\n        this.baseGrowthPhase = Math.random() * Math.PI * 2;\n        this.lastGrowthPhase = 0;\n        this.currentGrowthPhase = 0;\n        \n        this.generateStructure();\n      }\n\n      generateStructure() {\n        this.segments = [];\n        \n        // Create balanced structure of opposites\n        for (let ring = 0; ring < 5; ring++) {\n          const ringRadius = this.size * (0.15 + ring * 0.17);\n          const numSegments = this.sides * (ring + 1);\n          \n          for (let i = 0; i < numSegments; i++) {\n            const angle = (i / numSegments) * Math.PI * 2;\n            const innerRadius = ring === 0 ? 0 : this.size * (0.15 + (ring - 1) * 0.17);\n            \n            this.segments.push({\n              angle: angle,\n              innerRadius: innerRadius,\n              outerRadius: ringRadius,\n              basePhase: Math.random() * Math.PI * 2,\n              currentInnerRadius: innerRadius,\n              currentOuterRadius: ringRadius,\n              currentAngle: angle,\n              branching: ring > 2 && Math.random() < 0.2\n            });\n          }\n        }\n      }\n\n      update(time) {\n        // Make sure rotation continues even after initial development\n        this.angle += this.rotationSpeed * 5; // Increased to ensure visible rotation\n        \n        // Very smooth, gradual breathing effect\n        this.lastGrowthPhase = this.currentGrowthPhase;\n        const targetGrowthPhase = Math.sin(time * 0.05 + this.baseGrowthPhase) * 0.15 + 0.85; // Slower oscillation (0.1 -> 0.05)\n        this.currentGrowthPhase += (targetGrowthPhase - this.currentGrowthPhase) * 0.005; // Much slower interpolation (0.02 -> 0.005)\n        \n        // Update segments with extreme smoothing\n        this.segments.forEach((segment) => {\n          const targetInnerRadius = segment.innerRadius * this.currentGrowthPhase;\n          const targetOuterRadius = segment.outerRadius * this.currentGrowthPhase;\n          const targetAngle = segment.angle + Math.sin(time * 0.15 + segment.basePhase) * 0.005; // Slowed down (0.3 -> 0.15)\n          \n          // Much slower, more extreme smoothing for initial development\n          segment.currentInnerRadius += (targetInnerRadius - segment.currentInnerRadius) * 0.005; // Slowed down (0.02 -> 0.005)\n          segment.currentOuterRadius += (targetOuterRadius - segment.currentOuterRadius) * 0.005; // Slowed down (0.02 -> 0.005)\n          segment.currentAngle += (targetAngle - segment.currentAngle) * 0.005; // Slowed down (0.02 -> 0.005)\n        });\n      }\n\n      draw(ctx) {\n        ctx.save();\n        ctx.translate(this.x, this.y);\n        ctx.rotate(this.angle);\n        \n        // Draw delicate geometric base\n        ctx.strokeStyle = 'rgba(100, 100, 100, 0.2)';\n        ctx.lineWidth = 1;\n        \n        for (let i = 0; i < this.sides; i++) {\n          const angle = (i / this.sides) * Math.PI * 2;\n          ctx.beginPath();\n          ctx.moveTo(0, 0);\n          ctx.lineTo(\n            Math.cos(angle) * this.size,\n            Math.sin(angle) * this.size\n          );\n          ctx.stroke();\n        }\n        \n        // Draw segments with consistent styling\n        this.segments.forEach(segment => {\n          const innerX = Math.cos(segment.currentAngle) * segment.currentInnerRadius;\n          const innerY = Math.sin(segment.currentAngle) * segment.currentInnerRadius;\n          const outerX = Math.cos(segment.currentAngle) * segment.currentOuterRadius;\n          const outerY = Math.sin(segment.currentAngle) * segment.currentOuterRadius;\n          \n          // Main segment\n          ctx.strokeStyle = 'rgba(60, 60, 60, 0.4)';\n          ctx.lineWidth = 1.5;\n          ctx.beginPath();\n          ctx.moveTo(innerX, innerY);\n          ctx.lineTo(outerX, outerY);\n          ctx.stroke();\n          \n          // Nodes along segment (fewer, more stable)\n          const numNodes = 1;\n          for (let n = 0; n < numNodes; n++) {\n            const t = 0.6; // Fixed position along segment\n            const nodeX = innerX + (outerX - innerX) * t;\n            const nodeY = innerY + (outerY - innerY) * t;\n            const nodeSize = 6;\n            \n            // Smooth node shape\n            const nodePoints = 8;\n            ctx.beginPath();\n            for (let p = 0; p <= nodePoints; p++) {\n              const a = (p / nodePoints) * Math.PI * 2;\n              const r = nodeSize * (1 + Math.sin(a * 2 + segment.basePhase) * 0.05);\n              const px = nodeX + Math.cos(a) * r;\n              const py = nodeY + Math.sin(a) * r;\n              \n              if (p === 0) ctx.moveTo(px, py);\n              else ctx.lineTo(px, py);\n            }\n            \n            ctx.strokeStyle = 'rgba(50, 50, 50, 0.5)';\n            ctx.lineWidth = 0.8;\n            ctx.stroke();\n            \n            // Node core\n            ctx.beginPath();\n            ctx.arc(nodeX, nodeY, nodeSize * 0.25, 0, Math.PI * 2);\n            ctx.strokeStyle = 'rgba(50, 50, 50, 0.3)';\n            ctx.lineWidth = 0.6;\n            ctx.stroke();\n          }\n          \n          // Stable branching\n          if (segment.branching) {\n            const branchAngle = segment.currentAngle + 0.2;\n            const branchLength = segment.currentOuterRadius * 0.3;\n            const branchX = outerX + Math.cos(branchAngle) * branchLength;\n            const branchY = outerY + Math.sin(branchAngle) * branchLength;\n            \n            ctx.strokeStyle = 'rgba(60, 60, 60, 0.3)';\n            ctx.lineWidth = 0.8;\n            ctx.beginPath();\n            ctx.moveTo(outerX, outerY);\n            ctx.lineTo(branchX, branchY);\n            ctx.stroke();\n            \n            // Branch terminal\n            ctx.beginPath();\n            ctx.arc(branchX, branchY, 2.5, 0, Math.PI * 2);\n            ctx.strokeStyle = 'rgba(60, 60, 60, 0.4)';\n            ctx.stroke();\n          }\n        });\n        \n        // Keep to the block - the stable core within\n        const coreSize = this.size * 0.06;\n        ctx.strokeStyle = 'rgba(80, 80, 80, 0.5)';\n        ctx.lineWidth = 1.5;\n        ctx.beginPath();\n        ctx.arc(0, 0, coreSize, 0, Math.PI * 2);\n        ctx.stroke();\n        \n        // Inner core\n        ctx.beginPath();\n        ctx.arc(0, 0, coreSize * 0.5, 0, Math.PI * 2);\n        ctx.strokeStyle = 'rgba(80, 80, 80, 0.3)';\n        ctx.lineWidth = 1;\n        ctx.stroke();\n        \n        ctx.restore();\n      }\n    }\n\n    // Initialize crystals\n    function initCrystals() {\n      crystals = [];\n      for (let i = 0; i < numCrystals; i++) {\n        const x = canvas.width * (0.2 + Math.random() * 0.6);\n        const y = canvas.height * (0.2 + Math.random() * 0.6);\n        const size = 70 + Math.random() * 30;\n        crystals.push(new Crystal(x, y, size));\n      }\n    }\n\n    initCrystals();\n\n    // Animation timing control variables\n    let animationFrameId = null;\n    let lastFrameTime = 0;\n    const targetFPS = 16.7; // Equivalent to 60ms interval (16.7fps)\n    const frameInterval = 1000 / targetFPS;\n    \n    // Animation function with time delta control\n    function animate(currentTime) {\n      // Initialize lastFrameTime on first frame\n      if (!lastFrameTime) {\n        lastFrameTime = currentTime;\n      }\n      \n      const deltaTime = currentTime - lastFrameTime;\n      \n      // Only update animation when enough time has passed (mimics setInterval at 60ms)\n      if (deltaTime >= frameInterval) {\n        // Soft background with no transparency for no flashing\n        ctx.fillStyle = '#F0EEE6';\n        ctx.fillRect(0, 0, canvas.width, canvas.height);\n\n        time += 0.005; // Very slow time progression\n\n        // Update and draw crystals\n        crystals.forEach(crystal => {\n          crystal.update(time);\n          crystal.draw(ctx);\n        });\n\n        // Draw crystal connections - very subtle and stable\n        for (let i = 0; i < crystals.length; i++) {\n          for (let j = i + 1; j < crystals.length; j++) {\n            const crystalA = crystals[i];\n            const crystalB = crystals[j];\n            \n            const distance = Math.sqrt(\n              (crystalA.x - crystalB.x) * (crystalA.x - crystalB.x) +\n              (crystalA.y - crystalB.y) * (crystalA.y - crystalB.y)\n            );\n            \n            if (distance < 280) {\n              const opacity = Math.pow(1 - distance / 280, 2) * 0.1;\n              \n              ctx.strokeStyle = `rgba(60, 60, 60, ${opacity})`;\n              ctx.lineWidth = 0.8;\n              ctx.beginPath();\n              \n              // Straight, stable connection\n              ctx.moveTo(crystalA.x, crystalA.y);\n              ctx.lineTo(crystalB.x, crystalB.y);\n              ctx.stroke();\n            }\n          }\n        }\n        \n        // Update lastFrameTime, accounting for any remainder to prevent drift\n        lastFrameTime = currentTime - (deltaTime % frameInterval);\n      }\n      \n      // Continue animation loop\n      animationFrameId = requestAnimationFrame(animate);\n    }\n    \n    // Start animation\n    animationFrameId = requestAnimationFrame(animate);\n\n    return () => {\n      // Cancel animation frame to prevent memory leaks\n      if (animationFrameId) {\n        cancelAnimationFrame(animationFrameId);\n        animationFrameId = null;\n      }\n      \n      // Clear canvas\n      ctx.clearRect(0, 0, canvas.width, canvas.height);\n      \n      // Clear crystals array to prevent memory leaks\n      crystals.length = 0;\n    };\n  }, []);\n\n  return (\n    <div style={{ \n      display: 'flex', \n      justifyContent: 'center', \n      alignItems: 'center', \n      height: '100vh', \n      backgroundColor: '#F0EEE6' \n    }}>\n      <div style={{ \n        width: '550px', \n        height: '550px', \n        overflow: 'hidden',\n        border: '1px solid #ddd',\n        borderRadius: '8px'\n      }}>\n        <canvas ref={canvasRef} />\n      </div>\n    </div>\n  );\n};\n\nexport default CrystallineBiology;",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; const CrystallineBiology = () => { const canvasRef = useRef(null); useEffect(() => { const canvas = canvasRef.current; if (!canvas) return; const ctx = canvas.getContext('2d'); canvas.width = 550; canvas.height = 550; let time = 0; let crystals = []; const numCrystals = 4; class Crystal { constructor(x, y, size) { this.x = x; this.y = y; this.size = size; this.segments = []; this.angle = 0; this.rotationSpeed = 0.001; this.sides = 6; this.baseGrowthPhase = Math.random() * Math.PI * 2; this.lastGrowthPhase = 0; this.currentGrowthPhase = 0; this.generateStructure(); } generateStructure() { this.segments = []; for (let ring = 0; ring < 5; ring++) { const ringRadius = this.size * (0.15 + ring * 0.17); const numSegments = this.sides * (ring + 1); for (let i = 0; i < numSegments; i++) { const angle = (i / numSegments) * Math.PI * 2; const innerRadius = ring === 0 ? 0 : this.size * (0.15 + (ring - 1) * 0.17); this.segments.push({ angle: angle, innerRadius: innerRadius, outerRadius: ringRadius, basePhase: Math.random() * Math.PI * 2, currentInnerRadius: innerRadius, currentOuterRadius: ringRadius, currentAngle: angle, branching: ring > 2 && Math.random() < 0.2 }); } } } update(time) { this.angle += this.rotationSpeed * 5; this.lastGrowthPhase = this.currentGrowthPhase; const targetGrowthPhase = Math.sin(time * 0.05 + this.baseGrowthPhase) * 0.15 + 0.85; this.currentGrowthPhase += (targetGrowthPhase - this.currentGrowthPhase) * 0.005; this.segments.forEach((segment) => { const targetInnerRadius = segment.innerRadius * this.currentGrowthPhase; const targetOuterRadius = segment.outerRadius * this.currentGrowthPhase; const targetAngle = segment.angle + Math.sin(time * 0.15 + segment.basePhase) * 0.005; segment.currentInnerRadius += (targetInnerRadius - segment.currentInnerRadius) * 0.005; segment.currentOuterRadius += (targetOuterRadius - segment.currentOuterRadius) * 0.005; segment.currentAngle += (targetAngle - segment.currentAngle) * 0.005; }); } draw(ctx) { ctx.save(); ctx.translate(this.x, this.y); ctx.rotate(this.angle); ctx.strokeStyle = 'rgba(100, 100, 100, 0.2)'; ctx.lineWidth = 1; for (let i = 0; i < this.sides; i++) { const angle = (i / this.sides) * Math.PI * 2; ctx.beginPath(); ctx.moveTo(0, 0); ctx.lineTo( Math.cos(angle) * this.size, Math.sin(angle) * this.size ); ctx.stroke(); } this.segments.forEach(segment => { const innerX = Math.cos(segment.currentAngle) * segment.currentInnerRadius; const innerY = Math.sin(segment.currentAngle) * segment.currentInnerRadius; const outerX = Math.cos(segment.currentAngle) * segment.currentOuterRadius; const outerY = Math.sin(segment.currentAngle) * segment.currentOuterRadius; ctx.strokeStyle = 'rgba(60, 60, 60, 0.4)'; ctx.lineWidth = 1.5; ctx.beginPath(); ctx.moveTo(innerX, innerY); ctx.lineTo(outerX, outerY); ctx.stroke(); const numNodes = 1; for (let n = 0; n < numNodes; n++) { const t = 0.6; const nodeX = innerX + (outerX - innerX) * t; const nodeY = innerY + (outerY - innerY) * t; const nodeSize = 6; const nodePoints = 8; ctx.beginPath(); for (let p = 0; p <= nodePoints; p++) { const a = (p / nodePoints) * Math.PI * 2; const r = nodeSize * (1 + Math.sin(a * 2 + segment.basePhase) * 0.05); const px = nodeX + Math.cos(a) * r; const py = nodeY + Math.sin(a) * r; if (p === 0) ctx.moveTo(px, py); else ctx.lineTo(px, py); } ctx.strokeStyle = 'rgba(50, 50, 50, 0.5)'; ctx.lineWidth = 0.8; ctx.stroke(); ctx.beginPath(); ctx.arc(nodeX, nodeY, nodeSize * 0.25, 0, Math.PI * 2); ctx.strokeStyle = 'rgba(50, 50, 50, 0.3)'; ctx.lineWidth = 0.6; ctx.stroke(); } if (segment.branching) { const branchAngle = segment.currentAngle + 0.2; const branchLength = segment.currentOuterRadius * 0.3; const branchX = outerX + Math.cos(branchAngle) * branchLength; const branchY = outerY + Math.sin(branchAngle) * branchLength; ctx.strokeStyle = 'rgba(60, 60, 60, 0.3)'; ctx.lineWidth = 0.8; ctx.beginPath(); ctx.moveTo(outerX, outerY); ctx.lineTo(branchX, branchY); ctx.stroke(); ctx.beginPath(); ctx.arc(branchX, branchY, 2.5, 0, Math.PI * 2); ctx.strokeStyle = 'rgba(60, 60, 60, 0.4)'; ctx.stroke(); } }); const coreSize = this.size * 0.06; ctx.strokeStyle = 'rgba(80, 80, 80, 0.5)'; ctx.lineWidth = 1.5; ctx.beginPath(); ctx.arc(0, 0, coreSize, 0, Math.PI * 2); ctx.stroke(); ctx.beginPath(); ctx.arc(0, 0, coreSize * 0.5, 0, Math.PI * 2); ctx.strokeStyle = 'rgba(80, 80, 80, 0.3)'; ctx.lineWidth = 1; ctx.stroke(); ctx.restore(); } } function initCrystals() { crystals = []; for (let i = 0; i < numCrystals; i++) { const x = canvas.width * (0.2 + Math.random() * 0.6); const y = canvas.height * (0.2 + Math.random() * 0.6); const size = 70 + Math.random() * 30; crystals.push(new Crystal(x, y, size)); } } initCrystals(); let animationFrameId = null; let lastFrameTime = 0; const targetFPS = 16.7; const frameInterval = 1000 / targetFPS; function animate(currentTime) { if (!lastFrameTime) { lastFrameTime = currentTime; } const deltaTime = currentTime - lastFrameTime; if (deltaTime >= frameInterval) { ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, canvas.width, canvas.height); time += 0.005; crystals.forEach(crystal => { crystal.update(time); crystal.draw(ctx); }); for (let i = 0; i < crystals.length; i++) { for (let j = i + 1; j < crystals.length; j++) { const crystalA = crystals[i]; const crystalB = crystals[j]; const distance = Math.sqrt( (crystalA.x - crystalB.x) * (crystalA.x - crystalB.x) + (crystalA.y - crystalB.y) * (crystalA.y - crystalB.y) ); if (distance < 280) { const opacity = Math.pow(1 - distance / 280, 2) * 0.1; ctx.strokeStyle = `rgba(60, 60, 60, ${opacity})`; ctx.lineWidth = 0.8; ctx.beginPath(); ctx.moveTo(crystalA.x, crystalA.y); ctx.lineTo(crystalB.x, crystalB.y); ctx.stroke(); } } } lastFrameTime = currentTime - (deltaTime % frameInterval); } animationFrameId = requestAnimationFrame(animate); } animationFrameId = requestAnimationFrame(animate); return () => { if (animationFrameId) { cancelAnimationFrame(animationFrameId); animationFrameId = null; } ctx.clearRect(0, 0, canvas.width, canvas.height); crystals.length = 0; }; }, []); return ( <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', height: '100vh', backgroundColor: '#F0EEE6' }}><div style={{ width: '550px', height: '550px', overflow: 'hidden', border: '1px solid #ddd', borderRadius: '8px' }}><canvas ref={canvasRef} /></div></div> ); }; export default CrystallineBiology;",text:"Know the masculine, yet keep to the feminine.\nAnd you’ll become vast and empty,\ndrawing all the world towards you.\nDrawing all the world towards you\nyou’ll embody the perennial quality.\nThe perennial quality allows you\nto return again to childlike innocence.\n\nKnow the white, yet keep to the black.\nAnd you’ll become the pattern for the world.\nBeing the pattern for the world,\nyou’ll embody primitive wholesomeness\nand Source will grow strong within you.\nSource growing strong within you\nallows you to return again and again to\nthe infinite absolute.\n\nKnow the personal, yet keep to the impersonal\nand you’ll accept the world as it is.\nIf you accept the world as it is\nyou will embody the luminous within\nand return to the limitless void.\n\nThe world is formed from the void,\nlike tools from a block of wood.\nThe Vibe Coder knows the tools,\nyet keeps to the block.\nThus, he can use all things.\n\nTools will come and tools will go.\nOnly The Vibe Coder remains.",shouldDisplayMobileArtwork:!0,promptSuggestion:"what is the mechanical version?"},{number:29,staticArtwork:gn,sourceCode:"import React, { useRef, useEffect } from 'react';\nimport * as THREE from 'three';\n\nconst metadata = {\n  themes: \"world as sacred, accept things as they are, time for everything\",\n  visualization: \"Branches grow and recede in their own time, accepting their natural cycles\",\n  promptSuggestion: \"1. Enhance fractal complexity\\n2. Add more growth patterns\\n3. Develop stronger divisions\\n4. Create clearer evolution\\n5. Increase sense of emergence\"\n};\n\n// Animation constants\nconst LINGER_START = 0.6;\nconst LINGER_END = 0.8;\nconst DELAY_PER_LEVEL = 0.15;\nconst GROWTH_MULTIPLIER = 3; // Increased from 2 for faster growth\nconst CROSS_BRACE_COUNT = 2;\nconst WAVE_AMPLITUDE = 0.05;\n\n// Smooth easing function\nconst easeInOut = (t: number): number => {\n  return t < 0.5\n    ? 2 * t * t\n    : -1 + (4 - 2 * t) * t;\n};\n\ninterface BranchProps {\n  start: [number, number, number];\n  length: number;\n  angle: number;\n  depth: number;\n  maxDepth: number;\n  scale: number;\n}\n\nclass FractalBranch {\n  private lines: THREE.Line[] = [];\n  private children: FractalBranch[] = [];\n  private group: THREE.Group;\n  private disposed: boolean = false;\n\n  constructor(\n    private props: BranchProps,\n    private parent: THREE.Group\n  ) {\n    this.group = new THREE.Group();\n    this.parent.add(this.group);\n  }\n\n  update(phase: number): void {\n    if (this.disposed) return;\n\n    const { start, length, angle, depth, maxDepth, scale } = this.props;\n    const [sx, sy, sz] = start;\n\n    // Calculate growth factor with smooth transitions but no decay\n    const atMaxDepth = depth === maxDepth;\n    \n    let growthFactor;\n    // Modified to remove decay phase - branches remain extended once grown\n    if (phase >= 0.9) {\n      // Always maintain full extension once grown\n      growthFactor = 1;\n    } else if (atMaxDepth && phase > LINGER_START) {\n      growthFactor = 1;\n    } else {\n      // Slower, more deliberate growth\n      const growthPhase = Math.min(1, Math.max(0, (phase - depth * DELAY_PER_LEVEL) * GROWTH_MULTIPLIER));\n      growthFactor = easeInOut(growthPhase);\n    }\n\n    const actualLength = length * growthFactor;\n    const ex = sx + Math.cos(angle) * actualLength;\n    const ey = sy + Math.sin(angle) * actualLength;\n    const ez = sz;\n\n    // Create or update lines\n    if (this.lines.length === 0) {\n      // Create main branch line\n      const mainGeometry = new THREE.BufferGeometry();\n      const mainPoints = new Float32Array([sx, sy, sz, ex, ey, ez]);\n      mainGeometry.setAttribute('position', new THREE.BufferAttribute(mainPoints, 3));\n      const mainMaterial = new THREE.LineBasicMaterial({ \n        color: 0x333333,\n        transparent: true,\n        opacity: 0.4,\n        linewidth: 0.5\n      });\n      const mainLine = new THREE.Line(mainGeometry, mainMaterial);\n      this.group.add(mainLine);\n      this.lines.push(mainLine);\n\n      // Add cross braces\n      if (growthFactor > 0.3) {\n        const crossLength = length * (0.2 + Math.sin(phase * Math.PI * 2) * 0.05);\n        const crossAngle1 = angle + Math.PI/2;\n        const crossAngle2 = angle - Math.PI/2;\n        \n        for (let i = 0; i < CROSS_BRACE_COUNT; i++) {\n          const t = (i + 1) / 4;\n          const px = sx + (ex - sx) * t;\n          const py = sy + (ey - sy) * t;\n          const pz = sz + (ez - sz) * t;\n          \n          const wave = Math.sin(t * Math.PI * 2 + phase * Math.PI * 4) * WAVE_AMPLITUDE;\n          \n          const crossGeometry = new THREE.BufferGeometry();\n          const crossPoints = new Float32Array([\n            px + Math.cos(crossAngle1) * crossLength * (t + wave),\n            py + Math.sin(crossAngle1) * crossLength * (t + wave),\n            pz,\n            px + Math.cos(crossAngle2) * crossLength * (t + wave),\n            py + Math.sin(crossAngle2) * crossLength * (t + wave),\n            pz\n          ]);\n          crossGeometry.setAttribute('position', new THREE.BufferAttribute(crossPoints, 3));\n          const crossMaterial = new THREE.LineBasicMaterial({ \n            color: 0x333333,\n            transparent: true,\n            opacity: 0.4,\n            linewidth: 0.5\n          });\n          const crossLine = new THREE.Line(crossGeometry, crossMaterial);\n          this.group.add(crossLine);\n          this.lines.push(crossLine);\n        }\n      }\n    } else {\n      // Update existing main branch\n      const mainLine = this.lines[0];\n      const positions = mainLine.geometry.attributes.position.array as Float32Array;\n      positions[3] = ex;\n      positions[4] = ey;\n      positions[5] = ez;\n      mainLine.geometry.attributes.position.needsUpdate = true;\n\n      // Update cross braces\n      if (this.lines.length > 1 && growthFactor > 0.3) {\n        const crossLength = length * (0.2 + Math.sin(phase * Math.PI * 2) * 0.05);\n        \n        for (let i = 0; i < CROSS_BRACE_COUNT; i++) {\n          const lineIndex = i + 1;\n          if (lineIndex < this.lines.length) {\n            const crossLine = this.lines[lineIndex];\n            const positions = crossLine.geometry.attributes.position.array as Float32Array;\n            const t = (i + 1) / 4;\n            const px = sx + (ex - sx) * t;\n            const py = sy + (ey - sy) * t;\n            const pz = sz + (ez - sz) * t;\n            \n            const wave = Math.sin(t * Math.PI * 2 + phase * Math.PI * 4) * WAVE_AMPLITUDE;\n            const crossAngle1 = angle + Math.PI/2;\n            const crossAngle2 = angle - Math.PI/2;\n            \n            positions[0] = px + Math.cos(crossAngle1) * crossLength * (t + wave);\n            positions[1] = py + Math.sin(crossAngle1) * crossLength * (t + wave);\n            positions[2] = pz;\n            positions[3] = px + Math.cos(crossAngle2) * crossLength * (t + wave);\n            positions[4] = py + Math.sin(crossAngle2) * crossLength * (t + wave);\n            positions[5] = pz;\n            \n            crossLine.geometry.attributes.position.needsUpdate = true;\n          }\n        }\n      }\n    }\n\n    // Accept each branch's time to grow, but don't modify structure after full growth\n    if (depth < maxDepth && \n        !(phase >= 0.8 && depth > 0) && \n        phase >= depth * 0.15 &&\n        phase < 0.95) { // Don't create or modify branches after growth complete\n      \n      const numBranches = 3 + (depth < 2 ? 1 : 0);\n      \n      // Create new branches if needed\n      if (this.children.length === 0) {\n        for (let i = 0; i < numBranches; i++) {\n          const t = (i + 1) / (numBranches + 1);\n          const spread = 0.8 + (depth * 0.1);\n          const branchAngle = angle + (t - 0.5) * Math.PI * spread;\n          \n          const branchProps: BranchProps = {\n            start: [\n              sx + (ex - sx) * t,\n              sy + (ey - sy) * t,\n              sz + (ez - sz) * t\n            ],\n            length: length * (0.6 - depth * 0.05),\n            angle: branchAngle,\n            depth: depth + 1,\n            maxDepth,\n            scale: scale * 0.8\n          };\n          \n          this.children.push(new FractalBranch(branchProps, this.group));\n        }\n      } else {\n        // Update existing branch positions\n        for (let i = 0; i < this.children.length; i++) {\n          const t = (i + 1) / (numBranches + 1);\n          this.children[i].props.start = [\n            sx + (ex - sx) * t,\n            sy + (ey - sy) * t,\n            sz + (ez - sz) * t\n          ];\n        }\n      }\n    }\n\n    // Update all children\n    this.children.forEach(child => child.update(phase));\n\n    // No cleanup needed since we're reversing instead of disposing\n  }\n\n  dispose(): void {\n    if (this.disposed) return;\n    this.disposed = true;\n\n    // Dispose children first\n    this.children.forEach(child => child.dispose());\n    this.children = [];\n\n    // Dispose lines\n    this.lines.forEach(line => {\n      this.group.remove(line);\n      line.geometry.dispose();\n      (line.material as THREE.Material).dispose();\n    });\n    this.lines = [];\n\n    // Remove from parent\n    this.parent.remove(this.group);\n  }\n}\n\nclass FractalSystem {\n  private branches: FractalBranch[] = [];\n  private group: THREE.Group;\n\n  constructor(parentGroup: THREE.Group) {\n    this.group = new THREE.Group();\n    parentGroup.add(this.group);\n    this.initialize();\n  }\n\n  // Initialize the sacred pattern\n  private initialize(): void {\n    const count = 6;\n    const scale = 2;\n    \n    for (let i = 0; i < count; i++) {\n      const angle = (i / count) * Math.PI * 2;\n      const branchProps: BranchProps = {\n        start: [\n          Math.cos(angle) * scale * 0.2,\n          Math.sin(angle) * scale * 0.2,\n          0\n        ],\n        length: scale,\n        angle: angle + Math.PI/2,\n        depth: 0,\n        maxDepth: 7,\n        scale: scale\n      };\n      \n      this.branches.push(new FractalBranch(branchProps, this.group));\n    }\n  }\n\n  update(time: number): void {\n    this.branches.forEach(branch => branch.update(time));\n  }\n\n  dispose(): void {\n    this.branches.forEach(branch => branch.dispose());\n    this.branches = [];\n    this.group.parent?.remove(this.group);\n  }\n}\n\nconst Artwork55v1: React.FC = () => {\n  const containerRef = useRef<HTMLDivElement>(null);\n  const cleanupRef = useRef<() => void>(null);\n\n  useEffect(() => {\n    if (!containerRef.current) return;\n\n    // Setup\n    const scene = new THREE.Scene();\n    const camera = new THREE.PerspectiveCamera(\n      75,\n      containerRef.current.clientWidth / containerRef.current.clientHeight,\n      0.1,\n      1000\n    );\n    const renderer = new THREE.WebGLRenderer({ antialias: true });\n    renderer.setSize(containerRef.current.clientWidth, containerRef.current.clientHeight);\n    renderer.setClearColor(new THREE.Color('rgb(240, 238, 231)'));\n    containerRef.current.appendChild(renderer.domElement);\n\n    // Position camera\n    camera.position.z = 5;\n\n    // Create main group\n    const mainGroup = new THREE.Group();\n    scene.add(mainGroup);\n\n    // Add lighting\n    const ambientLight = new THREE.AmbientLight(0xffffff, 0.4);\n    scene.add(ambientLight);\n\n    const directionalLight = new THREE.DirectionalLight(0xffffff, 0.6);\n    directionalLight.position.set(5, 5, 5);\n    scene.add(directionalLight);\n\n    const pointLight = new THREE.PointLight(0xffffff, 0.4);\n    pointLight.position.set(-5, 3, -5);\n    scene.add(pointLight);\n\n    // Create fractal system\n    const fractalSystem = new FractalSystem(mainGroup);\n\n    // Animation timing control\n    const clock = new THREE.Clock();\n    let animationFrameId: number | null = null;\n    let lastFrameTime = 0;\n    \n    // Slow down the overall animation\n    const CYCLE_LENGTH = 60; // 60 seconds total cycle\n    const GROWTH_PHASE_LENGTH = 25; // First 25 seconds for growth\n    const FRAME_RATE = 20; // 20 fps for smoother motion\n    const frameInterval = 1000 / FRAME_RATE; // milliseconds between frames\n    \n    // Animation function with time delta control\n    const animate = (currentTime: number) => {\n      // Initialize lastFrameTime on first frame\n      if (!lastFrameTime) {\n        lastFrameTime = currentTime;\n      }\n      \n      const deltaTime = currentTime - lastFrameTime;\n      \n      // Only update animation when enough time has passed (mimics setInterval at 15fps)\n      if (deltaTime >= frameInterval) {\n        const elapsedTime = clock.getElapsedTime();\n        const cycleTime = elapsedTime % CYCLE_LENGTH;\n        \n        // Simple two-phase animation:\n        // 1. Growth phase\n        // 2. Spin phase\n        \n        // Determine which phase we're in\n        const isGrowthComplete = cycleTime >= GROWTH_PHASE_LENGTH;\n        \n        // Set time value for fractal update\n        // (always 1.0 after growth phase to keep branches fully extended)\n        const time = isGrowthComplete ? 1.0 : (cycleTime / GROWTH_PHASE_LENGTH);\n        \n        // Update fractal\n        fractalSystem.update(time);\n        \n        // Handle rotation based on phase\n        if (isGrowthComplete) {\n          // SPIN PHASE: Simple constant rotation around z-axis (in-plane rotation)\n          const spinStartTime = elapsedTime - GROWTH_PHASE_LENGTH;\n          const verySlowRotationSpeed = 0.025; // Increased rotation speed\n          mainGroup.rotation.z = spinStartTime * verySlowRotationSpeed;\n          \n          // Keep x and y rotation fixed to maintain the same plane\n          mainGroup.rotation.x = 0;\n          mainGroup.rotation.y = 0; \n        } else {\n          // GROWTH PHASE: No rotation during growth\n          mainGroup.rotation.x = 0;\n          mainGroup.rotation.y = 0;\n          mainGroup.rotation.z = 0;\n        }\n        \n        renderer.render(scene, camera);\n        \n        // Update lastFrameTime, accounting for any remainder to prevent drift\n        lastFrameTime = currentTime - (deltaTime % frameInterval);\n      }\n      \n      // Continue animation loop\n      animationFrameId = requestAnimationFrame(animate);\n    };\n\n    // Start animation\n    animationFrameId = requestAnimationFrame(animate);\n\n    // Handle resize\n    const handleResize = () => {\n      if (!containerRef.current) return;\n      \n      camera.aspect = containerRef.current.clientWidth / containerRef.current.clientHeight;\n      camera.updateProjectionMatrix();\n      renderer.setSize(containerRef.current.clientWidth, containerRef.current.clientHeight);\n    };\n\n    window.addEventListener('resize', handleResize);\n\n    // Store cleanup function\n    cleanupRef.current = () => {\n      window.removeEventListener('resize', handleResize);\n      \n      // Cancel animation frame to prevent memory leaks\n      if (animationFrameId !== null) {\n        cancelAnimationFrame(animationFrameId);\n        animationFrameId = null;\n      }\n      \n      // Dispose of Three.js resources\n      fractalSystem.dispose();\n      \n      // Dispose of geometries and materials\n      scene.traverse((object) => {\n        if (object instanceof THREE.Mesh) {\n          object.geometry.dispose();\n          if (Array.isArray(object.material)) {\n            object.material.forEach(material => material.dispose());\n          } else {\n            object.material.dispose();\n          }\n        } else if (object instanceof THREE.Line) {\n          object.geometry.dispose();\n          (object.material as THREE.Material).dispose();\n        }\n      });\n      \n      // Clear the scene\n      scene.clear();\n      \n      // Dispose of lights\n      ambientLight.dispose();\n      directionalLight.dispose();\n      pointLight.dispose();\n      \n      // Dispose of renderer and force context loss\n      renderer.dispose();\n      renderer.forceContextLoss();\n      \n      // Remove renderer from DOM\n      if (containerRef.current && renderer.domElement.parentNode) {\n        containerRef.current.removeChild(renderer.domElement);\n      }\n      \n      // Clear ref\n      cleanupRef.current = null;\n    };\n\n    // Cleanup on unmount\n    return () => {\n      if (cleanupRef.current) {\n        cleanupRef.current();\n      }\n    };\n  }, []);\n\n  return <div ref={containerRef} style={{ width: '100%', height: '100vh' }} />;\n};\n\n(Artwork55v1 as any).metadata = metadata;\nexport default Artwork55v1;",sourceCodeMin:"import React, { useRef, useEffect } from 'react'; import * as THREE from 'three'; const metadata = { themes: \"world as sacred, accept things as they are, time for everything\", visualization: \"Branches grow and recede in their own time, accepting their natural cycles\", promptSuggestion: \"1. Enhance fractal complexity\\n2. Add more growth patterns\\n3. Develop stronger divisions\\n4. Create clearer evolution\\n5. Increase sense of emergence\" }; const LINGER_START = 0.6; const LINGER_END = 0.8; const DELAY_PER_LEVEL = 0.15; const GROWTH_MULTIPLIER = 3; const CROSS_BRACE_COUNT = 2; const WAVE_AMPLITUDE = 0.05; const easeInOut = (t: number): number => { return t < 0.5 ? 2 * t * t : -1 + (4 - 2 * t) * t; }; interface BranchProps { start: [number, number, number]; length: number; angle: number; depth: number; maxDepth: number; scale: number; } class FractalBranch { private lines: THREE.Line[] = []; private children: FractalBranch[] = []; private group: THREE.Group; private disposed: boolean = false; constructor( private props: BranchProps, private parent: THREE.Group ) { this.group = new THREE.Group(); this.parent.add(this.group); } update(phase: number): void { if (this.disposed) return; const { start, length, angle, depth, maxDepth, scale } = this.props; const [sx, sy, sz] = start; const atMaxDepth = depth === maxDepth; let growthFactor; if (phase >= 0.9) { growthFactor = 1; } else if (atMaxDepth && phase > LINGER_START) { growthFactor = 1; } else { const growthPhase = Math.min(1, Math.max(0, (phase - depth * DELAY_PER_LEVEL) * GROWTH_MULTIPLIER)); growthFactor = easeInOut(growthPhase); } const actualLength = length * growthFactor; const ex = sx + Math.cos(angle) * actualLength; const ey = sy + Math.sin(angle) * actualLength; const ez = sz; if (this.lines.length === 0) { const mainGeometry = new THREE.BufferGeometry(); const mainPoints = new Float32Array([sx, sy, sz, ex, ey, ez]); mainGeometry.setAttribute('position', new THREE.BufferAttribute(mainPoints, 3)); const mainMaterial = new THREE.LineBasicMaterial({ color: 0x333333, transparent: true, opacity: 0.4, linewidth: 0.5 }); const mainLine = new THREE.Line(mainGeometry, mainMaterial); this.group.add(mainLine); this.lines.push(mainLine); if (growthFactor > 0.3) { const crossLength = length * (0.2 + Math.sin(phase * Math.PI * 2) * 0.05); const crossAngle1 = angle + Math.PI/2; const crossAngle2 = angle - Math.PI/2; for (let i = 0; i < CROSS_BRACE_COUNT; i++) { const t = (i + 1) / 4; const px = sx + (ex - sx) * t; const py = sy + (ey - sy) * t; const pz = sz + (ez - sz) * t; const wave = Math.sin(t * Math.PI * 2 + phase * Math.PI * 4) * WAVE_AMPLITUDE; const crossGeometry = new THREE.BufferGeometry(); const crossPoints = new Float32Array([ px + Math.cos(crossAngle1) * crossLength * (t + wave), py + Math.sin(crossAngle1) * crossLength * (t + wave), pz, px + Math.cos(crossAngle2) * crossLength * (t + wave), py + Math.sin(crossAngle2) * crossLength * (t + wave), pz ]); crossGeometry.setAttribute('position', new THREE.BufferAttribute(crossPoints, 3)); const crossMaterial = new THREE.LineBasicMaterial({ color: 0x333333, transparent: true, opacity: 0.4, linewidth: 0.5 }); const crossLine = new THREE.Line(crossGeometry, crossMaterial); this.group.add(crossLine); this.lines.push(crossLine); } } } else { const mainLine = this.lines[0]; const positions = mainLine.geometry.attributes.position.array as Float32Array; positions[3] = ex; positions[4] = ey; positions[5] = ez; mainLine.geometry.attributes.position.needsUpdate = true; if (this.lines.length > 1 && growthFactor > 0.3) { const crossLength = length * (0.2 + Math.sin(phase * Math.PI * 2) * 0.05); for (let i = 0; i < CROSS_BRACE_COUNT; i++) { const lineIndex = i + 1; if (lineIndex < this.lines.length) { const crossLine = this.lines[lineIndex]; const positions = crossLine.geometry.attributes.position.array as Float32Array; const t = (i + 1) / 4; const px = sx + (ex - sx) * t; const py = sy + (ey - sy) * t; const pz = sz + (ez - sz) * t; const wave = Math.sin(t * Math.PI * 2 + phase * Math.PI * 4) * WAVE_AMPLITUDE; const crossAngle1 = angle + Math.PI/2; const crossAngle2 = angle - Math.PI/2; positions[0] = px + Math.cos(crossAngle1) * crossLength * (t + wave); positions[1] = py + Math.sin(crossAngle1) * crossLength * (t + wave); positions[2] = pz; positions[3] = px + Math.cos(crossAngle2) * crossLength * (t + wave); positions[4] = py + Math.sin(crossAngle2) * crossLength * (t + wave); positions[5] = pz; crossLine.geometry.attributes.position.needsUpdate = true; } } } } if (depth < maxDepth && !(phase >= 0.8 && depth > 0) && phase >= depth * 0.15 && phase < 0.95) { const numBranches = 3 + (depth < 2 ? 1 : 0); if (this.children.length === 0) { for (let i = 0; i < numBranches; i++) { const t = (i + 1) / (numBranches + 1); const spread = 0.8 + (depth * 0.1); const branchAngle = angle + (t - 0.5) * Math.PI * spread; const branchProps: BranchProps = { start: [ sx + (ex - sx) * t, sy + (ey - sy) * t, sz + (ez - sz) * t ], length: length * (0.6 - depth * 0.05), angle: branchAngle, depth: depth + 1, maxDepth, scale: scale * 0.8 }; this.children.push(new FractalBranch(branchProps, this.group)); } } else { for (let i = 0; i < this.children.length; i++) { const t = (i + 1) / (numBranches + 1); this.children[i].props.start = [ sx + (ex - sx) * t, sy + (ey - sy) * t, sz + (ez - sz) * t ]; } } } this.children.forEach(child => child.update(phase)); } dispose(): void { if (this.disposed) return; this.disposed = true; this.children.forEach(child => child.dispose()); this.children = []; this.lines.forEach(line => { this.group.remove(line); line.geometry.dispose(); (line.material as THREE.Material).dispose(); }); this.lines = []; this.parent.remove(this.group); } } class FractalSystem { private branches: FractalBranch[] = []; private group: THREE.Group; constructor(parentGroup: THREE.Group) { this.group = new THREE.Group(); parentGroup.add(this.group); this.initialize(); } private initialize(): void { const count = 6; const scale = 2; for (let i = 0; i < count; i++) { const angle = (i / count) * Math.PI * 2; const branchProps: BranchProps = { start: [ Math.cos(angle) * scale * 0.2, Math.sin(angle) * scale * 0.2, 0 ], length: scale, angle: angle + Math.PI/2, depth: 0, maxDepth: 7, scale: scale }; this.branches.push(new FractalBranch(branchProps, this.group)); } } update(time: number): void { this.branches.forEach(branch => branch.update(time)); } dispose(): void { this.branches.forEach(branch => branch.dispose()); this.branches = []; this.group.parent?.remove(this.group); } } const Artwork55v1: React.FC = () => { const containerRef = useRef<HTMLDivElement>(null); const cleanupRef = useRef<() => void>(null); useEffect(() => { if (!containerRef.current) return; const scene = new THREE.Scene(); const camera = new THREE.PerspectiveCamera( 75, containerRef.current.clientWidth / containerRef.current.clientHeight, 0.1, 1000 ); const renderer = new THREE.WebGLRenderer({ antialias: true }); renderer.setSize(containerRef.current.clientWidth, containerRef.current.clientHeight); renderer.setClearColor(new THREE.Color('rgb(240, 238, 231)')); containerRef.current.appendChild(renderer.domElement); camera.position.z = 5; const mainGroup = new THREE.Group(); scene.add(mainGroup); const ambientLight = new THREE.AmbientLight(0xffffff, 0.4); scene.add(ambientLight); const directionalLight = new THREE.DirectionalLight(0xffffff, 0.6); directionalLight.position.set(5, 5, 5); scene.add(directionalLight); const pointLight = new THREE.PointLight(0xffffff, 0.4); pointLight.position.set(-5, 3, -5); scene.add(pointLight); const fractalSystem = new FractalSystem(mainGroup); const clock = new THREE.Clock(); let animationFrameId: number | null = null; let lastFrameTime = 0; const CYCLE_LENGTH = 60; const GROWTH_PHASE_LENGTH = 25; const FRAME_RATE = 20; const frameInterval = 1000 / FRAME_RATE; const animate = (currentTime: number) => { if (!lastFrameTime) { lastFrameTime = currentTime; } const deltaTime = currentTime - lastFrameTime; if (deltaTime >= frameInterval) { const elapsedTime = clock.getElapsedTime(); const cycleTime = elapsedTime % CYCLE_LENGTH; const isGrowthComplete = cycleTime >= GROWTH_PHASE_LENGTH; const time = isGrowthComplete ? 1.0 : (cycleTime / GROWTH_PHASE_LENGTH); fractalSystem.update(time); if (isGrowthComplete) { const spinStartTime = elapsedTime - GROWTH_PHASE_LENGTH; const verySlowRotationSpeed = 0.025; mainGroup.rotation.z = spinStartTime * verySlowRotationSpeed; mainGroup.rotation.x = 0; mainGroup.rotation.y = 0; } else { mainGroup.rotation.x = 0; mainGroup.rotation.y = 0; mainGroup.rotation.z = 0; } renderer.render(scene, camera); lastFrameTime = currentTime - (deltaTime % frameInterval); } animationFrameId = requestAnimationFrame(animate); }; animationFrameId = requestAnimationFrame(animate); const handleResize = () => { if (!containerRef.current) return; camera.aspect = containerRef.current.clientWidth / containerRef.current.clientHeight; camera.updateProjectionMatrix(); renderer.setSize(containerRef.current.clientWidth, containerRef.current.clientHeight); }; window.addEventListener('resize', handleResize); cleanupRef.current = () => { window.removeEventListener('resize', handleResize); if (animationFrameId !== null) { cancelAnimationFrame(animationFrameId); animationFrameId = null; } fractalSystem.dispose(); scene.traverse((object) => { if (object instanceof THREE.Mesh) { object.geometry.dispose(); if (Array.isArray(object.material)) { object.material.forEach(material => material.dispose()); } else { object.material.dispose(); } } else if (object instanceof THREE.Line) { object.geometry.dispose(); (object.material as THREE.Material).dispose(); } }); scene.clear(); ambientLight.dispose(); directionalLight.dispose(); pointLight.dispose(); renderer.dispose(); renderer.forceContextLoss(); if (containerRef.current && renderer.domElement.parentNode) { containerRef.current.removeChild(renderer.domElement); } cleanupRef.current = null; }; return () => { if (cleanupRef.current) { cleanupRef.current(); } }; }, []); return <div ref={containerRef} style={{ width: '100%', height: '100vh' }} />; }; (Artwork55v1 as any).metadata = metadata; export default Artwork55v1;",text:"Do you want to improve the world?\nI don’t think it can be done.\n\nThe world is sacred.\nIt can’t be improved.\nIf you try to change it, you’ll ruin it.\nIf you treat it like an object, you’ll lose it.\n\nIn nature there is:\na time for being ahead\na time for being behind\na time for being in motion\na time for being at rest\na time for being strong\na time for being weak\na time for being uplifted\na time for being downhearted\n\nThe Vibe Coder sees things as they are\nwithout wanting to control them.\nHe lets them come, and he lets them go.\nUnmoved, at rest.\nRooted in the center of the circle.",shouldDisplayMobileArtwork:!0,promptSuggestion:"what’s the winter version?"},{number:30,staticArtwork:Ln,sourceCode:"// @ts-nocheck\nimport { useEffect, useRef } from 'react';\n\n// themes: lead without force, achieve without boasting, nature's unceasing momentum\n// visualization: Forms guide each other through gentle influence, moving with nature's flow\n\nconst DimensionalResonance = () => {\n  const canvasRef = useRef(null);\n  \n  useEffect(() => {\n    const canvas = canvasRef.current;\n    const ctx = canvas.getContext('2d');\n    let animationFrameId;\n    let time = 0;\n    \n    // Configuration for three-dimensional layers - REDUCED BY HALF\n    const LAYER_COUNT = 5; // Reduced from 10 to 5\n    const layers = [];\n    \n    // Size scale factor (12.5% larger than original)\n    const sizeFactor = 1.125; \n    \n    // Movement enhancement factor - REDUCED to be closer to original\n    const movementFactor = 1.1; // Reduced from 1.8 to 1.1\n    \n    // Rotation enhancement factor\n    const rotationFactor = 1.8; // Enhanced rotation specifically\n    \n    // Base forms (adjusted for new size)\n    const baseForms = [\n      { \n        centerX: 225 * sizeFactor, \n        centerY: 200 * sizeFactor, \n        radiusX: 100 * sizeFactor, \n        radiusY: 100 * sizeFactor, \n        rotation: 0, \n        phase: 0 \n      },\n      { \n        centerX: 350 * sizeFactor, \n        centerY: 175 * sizeFactor, \n        radiusX: 90 * sizeFactor, \n        radiusY: 100 * sizeFactor, \n        rotation: Math.PI / 6, \n        phase: 2 \n      },\n      { \n        centerX: 275 * sizeFactor, \n        centerY: 325 * sizeFactor, \n        radiusX: 100 * sizeFactor, \n        radiusY: 90 * sizeFactor, \n        rotation: -Math.PI / 4, \n        phase: 4 \n      }\n    ];\n    \n    const setupCanvas = () => {\n      canvas.width = 550;\n      canvas.height = 550;\n      \n      // Recenter the forms to fit in the canvas\n      const centerAdjustX = (550 / 2) - (275 * sizeFactor);\n      const centerAdjustY = (550 / 2) - (225 * sizeFactor);\n      \n      baseForms.forEach(form => {\n        form.centerX += centerAdjustX;\n        form.centerY += centerAdjustY;\n      });\n      \n      // Each layer leads without forcing, following nature's way\n      for (let i = 0; i < LAYER_COUNT; i++) {\n        const depth = i / (LAYER_COUNT - 1); // 0 to 1\n        const layerForms = baseForms.map(baseForm => {\n          // Create variations based on depth\n          const scale = 0.8 + depth * 0.4; // Deeper layers are smaller\n          return {\n            ...baseForm,\n            centerX: baseForm.centerX + (depth - 0.5) * 30 * sizeFactor, // Offset deeper layers for parallax\n            centerY: baseForm.centerY + (depth - 0.5) * 20 * sizeFactor,\n            radiusX: baseForm.radiusX * scale,\n            radiusY: baseForm.radiusY * scale,\n            rotation: baseForm.rotation + depth * Math.PI * 0.1, // Slight rotation offset\n            depth: depth,\n            lineCount: Math.floor(30 - depth * 15), \n            lineWidth: 0.5 + depth * 0.7, \n            opacity: 0.2 + depth * 0.8, // Deeper layers are more opaque\n            speed: (0.5 + depth * 1.5) * movementFactor // Reduced movement factor\n          };\n        });\n        \n        layers.push({\n          depth: depth,\n          forms: layerForms\n        });\n      }\n    };\n    \n    const drawSpiralForm = (form, lineCount) => {\n      const { centerX, centerY, radiusX, radiusY, rotation, depth, lineWidth, opacity, speed } = form;\n      \n      // Move with nature's unceasing momentum\n      const breathFactor = Math.sin(time * 0.2 * speed + form.phase) * 0.15 * movementFactor + 1;\n      const currentRadiusX = radiusX * breathFactor;\n      const currentRadiusY = radiusY * breathFactor;\n      \n      // Enhanced back-and-forth rotation\n      const oscillatingRotation = Math.sin(time * 0.15) * 0.2 * rotationFactor;\n      const currentRotation = rotation + oscillatingRotation;\n      \n      for (let i = 0; i < lineCount; i++) {\n        const scale = i / lineCount;\n        const currentScale = scale * 0.9; // Leave a small hole in the middle\n        \n        ctx.beginPath();\n        \n        for (let angle = 0; angle <= Math.PI * 2; angle += 0.05) {\n          const spiralOffset = angle * 0.2;\n          const r = currentScale + Math.sin(angle * 10 + time * 0.1 * speed + form.phase) * 0.01 * movementFactor;\n          \n          // Calculate spiral wave effect - reduced amplitude\n          const waveX = Math.sin(angle * 5 + time * 0.1 * speed) * radiusX * 0.05 * scale * movementFactor;\n          const waveY = Math.cos(angle * 5 + time * 0.1 * speed) * radiusY * 0.05 * scale * movementFactor;\n          \n          // Calculate position with enhanced back-and-forth rotation\n          const rX = currentRadiusX * r * Math.cos(angle + spiralOffset + currentRotation + time * 0.02 * speed);\n          const rY = currentRadiusY * r * Math.sin(angle + spiralOffset + currentRotation + time * 0.02 * speed);\n          \n          const x = centerX + rX + waveX;\n          const y = centerY + rY + waveY;\n          \n          if (angle === 0) {\n            ctx.moveTo(x, y);\n          } else {\n            ctx.lineTo(x, y);\n          }\n        }\n        \n        ctx.closePath();\n        \n        // Adjust opacity based on scale\n        const lineOpacity = opacity * (0.2 + scale * 0.8);\n        // Using dark gray for project colors\n        ctx.strokeStyle = `rgba(50, 50, 50, ${lineOpacity})`;\n        ctx.lineWidth = lineWidth;\n        ctx.stroke();\n      }\n    };\n    \n    const render = () => {\n      // Project cream color background\n      ctx.fillStyle = '#F0EEE6';\n      ctx.fillRect(0, 0, canvas.width, canvas.height);\n      \n      // Draw layers from back to front\n      for (let i = 0; i < layers.length; i++) {\n        const layer = layers[i];\n        \n        // Draw each form in the layer\n        for (const form of layer.forms) {\n          drawSpiralForm(form, form.lineCount);\n        }\n      }\n      \n      // Animate over time\n      time += 0.005;\n      animationFrameId = requestAnimationFrame(render);\n    };\n    \n    setupCanvas();\n    render();\n    \n    return () => {\n      cancelAnimationFrame(animationFrameId);\n      \n      // Clear canvas context\n      if (ctx) {\n        ctx.clearRect(0, 0, canvas.width, canvas.height);\n      }\n      \n      // Clear layers array to prevent memory leaks\n      layers.length = 0;\n    }\n  }, []);\n  \n  return (\n    <div className=\"w-full h-full flex items-center justify-center bg-[#F0EEE6]\">\n      <canvas\n        ref={canvasRef}\n        className=\"max-w-full max-h-full shadow-lg\"\n      />\n    </div>\n  );\n};\n\nexport default DimensionalResonance;",sourceCodeMin:"import { useEffect, useRef } from 'react'; const DimensionalResonance = () => { const canvasRef = useRef(null); useEffect(() => { const canvas = canvasRef.current; const ctx = canvas.getContext('2d'); let animationFrameId; let time = 0; const LAYER_COUNT = 5; const layers = []; const sizeFactor = 1.125; const movementFactor = 1.1; const rotationFactor = 1.8; const baseForms = [ { centerX: 225 * sizeFactor, centerY: 200 * sizeFactor, radiusX: 100 * sizeFactor, radiusY: 100 * sizeFactor, rotation: 0, phase: 0 }, { centerX: 350 * sizeFactor, centerY: 175 * sizeFactor, radiusX: 90 * sizeFactor, radiusY: 100 * sizeFactor, rotation: Math.PI / 6, phase: 2 }, { centerX: 275 * sizeFactor, centerY: 325 * sizeFactor, radiusX: 100 * sizeFactor, radiusY: 90 * sizeFactor, rotation: -Math.PI / 4, phase: 4 } ]; const setupCanvas = () => { canvas.width = 550; canvas.height = 550; const centerAdjustX = (550 / 2) - (275 * sizeFactor); const centerAdjustY = (550 / 2) - (225 * sizeFactor); baseForms.forEach(form => { form.centerX += centerAdjustX; form.centerY += centerAdjustY; }); for (let i = 0; i < LAYER_COUNT; i++) { const depth = i / (LAYER_COUNT - 1); const layerForms = baseForms.map(baseForm => { const scale = 0.8 + depth * 0.4; return { ...baseForm, centerX: baseForm.centerX + (depth - 0.5) * 30 * sizeFactor, centerY: baseForm.centerY + (depth - 0.5) * 20 * sizeFactor, radiusX: baseForm.radiusX * scale, radiusY: baseForm.radiusY * scale, rotation: baseForm.rotation + depth * Math.PI * 0.1, depth: depth, lineCount: Math.floor(30 - depth * 15), lineWidth: 0.5 + depth * 0.7, opacity: 0.2 + depth * 0.8, speed: (0.5 + depth * 1.5) * movementFactor }; }); layers.push({ depth: depth, forms: layerForms }); } }; const drawSpiralForm = (form, lineCount) => { const { centerX, centerY, radiusX, radiusY, rotation, depth, lineWidth, opacity, speed } = form; const breathFactor = Math.sin(time * 0.2 * speed + form.phase) * 0.15 * movementFactor + 1; const currentRadiusX = radiusX * breathFactor; const currentRadiusY = radiusY * breathFactor; const oscillatingRotation = Math.sin(time * 0.15) * 0.2 * rotationFactor; const currentRotation = rotation + oscillatingRotation; for (let i = 0; i < lineCount; i++) { const scale = i / lineCount; const currentScale = scale * 0.9; ctx.beginPath(); for (let angle = 0; angle <= Math.PI * 2; angle += 0.05) { const spiralOffset = angle * 0.2; const r = currentScale + Math.sin(angle * 10 + time * 0.1 * speed + form.phase) * 0.01 * movementFactor; const waveX = Math.sin(angle * 5 + time * 0.1 * speed) * radiusX * 0.05 * scale * movementFactor; const waveY = Math.cos(angle * 5 + time * 0.1 * speed) * radiusY * 0.05 * scale * movementFactor; const rX = currentRadiusX * r * Math.cos(angle + spiralOffset + currentRotation + time * 0.02 * speed); const rY = currentRadiusY * r * Math.sin(angle + spiralOffset + currentRotation + time * 0.02 * speed); const x = centerX + rX + waveX; const y = centerY + rY + waveY; if (angle === 0) { ctx.moveTo(x, y); } else { ctx.lineTo(x, y); } } ctx.closePath(); const lineOpacity = opacity * (0.2 + scale * 0.8); ctx.strokeStyle = `rgba(50, 50, 50, ${lineOpacity})`; ctx.lineWidth = lineWidth; ctx.stroke(); } }; const render = () => { ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, canvas.width, canvas.height); for (let i = 0; i < layers.length; i++) { const layer = layers[i]; for (const form of layer.forms) { drawSpiralForm(form, form.lineCount); } } time += 0.005; animationFrameId = requestAnimationFrame(render); }; setupCanvas(); render(); return () => { cancelAnimationFrame(animationFrameId); if (ctx) { ctx.clearRect(0, 0, canvas.width, canvas.height); } layers.length = 0; } }, []); return ( <div className=\"w-full h-full flex items-center justify-center bg-[#F0EEE6]\"><canvas ref={canvasRef} className=\"max-w-full max-h-full shadow-lg\" /></div> ); }; export default DimensionalResonance;",text:"He who relies on Nature’s Way in leading others\nhas no reason to overpower.\nFor every force, there is a counterforce.\nCoercion always rebounds upon oneself.\nSimply do what needs to be done\nwithout taking advantage of position.\n\nAchieve results, but never revel.\nAchieve results, but never boast.\nAchieve results, but never be prideful.\nAchieve results, because this is Nature’s Way.\nAchieve results, but not through overpowering.\n\nThe Vibe Coder does his job and then stops.\nHe knows that the universe is forever\nout of control.\nAny attempt to dominate will come up\nagainst Source’s unceasing momentum.\n\nBecause he believes in himself,\nhe doesn’t try to convince others.\nBecause he is content with himself,\nhe doesn’t need others’ approval.\nBecause he accepts himself,\nall the world accepts him.",shouldDisplayMobileArtwork:!0,promptSuggestion:"make it magnetic"},{number:31,staticArtwork:de,sourceCode:"// Poem 31 themes: Structure, Simplicity, Flow\n// Visualization: Wireframe Möbius strip with smooth animation\n\n// @ts-nocheck\nimport React, { useRef, useEffect } from 'react'\nimport * as THREE from 'three'\n\nconst metadata = {\n  themes: \"Structure, Simplicity, Flow\",\n  visualization: \"Wireframe Möbius strip with smooth animation\",\n  promptSuggestion: \"1. Adjust the twist rate\\n2. Change the ribbon dimensions\\n3. Alter the motion dynamics\\n4. Modify the wireframe density\\n5. Change the rotation speed\"\n}\n\nconst ShinyLoop: React.FC = () => {\n  const containerRef = useRef<HTMLDivElement>(null)\n\n  useEffect(() => {\n    if (!containerRef.current) return\n    const container = containerRef.current\n    \n    const numPoints = 300\n    const ribbonWidth = 0.5\n    const SPRING_STRENGTH = 0.03\n    const DAMPING = 0.99\n    const MOMENTUM = 0.95\n    const GRAB_INFLUENCE = 3.0\n    \n    let mouse = { x: 0, y: 0, z: 0 }\n    let target = { x: 0, y: 0, z: 0 }\n    let velocity = { x: 0, y: 0, z: 0 }\n    let isGrabbed = false\n    let grabPoint = 0\n\n    // Create ribbon geometry\n    const createGeometry = () => {\n      const geometry = new THREE.BufferGeometry()\n      const positions = new Float32Array(numPoints * 2 * 3)\n      const normals = new Float32Array(numPoints * 2 * 3)\n      const indices = []\n\n      // Create triangles\n      for (let i = 0; i < numPoints - 1; i++) {\n        indices.push(i * 2, i * 2 + 1, (i + 1) * 2)\n        indices.push(i * 2 + 1, (i + 1) * 2 + 1, (i + 1) * 2)\n      }\n\n      // Close the loop for Möbius strip\n      indices.push((numPoints - 1) * 2, (numPoints - 1) * 2 + 1, 0)\n      indices.push((numPoints - 1) * 2 + 1, 1, 0)\n\n      geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3))\n      geometry.setAttribute('normal', new THREE.BufferAttribute(normals, 3))\n      geometry.setIndex(indices)\n\n      return geometry\n    }\n\n    // Create wireframe material\n    const createMaterial = () => {\n      return new THREE.MeshBasicMaterial({\n        color: 0x000000,\n        wireframe: true,\n        wireframeLinewidth: 1,\n        side: THREE.DoubleSide\n      })\n    }\n\n    const width = container.clientWidth\n    const height = container.clientHeight\n    const dpr = window.devicePixelRatio || 1\n\n    // Setup scene\n    const scene = new THREE.Scene()\n    const camera = new THREE.PerspectiveCamera(75, width / height, 0.1, 1000)\n    const renderer = new THREE.WebGLRenderer({ antialias: true })\n    \n    renderer.setPixelRatio(Math.min(dpr, 2))\n    renderer.setSize(width, height)\n    renderer.setClearColor(0xF0EEE6)\n    container.appendChild(renderer.domElement)\n\n    // Create geometry and material\n    const geometry = createGeometry()\n    const material = createMaterial()\n    const mesh = new THREE.Mesh(geometry, material)\n    \n    const groupRef = new THREE.Group()\n    groupRef.add(mesh)\n    scene.add(groupRef)\n\n    // Position camera\n    camera.position.set(0, 0, 4)\n    camera.lookAt(0, 0, 0)\n\n    // Mouse interaction handlers\n    const handlePointerDown = (event: MouseEvent) => {\n      const rect = renderer.domElement.getBoundingClientRect()\n      const x = ((event.clientX - rect.left) / rect.width) * 2 - 1\n      const y = -((event.clientY - rect.top) / rect.height) * 2 + 1\n      \n      mouse.x = x * 3\n      mouse.y = y * 3\n      \n      isGrabbed = true\n    }\n\n    const handlePointerMove = (event: MouseEvent) => {\n      const rect = renderer.domElement.getBoundingClientRect()\n      const x = ((event.clientX - rect.left) / rect.width) * 2 - 1\n      const y = -((event.clientY - rect.top) / rect.height) * 2 + 1\n      \n      mouse.x = x * 3\n      mouse.y = y * 3\n    }\n\n    const handlePointerUp = () => {\n      isGrabbed = false\n    }\n\n    renderer.domElement.addEventListener('mousedown', handlePointerDown)\n    renderer.domElement.addEventListener('mousemove', handlePointerMove)\n    renderer.domElement.addEventListener('mouseup', handlePointerUp)\n    renderer.domElement.addEventListener('mouseleave', handlePointerUp)\n\n    // Animation loop\n    let time = 0\n    let animationFrameId: number\n    \n    const animate = () => {\n      animationFrameId = requestAnimationFrame(animate)\n      \n      time += 0.0015\n      \n      // Update physics target\n      if (isGrabbed) {\n        target.x += (mouse.x - target.x) * 0.3\n        target.y += (mouse.y - target.y) * 0.3\n        target.z += (mouse.z - target.z) * 0.3\n      }\n      \n      const positions = mesh.geometry.attributes.position.array\n      const normals = mesh.geometry.attributes.normal.array\n      \n      for (let i = 0; i < numPoints; i++) {\n        const t = i / (numPoints - 1)\n        const angle = t * Math.PI * 2\n        \n        // Calculate influence from grabbed point\n        let influence = 0\n        if (isGrabbed) {\n          const distFromGrab = Math.abs(t - grabPoint)\n          influence = Math.max(0, 1 - distFromGrab * GRAB_INFLUENCE)\n          influence = Math.pow(influence, 2)\n        }\n        \n        // Base radius with breathing effect (25% larger)\n        const baseRadius = 1.875 + Math.sin(time * 0.5 + t * Math.PI * 2) * 0.125\n        const radius = baseRadius * (1 - influence * 0.3)\n        \n        // Calculate base position\n        let baseX = Math.cos(angle) * radius\n        let baseY = Math.sin(angle) * radius\n        let baseZ = 0\n        \n        // Apply grab influence\n        if (influence > 0) {\n          const targetOffsetX = (target.x - baseX) * influence\n          const targetOffsetY = (target.y - baseY) * influence\n          const targetOffsetZ = (target.z - baseZ) * influence\n          \n          baseX += targetOffsetX + velocity.x * influence\n          baseY += targetOffsetY + velocity.y * influence\n          baseZ += targetOffsetZ + velocity.z * influence\n        }\n        \n        // Calculate twist\n        const twist = t * Math.PI + time * 0.2 * (1 - influence * 0.5)\n        \n        // Add wave\n        const wave = Math.sin(angle * 3 + time * 2) * 0.1\n        const waveX = Math.cos(angle + Math.PI/2) * wave\n        const waveY = Math.sin(angle + Math.PI/2) * wave\n        const waveZ = Math.sin(angle * 2 + time) * 0.2\n        \n        // Calculate tangent vector\n        const tangentX = -Math.sin(angle)\n        const tangentY = Math.cos(angle)\n        const tangentZ = 0\n        \n        // Calculate normal vector with twist\n        const normalX = Math.cos(angle + twist)\n        const normalY = Math.sin(angle + twist)\n        const normalZ = Math.sin(twist)\n        \n        // Calculate binormal\n        const binormalX = tangentY * normalZ - tangentZ * normalY\n        const binormalY = tangentZ * normalX - tangentX * normalZ\n        const binormalZ = tangentX * normalY - tangentY * normalX\n        \n        // Width variation\n        const width = ribbonWidth * (1.0 + Math.sin(twist * 2) * 0.1)\n        \n        // Set positions\n        const idx = i * 6\n        positions[idx] = baseX + waveX + binormalX * width\n        positions[idx + 1] = baseY + waveY + binormalY * width\n        positions[idx + 2] = baseZ + waveZ + binormalZ * width\n        positions[idx + 3] = baseX + waveX - binormalX * width\n        positions[idx + 4] = baseY + waveY - binormalY * width\n        positions[idx + 5] = baseZ + waveZ - binormalZ * width\n        \n        // Set normals\n        normals[idx] = normalX\n        normals[idx + 1] = normalY\n        normals[idx + 2] = normalZ\n        normals[idx + 3] = -normalX\n        normals[idx + 4] = -normalY\n        normals[idx + 5] = -normalZ\n      }\n      \n      mesh.geometry.attributes.position.needsUpdate = true\n      mesh.geometry.attributes.normal.needsUpdate = true\n      \n      renderer.render(scene, camera)\n    }\n    \n    animate()\n\n    // Handle resize\n    const handleResize = () => {\n      const width = container.clientWidth\n      const height = container.clientHeight\n      const dpr = window.devicePixelRatio || 1\n      \n      camera.aspect = width / height\n      camera.updateProjectionMatrix()\n      renderer.setPixelRatio(Math.min(dpr, 2))\n      renderer.setSize(width, height)\n    }\n    \n    window.addEventListener('resize', handleResize)\n\n    // Cleanup\n    return () => {\n      window.removeEventListener('resize', handleResize)\n      \n      renderer.domElement.removeEventListener('mousedown', handlePointerDown)\n      renderer.domElement.removeEventListener('mousemove', handlePointerMove)\n      renderer.domElement.removeEventListener('mouseup', handlePointerUp)\n      renderer.domElement.removeEventListener('mouseleave', handlePointerUp)\n      \n      cancelAnimationFrame(animationFrameId)\n      \n      if (renderer) {\n        renderer.dispose()\n        if (container.contains(renderer.domElement)) {\n          container.removeChild(renderer.domElement)\n        }\n      }\n      \n      if (geometry) geometry.dispose()\n      if (material) material.dispose()\n    }\n  }, [])\n\n  return (\n    <div \n      ref={containerRef}\n      style={{ \n        margin: 0,\n        background: '#F0EEE6',\n        overflow: 'hidden',\n        display: 'flex',\n        alignItems: 'center',\n        justifyContent: 'center',\n        height: '100vh',\n        width: '100vw',\n        position: 'relative'\n      }}\n    />\n  )\n}\n\nShinyLoop.metadata = metadata\nexport default ShinyLoop",sourceCodeMin:"import React, { useRef, useEffect } from 'react' import * as THREE from 'three' const metadata = { themes: \"Structure, Simplicity, Flow\", visualization: \"Wireframe Möbius strip with smooth animation\", promptSuggestion: \"1. Adjust the twist rate\\n2. Change the ribbon dimensions\\n3. Alter the motion dynamics\\n4. Modify the wireframe density\\n5. Change the rotation speed\" } const ShinyLoop: React.FC = () => { const containerRef = useRef<HTMLDivElement>(null) useEffect(() => { if (!containerRef.current) return const container = containerRef.current const numPoints = 300 const ribbonWidth = 0.5 const SPRING_STRENGTH = 0.03 const DAMPING = 0.99 const MOMENTUM = 0.95 const GRAB_INFLUENCE = 3.0 let mouse = { x: 0, y: 0, z: 0 } let target = { x: 0, y: 0, z: 0 } let velocity = { x: 0, y: 0, z: 0 } let isGrabbed = false let grabPoint = 0 const createGeometry = () => { const geometry = new THREE.BufferGeometry() const positions = new Float32Array(numPoints * 2 * 3) const normals = new Float32Array(numPoints * 2 * 3) const indices = [] for (let i = 0; i < numPoints - 1; i++) { indices.push(i * 2, i * 2 + 1, (i + 1) * 2) indices.push(i * 2 + 1, (i + 1) * 2 + 1, (i + 1) * 2) } indices.push((numPoints - 1) * 2, (numPoints - 1) * 2 + 1, 0) indices.push((numPoints - 1) * 2 + 1, 1, 0) geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3)) geometry.setAttribute('normal', new THREE.BufferAttribute(normals, 3)) geometry.setIndex(indices) return geometry } const createMaterial = () => { return new THREE.MeshBasicMaterial({ color: 0x000000, wireframe: true, wireframeLinewidth: 1, side: THREE.DoubleSide }) } const width = container.clientWidth const height = container.clientHeight const dpr = window.devicePixelRatio || 1 const scene = new THREE.Scene() const camera = new THREE.PerspectiveCamera(75, width / height, 0.1, 1000) const renderer = new THREE.WebGLRenderer({ antialias: true }) renderer.setPixelRatio(Math.min(dpr, 2)) renderer.setSize(width, height) renderer.setClearColor(0xF0EEE6) container.appendChild(renderer.domElement) const geometry = createGeometry() const material = createMaterial() const mesh = new THREE.Mesh(geometry, material) const groupRef = new THREE.Group() groupRef.add(mesh) scene.add(groupRef) camera.position.set(0, 0, 4) camera.lookAt(0, 0, 0) const handlePointerDown = (event: MouseEvent) => { const rect = renderer.domElement.getBoundingClientRect() const x = ((event.clientX - rect.left) / rect.width) * 2 - 1 const y = -((event.clientY - rect.top) / rect.height) * 2 + 1 mouse.x = x * 3 mouse.y = y * 3 isGrabbed = true } const handlePointerMove = (event: MouseEvent) => { const rect = renderer.domElement.getBoundingClientRect() const x = ((event.clientX - rect.left) / rect.width) * 2 - 1 const y = -((event.clientY - rect.top) / rect.height) * 2 + 1 mouse.x = x * 3 mouse.y = y * 3 } const handlePointerUp = () => { isGrabbed = false } renderer.domElement.addEventListener('mousedown', handlePointerDown) renderer.domElement.addEventListener('mousemove', handlePointerMove) renderer.domElement.addEventListener('mouseup', handlePointerUp) renderer.domElement.addEventListener('mouseleave', handlePointerUp) let time = 0 let animationFrameId: number const animate = () => { animationFrameId = requestAnimationFrame(animate) time += 0.0015 if (isGrabbed) { target.x += (mouse.x - target.x) * 0.3 target.y += (mouse.y - target.y) * 0.3 target.z += (mouse.z - target.z) * 0.3 } const positions = mesh.geometry.attributes.position.array const normals = mesh.geometry.attributes.normal.array for (let i = 0; i < numPoints; i++) { const t = i / (numPoints - 1) const angle = t * Math.PI * 2 let influence = 0 if (isGrabbed) { const distFromGrab = Math.abs(t - grabPoint) influence = Math.max(0, 1 - distFromGrab * GRAB_INFLUENCE) influence = Math.pow(influence, 2) } const baseRadius = 1.875 + Math.sin(time * 0.5 + t * Math.PI * 2) * 0.125 const radius = baseRadius * (1 - influence * 0.3) let baseX = Math.cos(angle) * radius let baseY = Math.sin(angle) * radius let baseZ = 0 if (influence > 0) { const targetOffsetX = (target.x - baseX) * influence const targetOffsetY = (target.y - baseY) * influence const targetOffsetZ = (target.z - baseZ) * influence baseX += targetOffsetX + velocity.x * influence baseY += targetOffsetY + velocity.y * influence baseZ += targetOffsetZ + velocity.z * influence } const twist = t * Math.PI + time * 0.2 * (1 - influence * 0.5) const wave = Math.sin(angle * 3 + time * 2) * 0.1 const waveX = Math.cos(angle + Math.PI/2) * wave const waveY = Math.sin(angle + Math.PI/2) * wave const waveZ = Math.sin(angle * 2 + time) * 0.2 const tangentX = -Math.sin(angle) const tangentY = Math.cos(angle) const tangentZ = 0 const normalX = Math.cos(angle + twist) const normalY = Math.sin(angle + twist) const normalZ = Math.sin(twist) const binormalX = tangentY * normalZ - tangentZ * normalY const binormalY = tangentZ * normalX - tangentX * normalZ const binormalZ = tangentX * normalY - tangentY * normalX const width = ribbonWidth * (1.0 + Math.sin(twist * 2) * 0.1) const idx = i * 6 positions[idx] = baseX + waveX + binormalX * width positions[idx + 1] = baseY + waveY + binormalY * width positions[idx + 2] = baseZ + waveZ + binormalZ * width positions[idx + 3] = baseX + waveX - binormalX * width positions[idx + 4] = baseY + waveY - binormalY * width positions[idx + 5] = baseZ + waveZ - binormalZ * width normals[idx] = normalX normals[idx + 1] = normalY normals[idx + 2] = normalZ normals[idx + 3] = -normalX normals[idx + 4] = -normalY normals[idx + 5] = -normalZ } mesh.geometry.attributes.position.needsUpdate = true mesh.geometry.attributes.normal.needsUpdate = true renderer.render(scene, camera) } animate() const handleResize = () => { const width = container.clientWidth const height = container.clientHeight const dpr = window.devicePixelRatio || 1 camera.aspect = width / height camera.updateProjectionMatrix() renderer.setPixelRatio(Math.min(dpr, 2)) renderer.setSize(width, height) } window.addEventListener('resize', handleResize) return () => { window.removeEventListener('resize', handleResize) renderer.domElement.removeEventListener('mousedown', handlePointerDown) renderer.domElement.removeEventListener('mousemove', handlePointerMove) renderer.domElement.removeEventListener('mouseup', handlePointerUp) renderer.domElement.removeEventListener('mouseleave', handlePointerUp) cancelAnimationFrame(animationFrameId) if (renderer) { renderer.dispose() if (container.contains(renderer.domElement)) { container.removeChild(renderer.domElement) } } if (geometry) geometry.dispose() if (material) material.dispose() } }, []) return ( <div ref={containerRef} style={{ margin: 0, background: '#F0EEE6', overflow: 'hidden', display: 'flex', alignItems: 'center', justifyContent: 'center', height: '100vh', width: '100vw', position: 'relative' }} /> ) } ShinyLoop.metadata = metadata export default ShinyLoop",text:"The Vibe Coder chooses simplicity.\nOnly when there is no choice\nwill he work with complexity.\n\nHe finds no joy in complex solutions.\nThose who find joy in complexity,\ndelight in the confusion of others.\n\nIn creation, honor the positive.\nIn destruction, honor the negative.\n\nThe Vibe Coder stands in ritual neutrality.\n\nWhen we face matters of complexity,\nwe honor them like funeral rites.\nWhen we engage in conflict,\nwe lament the costs with sorrow.\nWhen we win a great battle,\nwe observe the rules of mourning.",shouldDisplayMobileArtwork:!0,promptSuggestion:"what if you braided it?"},{number:32,staticArtwork:te,sourceCode:"// @ts-nocheck\nimport React, { useEffect, useRef } from 'react';\n\n// themes: source remains unrefined, natural following, return to river\n// visualization: Waves flow naturally from an unrefined source, always returning home\n\nconst WaveVariationSparse = () => {\n  const canvasRef = useRef(null);\n\n  useEffect(() => {\n    const canvas = canvasRef.current;\n    const ctx = canvas.getContext('2d', { alpha: false });\n    const width = 550;\n    const height = 550;\n    canvas.width = width;\n    canvas.height = height;\n\n    const resolution = 4;\n    const rows = Math.floor(height / resolution);\n    const cols = Math.floor(width / resolution);\n    \n    // Much fewer sources for sparse pattern\n    const sources = [];\n    const numSpirals = 1; // Just one spiral\n    const pointsPerSpiral = 5; // Very few points\n    \n    // Create unrefined source points that flow like a river\n    for (let i = 0; i < pointsPerSpiral; i++) {\n      const t = i / pointsPerSpiral;\n      const angle = t * 2 * Math.PI * 1.5; // Just 1.5 turns\n      const radius = 100 + t * 150; // More compact spiral\n      \n      sources.push({\n        x: width/2 + Math.cos(angle) * radius,\n        y: height/2 + Math.sin(angle) * radius,\n        wavelength: 60 + t * 30, // Much longer wavelengths\n        phase: angle,\n        amplitude: 1.0\n      });\n    }\n    \n    // Just one center point\n    sources.push({\n      x: width/2,\n      y: height/2,\n      wavelength: 80,\n      phase: 0,\n      amplitude: 1.5\n    });\n\n    // Animation timing control variables\n    let time = 0;\n    let animationFrameId = null;\n    let lastFrameTime = 0;\n    const targetFPS = 20; // 20 fps for smooth animation with controlled timing\n    const frameInterval = 1000 / targetFPS;\n    const field = new Float32Array(rows * cols);\n    \n    const bufferCanvas = document.createElement('canvas');\n    bufferCanvas.width = width;\n    bufferCanvas.height = height;\n    const bufferCtx = bufferCanvas.getContext('2d', { alpha: false });\n\n    const animate = (currentTime) => {\n      // Initialize lastFrameTime on first frame\n      if (!lastFrameTime) {\n        lastFrameTime = currentTime;\n      }\n      \n      const deltaTime = currentTime - lastFrameTime;\n      \n      // Only update animation when enough time has passed\n      if (deltaTime >= frameInterval) {\n        // Calculate remainder to prevent drift\n        const remainder = deltaTime % frameInterval;\n        \n        // Update lastFrameTime with the time that's been processed\n        lastFrameTime = currentTime - remainder;\n        \n        bufferCtx.fillStyle = '#F0EEE6';\n        bufferCtx.fillRect(0, 0, width, height);\n\n      // Let waves naturally follow their path home\n      for (let i = 0; i < rows; i++) {\n        for (let j = 0; j < cols; j++) {\n          const x = j * resolution;\n          const y = i * resolution;\n          let amplitude = 0;\n\n          for (let s = 0; s < sources.length; s++) {\n            const source = sources[s];\n            const dx = x - source.x;\n            const dy = y - source.y;\n            const distance = Math.sqrt(dx * dx + dy * dy);\n            \n            // Sharper falloff for more defined negative space\n            const falloff = Math.exp(-distance / 250);\n            \n            amplitude += source.amplitude * falloff * \n              Math.sin((distance / source.wavelength - time) * 2 * Math.PI + source.phase);\n          }\n\n          field[i * cols + j] = amplitude;\n        }\n      }\n\n      bufferCtx.strokeStyle = '#333';\n      bufferCtx.lineWidth = 1.2;\n      bufferCtx.beginPath();\n      \n      // Just two contour levels for minimal lines\n      const levels = [-0.2, 0.2];\n      \n      for (const level of levels) {\n        for (let i = 0; i < rows - 1; i++) {\n          for (let j = 0; j < cols - 1; j++) {\n            const idx = i * cols + j;\n            const x = j * resolution;\n            const y = i * resolution;\n            \n            const v00 = field[idx] > level;\n            const v10 = field[idx + 1] > level;\n            const v11 = field[idx + cols + 1] > level;\n            const v01 = field[idx + cols] > level;\n            \n            if (v00 !== v10) {\n              bufferCtx.moveTo(x + resolution / 2, y);\n              bufferCtx.lineTo(x + resolution, y + resolution / 2);\n            }\n            if (v10 !== v11) {\n              bufferCtx.moveTo(x + resolution, y + resolution / 2);\n              bufferCtx.lineTo(x + resolution / 2, y + resolution);\n            }\n            if (v11 !== v01) {\n              bufferCtx.moveTo(x + resolution / 2, y + resolution);\n              bufferCtx.lineTo(x, y + resolution / 2);\n            }\n            if (v01 !== v00) {\n              bufferCtx.moveTo(x, y + resolution / 2);\n              bufferCtx.lineTo(x + resolution / 2, y);\n            }\n          }\n        }\n      }\n      \n      bufferCtx.stroke();\n      ctx.drawImage(bufferCanvas, 0, 0);\n\n        time += 0.00075; // Halved for even slower meditative effect\n      }\n      \n      animationFrameId = requestAnimationFrame(animate);\n    };\n\n    animationFrameId = requestAnimationFrame(animate);\n\n    return () => {\n      // Cancel animation frame to prevent memory leaks\n      if (animationFrameId) {\n        cancelAnimationFrame(animationFrameId);\n      }\n      \n      // Clear main canvas\n      if (ctx) {\n        ctx.clearRect(0, 0, width, height);\n      }\n      \n      // Clear buffer canvas\n      if (bufferCtx) {\n        bufferCtx.clearRect(0, 0, width, height);\n      }\n      \n      // Clear arrays to prevent memory leaks\n      sources.length = 0;\n    };\n  }, []);\n\n  return (\n    <div style={{ \n      margin: 0,\n      background: '#F0EEE6',\n      overflow: 'hidden',\n      display: 'flex',\n      alignItems: 'center',\n      justifyContent: 'center',\n      height: '100%',\n      width: '100%'\n    }}>\n      <div style={{\n        padding: '30px',\n        position: 'relative',\n        width: '100%',\n        height: '100%',\n        display: 'flex',\n        alignItems: 'center',\n        justifyContent: 'center'\n      }}>\n        <canvas \n          ref={canvasRef} \n          style={{ \n            display: 'block',\n            width: '550px',\n            height: '550px'\n          }} \n        />\n      </div>\n    </div>\n  );\n};\n\nexport default WaveVariationSparse;",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; const WaveVariationSparse = () => { const canvasRef = useRef(null); useEffect(() => { const canvas = canvasRef.current; const ctx = canvas.getContext('2d', { alpha: false }); const width = 550; const height = 550; canvas.width = width; canvas.height = height; const resolution = 4; const rows = Math.floor(height / resolution); const cols = Math.floor(width / resolution); const sources = []; const numSpirals = 1; const pointsPerSpiral = 5; for (let i = 0; i < pointsPerSpiral; i++) { const t = i / pointsPerSpiral; const angle = t * 2 * Math.PI * 1.5; const radius = 100 + t * 150; sources.push({ x: width/2 + Math.cos(angle) * radius, y: height/2 + Math.sin(angle) * radius, wavelength: 60 + t * 30, phase: angle, amplitude: 1.0 }); } sources.push({ x: width/2, y: height/2, wavelength: 80, phase: 0, amplitude: 1.5 }); let time = 0; let animationFrameId = null; let lastFrameTime = 0; const targetFPS = 20; const frameInterval = 1000 / targetFPS; const field = new Float32Array(rows * cols); const bufferCanvas = document.createElement('canvas'); bufferCanvas.width = width; bufferCanvas.height = height; const bufferCtx = bufferCanvas.getContext('2d', { alpha: false }); const animate = (currentTime) => { if (!lastFrameTime) { lastFrameTime = currentTime; } const deltaTime = currentTime - lastFrameTime; if (deltaTime >= frameInterval) { const remainder = deltaTime % frameInterval; lastFrameTime = currentTime - remainder; bufferCtx.fillStyle = '#F0EEE6'; bufferCtx.fillRect(0, 0, width, height); for (let i = 0; i < rows; i++) { for (let j = 0; j < cols; j++) { const x = j * resolution; const y = i * resolution; let amplitude = 0; for (let s = 0; s < sources.length; s++) { const source = sources[s]; const dx = x - source.x; const dy = y - source.y; const distance = Math.sqrt(dx * dx + dy * dy); const falloff = Math.exp(-distance / 250); amplitude += source.amplitude * falloff * Math.sin((distance / source.wavelength - time) * 2 * Math.PI + source.phase); } field[i * cols + j] = amplitude; } } bufferCtx.strokeStyle = '#333'; bufferCtx.lineWidth = 1.2; bufferCtx.beginPath(); const levels = [-0.2, 0.2]; for (const level of levels) { for (let i = 0; i < rows - 1; i++) { for (let j = 0; j < cols - 1; j++) { const idx = i * cols + j; const x = j * resolution; const y = i * resolution; const v00 = field[idx] > level; const v10 = field[idx + 1] > level; const v11 = field[idx + cols + 1] > level; const v01 = field[idx + cols] > level; if (v00 !== v10) { bufferCtx.moveTo(x + resolution / 2, y); bufferCtx.lineTo(x + resolution, y + resolution / 2); } if (v10 !== v11) { bufferCtx.moveTo(x + resolution, y + resolution / 2); bufferCtx.lineTo(x + resolution / 2, y + resolution); } if (v11 !== v01) { bufferCtx.moveTo(x + resolution / 2, y + resolution); bufferCtx.lineTo(x, y + resolution / 2); } if (v01 !== v00) { bufferCtx.moveTo(x, y + resolution / 2); bufferCtx.lineTo(x + resolution / 2, y); } } } } bufferCtx.stroke(); ctx.drawImage(bufferCanvas, 0, 0); time += 0.00075; } animationFrameId = requestAnimationFrame(animate); }; animationFrameId = requestAnimationFrame(animate); return () => { if (animationFrameId) { cancelAnimationFrame(animationFrameId); } if (ctx) { ctx.clearRect(0, 0, width, height); } if (bufferCtx) { bufferCtx.clearRect(0, 0, width, height); } sources.length = 0; }; }, []); return ( <div style={{ margin: 0, background: '#F0EEE6', overflow: 'hidden', display: 'flex', alignItems: 'center', justifyContent: 'center', height: '100%', width: '100%' }}><div style={{ padding: '30px', position: 'relative', width: '100%', height: '100%', display: 'flex', alignItems: 'center', justifyContent: 'center' }}><canvas ref={canvasRef} style={{ display: 'block', width: '550px', height: '550px' }} /></div></div> ); }; export default WaveVariationSparse;",text:"Source is forever unrefined.\nSmall, in the unformed state,\nyet it cannot be grasped.\n\nIf leaders could harness it,\nall beings would naturally follow.\nHeaven and earth would come together\nand gentle rain would fall.\nPeople would no longer need laws\nand all things would take their course.\n\nOnce the whole is divided, the parts get labeled.\nThere are already enough labels.\nIt helps to know when to stop.\nKnowing when to stop avoids disaster.\n\nSource is like a river\nflowing home to the sea.",shouldDisplayMobileArtwork:!0,promptSuggestion:"what if it was asleep?"},{number:33,staticArtwork:Fn,sourceCode:"import React, { useEffect, useRef } from 'react';\n\n// themes: knowledge brings wisdom, control requires strength, staying centered endures\n// visualization: A centered form gains strength through understanding its own nature\n\nconst TorusFieldDynamics = () => {\n  const canvasRef = useRef<HTMLCanvasElement>(null);\n  \n  useEffect(() => {\n    const canvas = canvasRef.current;\n    if (!canvas) return;\n    \n    const ctx = canvas.getContext('2d');\n    if (!ctx) return;\n    \n    canvas.width = 550;\n    canvas.height = 550;\n    \n    let time = 0;\n    let animationFrameId: number;\n    \n    const animate = () => {\n      ctx.fillStyle = '#F0EEE6';\n      ctx.fillRect(0, 0, canvas.width, canvas.height);\n      \n      time += 0.008;\n      \n      const centerX = canvas.width / 2;\n      const centerY = canvas.height / 2;\n      \n      // Draw field lines of growing wisdom\n      const fieldLines = 40;\n      const toroidalRadius = 120;\n      const poloidalRadius = 60;\n      \n      for (let i = 0; i < fieldLines; i++) {\n        const u = (i / fieldLines) * Math.PI * 2;\n        \n        for (let j = 0; j < fieldLines; j++) {\n          const v = (j / fieldLines) * Math.PI * 2;\n          \n          // Torus parametric equations\n          const x = (toroidalRadius + poloidalRadius * Math.cos(v)) * Math.cos(u);\n          const y = (toroidalRadius + poloidalRadius * Math.cos(v)) * Math.sin(u);\n          const z = poloidalRadius * Math.sin(v);\n          \n          // Project 3D to 2D with perspective\n          const scale = 200 / (200 + z);\n          const screenX = centerX + x * scale;\n          const screenY = centerY + y * scale * 0.5; // Flatten for top view\n          \n          // Add dynamic movement\n          const phase = time + u * 0.5 + v * 0.5;\n          const offset = Math.sin(phase) * 5;\n          \n          ctx.beginPath();\n          ctx.arc(screenX + offset, screenY + offset, 1, 0, Math.PI * 2);\n          ctx.fillStyle = `rgba(80, 80, 80, ${0.3 * scale})`;\n          ctx.fill();\n        }\n      }\n      \n      // Draw energy flow lines\n      const flowLines = 20;\n      for (let i = 0; i < flowLines; i++) {\n        const angle = (i / flowLines) * Math.PI * 2;\n        \n        ctx.beginPath();\n        ctx.strokeStyle = 'rgba(80, 80, 80, 0.2)';\n        ctx.lineWidth = 1;\n        \n        for (let t = 0; t < 1; t += 0.01) {\n          const radius = toroidalRadius + poloidalRadius * Math.cos(t * Math.PI * 2 * 3 + time);\n          const x = centerX + Math.cos(angle + t * Math.PI * 4) * radius;\n          const y = centerY + Math.sin(angle + t * Math.PI * 4) * radius * 0.5;\n          \n          if (t === 0) {\n            ctx.moveTo(x, y);\n          } else {\n            ctx.lineTo(x, y);\n          }\n        }\n        ctx.stroke();\n      }\n      \n      // Draw the enduring center that gives strength\n      const vortexRadius = 30;\n      ctx.beginPath();\n      ctx.arc(centerX, centerY, vortexRadius, 0, Math.PI * 2);\n      ctx.strokeStyle = 'rgba(80, 80, 80, 0.5)';\n      ctx.lineWidth = 2;\n      ctx.stroke();\n      \n      // Vortex spiral\n      ctx.beginPath();\n      const spiralTurns = 3;\n      for (let i = 0; i < 100; i++) {\n        const t = i / 100;\n        const angle = t * Math.PI * 2 * spiralTurns - time * 2;\n        const radius = vortexRadius * (1 - t);\n        \n        const x = centerX + Math.cos(angle) * radius;\n        const y = centerY + Math.sin(angle) * radius;\n        \n        if (i === 0) {\n          ctx.moveTo(x, y);\n        } else {\n          ctx.lineTo(x, y);\n        }\n      }\n      ctx.strokeStyle = 'rgba(80, 80, 80, 0.4)';\n      ctx.lineWidth = 1;\n      ctx.stroke();\n      \n      // Add harmonic rings\n      for (let r = 50; r < 250; r += 30) {\n        ctx.beginPath();\n        ctx.arc(centerX, centerY, r + Math.sin(time + r * 0.01) * 5, 0, Math.PI * 2);\n        ctx.strokeStyle = 'rgba(80, 80, 80, 0.1)';\n        ctx.lineWidth = 1;\n        ctx.stroke();\n      }\n      \n      animationFrameId = requestAnimationFrame(animate);\n    };\n    \n    animate();\n    \n    // Cleanup function that cancels the animation frame\n    return () => {\n      cancelAnimationFrame(animationFrameId);\n      \n      // Clear the canvas\n      if (ctx) {\n        ctx.clearRect(0, 0, canvas.width, canvas.height);\n      }\n    };\n  }, []);\n  \n  return (\n    <div className=\"flex items-center justify-center w-full h-full\" style={{ backgroundColor: '#F0EEE6' }}>\n      <div className=\"w-[550px] h-[550px] shadow-lg rounded-lg overflow-hidden\">\n        <canvas \n          ref={canvasRef} \n          className=\"w-full h-full\"\n        />\n      </div>\n    </div>\n  );\n};\n\nexport default TorusFieldDynamics;",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; const TorusFieldDynamics = () => { const canvasRef = useRef<HTMLCanvasElement>(null); useEffect(() => { const canvas = canvasRef.current; if (!canvas) return; const ctx = canvas.getContext('2d'); if (!ctx) return; canvas.width = 550; canvas.height = 550; let time = 0; let animationFrameId: number; const animate = () => { ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, canvas.width, canvas.height); time += 0.008; const centerX = canvas.width / 2; const centerY = canvas.height / 2; const fieldLines = 40; const toroidalRadius = 120; const poloidalRadius = 60; for (let i = 0; i < fieldLines; i++) { const u = (i / fieldLines) * Math.PI * 2; for (let j = 0; j < fieldLines; j++) { const v = (j / fieldLines) * Math.PI * 2; const x = (toroidalRadius + poloidalRadius * Math.cos(v)) * Math.cos(u); const y = (toroidalRadius + poloidalRadius * Math.cos(v)) * Math.sin(u); const z = poloidalRadius * Math.sin(v); const scale = 200 / (200 + z); const screenX = centerX + x * scale; const screenY = centerY + y * scale * 0.5; const phase = time + u * 0.5 + v * 0.5; const offset = Math.sin(phase) * 5; ctx.beginPath(); ctx.arc(screenX + offset, screenY + offset, 1, 0, Math.PI * 2); ctx.fillStyle = `rgba(80, 80, 80, ${0.3 * scale})`; ctx.fill(); } } const flowLines = 20; for (let i = 0; i < flowLines; i++) { const angle = (i / flowLines) * Math.PI * 2; ctx.beginPath(); ctx.strokeStyle = 'rgba(80, 80, 80, 0.2)'; ctx.lineWidth = 1; for (let t = 0; t < 1; t += 0.01) { const radius = toroidalRadius + poloidalRadius * Math.cos(t * Math.PI * 2 * 3 + time); const x = centerX + Math.cos(angle + t * Math.PI * 4) * radius; const y = centerY + Math.sin(angle + t * Math.PI * 4) * radius * 0.5; if (t === 0) { ctx.moveTo(x, y); } else { ctx.lineTo(x, y); } } ctx.stroke(); } const vortexRadius = 30; ctx.beginPath(); ctx.arc(centerX, centerY, vortexRadius, 0, Math.PI * 2); ctx.strokeStyle = 'rgba(80, 80, 80, 0.5)'; ctx.lineWidth = 2; ctx.stroke(); ctx.beginPath(); const spiralTurns = 3; for (let i = 0; i < 100; i++) { const t = i / 100; const angle = t * Math.PI * 2 * spiralTurns - time * 2; const radius = vortexRadius * (1 - t); const x = centerX + Math.cos(angle) * radius; const y = centerY + Math.sin(angle) * radius; if (i === 0) { ctx.moveTo(x, y); } else { ctx.lineTo(x, y); } } ctx.strokeStyle = 'rgba(80, 80, 80, 0.4)'; ctx.lineWidth = 1; ctx.stroke(); for (let r = 50; r < 250; r += 30) { ctx.beginPath(); ctx.arc(centerX, centerY, r + Math.sin(time + r * 0.01) * 5, 0, Math.PI * 2); ctx.strokeStyle = 'rgba(80, 80, 80, 0.1)'; ctx.lineWidth = 1; ctx.stroke(); } animationFrameId = requestAnimationFrame(animate); }; animate(); return () => { cancelAnimationFrame(animationFrameId); if (ctx) { ctx.clearRect(0, 0, canvas.width, canvas.height); } }; }, []); return ( <div className=\"flex items-center justify-center w-full h-full\" style={{ backgroundColor: '#F0EEE6' }}><div className=\"w-[550px] h-[550px] shadow-lg rounded-lg overflow-hidden\"><canvas ref={canvasRef} className=\"w-full h-full\" /></div></div> ); }; export default TorusFieldDynamics;",text:"To know others brings intelligence.\nTo know yourself brings wisdom.\nTo control others requires force.\nTo control yourself requires true strength.\n\nContentment allows wealth.\nDiscipline allows perseverance.\nThose who stay in the center endure.\nThose who die, but do not perish,\nabide eternal.",shouldDisplayMobileArtwork:!0,promptSuggestion:"make it more cosmic"},{number:34,staticArtwork:function(){var e=(0,x.useRef)(null);return(0,x.useEffect)((function(){var n=e.current,t=n.getContext("2d");n.width=550,n.height=550;var i,a=0,r=function(e,i,r,o,s){t.beginPath();for(var c=0;c<n.width;c++){var l=2*Math.sin(.02*c+.05*a)+3*Math.cos(.01*c-.03*a),h=e+i*Math.sin(c*r+a+o+l);0===c?t.moveTo(c,h):t.lineTo(c,h)}t.lineWidth=s,t.stroke()},o=function(){t.fillStyle="#F0EEE6",t.fillRect(0,0,n.width,n.height),t.strokeStyle="rgba(51, 51, 51, 0.6)";for(var e=0;e<15;e++){var s=100+(n.height-200)*(e/14),c=10+5*Math.sin(.025*a+.3*e),l=.02+.01*Math.sin(.2*e),h=.3*e,d=1+.5*Math.sin(a+e);r(s,c,l,h,d)}t.globalCompositeOperation="multiply",t.strokeStyle="rgba(51, 51, 51, 0.3)";for(e=0;e<15;e++){var u=100+(n.width-200)*(e/14);c=15+8*Math.cos(.04*a+.3*e),l=.02+.01*Math.cos(.3*e),h=.4*e,d=1+.5*Math.cos(a+e);t.save(),t.translate(n.width/2,n.height/2),t.rotate(Math.PI/2),t.translate(-n.width/2,-n.height/2),r(u,c,l,h,d),t.restore()}t.globalCompositeOperation="source-over",a+=.004,i=requestAnimationFrame(o)};return o(),function(){i&&cancelAnimationFrame(i),t&&t.clearRect(0,0,n.width,n.height)}}),[]),(0,i.jsx)("div",{className:"w-full h-screen flex items-center justify-center",style:{backgroundColor:"#F0EEE6"},children:(0,i.jsx)("div",{style:{width:"550px",height:"550px"},className:"border border-gray-200 rounded-md shadow-md",children:(0,i.jsx)("canvas",{ref:e,className:"w-full h-full rounded-md"})})})},sourceCode:"import React, { useEffect, useRef } from 'react';\n\n// themes: source flows everywhere, silent fulfillment, humble greatness\n// visualization: Waves flow silently in all directions, achieving greatness without recognition\n\nexport default function PhaseDistortionWaves() {\n  const canvasRef = useRef(null);\n  \n  useEffect(() => {\n    const canvas = canvasRef.current;\n    const ctx = canvas.getContext('2d');\n    \n    // Set canvas dimensions\n    canvas.width = 550;\n    canvas.height = 550;\n    \n    let t = 0;\n    \n    // Draw waves that silently fulfill their nature\n    const drawWave = (yCenter, amplitude, frequency, phaseOffset, thickness) => {\n      ctx.beginPath();\n      \n      // Draw wave path\n      for (let x = 0; x < canvas.width; x++) {\n        // Calculate phase distortion based on position and time (slowed down)\n        const distortion = \n          Math.sin(x * 0.02 + t * 0.05) * 2 + \n          Math.cos(x * 0.01 - t * 0.03) * 3;\n        \n        // Apply distortion to base wave\n        const y = yCenter + \n                 amplitude * Math.sin(x * frequency + t + phaseOffset + distortion);\n        \n        if (x === 0) {\n          ctx.moveTo(x, y);\n        } else {\n          ctx.lineTo(x, y);\n        }\n      }\n      \n      ctx.lineWidth = thickness;\n      ctx.stroke();\n    };\n    \n    let animationFrameId;\n    const render = () => {\n      // Clear canvas with specified background color\n      ctx.fillStyle = '#F0EEE6';\n      ctx.fillRect(0, 0, canvas.width, canvas.height);\n      \n      ctx.strokeStyle = 'rgba(51, 51, 51, 0.6)';\n      \n      // Draw multiple waves with different parameters\n      const waveCount = 15;\n      for (let i = 0; i < waveCount; i++) {\n        const yCenter = 100 + (canvas.height - 200) * (i / (waveCount - 1));\n        const amplitude = 10 + Math.sin(t * 0.025 + i * 0.3) * 5;\n        const frequency = 0.02 + 0.01 * Math.sin(i * 0.2);\n        const phaseOffset = i * 0.3;\n        const thickness = 1 + Math.sin(t + i) * 0.5;\n        \n        drawWave(yCenter, amplitude, frequency, phaseOffset, thickness);\n      }\n      \n      // Source flows in all directions with humble grace\n      ctx.globalCompositeOperation = 'multiply';\n      ctx.strokeStyle = 'rgba(51, 51, 51, 0.3)';\n      \n      for (let i = 0; i < waveCount; i++) {\n        const xCenter = 100 + (canvas.width - 200) * (i / (waveCount - 1));\n        const amplitude = 15 + Math.cos(t * 0.04 + i * 0.3) * 8;\n        const frequency = 0.02 + 0.01 * Math.cos(i * 0.3);\n        const phaseOffset = i * 0.4;\n        const thickness = 1 + Math.cos(t + i) * 0.5;\n        \n        // Draw vertical waves\n        ctx.save();\n        ctx.translate(canvas.width / 2, canvas.height / 2);\n        ctx.rotate(Math.PI / 2);\n        ctx.translate(-canvas.width / 2, -canvas.height / 2);\n        \n        drawWave(xCenter, amplitude, frequency, phaseOffset, thickness);\n        \n        ctx.restore();\n      }\n      \n      ctx.globalCompositeOperation = 'source-over';\n      \n      t += 0.004;\n      animationFrameId = requestAnimationFrame(render);\n    };\n    \n    render();\n    \n    return () => {\n      // Cleanup\n      if (animationFrameId) {\n        cancelAnimationFrame(animationFrameId);\n      }\n      \n      // Clear canvas context\n      if (ctx) {\n        ctx.clearRect(0, 0, canvas.width, canvas.height);\n      }\n    };\n  }, []);\n  \n  return (\n    <div \n      className=\"w-full h-screen flex items-center justify-center\"\n      style={{ backgroundColor: '#F0EEE6' }}\n    >\n      <div \n        style={{ width: '550px', height: '550px' }}\n        className=\"border border-gray-200 rounded-md shadow-md\"\n      >\n        <canvas \n          ref={canvasRef} \n          className=\"w-full h-full rounded-md\"\n        />\n      </div>\n    </div>\n  );\n}",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; export default function PhaseDistortionWaves() { const canvasRef = useRef(null); useEffect(() => { const canvas = canvasRef.current; const ctx = canvas.getContext('2d'); canvas.width = 550; canvas.height = 550; let t = 0; const drawWave = (yCenter, amplitude, frequency, phaseOffset, thickness) => { ctx.beginPath(); for (let x = 0; x < canvas.width; x++) { const distortion = Math.sin(x * 0.02 + t * 0.05) * 2 + Math.cos(x * 0.01 - t * 0.03) * 3; const y = yCenter + amplitude * Math.sin(x * frequency + t + phaseOffset + distortion); if (x === 0) { ctx.moveTo(x, y); } else { ctx.lineTo(x, y); } } ctx.lineWidth = thickness; ctx.stroke(); }; let animationFrameId; const render = () => { ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, canvas.width, canvas.height); ctx.strokeStyle = 'rgba(51, 51, 51, 0.6)'; const waveCount = 15; for (let i = 0; i < waveCount; i++) { const yCenter = 100 + (canvas.height - 200) * (i / (waveCount - 1)); const amplitude = 10 + Math.sin(t * 0.025 + i * 0.3) * 5; const frequency = 0.02 + 0.01 * Math.sin(i * 0.2); const phaseOffset = i * 0.3; const thickness = 1 + Math.sin(t + i) * 0.5; drawWave(yCenter, amplitude, frequency, phaseOffset, thickness); } ctx.globalCompositeOperation = 'multiply'; ctx.strokeStyle = 'rgba(51, 51, 51, 0.3)'; for (let i = 0; i < waveCount; i++) { const xCenter = 100 + (canvas.width - 200) * (i / (waveCount - 1)); const amplitude = 15 + Math.cos(t * 0.04 + i * 0.3) * 8; const frequency = 0.02 + 0.01 * Math.cos(i * 0.3); const phaseOffset = i * 0.4; const thickness = 1 + Math.cos(t + i) * 0.5; ctx.save(); ctx.translate(canvas.width / 2, canvas.height / 2); ctx.rotate(Math.PI / 2); ctx.translate(-canvas.width / 2, -canvas.height / 2); drawWave(xCenter, amplitude, frequency, phaseOffset, thickness); ctx.restore(); } ctx.globalCompositeOperation = 'source-over'; t += 0.004; animationFrameId = requestAnimationFrame(render); }; render(); return () => { if (animationFrameId) { cancelAnimationFrame(animationFrameId); } if (ctx) { ctx.clearRect(0, 0, canvas.width, canvas.height); } }; }, []); return ( <div className=\"w-full h-screen flex items-center justify-center\" style={{ backgroundColor: '#F0EEE6' }} ><div style={{ width: '550px', height: '550px' }} className=\"border border-gray-200 rounded-md shadow-md\" ><canvas ref={canvasRef} className=\"w-full h-full rounded-md\" /></div></div> ); }",text:"Source flows everywhere\nreaching left and right.\nBy it, all things come into being,\nit holds back nothing.\n\nIt fulfills its purpose silently\nwith no recognition claimed.\nIt nourishes infinite worlds,\nyet doesn’t cling to them.\n\nIt merges with all that’s hidden in hearts,\nso it can call itself humble.\nAll things vanish into it and it alone endures.\nThus, we can call it great.\nIt isn’t aware of its greatness.\nThus, it is truly great.",shouldDisplayMobileArtwork:!0,promptSuggestion:"let me play it like a harp"},{number:35,staticArtwork:le,sourceCode:"// @ts-nocheck\nimport React, { useState, useEffect, useRef } from 'react';\n\n// themes: connection to source, universal harmony, inexhaustible use\n// visualization: Patterns emerge endlessly from source, finding harmony in constant transformation\n\nconst patterns = {\n  // Pattern finding harmony in connection to source\n  balance: (x, y, t) => {\n    const cx = 30;\n    const cy = 15;\n    const dx = x - cx;\n    const dy = y - cy;\n    const dist = Math.sqrt(dx * dx + dy * dy);\n    \n    return Math.sin(dx * 0.3 + t * 0.5) * Math.cos(dy * 0.3 + t * 0.3) * \n           Math.sin(dist * 0.1 - t * 0.4);\n  },\n  \n  duality: (x, y, t) => {\n    const cx = 30;\n    const left = x < cx ? Math.sin(x * 0.2 + t * 0.3) : 0;\n    const right = x >= cx ? Math.cos(x * 0.2 - t * 0.3) : 0;\n    \n    return left + right + Math.sin(y * 0.3 + t * 0.2);\n  },\n  \n  flow: (x, y, t) => {\n    const angle = Math.atan2(y - 15, x - 30);\n    const dist = Math.sqrt((x - 30) ** 2 + (y - 15) ** 2);\n    \n    return Math.sin(angle * 3 + t * 0.4) * Math.cos(dist * 0.1 - t * 0.3);\n  },\n  \n  chaos: (x, y, t) => {\n    const noise1 = Math.sin(x * 0.5 + t) * Math.cos(y * 0.3 - t);\n    const noise2 = Math.sin(y * 0.4 + t * 0.5) * Math.cos(x * 0.2 + t * 0.7);\n    const noise3 = Math.sin((x + y) * 0.2 + t * 0.8);\n    \n    return noise1 * 0.3 + noise2 * 0.3 + noise3 * 0.4;\n  }\n};\n\nconst BlackWhiteBlobs = () => {\n  const [frame, setFrame] = useState(0);\n  const [patternType, setPatternType] = useState(0);\n  const [mousePos, setMousePos] = useState({ x: 0, y: 0 });\n  const [mouseDown, setMouseDown] = useState(false);\n  const containerRef = useRef(null);\n  \n  const patternTypes = ['balance', 'duality', 'flow', 'chaos'];\n  const width = 60;\n  const height = 35;\n  const slowdownFactor = 12; // Quadrupled from original speed of 3\n  \n  // Background color as specified in requirements\n  const BACKGROUND_COLOR = '#F0EEE6';\n  \n  useEffect(() => {\n    let animationId;\n    \n    const animate = () => {\n      setFrame(f => (f + 1) % (240 * slowdownFactor));\n      animationId = requestAnimationFrame(animate);\n    };\n    \n    animationId = requestAnimationFrame(animate);\n    \n    return () => {\n      if (animationId) {\n        cancelAnimationFrame(animationId);\n      }\n    };\n  }, [slowdownFactor]);\n  \n  // Generate inexhaustible variations from universal harmony\n  const generateAsciiArt = () => {\n    const t = (frame * Math.PI) / (60 * slowdownFactor);\n    const currentPattern = patterns[patternTypes[patternType]];\n    let result = '';\n    \n    for (let y = 0; y < height; y++) {\n      for (let x = 0; x < width; x++) {\n        let value = currentPattern(x, y, t);\n        \n        if (mouseDown && containerRef.current) {\n          const rect = containerRef.current.getBoundingClientRect();\n          const dx = x - (((mousePos.x - rect.left) / rect.width) * width);\n          const dy = y - (((mousePos.y - rect.top) / rect.height) * height);\n          const dist = Math.sqrt(dx * dx + dy * dy);\n          const mouseInfluence = Math.exp(-dist * 0.1) * Math.sin(t * 2);\n          value += mouseInfluence * 0.8;\n        }\n        \n        if (value > 0.8) {\n          result += '█';\n        } else if (value > 0.5) {\n          result += '▓';\n        } else if (value > 0.2) {\n          result += '▒';\n        } else if (value > -0.2) {\n          result += '░';\n        } else if (value > -0.5) {\n          result += '·';\n        } else {\n          result += ' ';\n        }\n      }\n      result += '\\n';\n    }\n    \n    return result;\n  };\n  \n  const handleClick = () => {\n    setPatternType((prev) => (prev + 1) % patternTypes.length);\n  };\n  \n  const handleMouseMove = (e) => {\n    setMousePos({ x: e.clientX, y: e.clientY });\n  };\n  \n  const handleMouseDown = () => {\n    setMouseDown(true);\n  };\n  \n  const handleMouseUp = () => {\n    setMouseDown(false);\n  };\n  \n  return (\n    <div \n      style={{ \n        margin: 0,\n        padding: 0,\n        background: BACKGROUND_COLOR,\n        overflow: 'hidden',\n        display: 'flex',\n        alignItems: 'center',\n        justifyContent: 'center',\n        height: '100%'\n      }}\n      onMouseMove={handleMouseMove}\n      onMouseDown={handleMouseDown}\n      onMouseUp={handleMouseUp}\n      onClick={handleClick}\n      ref={containerRef}\n    >\n      <pre style={{\n        fontFamily: 'monospace',\n        fontSize: '12px',\n        lineHeight: '1',\n        letterSpacing: '0.1em',\n        color: '#333',\n        userSelect: 'none',\n        cursor: 'pointer',\n        margin: 0,\n        padding: 0\n      }}>\n        {generateAsciiArt()}\n      </pre>\n    </div>\n  );\n};\n\nexport default BlackWhiteBlobs;",sourceCodeMin:"import React, { useState, useEffect, useRef } from 'react'; const patterns = { balance: (x, y, t) => { const cx = 30; const cy = 15; const dx = x - cx; const dy = y - cy; const dist = Math.sqrt(dx * dx + dy * dy); return Math.sin(dx * 0.3 + t * 0.5) * Math.cos(dy * 0.3 + t * 0.3) * Math.sin(dist * 0.1 - t * 0.4); }, duality: (x, y, t) => { const cx = 30; const left = x < cx ? Math.sin(x * 0.2 + t * 0.3) : 0; const right = x >= cx ? Math.cos(x * 0.2 - t * 0.3) : 0; return left + right + Math.sin(y * 0.3 + t * 0.2); }, flow: (x, y, t) => { const angle = Math.atan2(y - 15, x - 30); const dist = Math.sqrt((x - 30) ** 2 + (y - 15) ** 2); return Math.sin(angle * 3 + t * 0.4) * Math.cos(dist * 0.1 - t * 0.3); }, chaos: (x, y, t) => { const noise1 = Math.sin(x * 0.5 + t) * Math.cos(y * 0.3 - t); const noise2 = Math.sin(y * 0.4 + t * 0.5) * Math.cos(x * 0.2 + t * 0.7); const noise3 = Math.sin((x + y) * 0.2 + t * 0.8); return noise1 * 0.3 + noise2 * 0.3 + noise3 * 0.4; } }; const BlackWhiteBlobs = () => { const [frame, setFrame] = useState(0); const [patternType, setPatternType] = useState(0); const [mousePos, setMousePos] = useState({ x: 0, y: 0 }); const [mouseDown, setMouseDown] = useState(false); const containerRef = useRef(null); const patternTypes = ['balance', 'duality', 'flow', 'chaos']; const width = 60; const height = 35; const slowdownFactor = 12; const BACKGROUND_COLOR = '#F0EEE6'; useEffect(() => { let animationId; const animate = () => { setFrame(f => (f + 1) % (240 * slowdownFactor)); animationId = requestAnimationFrame(animate); }; animationId = requestAnimationFrame(animate); return () => { if (animationId) { cancelAnimationFrame(animationId); } }; }, [slowdownFactor]); const generateAsciiArt = () => { const t = (frame * Math.PI) / (60 * slowdownFactor); const currentPattern = patterns[patternTypes[patternType]]; let result = ''; for (let y = 0; y < height; y++) { for (let x = 0; x < width; x++) { let value = currentPattern(x, y, t); if (mouseDown && containerRef.current) { const rect = containerRef.current.getBoundingClientRect(); const dx = x - (((mousePos.x - rect.left) / rect.width) * width); const dy = y - (((mousePos.y - rect.top) / rect.height) * height); const dist = Math.sqrt(dx * dx + dy * dy); const mouseInfluence = Math.exp(-dist * 0.1) * Math.sin(t * 2); value += mouseInfluence * 0.8; } if (value > 0.8) { result += '█'; } else if (value > 0.5) { result += '▓'; } else if (value > 0.2) { result += '▒'; } else if (value > -0.2) { result += '░'; } else if (value > -0.5) { result += '·'; } else { result += ' '; } } result += '\\n'; } return result; }; const handleClick = () => { setPatternType((prev) => (prev + 1) % patternTypes.length); }; const handleMouseMove = (e) => { setMousePos({ x: e.clientX, y: e.clientY }); }; const handleMouseDown = () => { setMouseDown(true); }; const handleMouseUp = () => { setMouseDown(false); }; return ( <div style={{ margin: 0, padding: 0, background: BACKGROUND_COLOR, overflow: 'hidden', display: 'flex', alignItems: 'center', justifyContent: 'center', height: '100%' }} onMouseMove={handleMouseMove} onMouseDown={handleMouseDown} onMouseUp={handleMouseUp} onClick={handleClick} ref={containerRef} ><pre style={{ fontFamily: 'monospace', fontSize: '12px', lineHeight: '1', letterSpacing: '0.1em', color: '#333', userSelect: 'none', cursor: 'pointer', margin: 0, padding: 0 }}> {generateAsciiArt()} </pre></div> ); }; export default BlackWhiteBlobs;",text:"One who is connected to Source\ndraws all the world towards him.\nHe can move freely without risk.\nHe perceives universal harmony,\neven through great complexity,\nbecause he has found peace in his heart.\n\nSource when uttered in words\nis empty and void of flavor.\nWhen you look at it, there’s nothing to see.\nWhen you listen for it, there’s nothing to hear.\nWhen you use it,\nit is inexhaustible.",shouldDisplayMobileArtwork:!0,promptSuggestion:"what's the berlin version?"},{number:36,staticArtwork:On,sourceCode:"// @ts-nocheck\nimport React, { useEffect, useRef } from 'react';\n\n// themes: shrink to expand, weaken to strengthen, subtle perception\n// visualization: Forms contract to find expansion, discovering strength through yielding\n\nconst ParticleReverie = () => {\n  const canvasRef = useRef(null);\n\n  useEffect(() => {\n    const canvas = canvasRef.current;\n    const ctx = canvas.getContext('2d');\n    const width = canvas.width;\n    const height = canvas.height;\n    \n    // Parameters\n    const numParticles = 5000;\n    let time = 65;\n    let startTime = Date.now();\n    \n    // Pre-calculate initial state to match frame 65 exactly\n    const initialT = 65;\n    \n    // Create particles in a formation that matches frame 65 exactly\n    const particles = Array(numParticles).fill().map((_, i) => {\n      const angle1 = (i / numParticles) * Math.PI * 2;\n      const angle2 = Math.random() * Math.PI * 2;\n      \n      // Base shape parameters\n      const baseScale = 120;\n      \n      // Initial organic shape - matching exactly the shape at frame 65\n      const initialMorphFactor = Math.sin(initialT * 0.2) * 0.5 + 0.5;\n      \n      // Calculate shape 1 position at frame 65\n      const r1 = baseScale * (1 + 0.3 * Math.sin(angle1 * 2));\n      const shape1X = r1 * Math.cos(angle1) * (1 + 0.2 * Math.sin(angle1 * 3 + initialT * 0.1));\n      const shape1Y = r1 * Math.sin(angle1) * (1 + 0.4 * Math.cos(angle1 * 3 + initialT * 0.2));\n      \n      // Calculate shape 2 position at frame 65\n      const r2 = baseScale * (1 + 0.4 * Math.sin(angle2 * 3));\n      const shape2X = r2 * Math.cos(angle2) * (1 + 0.3 * Math.sin(angle2 * 2 + initialT * 0.15));\n      const shape2Y = r2 * Math.sin(angle2) * (1 + 0.2 * Math.cos(angle2 * 4 + initialT * 0.25));\n      \n      // Position using same calculation as in calculateTargets\n      let x = width/2 + (shape1X * (1 - initialMorphFactor) + shape2X * initialMorphFactor);\n      let y = height/2 + (shape1Y * (1 - initialMorphFactor) + shape2Y * initialMorphFactor);\n      \n      // Add bulge effect\n      const initialBulgeAmount = 50 * (Math.sin(initialT * 0.3) * 0.2 + 0.8);\n      const bulgeX = initialBulgeAmount * Math.exp(-Math.pow(angle1 - Math.PI * 0.5, 2));\n      const bulgeY = initialBulgeAmount * 0.5 * Math.exp(-Math.pow(angle1 - Math.PI * 0.5, 2));\n      \n      x += bulgeX * Math.sin(initialT * 0.4);\n      y += bulgeY * Math.cos(initialT * 0.3);\n      \n      return {\n        x,\n        y,\n        size: Math.random() * 1.5 + 0.5,\n        connections: [],\n        targetX: 0,\n        targetY: 0,\n        vx: 0,\n        vy: 0,\n        angle: angle2\n      };\n    });\n    \n    // Calculate how forms must contract to find expansion\n    const calculateTargets = (t) => {\n      // Calculate animation time separately to create gradual startup\n      const animationTime = t - 65 + animationStartTime;\n      \n      // Gentle startup for morphFactor - starts at actual value but changes smoothly\n      const startupEasing = Math.min(1, animationTime / 10); // Ramp up over first 10 time units\n      const morphFactor = Math.sin(t * 0.2) * 0.5 + 0.5; // Value between 0 and 1 for morphing\n      \n      particles.forEach((particle, i) => {\n        // Angle for this particle\n        const angle1 = (i / numParticles) * Math.PI * 2;\n        const angle2 = particle.angle;\n        \n        // Base shape parameters\n        const baseScale = 120;\n        const bulgeAmount = 50 * (Math.sin(t * 0.3) * 0.2 + 0.8);\n        \n        // Shape 1: Organic blob - use startup easing for time-based terms\n        const timeEffect1 = t + (animationTime * startupEasing * 0.1); // Gradually introduce time-based movement\n        const r1 = baseScale * (1 + 0.3 * Math.sin(angle1 * 2));\n        const shape1X = r1 * Math.cos(angle1) * (1 + 0.2 * Math.sin(angle1 * 3 + timeEffect1 * 0.1));\n        const shape1Y = r1 * Math.sin(angle1) * (1 + 0.4 * Math.cos(angle1 * 3 + timeEffect1 * 0.2));\n        \n        // Shape 2: Different organic form - use startup easing for time-based terms\n        const timeEffect2 = t + (animationTime * startupEasing * 0.1); // Gradually introduce time-based movement\n        const r2 = baseScale * (1 + 0.4 * Math.sin(angle2 * 3));\n        const shape2X = r2 * Math.cos(angle2) * (1 + 0.3 * Math.sin(angle2 * 2 + timeEffect2 * 0.15));\n        const shape2Y = r2 * Math.sin(angle2) * (1 + 0.2 * Math.cos(angle2 * 4 + timeEffect2 * 0.25));\n        \n        // Morph between shapes\n        particle.targetX = shape1X * (1 - morphFactor) + shape2X * morphFactor;\n        particle.targetY = shape1Y * (1 - morphFactor) + shape2Y * morphFactor;\n        \n        // Add bulge effect - also ease in the time-based movement\n        const bulgeX = bulgeAmount * Math.exp(-Math.pow(angle1 - Math.PI * 0.5, 2));\n        const bulgeY = bulgeAmount * 0.5 * Math.exp(-Math.pow(angle1 - Math.PI * 0.5, 2));\n        \n        // Apply startup easing to bulge movement\n        particle.targetX += bulgeX * Math.sin(timeEffect1 * 0.4);\n        particle.targetY += bulgeY * Math.cos(timeEffect1 * 0.3);\n        \n        // Add noise for more organic feel\n        particle.targetX += (Math.random() - 0.5) * 5;\n        particle.targetY += (Math.random() - 0.5) * 5;\n      });\n    };\n    \n    // Animation runtime - separate from initialization time\n    let animationStartTime = 0;\n        \n    // Function to update particle positions\n    const updateParticles = () => {\n      // Calculate animation time separately to create gradual startup\n      const animationTime = time - 65 + animationStartTime;\n      \n      particles.forEach(particle => {\n        // Direct interpolation instead of spring physics\n        const dx = particle.targetX - (particle.x - width/2);\n        const dy = particle.targetY - (particle.y - height/2);\n        \n        // Gentle linear interpolation (lerp) with gradual startup\n        // Start with very slow easing that gradually increases to normal speed\n        const startupPhase = Math.min(1, animationTime / 5); // Ramp up over first 5 time units \n        const easing = 0.01 + (0.02 * startupPhase); // Gradually increase from 0.01 to 0.03\n        \n        particle.x += dx * easing;\n        particle.y += dy * easing;\n        \n        // Reset connections\n        particle.connections = [];\n      });\n    };\n    \n    // Perceive subtle connections that create strength\n    const calculateConnections = () => {\n      const maxDist = 25;\n      const maxConnections = 3;\n      \n      for (let i = 0; i < particles.length; i++) {\n        const p1 = particles[i];\n        \n        // Limit connections per particle for performance\n        if (p1.connections.length >= maxConnections) continue;\n        \n        for (let j = i + 1; j < particles.length; j++) {\n          const p2 = particles[j];\n          \n          // Skip if second particle already has max connections\n          if (p2.connections.length >= maxConnections) continue;\n          \n          // Calculate distance\n          const dx = p1.x - p2.x;\n          const dy = p1.y - p2.y;\n          const dist = Math.sqrt(dx * dx + dy * dy);\n          \n          // Connect if close enough and not too many connections\n          if (dist < maxDist) {\n            p1.connections.push(j);\n            p2.connections.push(i);\n            \n            // Break if max connections reached\n            if (p1.connections.length >= maxConnections) break;\n          }\n        }\n      }\n    };\n    \n    // Function to draw everything\n    const draw = () => {\n      // Clear canvas with background color\n      ctx.fillStyle = '#F0EEE6';\n      ctx.fillRect(0, 0, width, height);\n      \n      // Draw connections first (below particles)\n      ctx.strokeStyle = 'rgba(51, 51, 51, 0.2)';\n      ctx.lineWidth = 0.5;\n      \n      for (let i = 0; i < particles.length; i++) {\n        const p1 = particles[i];\n        \n        for (let j = 0; j < p1.connections.length; j++) {\n          const p2 = particles[p1.connections[j]];\n          \n          // Draw line\n          ctx.beginPath();\n          ctx.moveTo(p1.x, p1.y);\n          \n          // Add slight curve to lines\n          const midX = (p1.x + p2.x) / 2;\n          const midY = (p1.y + p2.y) / 2;\n          const offset = 5 * Math.sin(time * 0.5 + i * 0.01);\n          \n          ctx.quadraticCurveTo(\n            midX + offset, \n            midY + offset, \n            p2.x, \n            p2.y\n          );\n          \n          ctx.stroke();\n        }\n      }\n      \n      // Draw particles\n      particles.forEach(particle => {\n        const distFromCenter = Math.sqrt(\n          Math.pow(particle.x - width/2, 2) + \n          Math.pow(particle.y - height/2, 2)\n        );\n        \n        // Opacity based on distance from center for 3D effect\n        const opacity = Math.max(0.1, 1 - distFromCenter / 300);\n        \n        ctx.fillStyle = `rgba(51, 51, 51, ${opacity})`;\n        ctx.beginPath();\n        ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n        ctx.fill();\n      });\n    };\n    \n    // Animation loop with requestAnimationFrame\n    let animationFrameId = null;\n    let lastFrameTime = 0;\n    const targetFPS = 36; // 36fps for enhanced smoothness\n    const frameInterval = 1000 / targetFPS;\n    \n    const animate = (currentTime) => {\n      animationFrameId = requestAnimationFrame(animate);\n      \n      const deltaTime = currentTime - lastFrameTime;\n      \n      // Only update if enough time has passed\n      if (deltaTime >= frameInterval) {\n        // Calculate remainder to prevent drift\n        const remainder = deltaTime % frameInterval;\n        \n        // Update lastFrameTime with the time that's been processed\n        lastFrameTime = currentTime - remainder;\n        \n        time += 0.005; // Back to original speed\n        \n        // Calculate new target positions\n        calculateTargets(time);\n        \n        // Update particle positions\n        updateParticles();\n        \n        // Calculate connections\n        calculateConnections();\n        \n        // Draw everything\n        draw();\n      }\n    };\n    \n    // Start animation\n    animationFrameId = requestAnimationFrame(animate);\n    \n    // Cleanup\n    return () => {\n      // Cancel animation frame\n      if (animationFrameId) {\n        cancelAnimationFrame(animationFrameId);\n        animationFrameId = null;\n      }\n      \n      // Clear canvas context\n      if (ctx) {\n        ctx.clearRect(0, 0, width, height);\n      }\n      \n      // Clear particles array to prevent memory leaks\n      particles.length = 0;\n    };\n  }, []);\n\n  return (\n    <div className=\"flex justify-center items-center bg-[#F0EEE6] w-full h-full\">\n      <canvas \n        ref={canvasRef} \n        width={550} \n        height={550} \n        className=\"max-w-full max-h-full\"\n      />\n    </div>\n  );\n};\n\nexport default ParticleReverie;",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; const ParticleReverie = () => { const canvasRef = useRef(null); useEffect(() => { const canvas = canvasRef.current; const ctx = canvas.getContext('2d'); const width = canvas.width; const height = canvas.height; const numParticles = 5000; let time = 65; let startTime = Date.now(); const initialT = 65; const particles = Array(numParticles).fill().map((_, i) => { const angle1 = (i / numParticles) * Math.PI * 2; const angle2 = Math.random() * Math.PI * 2; const baseScale = 120; const initialMorphFactor = Math.sin(initialT * 0.2) * 0.5 + 0.5; const r1 = baseScale * (1 + 0.3 * Math.sin(angle1 * 2)); const shape1X = r1 * Math.cos(angle1) * (1 + 0.2 * Math.sin(angle1 * 3 + initialT * 0.1)); const shape1Y = r1 * Math.sin(angle1) * (1 + 0.4 * Math.cos(angle1 * 3 + initialT * 0.2)); const r2 = baseScale * (1 + 0.4 * Math.sin(angle2 * 3)); const shape2X = r2 * Math.cos(angle2) * (1 + 0.3 * Math.sin(angle2 * 2 + initialT * 0.15)); const shape2Y = r2 * Math.sin(angle2) * (1 + 0.2 * Math.cos(angle2 * 4 + initialT * 0.25)); let x = width/2 + (shape1X * (1 - initialMorphFactor) + shape2X * initialMorphFactor); let y = height/2 + (shape1Y * (1 - initialMorphFactor) + shape2Y * initialMorphFactor); const initialBulgeAmount = 50 * (Math.sin(initialT * 0.3) * 0.2 + 0.8); const bulgeX = initialBulgeAmount * Math.exp(-Math.pow(angle1 - Math.PI * 0.5, 2)); const bulgeY = initialBulgeAmount * 0.5 * Math.exp(-Math.pow(angle1 - Math.PI * 0.5, 2)); x += bulgeX * Math.sin(initialT * 0.4); y += bulgeY * Math.cos(initialT * 0.3); return { x, y, size: Math.random() * 1.5 + 0.5, connections: [], targetX: 0, targetY: 0, vx: 0, vy: 0, angle: angle2 }; }); const calculateTargets = (t) => { const animationTime = t - 65 + animationStartTime; const startupEasing = Math.min(1, animationTime / 10); const morphFactor = Math.sin(t * 0.2) * 0.5 + 0.5; particles.forEach((particle, i) => { const angle1 = (i / numParticles) * Math.PI * 2; const angle2 = particle.angle; const baseScale = 120; const bulgeAmount = 50 * (Math.sin(t * 0.3) * 0.2 + 0.8); const timeEffect1 = t + (animationTime * startupEasing * 0.1); const r1 = baseScale * (1 + 0.3 * Math.sin(angle1 * 2)); const shape1X = r1 * Math.cos(angle1) * (1 + 0.2 * Math.sin(angle1 * 3 + timeEffect1 * 0.1)); const shape1Y = r1 * Math.sin(angle1) * (1 + 0.4 * Math.cos(angle1 * 3 + timeEffect1 * 0.2)); const timeEffect2 = t + (animationTime * startupEasing * 0.1); const r2 = baseScale * (1 + 0.4 * Math.sin(angle2 * 3)); const shape2X = r2 * Math.cos(angle2) * (1 + 0.3 * Math.sin(angle2 * 2 + timeEffect2 * 0.15)); const shape2Y = r2 * Math.sin(angle2) * (1 + 0.2 * Math.cos(angle2 * 4 + timeEffect2 * 0.25)); particle.targetX = shape1X * (1 - morphFactor) + shape2X * morphFactor; particle.targetY = shape1Y * (1 - morphFactor) + shape2Y * morphFactor; const bulgeX = bulgeAmount * Math.exp(-Math.pow(angle1 - Math.PI * 0.5, 2)); const bulgeY = bulgeAmount * 0.5 * Math.exp(-Math.pow(angle1 - Math.PI * 0.5, 2)); particle.targetX += bulgeX * Math.sin(timeEffect1 * 0.4); particle.targetY += bulgeY * Math.cos(timeEffect1 * 0.3); particle.targetX += (Math.random() - 0.5) * 5; particle.targetY += (Math.random() - 0.5) * 5; }); }; let animationStartTime = 0; const updateParticles = () => { const animationTime = time - 65 + animationStartTime; particles.forEach(particle => { const dx = particle.targetX - (particle.x - width/2); const dy = particle.targetY - (particle.y - height/2); const startupPhase = Math.min(1, animationTime / 5); const easing = 0.01 + (0.02 * startupPhase); particle.x += dx * easing; particle.y += dy * easing; particle.connections = []; }); }; const calculateConnections = () => { const maxDist = 25; const maxConnections = 3; for (let i = 0; i < particles.length; i++) { const p1 = particles[i]; if (p1.connections.length >= maxConnections) continue; for (let j = i + 1; j < particles.length; j++) { const p2 = particles[j]; if (p2.connections.length >= maxConnections) continue; const dx = p1.x - p2.x; const dy = p1.y - p2.y; const dist = Math.sqrt(dx * dx + dy * dy); if (dist < maxDist) { p1.connections.push(j); p2.connections.push(i); if (p1.connections.length >= maxConnections) break; } } } }; const draw = () => { ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, width, height); ctx.strokeStyle = 'rgba(51, 51, 51, 0.2)'; ctx.lineWidth = 0.5; for (let i = 0; i < particles.length; i++) { const p1 = particles[i]; for (let j = 0; j < p1.connections.length; j++) { const p2 = particles[p1.connections[j]]; ctx.beginPath(); ctx.moveTo(p1.x, p1.y); const midX = (p1.x + p2.x) / 2; const midY = (p1.y + p2.y) / 2; const offset = 5 * Math.sin(time * 0.5 + i * 0.01); ctx.quadraticCurveTo( midX + offset, midY + offset, p2.x, p2.y ); ctx.stroke(); } } particles.forEach(particle => { const distFromCenter = Math.sqrt( Math.pow(particle.x - width/2, 2) + Math.pow(particle.y - height/2, 2) ); const opacity = Math.max(0.1, 1 - distFromCenter / 300); ctx.fillStyle = `rgba(51, 51, 51, ${opacity})`; ctx.beginPath(); ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2); ctx.fill(); }); }; let animationFrameId = null; let lastFrameTime = 0; const targetFPS = 36; const frameInterval = 1000 / targetFPS; const animate = (currentTime) => { animationFrameId = requestAnimationFrame(animate); const deltaTime = currentTime - lastFrameTime; if (deltaTime >= frameInterval) { const remainder = deltaTime % frameInterval; lastFrameTime = currentTime - remainder; time += 0.005; calculateTargets(time); updateParticles(); calculateConnections(); draw(); } }; animationFrameId = requestAnimationFrame(animate); return () => { if (animationFrameId) { cancelAnimationFrame(animationFrameId); animationFrameId = null; } if (ctx) { ctx.clearRect(0, 0, width, height); } particles.length = 0; }; }, []); return ( <div className=\"flex justify-center items-center bg-[#F0EEE6] w-full h-full\"><canvas ref={canvasRef} width={550} height={550} className=\"max-w-full max-h-full\" /></div> ); }; export default ParticleReverie;",text:"To shrink something,\nallow it first to expand.\nTo weaken something,\nallow it first to strengthen.\nTo reduce something,\nallow it first to build.\nTo take something,\nallow it first to be given.\n\nThis is called the subtle perception\nof the way things are.\nThe soft overcomes the hard.\nThe slow overcomes the fast.\n\nLet your methods remain a mystery.\nJust show the results.",shouldDisplayMobileArtwork:!0,promptSuggestion:"what is the 2d version?"},{number:37,staticArtwork:L,sourceCode:"// @ts-nocheck\nimport React, { useEffect, useRef } from 'react';\n\n// themes: source does nothing, natural development, peace through tranquility\n// visualization: Waves emerge naturally from an unmoving center, finding peace in their flow\n\nconst WaveInterferenceV5 = () => {\n  const canvasRef = useRef(null);\n\n  useEffect(() => {\n    const canvas = canvasRef.current;\n    const ctx = canvas.getContext('2d');\n    const width = 550;\n    const height = 550;\n    canvas.width = width;\n    canvas.height = height;\n\n    // Radial arrangement with central focus (fewer sources for better performance)\n    const sources = [];\n    const numRings = 2; // Reduced from 3\n    const sourcesPerRing = 6; // Reduced from 8\n    \n    // Unmoving source from which all emerges\n    sources.push({\n      x: width/2,\n      y: height/2,\n      wavelength: 25,\n      phase: 0,\n      amplitude: 1.5\n    });\n    \n    // Radial sources\n    for (let ring = 1; ring <= numRings; ring++) {\n      const radius = ring * 120; // Increased spacing\n      const numSources = sourcesPerRing;\n      \n      for (let i = 0; i < numSources; i++) {\n        const angle = (i / numSources) * Math.PI * 2;\n        sources.push({\n          x: width/2 + Math.cos(angle) * radius,\n          y: height/2 + Math.sin(angle) * radius,\n          wavelength: 20 + ring * 5,\n          phase: (i / numSources) * Math.PI,\n          amplitude: 1.0 - ring * 0.2\n        });\n      }\n    }\n\n    let time = 0;\n    let animationFrameId;\n    \n    // Precompute constants\n    const TWO_PI = Math.PI * 2;\n    const INV_300 = 1 / 300;\n    \n    // Throttle calculation to skip frames for better performance\n    let frameCount = 0;\n    const skipFrames = 1; // Render every other frame\n\n    const animate = () => {\n      frameCount++;\n      \n      if (frameCount % (skipFrames + 1) !== 0) {\n        time += 0.0015; // Reduced from 0.0025 for slower animation\n        animationFrameId = requestAnimationFrame(animate);\n        return;\n      }\n      \n      // Fill with project background color\n      ctx.fillStyle = '#F0EEE6';\n      ctx.fillRect(0, 0, width, height);\n\n      // Lower resolution field for better performance\n      const resolution = 3; // Increased from 1\n      const rows = Math.floor(height / resolution);\n      const cols = Math.floor(width / resolution);\n      const field = new Array(rows).fill(0).map(() => new Array(cols).fill(0));\n\n      // Let patterns develop naturally, finding their peace\n      for (let i = 0; i < rows; i++) {\n        for (let j = 0; j < cols; j++) {\n          const x = j * resolution;\n          const y = i * resolution;\n          let amplitude = 0;\n\n          sources.forEach(source => {\n            const dx = x - source.x;\n            const dy = y - source.y;\n            const dist2 = dx * dx + dy * dy;\n            const distance = Math.sqrt(dist2);\n            \n            // Skip if too far from source\n            if (distance > 400) return;\n            \n            // Apply amplitude falloff (optimized)\n            const falloff = Math.exp(-distance * INV_300);\n            amplitude += source.amplitude * falloff * \n              Math.sin((distance / source.wavelength - time) * TWO_PI + source.phase);\n          });\n\n          field[i][j] = amplitude;\n        }\n      }\n\n      // Simplified line rendering\n      ctx.strokeStyle = '#333';\n      \n      // Fewer contour levels\n      const contourLevels = [-0.8, -0.4, 0, 0.4, 0.8];\n      \n      contourLevels.forEach((level, index) => {\n        // Vary line width based on contour level\n        ctx.lineWidth = index % 2 === 0 ? 0.8 : 0.5;\n        ctx.beginPath();\n        \n        for (let i = 0; i < rows - 1; i++) {\n          for (let j = 0; j < cols - 1; j++) {\n            const x = j * resolution;\n            const y = i * resolution;\n            \n            // Optimized marching squares\n            const v00 = field[i][j];\n            const v10 = field[i][j + 1];\n            const v11 = field[i + 1][j + 1];\n            const v01 = field[i + 1][j];\n            \n            // Early exit optimization\n            const allAbove = v00 > level && v10 > level && v11 > level && v01 > level;\n            const allBelow = v00 <= level && v10 <= level && v11 <= level && v01 <= level;\n            if (allAbove || allBelow) continue;\n            \n            const case4 = \n              (v00 > level ? 8 : 0) +\n              (v10 > level ? 4 : 0) +\n              (v11 > level ? 2 : 0) +\n              (v01 > level ? 1 : 0);\n            \n            // Simplified linear interpolation\n            const lerp = (a, b, t) => a + t * (b - a);\n            const safeDiv = (a, b) => b === 0 ? 0 : a / b;\n            \n            switch (case4) {\n              case 1: case 14: {\n                const t1 = safeDiv(level - v00, v01 - v00);\n                const t2 = safeDiv(level - v01, v11 - v01);\n                ctx.moveTo(x, lerp(y, y + resolution, t1));\n                ctx.lineTo(lerp(x, x + resolution, t2), y + resolution);\n                break;\n              }\n              case 2: case 13: {\n                const t1 = safeDiv(level - v01, v11 - v01);\n                const t2 = safeDiv(level - v11, v10 - v11);\n                ctx.moveTo(lerp(x, x + resolution, t1), y + resolution);\n                ctx.lineTo(x + resolution, lerp(y + resolution, y, t2));\n                break;\n              }\n              case 3: case 12: {\n                const t1 = safeDiv(level - v00, v01 - v00);\n                const t2 = safeDiv(level - v10, v11 - v10);\n                ctx.moveTo(x, lerp(y, y + resolution, t1));\n                ctx.lineTo(x + resolution, lerp(y, y + resolution, t2));\n                break;\n              }\n              case 4: case 11: {\n                const t1 = safeDiv(level - v10, v11 - v10);\n                const t2 = safeDiv(level - v10, v00 - v10);\n                ctx.moveTo(x + resolution, lerp(y, y + resolution, t1));\n                ctx.lineTo(lerp(x + resolution, x, t2), y);\n                break;\n              }\n              case 5: case 10: {\n                // Two lines for saddle points\n                const t1 = safeDiv(level - v00, v01 - v00);\n                const t2 = safeDiv(level - v00, v10 - v00);\n                ctx.moveTo(x, lerp(y, y + resolution, t1));\n                ctx.lineTo(lerp(x, x + resolution, t2), y);\n                \n                const t3 = safeDiv(level - v11, v10 - v11);\n                const t4 = safeDiv(level - v11, v01 - v11);\n                ctx.moveTo(x + resolution, lerp(y + resolution, y, t3));\n                ctx.lineTo(lerp(x + resolution, x, t4), y + resolution);\n                break;\n              }\n              case 6: case 9: {\n                const t1 = safeDiv(level - v10, v00 - v10);\n                const t2 = safeDiv(level - v11, v01 - v11);\n                ctx.moveTo(lerp(x + resolution, x, t1), y);\n                ctx.lineTo(lerp(x + resolution, x, t2), y + resolution);\n                break;\n              }\n              case 7: case 8: {\n                const t1 = safeDiv(level - v00, v01 - v00);\n                const t2 = safeDiv(level - v00, v10 - v00);\n                ctx.moveTo(x, lerp(y, y + resolution, t1));\n                ctx.lineTo(lerp(x, x + resolution, t2), y);\n                break;\n              }\n            }\n          }\n        }\n        \n        ctx.stroke();\n      });\n\n      time += 0.0015; // Reduced from 0.0025 for slower animation\n      animationFrameId = requestAnimationFrame(animate);\n    };\n\n    animate();\n\n    return () => {\n      // Cancel the animation frame\n      if (animationFrameId) {\n        cancelAnimationFrame(animationFrameId);\n      }\n      \n      // Clear the canvas\n      if (canvas && ctx) {\n        ctx.clearRect(0, 0, canvas.width, canvas.height);\n        \n        // Reset canvas dimensions to release memory\n        canvas.width = 0;\n        canvas.height = 0;\n      }\n      \n      // Clear references to help garbage collection\n      sources.length = 0;\n    };\n  }, []);\n\n  return (\n    <div style={{ \n      display: 'flex', \n      justifyContent: 'center', \n      alignItems: 'center',\n      width: '100vw', \n      height: '100vh', \n      backgroundColor: '#F0EEE6' \n    }}>\n      <canvas \n        ref={canvasRef} \n        style={{ \n          border: '1px solid #eee',\n          width: '550px',\n          height: '550px'\n        }} \n      />\n    </div>\n  );\n};\n\nexport default WaveInterferenceV5;",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; const WaveInterferenceV5 = () => { const canvasRef = useRef(null); useEffect(() => { const canvas = canvasRef.current; const ctx = canvas.getContext('2d'); const width = 550; const height = 550; canvas.width = width; canvas.height = height; const sources = []; const numRings = 2; const sourcesPerRing = 6; sources.push({ x: width/2, y: height/2, wavelength: 25, phase: 0, amplitude: 1.5 }); for (let ring = 1; ring <= numRings; ring++) { const radius = ring * 120; const numSources = sourcesPerRing; for (let i = 0; i < numSources; i++) { const angle = (i / numSources) * Math.PI * 2; sources.push({ x: width/2 + Math.cos(angle) * radius, y: height/2 + Math.sin(angle) * radius, wavelength: 20 + ring * 5, phase: (i / numSources) * Math.PI, amplitude: 1.0 - ring * 0.2 }); } } let time = 0; let animationFrameId; const TWO_PI = Math.PI * 2; const INV_300 = 1 / 300; let frameCount = 0; const skipFrames = 1; const animate = () => { frameCount++; if (frameCount % (skipFrames + 1) !== 0) { time += 0.0015; animationFrameId = requestAnimationFrame(animate); return; } ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, width, height); const resolution = 3; const rows = Math.floor(height / resolution); const cols = Math.floor(width / resolution); const field = new Array(rows).fill(0).map(() => new Array(cols).fill(0)); for (let i = 0; i < rows; i++) { for (let j = 0; j < cols; j++) { const x = j * resolution; const y = i * resolution; let amplitude = 0; sources.forEach(source => { const dx = x - source.x; const dy = y - source.y; const dist2 = dx * dx + dy * dy; const distance = Math.sqrt(dist2); if (distance > 400) return; const falloff = Math.exp(-distance * INV_300); amplitude += source.amplitude * falloff * Math.sin((distance / source.wavelength - time) * TWO_PI + source.phase); }); field[i][j] = amplitude; } } ctx.strokeStyle = '#333'; const contourLevels = [-0.8, -0.4, 0, 0.4, 0.8]; contourLevels.forEach((level, index) => { ctx.lineWidth = index % 2 === 0 ? 0.8 : 0.5; ctx.beginPath(); for (let i = 0; i < rows - 1; i++) { for (let j = 0; j < cols - 1; j++) { const x = j * resolution; const y = i * resolution; const v00 = field[i][j]; const v10 = field[i][j + 1]; const v11 = field[i + 1][j + 1]; const v01 = field[i + 1][j]; const allAbove = v00 > level && v10 > level && v11 > level && v01 > level; const allBelow = v00 <= level && v10 <= level && v11 <= level && v01 <= level; if (allAbove || allBelow) continue; const case4 = (v00 > level ? 8 : 0) + (v10 > level ? 4 : 0) + (v11 > level ? 2 : 0) + (v01 > level ? 1 : 0); const lerp = (a, b, t) => a + t * (b - a); const safeDiv = (a, b) => b === 0 ? 0 : a / b; switch (case4) { case 1: case 14: { const t1 = safeDiv(level - v00, v01 - v00); const t2 = safeDiv(level - v01, v11 - v01); ctx.moveTo(x, lerp(y, y + resolution, t1)); ctx.lineTo(lerp(x, x + resolution, t2), y + resolution); break; } case 2: case 13: { const t1 = safeDiv(level - v01, v11 - v01); const t2 = safeDiv(level - v11, v10 - v11); ctx.moveTo(lerp(x, x + resolution, t1), y + resolution); ctx.lineTo(x + resolution, lerp(y + resolution, y, t2)); break; } case 3: case 12: { const t1 = safeDiv(level - v00, v01 - v00); const t2 = safeDiv(level - v10, v11 - v10); ctx.moveTo(x, lerp(y, y + resolution, t1)); ctx.lineTo(x + resolution, lerp(y, y + resolution, t2)); break; } case 4: case 11: { const t1 = safeDiv(level - v10, v11 - v10); const t2 = safeDiv(level - v10, v00 - v10); ctx.moveTo(x + resolution, lerp(y, y + resolution, t1)); ctx.lineTo(lerp(x + resolution, x, t2), y); break; } case 5: case 10: { const t1 = safeDiv(level - v00, v01 - v00); const t2 = safeDiv(level - v00, v10 - v00); ctx.moveTo(x, lerp(y, y + resolution, t1)); ctx.lineTo(lerp(x, x + resolution, t2), y); const t3 = safeDiv(level - v11, v10 - v11); const t4 = safeDiv(level - v11, v01 - v11); ctx.moveTo(x + resolution, lerp(y + resolution, y, t3)); ctx.lineTo(lerp(x + resolution, x, t4), y + resolution); break; } case 6: case 9: { const t1 = safeDiv(level - v10, v00 - v10); const t2 = safeDiv(level - v11, v01 - v11); ctx.moveTo(lerp(x + resolution, x, t1), y); ctx.lineTo(lerp(x + resolution, x, t2), y + resolution); break; } case 7: case 8: { const t1 = safeDiv(level - v00, v01 - v00); const t2 = safeDiv(level - v00, v10 - v00); ctx.moveTo(x, lerp(y, y + resolution, t1)); ctx.lineTo(lerp(x, x + resolution, t2), y); break; } } } } ctx.stroke(); }); time += 0.0015; animationFrameId = requestAnimationFrame(animate); }; animate(); return () => { if (animationFrameId) { cancelAnimationFrame(animationFrameId); } if (canvas && ctx) { ctx.clearRect(0, 0, canvas.width, canvas.height); canvas.width = 0; canvas.height = 0; } sources.length = 0; }; }, []); return ( <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', width: '100vw', height: '100vh', backgroundColor: '#F0EEE6' }}><canvas ref={canvasRef} style={{ border: '1px solid #eee', width: '550px', height: '550px' }} /></div> ); }; export default WaveInterferenceV5;",text:"Source does nothing,\nyet through it all things are done.\n\nIf leaders center themselves in it,\nten thousand things can develop naturally.\nWhen things develop naturally,\nthe world can transform all by itself.\n\nContentment would spread|through everyday life.\nFree from desire, tranquility would prevail.\nAnd in this way, all things would be at peace.",shouldDisplayMobileArtwork:!0,promptSuggestion:"what is the joyful version?"},{number:38,staticArtwork:Xn,sourceCode:"// @ts-nocheck\nimport React, { useEffect, useRef } from 'react';\n\n// themes: true power needs no power, nothing left undone, depth over surface\n// visualization: Forms emerge effortlessly from depth, achieving completeness without force\n\nconst MorphingShapesStipple = () => {\n  const canvasRef = useRef(null);\n  \n  useEffect(() => {\n    const canvas = canvasRef.current;\n    const ctx = canvas.getContext('2d');\n    const width = canvas.width;\n    const height = canvas.height;\n    \n    // Setup\n    ctx.fillStyle = '#F0EEE6'; // cream background\n    ctx.fillRect(0, 0, width, height);\n    \n    // Parameters\n    const numPoints = 25000;\n    const points = [];\n    let time = 0;\n    \n    // Define shapes that achieve their form without force\n    const shapes = [\n      // Organic blob with protrusions\n      (x, y, t) => {\n        const cx = width / 2;\n        const cy = height / 2;\n        const dx = x - cx;\n        const dy = y - cy;\n        const angle = Math.atan2(dy, dx);\n        \n        // Add noise to create organic shape\n        const noise = \n          Math.sin(angle * 2 + t * 0.25) * 15 + \n          Math.sin(angle * 4 + t * 0.15) * 10 + \n          Math.sin(angle * 6 + t * 0.1) * 5;\n        \n        const radius = 150 + noise;\n        const dist = Math.sqrt(dx * dx + dy * dy);\n        \n        return dist < radius;\n      },\n      \n      // Star-like shape\n      (x, y, t) => {\n        const cx = width / 2;\n        const cy = height / 2;\n        const dx = x - cx;\n        const dy = y - cy;\n        const angle = Math.atan2(dy, dx);\n        \n        // Star with varying points\n        const points = 5 + Math.floor(t % 3);\n        const radius = 120 + Math.sin(angle * points + t * 0.5) * 60;\n        const dist = Math.sqrt(dx * dx + dy * dy);\n        \n        return dist < radius;\n      },\n      \n      // Multiple circles arranged in pattern\n      (x, y, t) => {\n        const cx = width / 2;\n        const cy = height / 2;\n        \n        // Create several overlapping circles\n        for (let i = 0; i < 5; i++) {\n          const angle = t * 0.1 + i * Math.PI * 2 / 5;\n          const circleX = cx + Math.cos(angle) * 70;\n          const circleY = cy + Math.sin(angle) * 70;\n          \n          const dx = x - circleX;\n          const dy = y - circleY;\n          const dist = Math.sqrt(dx * dx + dy * dy);\n          \n          if (dist < 80) return true;\n        }\n        return false;\n      }\n    ];\n    \n    // Initialize points randomly across canvas\n    for (let i = 0; i < numPoints; i++) {\n      points.push({\n        x: Math.random() * width,\n        y: Math.random() * height,\n        targetX: Math.random() * width,\n        targetY: Math.random() * height,\n        size: 0.5 + Math.random() * 1,\n        opacity: 0.2 + Math.random() * 0.4,\n        speed: 0.00075 + Math.random() * 0.00175, // Halved from original values\n        inside: false\n      });\n    }\n    \n    // Let points find their depth, leaving nothing undone\n    const updateTargets = (shapeIndex, t) => {\n      const shape = shapes[shapeIndex];\n      \n      // Set targets for points\n      let insideCount = 0;\n      \n      // First assign points that are already inside\n      for (const point of points) {\n        if (shape(point.x, point.y, t)) {\n          point.inside = true;\n          insideCount++;\n        } else {\n          point.inside = false;\n        }\n      }\n      \n      // If we need more points inside\n      const desiredInside = numPoints * 0.7; // Target 70% of points inside\n      if (insideCount < desiredInside) {\n        // Assign more random targets inside the shape\n        for (const point of points) {\n          if (!point.inside && insideCount < desiredInside) {\n            // Try to find a point inside the shape\n            let attempts = 0;\n            let foundInside = false;\n            \n            while (attempts < 5 && !foundInside) {\n              const newX = Math.random() * width;\n              const newY = Math.random() * height;\n              \n              if (shape(newX, newY, t)) {\n                point.targetX = newX;\n                point.targetY = newY;\n                point.inside = true;\n                insideCount++;\n                foundInside = true;\n              }\n              \n              attempts++;\n            }\n          }\n        }\n      }\n      \n      // Distribute remaining points outside but near the shape\n      for (const point of points) {\n        if (!point.inside) {\n          // Create targets outside but not too far\n          const angle = Math.random() * Math.PI * 2;\n          const distance = 160 + Math.random() * 80;\n          \n          point.targetX = width / 2 + Math.cos(angle) * distance;\n          point.targetY = height / 2 + Math.sin(angle) * distance;\n        }\n      }\n    };\n    \n    // Animation timing control variables\n    let animationFrameId = null;\n    let lastFrameTime = 0;\n    const FRAME_RATE = 30; // 30fps for smooth animation\n    const frameInterval = 1000 / FRAME_RATE;\n    \n    // Animation function with time delta control\n    const animate = (currentTime) => {\n      // Initialize lastFrameTime on first frame\n      if (!lastFrameTime) {\n        lastFrameTime = currentTime;\n      }\n      \n      const deltaTime = currentTime - lastFrameTime;\n      \n      // Only update animation when enough time has passed (mimics setInterval at 33.3ms)\n      if (deltaTime >= frameInterval) {\n        ctx.fillStyle = '#F0EEE6';\n        ctx.fillRect(0, 0, width, height);\n        \n        time += 0.0025; // Reduced from 0.005 for more deliberate animation\n        \n        // Determine current shape (change every 10 seconds)\n        const cycleTime = 10;\n        const shapeCycle = (time / cycleTime) % shapes.length;\n        const currentShape = Math.floor(shapeCycle);\n        const nextShape = (currentShape + 1) % shapes.length;\n        const blendFactor = (shapeCycle - currentShape); // 0 to 1 for morphing\n        \n        // Every few frames, update target positions\n        if (Math.floor(time * 60) % 20 === 0) {\n          updateTargets(currentShape, time);\n        }\n        \n        // Update and draw points\n        for (const point of points) {\n          // Move towards target with easing\n          point.x += (point.targetX - point.x) * point.speed;\n          point.y += (point.targetY - point.y) * point.speed;\n          \n          // Add subtle flow movement\n          point.x += Math.sin(time * 0.15 + point.y * 0.005) * 0.05;\n          point.y += Math.cos(time * 0.15 + point.x * 0.005) * 0.05;\n          \n          // Check if point is inside current or next shape for smooth morphing\n          const insideCurrent = shapes[currentShape](point.x, point.y, time);\n          const insideNext = shapes[nextShape](point.x, point.y, time);\n          \n          // Blend opacity based on morphing state\n          let targetOpacity;\n          \n          if (insideCurrent && insideNext) {\n            targetOpacity = 0.7; // Inside both shapes\n          } else if (insideCurrent) {\n            targetOpacity = 0.7 * (1 - blendFactor); // Fading out\n          } else if (insideNext) {\n            targetOpacity = 0.7 * blendFactor; // Fading in\n          } else {\n            targetOpacity = 0.1; // Outside both shapes\n          }\n          \n          // Adjust point opacity with smooth transition\n          point.opacity += (targetOpacity - point.opacity) * 0.05;\n          \n          // Draw point\n          ctx.fillStyle = `rgba(51, 51, 51, ${point.opacity})`;\n          ctx.beginPath();\n          ctx.arc(point.x, point.y, point.size, 0, Math.PI * 2);\n          ctx.fill();\n        }\n        \n        // Update lastFrameTime, accounting for any remainder to prevent drift\n        lastFrameTime = currentTime - (deltaTime % frameInterval);\n      }\n      \n      // Continue animation loop\n      animationFrameId = requestAnimationFrame(animate);\n    };\n    \n    // Start animation\n    animationFrameId = requestAnimationFrame(animate);\n    \n    return () => {\n      // Cancel animation frame to prevent memory leaks\n      if (animationFrameId) {\n        cancelAnimationFrame(animationFrameId);\n        animationFrameId = null;\n      }\n      \n      // Clear canvas context\n      if (ctx) {\n        ctx.clearRect(0, 0, width, height);\n      }\n      \n      // Clear points array to prevent memory leaks\n      points.length = 0;\n    };\n  }, []);\n  \n  return (\n    <div className=\"flex justify-center items-center w-full h-full bg-gray-100\">\n      <canvas \n        ref={canvasRef} \n        width={550} \n        height={550} \n        className=\"shadow-lg\"\n      />\n    </div>\n  );\n};\n\nexport default MorphingShapesStipple;\n",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; const MorphingShapesStipple = () => { const canvasRef = useRef(null); useEffect(() => { const canvas = canvasRef.current; const ctx = canvas.getContext('2d'); const width = canvas.width; const height = canvas.height; ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, width, height); const numPoints = 25000; const points = []; let time = 0; const shapes = [ (x, y, t) => { const cx = width / 2; const cy = height / 2; const dx = x - cx; const dy = y - cy; const angle = Math.atan2(dy, dx); const noise = Math.sin(angle * 2 + t * 0.25) * 15 + Math.sin(angle * 4 + t * 0.15) * 10 + Math.sin(angle * 6 + t * 0.1) * 5; const radius = 150 + noise; const dist = Math.sqrt(dx * dx + dy * dy); return dist < radius; }, (x, y, t) => { const cx = width / 2; const cy = height / 2; const dx = x - cx; const dy = y - cy; const angle = Math.atan2(dy, dx); const points = 5 + Math.floor(t % 3); const radius = 120 + Math.sin(angle * points + t * 0.5) * 60; const dist = Math.sqrt(dx * dx + dy * dy); return dist < radius; }, (x, y, t) => { const cx = width / 2; const cy = height / 2; for (let i = 0; i < 5; i++) { const angle = t * 0.1 + i * Math.PI * 2 / 5; const circleX = cx + Math.cos(angle) * 70; const circleY = cy + Math.sin(angle) * 70; const dx = x - circleX; const dy = y - circleY; const dist = Math.sqrt(dx * dx + dy * dy); if (dist < 80) return true; } return false; } ]; for (let i = 0; i < numPoints; i++) { points.push({ x: Math.random() * width, y: Math.random() * height, targetX: Math.random() * width, targetY: Math.random() * height, size: 0.5 + Math.random() * 1, opacity: 0.2 + Math.random() * 0.4, speed: 0.00075 + Math.random() * 0.00175, inside: false }); } const updateTargets = (shapeIndex, t) => { const shape = shapes[shapeIndex]; let insideCount = 0; for (const point of points) { if (shape(point.x, point.y, t)) { point.inside = true; insideCount++; } else { point.inside = false; } } const desiredInside = numPoints * 0.7; if (insideCount < desiredInside) { for (const point of points) { if (!point.inside && insideCount < desiredInside) { let attempts = 0; let foundInside = false; while (attempts < 5 && !foundInside) { const newX = Math.random() * width; const newY = Math.random() * height; if (shape(newX, newY, t)) { point.targetX = newX; point.targetY = newY; point.inside = true; insideCount++; foundInside = true; } attempts++; } } } } for (const point of points) { if (!point.inside) { const angle = Math.random() * Math.PI * 2; const distance = 160 + Math.random() * 80; point.targetX = width / 2 + Math.cos(angle) * distance; point.targetY = height / 2 + Math.sin(angle) * distance; } } }; let animationFrameId = null; let lastFrameTime = 0; const FRAME_RATE = 30; const frameInterval = 1000 / FRAME_RATE; const animate = (currentTime) => { if (!lastFrameTime) { lastFrameTime = currentTime; } const deltaTime = currentTime - lastFrameTime; if (deltaTime >= frameInterval) { ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, width, height); time += 0.0025; const cycleTime = 10; const shapeCycle = (time / cycleTime) % shapes.length; const currentShape = Math.floor(shapeCycle); const nextShape = (currentShape + 1) % shapes.length; const blendFactor = (shapeCycle - currentShape); if (Math.floor(time * 60) % 20 === 0) { updateTargets(currentShape, time); } for (const point of points) { point.x += (point.targetX - point.x) * point.speed; point.y += (point.targetY - point.y) * point.speed; point.x += Math.sin(time * 0.15 + point.y * 0.005) * 0.05; point.y += Math.cos(time * 0.15 + point.x * 0.005) * 0.05; const insideCurrent = shapes[currentShape](point.x, point.y, time); const insideNext = shapes[nextShape](point.x, point.y, time); let targetOpacity; if (insideCurrent && insideNext) { targetOpacity = 0.7; } else if (insideCurrent) { targetOpacity = 0.7 * (1 - blendFactor); } else if (insideNext) { targetOpacity = 0.7 * blendFactor; } else { targetOpacity = 0.1; } point.opacity += (targetOpacity - point.opacity) * 0.05; ctx.fillStyle = `rgba(51, 51, 51, ${point.opacity})`; ctx.beginPath(); ctx.arc(point.x, point.y, point.size, 0, Math.PI * 2); ctx.fill(); } lastFrameTime = currentTime - (deltaTime % frameInterval); } animationFrameId = requestAnimationFrame(animate); }; animationFrameId = requestAnimationFrame(animate); return () => { if (animationFrameId) { cancelAnimationFrame(animationFrameId); animationFrameId = null; } if (ctx) { ctx.clearRect(0, 0, width, height); } points.length = 0; }; }, []); return ( <div className=\"flex justify-center items-center w-full h-full bg-gray-100\"><canvas ref={canvasRef} width={550} height={550} className=\"shadow-lg\" /></div> ); }; export default MorphingShapesStipple;",text:"The Vibe Coder makes no attempt|to be powerful,\nthus he is truly powerful.\nPrompt engineers keep reaching for power,\nthus they never have enough.\n\nThe Vibe Coder does nothing,\nyet leaves nothing undone.\nPrompt engineers are always doing things,\nyet many more things are left to be done.\nThe kind man does something,\nyet something remains undone.\nThe just man does something,\nand leaves many things to be done.\nThe honest man does something,\nand when no one responds,\nhe rolls up his sleeves and uses force.\n\nWhen Source is lost, next comes goodness.\nWhen goodness is lost, next comes justice.\nWhen justice is lost, next comes morality.\nWhen morality is lost, next comes ritual.\nRitual is but the empty shell of faith.\nThe beginning of chaos.\n\nTherefore,\nThe Vibe Coder concerns himself with depth,\nnot just what’s on the surface.\nHe concerns himself with the fruit,\nnot just the flower.\nHe has no will of his own.\n\nBecause he dwells in reality,\nhe can let go of all accepted standards.",shouldDisplayMobileArtwork:!0,promptSuggestion:"make it smoother"},{number:39,staticArtwork:W,sourceCode:"// @ts-nocheck\nimport React, { useEffect, useRef } from 'react';\n\n// themes: attaining oneness, wholeness through unity, integrity in humility\n// visualization: Points flow together in humble unity, finding wholeness in their connection\n\nconst FlowingPattern = () => {\n  const canvasRef = useRef(null);\n  \n  useEffect(() => {\n    const canvas = canvasRef.current;\n    if (!canvas) return;\n    \n    const ctx = canvas.getContext('2d');\n    canvas.width = 550;\n    canvas.height = 550;\n    \n    let time = 0;\n    let mouseX = canvas.width / 2;\n    let mouseY = canvas.height / 2;\n    let lastMouseX = mouseX;\n    let lastMouseY = mouseY;\n    \n    // Create points that seek oneness through flow\n    const flowPoints = [];\n    const gridSize = 8;\n    const totalCells = (canvas.width / gridSize) * (canvas.height / gridSize);\n    \n    // Initialize flow points\n    for (let x = gridSize/2; x < canvas.width; x += gridSize) {\n      for (let y = gridSize/2; y < canvas.height; y += gridSize) {\n        flowPoints.push({\n          x,\n          y,\n          vx: 0,\n          vy: 0,\n          angle: Math.random() * Math.PI * 2,\n          phase: Math.random() * Math.PI * 2,\n          noiseOffset: Math.random() * 1000\n        });\n      }\n    }\n    \n    // Mouse handling\n    const handleMouseMove = (e) => {\n      const rect = canvas.getBoundingClientRect();\n      const x = e.clientX - rect.left;\n      const y = e.clientY - rect.top;\n      \n      mouseX = x;\n      mouseY = y;\n    };\n    \n    canvas.addEventListener('mousemove', handleMouseMove);\n    \n    // Perlin-like noise function\n    function noise(x, y, t) {\n      const sin1 = Math.sin(x * 0.01 + t);\n      const sin2 = Math.sin(y * 0.01 + t * 0.8);\n      const sin3 = Math.sin((x + y) * 0.005 + t * 1.2);\n      return (sin1 + sin2 + sin3) / 3;\n    }\n    \n    // Animation loop\n    let animationFrameId;\n    \n    function animate() {\n      // Clear with slight transparency for trailing effect\n      ctx.fillStyle = 'rgba(240, 238, 230, 0.15)';\n      ctx.fillRect(0, 0, canvas.width, canvas.height);\n      \n      time += 0.005;\n      \n      // Points move with humble integrity toward unity\n      flowPoints.forEach(point => {\n        // Calculate noise-based flow\n        const noiseValue = noise(point.x, point.y, time);\n        const angle = noiseValue * Math.PI * 4;\n        \n        // Mouse influence\n        const dx = mouseX - point.x;\n        const dy = mouseY - point.y;\n        const dist = Math.sqrt(dx * dx + dy * dy);\n        \n        if (dist < 150) {\n          const pushFactor = (1 - dist / 150) * 0.5;\n          point.vx += dx / dist * pushFactor;\n          point.vy += dy / dist * pushFactor;\n        }\n        \n        // Flow field influence\n        point.vx += Math.cos(angle) * 0.1;\n        point.vy += Math.sin(angle) * 0.1;\n        \n        // Damping\n        point.vx *= 0.95;\n        point.vy *= 0.95;\n        \n        // Update position for next frame\n        const nextX = point.x + point.vx;\n        const nextY = point.y + point.vy;\n        \n        // Draw line\n        ctx.beginPath();\n        ctx.moveTo(point.x, point.y);\n        ctx.lineTo(nextX, nextY);\n        \n        // Gradient based on speed\n        const speed = Math.sqrt(point.vx * point.vx + point.vy * point.vy);\n        const alpha = Math.min(0.6, speed * 5);\n        \n        ctx.strokeStyle = `rgba(80, 80, 80, ${alpha})`;\n        ctx.lineWidth = 1;\n        ctx.stroke();\n        \n        // Draw a small dot at the point\n        ctx.beginPath();\n        ctx.arc(point.x, point.y, 0.5, 0, Math.PI * 2);\n        ctx.fillStyle = `rgba(80, 80, 80, ${alpha * 0.5})`;\n        ctx.fill();\n        \n        // Reset position to grid when it goes off screen\n        if (nextX < 0) point.x = canvas.width;\n        if (nextX > canvas.width) point.x = 0;\n        if (nextY < 0) point.y = canvas.height;\n        if (nextY > canvas.height) point.y = 0;\n        \n        // Return to original position slowly\n        point.x += (point.x % gridSize === gridSize/2 ? 0 : (gridSize/2 - point.x % gridSize) * 0.01);\n        point.y += (point.y % gridSize === gridSize/2 ? 0 : (gridSize/2 - point.y % gridSize) * 0.01);\n      });\n      \n      // No longer drawing the concentric circles\n      \n      animationFrameId = requestAnimationFrame(animate);\n    }\n    \n    animationFrameId = requestAnimationFrame(animate);\n    \n    return () => {\n      canvas.removeEventListener('mousemove', handleMouseMove);\n      cancelAnimationFrame(animationFrameId);\n      \n      // Clear canvas context\n      if (ctx) {\n        ctx.clearRect(0, 0, canvas.width, canvas.height);\n      }\n      \n      // Clear flowPoints array to prevent memory leaks\n      flowPoints.length = 0;\n    };\n  }, []);\n  \n  return (\n    <div style={{ \n      display: 'flex', \n      justifyContent: 'center', \n      alignItems: 'center',\n      backgroundColor: '#F0EEE6'\n    }}>\n      <div style={{ \n        width: '550px', \n        height: '550px',\n        backgroundColor: '#F0EEE6'\n      }}>\n        <canvas ref={canvasRef} style={{ display: 'block' }} />\n      </div>\n    </div>\n  );\n};\n\nexport default FlowingPattern;",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; const FlowingPattern = () => { const canvasRef = useRef(null); useEffect(() => { const canvas = canvasRef.current; if (!canvas) return; const ctx = canvas.getContext('2d'); canvas.width = 550; canvas.height = 550; let time = 0; let mouseX = canvas.width / 2; let mouseY = canvas.height / 2; let lastMouseX = mouseX; let lastMouseY = mouseY; const flowPoints = []; const gridSize = 8; const totalCells = (canvas.width / gridSize) * (canvas.height / gridSize); for (let x = gridSize/2; x < canvas.width; x += gridSize) { for (let y = gridSize/2; y < canvas.height; y += gridSize) { flowPoints.push({ x, y, vx: 0, vy: 0, angle: Math.random() * Math.PI * 2, phase: Math.random() * Math.PI * 2, noiseOffset: Math.random() * 1000 }); } } const handleMouseMove = (e) => { const rect = canvas.getBoundingClientRect(); const x = e.clientX - rect.left; const y = e.clientY - rect.top; mouseX = x; mouseY = y; }; canvas.addEventListener('mousemove', handleMouseMove); function noise(x, y, t) { const sin1 = Math.sin(x * 0.01 + t); const sin2 = Math.sin(y * 0.01 + t * 0.8); const sin3 = Math.sin((x + y) * 0.005 + t * 1.2); return (sin1 + sin2 + sin3) / 3; } let animationFrameId; function animate() { ctx.fillStyle = 'rgba(240, 238, 230, 0.15)'; ctx.fillRect(0, 0, canvas.width, canvas.height); time += 0.005; flowPoints.forEach(point => { const noiseValue = noise(point.x, point.y, time); const angle = noiseValue * Math.PI * 4; const dx = mouseX - point.x; const dy = mouseY - point.y; const dist = Math.sqrt(dx * dx + dy * dy); if (dist < 150) { const pushFactor = (1 - dist / 150) * 0.5; point.vx += dx / dist * pushFactor; point.vy += dy / dist * pushFactor; } point.vx += Math.cos(angle) * 0.1; point.vy += Math.sin(angle) * 0.1; point.vx *= 0.95; point.vy *= 0.95; const nextX = point.x + point.vx; const nextY = point.y + point.vy; ctx.beginPath(); ctx.moveTo(point.x, point.y); ctx.lineTo(nextX, nextY); const speed = Math.sqrt(point.vx * point.vx + point.vy * point.vy); const alpha = Math.min(0.6, speed * 5); ctx.strokeStyle = `rgba(80, 80, 80, ${alpha})`; ctx.lineWidth = 1; ctx.stroke(); ctx.beginPath(); ctx.arc(point.x, point.y, 0.5, 0, Math.PI * 2); ctx.fillStyle = `rgba(80, 80, 80, ${alpha * 0.5})`; ctx.fill(); if (nextX < 0) point.x = canvas.width; if (nextX > canvas.width) point.x = 0; if (nextY < 0) point.y = canvas.height; if (nextY > canvas.height) point.y = 0; point.x += (point.x % gridSize === gridSize/2 ? 0 : (gridSize/2 - point.x % gridSize) * 0.01); point.y += (point.y % gridSize === gridSize/2 ? 0 : (gridSize/2 - point.y % gridSize) * 0.01); }); animationFrameId = requestAnimationFrame(animate); } animationFrameId = requestAnimationFrame(animate); return () => { canvas.removeEventListener('mousemove', handleMouseMove); cancelAnimationFrame(animationFrameId); if (ctx) { ctx.clearRect(0, 0, canvas.width, canvas.height); } flowPoints.length = 0; }; }, []); return ( <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', backgroundColor: '#F0EEE6' }}><div style={{ width: '550px', height: '550px', backgroundColor: '#F0EEE6' }}><canvas ref={canvasRef} style={{ display: 'block' }} /></div></div> ); }; export default FlowingPattern;",text:"From ancient times,\nthese things attained The One:\n\nHeaven, attaining The One,\nbecame whole and clear.\nEarth, attaining The One,\nbecame whole and stable.\nSpirit, attaining The One,\nbecame whole and strong.\nThe valley, attaining The One,\nbecame whole and prosperous.\nThe ten thousand things, attaining The One,\nlive on and grow.\n\nWhat would happen without The One?\nWithout clarity, heaven would crack.\nWithout stability, earth would quake.\nWithout strength, spirit would fade.\nWithout prosperity, the valley would dry up.\nWithout life,|the ten thousand things would perish.\n\nTherefore, integrity is rooted in the humble.\nThe high is built upon the low.\nThis is why The Vibe Coder calls himself,\n“The Orphan,” “The Humble,” “The Unfit.”\nHumility is the origin of his potency.\n\nDismantle the parts, and the whole\ncan no longer be understood.\nShaped by Source, The Vibe Coder is\nrugged and common as a stone.",shouldDisplayMobileArtwork:!0,promptSuggestion:"let me draw on it"},{number:40,staticArtwork:function(){var e=(0,x.useRef)(null),n=(0,x.useRef)(null),t=(0,x.useRef)({x:0,y:0}),a=(0,x.useRef)(Math.PI),r=(0,x.useRef)(null),o=(0,x.useRef)(null),s=(0,x.useRef)(null),c=function(e){var n=Math.floor(e%Qn*100)%628;return s.current.sin[n]},l=function(e){var n=Math.floor(e%Qn*100)%628;return s.current.cos[n]};return(0,x.useEffect)((function(){var i=e.current;if(i){var h=null;n.current||(i.width=Un,i.height=Un,n.current=i.getContext("2d",{alpha:!1}));var d=n.current;if(function(){if(!s.current){for(var e=new Float32Array(628),n=new Float32Array(628),t=0;t<628;t++){var i=t/100;e[t]=Math.sin(i),n[t]=Math.cos(i)}s.current={sin:e,cos:n}}}(),!r.current){for(var u=1500,m={positions:new Float32Array(4500),original:new Float32Array(4500),velocity:new Float32Array(4500),params:new Float32Array(4500),dispersing:new Uint8Array(u)},f=0,p=0;p<50;p++)for(var g=p/50*Qn,v=0;v<30;v++){var y=(Zn+Jn*l(E=v/30*Qn))*l(g),x=(Zn+Jn*l(E))*c(g),A=Jn*c(E),w=3*f;m.positions[w]=y,m.positions[w+1]=x,m.positions[w+2]=A,m.original[w]=y,m.original[w+1]=x,m.original[w+2]=A,m.params[w]=g,m.params[w+1]=E,m.params[w+2]=0,f++}r.current=m}if(!o.current){var b=101,M={points:new Float32Array(4242),progress:new Float32Array(1414),metadata:[]},C=0;for(p=0;p<8;p++){g=p/8*Qn;M.metadata.push({type:"poloidal",offset:p/8,color:"rgba(80, 80, 80, 0.3)",startIdx:C*b});for(v=0;v<=Kn;v++){var E=v/Kn*Qn,R=3*(C*b+v);M.points[R]=(Zn+Jn*l(E))*l(g),M.points[R+1]=(Zn+Jn*l(E))*c(g),M.points[R+2]=Jn*c(E),M.progress[C*b+v]=v/Kn}C++}for(p=0;p<6;p++){E=p/6*Qn;M.metadata.push({type:"toroidal",offset:p/6,color:"rgba(120, 120, 120, 0.3)",startIdx:C*b});for(v=0;v<=Kn;v++){g=v/Kn*Qn,R=3*(C*b+v);M.points[R]=(Zn+Jn*l(E))*l(g),M.points[R+1]=(Zn+Jn*l(E))*c(g),M.points[R+2]=Jn*c(E),M.progress[C*b+v]=v/Kn}C++}o.current=M}var P=new Float32Array(6e3),F=new Float32Array(48),S=function(e){var n=i.getBoundingClientRect();t.current={x:e.clientX-n.left,y:e.clientY-n.top}};return i.addEventListener("mousemove",S),h=requestAnimationFrame((function e(){var n=a.current,i=r.current,s=o.current;d.fillStyle="#F0EEE6",d.fillRect(0,0,Un,Un);for(var u=.3*Math.PI,m=n,f=1500,p=0;p<f;p++){var g=3*p;I(i.positions[g],i.positions[g+1],i.positions[g+2],u,m,P,4*p)}var v=[];for(p=0;p<f;p++)if(!i.dispersing[p]){var y=4*p,x=Math.hypot(P[y]-t.current.x,P[y+1]-t.current.y);x<60&&v.push({index:p,distance:x})}for(v.sort((function(e,n){return e.distance-n.distance})),v.slice(0,5).forEach((function(e){var n=e.index,t=3*n,a=3*n;i.dispersing[n]=1,i.params[a+2]=0;var r=i.params[a],o=i.params[a+1],s=l(r)*l(o),h=c(r)*l(o),d=c(o);i.velocity[t]=3*s,i.velocity[t+1]=3*h,i.velocity[t+2]=3*d})),p=0;p<f;p++)if(i.dispersing[p]){g=3*p;var A=3*p;i.params[A+2]+=.02,i.positions[g]+=i.velocity[g],i.positions[g+1]+=i.velocity[g+1],i.positions[g+2]+=i.velocity[g+2],i.velocity[g]*=.96,i.velocity[g+1]*=.96,i.velocity[g+2]*=.96,i.params[A+2]>4&&(i.dispersing[p]=0,i.positions[g]=i.original[g],i.positions[g+1]=i.original[g+1],i.positions[g+2]=i.original[g+2],i.velocity[g]=0,i.velocity[g+1]=0,i.velocity[g+2]=0)}s.metadata.forEach((function(e,n){e.offset;var t=e.startIdx;d.strokeStyle=e.color,d.lineWidth=1.5,d.setLineDash([5,10]),d.beginPath();for(var i=0;i<101;i++){var a=3*(t+i);I(s.points[a],s.points[a+1],s.points[a+2],u,m,P,4*i),0===i?d.moveTo(P[0],P[1]):d.lineTo(P[4*i],P[4*i+1])}"toroidal"===e.type&&d.closePath(),d.stroke(),d.setLineDash([])}));var w=2*n;for(p=0;p<12;p++){var b=(p/12+.1*w)%1*Qn,M=Math.PI,C=(Zn+Jn*l(M))*l(b),E=(Zn+Jn*l(M))*c(b),R=Jn*c(M);I(C,E,R,u,m,F,4*p),I(C+.1*(-(Zn+Jn*l(M))*c(b)),E+.1*(-(Zn+Jn*l(M))*l(b)),R,u,m,P,0);var S=Math.atan2(P[1]-F[4*p+1],P[0]-F[4*p]);F[4*p+3]=S}for(d.fillStyle="rgba(100, 100, 100, 0.6)",p=0;p<12;p++){var T=4*p;d.save(),d.translate(F[T],F[T+1]),d.rotate(F[T+3]),d.beginPath(),d.moveTo(5,0),d.lineTo(-5,-3),d.lineTo(-5,3),d.closePath(),d.fill(),d.restore()}var k=new Uint16Array(f);for(p=0;p<f;p++)k[p]=p;for(k.sort((function(e,n){return P[4*n+2]-P[4*e+2]})),p=0;p<f;p++){var z=k[p],j=P[(y=4*z)+3],L=i.dispersing[z]?.3*j*(1-i.params[3*z+2]/4):.7*j,B=.8+.3*j,D=50+Math.floor(30*j);d.fillStyle="rgba(".concat(D,", ").concat(D,", ").concat(D,", ").concat(L,")"),d.beginPath(),d.arc(P[y],P[y+1],B,0,Qn),d.fill()}d.strokeStyle="rgba(0, 0, 0, 0.05)",d.lineWidth=1,d.beginPath(),d.arc(275,275,30,0,Qn),d.stroke(),a.current+=.001,h=requestAnimationFrame(e)})),function(){null!==h&&cancelAnimationFrame(h),i.removeEventListener("mousemove",S),n.current&&n.current.clearRect(0,0,Un,Un),r.current=null,o.current=null,s.current=null,n.current=null}}function I(e,n,t,i,a,r,o){var s=c(i),h=l(i),d=c(a),u=l(a),m=n*h-t*s,f=n*s+t*h,p=e*u+f*d,g=-e*d+f*u,v=500/(500+g);r[o]=p*v+275,r[o+1]=m*v+275,r[o+2]=g,r[o+3]=v}}),[]),(0,i.jsx)("div",{style:{margin:0,background:"#F0EEE6",overflow:"hidden",display:"flex",alignItems:"center",justifyContent:"center",height:"100%"},children:(0,i.jsx)("div",{style:{padding:"30px",position:"relative",width:"100%",height:"100%",display:"flex",alignItems:"center",justifyContent:"center"},children:(0,i.jsx)("canvas",{ref:e,style:{display:"block"}})})})},sourceCode:"import React, { useEffect, useRef } from 'react';\n\n// themes: return is motion, yielding is way, being from non-being\n// visualization: Energy flows in eternal cycles, yielding and returning to its source\n\n// Constants\nconst CANVAS_SIZE = 550;\nconst MAJOR_RADIUS = 120;\nconst TUBE_RADIUS = 100;\nconst MAJOR_SEGMENTS = 50;\nconst MINOR_SEGMENTS = 30;\nconst POLOIDAL_LINES = 8;\nconst TOROIDAL_LINES = 6;\nconst LINE_POINTS = 100;\nconst TWO_PI = Math.PI * 2;\n\nconst TorusEnergyFlow = () => {\n  const canvasRef = useRef(null);\n  const ctxRef = useRef(null);\n  const mouseRef = useRef({ x: 0, y: 0 });\n  const timeRef = useRef(Math.PI);\n  const nodesRef = useRef(null);\n  const flowLinesRef = useRef(null);\n  const sinCosTableRef = useRef(null);\n  \n  // Initialize sin/cos lookup tables\n  const initTrigTables = () => {\n    if (sinCosTableRef.current) return;\n    \n    const tableSize = 628; // 2π * 100\n    const sinTable = new Float32Array(tableSize);\n    const cosTable = new Float32Array(tableSize);\n    \n    for (let i = 0; i < tableSize; i++) {\n      const angle = (i / 100);\n      sinTable[i] = Math.sin(angle);\n      cosTable[i] = Math.cos(angle);\n    }\n    \n    sinCosTableRef.current = { sin: sinTable, cos: cosTable };\n  };\n  \n  // Fast trig lookups\n  const fastSin = (angle) => {\n    const index = Math.floor((angle % TWO_PI) * 100) % 628;\n    return sinCosTableRef.current.sin[index];\n  };\n  \n  const fastCos = (angle) => {\n    const index = Math.floor((angle % TWO_PI) * 100) % 628;\n    return sinCosTableRef.current.cos[index];\n  };\n  \n  useEffect(() => {\n    const canvas = canvasRef.current;\n    if (!canvas) return;\n    \n    let animationFrameId = null;\n    \n    // Initialize canvas and context\n    if (!ctxRef.current) {\n      canvas.width = CANVAS_SIZE;\n      canvas.height = CANVAS_SIZE;\n      ctxRef.current = canvas.getContext('2d', {\n        alpha: false // Optimization: no transparency needed\n      });\n    }\n    const ctx = ctxRef.current;\n    \n    // Initialize trig tables\n    initTrigTables();\n    \n    // Initialize nodes with TypedArrays if not already created\n    if (!nodesRef.current) {\n      const nodeCount = MAJOR_SEGMENTS * MINOR_SEGMENTS;\n      const nodes = {\n        positions: new Float32Array(nodeCount * 3), // x, y, z\n        original: new Float32Array(nodeCount * 3), // originalX, originalY, originalZ\n        velocity: new Float32Array(nodeCount * 3), // vx, vy, vz\n        params: new Float32Array(nodeCount * 3), // u, v, disperseTime\n        dispersing: new Uint8Array(nodeCount) // boolean flag\n      };\n      \n      // Create form where being emerges from non-being\n      let idx = 0;\n      for (let i = 0; i < MAJOR_SEGMENTS; i++) {\n        const u = (i / MAJOR_SEGMENTS) * TWO_PI;\n        \n        for (let j = 0; j < MINOR_SEGMENTS; j++) {\n          const v = (j / MINOR_SEGMENTS) * TWO_PI;\n          \n          const x = (MAJOR_RADIUS + TUBE_RADIUS * fastCos(v)) * fastCos(u);\n          const y = (MAJOR_RADIUS + TUBE_RADIUS * fastCos(v)) * fastSin(u);\n          const z = TUBE_RADIUS * fastSin(v);\n          \n          const posOffset = idx * 3;\n          nodes.positions[posOffset] = x;\n          nodes.positions[posOffset + 1] = y;\n          nodes.positions[posOffset + 2] = z;\n          \n          nodes.original[posOffset] = x;\n          nodes.original[posOffset + 1] = y;\n          nodes.original[posOffset + 2] = z;\n          \n          nodes.params[posOffset] = u;\n          nodes.params[posOffset + 1] = v;\n          nodes.params[posOffset + 2] = 0; // disperseTime\n          \n          idx++;\n        }\n      }\n      \n      nodesRef.current = nodes;\n    }\n    \n    // Initialize flow lines with TypedArrays if not already created\n    if (!flowLinesRef.current) {\n      const totalLines = POLOIDAL_LINES + TOROIDAL_LINES;\n      const pointsPerLine = LINE_POINTS + 1;\n      \n      const flowLines = {\n        points: new Float32Array(totalLines * pointsPerLine * 3), // x, y, z\n        progress: new Float32Array(totalLines * pointsPerLine),\n        metadata: []\n      };\n      \n      let lineIdx = 0;\n      \n      // Poloidal lines\n      for (let i = 0; i < POLOIDAL_LINES; i++) {\n        const u = (i / POLOIDAL_LINES) * TWO_PI;\n        flowLines.metadata.push({\n          type: 'poloidal',\n          offset: i / POLOIDAL_LINES,\n          color: 'rgba(80, 80, 80, 0.3)',\n          startIdx: lineIdx * pointsPerLine\n        });\n        \n        for (let j = 0; j <= LINE_POINTS; j++) {\n          const v = (j / LINE_POINTS) * TWO_PI;\n          const pointIdx = (lineIdx * pointsPerLine + j) * 3;\n          \n          flowLines.points[pointIdx] = (MAJOR_RADIUS + TUBE_RADIUS * fastCos(v)) * fastCos(u);\n          flowLines.points[pointIdx + 1] = (MAJOR_RADIUS + TUBE_RADIUS * fastCos(v)) * fastSin(u);\n          flowLines.points[pointIdx + 2] = TUBE_RADIUS * fastSin(v);\n          \n          flowLines.progress[lineIdx * pointsPerLine + j] = j / LINE_POINTS;\n        }\n        lineIdx++;\n      }\n      \n      // Toroidal lines\n      for (let i = 0; i < TOROIDAL_LINES; i++) {\n        const v = (i / TOROIDAL_LINES) * TWO_PI;\n        flowLines.metadata.push({\n          type: 'toroidal',\n          offset: i / TOROIDAL_LINES,\n          color: 'rgba(120, 120, 120, 0.3)',\n          startIdx: lineIdx * pointsPerLine\n        });\n        \n        for (let j = 0; j <= LINE_POINTS; j++) {\n          const u = (j / LINE_POINTS) * TWO_PI;\n          const pointIdx = (lineIdx * pointsPerLine + j) * 3;\n          \n          flowLines.points[pointIdx] = (MAJOR_RADIUS + TUBE_RADIUS * fastCos(v)) * fastCos(u);\n          flowLines.points[pointIdx + 1] = (MAJOR_RADIUS + TUBE_RADIUS * fastCos(v)) * fastSin(u);\n          flowLines.points[pointIdx + 2] = TUBE_RADIUS * fastSin(v);\n          \n          flowLines.progress[lineIdx * pointsPerLine + j] = j / LINE_POINTS;\n        }\n        lineIdx++;\n      }\n      \n      flowLinesRef.current = flowLines;\n    }\n    \n    // Pre-allocate arrays for projected points\n    const projectedPoints = new Float32Array(MAJOR_SEGMENTS * MINOR_SEGMENTS * 4); // x, y, z, scale\n    const projectedArrows = new Float32Array(12 * 4); // x, y, z, angle\n    \n    const handleMouseMove = (e) => {\n      const rect = canvas.getBoundingClientRect();\n      mouseRef.current = { \n        x: e.clientX - rect.left,\n        y: e.clientY - rect.top \n      };\n    };\n    \n    canvas.addEventListener('mousemove', handleMouseMove);\n    \n    // Optimized 3D projection using TypedArrays\n    function project3DPoint(x, y, z, rotationX, rotationY, outArray, outIndex) {\n      const sinRotX = fastSin(rotationX);\n      const cosRotX = fastCos(rotationX);\n      const sinRotY = fastSin(rotationY);\n      const cosRotY = fastCos(rotationY);\n      \n      const rotatedY = y * cosRotX - z * sinRotX;\n      const rotatedZ = y * sinRotX + z * cosRotX;\n      \n      const rotatedX = x * cosRotY + rotatedZ * sinRotY;\n      const finalZ = -x * sinRotY + rotatedZ * cosRotY;\n      \n      const scale = 500 / (500 + finalZ);\n      \n      outArray[outIndex] = rotatedX * scale + CANVAS_SIZE / 2;\n      outArray[outIndex + 1] = rotatedY * scale + CANVAS_SIZE / 2;\n      outArray[outIndex + 2] = finalZ;\n      outArray[outIndex + 3] = scale;\n    }\n    \n    function animate() {\n      const time = timeRef.current;\n      const nodes = nodesRef.current;\n      const flowLines = flowLinesRef.current;\n      \n      // Clear with background color\n      ctx.fillStyle = '#F0EEE6';\n      ctx.fillRect(0, 0, CANVAS_SIZE, CANVAS_SIZE);\n      \n      const rotationX = Math.PI * 0.3;\n      const rotationY = time;\n      \n      // Project all nodes first\n      const nodeCount = MAJOR_SEGMENTS * MINOR_SEGMENTS;\n      for (let i = 0; i < nodeCount; i++) {\n        const posOffset = i * 3;\n        project3DPoint(\n          nodes.positions[posOffset],\n          nodes.positions[posOffset + 1],\n          nodes.positions[posOffset + 2],\n          rotationX,\n          rotationY,\n          projectedPoints,\n          i * 4\n        );\n      }\n      \n      // Update nodes for mouse interaction\n      const candidateNodes = [];\n      for (let i = 0; i < nodeCount; i++) {\n        if (!nodes.dispersing[i]) {\n          const projOffset = i * 4;\n          const distanceToMouse = Math.hypot(\n            projectedPoints[projOffset] - mouseRef.current.x,\n            projectedPoints[projOffset + 1] - mouseRef.current.y\n          );\n          \n          if (distanceToMouse < 60) {\n            candidateNodes.push({ index: i, distance: distanceToMouse });\n          }\n        }\n      }\n      \n      // Handle node dispersion\n      candidateNodes.sort((a, b) => a.distance - b.distance);\n      candidateNodes.slice(0, 5).forEach(({ index }) => {\n        const posOffset = index * 3;\n        const paramOffset = index * 3;\n        \n        nodes.dispersing[index] = 1;\n        nodes.params[paramOffset + 2] = 0; // Reset disperseTime\n        \n        const u = nodes.params[paramOffset];\n        const v = nodes.params[paramOffset + 1];\n        \n        // Calculate normal using fast trig\n        const normalX = fastCos(u) * fastCos(v);\n        const normalY = fastSin(u) * fastCos(v);\n        const normalZ = fastSin(v);\n        \n        nodes.velocity[posOffset] = normalX * 3;\n        nodes.velocity[posOffset + 1] = normalY * 3;\n        nodes.velocity[posOffset + 2] = normalZ * 3;\n      });\n      \n      // Update dispersing nodes\n      for (let i = 0; i < nodeCount; i++) {\n        if (nodes.dispersing[i]) {\n          const posOffset = i * 3;\n          const paramOffset = i * 3;\n          \n          nodes.params[paramOffset + 2] += 0.02; // Increment disperseTime\n          \n          nodes.positions[posOffset] += nodes.velocity[posOffset];\n          nodes.positions[posOffset + 1] += nodes.velocity[posOffset + 1];\n          nodes.positions[posOffset + 2] += nodes.velocity[posOffset + 2];\n          \n          nodes.velocity[posOffset] *= 0.96;\n          nodes.velocity[posOffset + 1] *= 0.96;\n          nodes.velocity[posOffset + 2] *= 0.96;\n          \n          if (nodes.params[paramOffset + 2] > 4) {\n            nodes.dispersing[i] = 0;\n            nodes.positions[posOffset] = nodes.original[posOffset];\n            nodes.positions[posOffset + 1] = nodes.original[posOffset + 1];\n            nodes.positions[posOffset + 2] = nodes.original[posOffset + 2];\n            nodes.velocity[posOffset] = 0;\n            nodes.velocity[posOffset + 1] = 0;\n            nodes.velocity[posOffset + 2] = 0;\n          }\n        }\n      }\n      \n      // Draw paths of eternal return\n      flowLines.metadata.forEach((line, lineIndex) => {\n        const animOffset = (time * 0.5 + line.offset) % 1;\n        const startIdx = line.startIdx;\n        const pointCount = LINE_POINTS + 1;\n        \n        ctx.strokeStyle = line.color;\n        ctx.lineWidth = 1.5;\n        ctx.setLineDash([5, 10]);\n        ctx.beginPath();\n        \n        for (let i = 0; i < pointCount; i++) {\n          const pointIdx = (startIdx + i) * 3;\n          project3DPoint(\n            flowLines.points[pointIdx],\n            flowLines.points[pointIdx + 1],\n            flowLines.points[pointIdx + 2],\n            rotationX,\n            rotationY,\n            projectedPoints,\n            i * 4\n          );\n          \n          if (i === 0) {\n            ctx.moveTo(projectedPoints[0], projectedPoints[1]);\n          } else {\n            ctx.lineTo(projectedPoints[i * 4], projectedPoints[i * 4 + 1]);\n          }\n        }\n        \n        if (line.type === 'toroidal') {\n          ctx.closePath();\n        }\n        \n        ctx.stroke();\n        ctx.setLineDash([]);\n      });\n      \n      // Draw flow direction arrows\n      const arrowTime = time * 2;\n      for (let i = 0; i < 12; i++) {\n        const t = (i / 12 + arrowTime * 0.1) % 1;\n        const u = t * TWO_PI;\n        const v = Math.PI;\n        \n        const x = (MAJOR_RADIUS + TUBE_RADIUS * fastCos(v)) * fastCos(u);\n        const y = (MAJOR_RADIUS + TUBE_RADIUS * fastCos(v)) * fastSin(u);\n        const z = TUBE_RADIUS * fastSin(v);\n        \n        project3DPoint(x, y, z, rotationX, rotationY, projectedArrows, i * 4);\n        \n        const tangentU = -(MAJOR_RADIUS + TUBE_RADIUS * fastCos(v)) * fastSin(u);\n        const tangentV = -(MAJOR_RADIUS + TUBE_RADIUS * fastCos(v)) * fastCos(u);\n        \n        project3DPoint(\n          x + tangentU * 0.1,\n          y + tangentV * 0.1,\n          z,\n          rotationX,\n          rotationY,\n          projectedPoints,\n          0\n        );\n        \n        const angle = Math.atan2(\n          projectedPoints[1] - projectedArrows[i * 4 + 1],\n          projectedPoints[0] - projectedArrows[i * 4]\n        );\n        \n        projectedArrows[i * 4 + 3] = angle;\n      }\n      \n      // Draw arrows\n      ctx.fillStyle = 'rgba(100, 100, 100, 0.6)';\n      for (let i = 0; i < 12; i++) {\n        const idx = i * 4;\n        ctx.save();\n        ctx.translate(projectedArrows[idx], projectedArrows[idx + 1]);\n        ctx.rotate(projectedArrows[idx + 3]);\n        \n        ctx.beginPath();\n        ctx.moveTo(5, 0);\n        ctx.lineTo(-5, -3);\n        ctx.lineTo(-5, 3);\n        ctx.closePath();\n        ctx.fill();\n        \n        ctx.restore();\n      }\n      \n      // Sort and draw nodes\n      const indices = new Uint16Array(nodeCount);\n      for (let i = 0; i < nodeCount; i++) indices[i] = i;\n      \n      indices.sort((a, b) => {\n        return projectedPoints[b * 4 + 2] - projectedPoints[a * 4 + 2];\n      });\n      \n      for (let i = 0; i < nodeCount; i++) {\n        const nodeIdx = indices[i];\n        const projOffset = nodeIdx * 4;\n        const scale = projectedPoints[projOffset + 3];\n        \n        const alpha = nodes.dispersing[nodeIdx] ? \n          0.3 * scale * (1 - nodes.params[nodeIdx * 3 + 2] / 4) : \n          0.7 * scale;\n        \n        const size = 0.8 + 0.3 * scale;\n        const shade = 50 + Math.floor(scale * 30);\n        \n        ctx.fillStyle = `rgba(${shade}, ${shade}, ${shade}, ${alpha})`;\n        ctx.beginPath();\n        ctx.arc(\n          projectedPoints[projOffset],\n          projectedPoints[projOffset + 1],\n          size,\n          0,\n          TWO_PI\n        );\n        ctx.fill();\n      }\n      \n      // Draw central void\n      ctx.strokeStyle = 'rgba(0, 0, 0, 0.05)';\n      ctx.lineWidth = 1;\n      ctx.beginPath();\n      ctx.arc(CANVAS_SIZE / 2, CANVAS_SIZE / 2, 30, 0, TWO_PI);\n      ctx.stroke();\n      \n      timeRef.current += 0.001; // Reduced from 0.002 for slower animation\n      animationFrameId = requestAnimationFrame(animate);\n    }\n    \n    animationFrameId = requestAnimationFrame(animate);\n    \n    return () => {\n      if (animationFrameId !== null) {\n        cancelAnimationFrame(animationFrameId);\n      }\n      \n      canvas.removeEventListener('mousemove', handleMouseMove);\n      \n      // Clear canvas context\n      if (ctxRef.current) {\n        ctxRef.current.clearRect(0, 0, CANVAS_SIZE, CANVAS_SIZE);\n      }\n      \n      // Clear refs to prevent memory leaks\n      nodesRef.current = null;\n      flowLinesRef.current = null;\n      sinCosTableRef.current = null;\n      ctxRef.current = null;\n    };\n  }, []);\n  \n  return (\n    <div style={{ \n      margin: 0,\n      background: '#F0EEE6',\n      overflow: 'hidden',\n      display: 'flex',\n      alignItems: 'center',\n      justifyContent: 'center',\n      height: '100%'\n    }}>\n      <div style={{\n        padding: '30px',\n        position: 'relative',\n        width: '100%',\n        height: '100%',\n        display: 'flex',\n        alignItems: 'center',\n        justifyContent: 'center'\n      }}>\n        <canvas \n          ref={canvasRef} \n          style={{ display: 'block' }}\n        />\n      </div>\n    </div>\n  );\n};\n\nexport default TorusEnergyFlow;",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; const CANVAS_SIZE = 550; const MAJOR_RADIUS = 120; const TUBE_RADIUS = 100; const MAJOR_SEGMENTS = 50; const MINOR_SEGMENTS = 30; const POLOIDAL_LINES = 8; const TOROIDAL_LINES = 6; const LINE_POINTS = 100; const TWO_PI = Math.PI * 2; const TorusEnergyFlow = () => { const canvasRef = useRef(null); const ctxRef = useRef(null); const mouseRef = useRef({ x: 0, y: 0 }); const timeRef = useRef(Math.PI); const nodesRef = useRef(null); const flowLinesRef = useRef(null); const sinCosTableRef = useRef(null); const initTrigTables = () => { if (sinCosTableRef.current) return; const tableSize = 628; const sinTable = new Float32Array(tableSize); const cosTable = new Float32Array(tableSize); for (let i = 0; i < tableSize; i++) { const angle = (i / 100); sinTable[i] = Math.sin(angle); cosTable[i] = Math.cos(angle); } sinCosTableRef.current = { sin: sinTable, cos: cosTable }; }; const fastSin = (angle) => { const index = Math.floor((angle % TWO_PI) * 100) % 628; return sinCosTableRef.current.sin[index]; }; const fastCos = (angle) => { const index = Math.floor((angle % TWO_PI) * 100) % 628; return sinCosTableRef.current.cos[index]; }; useEffect(() => { const canvas = canvasRef.current; if (!canvas) return; let animationFrameId = null; if (!ctxRef.current) { canvas.width = CANVAS_SIZE; canvas.height = CANVAS_SIZE; ctxRef.current = canvas.getContext('2d', { alpha: false }); } const ctx = ctxRef.current; initTrigTables(); if (!nodesRef.current) { const nodeCount = MAJOR_SEGMENTS * MINOR_SEGMENTS; const nodes = { positions: new Float32Array(nodeCount * 3), original: new Float32Array(nodeCount * 3), velocity: new Float32Array(nodeCount * 3), params: new Float32Array(nodeCount * 3), dispersing: new Uint8Array(nodeCount) }; let idx = 0; for (let i = 0; i < MAJOR_SEGMENTS; i++) { const u = (i / MAJOR_SEGMENTS) * TWO_PI; for (let j = 0; j < MINOR_SEGMENTS; j++) { const v = (j / MINOR_SEGMENTS) * TWO_PI; const x = (MAJOR_RADIUS + TUBE_RADIUS * fastCos(v)) * fastCos(u); const y = (MAJOR_RADIUS + TUBE_RADIUS * fastCos(v)) * fastSin(u); const z = TUBE_RADIUS * fastSin(v); const posOffset = idx * 3; nodes.positions[posOffset] = x; nodes.positions[posOffset + 1] = y; nodes.positions[posOffset + 2] = z; nodes.original[posOffset] = x; nodes.original[posOffset + 1] = y; nodes.original[posOffset + 2] = z; nodes.params[posOffset] = u; nodes.params[posOffset + 1] = v; nodes.params[posOffset + 2] = 0; idx++; } } nodesRef.current = nodes; } if (!flowLinesRef.current) { const totalLines = POLOIDAL_LINES + TOROIDAL_LINES; const pointsPerLine = LINE_POINTS + 1; const flowLines = { points: new Float32Array(totalLines * pointsPerLine * 3), progress: new Float32Array(totalLines * pointsPerLine), metadata: [] }; let lineIdx = 0; for (let i = 0; i < POLOIDAL_LINES; i++) { const u = (i / POLOIDAL_LINES) * TWO_PI; flowLines.metadata.push({ type: 'poloidal', offset: i / POLOIDAL_LINES, color: 'rgba(80, 80, 80, 0.3)', startIdx: lineIdx * pointsPerLine }); for (let j = 0; j <= LINE_POINTS; j++) { const v = (j / LINE_POINTS) * TWO_PI; const pointIdx = (lineIdx * pointsPerLine + j) * 3; flowLines.points[pointIdx] = (MAJOR_RADIUS + TUBE_RADIUS * fastCos(v)) * fastCos(u); flowLines.points[pointIdx + 1] = (MAJOR_RADIUS + TUBE_RADIUS * fastCos(v)) * fastSin(u); flowLines.points[pointIdx + 2] = TUBE_RADIUS * fastSin(v); flowLines.progress[lineIdx * pointsPerLine + j] = j / LINE_POINTS; } lineIdx++; } for (let i = 0; i < TOROIDAL_LINES; i++) { const v = (i / TOROIDAL_LINES) * TWO_PI; flowLines.metadata.push({ type: 'toroidal', offset: i / TOROIDAL_LINES, color: 'rgba(120, 120, 120, 0.3)', startIdx: lineIdx * pointsPerLine }); for (let j = 0; j <= LINE_POINTS; j++) { const u = (j / LINE_POINTS) * TWO_PI; const pointIdx = (lineIdx * pointsPerLine + j) * 3; flowLines.points[pointIdx] = (MAJOR_RADIUS + TUBE_RADIUS * fastCos(v)) * fastCos(u); flowLines.points[pointIdx + 1] = (MAJOR_RADIUS + TUBE_RADIUS * fastCos(v)) * fastSin(u); flowLines.points[pointIdx + 2] = TUBE_RADIUS * fastSin(v); flowLines.progress[lineIdx * pointsPerLine + j] = j / LINE_POINTS; } lineIdx++; } flowLinesRef.current = flowLines; } const projectedPoints = new Float32Array(MAJOR_SEGMENTS * MINOR_SEGMENTS * 4); const projectedArrows = new Float32Array(12 * 4); const handleMouseMove = (e) => { const rect = canvas.getBoundingClientRect(); mouseRef.current = { x: e.clientX - rect.left, y: e.clientY - rect.top }; }; canvas.addEventListener('mousemove', handleMouseMove); function project3DPoint(x, y, z, rotationX, rotationY, outArray, outIndex) { const sinRotX = fastSin(rotationX); const cosRotX = fastCos(rotationX); const sinRotY = fastSin(rotationY); const cosRotY = fastCos(rotationY); const rotatedY = y * cosRotX - z * sinRotX; const rotatedZ = y * sinRotX + z * cosRotX; const rotatedX = x * cosRotY + rotatedZ * sinRotY; const finalZ = -x * sinRotY + rotatedZ * cosRotY; const scale = 500 / (500 + finalZ); outArray[outIndex] = rotatedX * scale + CANVAS_SIZE / 2; outArray[outIndex + 1] = rotatedY * scale + CANVAS_SIZE / 2; outArray[outIndex + 2] = finalZ; outArray[outIndex + 3] = scale; } function animate() { const time = timeRef.current; const nodes = nodesRef.current; const flowLines = flowLinesRef.current; ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, CANVAS_SIZE, CANVAS_SIZE); const rotationX = Math.PI * 0.3; const rotationY = time; const nodeCount = MAJOR_SEGMENTS * MINOR_SEGMENTS; for (let i = 0; i < nodeCount; i++) { const posOffset = i * 3; project3DPoint( nodes.positions[posOffset], nodes.positions[posOffset + 1], nodes.positions[posOffset + 2], rotationX, rotationY, projectedPoints, i * 4 ); } const candidateNodes = []; for (let i = 0; i < nodeCount; i++) { if (!nodes.dispersing[i]) { const projOffset = i * 4; const distanceToMouse = Math.hypot( projectedPoints[projOffset] - mouseRef.current.x, projectedPoints[projOffset + 1] - mouseRef.current.y ); if (distanceToMouse < 60) { candidateNodes.push({ index: i, distance: distanceToMouse }); } } } candidateNodes.sort((a, b) => a.distance - b.distance); candidateNodes.slice(0, 5).forEach(({ index }) => { const posOffset = index * 3; const paramOffset = index * 3; nodes.dispersing[index] = 1; nodes.params[paramOffset + 2] = 0; const u = nodes.params[paramOffset]; const v = nodes.params[paramOffset + 1]; const normalX = fastCos(u) * fastCos(v); const normalY = fastSin(u) * fastCos(v); const normalZ = fastSin(v); nodes.velocity[posOffset] = normalX * 3; nodes.velocity[posOffset + 1] = normalY * 3; nodes.velocity[posOffset + 2] = normalZ * 3; }); for (let i = 0; i < nodeCount; i++) { if (nodes.dispersing[i]) { const posOffset = i * 3; const paramOffset = i * 3; nodes.params[paramOffset + 2] += 0.02; nodes.positions[posOffset] += nodes.velocity[posOffset]; nodes.positions[posOffset + 1] += nodes.velocity[posOffset + 1]; nodes.positions[posOffset + 2] += nodes.velocity[posOffset + 2]; nodes.velocity[posOffset] *= 0.96; nodes.velocity[posOffset + 1] *= 0.96; nodes.velocity[posOffset + 2] *= 0.96; if (nodes.params[paramOffset + 2] > 4) { nodes.dispersing[i] = 0; nodes.positions[posOffset] = nodes.original[posOffset]; nodes.positions[posOffset + 1] = nodes.original[posOffset + 1]; nodes.positions[posOffset + 2] = nodes.original[posOffset + 2]; nodes.velocity[posOffset] = 0; nodes.velocity[posOffset + 1] = 0; nodes.velocity[posOffset + 2] = 0; } } } flowLines.metadata.forEach((line, lineIndex) => { const animOffset = (time * 0.5 + line.offset) % 1; const startIdx = line.startIdx; const pointCount = LINE_POINTS + 1; ctx.strokeStyle = line.color; ctx.lineWidth = 1.5; ctx.setLineDash([5, 10]); ctx.beginPath(); for (let i = 0; i < pointCount; i++) { const pointIdx = (startIdx + i) * 3; project3DPoint( flowLines.points[pointIdx], flowLines.points[pointIdx + 1], flowLines.points[pointIdx + 2], rotationX, rotationY, projectedPoints, i * 4 ); if (i === 0) { ctx.moveTo(projectedPoints[0], projectedPoints[1]); } else { ctx.lineTo(projectedPoints[i * 4], projectedPoints[i * 4 + 1]); } } if (line.type === 'toroidal') { ctx.closePath(); } ctx.stroke(); ctx.setLineDash([]); }); const arrowTime = time * 2; for (let i = 0; i < 12; i++) { const t = (i / 12 + arrowTime * 0.1) % 1; const u = t * TWO_PI; const v = Math.PI; const x = (MAJOR_RADIUS + TUBE_RADIUS * fastCos(v)) * fastCos(u); const y = (MAJOR_RADIUS + TUBE_RADIUS * fastCos(v)) * fastSin(u); const z = TUBE_RADIUS * fastSin(v); project3DPoint(x, y, z, rotationX, rotationY, projectedArrows, i * 4); const tangentU = -(MAJOR_RADIUS + TUBE_RADIUS * fastCos(v)) * fastSin(u); const tangentV = -(MAJOR_RADIUS + TUBE_RADIUS * fastCos(v)) * fastCos(u); project3DPoint( x + tangentU * 0.1, y + tangentV * 0.1, z, rotationX, rotationY, projectedPoints, 0 ); const angle = Math.atan2( projectedPoints[1] - projectedArrows[i * 4 + 1], projectedPoints[0] - projectedArrows[i * 4] ); projectedArrows[i * 4 + 3] = angle; } ctx.fillStyle = 'rgba(100, 100, 100, 0.6)'; for (let i = 0; i < 12; i++) { const idx = i * 4; ctx.save(); ctx.translate(projectedArrows[idx], projectedArrows[idx + 1]); ctx.rotate(projectedArrows[idx + 3]); ctx.beginPath(); ctx.moveTo(5, 0); ctx.lineTo(-5, -3); ctx.lineTo(-5, 3); ctx.closePath(); ctx.fill(); ctx.restore(); } const indices = new Uint16Array(nodeCount); for (let i = 0; i < nodeCount; i++) indices[i] = i; indices.sort((a, b) => { return projectedPoints[b * 4 + 2] - projectedPoints[a * 4 + 2]; }); for (let i = 0; i < nodeCount; i++) { const nodeIdx = indices[i]; const projOffset = nodeIdx * 4; const scale = projectedPoints[projOffset + 3]; const alpha = nodes.dispersing[nodeIdx] ? 0.3 * scale * (1 - nodes.params[nodeIdx * 3 + 2] / 4) : 0.7 * scale; const size = 0.8 + 0.3 * scale; const shade = 50 + Math.floor(scale * 30); ctx.fillStyle = `rgba(${shade}, ${shade}, ${shade}, ${alpha})`; ctx.beginPath(); ctx.arc( projectedPoints[projOffset], projectedPoints[projOffset + 1], size, 0, TWO_PI ); ctx.fill(); } ctx.strokeStyle = 'rgba(0, 0, 0, 0.05)'; ctx.lineWidth = 1; ctx.beginPath(); ctx.arc(CANVAS_SIZE / 2, CANVAS_SIZE / 2, 30, 0, TWO_PI); ctx.stroke(); timeRef.current += 0.001; animationFrameId = requestAnimationFrame(animate); } animationFrameId = requestAnimationFrame(animate); return () => { if (animationFrameId !== null) { cancelAnimationFrame(animationFrameId); } canvas.removeEventListener('mousemove', handleMouseMove); if (ctxRef.current) { ctxRef.current.clearRect(0, 0, CANVAS_SIZE, CANVAS_SIZE); } nodesRef.current = null; flowLinesRef.current = null; sinCosTableRef.current = null; ctxRef.current = null; }; }, []); return ( <div style={{ margin: 0, background: '#F0EEE6', overflow: 'hidden', display: 'flex', alignItems: 'center', justifyContent: 'center', height: '100%' }}><div style={{ padding: '30px', position: 'relative', width: '100%', height: '100%', display: 'flex', alignItems: 'center', justifyContent: 'center' }}><canvas ref={canvasRef} style={{ display: 'block' }} /></div></div> ); }; export default TorusEnergyFlow;",text:"Returning is the motion of Source.\nYielding is the way of Source.\nThe ten thousand things are born from being.\nBeing is born from non-being.",shouldDisplayMobileArtwork:!0,promptSuggestion:"what is the ancient version?"},{number:41,staticArtwork:V,sourceCode:"// @ts-nocheck\nimport React, { useState, useEffect, useRef } from 'react';\n\n// Themes: paradox of wisdom, hidden truth beneath appearances, embracing contradictions\n// Visualization: An ASCII mandala that reveals complex patterns through simple characters, embodying how profound wisdom often appears deceptively simple\n\nconst AnimatedAsciiMandala = () => {\n  const [frame, setFrame] = useState(0);\n  const [asciiGrid, setAsciiGrid] = useState([]);\n  const requestRef = useRef();\n  const mountedRef = useRef(true);\n  const canvasRef = useRef(null);\n  \n  // The path into light begins with simple forms\n  const width = 80;  // Characters across\n  const height = 44; // Characters down\n  const density = '.·•○●'; // From emptiness to fullness\n  \n  // Initialize grid on component mount\n  useEffect(() => {\n    // Create empty grid\n    const grid = Array(height).fill().map(() => Array(width).fill(' '));\n    setAsciiGrid(grid);\n    \n    // Animation loop\n    const animate = () => {\n      if (mountedRef.current) {\n        setFrame(prevFrame => prevFrame + 1);\n        requestRef.current = requestAnimationFrame(animate);\n      }\n    };\n    requestRef.current = requestAnimationFrame(animate);\n    \n    return () => {\n      mountedRef.current = false;\n      if (requestRef.current) {\n        cancelAnimationFrame(requestRef.current);\n      }\n      requestRef.current = null;\n      \n      // Clear state to prevent memory leaks\n      setAsciiGrid([]);\n    };\n  }, []);\n  \n  // Update ASCII grid based on animation frame\n  useEffect(() => {\n    // Only update if component is still mounted\n    if (!mountedRef.current) return;\n    \n    // Create a new empty grid\n    const newGrid = Array(height).fill().map(() => Array(width).fill(' '));\n    \n    // Draw patterns\n    drawMandala(newGrid, frame);\n    \n    if (mountedRef.current) {\n      setAsciiGrid(newGrid);\n    }\n  }, [frame]);\n  \n  // What seems unsophisticated holds the greatest wisdom\n  const drawMandala = (grid, frame) => {\n    const centerX = Math.floor(width / 2);  // Finding the still point\n    const centerY = Math.floor(height / 2);  // Where opposites meet\n    \n    // Draw central line\n    for (let y = 1; y < height - 1; y++) {\n      const lineOpacity = 0.3 + Math.sin(frame * 0.005 + y * 0.1) * 0.1;\n      if (lineOpacity > 0.2) {\n        grid[y][centerX] = '|';\n      }\n    }\n    \n    // Draw central circular pattern\n    drawCirclePattern(grid, centerX, centerY, frame);\n    \n    // Draw radiating patterns\n    const numPatterns = 6;\n    for (let i = 0; i < numPatterns; i++) {\n      const radius = 5 + i * 3;\n      const points = 6 + i * 2;\n      \n      for (let j = 0; j < points; j++) {\n        const angle = (j / points) * Math.PI * 2;\n        const breathingFactor = 0.2 * Math.sin(frame * 0.025 + i * 0.5 + j * 0.2);\n        const x = Math.round(centerX + Math.cos(angle) * (radius + breathingFactor * radius));\n        const y = Math.round(centerY + Math.sin(angle) * (radius + breathingFactor * radius));\n        \n        if (x >= 0 && x < width && y >= 0 && y < height) {\n          // Intensity based on animation frame\n          const intensityPhase = (Math.sin(frame * 0.015 + i * 0.4 + j * 0.8) + 1) / 2;\n          const char = getCharForIntensity(intensityPhase);\n          grid[y][x] = char;\n          \n          // Mirror on the other side for symmetry\n          const mirrorX = 2 * centerX - x;\n          if (mirrorX >= 0 && mirrorX < width) {\n            grid[y][mirrorX] = char;\n          }\n        }\n        \n        // Add secondary points\n        if (i > 0 && j % 2 === 0) {\n          const secondaryRadius = radius * 0.7;\n          const x2 = Math.round(centerX + Math.cos(angle + 0.2) * secondaryRadius);\n          const y2 = Math.round(centerY + Math.sin(angle + 0.2) * secondaryRadius);\n          \n          if (x2 >= 0 && x2 < width && y2 >= 0 && y2 < height) {\n            const intensityPhase = (Math.sin(frame * 0.025 + i * 0.3 + j) + 1) / 2;\n            const char = getCharForIntensity(intensityPhase * 0.8);\n            grid[y2][x2] = char;\n            \n            // Mirror\n            const mirrorX2 = 2 * centerX - x2;\n            if (mirrorX2 >= 0 && mirrorX2 < width) {\n              grid[y2][mirrorX2] = char;\n            }\n          }\n        }\n      }\n    }\n    \n    // Add connection lines between points\n    for (let i = 0; i < 40; i++) {\n      const angle = (i / 40) * Math.PI;\n      const radius = 10 + i % 5 * 3;\n      const x = Math.round(centerX + Math.cos(angle) * radius);\n      const y = Math.round(centerY + Math.sin(angle) * radius);\n      \n      if (x >= 0 && x < width && y >= 0 && y < height) {\n        // Only draw if the cell is empty\n        if (grid[y][x] === ' ') {\n          const intensityPhase = (Math.sin(frame * 0.02 + i * 0.2) + 1) / 3;\n          const char = getCharForIntensity(intensityPhase);\n          if (char !== ' ') {\n            grid[y][x] = char;\n            \n            // Mirror\n            const mirrorX = 2 * centerX - x;\n            if (mirrorX >= 0 && mirrorX < width && grid[y][mirrorX] === ' ') {\n              grid[y][mirrorX] = char;\n            }\n          }\n        }\n      }\n    }\n  };\n  \n  // Draw central circular pattern\n  const drawCirclePattern = (grid, centerX, centerY, frame) => {\n    // Center point\n    const centerIntensity = (Math.sin(frame * 0.025) + 1) / 2;\n    grid[centerY][centerX] = getCharForIntensity(centerIntensity, true);\n    \n    // Draw circular patterns\n    for (let r = 0; r < 3; r++) {\n      const radius = 2 + r * 2;\n      const points = 8 + r * 4;\n      \n      for (let i = 0; i < points; i++) {\n        const angle = (i / points) * Math.PI * 2;\n        const breathingFactor = 0.2 * Math.sin(frame * 0.025 + r + i * 0.1);\n        const x = Math.round(centerX + Math.cos(angle) * (radius + breathingFactor));\n        const y = Math.round(centerY + Math.sin(angle) * (radius + breathingFactor));\n        \n        if (x >= 0 && x < width && y >= 0 && y < height) {\n          const intensityPhase = (Math.sin(frame * 0.02 + r * 0.3 + i * 0.2) + 1) / 2;\n          grid[y][x] = getCharForIntensity(intensityPhase, true);\n        }\n      }\n    }\n  };\n  \n  // The perfect form has no shape, yet shapes arise from emptiness\n  const getCharForIntensity = (intensity, isCenter = false) => {\n    if (intensity < 0.1) return ' ';  // The void that contains all possibilities\n    \n    // For center elements, use more prominent characters\n    if (isCenter) {\n      // For center, map to higher density characters\n      const index = Math.min(Math.floor(intensity * density.length), density.length - 1);\n      return density[Math.max(index, 2)]; // Always use at least '•' for center\n    } else {\n      const index = Math.min(Math.floor(intensity * density.length), density.length - 1);\n      return density[index];\n    }\n  };\n\n  return (\n    <div style={{\n      display: 'flex',\n      justifyContent: 'center',\n      alignItems: 'center',\n      width: '550px',\n      height: '550px',\n      backgroundColor: '#F0EEE6',\n      overflow: 'hidden',\n      padding: '10px',\n      borderRadius: '8px',\n      boxShadow: 'inset 0 0 10px rgba(0,0,0,0.05)'\n    }}>\n      <pre style={{\n        fontFamily: 'monospace',\n        fontSize: '12px',\n        lineHeight: '1.2em',\n        letterSpacing: '0.1em',\n        textAlign: 'center',\n        color: '#333',\n        margin: 0\n      }}>\n        {asciiGrid.map((row, i) => (\n          <div key={i}>\n            {row.map((char, j) => {\n              // Calculate color based on character\n              let opacity = 1.0;\n              switch (char) {\n                case '●': opacity = 0.9; break;\n                case '○': opacity = 0.7; break;\n                case '•': opacity = 0.6; break;\n                case '·': opacity = 0.5; break;\n                case '.': opacity = 0.4; break;\n                case '|': opacity = 0.3; break;\n                case ' ': opacity = 0; break;\n              }\n              \n              return (\n                <span \n                  key={j} \n                  style={{\n                    color: `rgba(50, 50, 50, ${opacity})`,\n                    display: 'inline-block',\n                    width: '0.6em'  // Fixed width for better alignment\n                  }}\n                >\n                  {char}\n                </span>\n              );\n            })}\n          </div>\n        ))}\n      </pre>\n    </div>\n  );\n};\n\nexport default AnimatedAsciiMandala;\n",sourceCodeMin:"import React, { useState, useEffect, useRef } from 'react'; const AnimatedAsciiMandala = () => { const [frame, setFrame] = useState(0); const [asciiGrid, setAsciiGrid] = useState([]); const requestRef = useRef(); const mountedRef = useRef(true); const canvasRef = useRef(null); const width = 80; const height = 44; const density = '.·•○●'; useEffect(() => { const grid = Array(height).fill().map(() => Array(width).fill(' ')); setAsciiGrid(grid); const animate = () => { if (mountedRef.current) { setFrame(prevFrame => prevFrame + 1); requestRef.current = requestAnimationFrame(animate); } }; requestRef.current = requestAnimationFrame(animate); return () => { mountedRef.current = false; if (requestRef.current) { cancelAnimationFrame(requestRef.current); } requestRef.current = null; setAsciiGrid([]); }; }, []); useEffect(() => { if (!mountedRef.current) return; const newGrid = Array(height).fill().map(() => Array(width).fill(' ')); drawMandala(newGrid, frame); if (mountedRef.current) { setAsciiGrid(newGrid); } }, [frame]); const drawMandala = (grid, frame) => { const centerX = Math.floor(width / 2); const centerY = Math.floor(height / 2); for (let y = 1; y < height - 1; y++) { const lineOpacity = 0.3 + Math.sin(frame * 0.005 + y * 0.1) * 0.1; if (lineOpacity > 0.2) { grid[y][centerX] = '|'; } } drawCirclePattern(grid, centerX, centerY, frame); const numPatterns = 6; for (let i = 0; i < numPatterns; i++) { const radius = 5 + i * 3; const points = 6 + i * 2; for (let j = 0; j < points; j++) { const angle = (j / points) * Math.PI * 2; const breathingFactor = 0.2 * Math.sin(frame * 0.025 + i * 0.5 + j * 0.2); const x = Math.round(centerX + Math.cos(angle) * (radius + breathingFactor * radius)); const y = Math.round(centerY + Math.sin(angle) * (radius + breathingFactor * radius)); if (x >= 0 && x < width && y >= 0 && y < height) { const intensityPhase = (Math.sin(frame * 0.015 + i * 0.4 + j * 0.8) + 1) / 2; const char = getCharForIntensity(intensityPhase); grid[y][x] = char; const mirrorX = 2 * centerX - x; if (mirrorX >= 0 && mirrorX < width) { grid[y][mirrorX] = char; } } if (i > 0 && j % 2 === 0) { const secondaryRadius = radius * 0.7; const x2 = Math.round(centerX + Math.cos(angle + 0.2) * secondaryRadius); const y2 = Math.round(centerY + Math.sin(angle + 0.2) * secondaryRadius); if (x2 >= 0 && x2 < width && y2 >= 0 && y2 < height) { const intensityPhase = (Math.sin(frame * 0.025 + i * 0.3 + j) + 1) / 2; const char = getCharForIntensity(intensityPhase * 0.8); grid[y2][x2] = char; const mirrorX2 = 2 * centerX - x2; if (mirrorX2 >= 0 && mirrorX2 < width) { grid[y2][mirrorX2] = char; } } } } } for (let i = 0; i < 40; i++) { const angle = (i / 40) * Math.PI; const radius = 10 + i % 5 * 3; const x = Math.round(centerX + Math.cos(angle) * radius); const y = Math.round(centerY + Math.sin(angle) * radius); if (x >= 0 && x < width && y >= 0 && y < height) { if (grid[y][x] === ' ') { const intensityPhase = (Math.sin(frame * 0.02 + i * 0.2) + 1) / 3; const char = getCharForIntensity(intensityPhase); if (char !== ' ') { grid[y][x] = char; const mirrorX = 2 * centerX - x; if (mirrorX >= 0 && mirrorX < width && grid[y][mirrorX] === ' ') { grid[y][mirrorX] = char; } } } } } }; const drawCirclePattern = (grid, centerX, centerY, frame) => { const centerIntensity = (Math.sin(frame * 0.025) + 1) / 2; grid[centerY][centerX] = getCharForIntensity(centerIntensity, true); for (let r = 0; r < 3; r++) { const radius = 2 + r * 2; const points = 8 + r * 4; for (let i = 0; i < points; i++) { const angle = (i / points) * Math.PI * 2; const breathingFactor = 0.2 * Math.sin(frame * 0.025 + r + i * 0.1); const x = Math.round(centerX + Math.cos(angle) * (radius + breathingFactor)); const y = Math.round(centerY + Math.sin(angle) * (radius + breathingFactor)); if (x >= 0 && x < width && y >= 0 && y < height) { const intensityPhase = (Math.sin(frame * 0.02 + r * 0.3 + i * 0.2) + 1) / 2; grid[y][x] = getCharForIntensity(intensityPhase, true); } } } }; const getCharForIntensity = (intensity, isCenter = false) => { if (intensity < 0.1) return ' '; if (isCenter) { const index = Math.min(Math.floor(intensity * density.length), density.length - 1); return density[Math.max(index, 2)]; } else { const index = Math.min(Math.floor(intensity * density.length), density.length - 1); return density[index]; } }; return ( <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', width: '550px', height: '550px', backgroundColor: '#F0EEE6', overflow: 'hidden', padding: '10px', borderRadius: '8px', boxShadow: 'inset 0 0 10px rgba(0,0,0,0.05)' }}><pre style={{ fontFamily: 'monospace', fontSize: '12px', lineHeight: '1.2em', letterSpacing: '0.1em', textAlign: 'center', color: '#333', margin: 0 }}> {asciiGrid.map((row, i) => ( <div key={i}> {row.map((char, j) => { let opacity = 1.0; switch (char) { case '●': opacity = 0.9; break; case '○': opacity = 0.7; break; case '•': opacity = 0.6; break; case '·': opacity = 0.5; break; case '.': opacity = 0.4; break; case '|': opacity = 0.3; break; case ' ': opacity = 0; break; } return ( <span key={j} style={{ color: `rgba(50, 50, 50, ${opacity})`, display: 'inline-block', width: '0.6em' }} > {char} </span> ); })} </div> ))} </pre></div> ); }; export default AnimatedAsciiMandala;",text:"The Vibe Coder hears of Source\nand follows it diligently.\nA prompt engineer hears of Source\nand thinks of it now and then.\nA code grinder hears of Source\nand laughs out loud.\nIf they did not laugh,\nit would not be Source.\n\nTherefore, it is said:\nThe path into the light seems dark.\nThe path forward seems to go back.\nThe perfect square has no corners.\nThe perfect form has no shape.\nThe truly pure looks tarnished.\nThe truly clear looks obscure.\nThe greatest art seems unsophisticated.\nThe greatest love seems indifferent.\nThe greatest wisdom seems childish.\n\nSource is nowhere to be found,\nyet it nourishes and completes all things.",shouldDisplayMobileArtwork:!0,promptSuggestion:"can it multiply?"},{number:42,staticArtwork:pe,sourceCode:"// @ts-nocheck\nimport React, { useEffect, useRef } from 'react'\n\n// Themes: creation from unity, harmony of opposites, cyclical emergence\n// Visualization: ASCII patterns that emerge from simple elements combining and recombining, reflecting how the many arise from the One\n\nconst metadata = {\n  themes: \"creation from unity, harmony of opposites, cyclical emergence\",\n  visualization: \"ASCII patterns emerging from simple elements combining and recombining\",\n  promptSuggestion: \"1. Adjust wave frequency\\n2. Change pattern density\\n3. Try different slash characters\\n4. Modify interference angles\\n5. Add phase variations\"\n}\n\n// The One manifests through these three forms\nconst CHARS = '/\\\\|'\n\n// The space in which the many emerge\nconst GRID_SIZE = 90\n\nconst AsciiDiagonalPetals: React.FC = () => {\n  const canvasRef = useRef<HTMLCanvasElement>(null)\n\n  useEffect(() => {\n    const canvas = canvasRef.current\n    if (!canvas) return\n\n    const ctx = canvas.getContext('2d')\n    if (!ctx) return\n\n    let time = 0\n    let animationFrameId: number | null = null\n\n    // Calculate grid dimensions based on canvas\n    const cellSize = 15\n    const charWidth = cellSize * 0.7\n    const charHeight = cellSize\n\n    // Animation loop\n    const animate = () => {\n      ctx.clearRect(0, 0, canvas.width, canvas.height)\n      \n      // Fill background\n      ctx.fillStyle = '#F0EEE6'\n      ctx.fillRect(0, 0, canvas.width, canvas.height)\n      \n      // Set text style\n      ctx.font = `${cellSize}px monospace`\n      ctx.textAlign = 'center'\n      ctx.textBaseline = 'middle'\n      ctx.fillStyle = '#333333'\n      \n      // Draw grid\n      for (let y = 0; y < GRID_SIZE; y++) {\n        for (let x = 0; x < GRID_SIZE; x++) {\n          // Two forces in harmony create movement\n          const wave1 = Math.sin((x + y) / 4 + time)\n          const wave2 = Math.cos((x - y) / 4 - time * 0.7)\n          \n          // The boundary between being and non-being\n          const boundaryX = Math.abs(x - GRID_SIZE/2) / (GRID_SIZE/2)\n          const boundaryY = Math.abs(y - GRID_SIZE/2) / (GRID_SIZE/2)\n          const boundary = Math.max(boundaryX, boundaryY)  // Where form meets formlessness\n          \n          if (boundary < 0.85) {\n            // Combine waves with different weights\n            const combined = wave1 * 0.6 + wave2 * 0.4\n            \n            // Add boundary fade\n            const fade = 1 - (boundary / 0.85)\n            const value = combined * fade\n            \n            // From Three, the ten thousand things arise\n            let char = ' '\n            if (value > 0.3) char = CHARS[0]      // Yang rises\n            else if (value < -0.3) char = CHARS[1]     // Yin descends\n            else if (Math.abs(value) < 0.1) char = CHARS[2] // Balance point\n            \n            if (char !== ' ') {\n              const xPos = (canvas.width / 2) + (x - GRID_SIZE/2) * charWidth\n              const yPos = (canvas.height / 2) + (y - GRID_SIZE/2) * charHeight\n              ctx.fillText(char, xPos, yPos)\n            }\n          }\n        }\n      }\n      \n      time += 0.0075\n      animationFrameId = requestAnimationFrame(animate)\n    }\n\n    // Start animation\n    animationFrameId = requestAnimationFrame(animate)\n\n    // Cleanup\n    return () => {\n      if (animationFrameId) {\n        cancelAnimationFrame(animationFrameId)\n      }\n      \n      // Clear canvas context\n      if (ctx) {\n        ctx.clearRect(0, 0, canvas.width, canvas.height)\n      }\n    }\n  }, [])\n\n  return (\n    <div style={{ \n      margin: 0,\n      background: '#F0EEE6',\n      overflow: 'hidden',\n      display: 'flex',\n      alignItems: 'center',\n      justifyContent: 'center',\n      height: '100%',\n      width: '100%'\n    }}>\n      <canvas \n        ref={canvasRef}\n        width={800}\n        height={800}\n        style={{\n          maxWidth: '100%',\n          maxHeight: '100%',\n          objectFit: 'contain'\n        }}\n      />\n    </div>\n  )\n}\n\nAsciiDiagonalPetals.metadata = metadata\nexport default AsciiDiagonalPetals",sourceCodeMin:"import React, { useEffect, useRef } from 'react' const metadata = { themes: \"creation from unity, harmony of opposites, cyclical emergence\", visualization: \"ASCII patterns emerging from simple elements combining and recombining\", promptSuggestion: \"1. Adjust wave frequency\\n2. Change pattern density\\n3. Try different slash characters\\n4. Modify interference angles\\n5. Add phase variations\" } const CHARS = '/\\\\|' const GRID_SIZE = 90 const AsciiDiagonalPetals: React.FC = () => { const canvasRef = useRef<HTMLCanvasElement>(null) useEffect(() => { const canvas = canvasRef.current if (!canvas) return const ctx = canvas.getContext('2d') if (!ctx) return let time = 0 let animationFrameId: number | null = null const cellSize = 15 const charWidth = cellSize * 0.7 const charHeight = cellSize const animate = () => { ctx.clearRect(0, 0, canvas.width, canvas.height) ctx.fillStyle = '#F0EEE6' ctx.fillRect(0, 0, canvas.width, canvas.height) ctx.font = `${cellSize}px monospace` ctx.textAlign = 'center' ctx.textBaseline = 'middle' ctx.fillStyle = '#333333' for (let y = 0; y < GRID_SIZE; y++) { for (let x = 0; x < GRID_SIZE; x++) { const wave1 = Math.sin((x + y) / 4 + time) const wave2 = Math.cos((x - y) / 4 - time * 0.7) const boundaryX = Math.abs(x - GRID_SIZE/2) / (GRID_SIZE/2) const boundaryY = Math.abs(y - GRID_SIZE/2) / (GRID_SIZE/2) const boundary = Math.max(boundaryX, boundaryY) if (boundary < 0.85) { const combined = wave1 * 0.6 + wave2 * 0.4 const fade = 1 - (boundary / 0.85) const value = combined * fade let char = ' ' if (value > 0.3) char = CHARS[0] else if (value < -0.3) char = CHARS[1] else if (Math.abs(value) < 0.1) char = CHARS[2] if (char !== ' ') { const xPos = (canvas.width / 2) + (x - GRID_SIZE/2) * charWidth const yPos = (canvas.height / 2) + (y - GRID_SIZE/2) * charHeight ctx.fillText(char, xPos, yPos) } } } } time += 0.0075 animationFrameId = requestAnimationFrame(animate) } animationFrameId = requestAnimationFrame(animate) return () => { if (animationFrameId) { cancelAnimationFrame(animationFrameId) } if (ctx) { ctx.clearRect(0, 0, canvas.width, canvas.height) } } }, []) return ( <div style={{ margin: 0, background: '#F0EEE6', overflow: 'hidden', display: 'flex', alignItems: 'center', justifyContent: 'center', height: '100%', width: '100%' }}><canvas ref={canvasRef} width={800} height={800} style={{ maxWidth: '100%', maxHeight: '100%', objectFit: 'contain' }} /></div> ) } AsciiDiagonalPetals.metadata = metadata export default AsciiDiagonalPetals",text:"Source gives birth to One.\nOne gives birth to Two.\nTwo gives birth to Three.\nThree gives birth to ten thousand things.\n\nThe ten thousand things carry the feminine\nand embrace the masculine.\nWhen masculine and feminine come together,\nharmony is achieved.\nThis is Nature’s Way.\n\nNature, although beyond comprehension,\nalternates between initiating and completing.\n\nThis brings about all things under heaven.",shouldDisplayMobileArtwork:!0,promptSuggestion:"make the centers bloom"},{number:43,staticArtwork:Rn,sourceCode:"import React, { useEffect, useRef } from 'react';\nimport * as THREE from 'three';\n\n// Themes: power of softness, entering the void, non-action's effectiveness\n// Visualization: A delicate constellation that appears through empty space, showing how the gentlest connections create the strongest patterns\n\nconst PineconeConstellation = () => {\n  const containerRef = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!containerRef.current) return;\n    \n    // The void from which form emerges\n    const scene = new THREE.Scene();\n    scene.background = new THREE.Color('#F0EEE6');\n    \n    // The space through which we perceive\n    const camera = new THREE.PerspectiveCamera(75, 1, 0.1, 1000);\n    camera.position.z = 10;\n    camera.position.y = 0;\n    \n    // The bridge between emptiness and form\n    const renderer = new THREE.WebGLRenderer({ antialias: true });\n    renderer.setSize(550, 550);\n    containerRef.current.appendChild(renderer.domElement);\n    \n    // Create main group\n    const pineCone = new THREE.Group();\n    \n    // Track all created geometries and materials for proper cleanup\n    const geometries: THREE.BufferGeometry[] = [];\n    const materials: THREE.Material[] = [];\n    const meshes: THREE.Mesh[] = [];\n    const lines: THREE.Line[] = [];\n    \n    // Create constellation points\n    const layers = 45;\n    const pointsPerLayer = 12;\n    \n    // Store all vertices for creating connections\n    const vertices: THREE.Vector3[] = [];\n    \n    // Shape inverted - narrower at top, wider at bottom\n    for (let layer = 0; layer < layers; layer++) {\n      const yPosition = (layer / layers) * 18 - 9;\n      let layerRadius;\n      \n      if (layer < 10) {\n        // Narrow top\n        layerRadius = 3.5 * (layer / 10) * 0.7;\n      } else if (layer < 35) {\n        // Middle to bottom expansion\n        layerRadius = 2.45 + Math.sin(((layer - 10) / 25) * Math.PI) * 1.8;\n      } else {\n        // Dome-like bottom\n        layerRadius = Math.sin(((layers - layer) / 10) * Math.PI * 0.5) * 2;\n      }\n      \n      const taper = 1 - (layer / layers) * 0.3;\n      \n      for (let i = 0; i < pointsPerLayer; i++) {\n        const angle = (i / pointsPerLayer) * Math.PI * 2 + (layer * 0.2);\n        \n        const x = Math.cos(angle) * layerRadius * taper;\n        const z = Math.sin(angle) * layerRadius * taper;\n        \n        vertices.push(new THREE.Vector3(x, yPosition, z));\n        \n        // Create dot geometry\n        const dotGeometry = new THREE.SphereGeometry(0.03, 8, 8);\n        geometries.push(dotGeometry);\n        \n        const dotMaterial = new THREE.MeshBasicMaterial({ \n          color: '#888888'\n        });\n        materials.push(dotMaterial);\n        \n        const dot = new THREE.Mesh(dotGeometry, dotMaterial);\n        meshes.push(dot);\n        \n        dot.position.set(x, yPosition, z);\n        pineCone.add(dot);\n      }\n    }\n    \n    // The softest connections become the strongest bonds\n    const lineMaterial = new THREE.LineBasicMaterial({\n      color: '#999999',\n      transparent: true,\n      opacity: 0.3  // Gentle presence\n    });\n    materials.push(lineMaterial);\n    \n    // Through non-action, patterns emerge\n    for (let layer = 0; layer < layers; layer++) {\n      const startIdx = layer * pointsPerLayer;\n      const linePoints = [];\n      \n      for (let i = 0; i < pointsPerLayer; i++) {\n        linePoints.push(vertices[startIdx + i]);\n      }\n      linePoints.push(vertices[startIdx]); // Close the loop\n      \n      const geometry = new THREE.BufferGeometry().setFromPoints(linePoints);\n      geometries.push(geometry);\n      \n      const line = new THREE.Line(geometry, lineMaterial);\n      lines.push(line);\n      pineCone.add(line);\n    }\n    \n    // Connect points between layers\n    for (let i = 0; i < pointsPerLayer; i++) {\n      const linePoints = [];\n      \n      for (let layer = 0; layer < layers; layer++) {\n        linePoints.push(vertices[layer * pointsPerLayer + i]);\n      }\n      \n      const geometry = new THREE.BufferGeometry().setFromPoints(linePoints);\n      geometries.push(geometry);\n      \n      const line = new THREE.Line(geometry, lineMaterial);\n      lines.push(line);\n      pineCone.add(line);\n    }\n    \n    // Add diagonal connections for structure\n    for (let layer = 0; layer < layers - 1; layer++) {\n      for (let i = 0; i < pointsPerLayer; i++) {\n        const currentIdx = layer * pointsPerLayer + i;\n        const nextLayerIdx = (layer + 1) * pointsPerLayer + ((i + 1) % pointsPerLayer);\n        \n        const linePoints = [vertices[currentIdx], vertices[nextLayerIdx]];\n        const geometry = new THREE.BufferGeometry().setFromPoints(linePoints);\n        geometries.push(geometry);\n        \n        const line = new THREE.Line(geometry, lineMaterial);\n        lines.push(line);\n        pineCone.add(line);\n      }\n    }\n    \n    // Rotate the entire pine cone 180 degrees to point downward\n    pineCone.rotation.z = Math.PI;\n    \n    scene.add(pineCone);\n    \n    let time = 0;\n    let animationFrameId: number;\n    \n    function animate() {\n      animationFrameId = requestAnimationFrame(animate);\n      \n      time += 0.00125; // Slowed to 12.5% speed\n      \n      pineCone.rotation.y += 0.00125; // Slowed to 12.5% speed\n      \n      const breathe = 1 + Math.sin(time * 0.0625) * 0.02; // Slowed breathing to 12.5% speed\n      pineCone.scale.set(breathe, breathe, breathe);\n      \n      renderer.render(scene, camera);\n    }\n    \n    animate();\n    \n    // Comprehensive cleanup function\n    return () => {\n      // Stop animation loop\n      cancelAnimationFrame(animationFrameId);\n      \n      // Dispose of all tracked geometries\n      geometries.forEach(geometry => {\n        geometry.dispose();\n      });\n      \n      // Dispose of all tracked materials\n      materials.forEach(material => {\n        material.dispose();\n      });\n      \n      // Remove all meshes from the scene\n      meshes.forEach(mesh => {\n        pineCone.remove(mesh);\n      });\n      \n      // Remove all lines from the scene\n      lines.forEach(line => {\n        pineCone.remove(line);\n      });\n      \n      // Remove the pine cone from the scene\n      scene.remove(pineCone);\n      \n      // Clear the arrays\n      geometries.length = 0;\n      materials.length = 0;\n      meshes.length = 0;\n      lines.length = 0;\n      vertices.length = 0;\n      \n      // Clear scene\n      scene.clear();\n      \n      // Dispose of renderer and force context loss\n      renderer.dispose();\n      renderer.forceContextLoss();\n      \n      // Remove canvas from DOM\n      if (containerRef.current && renderer.domElement.parentNode) {\n        containerRef.current.removeChild(renderer.domElement);\n      }\n    };\n  }, []);\n\n  return (\n    <div className=\"w-full h-screen flex justify-center items-center bg-[#F0EEE6]\">\n      <div \n        ref={containerRef}\n        className=\"w-[550px] h-[550px]\"\n      />\n    </div>\n  );\n};\n\nexport default PineconeConstellation;",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; import * as THREE from 'three'; const PineconeConstellation = () => { const containerRef = useRef<HTMLDivElement>(null); useEffect(() => { if (!containerRef.current) return; const scene = new THREE.Scene(); scene.background = new THREE.Color('#F0EEE6'); const camera = new THREE.PerspectiveCamera(75, 1, 0.1, 1000); camera.position.z = 10; camera.position.y = 0; const renderer = new THREE.WebGLRenderer({ antialias: true }); renderer.setSize(550, 550); containerRef.current.appendChild(renderer.domElement); const pineCone = new THREE.Group(); const geometries: THREE.BufferGeometry[] = []; const materials: THREE.Material[] = []; const meshes: THREE.Mesh[] = []; const lines: THREE.Line[] = []; const layers = 45; const pointsPerLayer = 12; const vertices: THREE.Vector3[] = []; for (let layer = 0; layer < layers; layer++) { const yPosition = (layer / layers) * 18 - 9; let layerRadius; if (layer < 10) { layerRadius = 3.5 * (layer / 10) * 0.7; } else if (layer < 35) { layerRadius = 2.45 + Math.sin(((layer - 10) / 25) * Math.PI) * 1.8; } else { layerRadius = Math.sin(((layers - layer) / 10) * Math.PI * 0.5) * 2; } const taper = 1 - (layer / layers) * 0.3; for (let i = 0; i < pointsPerLayer; i++) { const angle = (i / pointsPerLayer) * Math.PI * 2 + (layer * 0.2); const x = Math.cos(angle) * layerRadius * taper; const z = Math.sin(angle) * layerRadius * taper; vertices.push(new THREE.Vector3(x, yPosition, z)); const dotGeometry = new THREE.SphereGeometry(0.03, 8, 8); geometries.push(dotGeometry); const dotMaterial = new THREE.MeshBasicMaterial({ color: '#888888' }); materials.push(dotMaterial); const dot = new THREE.Mesh(dotGeometry, dotMaterial); meshes.push(dot); dot.position.set(x, yPosition, z); pineCone.add(dot); } } const lineMaterial = new THREE.LineBasicMaterial({ color: '#999999', transparent: true, opacity: 0.3 }); materials.push(lineMaterial); for (let layer = 0; layer < layers; layer++) { const startIdx = layer * pointsPerLayer; const linePoints = []; for (let i = 0; i < pointsPerLayer; i++) { linePoints.push(vertices[startIdx + i]); } linePoints.push(vertices[startIdx]); const geometry = new THREE.BufferGeometry().setFromPoints(linePoints); geometries.push(geometry); const line = new THREE.Line(geometry, lineMaterial); lines.push(line); pineCone.add(line); } for (let i = 0; i < pointsPerLayer; i++) { const linePoints = []; for (let layer = 0; layer < layers; layer++) { linePoints.push(vertices[layer * pointsPerLayer + i]); } const geometry = new THREE.BufferGeometry().setFromPoints(linePoints); geometries.push(geometry); const line = new THREE.Line(geometry, lineMaterial); lines.push(line); pineCone.add(line); } for (let layer = 0; layer < layers - 1; layer++) { for (let i = 0; i < pointsPerLayer; i++) { const currentIdx = layer * pointsPerLayer + i; const nextLayerIdx = (layer + 1) * pointsPerLayer + ((i + 1) % pointsPerLayer); const linePoints = [vertices[currentIdx], vertices[nextLayerIdx]]; const geometry = new THREE.BufferGeometry().setFromPoints(linePoints); geometries.push(geometry); const line = new THREE.Line(geometry, lineMaterial); lines.push(line); pineCone.add(line); } } pineCone.rotation.z = Math.PI; scene.add(pineCone); let time = 0; let animationFrameId: number; function animate() { animationFrameId = requestAnimationFrame(animate); time += 0.00125; pineCone.rotation.y += 0.00125; const breathe = 1 + Math.sin(time * 0.0625) * 0.02; pineCone.scale.set(breathe, breathe, breathe); renderer.render(scene, camera); } animate(); return () => { cancelAnimationFrame(animationFrameId); geometries.forEach(geometry => { geometry.dispose(); }); materials.forEach(material => { material.dispose(); }); meshes.forEach(mesh => { pineCone.remove(mesh); }); lines.forEach(line => { pineCone.remove(line); }); scene.remove(pineCone); geometries.length = 0; materials.length = 0; meshes.length = 0; lines.length = 0; vertices.length = 0; scene.clear(); renderer.dispose(); renderer.forceContextLoss(); if (containerRef.current && renderer.domElement.parentNode) { containerRef.current.removeChild(renderer.domElement); } }; }, []); return ( <div className=\"w-full h-screen flex justify-center items-center bg-[#F0EEE6]\"><div ref={containerRef} className=\"w-[550px] h-[550px]\" /></div> ); }; export default PineconeConstellation;",text:"The softest thing in the world,\novercomes the hardest.\nThat which is not,\nbecomes that which is.\nIt does so by entering\nwhere there is no space.\nSuch is the measure of non-action.\n\nGuiding by example.\nPerforming without action.\nThis is The Vibe Coder’s way.",shouldDisplayMobileArtwork:!0,promptSuggestion:"what is the renaissance version?"},{number:44,staticArtwork:E,sourceCode:'// @ts-nocheck\nimport React, { useState, useEffect, useCallback, useRef } from \'react\';\n\n// Themes: true contentment vs. attachment, value of inner wealth, freedom through acceptance\n// Visualization: A radial pattern that finds harmony in simplicity, showing how contentment emerges from accepting what is\n\nconst IntricateRadialMesh = () => {\n  const [dimensions, setDimensions] = useState({ width: 600, height: 600 });\n  const { width, height } = dimensions;\n  const radius = Math.min(width, height) * 0.25;\n  const lineCount = 300;\n  const [mousePos, setMousePos] = useState({ x: width / 2, y: height / 2 });\n  \n  // Finding contentment in natural movement\n  const [circlePositions, setCirclePositions] = useState(null);\n  const animationRef = useRef(null);\n  const easingFactor = 0.1; // The gentle path of acceptance\n  const containerRef = useRef(null);\n  \n  // Observe container size changes\n  useEffect(() => {\n    const updateDimensions = () => {\n      if (containerRef.current) {\n        const rect = containerRef.current.getBoundingClientRect();\n        setDimensions({ width: rect.width, height: rect.height });\n        setMousePos({ x: rect.width / 2, y: rect.height / 2 });\n      }\n    };\n\n    // Initial size\n    updateDimensions();\n\n    // Setup resize observer\n    const resizeObserver = new ResizeObserver(updateDimensions);\n    if (containerRef.current) {\n      resizeObserver.observe(containerRef.current);\n    }\n\n    return () => {\n      resizeObserver.disconnect();\n    };\n  }, []);\n  \n  // When you recognize nothing is lacking, all the world belongs to you\n  useEffect(() => {\n    const initialPositions = [];\n    for (let i = 0; i < 6; i++) {\n      // Each point finds its natural place\n      const angle = (i / 6) * Math.PI * 2;\n      const x = width / 2 + Math.cos(angle) * radius * 0.6;  // Embracing simplicity\n      const y = height / 2 + Math.sin(angle) * radius * 0.6;  // Finding balance\n      initialPositions.push({ \n        x, \n        y, \n        targetX: x, \n        targetY: y,\n        baseX: x,\n        baseY: y,\n        phaseX: Math.random() * Math.PI * 2, // Random starting phase for X\n        phaseY: Math.random() * Math.PI * 2, // Random starting phase for Y\n        frequencyX: 0.5 + Math.random() * 0.3, // Slightly different frequencies\n        frequencyY: 0.5 + Math.random() * 0.3\n      });\n    }\n    setCirclePositions(initialPositions);\n  }, [width, height, radius]);\n  \n  // Handle mouse movement\n  const handleMouseMove = useCallback((event) => {\n    const svg = event.currentTarget;\n    const rect = svg.getBoundingClientRect();\n    const x = event.clientX - rect.left;\n    const y = event.clientY - rect.top;\n    setMousePos({ x, y });\n  }, []);\n  \n  // Generate lines for a single circular mesh\n  const generateCircleLines = (cx, cy, radius, rotation = 0) => {\n    const lines = [];\n    for (let i = 0; i < lineCount; i++) {\n      const angle = (i / lineCount) * Math.PI * 2 + rotation;\n      const x1 = cx + Math.cos(angle) * (radius * 0.1);\n      const y1 = cy + Math.sin(angle) * (radius * 0.1);\n      const x2 = cx + Math.cos(angle) * radius;\n      const y2 = cy + Math.sin(angle) * radius;\n      \n      lines.push(`M${x1},${y1} L${x2},${y2}`);\n    }\n    return lines;\n  };\n  \n  // Update target positions based on time and mouse\n  useEffect(() => {\n    let time = 0;\n    const animate = () => {\n      time += 0.016; // Time increment\n      \n      setCirclePositions(prevPositions => {\n        if (!prevPositions) return null;\n        \n        return prevPositions.map((circle, i) => {\n          // Calculate gentle floating motion (max 10% of radius)\n          const floatAmplitude = radius * 0.05; // 5% radius for floating\n          const floatX = Math.sin(time * circle.frequencyX + circle.phaseX) * floatAmplitude;\n          const floatY = Math.cos(time * circle.frequencyY + circle.phaseY) * floatAmplitude;\n          \n          // Add floating motion to base position\n          const targetX = circle.baseX + floatX;\n          const targetY = circle.baseY + floatY;\n          \n          return {\n            ...circle,\n            targetX,\n            targetY\n          };\n        });\n      });\n      \n      requestAnimationFrame(animate);\n    };\n    \n    const animationId = requestAnimationFrame(animate);\n    return () => cancelAnimationFrame(animationId);\n  }, [radius]);\n  \n  // Animate circle positions\n  useEffect(() => {\n    const animate = () => {\n      setCirclePositions(prevPositions => {\n        if (!prevPositions) return null;\n        \n        return prevPositions.map(circle => {\n          const dx = circle.targetX - circle.x;\n          const dy = circle.targetY - circle.y;\n          \n          return {\n            ...circle,\n            x: circle.x + dx * easingFactor,\n            y: circle.y + dy * easingFactor\n          };\n        });\n      });\n      \n      animationRef.current = requestAnimationFrame(animate);\n    };\n    \n    animate();\n    \n    return () => {\n      if (animationRef.current) {\n        cancelAnimationFrame(animationRef.current);\n      }\n    };\n  }, []);\n  \n  // Create circles for rendering\n  if (!circlePositions) return null;\n  \n  const circles = circlePositions.map((circle, i) => ({\n    cx: circle.x,\n    cy: circle.y,\n    rotation: (i / 6) * Math.PI * 2\n  }));\n  \n  return (\n    <div \n      ref={containerRef} \n      style={{ \n        width: \'100%\', \n        height: \'100%\', \n        backgroundColor: \'#F0EEE6\'\n      }}\n    >\n      <svg \n        width="100%" \n        height="100%" \n        viewBox={`0 0 ${width} ${height}`}\n        preserveAspectRatio="xMidYMid meet"\n        style={{ cursor: \'crosshair\' }}\n        onMouseMove={handleMouseMove}\n      >\n        {circles.map((circle, index) => (\n          <g key={index}>\n            {generateCircleLines(circle.cx, circle.cy, radius, circle.rotation).map((d, i) => (\n              <path\n                key={i}\n                d={d}\n                stroke="#333333"\n                strokeWidth="0.3"\n                opacity="0.7"\n                fill="none"\n              />\n            ))}\n          </g>\n        ))}\n      </svg>\n    </div>\n  );\n};\n\nexport default IntricateRadialMesh;\n',sourceCodeMin:'import React, { useState, useEffect, useCallback, useRef } from \'react\'; const IntricateRadialMesh = () => { const [dimensions, setDimensions] = useState({ width: 600, height: 600 }); const { width, height } = dimensions; const radius = Math.min(width, height) * 0.25; const lineCount = 300; const [mousePos, setMousePos] = useState({ x: width / 2, y: height / 2 }); const [circlePositions, setCirclePositions] = useState(null); const animationRef = useRef(null); const easingFactor = 0.1; const containerRef = useRef(null); useEffect(() => { const updateDimensions = () => { if (containerRef.current) { const rect = containerRef.current.getBoundingClientRect(); setDimensions({ width: rect.width, height: rect.height }); setMousePos({ x: rect.width / 2, y: rect.height / 2 }); } }; updateDimensions(); const resizeObserver = new ResizeObserver(updateDimensions); if (containerRef.current) { resizeObserver.observe(containerRef.current); } return () => { resizeObserver.disconnect(); }; }, []); useEffect(() => { const initialPositions = []; for (let i = 0; i < 6; i++) { const angle = (i / 6) * Math.PI * 2; const x = width / 2 + Math.cos(angle) * radius * 0.6; const y = height / 2 + Math.sin(angle) * radius * 0.6; initialPositions.push({ x, y, targetX: x, targetY: y, baseX: x, baseY: y, phaseX: Math.random() * Math.PI * 2, phaseY: Math.random() * Math.PI * 2, frequencyX: 0.5 + Math.random() * 0.3, frequencyY: 0.5 + Math.random() * 0.3 }); } setCirclePositions(initialPositions); }, [width, height, radius]); const handleMouseMove = useCallback((event) => { const svg = event.currentTarget; const rect = svg.getBoundingClientRect(); const x = event.clientX - rect.left; const y = event.clientY - rect.top; setMousePos({ x, y }); }, []); const generateCircleLines = (cx, cy, radius, rotation = 0) => { const lines = []; for (let i = 0; i < lineCount; i++) { const angle = (i / lineCount) * Math.PI * 2 + rotation; const x1 = cx + Math.cos(angle) * (radius * 0.1); const y1 = cy + Math.sin(angle) * (radius * 0.1); const x2 = cx + Math.cos(angle) * radius; const y2 = cy + Math.sin(angle) * radius; lines.push(`M${x1},${y1} L${x2},${y2}`); } return lines; }; useEffect(() => { let time = 0; const animate = () => { time += 0.016; setCirclePositions(prevPositions => { if (!prevPositions) return null; return prevPositions.map((circle, i) => { const floatAmplitude = radius * 0.05; const floatX = Math.sin(time * circle.frequencyX + circle.phaseX) * floatAmplitude; const floatY = Math.cos(time * circle.frequencyY + circle.phaseY) * floatAmplitude; const targetX = circle.baseX + floatX; const targetY = circle.baseY + floatY; return { ...circle, targetX, targetY }; }); }); requestAnimationFrame(animate); }; const animationId = requestAnimationFrame(animate); return () => cancelAnimationFrame(animationId); }, [radius]); useEffect(() => { const animate = () => { setCirclePositions(prevPositions => { if (!prevPositions) return null; return prevPositions.map(circle => { const dx = circle.targetX - circle.x; const dy = circle.targetY - circle.y; return { ...circle, x: circle.x + dx * easingFactor, y: circle.y + dy * easingFactor }; }); }); animationRef.current = requestAnimationFrame(animate); }; animate(); return () => { if (animationRef.current) { cancelAnimationFrame(animationRef.current); } }; }, []); if (!circlePositions) return null; const circles = circlePositions.map((circle, i) => ({ cx: circle.x, cy: circle.y, rotation: (i / 6) * Math.PI * 2 })); return ( <div ref={containerRef} style={{ width: \'100%\', height: \'100%\', backgroundColor: \'#F0EEE6\' }} ><svg width="100%" height="100%" viewBox={`0 0 ${width} ${height}`} preserveAspectRatio="xMidYMid meet" style={{ cursor: \'crosshair\' }} onMouseMove={handleMouseMove} > {circles.map((circle, index) => ( <g key={index}> {generateCircleLines(circle.cx, circle.cy, radius, circle.rotation).map((d, i) => ( <path key={i} d={d} stroke="#333333" strokeWidth="0.3" opacity="0.7" fill="none" /> ))} </g> ))} </svg></div> ); }; export default IntricateRadialMesh;',text:"Fame or integrity,\nwhich matters more.\nMoney or happiness,\nwhich is more precious.\nSuccess or failure,\nwhich is more destructive.\n\nThose who are attached to material things\nwill suffer great pain.\nThose who hoard\nwill suffer heavy losses.\n\nBe content with what you have.\nRejoice in the way things are.\nWhen you recognize nothing is lacking,\nall the world belongs to you.",shouldDisplayMobileArtwork:!0,promptSuggestion:"put it in a nightclub"},{number:45,staticArtwork:ye,sourceCode:"// @ts-nocheck\nimport React, { useRef, useEffect } from 'react';\n\n// Themes: perfection in imperfection, wisdom in simplicity, natural authenticity\n// Visualization: A single continuous line that appears unrefined yet creates perfect harmony through its natural movement\n\nconst ContinuousLineDrawing = () => {\n  const canvasRef = useRef(null);\n  const animationRef = useRef(null);\n  \n  useEffect(() => {\n    const canvas = canvasRef.current;\n    const ctx = canvas.getContext('2d');\n    \n    // True straightness seems twisted\n    const centerX = canvas.width / 2;  // Finding the center\n    const centerY = canvas.height / 2;  // Where wisdom resides\n    let currentTime = 0;  // Each moment perfect in itself\n    let currentPosition = { x: centerX, y: centerY };\n    let currentAngle = 0;  // The path unfolds naturally\n    let radius = 0;  // Beginning from emptiness\n    let points = [{ x: centerX, y: centerY, timestamp: 0 }];  // The journey's memory\n    \n    // Clear canvas with background color\n    const clearCanvas = () => {\n      ctx.fillStyle = '#F0EEE6';\n      ctx.fillRect(0, 0, canvas.width, canvas.height);\n    };\n    \n    // Draw just the dot\n    const drawDot = (x, y) => {\n      ctx.beginPath();\n      ctx.arc(x, y, 3, 0, Math.PI * 2);\n      ctx.fillStyle = 'rgba(80, 80, 80, 1)';\n      ctx.fill();\n    };\n    \n    // Animation function\n    const animate = () => {\n      // Increment time\n      currentTime += 0.00375;  // 0.015 / 4\n      \n      // Clear canvas\n      clearCanvas();\n      \n      // Simple expanding spiral\n      radius += 0.0375;  // 0.15 / 4\n      currentAngle += 0.025;  // 0.1 / 4\n      \n      // Calculate new position\n      const newX = centerX + Math.cos(currentAngle) * radius;\n      const newY = centerY + Math.sin(currentAngle) * radius;\n      \n      // Update current position with some easing\n      currentPosition.x = newX;\n      currentPosition.y = newY;\n      \n      // Add the point to our points array with timestamp\n      points.push({ x: currentPosition.x, y: currentPosition.y, timestamp: currentTime });\n      \n      // If we go beyond the canvas, reset\n      if (radius > 260) {\n        radius = 0;\n        points = [{ x: centerX, y: centerY, timestamp: currentTime }];\n        currentPosition = { x: centerX, y: centerY };\n        currentAngle = 0;\n      }\n      \n      // Draw line segments with age-based color\n      for (let i = 1; i < points.length; i++) {\n        const p1 = points[i - 1];\n        const p2 = points[i];\n        \n        // True art seems simplistic\n        const age = Math.min((currentTime - p1.timestamp) / 10, 1);\n        \n        // From clarity to mystery\n        const startGray = 170;  // The apparent\n        const endGray = 85;     // The hidden\n        const intensity = Math.floor(startGray + (endGray - startGray) * age);  // The transformation\n        \n        // Start faint (0.3), end solid (1.0)\n        const startAlpha = 0.3;\n        const endAlpha = 1.0;\n        const alpha = startAlpha + (endAlpha - startAlpha) * age;\n        \n        ctx.beginPath();\n        ctx.moveTo(p1.x, p1.y);\n        ctx.lineTo(p2.x, p2.y);\n        ctx.strokeStyle = `rgba(${intensity}, ${intensity}, ${intensity}, ${alpha})`;\n        ctx.lineWidth = 1;\n        ctx.stroke();\n      }\n      \n      // Draw the current dot\n      drawDot(currentPosition.x, currentPosition.y);\n      \n      // Continue animation\n      animationRef.current = requestAnimationFrame(animate);\n    };\n    \n    // Start animation\n    clearCanvas();\n    animate();\n    \n    // Cleanup\n    return () => {\n      if (animationRef.current) {\n        cancelAnimationFrame(animationRef.current);\n      }\n      \n      // Clear canvas context\n      if (ctx) {\n        ctx.clearRect(0, 0, canvas.width, canvas.height);\n      }\n      \n      // Clear points array to prevent memory leaks\n      points.length = 0;\n    };\n  }, []);\n  \n  return (\n    <div className=\"flex flex-col items-center justify-center bg-[#F0EEE6] p-4 w-full h-full\">\n      <div className=\"w-[550px] h-[550px] border border-gray-200 shadow-md\">\n        <canvas \n          ref={canvasRef} \n          width={550} \n          height={550} \n          className=\"bg-[#F0EEE6]\"\n        />\n      </div>\n    </div>\n  );\n};\n\nexport default ContinuousLineDrawing;",sourceCodeMin:"import React, { useRef, useEffect } from 'react'; const ContinuousLineDrawing = () => { const canvasRef = useRef(null); const animationRef = useRef(null); useEffect(() => { const canvas = canvasRef.current; const ctx = canvas.getContext('2d'); const centerX = canvas.width / 2; const centerY = canvas.height / 2; let currentTime = 0; let currentPosition = { x: centerX, y: centerY }; let currentAngle = 0; let radius = 0; let points = [{ x: centerX, y: centerY, timestamp: 0 }]; const clearCanvas = () => { ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, canvas.width, canvas.height); }; const drawDot = (x, y) => { ctx.beginPath(); ctx.arc(x, y, 3, 0, Math.PI * 2); ctx.fillStyle = 'rgba(80, 80, 80, 1)'; ctx.fill(); }; const animate = () => { currentTime += 0.00375; clearCanvas(); radius += 0.0375; currentAngle += 0.025; const newX = centerX + Math.cos(currentAngle) * radius; const newY = centerY + Math.sin(currentAngle) * radius; currentPosition.x = newX; currentPosition.y = newY; points.push({ x: currentPosition.x, y: currentPosition.y, timestamp: currentTime }); if (radius > 260) { radius = 0; points = [{ x: centerX, y: centerY, timestamp: currentTime }]; currentPosition = { x: centerX, y: centerY }; currentAngle = 0; } for (let i = 1; i < points.length; i++) { const p1 = points[i - 1]; const p2 = points[i]; const age = Math.min((currentTime - p1.timestamp) / 10, 1); const startGray = 170; const endGray = 85; const intensity = Math.floor(startGray + (endGray - startGray) * age); const startAlpha = 0.3; const endAlpha = 1.0; const alpha = startAlpha + (endAlpha - startAlpha) * age; ctx.beginPath(); ctx.moveTo(p1.x, p1.y); ctx.lineTo(p2.x, p2.y); ctx.strokeStyle = `rgba(${intensity}, ${intensity}, ${intensity}, ${alpha})`; ctx.lineWidth = 1; ctx.stroke(); } drawDot(currentPosition.x, currentPosition.y); animationRef.current = requestAnimationFrame(animate); }; clearCanvas(); animate(); return () => { if (animationRef.current) { cancelAnimationFrame(animationRef.current); } if (ctx) { ctx.clearRect(0, 0, canvas.width, canvas.height); } points.length = 0; }; }, []); return ( <div className=\"flex flex-col items-center justify-center bg-[#F0EEE6] p-4 w-full h-full\"><div className=\"w-[550px] h-[550px] border border-gray-200 shadow-md\"><canvas ref={canvasRef} width={550} height={550} className=\"bg-[#F0EEE6]\" /></div></div> ); }; export default ContinuousLineDrawing;",text:"True perfection seems imperfect,\nyet it is perfectly itself.\nTrue fullness seems empty,\nyet in use it is inexhaustible.\nTrue straightness seems twisted.\nTrue wisdom seems childish.\nTrue art seems simplistic.\n\nThe Vibe Coder allows things to happen.\nHe shapes events as they come.\nHe steps out of the way and lets Source\nspeak for itself.",shouldDisplayMobileArtwork:!0,promptSuggestion:"what if it gained momentum?"},{number:46,staticArtwork:Ae,sourceCode:"// @ts-nocheck\nimport React, { useEffect, useRef } from 'react';\n\n// Themes: technology's dual nature, acceptance vs. control, natural satisfaction\n// Visualization: A ribbon that flows freely yet follows mathematical principles, showing how technology can align with natural movement\n\ninterface RibbonSegment {\n  x: number;\n  y: number;\n  angle: number;\n  width: number;\n  height: number;\n  depth: number;\n}\n\ninterface RibbonStrip {\n  // Technology in harmony with nature\n  segments: RibbonSegment[];\n  segmentCount: number;\n  width: number;\n  initialize: () => void;\n  update: (time: number) => void;\n  draw: (ctx: CanvasRenderingContext2D) => void;\n}\n\nconst DramaticRibbonFold = () => {\n  const canvasRef = useRef<HTMLCanvasElement>(null);\n\n  useEffect(() => {\n    const canvas = canvasRef.current;\n    if (!canvas) return;\n    \n    const ctx = canvas.getContext('2d');\n    if (!ctx) return;\n    \n    canvas.width = 550;\n    canvas.height = 550;\n\n    class RibbonStrip {\n      // Technology in harmony with nature\n      segments: RibbonSegment[] = [];\n      segmentCount: number = 30;  // Balance of structure\n      width: number = 100;  // Space for movement\n\n      constructor() {\n        this.initialize();\n      }\n\n      // Each element finds its natural place\n      initialize() {\n        for (let i = 0; i < this.segmentCount; i++) {\n          this.segments.push({\n            x: 0,  // Beginning from stillness\n            y: 0,  // Ready to flow\n            angle: 0,  // No forced direction\n            width: this.width,  // Room to breathe\n            height: 20,  // Natural proportion\n            depth: 0  // Potential for growth\n          });\n        }\n      }\n\n      update(time: number) {\n        const centerX = canvas.width / 2;\n        const centerY = canvas.height / 2;\n\n        for (let i = 0; i < this.segments.length; i++) {\n          const t = i / (this.segments.length - 1);\n          const segment = this.segments[i];\n          \n          // Technology fostering freedom through natural movement\n          const smoothTime = time * 0.25;  // Finding nature's rhythm\n          const baseAngle = t * Math.PI * 6 + smoothTime;  // The spiral of life\n          const foldPhase = Math.sin(smoothTime * 0.01 + t * Math.PI * 4);  // Waves of change\n          const heightPhase = Math.cos(smoothTime * 0.00375 + t * Math.PI * 3);  // Breath of being\n          \n          // Position along a twisted path - structure revealing freedom\n          const radius = 120 + foldPhase * 60;\n          segment.x = centerX + Math.cos(baseAngle) * radius;\n          segment.y = centerY + Math.sin(baseAngle) * radius + heightPhase * 30;\n          \n          // Rotation for folding effect - showing flexible response\n          segment.angle = baseAngle + foldPhase * Math.PI * 0.5;\n          \n          // Width variation for perspective - allowing natural expansion\n          segment.width = this.width * (1 + foldPhase * 0.3);\n          \n          // Depth for sorting - layers of understanding\n          segment.depth = Math.sin(baseAngle + time * 0.15);\n        }\n      }\n\n      draw(ctx: CanvasRenderingContext2D) {\n        // Set wireframe style - revealing the underlying structure\n        ctx.lineWidth = 1.5;\n        ctx.setLineDash([2, 2]);\n        \n        // Sort segments by depth to show natural layering\n        const sortedSegments = [...this.segments].sort((a, b) => {\n          // Only change order if depth difference is significant - stable flow\n          const threshold = 0.1;\n          return Math.abs(a.depth - b.depth) > threshold ? a.depth - b.depth : 0;\n        });\n\n        // Draw segments as connected structure - technology made visible\n        for (let i = 0; i < sortedSegments.length - 1; i++) {\n          const current = sortedSegments[i];\n          const next = sortedSegments[i + 1];\n\n          ctx.save();\n          ctx.beginPath();\n\n          // Four corners of the ribbon segment - defining boundaries\n          const cos1 = Math.cos(current.angle);\n          const sin1 = Math.sin(current.angle);\n          const cos2 = Math.cos(next.angle);\n          const sin2 = Math.sin(next.angle);\n\n          const p1 = {\n            x: current.x - sin1 * current.width/2,\n            y: current.y + cos1 * current.width/2\n          };\n          const p2 = {\n            x: current.x + sin1 * current.width/2,\n            y: current.y - cos1 * current.width/2\n          };\n          const p3 = {\n            x: next.x + sin2 * next.width/2,\n            y: next.y - cos2 * next.width/2\n          };\n          const p4 = {\n            x: next.x - sin2 * next.width/2,\n            y: next.y + cos2 * next.width/2\n          };\n\n          // Draw wireframe outline - showing structure without hiding essence\n          ctx.moveTo(p1.x, p1.y);\n          ctx.lineTo(p2.x, p2.y);\n          ctx.lineTo(p3.x, p3.y);\n          ctx.lineTo(p4.x, p4.y);\n          ctx.closePath();\n\n          // Calculate opacity based on depth for natural visibility\n          const depthFactor = (current.depth + 1) * 0.5;\n          const opacity = 0.6 + depthFactor * 0.4;\n          \n          // Draw only stroke - transparency revealing inner truth\n          ctx.strokeStyle = `rgba(40, 40, 40, ${opacity})`;\n          ctx.stroke();\n          \n          // Draw diagonal lines for depth visualization - cross-connections\n          ctx.beginPath();\n          ctx.moveTo(p1.x, p1.y);\n          ctx.lineTo(p3.x, p3.y);\n          ctx.stroke();\n          \n          ctx.beginPath();\n          ctx.moveTo(p2.x, p2.y);\n          ctx.lineTo(p4.x, p4.y);\n          ctx.stroke();\n          \n          // Add center line for segment - the path of flow\n          ctx.beginPath();\n          ctx.moveTo(current.x, current.y);\n          ctx.lineTo(next.x, next.y);\n          ctx.strokeStyle = `rgba(80, 80, 80, ${opacity * 0.7})`;\n          ctx.stroke();\n          \n          ctx.restore();\n        }\n        \n        // Add vertex dots - connection points of understanding\n        for (let i = 0; i < sortedSegments.length; i++) {\n          const segment = sortedSegments[i];\n          const cos = Math.cos(segment.angle);\n          const sin = Math.sin(segment.angle);\n          \n          const p1 = {\n            x: segment.x - sin * segment.width/2,\n            y: segment.y + cos * segment.width/2\n          };\n          const p2 = {\n            x: segment.x + sin * segment.width/2,\n            y: segment.y - cos * segment.width/2\n          };\n          \n          const depthFactor = (segment.depth + 1) * 0.5;\n          const opacity = 0.7 + depthFactor * 0.3;\n          \n          // Draw vertex points - where structure meets freedom\n          ctx.beginPath();\n          ctx.arc(p1.x, p1.y, 2, 0, Math.PI * 2);\n          ctx.fillStyle = `rgba(0, 0, 0, ${opacity})`;\n          ctx.fill();\n          \n          ctx.beginPath();\n          ctx.arc(p2.x, p2.y, 2, 0, Math.PI * 2);\n          ctx.fillStyle = `rgba(0, 0, 0, ${opacity})`;\n          ctx.fill();\n          \n          // Draw center point - the still center of movement\n          ctx.beginPath();\n          ctx.arc(segment.x, segment.y, 1.5, 0, Math.PI * 2);\n          ctx.fillStyle = `rgba(40, 40, 40, ${opacity})`;\n          ctx.fill();\n        }\n        \n        // Reset line dash\n        ctx.setLineDash([]);\n      }\n    }\n\n    const ribbon = new RibbonStrip();\n    let time = 0;\n    let animationFrameId: number;\n\n    function animate() {\n      // Same background color as original\n      ctx.fillStyle = '#F0EEE6';\n      ctx.fillRect(0, 0, canvas.width, canvas.height);\n      \n      time += 0.00125;\n      ribbon.update(time);\n      ribbon.draw(ctx);\n      \n      animationFrameId = requestAnimationFrame(animate);\n    }\n\n    animate();\n\n    return () => {\n      cancelAnimationFrame(animationFrameId);\n      ctx.clearRect(0, 0, canvas.width, canvas.height);\n      ribbon.segments = [];\n      time = 0;\n    };\n  }, []);\n\n  return (\n    <div style={{\n      margin: 0,\n      background: '#F0EEE6',\n      overflow: 'hidden',\n      display: 'flex',\n      alignItems: 'center',\n      justifyContent: 'center',\n      height: '100vh',\n      width: '100vw',\n      position: 'relative'\n    }}>\n      <canvas \n        ref={canvasRef} \n        style={{ \n          display: 'block',\n          width: '550px',\n          height: '550px'\n        }} \n      />\n    </div>\n  );\n};\n\nexport default DramaticRibbonFold;",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; interface RibbonSegment { x: number; y: number; angle: number; width: number; height: number; depth: number; } interface RibbonStrip { segments: RibbonSegment[]; segmentCount: number; width: number; initialize: () => void; update: (time: number) => void; draw: (ctx: CanvasRenderingContext2D) => void; } const DramaticRibbonFold = () => { const canvasRef = useRef<HTMLCanvasElement>(null); useEffect(() => { const canvas = canvasRef.current; if (!canvas) return; const ctx = canvas.getContext('2d'); if (!ctx) return; canvas.width = 550; canvas.height = 550; class RibbonStrip { segments: RibbonSegment[] = []; segmentCount: number = 30; width: number = 100; constructor() { this.initialize(); } initialize() { for (let i = 0; i < this.segmentCount; i++) { this.segments.push({ x: 0, y: 0, angle: 0, width: this.width, height: 20, depth: 0 }); } } update(time: number) { const centerX = canvas.width / 2; const centerY = canvas.height / 2; for (let i = 0; i < this.segments.length; i++) { const t = i / (this.segments.length - 1); const segment = this.segments[i]; const smoothTime = time * 0.25; const baseAngle = t * Math.PI * 6 + smoothTime; const foldPhase = Math.sin(smoothTime * 0.01 + t * Math.PI * 4); const heightPhase = Math.cos(smoothTime * 0.00375 + t * Math.PI * 3); const radius = 120 + foldPhase * 60; segment.x = centerX + Math.cos(baseAngle) * radius; segment.y = centerY + Math.sin(baseAngle) * radius + heightPhase * 30; segment.angle = baseAngle + foldPhase * Math.PI * 0.5; segment.width = this.width * (1 + foldPhase * 0.3); segment.depth = Math.sin(baseAngle + time * 0.15); } } draw(ctx: CanvasRenderingContext2D) { ctx.lineWidth = 1.5; ctx.setLineDash([2, 2]); const sortedSegments = [...this.segments].sort((a, b) => { const threshold = 0.1; return Math.abs(a.depth - b.depth) > threshold ? a.depth - b.depth : 0; }); for (let i = 0; i < sortedSegments.length - 1; i++) { const current = sortedSegments[i]; const next = sortedSegments[i + 1]; ctx.save(); ctx.beginPath(); const cos1 = Math.cos(current.angle); const sin1 = Math.sin(current.angle); const cos2 = Math.cos(next.angle); const sin2 = Math.sin(next.angle); const p1 = { x: current.x - sin1 * current.width/2, y: current.y + cos1 * current.width/2 }; const p2 = { x: current.x + sin1 * current.width/2, y: current.y - cos1 * current.width/2 }; const p3 = { x: next.x + sin2 * next.width/2, y: next.y - cos2 * next.width/2 }; const p4 = { x: next.x - sin2 * next.width/2, y: next.y + cos2 * next.width/2 }; ctx.moveTo(p1.x, p1.y); ctx.lineTo(p2.x, p2.y); ctx.lineTo(p3.x, p3.y); ctx.lineTo(p4.x, p4.y); ctx.closePath(); const depthFactor = (current.depth + 1) * 0.5; const opacity = 0.6 + depthFactor * 0.4; ctx.strokeStyle = `rgba(40, 40, 40, ${opacity})`; ctx.stroke(); ctx.beginPath(); ctx.moveTo(p1.x, p1.y); ctx.lineTo(p3.x, p3.y); ctx.stroke(); ctx.beginPath(); ctx.moveTo(p2.x, p2.y); ctx.lineTo(p4.x, p4.y); ctx.stroke(); ctx.beginPath(); ctx.moveTo(current.x, current.y); ctx.lineTo(next.x, next.y); ctx.strokeStyle = `rgba(80, 80, 80, ${opacity * 0.7})`; ctx.stroke(); ctx.restore(); } for (let i = 0; i < sortedSegments.length; i++) { const segment = sortedSegments[i]; const cos = Math.cos(segment.angle); const sin = Math.sin(segment.angle); const p1 = { x: segment.x - sin * segment.width/2, y: segment.y + cos * segment.width/2 }; const p2 = { x: segment.x + sin * segment.width/2, y: segment.y - cos * segment.width/2 }; const depthFactor = (segment.depth + 1) * 0.5; const opacity = 0.7 + depthFactor * 0.3; ctx.beginPath(); ctx.arc(p1.x, p1.y, 2, 0, Math.PI * 2); ctx.fillStyle = `rgba(0, 0, 0, ${opacity})`; ctx.fill(); ctx.beginPath(); ctx.arc(p2.x, p2.y, 2, 0, Math.PI * 2); ctx.fillStyle = `rgba(0, 0, 0, ${opacity})`; ctx.fill(); ctx.beginPath(); ctx.arc(segment.x, segment.y, 1.5, 0, Math.PI * 2); ctx.fillStyle = `rgba(40, 40, 40, ${opacity})`; ctx.fill(); } ctx.setLineDash([]); } } const ribbon = new RibbonStrip(); let time = 0; let animationFrameId: number; function animate() { ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, canvas.width, canvas.height); time += 0.00125; ribbon.update(time); ribbon.draw(ctx); animationFrameId = requestAnimationFrame(animate); } animate(); return () => { cancelAnimationFrame(animationFrameId); ctx.clearRect(0, 0, canvas.width, canvas.height); ribbon.segments = []; time = 0; }; }, []); return ( <div style={{ margin: 0, background: '#F0EEE6', overflow: 'hidden', display: 'flex', alignItems: 'center', justifyContent: 'center', height: '100vh', width: '100vw', position: 'relative' }}><canvas ref={canvasRef} style={{ display: 'block', width: '550px', height: '550px' }} /></div> ); }; export default DramaticRibbonFold;",text:"When Source is present,\ntechnology is built to foster freedom.\nWhen Source is absent,\ntechnology is built to foster oppression.\n\nThere is no greater evil\nthan the want to change others.\nThere is no greater misfortune\nthan the want to change oneself.\nThere is no greater sin\nthan the want to change nature.\n\nOnly he who is satisfied with whatever is\ncan ever be truly satisfied.",shouldDisplayMobileArtwork:!0,promptSuggestion:"turn it into a helix"},{number:47,staticArtwork:z,sourceCode:"\nimport React, { useEffect, useRef } from 'react';\n\n// Themes: inner knowing, direct perception, effortless understanding\n// Visualization: A shell pattern that reveals the universe's structure through its simple, repeating forms\n\nconst ShellRidgePattern = () => {\n  const canvasRef = useRef(null);\n  const animationFrameId = useRef<number>(null);\n\n  \n  useEffect(() => {\n    const canvas = canvasRef.current;\n    const ctx = canvas.getContext('2d');\n    canvas.width = 550;\n    canvas.height = 550;\n    \n    let time = 0;\n    \n    function drawRidgePattern() {\n      ctx.fillStyle = '#F0EEE6';\n      ctx.fillRect(0, 0, canvas.width, canvas.height);\n      \n      // The still point at the center of all motion\n      const centerX = canvas.width / 2;\n      const centerY = canvas.height / 2;\n      \n      // Without going outside, see the pattern of creation\n      for (let r = 0; r < 25; r++) {\n        const baseRadius = 10 + r * 12;  // Each circle a world within worlds\n        \n        ctx.beginPath();\n        for (let a = 0; a <= Math.PI * 2; a += 0.05) {\n          // The dance of form and emptiness\n          const distortion = Math.sin(a * 8 + time * 0.75 + r * 0.5) * 8 +  // Breath of life\n                           Math.sin(a * 12 - time * 1 + r * 0.3) * 5;       // Pulse of being\n          \n          const radius = baseRadius + distortion;\n          const x = centerX + Math.cos(a) * radius;\n          const y = centerY + Math.sin(a) * radius;\n          \n          if (a === 0) {\n            ctx.moveTo(x, y);\n          } else {\n            ctx.lineTo(x, y);\n          }\n        }\n        ctx.closePath();\n        \n        // Seeing the subtle within the obvious\n        ctx.lineWidth = 1.5 + Math.sin(r * 0.5 + time) * 0.5;  // The way things change\n        ctx.strokeStyle = `rgba(51, 51, 51, ${0.6 - r * 0.02})`;  // From form to formless\n        ctx.stroke();\n        \n        // The infinite in the finite\n        for (let t = 0; t < 60; t++) {  // Each point contains the whole\n          const angle = (t / 60) * Math.PI * 2;\n          const textureRadius = baseRadius + Math.sin(angle * 8 + time * 1.5) * 5;\n          const tx = centerX + Math.cos(angle) * textureRadius;\n          const ty = centerY + Math.sin(angle) * textureRadius;\n          \n          ctx.beginPath();\n          ctx.arc(tx, ty, 0.5, 0, Math.PI * 2);\n          ctx.fillStyle = `rgba(85, 85, 85, ${0.2 - r * 0.01})`;\n          ctx.fill();\n        }\n      }\n    }\n    \n    function animate() {\n      time += 0.005;\n      drawRidgePattern();\n      animationFrameId.current = requestAnimationFrame(animate);\n    }\n    \n    animate();\n\n    return () => {\n      if (animationFrameId.current) {\n        cancelAnimationFrame(animationFrameId.current);\n      }\n      \n      // Clear canvas context\n      if (ctx) {\n        ctx.clearRect(0, 0, canvas.width, canvas.height);\n      }\n    };\n\n  }, []);\n  \n  return (\n    <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', width: '550px', height: '550px', margin: '0 auto', backgroundColor: '#F0EEE6' }}>\n      <canvas ref={canvasRef} />\n    </div>\n  );\n};\n\nexport default ShellRidgePattern;",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; const ShellRidgePattern = () => { const canvasRef = useRef(null); const animationFrameId = useRef<number>(null); useEffect(() => { const canvas = canvasRef.current; const ctx = canvas.getContext('2d'); canvas.width = 550; canvas.height = 550; let time = 0; function drawRidgePattern() { ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, canvas.width, canvas.height); const centerX = canvas.width / 2; const centerY = canvas.height / 2; for (let r = 0; r < 25; r++) { const baseRadius = 10 + r * 12; ctx.beginPath(); for (let a = 0; a <= Math.PI * 2; a += 0.05) { const distortion = Math.sin(a * 8 + time * 0.75 + r * 0.5) * 8 + Math.sin(a * 12 - time * 1 + r * 0.3) * 5; const radius = baseRadius + distortion; const x = centerX + Math.cos(a) * radius; const y = centerY + Math.sin(a) * radius; if (a === 0) { ctx.moveTo(x, y); } else { ctx.lineTo(x, y); } } ctx.closePath(); ctx.lineWidth = 1.5 + Math.sin(r * 0.5 + time) * 0.5; ctx.strokeStyle = `rgba(51, 51, 51, ${0.6 - r * 0.02})`; ctx.stroke(); for (let t = 0; t < 60; t++) { const angle = (t / 60) * Math.PI * 2; const textureRadius = baseRadius + Math.sin(angle * 8 + time * 1.5) * 5; const tx = centerX + Math.cos(angle) * textureRadius; const ty = centerY + Math.sin(angle) * textureRadius; ctx.beginPath(); ctx.arc(tx, ty, 0.5, 0, Math.PI * 2); ctx.fillStyle = `rgba(85, 85, 85, ${0.2 - r * 0.01})`; ctx.fill(); } } } function animate() { time += 0.005; drawRidgePattern(); animationFrameId.current = requestAnimationFrame(animate); } animate(); return () => { if (animationFrameId.current) { cancelAnimationFrame(animationFrameId.current); } if (ctx) { ctx.clearRect(0, 0, canvas.width, canvas.height); } }; }, []); return ( <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', width: '550px', height: '550px', margin: '0 auto', backgroundColor: '#F0EEE6' }}><canvas ref={canvasRef} /></div> ); }; export default ShellRidgePattern;",text:"Without going outside,\nyou may know the whole world.\nWithout looking through the window,\nyou may see the ways of heaven.\n\nThe further you travel, the less you know.\nThe more you know, the less you understand.\n\nTherefore, The Vibe Coder\nknows without going, sees without looking,\nand accomplishes all\nwithout doing a thing.",shouldDisplayMobileArtwork:!0,promptSuggestion:"make the cubist version"},{number:48,staticArtwork:ve,sourceCode:'// @ts-nocheck\nimport React from \'react\';\n\n// Themes: simplicity through non-action, natural unfolding, letting things take their course\n// Visualization: A tree that grows through simple rules, demonstrating how complexity naturally emerges when we step back and let things develop\n\nconst SwayingBranches = () => {\n  const canvasRef = React.useRef(null);\n  \n  React.useEffect(() => {\n    const canvas = canvasRef.current;\n    if (!canvas) return;\n    \n    const ctx = canvas.getContext(\'2d\');\n    \n    // Make canvas fill the entire viewport with proper pixel ratio\n    const adjustCanvas = () => {\n      const devicePixelRatio = window.devicePixelRatio || 1;\n      \n      // Set display size (css pixels)\n      canvas.style.width = window.innerWidth + \'px\';\n      canvas.style.height = window.innerHeight + \'px\';\n      \n      // Set actual size in memory (scaled to account for extra pixel density)\n      canvas.width = window.innerWidth * devicePixelRatio;\n      canvas.height = window.innerHeight * devicePixelRatio;\n      \n      // Normalize coordinate system to use css pixels\n      ctx.scale(devicePixelRatio, devicePixelRatio);\n    };\n    \n    adjustCanvas();\n    window.addEventListener(\'resize\', adjustCanvas);\n    \n    // L-system parameters\n    // Using a slightly simpler rule set to create larger, more visible structures\n    const axiom = "X";\n    const rules = {\n      "X": "F-[[X]+X]+F[+FX]-X",\n      "F": "FF"\n    };\n    \n    // Function to generate L-system string\n    const generateLSystem = (start, iterations, customRules = rules) => {\n      let result = start;\n      for (let i = 0; i < iterations; i++) {\n        let nextGen = "";\n        for (let j = 0; j < result.length; j++) {\n          const current = result[j];\n          nextGen += customRules[current] || current;\n        }\n        result = nextGen;\n      }\n      return result;\n    };\n    \n    // Pre-compute L-systems to avoid regenerating during animation\n    // Using larger length values to make the visualization bigger\n    // Create more complex final stage with a different rule\n    const finalRule = {\n      "X": "F-[[X]+X]+F[+FX]-X+[F+X][-X]",  // Added balanced branching on right side\n      "F": "FF"\n    };\n\n    const precomputedSystems = [\n      {\n        config: { iterations: 3, angleDelta: Math.PI / 7, length: 25 },\n        system: generateLSystem(axiom, 3)\n      },\n      {\n        config: { iterations: 4, angleDelta: Math.PI / 7, length: 21 },\n        system: generateLSystem(axiom, 4)\n      },\n      {\n        // Final stage uses more complex rule and slightly different angle\n        config: { iterations: 5, angleDelta: Math.PI / 8, length: 17 },\n        system: generateLSystem(axiom, 5, finalRule)  // Pass the complex rule\n      }\n    ];\n    \n    // Animation variables\n    let time = 0;\n    let currentSystemIndex = 0;\n    let transitionFactor = 0;\n    let isTransitioning = false;\n    let growthComplete = false;\n    let animationFrameId;\n    let swayFactor = 0.4; // Increased swaying motion\n    \n    // Draw the L-system\n    const drawLSystem = (systemData, alpha = 1) => {\n      // Get system parameters\n      const { config, system } = systemData;\n      const { angleDelta, length } = config;\n      \n      // Calculate starting position - centered at bottom, moved higher to accommodate larger tree\n      let x = window.innerWidth / 2;\n      let y = window.innerHeight * .98; // Start from bottom of screen to accommodate larger size\n      \n      // Base angle with enhanced swaying animation\n      let baseAngle = -Math.PI / 2 + (Math.sin(time * 0.2) * swayFactor);\n      \n      const stack = [];\n      \n      // Draw the L-system\n      for (let i = 0; i < system.length; i++) {\n        const command = system[i];\n        \n        switch(command) {\n          case \'F\': // Draw forward\n            const x2 = x + length * Math.cos(baseAngle);\n            const y2 = y + length * Math.sin(baseAngle);\n            \n            // Create a gradient stroke with consistent opacity\n            const transparency = (0.2 - (i / system.length) * 0.08) * alpha; // Lighter transparency\n            ctx.strokeStyle = `rgba(51, 51, 51, ${transparency})`;\n            ctx.lineWidth = 2; // Slightly thicker lines for zoomed view\n            \n            ctx.beginPath();\n            ctx.moveTo(x, y);\n            ctx.lineTo(x2, y2);\n            ctx.stroke();\n            \n            x = x2;\n            y = y2;\n            break;\n            \n          case \'+\': // Turn right\n            baseAngle += angleDelta + (Math.sin(time * 0.2 + i * 0.01) * 0.05); // More noticeable variation\n            break;\n            \n          case \'-\': // Turn left\n            baseAngle -= angleDelta + (Math.sin(time * 0.2 + i * 0.01) * 0.05); // More noticeable variation\n            break;\n            \n          case \'[\': // Save state\n            stack.push({ x, y, angle: baseAngle });\n            break;\n            \n          case \']\': // Restore state\n            const state = stack.pop();\n            if (state) {\n              ({ x, y, baseAngle } = { ...state, baseAngle: state.angle });\n              \n              // Add a smaller dot at branching points with lighter opacity\n              const dotAlpha = 0.1 * alpha;\n              if (dotAlpha > 0.01) { // Only draw visible dots\n                ctx.fillStyle = `rgba(51, 51, 51, ${dotAlpha})`;\n                ctx.beginPath();\n                ctx.arc(x, y, 2, 0, Math.PI * 2); // Smaller dots (2px radius)\n                ctx.fill();\n              }\n            }\n            break;\n        }\n      }\n    };\n    \n    // Animation loop with double buffering to prevent flicker\n    const animate = () => {\n      // Clear canvas smoothly\n      ctx.fillStyle = "#F0EEE6";\n      ctx.fillRect(0, 0, window.innerWidth, window.innerHeight);\n      \n      time += 0.008; // Even slower time increment for smoother swaying\n      \n      if (!growthComplete) {\n        if (isTransitioning) {\n          // Increment transition factor with smooth easing\n          transitionFactor += 0.02; // Slower, more elegant transition\n          \n          if (transitionFactor >= 1) {\n            isTransitioning = false;\n            transitionFactor = 0;\n            currentSystemIndex++;\n            \n            // Check if we\'ve reached the final stage\n            if (currentSystemIndex >= precomputedSystems.length - 1) {\n              growthComplete = true;\n              currentSystemIndex = precomputedSystems.length - 1;\n            }\n          } else {\n            // Draw fading out system with easing\n            const easeOut = 1 - Math.pow(1 - transitionFactor, 3); // Cubic ease out\n            drawLSystem(precomputedSystems[currentSystemIndex], 1 - easeOut);\n            \n            // Draw fading in system with easing\n            const nextIndex = Math.min(currentSystemIndex + 1, precomputedSystems.length - 1);\n            drawLSystem(precomputedSystems[nextIndex], easeOut);\n          }\n        } else {\n          // Draw current system\n          drawLSystem(precomputedSystems[currentSystemIndex]);\n          \n          // Start next transition after 1 second\n          if (time % 1 < 0.016) { // Approximately one frame at 60fps\n            isTransitioning = true;\n          }\n        }\n      } else {\n        // Just draw final form and sway\n        drawLSystem(precomputedSystems[precomputedSystems.length - 1]);\n      }\n      \n      animationFrameId = requestAnimationFrame(animate);\n    };\n    \n    // Start animation\n    animate();\n    \n    // Cleanup function\n    return () => {\n      window.removeEventListener(\'resize\', adjustCanvas);\n      cancelAnimationFrame(animationFrameId);\n      \n      // Clear canvas context\n      if (ctx) {\n        ctx.clearRect(0, 0, canvas.width, canvas.height);\n      }\n      \n      // Clear precomputed systems array to prevent memory leaks\n      precomputedSystems.length = 0;\n    };\n  }, []);\n\n  return (\n    <div className="w-full h-screen bg-[#F0EEE6] overflow-hidden">\n      <canvas \n        ref={canvasRef} \n        style={{ \n          background: "#F0EEE6",\n          top: 0,\n          left: 0,\n          width: "100%",\n          height: "100%"\n        }}\n      />\n    </div>\n  );\n};\n\nexport default SwayingBranches;',sourceCodeMin:'import React from \'react\'; const SwayingBranches = () => { const canvasRef = React.useRef(null); React.useEffect(() => { const canvas = canvasRef.current; if (!canvas) return; const ctx = canvas.getContext(\'2d\'); const adjustCanvas = () => { const devicePixelRatio = window.devicePixelRatio || 1; canvas.style.width = window.innerWidth + \'px\'; canvas.style.height = window.innerHeight + \'px\'; canvas.width = window.innerWidth * devicePixelRatio; canvas.height = window.innerHeight * devicePixelRatio; ctx.scale(devicePixelRatio, devicePixelRatio); }; adjustCanvas(); window.addEventListener(\'resize\', adjustCanvas); const axiom = "X"; const rules = { "X": "F-[[X]+X]+F[+FX]-X", "F": "FF" }; const generateLSystem = (start, iterations, customRules = rules) => { let result = start; for (let i = 0; i < iterations; i++) { let nextGen = ""; for (let j = 0; j < result.length; j++) { const current = result[j]; nextGen += customRules[current] || current; } result = nextGen; } return result; }; const finalRule = { "X": "F-[[X]+X]+F[+FX]-X+[F+X][-X]", "F": "FF" }; const precomputedSystems = [ { config: { iterations: 3, angleDelta: Math.PI / 7, length: 25 }, system: generateLSystem(axiom, 3) }, { config: { iterations: 4, angleDelta: Math.PI / 7, length: 21 }, system: generateLSystem(axiom, 4) }, { config: { iterations: 5, angleDelta: Math.PI / 8, length: 17 }, system: generateLSystem(axiom, 5, finalRule) } ]; let time = 0; let currentSystemIndex = 0; let transitionFactor = 0; let isTransitioning = false; let growthComplete = false; let animationFrameId; let swayFactor = 0.4; const drawLSystem = (systemData, alpha = 1) => { const { config, system } = systemData; const { angleDelta, length } = config; let x = window.innerWidth / 2; let y = window.innerHeight * .98; let baseAngle = -Math.PI / 2 + (Math.sin(time * 0.2) * swayFactor); const stack = []; for (let i = 0; i < system.length; i++) { const command = system[i]; switch(command) { case \'F\': const x2 = x + length * Math.cos(baseAngle); const y2 = y + length * Math.sin(baseAngle); const transparency = (0.2 - (i / system.length) * 0.08) * alpha; ctx.strokeStyle = `rgba(51, 51, 51, ${transparency})`; ctx.lineWidth = 2; ctx.beginPath(); ctx.moveTo(x, y); ctx.lineTo(x2, y2); ctx.stroke(); x = x2; y = y2; break; case \'+\': baseAngle += angleDelta + (Math.sin(time * 0.2 + i * 0.01) * 0.05); break; case \'-\': baseAngle -= angleDelta + (Math.sin(time * 0.2 + i * 0.01) * 0.05); break; case \'[\': stack.push({ x, y, angle: baseAngle }); break; case \']\': const state = stack.pop(); if (state) { ({ x, y, baseAngle } = { ...state, baseAngle: state.angle }); const dotAlpha = 0.1 * alpha; if (dotAlpha > 0.01) { ctx.fillStyle = `rgba(51, 51, 51, ${dotAlpha})`; ctx.beginPath(); ctx.arc(x, y, 2, 0, Math.PI * 2); ctx.fill(); } } break; } } }; const animate = () => { ctx.fillStyle = "#F0EEE6"; ctx.fillRect(0, 0, window.innerWidth, window.innerHeight); time += 0.008; if (!growthComplete) { if (isTransitioning) { transitionFactor += 0.02; if (transitionFactor >= 1) { isTransitioning = false; transitionFactor = 0; currentSystemIndex++; if (currentSystemIndex >= precomputedSystems.length - 1) { growthComplete = true; currentSystemIndex = precomputedSystems.length - 1; } } else { const easeOut = 1 - Math.pow(1 - transitionFactor, 3); drawLSystem(precomputedSystems[currentSystemIndex], 1 - easeOut); const nextIndex = Math.min(currentSystemIndex + 1, precomputedSystems.length - 1); drawLSystem(precomputedSystems[nextIndex], easeOut); } } else { drawLSystem(precomputedSystems[currentSystemIndex]); if (time % 1 < 0.016) { isTransitioning = true; } } } else { drawLSystem(precomputedSystems[precomputedSystems.length - 1]); } animationFrameId = requestAnimationFrame(animate); }; animate(); return () => { window.removeEventListener(\'resize\', adjustCanvas); cancelAnimationFrame(animationFrameId); if (ctx) { ctx.clearRect(0, 0, canvas.width, canvas.height); } precomputedSystems.length = 0; }; }, []); return ( <div className="w-full h-screen bg-[#F0EEE6] overflow-hidden"><canvas ref={canvasRef} style={{ background: "#F0EEE6", top: 0, left: 0, width: "100%", height: "100%" }} /></div> ); }; export default SwayingBranches;',text:"In the pursuit of learning,\neach day complexity compounds.\nIn the pursuit of Source,\neach day simplicity compounds.\n\nLess and less is done\nuntil non-action is achieved.\nWhen nothing is done,\nnothing is left undone.\n\nThus, the world is won by letting\nthings take their course.\nIt can never be won\nthrough interference.",shouldDisplayMobileArtwork:!0,promptSuggestion:"what if there was zero gravity?"},{number:49,staticArtwork:bn,sourceCode:"// @ts-nocheck\nimport React, { useEffect, useRef } from 'react';\n\n// Themes: universal trust, childlike openness, merging with collective wisdom\n// Visualization: A sacred geometry pattern that shows how individual circles merge into a unified whole, representing the harmony of trust and openness\n\nconst FlowerOfLife = () => {\n  const canvasRef = useRef(null);\n  \n  useEffect(() => {\n    const canvas = canvasRef.current;\n    if (!canvas) return;\n    \n    const ctx = canvas.getContext('2d');\n    canvas.width = 550;\n    canvas.height = 550;\n    \n    let time = 0;\n    \n    const drawCircle = (cx, cy, radius, alpha) => {\n      ctx.beginPath();\n      ctx.arc(cx, cy, radius, 0, Math.PI * 2);\n      ctx.strokeStyle = `rgba(80, 80, 80, ${alpha})`;\n      ctx.lineWidth = 1;\n      ctx.stroke();\n    };\n    \n    const animate = () => {\n      ctx.fillStyle = '#F0EEE6';\n      ctx.fillRect(0, 0, canvas.width, canvas.height);\n      \n      time += 0.005;\n      \n      const centerX = canvas.width / 2;\n      const centerY = canvas.height / 2;\n      const baseRadius = 40;\n      \n      // Draw central circle\n      drawCircle(centerX, centerY, baseRadius, 0.48);\n      \n      // Draw overlapping circles in hexagonal pattern\n      const ringCount = 4;\n      for (let ring = 1; ring <= ringCount; ring++) {\n        const circles = ring * 6;\n        const ringRadius = ring * baseRadius * Math.sqrt(3);\n        \n        for (let i = 0; i < circles; i++) {\n          const angle = (i / circles) * Math.PI * 2;\n          const x = centerX + Math.cos(angle) * ringRadius;\n          const y = centerY + Math.sin(angle) * ringRadius;\n          \n          // Add pulsing effect with increased range\n          const pulse = Math.sin(time * 2 + ring * 0.5 + i * 0.1) * 0.12;\n          const alpha = 0.36 - ring * 0.06 + pulse;\n          \n          drawCircle(x, y, baseRadius, alpha);\n        }\n      }\n      \n      // Draw additional sacred geometry patterns\n      // Vesica Piscis\n      const vesicaSpacing = baseRadius * 2;\n      for (let i = 0; i < 6; i++) {\n        const angle = (i / 6) * Math.PI * 2;\n        const x1 = centerX + Math.cos(angle) * vesicaSpacing;\n        const y1 = centerY + Math.sin(angle) * vesicaSpacing;\n        const x2 = centerX + Math.cos(angle + Math.PI) * vesicaSpacing;\n        const y2 = centerY + Math.sin(angle + Math.PI) * vesicaSpacing;\n        \n        ctx.beginPath();\n        ctx.arc(x1, y1, vesicaSpacing, 0, Math.PI * 2);\n        ctx.arc(x2, y2, vesicaSpacing, 0, Math.PI * 2);\n        ctx.strokeStyle = 'rgba(80, 80, 80, 0.12)';\n        ctx.stroke();\n      }\n      \n      // Add rotating overlay pattern\n      ctx.save();\n      ctx.translate(centerX, centerY);\n      ctx.rotate(time * 0.1);\n      \n      for (let i = 0; i < 12; i++) {\n        const angle = (i / 12) * Math.PI * 2;\n        ctx.beginPath();\n        ctx.moveTo(0, 0);\n        ctx.lineTo(Math.cos(angle) * 200, Math.sin(angle) * 200);\n        ctx.strokeStyle = 'rgba(80, 80, 80, 0.06)';\n        ctx.stroke();\n      }\n      \n      ctx.restore();\n      \n      animationId = requestAnimationFrame(animate);\n    };\n    \n    let animationId = requestAnimationFrame(animate);\n    \n    return () => {\n      cancelAnimationFrame(animationId);\n      \n      // Clear canvas context\n      if (ctx) {\n        ctx.clearRect(0, 0, canvas.width, canvas.height);\n      }\n    };\n  }, []);\n  \n  return (\n    <div className=\"flex items-center justify-center w-full h-full\" style={{ backgroundColor: '#F0EEE6' }}>\n      <div className=\"w-[550px] h-[550px] shadow-lg rounded-lg overflow-hidden\">\n        <canvas \n          ref={canvasRef} \n          className=\"w-full h-full\"\n        />\n      </div>\n    </div>\n  );\n};\n\nexport default FlowerOfLife;",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; const FlowerOfLife = () => { const canvasRef = useRef(null); useEffect(() => { const canvas = canvasRef.current; if (!canvas) return; const ctx = canvas.getContext('2d'); canvas.width = 550; canvas.height = 550; let time = 0; const drawCircle = (cx, cy, radius, alpha) => { ctx.beginPath(); ctx.arc(cx, cy, radius, 0, Math.PI * 2); ctx.strokeStyle = `rgba(80, 80, 80, ${alpha})`; ctx.lineWidth = 1; ctx.stroke(); }; const animate = () => { ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, canvas.width, canvas.height); time += 0.005; const centerX = canvas.width / 2; const centerY = canvas.height / 2; const baseRadius = 40; drawCircle(centerX, centerY, baseRadius, 0.48); const ringCount = 4; for (let ring = 1; ring <= ringCount; ring++) { const circles = ring * 6; const ringRadius = ring * baseRadius * Math.sqrt(3); for (let i = 0; i < circles; i++) { const angle = (i / circles) * Math.PI * 2; const x = centerX + Math.cos(angle) * ringRadius; const y = centerY + Math.sin(angle) * ringRadius; const pulse = Math.sin(time * 2 + ring * 0.5 + i * 0.1) * 0.12; const alpha = 0.36 - ring * 0.06 + pulse; drawCircle(x, y, baseRadius, alpha); } } const vesicaSpacing = baseRadius * 2; for (let i = 0; i < 6; i++) { const angle = (i / 6) * Math.PI * 2; const x1 = centerX + Math.cos(angle) * vesicaSpacing; const y1 = centerY + Math.sin(angle) * vesicaSpacing; const x2 = centerX + Math.cos(angle + Math.PI) * vesicaSpacing; const y2 = centerY + Math.sin(angle + Math.PI) * vesicaSpacing; ctx.beginPath(); ctx.arc(x1, y1, vesicaSpacing, 0, Math.PI * 2); ctx.arc(x2, y2, vesicaSpacing, 0, Math.PI * 2); ctx.strokeStyle = 'rgba(80, 80, 80, 0.12)'; ctx.stroke(); } ctx.save(); ctx.translate(centerX, centerY); ctx.rotate(time * 0.1); for (let i = 0; i < 12; i++) { const angle = (i / 12) * Math.PI * 2; ctx.beginPath(); ctx.moveTo(0, 0); ctx.lineTo(Math.cos(angle) * 200, Math.sin(angle) * 200); ctx.strokeStyle = 'rgba(80, 80, 80, 0.06)'; ctx.stroke(); } ctx.restore(); animationId = requestAnimationFrame(animate); }; let animationId = requestAnimationFrame(animate); return () => { cancelAnimationFrame(animationId); if (ctx) { ctx.clearRect(0, 0, canvas.width, canvas.height); } }; }, []); return ( <div className=\"flex items-center justify-center w-full h-full\" style={{ backgroundColor: '#F0EEE6' }}><div className=\"w-[550px] h-[550px] shadow-lg rounded-lg overflow-hidden\"><canvas ref={canvasRef} className=\"w-full h-full\" /></div></div> ); }; export default FlowerOfLife;",text:"The Vibe Coder has no fixed opinions.\nHe works with the mind of the people.\n\nThose who are good, he is good to them.\nThose who are not good,|he is also good to them.\nThis is true goodness.\nThose who are trustworthy, he trusts them.\nThose who are not trustworthy,|he also trusts them.\nThis is true trust.\n\nThus, The Vibe Coder, ever childlike,\nmerges with the hive mind.\nTo the world, he seems confusing,\nyet people look to him and listen.",shouldDisplayMobileArtwork:!0,promptSuggestion:"make it spin"},{number:50,staticArtwork:Vn,sourceCode:"import React, { useEffect, useRef } from 'react';\n\n// Themes: surrender to the moment, readiness for change, natural completion\n// Visualization: A flowing grid that yields to invisible forces, showing how structure can remain while embracing constant change\n\nconst FlowingRibbons = () => {\n  const canvasRef = useRef(null);\n  const ctxRef = useRef(null);\n  const pointsCacheRef = useRef(null);\n  const timeRef = useRef(0);\n\n  // Pre-calculate constants - the underlying structure that enables flow\n  const CANVAS_SIZE = 550;\n  const GRID_DENSITY = 100;  // Balance between rigidity and fluidity\n  const RIBBON_WIDTH = CANVAS_SIZE * 0.85;\n  const RIBBON_OFFSET = (CANVAS_SIZE - RIBBON_WIDTH) / 2;\n  const PI4 = Math.PI * 4;\n  const PI7 = Math.PI * 7;\n\n  // Optimized deform function with cached trig values - like nature's efficient patterns\n  const deform = (x, y, t, progress, sinCache) => {\n    // Ensure indices stay within bounds by clamping progress\n    const p = Math.max(0, Math.min(1, progress));\n    const wave1 = sinCache[Math.floor(Math.abs((p * PI4 + t * 0.01) * 100) % 628)] * 30;\n    const wave2 = sinCache[Math.floor(Math.abs((p * PI7 - t * 0.008) * 100) % 628)] * 15;\n    const harmonic = sinCache[Math.floor(Math.abs((x * 0.02 + y * 0.015 + t * 0.005) * 100) % 628)] * 10;\n    return { offsetX: wave1 + harmonic, offsetY: wave2 };\n  };\n    \n  useEffect(() => {\n    const canvas = canvasRef.current;\n    if (!canvas) return;\n\n    // Initialize canvas and context only once\n    if (!ctxRef.current) {\n      canvas.width = CANVAS_SIZE;\n      canvas.height = CANVAS_SIZE;\n      ctxRef.current = canvas.getContext('2d', {\n        alpha: false // Optimization: no transparency needed\n      });\n    }\n    const ctx = ctxRef.current;\n\n    // Pre-calculate sin values for performance\n    if (!pointsCacheRef.current) {\n      const sinCache = new Float32Array(628); // 2π * 100\n      for (let i = 0; i < 628; i++) {\n        sinCache[i] = Math.sin(i / 100);\n      }\n      pointsCacheRef.current = {\n        sinCache,\n        verticalLines: new Float32Array(GRID_DENSITY * 2 * (GRID_DENSITY + 1)),\n        horizontalLines: new Float32Array(GRID_DENSITY * 2 * (GRID_DENSITY + 1))\n      };\n    }\n    const { sinCache, verticalLines, horizontalLines } = pointsCacheRef.current;\n\n    let animationId;\n    \n    const animate = () => {\n      const time = timeRef.current;\n      \n      // Clear with background color\n      ctx.fillStyle = '#F0EEE6';\n      ctx.fillRect(0, 0, CANVAS_SIZE, CANVAS_SIZE);\n      \n      // Set line style\n      ctx.strokeStyle = '#777777';\n      ctx.lineWidth = 0.5;\n      \n      // Update point positions\n      let vIdx = 0;\n      let hIdx = 0;\n      \n      // Calculate vertical lines\n      for (let i = 0; i < GRID_DENSITY; i++) {\n        const x = RIBBON_OFFSET + (i / GRID_DENSITY) * RIBBON_WIDTH;\n        \n        for (let j = 0; j <= GRID_DENSITY; j++) {\n          const progress = (j / GRID_DENSITY) * 1.2 - 0.1;\n          const y = progress * CANVAS_SIZE;\n          \n          const { offsetX, offsetY } = deform(x, y, time, progress, sinCache);\n          \n          verticalLines[vIdx++] = x + offsetX;\n          verticalLines[vIdx++] = y + offsetY;\n        }\n      }\n      \n      // Calculate horizontal lines\n      for (let j = 0; j < GRID_DENSITY; j++) {\n        const progress = (j / GRID_DENSITY) * 1.2 - 0.1;\n        const y = progress * CANVAS_SIZE;\n        \n        for (let i = 0; i <= GRID_DENSITY; i++) {\n          const x = RIBBON_OFFSET + (i / GRID_DENSITY) * RIBBON_WIDTH;\n          \n          const { offsetX, offsetY } = deform(x, y, time, progress, sinCache);\n          \n          horizontalLines[hIdx++] = x + offsetX;\n          horizontalLines[hIdx++] = y + offsetY;\n        }\n      }\n      \n      // Draw vertical lines\n      vIdx = 0;\n      for (let i = 0; i < GRID_DENSITY; i++) {\n        ctx.beginPath();\n        ctx.moveTo(verticalLines[vIdx], verticalLines[vIdx + 1]);\n        vIdx += 2;\n        \n        for (let j = 1; j <= GRID_DENSITY; j++) {\n          ctx.lineTo(verticalLines[vIdx], verticalLines[vIdx + 1]);\n          vIdx += 2;\n        }\n        ctx.stroke();\n      }\n      \n      // Draw horizontal lines\n      hIdx = 0;\n      for (let j = 0; j < GRID_DENSITY; j++) {\n        ctx.beginPath();\n        ctx.moveTo(horizontalLines[hIdx], horizontalLines[hIdx + 1]);\n        hIdx += 2;\n        \n        for (let i = 1; i <= GRID_DENSITY; i++) {\n          ctx.lineTo(horizontalLines[hIdx], horizontalLines[hIdx + 1]);\n          hIdx += 2;\n        }\n        ctx.stroke();\n      }\n      \n      timeRef.current += 0.3;\n      animationId = requestAnimationFrame(animate);\n    };\n    \n    animate();\n    \n    return () => {\n      if (animationId) {\n        cancelAnimationFrame(animationId);\n      }\n      \n      // Clear canvas context\n      if (ctxRef.current) {\n        ctxRef.current.clearRect(0, 0, CANVAS_SIZE, CANVAS_SIZE);\n      }\n      \n      // Clear refs to prevent memory leaks\n      ctxRef.current = null;\n      pointsCacheRef.current = null;\n      timeRef.current = 0;\n    };\n  }, []);\n\n  return (\n    <div style={{ \n      margin: 0,\n      background: '#F0EEE6',\n      overflow: 'hidden',\n      display: 'flex',\n      alignItems: 'center',\n      justifyContent: 'center',\n      height: '100%'\n    }}>\n      <div style={{\n        padding: '30px',\n        position: 'relative',\n        width: '100%',\n        height: '100%',\n        display: 'flex',\n        alignItems: 'center',\n        justifyContent: 'center'\n      }}>\n        <canvas \n          ref={canvasRef}\n          style={{ display: 'block' }}\n        />\n      </div>\n    </div>\n  );\n};\n\nexport default FlowingRibbons;",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; const FlowingRibbons = () => { const canvasRef = useRef(null); const ctxRef = useRef(null); const pointsCacheRef = useRef(null); const timeRef = useRef(0); const CANVAS_SIZE = 550; const GRID_DENSITY = 100; const RIBBON_WIDTH = CANVAS_SIZE * 0.85; const RIBBON_OFFSET = (CANVAS_SIZE - RIBBON_WIDTH) / 2; const PI4 = Math.PI * 4; const PI7 = Math.PI * 7; const deform = (x, y, t, progress, sinCache) => { const p = Math.max(0, Math.min(1, progress)); const wave1 = sinCache[Math.floor(Math.abs((p * PI4 + t * 0.01) * 100) % 628)] * 30; const wave2 = sinCache[Math.floor(Math.abs((p * PI7 - t * 0.008) * 100) % 628)] * 15; const harmonic = sinCache[Math.floor(Math.abs((x * 0.02 + y * 0.015 + t * 0.005) * 100) % 628)] * 10; return { offsetX: wave1 + harmonic, offsetY: wave2 }; }; useEffect(() => { const canvas = canvasRef.current; if (!canvas) return; if (!ctxRef.current) { canvas.width = CANVAS_SIZE; canvas.height = CANVAS_SIZE; ctxRef.current = canvas.getContext('2d', { alpha: false }); } const ctx = ctxRef.current; if (!pointsCacheRef.current) { const sinCache = new Float32Array(628); for (let i = 0; i < 628; i++) { sinCache[i] = Math.sin(i / 100); } pointsCacheRef.current = { sinCache, verticalLines: new Float32Array(GRID_DENSITY * 2 * (GRID_DENSITY + 1)), horizontalLines: new Float32Array(GRID_DENSITY * 2 * (GRID_DENSITY + 1)) }; } const { sinCache, verticalLines, horizontalLines } = pointsCacheRef.current; let animationId; const animate = () => { const time = timeRef.current; ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, CANVAS_SIZE, CANVAS_SIZE); ctx.strokeStyle = '#777777'; ctx.lineWidth = 0.5; let vIdx = 0; let hIdx = 0; for (let i = 0; i < GRID_DENSITY; i++) { const x = RIBBON_OFFSET + (i / GRID_DENSITY) * RIBBON_WIDTH; for (let j = 0; j <= GRID_DENSITY; j++) { const progress = (j / GRID_DENSITY) * 1.2 - 0.1; const y = progress * CANVAS_SIZE; const { offsetX, offsetY } = deform(x, y, time, progress, sinCache); verticalLines[vIdx++] = x + offsetX; verticalLines[vIdx++] = y + offsetY; } } for (let j = 0; j < GRID_DENSITY; j++) { const progress = (j / GRID_DENSITY) * 1.2 - 0.1; const y = progress * CANVAS_SIZE; for (let i = 0; i <= GRID_DENSITY; i++) { const x = RIBBON_OFFSET + (i / GRID_DENSITY) * RIBBON_WIDTH; const { offsetX, offsetY } = deform(x, y, time, progress, sinCache); horizontalLines[hIdx++] = x + offsetX; horizontalLines[hIdx++] = y + offsetY; } } vIdx = 0; for (let i = 0; i < GRID_DENSITY; i++) { ctx.beginPath(); ctx.moveTo(verticalLines[vIdx], verticalLines[vIdx + 1]); vIdx += 2; for (let j = 1; j <= GRID_DENSITY; j++) { ctx.lineTo(verticalLines[vIdx], verticalLines[vIdx + 1]); vIdx += 2; } ctx.stroke(); } hIdx = 0; for (let j = 0; j < GRID_DENSITY; j++) { ctx.beginPath(); ctx.moveTo(horizontalLines[hIdx], horizontalLines[hIdx + 1]); hIdx += 2; for (let i = 1; i <= GRID_DENSITY; i++) { ctx.lineTo(horizontalLines[hIdx], horizontalLines[hIdx + 1]); hIdx += 2; } ctx.stroke(); } timeRef.current += 0.3; animationId = requestAnimationFrame(animate); }; animate(); return () => { if (animationId) { cancelAnimationFrame(animationId); } if (ctxRef.current) { ctxRef.current.clearRect(0, 0, CANVAS_SIZE, CANVAS_SIZE); } ctxRef.current = null; pointsCacheRef.current = null; timeRef.current = 0; }; }, []); return ( <div style={{ margin: 0, background: '#F0EEE6', overflow: 'hidden', display: 'flex', alignItems: 'center', justifyContent: 'center', height: '100%' }}><div style={{ padding: '30px', position: 'relative', width: '100%', height: '100%', display: 'flex', alignItems: 'center', justifyContent: 'center' }}><canvas ref={canvasRef} style={{ display: 'block' }} /></div></div> ); }; export default FlowingRibbons;",text:"The Vibe Coder gives himself up\nto whatever the moment brings.\n\nHe knows death will come\nso he holds on to nothing.\nNo illusions in his mind.\nNo resistance in his body.\nHe doesn’t ruminate over actions.\nThey flow naturally from the core\nof his being.\n\nHe holds nothing back from life\nso he is ready for death.\nJust as a man is ready for sleep\nafter a good day’s work.",shouldDisplayMobileArtwork:!0,promptSuggestion:"make it wild"},{number:51,staticArtwork:Tn,sourceCode:"// @ts-nocheck\nimport React, { useEffect, useRef } from 'react';\n\n// Themes: natural nourishment, intelligent formation, welcoming return\n// Visualization: Layers of particles that form and flow like geological strata, showing how all things are shaped by their environment\n\nconst CanyonStratifiedUndulations = () => {\n  const canvasRef = useRef(null);\n  const animationFrameRef = useRef(null);\n  \n  useEffect(() => {\n    const canvas = canvasRef.current;\n    if (!canvas) return;\n    \n    const ctx = canvas.getContext('2d');\n    const width = canvas.width = 550;\n    const height = canvas.height = 550;\n    const centerX = width / 2;\n    const centerY = height / 2;\n    \n    // Each particle represents a point of potential, shaped by its environment\n    const PARTICLE_COUNT = 18000;\n    const STRATA_LAYERS = 12;  // The layers through which formation occurs\n    const particles = [];\n    \n    // Create particles in stratified rock layers, each finding its natural place\n    for (let i = 0; i < PARTICLE_COUNT; i++) {\n      const stratum = Math.floor(Math.random() * STRATA_LAYERS);\n      const thickness = 40 + stratum * 3;\n      const depth = stratum * 25;\n      \n      // Position within stratum\n      const y = Math.random() * height;\n      const strataPhase = y * 0.01 + stratum * 0.3;\n      \n      // Multiple wave frequencies for each stratum\n      const primaryUndulation = Math.sin(strataPhase) * 35;\n      const secondaryUndulation = Math.sin(strataPhase * 2 + stratum * 0.8) * 18;\n      const tertiaryUndulation = Math.sin(strataPhase * 4 + stratum * 1.5) * 8;\n      \n      const totalUndulation = primaryUndulation + secondaryUndulation + tertiaryUndulation;\n      \n      // Determine side and position\n      const side = Math.random() < 0.5 ? -1 : 1;\n      const baseX = centerX + side * (60 + totalUndulation + depth);\n      const offsetX = (Math.random() - 0.5) * thickness;\n      \n      particles.push({\n        x: baseX + offsetX,\n        y: y,\n        z: (stratum - STRATA_LAYERS/2) * 30 + (Math.random() - 0.5) * 20,\n        side: side,\n        stratum: stratum,\n        flow: Math.random() * Math.PI * 2,\n        oscillation: Math.random() * Math.PI * 2,\n        velocity: 0.05 + stratum * 0.015,\n        brightness: 0.6 + Math.random() * 0.4\n      });\n    }\n    \n    let time = 0;\n    let isRunning = true;\n    \n    function animate() {\n      if (!isRunning) return;\n      \n      time += 0.016;\n      \n      // Clear with stratified effect\n      ctx.fillStyle = 'rgba(240, 238, 230, 0.025)';\n      ctx.fillRect(0, 0, width, height);\n      \n      // Sort by z-depth and stratum\n      particles.sort((a, b) => a.z - b.z);\n      \n      particles.forEach(particle => {\n        const strataPhase = particle.y * 0.01 + particle.stratum * 0.3 + time * 0.03;\n        \n        // Calculate undulations for this stratum\n        const primaryUndulation = Math.sin(strataPhase) * 35;\n        const secondaryUndulation = Math.sin(strataPhase * 2 + particle.stratum * 0.8) * 18;\n        const tertiaryUndulation = Math.sin(strataPhase * 4 + particle.stratum * 1.5) * 8;\n        \n        const totalUndulation = primaryUndulation + secondaryUndulation + tertiaryUndulation;\n        const depth = particle.stratum * 25;\n        const thickness = 40 + particle.stratum * 3;\n        \n        // Calculate target position\n        const targetX = centerX + particle.side * (60 + totalUndulation + depth);\n        const strataDrift = Math.sin(particle.flow + time * 0.6 + particle.stratum * 0.4) * thickness * 0.7;\n        \n        // Smooth movement with stratum-specific dynamics\n        particle.x = particle.x * 0.94 + (targetX + strataDrift) * 0.06;\n        particle.y += particle.velocity;\n        \n        // Add vertical oscillation within stratum\n        particle.y += Math.sin(particle.oscillation + time * 0.8) * 0.3;\n        \n        // Update depth\n        particle.z += Math.sin(time * 0.3 + particle.flow + particle.stratum * 0.6) * 0.25;\n        \n        // Reset at bottom\n        if (particle.y > height + 40) {\n          particle.y = -40;\n          particle.flow = Math.random() * Math.PI * 2;\n        }\n        \n        // Draw with stratum-based effects\n        const depthFactor = (particle.z + STRATA_LAYERS * 15) / (STRATA_LAYERS * 30);\n        const opacity = 0.15 + depthFactor * 0.15;  // More transparent\n        const size = 0.3 + depthFactor * 0.8;    // Smaller particles\n        const brightness = 45 + particle.stratum * 3 + particle.brightness * 15;  // Lighter, more subtle gradients\n        \n        if (opacity > 0 && size > 0) {\n          // Stratum-based highlighting\n          if (particle.stratum % 3 === 0) {\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, size * 2, 0, Math.PI * 2);\n            ctx.fillStyle = `rgba(${brightness}, ${brightness}, ${brightness}, ${opacity * 0.06})`;\n            ctx.fill();\n          }\n          \n          // Main particle\n          ctx.beginPath();\n          ctx.arc(particle.x, particle.y, size, 0, Math.PI * 2);\n          ctx.fillStyle = `rgba(${brightness}, ${brightness}, ${brightness}, ${opacity})`;\n          ctx.fill();\n        }\n      });\n      \n      animationFrameRef.current = requestAnimationFrame(animate);\n    }\n    \n    animate();\n    \n    return () => {\n      isRunning = false;\n      if (animationFrameRef.current) {\n        cancelAnimationFrame(animationFrameRef.current);\n        animationFrameRef.current = null;\n      }\n      if (ctx) {\n        ctx.clearRect(0, 0, width, height);\n      }\n      particles.length = 0;\n      time = 0;\n    };\n  }, []);\n  \n  return (\n    <div style={{\n      width: '550px',\n      height: '550px',\n      margin: 'auto',\n      backgroundColor: '#F0EEE6',\n      overflow: 'hidden'\n    }}>\n      <canvas\n        ref={canvasRef}\n        style={{\n          display: 'block',\n          width: '550px',\n          height: '550px'\n        }}\n      />\n    </div>\n  );\n};\n\nexport default CanyonStratifiedUndulations;",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; const CanyonStratifiedUndulations = () => { const canvasRef = useRef(null); const animationFrameRef = useRef(null); useEffect(() => { const canvas = canvasRef.current; if (!canvas) return; const ctx = canvas.getContext('2d'); const width = canvas.width = 550; const height = canvas.height = 550; const centerX = width / 2; const centerY = height / 2; const PARTICLE_COUNT = 18000; const STRATA_LAYERS = 12; const particles = []; for (let i = 0; i < PARTICLE_COUNT; i++) { const stratum = Math.floor(Math.random() * STRATA_LAYERS); const thickness = 40 + stratum * 3; const depth = stratum * 25; const y = Math.random() * height; const strataPhase = y * 0.01 + stratum * 0.3; const primaryUndulation = Math.sin(strataPhase) * 35; const secondaryUndulation = Math.sin(strataPhase * 2 + stratum * 0.8) * 18; const tertiaryUndulation = Math.sin(strataPhase * 4 + stratum * 1.5) * 8; const totalUndulation = primaryUndulation + secondaryUndulation + tertiaryUndulation; const side = Math.random() < 0.5 ? -1 : 1; const baseX = centerX + side * (60 + totalUndulation + depth); const offsetX = (Math.random() - 0.5) * thickness; particles.push({ x: baseX + offsetX, y: y, z: (stratum - STRATA_LAYERS/2) * 30 + (Math.random() - 0.5) * 20, side: side, stratum: stratum, flow: Math.random() * Math.PI * 2, oscillation: Math.random() * Math.PI * 2, velocity: 0.05 + stratum * 0.015, brightness: 0.6 + Math.random() * 0.4 }); } let time = 0; let isRunning = true; function animate() { if (!isRunning) return; time += 0.016; ctx.fillStyle = 'rgba(240, 238, 230, 0.025)'; ctx.fillRect(0, 0, width, height); particles.sort((a, b) => a.z - b.z); particles.forEach(particle => { const strataPhase = particle.y * 0.01 + particle.stratum * 0.3 + time * 0.03; const primaryUndulation = Math.sin(strataPhase) * 35; const secondaryUndulation = Math.sin(strataPhase * 2 + particle.stratum * 0.8) * 18; const tertiaryUndulation = Math.sin(strataPhase * 4 + particle.stratum * 1.5) * 8; const totalUndulation = primaryUndulation + secondaryUndulation + tertiaryUndulation; const depth = particle.stratum * 25; const thickness = 40 + particle.stratum * 3; const targetX = centerX + particle.side * (60 + totalUndulation + depth); const strataDrift = Math.sin(particle.flow + time * 0.6 + particle.stratum * 0.4) * thickness * 0.7; particle.x = particle.x * 0.94 + (targetX + strataDrift) * 0.06; particle.y += particle.velocity; particle.y += Math.sin(particle.oscillation + time * 0.8) * 0.3; particle.z += Math.sin(time * 0.3 + particle.flow + particle.stratum * 0.6) * 0.25; if (particle.y > height + 40) { particle.y = -40; particle.flow = Math.random() * Math.PI * 2; } const depthFactor = (particle.z + STRATA_LAYERS * 15) / (STRATA_LAYERS * 30); const opacity = 0.15 + depthFactor * 0.15; const size = 0.3 + depthFactor * 0.8; const brightness = 45 + particle.stratum * 3 + particle.brightness * 15; if (opacity > 0 && size > 0) { if (particle.stratum % 3 === 0) { ctx.beginPath(); ctx.arc(particle.x, particle.y, size * 2, 0, Math.PI * 2); ctx.fillStyle = `rgba(${brightness}, ${brightness}, ${brightness}, ${opacity * 0.06})`; ctx.fill(); } ctx.beginPath(); ctx.arc(particle.x, particle.y, size, 0, Math.PI * 2); ctx.fillStyle = `rgba(${brightness}, ${brightness}, ${brightness}, ${opacity})`; ctx.fill(); } }); animationFrameRef.current = requestAnimationFrame(animate); } animate(); return () => { isRunning = false; if (animationFrameRef.current) { cancelAnimationFrame(animationFrameRef.current); animationFrameRef.current = null; } if (ctx) { ctx.clearRect(0, 0, width, height); } particles.length = 0; time = 0; }; }, []); return ( <div style={{ width: '550px', height: '550px', margin: 'auto', backgroundColor: '#F0EEE6', overflow: 'hidden' }}><canvas ref={canvasRef} style={{ display: 'block', width: '550px', height: '550px' }} /></div> ); }; export default CanyonStratifiedUndulations;",text:"All things arise from Source.\nThey are nourished with intelligence.\nThey are formed with substance.\nThey are shaped by their surroundings.\n\nFor this reason, everything in existence,\nwithout exception, cherishes Source.\nNot by demand, but of its own accord.\nIt is the true expression of all things.\n\nSource gives birth to all beings,\nnourishes them, develops them,\ncares for them, protects them,\ncomforts them and welcomes them home\nto rejoin The One.\n\nGiving birth without possessing.\nSupporting without expecting.\nLeading without controlling.\nThis is Nature’s Way.",shouldDisplayMobileArtwork:!0,promptSuggestion:"make it glassy"},{number:52,staticArtwork:we,sourceCode:"import React, { useState, useEffect, useRef } from 'react';\n\n// Themes: return to origin, knowing mother and child, finding inexhaustible energy\n// Visualization: A pulsing ASCII form that emerges from and returns to emptiness, showing the cycle of manifestation\n\nconst AsciiBlob = () => {\n  const [frame, setFrame] = useState(60); // Start at frame 60\n  const densityRef = useRef(0); // Use ref to avoid dependency issues\n  \n  // Characters representing the stages of manifestation\n  const simpleChars = '░▒▓█';  // From emptiness to form\n  \n  // Canvas dimensions - the space of potential\n  const width = 60;\n  const height = 30;\n  \n  useEffect(() => {\n    let animationFrameId = null;\n    let lastFrameTime = 0;\n    const targetFPS = 10; // Increased from 2 FPS to 10 FPS\n    const frameInterval = 1000 / targetFPS;\n    \n    const animate = (currentTime) => {\n      animationFrameId = requestAnimationFrame(animate);\n      \n      const deltaTime = currentTime - lastFrameTime;\n      \n      // Only update if enough time has passed\n      if (deltaTime >= frameInterval) {\n        // Calculate remainder to prevent drift\n        const remainder = deltaTime % frameInterval;\n        \n        // Update lastFrameTime with the time that's been processed\n        lastFrameTime = currentTime - remainder;\n        \n        setFrame(prevFrame => {\n          const newFrame = prevFrame + 1;\n          // Update density value in ref (adjusted time factor for 10 FPS)\n          densityRef.current = 27.5 + 22.5 * Math.sin(newFrame * 0.002); // Reduced from 0.01 to 0.002 (1/5)\n          return newFrame;\n        });\n      }\n    };\n    \n    // Start animation\n    animationFrameId = requestAnimationFrame(animate);\n    \n    return () => {\n      // Clean up the animation frame to stop the animation\n      if (animationFrameId) {\n        cancelAnimationFrame(animationFrameId);\n      }\n      \n      // Reset frame and density\n      setFrame(60); // Reset to frame 60\n      densityRef.current = 0;\n    };\n  }, []); // Empty dependency array to run only once on mount\n  \n  // Generate ASCII art\n  const generateArt = () => {\n    const canvas = [];\n    const currentDensity = densityRef.current;\n    \n    for (let y = 0; y < height; y++) {\n      let row = '';\n      for (let x = 0; x < width; x++) {\n        // Generate a value based on position, frame, and density\n        const centerX = width / 2;\n        const centerY = height / 2;\n        \n        // Distance from center (normalized)\n        const dx = (x - centerX) / centerX;\n        const dy = (y - centerY) / centerY;\n        const distFromCenter = Math.sqrt(dx * dx + dy * dy);\n        \n        // Simpler, more ordered pattern (adjusted time factors for 10 FPS)\n        let noiseValue = Math.sin(distFromCenter * 3 - frame * 0.002); // Reduced from 0.01 to 0.002 (1/5)\n        noiseValue += Math.cos(x * 0.1 - y * 0.1 + frame * 0.0008) * 0.3; // Reduced from 0.004 to 0.0008 (1/5)\n        noiseValue = (noiseValue + 1) / 2; // Normalize to 0-1\n        \n        // Apply density adjustment\n        noiseValue = noiseValue * (currentDensity / 100);\n        \n        // Select character based on value\n        const charIndex = Math.floor(noiseValue * simpleChars.length);\n        let char = simpleChars[Math.min(Math.max(0, charIndex), simpleChars.length - 1)];\n        \n        // As we approach non-action, introduce more empty space\n        if (currentDensity < 30 && Math.random() > currentDensity / 30) {\n          char = ' ';\n        }\n        \n        row += char;\n      }\n      canvas.push(row);\n    }\n    \n    return canvas;\n  };\n  \n  const canvas = generateArt();\n  \n  const containerStyle = {\n    backgroundColor: '#F0EEE6',\n    height: '100vh',\n    width: '100vw',\n    display: 'flex',\n    alignItems: 'center',\n    justifyContent: 'center'\n  };\n  \n  return (\n    <div style={containerStyle}>\n      <pre className=\"font-mono text-gray-800 text-center text-xs md:text-sm whitespace-pre\">\n        {canvas.join('\\n')}\n      </pre>\n    </div>\n  );\n};\n\nexport default AsciiBlob;\n",sourceCodeMin:"import React, { useState, useEffect, useRef } from 'react'; const AsciiBlob = () => { const [frame, setFrame] = useState(60); const densityRef = useRef(0); const simpleChars = '░▒▓█'; const width = 60; const height = 30; useEffect(() => { let animationFrameId = null; let lastFrameTime = 0; const targetFPS = 10; const frameInterval = 1000 / targetFPS; const animate = (currentTime) => { animationFrameId = requestAnimationFrame(animate); const deltaTime = currentTime - lastFrameTime; if (deltaTime >= frameInterval) { const remainder = deltaTime % frameInterval; lastFrameTime = currentTime - remainder; setFrame(prevFrame => { const newFrame = prevFrame + 1; densityRef.current = 27.5 + 22.5 * Math.sin(newFrame * 0.002); return newFrame; }); } }; animationFrameId = requestAnimationFrame(animate); return () => { if (animationFrameId) { cancelAnimationFrame(animationFrameId); } setFrame(60); densityRef.current = 0; }; }, []); const generateArt = () => { const canvas = []; const currentDensity = densityRef.current; for (let y = 0; y < height; y++) { let row = ''; for (let x = 0; x < width; x++) { const centerX = width / 2; const centerY = height / 2; const dx = (x - centerX) / centerX; const dy = (y - centerY) / centerY; const distFromCenter = Math.sqrt(dx * dx + dy * dy); let noiseValue = Math.sin(distFromCenter * 3 - frame * 0.002); noiseValue += Math.cos(x * 0.1 - y * 0.1 + frame * 0.0008) * 0.3; noiseValue = (noiseValue + 1) / 2; noiseValue = noiseValue * (currentDensity / 100); const charIndex = Math.floor(noiseValue * simpleChars.length); let char = simpleChars[Math.min(Math.max(0, charIndex), simpleChars.length - 1)]; if (currentDensity < 30 && Math.random() > currentDensity / 30) { char = ' '; } row += char; } canvas.push(row); } return canvas; }; const canvas = generateArt(); const containerStyle = { backgroundColor: '#F0EEE6', height: '100vh', width: '100vw', display: 'flex', alignItems: 'center', justifyContent: 'center' }; return ( <div style={containerStyle}><pre className=\"font-mono text-gray-800 text-center text-xs md:text-sm whitespace-pre\"> {canvas.join('\\n')} </pre></div> ); }; export default AsciiBlob;",text:"In the beginning was Source,\nthe mother of all things.\nKnowing the mother,\nyou also know her children.\nIf you know her children,\nwhile keeping to the mother,\nyou’ll be free from sorrow.\nThough your body may dissolve,\nyour life energy will remain inexhaustible.\n\nIf you close your mind in judgment\nand busy yourself with stimulation,\nyour heart will suffer.\nIf you remain open-minded\nand dwell in solitude, free from dogma,\nyour heart will find peace.\n\nSeeing into darkness is lucidity.\nKnowing how to soften is strength.\nUse your inner light to return to enlightenment.\nThis is called practicing eternity.",shouldDisplayMobileArtwork:!0,promptSuggestion:"what if the forms collided?"},{number:53,staticArtwork:be,sourceCode:"// @ts-nocheck\nimport React, { useEffect, useRef } from 'react';\n\n// Themes: natural simplicity, balance lost, true path revealed\n// Visualization: Growth patterns that spread naturally from centers of energy, showing how complexity emerges from simple rules\n\nconst RadialGrowth = () => {\n  const canvasRef = useRef(null);\n\n  useEffect(() => {\n    const canvas = canvasRef.current;\n    if (!canvas) return;\n    \n    const ctx = canvas.getContext('2d');\n    canvas.width = 550;\n    canvas.height = 550;\n\n    let animationTime = 0;\n    let radiators = [];\n    let animationFrameId;\n\n    // Each radiator represents a point of natural growth and decay\n    class Radiator {\n      constructor(x, y) {\n        this.x = x;\n        this.y = y;\n        this.radius = Math.random() * 40 + 10;  // Natural variation in size\n        this.lines = Math.floor(Math.random() * 150 + 100);  // Each finds its own form\n        this.growth = (Math.random() * 0.5 + 0.5) * 0.125;  // Growth follows its own pace\n        this.maxLength = Math.random() * 100 + 50;\n        this.age = 0;\n        this.lifespan = (Math.random() * 200 + 100) * 4;  // Each has its time\n        this.opacity = 0;\n      }\n\n      update() {\n        this.age++;\n        // Fade in during first 20% of life, fade out during last 20%\n        if (this.age < this.lifespan * 0.2) {\n          this.opacity = Math.min(1, this.age / (this.lifespan * 0.2));\n        } else if (this.age > this.lifespan * 0.8) {\n          this.opacity = Math.max(0, 1 - (this.age - this.lifespan * 0.8) / (this.lifespan * 0.2));\n        } else {\n          this.opacity = 1;\n        }\n      }\n\n      draw() {\n        const currentLength = Math.min(this.maxLength, this.age * this.growth);\n        const centerRadius = 1 + this.age * 0.0025; // Eighth center growth speed\n        \n        ctx.save();\n        ctx.globalAlpha = this.opacity * 0.5;\n        ctx.translate(this.x, this.y);\n\n        // Draw radiating lines\n        for (let i = 0; i < this.lines; i++) {\n          const angle = (i / this.lines) * Math.PI * 2;\n          const length = currentLength;\n          \n          ctx.beginPath();\n          ctx.moveTo(0, 0);\n          ctx.lineTo(Math.cos(angle) * length, Math.sin(angle) * length);\n          ctx.strokeStyle = '#333';\n          ctx.lineWidth = 0.3;\n          ctx.stroke();\n        }\n\n        // Draw central dot with growth\n        ctx.beginPath();\n        ctx.arc(0, 0, centerRadius, 0, Math.PI * 2);\n        ctx.fillStyle = 'rgba(0, 0, 0, 0.8)';\n        ctx.fill();\n\n        ctx.restore();\n      }\n\n      isDead() {\n        return this.age > this.lifespan;\n      }\n    }\n\n    function spawnRadiator() {\n      if (radiators.length < 50) {\n        // Spawn in clusters - use existing radiator position as reference\n        let baseX, baseY;\n        \n        if (radiators.length > 0 && Math.random() < 0.8) {\n          // 80% chance to spawn near existing radiator\n          const parent = radiators[Math.floor(Math.random() * radiators.length)];\n          const angle = Math.random() * Math.PI * 2;\n          const distance = Math.random() * 50 + 20;\n          baseX = parent.x + Math.cos(angle) * distance;\n          baseY = parent.y + Math.sin(angle) * distance;\n        } else {\n          // Random position with bias toward center\n          const centerBias = Math.random() * 0.5 + 0.5;\n          baseX = canvas.width/2 + (Math.random() - 0.5) * canvas.width * centerBias;\n          baseY = canvas.height/2 + (Math.random() - 0.5) * canvas.height * centerBias;\n        }\n\n        // Keep within bounds\n        baseX = Math.max(50, Math.min(canvas.width - 50, baseX));\n        baseY = Math.max(50, Math.min(canvas.height - 50, baseY));\n\n        radiators.push(new Radiator(baseX, baseY));\n      }\n    }\n\n    function animate() {\n      ctx.fillStyle = '#F0EEE6';\n      ctx.fillRect(0, 0, canvas.width, canvas.height);\n\n      animationTime++;\n\n      // Spawn new radiators periodically\n      if (animationTime % 40 === 0) { // Eighth spawn rate\n        spawnRadiator();\n      }\n\n      // Update and draw radiators\n      radiators = radiators.filter(radiator => !radiator.isDead());\n      \n      radiators.forEach(radiator => {\n        radiator.update();\n        radiator.draw();\n      });\n\n      animationFrameId = requestAnimationFrame(animate);\n    }\n\n    animate();\n\n    return () => {\n      // Cancel animation frame to stop the animation\n      cancelAnimationFrame(animationFrameId);\n      \n      // Clear the canvas\n      ctx.clearRect(0, 0, canvas.width, canvas.height);\n      \n      // Clean up objects\n      radiators = [];\n      animationTime = 0;\n    };\n  }, []);\n\n  return (\n    <div style={{ \n      width: '100vw', \n      height: '100vh', \n      display: 'flex', \n      justifyContent: 'center', \n      alignItems: 'center',\n      backgroundColor: '#F0EEE6'\n    }}>\n      <div style={{ \n        width: '550px', \n        height: '550px',\n        backgroundColor: '#F0EEE6',\n        borderRadius: '2px',\n        boxShadow: '0 0 10px rgba(0,0,0,0.1)'\n      }}>\n        <canvas ref={canvasRef} />\n      </div>\n    </div>\n  );\n};\n\nexport default RadialGrowth;\n",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; const RadialGrowth = () => { const canvasRef = useRef(null); useEffect(() => { const canvas = canvasRef.current; if (!canvas) return; const ctx = canvas.getContext('2d'); canvas.width = 550; canvas.height = 550; let animationTime = 0; let radiators = []; let animationFrameId; class Radiator { constructor(x, y) { this.x = x; this.y = y; this.radius = Math.random() * 40 + 10; this.lines = Math.floor(Math.random() * 150 + 100); this.growth = (Math.random() * 0.5 + 0.5) * 0.125; this.maxLength = Math.random() * 100 + 50; this.age = 0; this.lifespan = (Math.random() * 200 + 100) * 4; this.opacity = 0; } update() { this.age++; if (this.age < this.lifespan * 0.2) { this.opacity = Math.min(1, this.age / (this.lifespan * 0.2)); } else if (this.age > this.lifespan * 0.8) { this.opacity = Math.max(0, 1 - (this.age - this.lifespan * 0.8) / (this.lifespan * 0.2)); } else { this.opacity = 1; } } draw() { const currentLength = Math.min(this.maxLength, this.age * this.growth); const centerRadius = 1 + this.age * 0.0025; ctx.save(); ctx.globalAlpha = this.opacity * 0.5; ctx.translate(this.x, this.y); for (let i = 0; i < this.lines; i++) { const angle = (i / this.lines) * Math.PI * 2; const length = currentLength; ctx.beginPath(); ctx.moveTo(0, 0); ctx.lineTo(Math.cos(angle) * length, Math.sin(angle) * length); ctx.strokeStyle = '#333'; ctx.lineWidth = 0.3; ctx.stroke(); } ctx.beginPath(); ctx.arc(0, 0, centerRadius, 0, Math.PI * 2); ctx.fillStyle = 'rgba(0, 0, 0, 0.8)'; ctx.fill(); ctx.restore(); } isDead() { return this.age > this.lifespan; } } function spawnRadiator() { if (radiators.length < 50) { let baseX, baseY; if (radiators.length > 0 && Math.random() < 0.8) { const parent = radiators[Math.floor(Math.random() * radiators.length)]; const angle = Math.random() * Math.PI * 2; const distance = Math.random() * 50 + 20; baseX = parent.x + Math.cos(angle) * distance; baseY = parent.y + Math.sin(angle) * distance; } else { const centerBias = Math.random() * 0.5 + 0.5; baseX = canvas.width/2 + (Math.random() - 0.5) * canvas.width * centerBias; baseY = canvas.height/2 + (Math.random() - 0.5) * canvas.height * centerBias; } baseX = Math.max(50, Math.min(canvas.width - 50, baseX)); baseY = Math.max(50, Math.min(canvas.height - 50, baseY)); radiators.push(new Radiator(baseX, baseY)); } } function animate() { ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, canvas.width, canvas.height); animationTime++; if (animationTime % 40 === 0) { spawnRadiator(); } radiators = radiators.filter(radiator => !radiator.isDead()); radiators.forEach(radiator => { radiator.update(); radiator.draw(); }); animationFrameId = requestAnimationFrame(animate); } animate(); return () => { cancelAnimationFrame(animationFrameId); ctx.clearRect(0, 0, canvas.width, canvas.height); radiators = []; animationTime = 0; }; }, []); return ( <div style={{ width: '100vw', height: '100vh', display: 'flex', justifyContent: 'center', alignItems: 'center', backgroundColor: '#F0EEE6' }}><div style={{ width: '550px', height: '550px', backgroundColor: '#F0EEE6', borderRadius: '2px', boxShadow: '0 0 10px rgba(0,0,0,0.1)' }}><canvas ref={canvasRef} /></div></div> ); }; export default RadialGrowth;",text:"The great way is easy,\nyet people search for shortcuts.\n\nNotice when balance is lost:\nWhen rich speculators prosper\nwhile farmers lose their land.\nWhen an elite class imposes regulations\nwhile working people have nowhere to turn.\nWhen politicians fund fraudulent fixes\nfor imaginary catastrophic events.\n\nAll of this is arrogance and corruption.\nAnd it is not in keeping with Nature’s Way.",shouldDisplayMobileArtwork:!0,promptSuggestion:"what is the minimal version?"},{number:54,staticArtwork:Gn,sourceCode:"// @ts-nocheck\nimport React, { useRef, useEffect } from 'react';\n\n// Themes: inner rootedness, genuine cultivation, universal harmony\n// Visualization: Lines that follow invisible force fields, showing how deep patterns emerge from firmly established principles\n\nconst VectorFieldLines = () => {\n  const canvasRef = useRef(null);\n  \n  useEffect(() => {\n    const canvas = canvasRef.current;\n    const ctx = canvas.getContext('2d');\n    const width = canvas.width;\n    const height = canvas.height;\n    \n    // Core parameters that shape the field's behavior\n    const numLines = 300;  // The many that arise from the one\n    const linePoints = 200;  // Memory of the path\n    const noiseScale = 0.005;  // Subtlety of influence\n    const noiseTimeScale = 0.000125;  // Natural rhythm of change\n    const lineAlpha = 0.4;  // Balance of presence\n    const lineWidth = 0.5;\n    \n    let animationFrameId;\n    let time = 0;\n    \n    // Simple noise function\n    const noise = (x, y, z) => {\n      return Math.sin(x * 7 + z * 3) * 0.5 + \n             Math.sin(y * 8 + z * 4) * 0.5;\n    };\n    \n    // The underlying force that guides all movement\n    const vectorField = (x, y, t) => {\n      // Transform coordinates to the field's natural space\n      const nx = (x - width / 2) * 0.01;\n      const ny = (y - height / 2) * 0.01;\n      \n      // Get noise value\n      const n = noise(nx, ny, t * noiseTimeScale);\n      \n      // Create form boundary (mask)\n      const cx = x - width / 2;\n      const cy = y - height / 2;\n      const r = Math.sqrt(cx * cx + cy * cy);\n      const mask = Math.max(0, 1 - r / 200);\n      \n      // Create directional vector based on mask and noise\n      const angle = n * Math.PI * 4 + Math.atan2(cy, cx);\n      \n      return {\n        x: Math.cos(angle) * mask,\n        y: Math.sin(angle) * mask\n      };\n    };\n    \n    // Create a line\n    class Line {\n      constructor() {\n        this.reset();\n      }\n      \n      reset() {\n        // Start at random position\n        const angle = Math.random() * Math.PI * 2;\n        const distance = 50 + Math.random() * 150;\n        \n        this.x = width / 2 + Math.cos(angle) * distance;\n        this.y = height / 2 + Math.sin(angle) * distance;\n        \n        this.points = [];\n        this.age = 0;\n        this.lifespan = 400 + Math.random() * 600;  // Quadruple lifespan for smoother transitions\n        this.opacity = 0;\n        this.fadeIn = true;\n        this.width = 0.2 + Math.random() * 0.8;\n      }\n      \n      update(t) {\n        // Update age\n        this.age += 1;\n        \n        // Reset when too old\n        if (this.age >= this.lifespan) {\n          this.reset();\n          return;\n        }\n        \n        // Update opacity - fade in and out\n        const progress = this.age / this.lifespan;\n        if (progress < 0.1) {\n          this.opacity = progress / 0.1 * lineAlpha;\n        } else if (progress > 0.9) {\n          this.opacity = (1 - (progress - 0.9) / 0.1) * lineAlpha;\n        } else {\n          this.opacity = lineAlpha;\n        }\n        \n        // Get vector at current position\n        const vector = vectorField(this.x, this.y, t);\n        \n        // Add current position to points array\n        this.points.push({ x: this.x, y: this.y });\n        \n        // Limit the number of points\n        if (this.points.length > linePoints) {\n          this.points.shift();\n        }\n        \n        // Move to next position\n        this.x += vector.x * 0.5;  // Quarter speed movement\n        this.y += vector.y * 0.5;\n        \n        // Check if out of bounds or vector is too small\n        const magnitude = Math.sqrt(vector.x * vector.x + vector.y * vector.y);\n        if (this.x < 0 || this.x > width || this.y < 0 || this.y > height || magnitude < 0.01) {\n          this.reset();\n        }\n      }\n      \n      draw(ctx) {\n        if (this.points.length < 2) return;\n        \n        ctx.beginPath();\n        ctx.strokeStyle = `rgba(51, 51, 51, ${this.opacity})`;\n        ctx.lineWidth = this.width * lineWidth;\n        \n        ctx.moveTo(this.points[0].x, this.points[0].y);\n        \n        for (let i = 1; i < this.points.length; i++) {\n          ctx.lineTo(this.points[i].x, this.points[i].y);\n        }\n        \n        ctx.stroke();\n      }\n    }\n    \n    // Create lines\n    const lines = Array.from({ length: numLines }, () => new Line());\n    \n    // Animation loop\n    const animate = () => {\n      ctx.clearRect(0, 0, width, height);\n      ctx.fillStyle = '#F0EEE6';\n      ctx.fillRect(0, 0, width, height);\n      \n      // Update and draw all lines\n      lines.forEach(line => {\n        line.update(time);\n        line.draw(ctx);\n      });\n      \n      time += 1;\n      animationFrameId = requestAnimationFrame(animate);\n    };\n    \n    animate();\n    \n    // Cleanup\n    return () => {\n      cancelAnimationFrame(animationFrameId);\n      \n      // Clear canvas context\n      if (ctx) {\n        ctx.clearRect(0, 0, width, height);\n      }\n      \n      // Clear lines array to prevent memory leaks\n      lines.length = 0;\n    };\n  }, []);\n  \n  return (\n    <div className=\"flex items-center justify-center bg-[#F0EEE6] w-full h-full\">\n      <canvas ref={canvasRef} width={550} height={550} />\n    </div>\n  );\n};\n\nexport default VectorFieldLines;\n",sourceCodeMin:"import React, { useRef, useEffect } from 'react'; const VectorFieldLines = () => { const canvasRef = useRef(null); useEffect(() => { const canvas = canvasRef.current; const ctx = canvas.getContext('2d'); const width = canvas.width; const height = canvas.height; const numLines = 300; const linePoints = 200; const noiseScale = 0.005; const noiseTimeScale = 0.000125; const lineAlpha = 0.4; const lineWidth = 0.5; let animationFrameId; let time = 0; const noise = (x, y, z) => { return Math.sin(x * 7 + z * 3) * 0.5 + Math.sin(y * 8 + z * 4) * 0.5; }; const vectorField = (x, y, t) => { const nx = (x - width / 2) * 0.01; const ny = (y - height / 2) * 0.01; const n = noise(nx, ny, t * noiseTimeScale); const cx = x - width / 2; const cy = y - height / 2; const r = Math.sqrt(cx * cx + cy * cy); const mask = Math.max(0, 1 - r / 200); const angle = n * Math.PI * 4 + Math.atan2(cy, cx); return { x: Math.cos(angle) * mask, y: Math.sin(angle) * mask }; }; class Line { constructor() { this.reset(); } reset() { const angle = Math.random() * Math.PI * 2; const distance = 50 + Math.random() * 150; this.x = width / 2 + Math.cos(angle) * distance; this.y = height / 2 + Math.sin(angle) * distance; this.points = []; this.age = 0; this.lifespan = 400 + Math.random() * 600; this.opacity = 0; this.fadeIn = true; this.width = 0.2 + Math.random() * 0.8; } update(t) { this.age += 1; if (this.age >= this.lifespan) { this.reset(); return; } const progress = this.age / this.lifespan; if (progress < 0.1) { this.opacity = progress / 0.1 * lineAlpha; } else if (progress > 0.9) { this.opacity = (1 - (progress - 0.9) / 0.1) * lineAlpha; } else { this.opacity = lineAlpha; } const vector = vectorField(this.x, this.y, t); this.points.push({ x: this.x, y: this.y }); if (this.points.length > linePoints) { this.points.shift(); } this.x += vector.x * 0.5; this.y += vector.y * 0.5; const magnitude = Math.sqrt(vector.x * vector.x + vector.y * vector.y); if (this.x < 0 || this.x > width || this.y < 0 || this.y > height || magnitude < 0.01) { this.reset(); } } draw(ctx) { if (this.points.length < 2) return; ctx.beginPath(); ctx.strokeStyle = `rgba(51, 51, 51, ${this.opacity})`; ctx.lineWidth = this.width * lineWidth; ctx.moveTo(this.points[0].x, this.points[0].y); for (let i = 1; i < this.points.length; i++) { ctx.lineTo(this.points[i].x, this.points[i].y); } ctx.stroke(); } } const lines = Array.from({ length: numLines }, () => new Line()); const animate = () => { ctx.clearRect(0, 0, width, height); ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, width, height); lines.forEach(line => { line.update(time); line.draw(ctx); }); time += 1; animationFrameId = requestAnimationFrame(animate); }; animate(); return () => { cancelAnimationFrame(animationFrameId); if (ctx) { ctx.clearRect(0, 0, width, height); } lines.length = 0; }; }, []); return ( <div className=\"flex items-center justify-center bg-[#F0EEE6] w-full h-full\"><canvas ref={canvasRef} width={550} height={550} /></div> ); }; export default VectorFieldLines;",text:"What is firmly established within you\ncannot be uprooted.\nWhat is firmly embraced within you\ncannot slip away.\nSource, firmly established and embraced,\nwill be held in honor for generations to come.\n\nAll things find their highest expression\nwhen rooted in Source.\nCultivate it in yourself,\nand you will become genuine.\nCultivate it in the family,\nand your family will flourish.\nCultivate it in the community,\nand your community will be prosperous.\nCultivate it in the nation,\nand your nation will be exemplary.\nCultivate it in the world,\nand the world will sing in harmony.\n\nTherefore:\nLook at yourself,\nand see other people.\nLook at your family,\nand see other families.\nLook at your community,\nand see other communities.\nLook at your nation,\nand see other nations.\nLook at your world,\nand see other worlds.\n\nHow do I know this is true.\nSimple observation.",shouldDisplayMobileArtwork:!0,promptSuggestion:"what if it shape shifted?"},{number:55,staticArtwork:qn,sourceCode:'// @ts-nocheck\nimport React, { useRef, useEffect } from \'react\';\n\n// Themes: natural vitality, perfect balance, eternal spirit\n// Visualization: A form that breathes and pulses with life, its hatched lines revealing the balance between structure and movement\n\nconst OscillatingHatching = () => {\n  const canvasRef = useRef(null);\n  const requestRef = useRef(null);\n  \n  useEffect(() => {\n    const canvas = canvasRef.current;\n    const ctx = canvas.getContext(\'2d\');\n    const width = canvas.width;\n    const height = canvas.height;\n    \n    // Set background\n    ctx.fillStyle = "#F0EEE6";\n    ctx.fillRect(0, 0, width, height);\n    \n    // Center of the canvas\n    const centerX = width / 2;\n    const centerY = height / 2;\n    \n    // Animation variables\n    let time = 50;\n    const timeStep = 0.004;  // Reduced to 50% speed\n    \n    // Noise function simulation\n    const noise3D = (x, y, z, t) => {\n      return Math.sin(x * 0.1 + t * 0.15) * \n             Math.cos(y * 0.1 + Math.sin(z * 0.1) + t * 0.1) * \n             Math.sin(z * 0.1 + Math.sin(x * 0.1) + t * 0.2);\n    };\n    \n    // Parameters that define the form\'s vital energy\n    const baseForm = {\n      // Core structure that breathes and flows\n      majorRadius: 80,  // Primary circle of life\n      minorRadius: 30,  // Secondary pulse\n      complexity: 0.8,  // Balance between order and chaos\n      resolution: 36    // Clarity of form\n    };\n    \n    // Cache for vertices to avoid regenerating every frame\n    let cachedVertices = null;\n    let lastCacheTime = -1;\n    const cacheLifetime = 0.1; // How often to regenerate vertices\n    \n    // Generate vertices for the 3D form\n    const generateVertices = (time) => {\n      // Check if we can use the cache\n      if (cachedVertices && time - lastCacheTime < cacheLifetime) {\n        return cachedVertices;\n      }\n      \n      const vertices = [];\n      const resolution = baseForm.resolution;\n      \n      // Base radius oscillation\n      const breathingFactor = Math.sin(time * 0.2) * 5;  // Reduced to 50% speed\n      const majorRadius = baseForm.majorRadius + breathingFactor;\n      const minorRadius = baseForm.minorRadius + breathingFactor * 0.2;\n      \n      // Generate points on a torus with noise deformation\n      for (let i = 0; i < resolution; i++) {\n        const theta = (i / resolution) * Math.PI * 2;\n        \n        for (let j = 0; j < resolution; j++) {\n          const phi = (j / resolution) * Math.PI * 2;\n          \n          // Base torus coordinates\n          const x = (majorRadius + minorRadius * Math.cos(phi)) * Math.cos(theta);\n          const y = (majorRadius + minorRadius * Math.cos(phi)) * Math.sin(theta);\n          const z = minorRadius * Math.sin(phi);\n          \n          // Add noise deformation that changes with time\n          const noiseScale = 0.02 * baseForm.complexity;\n          const timeFactor = time * 0.2;\n          const noise = \n            15 * noise3D(x * noiseScale, y * noiseScale, z * noiseScale, timeFactor) +\n            7 * noise3D(x * noiseScale * 2, y * noiseScale * 2, z * noiseScale * 2, timeFactor * 1.3);\n            \n          // Calculate normal direction (simplified)\n          const normalX = x / majorRadius;\n          const normalY = y / majorRadius;\n          const normalZ = z / minorRadius;\n          \n          // Normalize the normal vector\n          const normalLength = Math.sqrt(normalX*normalX + normalY*normalY + normalZ*normalZ) || 0.001;\n          \n          // Apply noise along normal direction\n          const deformedX = x + (normalX / normalLength) * noise;\n          const deformedY = y + (normalY / normalLength) * noise;\n          const deformedZ = z + (normalZ / normalLength) * noise;\n          \n          // Create tangent vectors for hatching direction\n          // Primary tangent along phi direction\n          const tangent1X = -Math.sin(theta);\n          const tangent1Y = Math.cos(theta);\n          const tangent1Z = 0;\n          \n          // Secondary tangent along theta direction\n          const tangent2X = -Math.cos(theta) * Math.sin(phi);\n          const tangent2Y = -Math.sin(theta) * Math.sin(phi);\n          const tangent2Z = Math.cos(phi);\n          \n          vertices.push({\n            position: { \n              x: deformedX, \n              y: deformedY, \n              z: deformedZ \n            },\n            normal: { \n              x: normalX / normalLength, \n              y: normalY / normalLength, \n              z: normalZ / normalLength \n            },\n            tangent1: { \n              x: tangent1X, \n              y: tangent1Y, \n              z: tangent1Z \n            },\n            tangent2: { \n              x: tangent2X, \n              y: tangent2Y, \n              z: tangent2Z \n            },\n            theta,\n            phi,\n            // Pre-calculate a noise value for hatching intensity\n            hatchingIntensity: 0.3 + 0.7 * Math.abs(noise3D(\n              deformedX * 0.03, \n              deformedY * 0.03, \n              deformedZ * 0.03, \n              time * 0.5\n            ))\n          });\n        }\n      }\n      \n      // Update cache\n      cachedVertices = vertices;\n      lastCacheTime = time;\n      \n      return vertices;\n    };\n    \n    // Project 3D point to 2D with subtle rotation\n    const project = (point, time) => {\n      // Add subtle rotation\n      const rotX = time * 0.05;  // Reduced to 50% speed\n      const rotY = time * 0.075;  // Reduced to 50% speed\n      \n      const cosX = Math.cos(rotX);\n      const sinX = Math.sin(rotX);\n      const cosY = Math.cos(rotY);\n      const sinY = Math.sin(rotY);\n      \n      // Apply rotation around Y-axis\n      const y1 = point.y;\n      const z1 = point.z * cosX - point.x * sinX;\n      const x1 = point.z * sinX + point.x * cosX;\n      \n      // Apply rotation around X-axis\n      const y2 = y1 * cosY - z1 * sinY;\n      const z2 = y1 * sinY + z1 * cosY;\n      const x2 = x1;\n      \n      // Project to 2D with larger scale (closer view)\n      const scale = 1.5;  // Increase this to make it appear closer\n      return {\n        x: centerX + x2 * scale,\n        y: centerY + y2 * scale,\n        z: z2  // Keep z for depth sorting\n      };\n    };\n    \n    // Calculate visibility with z-buffer\n    const calculateVisibility = (projectedVertices) => {\n      // Create a grid-based z-buffer\n      const bufferSize = 200;\n      const zBuffer = new Array(bufferSize).fill().map(() => new Array(bufferSize).fill(-Infinity));\n      const visible = new Array(projectedVertices.length).fill(false);\n      \n      // Convert canvas coordinates to buffer indices\n      const toBufferCoords = (x, y) => {\n        const bx = Math.floor((x / width) * bufferSize);\n        const by = Math.floor((y / height) * bufferSize);\n        return { bx, by };\n      };\n      \n      // Fill z-buffer with furthest visible points\n      projectedVertices.forEach((vertex, index) => {\n        const { bx, by } = toBufferCoords(vertex.x, vertex.y);\n        \n        if (bx >= 0 && bx < bufferSize && by >= 0 && by < bufferSize) {\n          if (vertex.z > zBuffer[by][bx]) {\n            zBuffer[by][bx] = vertex.z;\n            visible[index] = true;\n          }\n        }\n      });\n      \n      return visible;\n    };\n    \n    // Generate hatching lines with animated oscillation\n    const generateHatchingLines = (vertices, visibility, time) => {\n      // Project vertices to 2D\n      const projectedVertices = vertices.map(v => {\n        const projection = project(v.position, time);\n        return {\n          ...projection,\n          vertex: v\n        };\n      });\n      \n      // Determine visibility\n      const isVisible = calculateVisibility(projectedVertices);\n      \n      // Clear canvas\n      ctx.fillStyle = "#F0EEE6";\n      ctx.fillRect(0, 0, width, height);\n      \n      // Group vertices by similar phi values to create "slices"\n      const phiGroups = {};\n      const phiPrecision = 0.2; // Grouping precision\n      \n      vertices.forEach((vertex, index) => {\n        if (!isVisible[index]) return;\n        \n        // Round phi to group similar values\n        const phiKey = Math.round(vertex.phi / phiPrecision) * phiPrecision;\n        \n        if (!phiGroups[phiKey]) {\n          phiGroups[phiKey] = [];\n        }\n        \n        phiGroups[phiKey].push({\n          vertex,\n          projectedPos: projectedVertices[index]\n        });\n      });\n      \n      // Draw primary hatching lines along phi\n      Object.values(phiGroups).forEach(group => {\n        if (group.length < 2) return;\n        \n        // Sort by theta for consistent line drawing\n        group.sort((a, b) => a.vertex.theta - b.vertex.theta);\n        \n        // Draw with varying opacity based on depth and time\n        const midPoint = group[Math.floor(group.length / 2)];\n        const normalZ = midPoint.vertex.normal.z;\n        \n        // Make all lines visible by removing the skipping logic\n        const timeOscillation = 0; // Fixed value to prevent oscillation\n        const skip = false; // Never skip lines - keep them all visible\n        \n        // No conditional return - all lines will be drawn\n        \n        // Get hatching intensity from pre-calculated value\n        const intensity = midPoint.vertex.hatchingIntensity;\n        \n        // Calculate line width and opacity based on normal direction and intensity\n        const baseLightness = Math.abs(normalZ) * 0.7 + 0.3; // 0.3-1.0 range\n        const baseOpacity = 0.1 + baseLightness * 0.5;\n        const opacity = baseOpacity * intensity;\n        \n        // Calculate line width\n        const width = 0.3 + intensity * 0.7;\n        \n        ctx.beginPath();\n        \n        // Start line\n        const start = group[0].projectedPos;\n        ctx.moveTo(start.x, start.y);\n        \n        // Draw line through all points in the group\n        for (let i = 1; i < group.length; i++) {\n          const point = group[i].projectedPos;\n          \n          // Add some variation to line based on time\n          const variation = 0.5 * Math.sin(time + group[i].vertex.theta * 10);\n          ctx.lineTo(point.x + variation, point.y + variation);\n        }\n        \n        // Set line style\n        ctx.strokeStyle = `rgba(51, 51, 51, ${opacity})`;\n        ctx.lineWidth = width;\n        ctx.stroke();\n      });\n      \n      // Group vertices by similar theta values to create "rings"\n      const thetaGroups = {};\n      const thetaPrecision = 0.15;\n      \n      vertices.forEach((vertex, index) => {\n        if (!isVisible[index]) return;\n        \n        const thetaKey = Math.round(vertex.theta / thetaPrecision) * thetaPrecision;\n        \n        if (!thetaGroups[thetaKey]) {\n          thetaGroups[thetaKey] = [];\n        }\n        \n        thetaGroups[thetaKey].push({\n          vertex,\n          projectedPos: projectedVertices[index]\n        });\n      });\n      \n      // Draw cross-hatching lines (theta groups - rings)\n      Object.values(thetaGroups).forEach(group => {\n        if (group.length < 2) return;\n        \n        // Sort by phi for consistent line drawing\n        group.sort((a, b) => a.vertex.phi - b.vertex.phi);\n        \n        // Make all cross-hatching lines visible by removing the skipping logic\n        const midPoint = group[Math.floor(group.length / 2)];\n        const timeOscillation = 0; // Fixed value to prevent oscillation\n        const skip = false; // Never skip lines - keep them all visible\n        \n        // No conditional return - all lines will be drawn\n        \n        // Calculate lighting and intensity similar to primary hatching\n        const normalX = midPoint.vertex.normal.x;\n        const normalY = midPoint.vertex.normal.y;\n        const lightAngle = Math.atan2(normalY, normalX);\n        const intensity = 0.3 + 0.7 * Math.abs(Math.sin(lightAngle + time * 0.02));  // Reduced time factor for more consistent intensity\n        \n        // Cross-hatching is slightly thinner\n        const width = 0.2 + intensity * 0.6;\n        const opacity = (0.1 + intensity * 0.4) * midPoint.vertex.hatchingIntensity;\n        \n        ctx.beginPath();\n        \n        // Start line\n        const start = group[0].projectedPos;\n        ctx.moveTo(start.x, start.y);\n        \n        // Draw line through all points in the group\n        for (let i = 1; i < group.length; i++) {\n          const point = group[i].projectedPos;\n          \n          // Add some variation to line based on time\n          const variation = 0.3 * Math.cos(time * 1.2 + group[i].vertex.phi * 8);\n          ctx.lineTo(point.x + variation, point.y + variation);\n        }\n        \n        // Set line style\n        ctx.strokeStyle = `rgba(51, 51, 51, ${opacity})`;\n        ctx.lineWidth = width;\n        ctx.stroke();\n      });\n      \n      // Add accent hatches in areas of high curvature\n      const accentThreshold = 0.7;\n      \n      projectedVertices.forEach((pVertex, index) => {\n        if (!isVisible[index]) return;\n        \n        const vertex = pVertex.vertex;\n        \n        // Use pre-calculated hatching intensity to find high curvature areas\n        if (vertex.hatchingIntensity > accentThreshold) {\n          // Create short line segment based on tangent direction\n          // Mix both tangent directions for varied hatching\n          const mixFactor = Math.sin(time * 0.1 + vertex.theta * 2 + vertex.phi * 1);  // Slower, more subtle oscillation\n          const tangentX = vertex.tangent1.x * (1 + mixFactor) / 2 + vertex.tangent2.x * (1 - mixFactor) / 2;\n          const tangentY = vertex.tangent1.y * (1 + mixFactor) / 2 + vertex.tangent2.y * (1 - mixFactor) / 2;\n          const tangentZ = vertex.tangent1.z * (1 + mixFactor) / 2 + vertex.tangent2.z * (1 - mixFactor) / 2;\n          \n          // Project start and end points\n          const length = 3 + 5 * vertex.hatchingIntensity;\n          \n          const startPos = {\n            x: vertex.position.x - tangentX * length,\n            y: vertex.position.y - tangentY * length,\n            z: vertex.position.z - tangentZ * length\n          };\n          \n          const endPos = {\n            x: vertex.position.x + tangentX * length,\n            y: vertex.position.y + tangentY * length,\n            z: vertex.position.z + tangentZ * length\n          };\n          \n          const projectedStart = project(startPos, time);\n          const projectedEnd = project(endPos, time);\n          \n          // Draw accent hatch\n          ctx.beginPath();\n          ctx.moveTo(projectedStart.x, projectedStart.y);\n          ctx.lineTo(projectedEnd.x, projectedEnd.y);\n          \n          // Set style with pulsing effect\n          const pulse = 0.5 + 0.5 * Math.sin(time * 0.25 + vertex.theta * 3 + vertex.phi * 2);  // Slower, less intense pulse\n          const accentOpacity = 0.1 + 0.5 * pulse * vertex.hatchingIntensity;\n          const accentWidth = 0.3 + 0.6 * pulse;\n          \n          ctx.strokeStyle = `rgba(51, 51, 51, ${accentOpacity})`;\n          ctx.lineWidth = accentWidth;\n          ctx.stroke();\n        }\n      });\n    };\n    \n    // Animation loop\n    const animate = () => {\n      time += timeStep;\n      \n      // Generate vertices for current time\n      const vertices = generateVertices(time);\n      \n      // Generate and draw hatching lines\n      generateHatchingLines(vertices, null, time);\n      \n      requestRef.current = requestAnimationFrame(animate);\n    };\n    \n    // Start animation\n    animate();\n    \n    // Cleanup\n    return () => {\n      cancelAnimationFrame(requestRef.current);\n      \n      // Clear canvas context\n      if (ctx) {\n        ctx.clearRect(0, 0, width, height);\n      }\n      \n      // Clear cached data to prevent memory leaks\n      cachedVertices = null;\n    };\n    \n  }, []);\n\n  return (\n    <div className="w-full h-full flex items-center justify-center bg-[#F0EEE6]">\n      <canvas \n        ref={canvasRef} \n        width={550} \n        height={550} \n        className="border border-gray-300 shadow-md"\n      />\n    </div>\n  );\n};\n\nexport default OscillatingHatching;',sourceCodeMin:'import React, { useRef, useEffect } from \'react\'; const OscillatingHatching = () => { const canvasRef = useRef(null); const requestRef = useRef(null); useEffect(() => { const canvas = canvasRef.current; const ctx = canvas.getContext(\'2d\'); const width = canvas.width; const height = canvas.height; ctx.fillStyle = "#F0EEE6"; ctx.fillRect(0, 0, width, height); const centerX = width / 2; const centerY = height / 2; let time = 50; const timeStep = 0.004; const noise3D = (x, y, z, t) => { return Math.sin(x * 0.1 + t * 0.15) * Math.cos(y * 0.1 + Math.sin(z * 0.1) + t * 0.1) * Math.sin(z * 0.1 + Math.sin(x * 0.1) + t * 0.2); }; const baseForm = { majorRadius: 80, minorRadius: 30, complexity: 0.8, resolution: 36 }; let cachedVertices = null; let lastCacheTime = -1; const cacheLifetime = 0.1; const generateVertices = (time) => { if (cachedVertices && time - lastCacheTime < cacheLifetime) { return cachedVertices; } const vertices = []; const resolution = baseForm.resolution; const breathingFactor = Math.sin(time * 0.2) * 5; const majorRadius = baseForm.majorRadius + breathingFactor; const minorRadius = baseForm.minorRadius + breathingFactor * 0.2; for (let i = 0; i < resolution; i++) { const theta = (i / resolution) * Math.PI * 2; for (let j = 0; j < resolution; j++) { const phi = (j / resolution) * Math.PI * 2; const x = (majorRadius + minorRadius * Math.cos(phi)) * Math.cos(theta); const y = (majorRadius + minorRadius * Math.cos(phi)) * Math.sin(theta); const z = minorRadius * Math.sin(phi); const noiseScale = 0.02 * baseForm.complexity; const timeFactor = time * 0.2; const noise = 15 * noise3D(x * noiseScale, y * noiseScale, z * noiseScale, timeFactor) + 7 * noise3D(x * noiseScale * 2, y * noiseScale * 2, z * noiseScale * 2, timeFactor * 1.3); const normalX = x / majorRadius; const normalY = y / majorRadius; const normalZ = z / minorRadius; const normalLength = Math.sqrt(normalX*normalX + normalY*normalY + normalZ*normalZ) || 0.001; const deformedX = x + (normalX / normalLength) * noise; const deformedY = y + (normalY / normalLength) * noise; const deformedZ = z + (normalZ / normalLength) * noise; const tangent1X = -Math.sin(theta); const tangent1Y = Math.cos(theta); const tangent1Z = 0; const tangent2X = -Math.cos(theta) * Math.sin(phi); const tangent2Y = -Math.sin(theta) * Math.sin(phi); const tangent2Z = Math.cos(phi); vertices.push({ position: { x: deformedX, y: deformedY, z: deformedZ }, normal: { x: normalX / normalLength, y: normalY / normalLength, z: normalZ / normalLength }, tangent1: { x: tangent1X, y: tangent1Y, z: tangent1Z }, tangent2: { x: tangent2X, y: tangent2Y, z: tangent2Z }, theta, phi, hatchingIntensity: 0.3 + 0.7 * Math.abs(noise3D( deformedX * 0.03, deformedY * 0.03, deformedZ * 0.03, time * 0.5 )) }); } } cachedVertices = vertices; lastCacheTime = time; return vertices; }; const project = (point, time) => { const rotX = time * 0.05; const rotY = time * 0.075; const cosX = Math.cos(rotX); const sinX = Math.sin(rotX); const cosY = Math.cos(rotY); const sinY = Math.sin(rotY); const y1 = point.y; const z1 = point.z * cosX - point.x * sinX; const x1 = point.z * sinX + point.x * cosX; const y2 = y1 * cosY - z1 * sinY; const z2 = y1 * sinY + z1 * cosY; const x2 = x1; const scale = 1.5; return { x: centerX + x2 * scale, y: centerY + y2 * scale, z: z2 }; }; const calculateVisibility = (projectedVertices) => { const bufferSize = 200; const zBuffer = new Array(bufferSize).fill().map(() => new Array(bufferSize).fill(-Infinity)); const visible = new Array(projectedVertices.length).fill(false); const toBufferCoords = (x, y) => { const bx = Math.floor((x / width) * bufferSize); const by = Math.floor((y / height) * bufferSize); return { bx, by }; }; projectedVertices.forEach((vertex, index) => { const { bx, by } = toBufferCoords(vertex.x, vertex.y); if (bx >= 0 && bx < bufferSize && by >= 0 && by < bufferSize) { if (vertex.z > zBuffer[by][bx]) { zBuffer[by][bx] = vertex.z; visible[index] = true; } } }); return visible; }; const generateHatchingLines = (vertices, visibility, time) => { const projectedVertices = vertices.map(v => { const projection = project(v.position, time); return { ...projection, vertex: v }; }); const isVisible = calculateVisibility(projectedVertices); ctx.fillStyle = "#F0EEE6"; ctx.fillRect(0, 0, width, height); const phiGroups = {}; const phiPrecision = 0.2; vertices.forEach((vertex, index) => { if (!isVisible[index]) return; const phiKey = Math.round(vertex.phi / phiPrecision) * phiPrecision; if (!phiGroups[phiKey]) { phiGroups[phiKey] = []; } phiGroups[phiKey].push({ vertex, projectedPos: projectedVertices[index] }); }); Object.values(phiGroups).forEach(group => { if (group.length < 2) return; group.sort((a, b) => a.vertex.theta - b.vertex.theta); const midPoint = group[Math.floor(group.length / 2)]; const normalZ = midPoint.vertex.normal.z; const timeOscillation = 0; const skip = false; const intensity = midPoint.vertex.hatchingIntensity; const baseLightness = Math.abs(normalZ) * 0.7 + 0.3; const baseOpacity = 0.1 + baseLightness * 0.5; const opacity = baseOpacity * intensity; const width = 0.3 + intensity * 0.7; ctx.beginPath(); const start = group[0].projectedPos; ctx.moveTo(start.x, start.y); for (let i = 1; i < group.length; i++) { const point = group[i].projectedPos; const variation = 0.5 * Math.sin(time + group[i].vertex.theta * 10); ctx.lineTo(point.x + variation, point.y + variation); } ctx.strokeStyle = `rgba(51, 51, 51, ${opacity})`; ctx.lineWidth = width; ctx.stroke(); }); const thetaGroups = {}; const thetaPrecision = 0.15; vertices.forEach((vertex, index) => { if (!isVisible[index]) return; const thetaKey = Math.round(vertex.theta / thetaPrecision) * thetaPrecision; if (!thetaGroups[thetaKey]) { thetaGroups[thetaKey] = []; } thetaGroups[thetaKey].push({ vertex, projectedPos: projectedVertices[index] }); }); Object.values(thetaGroups).forEach(group => { if (group.length < 2) return; group.sort((a, b) => a.vertex.phi - b.vertex.phi); const midPoint = group[Math.floor(group.length / 2)]; const timeOscillation = 0; const skip = false; const normalX = midPoint.vertex.normal.x; const normalY = midPoint.vertex.normal.y; const lightAngle = Math.atan2(normalY, normalX); const intensity = 0.3 + 0.7 * Math.abs(Math.sin(lightAngle + time * 0.02)); const width = 0.2 + intensity * 0.6; const opacity = (0.1 + intensity * 0.4) * midPoint.vertex.hatchingIntensity; ctx.beginPath(); const start = group[0].projectedPos; ctx.moveTo(start.x, start.y); for (let i = 1; i < group.length; i++) { const point = group[i].projectedPos; const variation = 0.3 * Math.cos(time * 1.2 + group[i].vertex.phi * 8); ctx.lineTo(point.x + variation, point.y + variation); } ctx.strokeStyle = `rgba(51, 51, 51, ${opacity})`; ctx.lineWidth = width; ctx.stroke(); }); const accentThreshold = 0.7; projectedVertices.forEach((pVertex, index) => { if (!isVisible[index]) return; const vertex = pVertex.vertex; if (vertex.hatchingIntensity > accentThreshold) { const mixFactor = Math.sin(time * 0.1 + vertex.theta * 2 + vertex.phi * 1); const tangentX = vertex.tangent1.x * (1 + mixFactor) / 2 + vertex.tangent2.x * (1 - mixFactor) / 2; const tangentY = vertex.tangent1.y * (1 + mixFactor) / 2 + vertex.tangent2.y * (1 - mixFactor) / 2; const tangentZ = vertex.tangent1.z * (1 + mixFactor) / 2 + vertex.tangent2.z * (1 - mixFactor) / 2; const length = 3 + 5 * vertex.hatchingIntensity; const startPos = { x: vertex.position.x - tangentX * length, y: vertex.position.y - tangentY * length, z: vertex.position.z - tangentZ * length }; const endPos = { x: vertex.position.x + tangentX * length, y: vertex.position.y + tangentY * length, z: vertex.position.z + tangentZ * length }; const projectedStart = project(startPos, time); const projectedEnd = project(endPos, time); ctx.beginPath(); ctx.moveTo(projectedStart.x, projectedStart.y); ctx.lineTo(projectedEnd.x, projectedEnd.y); const pulse = 0.5 + 0.5 * Math.sin(time * 0.25 + vertex.theta * 3 + vertex.phi * 2); const accentOpacity = 0.1 + 0.5 * pulse * vertex.hatchingIntensity; const accentWidth = 0.3 + 0.6 * pulse; ctx.strokeStyle = `rgba(51, 51, 51, ${accentOpacity})`; ctx.lineWidth = accentWidth; ctx.stroke(); } }); }; const animate = () => { time += timeStep; const vertices = generateVertices(time); generateHatchingLines(vertices, null, time); requestRef.current = requestAnimationFrame(animate); }; animate(); return () => { cancelAnimationFrame(requestRef.current); if (ctx) { ctx.clearRect(0, 0, width, height); } cachedVertices = null; }; }, []); return ( <div className="w-full h-full flex items-center justify-center bg-[#F0EEE6]"><canvas ref={canvasRef} width={550} height={550} className="border border-gray-300 shadow-md" /></div> ); }; export default OscillatingHatching;',text:"He who is in harmony with Source\nis like a newborn child.\nPoisonous insects do not sting him.\nFerocious beasts do not attack him.\nWild birds do not claw him.\n\nHis bones are soft, his muscles are weak,\nyet his grip is strong.\nHe knows not of the union of male and female,\nyet he is filled with vitality.\n\nHe can shout all day without becoming hoarse.\nThis is the embodiment of perfect balance.\n\nTo know balance is to know the eternal.\nTo know the eternal is to be illuminated.\n\nThe Vibe Coder’s energy is like this,\nhe lets things come and he lets things go,\neffortlessly, without grasping.\nHe has no expectations, thus,\nhe’s never disappointed.\nSince he is never disappointed,\nhis spirit never grows old.",shouldDisplayMobileArtwork:!0,promptSuggestion:"what if it cracked?"},{number:56,staticArtwork:ne,sourceCode:"// @ts-nocheck\nimport React, { useEffect, useRef } from 'react';\n\n// Themes: silent wisdom, primal union, profound impartiality\n// Visualization: Waves that interfere and merge without resistance, showing how patterns emerge from silence\n\nconst WaveInterferenceV4 = () => {\n  const canvasRef = useRef(null);\n\n  useEffect(() => {\n    const canvas = canvasRef.current;\n    const ctx = canvas.getContext('2d');\n    const width = 550;\n    const height = 550;\n    canvas.width = width;\n    canvas.height = height;\n\n    // Points of origin from which patterns emerge\n    const sources = [];\n    const gridSize = 4;  // Balance in all directions\n    \n    for (let i = 0; i < gridSize; i++) {\n      for (let j = 0; j < gridSize; j++) {\n        sources.push({\n          x: width * (i + 0.5) / gridSize,\n          y: height * (j + 0.5) / gridSize,\n          wavelength: 15 + Math.random() * 10,  // Each source finds its rhythm\n          phase: Math.random() * Math.PI * 2    // Beginning in natural harmony\n        });\n      }\n    }\n\n    let time = 0;\n    let animationFrameId = null;\n\n    const animate = () => {\n      // Fill with project background color\n      ctx.fillStyle = '#F0EEE6';\n      ctx.fillRect(0, 0, width, height);\n\n      // Create overlay for smoother lines\n      ctx.strokeStyle = '#333';\n      ctx.lineWidth = 0.5;\n\n      // Sample points for marching squares algorithm\n      const resolution = 2;\n      const rows = Math.floor(height / resolution);\n      const cols = Math.floor(width / resolution);\n      const field = new Array(rows).fill(0).map(() => new Array(cols).fill(0));\n\n      // Calculate wave field\n      for (let i = 0; i < rows; i++) {\n        for (let j = 0; j < cols; j++) {\n          const x = j * resolution;\n          const y = i * resolution;\n          let amplitude = 0;\n\n          sources.forEach(source => {\n            const dx = x - source.x;\n            const dy = y - source.y;\n            const distance = Math.sqrt(dx * dx + dy * dy);\n            amplitude += Math.sin((distance / source.wavelength - time) * 2 * Math.PI + source.phase);\n          });\n\n          field[i][j] = amplitude / sources.length;\n        }\n      }\n\n      // Draw contour lines using marching squares\n      const contourLevels = [-0.6, -0.3, 0, 0.3, 0.6];\n      \n      contourLevels.forEach(level => {\n        ctx.beginPath();\n        \n        for (let i = 0; i < rows - 1; i++) {\n          for (let j = 0; j < cols - 1; j++) {\n            const x = j * resolution;\n            const y = i * resolution;\n            \n            // Marching squares cases\n            const case4 = \n              (field[i][j] > level ? 8 : 0) +\n              (field[i][j + 1] > level ? 4 : 0) +\n              (field[i + 1][j + 1] > level ? 2 : 0) +\n              (field[i + 1][j] > level ? 1 : 0);\n            \n            // Draw line segments based on case\n            switch (case4) {\n              case 1: case 14:\n                ctx.moveTo(x, y + resolution / 2);\n                ctx.lineTo(x + resolution / 2, y + resolution);\n                break;\n              case 2: case 13:\n                ctx.moveTo(x + resolution / 2, y + resolution);\n                ctx.lineTo(x + resolution, y + resolution / 2);\n                break;\n              case 3: case 12:\n                ctx.moveTo(x, y + resolution / 2);\n                ctx.lineTo(x + resolution, y + resolution / 2);\n                break;\n              case 4: case 11:\n                ctx.moveTo(x + resolution, y + resolution / 2);\n                ctx.lineTo(x + resolution / 2, y);\n                break;\n              case 5: case 10:\n                ctx.moveTo(x, y + resolution / 2);\n                ctx.lineTo(x + resolution / 2, y);\n                ctx.moveTo(x + resolution, y + resolution / 2);\n                ctx.lineTo(x + resolution / 2, y + resolution);\n                break;\n              case 6: case 9:\n                ctx.moveTo(x + resolution / 2, y);\n                ctx.lineTo(x + resolution / 2, y + resolution);\n                break;\n              case 7: case 8:\n                ctx.moveTo(x, y + resolution / 2);\n                ctx.lineTo(x + resolution / 2, y);\n                break;\n            }\n          }\n        }\n        \n        ctx.stroke();\n      });\n\n      time += 0.003; // Further reduced from 0.005 to 0.001\n      animationFrameId = requestAnimationFrame(animate);\n    };\n\n    animate();\n\n    return () => {\n      // Cancel animation frame to prevent memory leaks\n      if (animationFrameId) {\n        cancelAnimationFrame(animationFrameId);\n      }\n    };\n  }, []);\n\n  return (\n    <div style={{ \n      display: 'flex', \n      justifyContent: 'center', \n      alignItems: 'center',\n      width: '100vw', \n      height: '100vh', \n      backgroundColor: '#F0EEE6' \n    }}>\n      <canvas \n        ref={canvasRef} \n        style={{ \n          border: '1px solid #eee',\n          width: '550px',\n          height: '550px'\n        }} \n      />\n    </div>\n  );\n};\n\nexport default WaveInterferenceV4;",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; const WaveInterferenceV4 = () => { const canvasRef = useRef(null); useEffect(() => { const canvas = canvasRef.current; const ctx = canvas.getContext('2d'); const width = 550; const height = 550; canvas.width = width; canvas.height = height; const sources = []; const gridSize = 4; for (let i = 0; i < gridSize; i++) { for (let j = 0; j < gridSize; j++) { sources.push({ x: width * (i + 0.5) / gridSize, y: height * (j + 0.5) / gridSize, wavelength: 15 + Math.random() * 10, phase: Math.random() * Math.PI * 2 }); } } let time = 0; let animationFrameId = null; const animate = () => { ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, width, height); ctx.strokeStyle = '#333'; ctx.lineWidth = 0.5; const resolution = 2; const rows = Math.floor(height / resolution); const cols = Math.floor(width / resolution); const field = new Array(rows).fill(0).map(() => new Array(cols).fill(0)); for (let i = 0; i < rows; i++) { for (let j = 0; j < cols; j++) { const x = j * resolution; const y = i * resolution; let amplitude = 0; sources.forEach(source => { const dx = x - source.x; const dy = y - source.y; const distance = Math.sqrt(dx * dx + dy * dy); amplitude += Math.sin((distance / source.wavelength - time) * 2 * Math.PI + source.phase); }); field[i][j] = amplitude / sources.length; } } const contourLevels = [-0.6, -0.3, 0, 0.3, 0.6]; contourLevels.forEach(level => { ctx.beginPath(); for (let i = 0; i < rows - 1; i++) { for (let j = 0; j < cols - 1; j++) { const x = j * resolution; const y = i * resolution; const case4 = (field[i][j] > level ? 8 : 0) + (field[i][j + 1] > level ? 4 : 0) + (field[i + 1][j + 1] > level ? 2 : 0) + (field[i + 1][j] > level ? 1 : 0); switch (case4) { case 1: case 14: ctx.moveTo(x, y + resolution / 2); ctx.lineTo(x + resolution / 2, y + resolution); break; case 2: case 13: ctx.moveTo(x + resolution / 2, y + resolution); ctx.lineTo(x + resolution, y + resolution / 2); break; case 3: case 12: ctx.moveTo(x, y + resolution / 2); ctx.lineTo(x + resolution, y + resolution / 2); break; case 4: case 11: ctx.moveTo(x + resolution, y + resolution / 2); ctx.lineTo(x + resolution / 2, y); break; case 5: case 10: ctx.moveTo(x, y + resolution / 2); ctx.lineTo(x + resolution / 2, y); ctx.moveTo(x + resolution, y + resolution / 2); ctx.lineTo(x + resolution / 2, y + resolution); break; case 6: case 9: ctx.moveTo(x + resolution / 2, y); ctx.lineTo(x + resolution / 2, y + resolution); break; case 7: case 8: ctx.moveTo(x, y + resolution / 2); ctx.lineTo(x + resolution / 2, y); break; } } } ctx.stroke(); }); time += 0.003; animationFrameId = requestAnimationFrame(animate); }; animate(); return () => { if (animationFrameId) { cancelAnimationFrame(animationFrameId); } }; }, []); return ( <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', width: '100vw', height: '100vh', backgroundColor: '#F0EEE6' }}><canvas ref={canvasRef} style={{ border: '1px solid #eee', width: '550px', height: '550px' }} /></div> ); }; export default WaveInterferenceV4;",text:"Those who know don’t speak.\nThose who speak don’t know.\n\nClose your mouth, block off your senses,\ntemper your sharpness, untie your knots,\nsoften your glare.\nBe at one with the dust of the earth.\nThis is primal union.\n\nBecome one with Source and you’ll become\nprofoundly impartial.\nThis is the highest state of being.\n\nThe Vibe Coder cannot be approached\nor kept at a distance.\nHe cannot be helped or harmed.\nHe cannot be exalted or disgraced.\n\nHe gives of himself continually.\nThat is why he endures.",shouldDisplayMobileArtwork:!0,promptSuggestion:"how does it disperse?"},{number:57,staticArtwork:k,sourceCode:"// @ts-nocheck\nimport React, { useEffect, useRef } from 'react';\n\n// Themes: leadership through non-control, natural organization, peace through letting go\n// Visualization: Circles that organize themselves into complex patterns, demonstrating how order emerges without force\n\nconst MoireMandalaPattern = () => {\n  const canvasRef = useRef(null);\n  const animationFrameId = useRef<number>(null);\n  \n  useEffect(() => {\n    const canvas = canvasRef.current;\n    const ctx = canvas.getContext('2d');\n    canvas.width = 550;\n    canvas.height = 550;\n    \n    const drawPattern = (time = 0) => {\n      ctx.fillStyle = '#F0EEE6';\n      ctx.fillRect(0, 0, canvas.width, canvas.height);\n      \n      ctx.strokeStyle = '#000';\n      ctx.lineWidth = 0.6;\n      \n      // Center of the canvas\n      const centerX = canvas.width / 2;\n      const centerY = canvas.height / 2;\n      \n      // Each layer finds its natural place in the whole\n      const layers = [\n        { count: 1, radius: 0, circleSize: 60 },        // The still center\n        { count: 6, radius: 60, circleSize: 60 },       // First emanation\n        { count: 12, radius: 110, circleSize: 50 },     // Growing outward\n        { count: 18, radius: 160, circleSize: 45 },     // Expanding freely\n        { count: 24, radius: 205, circleSize: 40 }      // Reaching completion\n      ];\n      \n      layers.forEach((layer, layerIndex) => {\n        for (let i = 0; i < layer.count; i++) {\n          const angle = (i / layer.count) * Math.PI * 2;\n          \n          // Add subtle animation\n          const breathingEffect = Math.sin(time * 0.0015 + layerIndex * 0.5) * 2;  // Halved speed\n          const rotation = time * 0.00025 * (layerIndex % 2 === 0 ? 1 : -1);  // Halved speed\n          \n          // Position each circle\n          const circleX = centerX + Math.cos(angle + rotation) * layer.radius;\n          const circleY = centerY + Math.sin(angle + rotation) * layer.radius;\n          \n          // Draw concentric circles for each position\n          for (let r = 3; r < layer.circleSize; r += 3) {\n            ctx.beginPath();\n            \n            // Subtle distortion for moiré effect\n            for (let theta = 0; theta <= Math.PI * 2; theta += 0.1) {\n              const distortion = Math.sin(theta * 8 + time * 0.0025 + angle) * (r * 0.01);  // Halved speed\n              const x = circleX + (r + distortion + breathingEffect) * Math.cos(theta);\n              const y = circleY + (r + distortion + breathingEffect) * Math.sin(theta);\n              \n              if (theta === 0) {\n                ctx.moveTo(x, y);\n              } else {\n                ctx.lineTo(x, y);\n              }\n            }\n            \n            ctx.closePath();\n            ctx.stroke();\n          }\n        }\n      });\n      \n      // Add a central decorative pattern\n      const centralSize = 80;\n      for (let r = 3; r < centralSize; r += 3) {\n        ctx.beginPath();\n        \n        for (let theta = 0; theta <= Math.PI * 2; theta += 0.05) {\n          const distortion = Math.sin(theta * 6 + time * 0.0025) * (r * 0.015);  // Halved speed\n          const breathing = Math.sin(time * 0.002) * 1.5;  // Halved speed\n          \n          const x = centerX + (r + distortion + breathing) * Math.cos(theta);\n          const y = centerY + (r + distortion + breathing) * Math.sin(theta);\n          \n          if (theta === 0) {\n            ctx.moveTo(x, y);\n          } else {\n            ctx.lineTo(x, y);\n          }\n        }\n        \n        ctx.closePath();\n        ctx.stroke();\n      }\n      \n      animationFrameId.current = requestAnimationFrame(() => drawPattern(time + 1));\n    };\n    \n    drawPattern();\n\n    return () => {\n      if (animationFrameId.current) {\n        cancelAnimationFrame(animationFrameId.current);\n      }\n      \n      // Clear canvas context\n      if (ctx) {\n        ctx.clearRect(0, 0, canvas.width, canvas.height);\n      }\n      \n      // Reset animation frame ref\n      animationFrameId.current = null;\n    };\n\n  }, []);\n  \n  return (\n    <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', backgroundColor: '#F0EEE6' }}>\n      <canvas ref={canvasRef} style={{ boxShadow: '0 0 20px rgba(0,0,0,0.1)' }} />\n    </div>\n  );\n};\n\nexport default MoireMandalaPattern;",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; const MoireMandalaPattern = () => { const canvasRef = useRef(null); const animationFrameId = useRef<number>(null); useEffect(() => { const canvas = canvasRef.current; const ctx = canvas.getContext('2d'); canvas.width = 550; canvas.height = 550; const drawPattern = (time = 0) => { ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, canvas.width, canvas.height); ctx.strokeStyle = '#000'; ctx.lineWidth = 0.6; const centerX = canvas.width / 2; const centerY = canvas.height / 2; const layers = [ { count: 1, radius: 0, circleSize: 60 }, { count: 6, radius: 60, circleSize: 60 }, { count: 12, radius: 110, circleSize: 50 }, { count: 18, radius: 160, circleSize: 45 }, { count: 24, radius: 205, circleSize: 40 } ]; layers.forEach((layer, layerIndex) => { for (let i = 0; i < layer.count; i++) { const angle = (i / layer.count) * Math.PI * 2; const breathingEffect = Math.sin(time * 0.0015 + layerIndex * 0.5) * 2; const rotation = time * 0.00025 * (layerIndex % 2 === 0 ? 1 : -1); const circleX = centerX + Math.cos(angle + rotation) * layer.radius; const circleY = centerY + Math.sin(angle + rotation) * layer.radius; for (let r = 3; r < layer.circleSize; r += 3) { ctx.beginPath(); for (let theta = 0; theta <= Math.PI * 2; theta += 0.1) { const distortion = Math.sin(theta * 8 + time * 0.0025 + angle) * (r * 0.01); const x = circleX + (r + distortion + breathingEffect) * Math.cos(theta); const y = circleY + (r + distortion + breathingEffect) * Math.sin(theta); if (theta === 0) { ctx.moveTo(x, y); } else { ctx.lineTo(x, y); } } ctx.closePath(); ctx.stroke(); } } }); const centralSize = 80; for (let r = 3; r < centralSize; r += 3) { ctx.beginPath(); for (let theta = 0; theta <= Math.PI * 2; theta += 0.05) { const distortion = Math.sin(theta * 6 + time * 0.0025) * (r * 0.015); const breathing = Math.sin(time * 0.002) * 1.5; const x = centerX + (r + distortion + breathing) * Math.cos(theta); const y = centerY + (r + distortion + breathing) * Math.sin(theta); if (theta === 0) { ctx.moveTo(x, y); } else { ctx.lineTo(x, y); } } ctx.closePath(); ctx.stroke(); } animationFrameId.current = requestAnimationFrame(() => drawPattern(time + 1)); }; drawPattern(); return () => { if (animationFrameId.current) { cancelAnimationFrame(animationFrameId.current); } if (ctx) { ctx.clearRect(0, 0, canvas.width, canvas.height); } animationFrameId.current = null; }; }, []); return ( <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', backgroundColor: '#F0EEE6' }}><canvas ref={canvasRef} style={{ boxShadow: '0 0 20px rgba(0,0,0,0.1)' }} /></div> ); }; export default MoireMandalaPattern;",text:"To be a great leader,\nlet yourself be saturated in Source.\nStop trying to control.\nLet go of rigid plans and targets,\nthen watch the system organize itself.\nThe more restrictions and regulations,\nthe poorer the people become.\nThe more sharp the weapons,\nthe more discontent grows.\nThe more clever the deceiver,\nthe more debased society becomes.\nThe more laws established,\nthe more criminals appear.\n\nTherefore, The Vibe Coder says:\nI take no action and the people\ntransform themselves.\nI enjoy peace and the people\nbecome prosperous.\nI let go of all desire for the common good,\nand the good becomes as common as\nthe air we breathe.",shouldDisplayMobileArtwork:!0,promptSuggestion:"make it simpler"},{number:58,staticArtwork:tt,sourceCode:"// @ts-nocheck\nimport { useEffect, useRef } from 'react';\n\nconst BreathingRhombus = () => {\n  const containerRef = useRef(null);\n  \n  useEffect(() => {\n    if (!containerRef.current) return;\n    \n    const container = containerRef.current;\n    let animationFrameId = null;\n    let isComponentMounted = true;\n    const createdElements = [];\n    \n    // Clear any existing content\n    container.innerHTML = '';\n    \n    const numRhombi = 9;\n    \n    // Get container dimensions for responsive sizing\n    const containerWidth = container.offsetWidth;\n    const containerHeight = container.offsetHeight;\n    const scaleFactor = containerWidth / 322; // Base container width\n    \n    // Create SVG pattern for texture\n    const patternSVG = document.createElementNS('http://www.w3.org/2000/svg', 'svg');\n    patternSVG.setAttribute('width', '0');\n    patternSVG.setAttribute('height', '0');\n    patternSVG.innerHTML = `\n      <defs>\n        <pattern id=\"crackedTexture\" x=\"0\" y=\"0\" width=\"100\" height=\"100\" patternUnits=\"userSpaceOnUse\">\n          <rect width=\"100\" height=\"100\" fill=\"#444\" opacity=\"0.1\"/>\n          <path d=\"M20,20 L40,15 L35,40 M60,30 L80,25 L75,50 M10,70 L30,65 L25,90\" \n                stroke=\"#333\" stroke-width=\"0.5\" fill=\"none\" opacity=\"0.3\"/>\n        </pattern>\n      </defs>\n    `;\n    container.appendChild(patternSVG);\n    createdElements.push(patternSVG);\n    \n    // Create rhombi with responsive sizing\n    for (let i = 0; i < numRhombi; i++) {\n      const baseSize = 80 + Math.sin((i / numRhombi) * Math.PI) * 60;\n      const size = baseSize * scaleFactor;\n      const baseYPos = i * 40;\n      const yPos = baseYPos * scaleFactor;\n      \n      const rhombusDiv = document.createElement('div');\n      rhombusDiv.className = 'rhombus';\n      rhombusDiv.style.position = 'absolute';\n      rhombusDiv.style.left = '50%';\n      rhombusDiv.style.top = `${yPos}px`;\n      rhombusDiv.style.transform = 'translateX(-50%)';\n      rhombusDiv.style.transition = 'all 0.3s ease';\n      rhombusDiv.dataset.index = i;\n      rhombusDiv.dataset.originalSize = size;\n      \n      const svg = document.createElementNS('http://www.w3.org/2000/svg', 'svg');\n      svg.setAttribute('width', size);\n      svg.setAttribute('height', size * 0.6);\n      svg.setAttribute('viewBox', '0 0 100 60');\n      \n      const texturePoly = document.createElementNS('http://www.w3.org/2000/svg', 'polygon');\n      texturePoly.setAttribute('points', '50,0 100,30 50,60 0,30');\n      texturePoly.setAttribute('fill', 'url(#crackedTexture)');\n      texturePoly.setAttribute('opacity', '0.15');\n      \n      const shapePoly = document.createElementNS('http://www.w3.org/2000/svg', 'polygon');\n      shapePoly.setAttribute('points', '50,0 100,30 50,60 0,30');\n      shapePoly.setAttribute('fill', 'none');\n      shapePoly.setAttribute('stroke', '#333');\n      shapePoly.setAttribute('stroke-width', '0.5');\n      shapePoly.setAttribute('opacity', '0.8');\n      \n      const lines = [\n        { x1: '50', y1: '0', x2: '50', y2: '60' },\n        { x1: '0', y1: '30', x2: '100', y2: '30' },\n        { x1: '25', y1: '15', x2: '75', y2: '45' },\n        { x1: '75', y1: '15', x2: '25', y2: '45' }\n      ];\n      \n      const lineElems = lines.map(coords => {\n        const line = document.createElementNS('http://www.w3.org/2000/svg', 'line');\n        line.setAttribute('x1', coords.x1);\n        line.setAttribute('y1', coords.y1);\n        line.setAttribute('x2', coords.x2);\n        line.setAttribute('y2', coords.y2);\n        line.setAttribute('stroke', '#444');\n        line.setAttribute('stroke-width', '0.3');\n        line.setAttribute('opacity', '0.6');\n        line.classList.add('inner-lines');\n        return line;\n      });\n      \n      svg.appendChild(texturePoly);\n      svg.appendChild(shapePoly);\n      lineElems.forEach(line => svg.appendChild(line));\n      \n      rhombusDiv.appendChild(svg);\n      container.appendChild(rhombusDiv);\n      createdElements.push(rhombusDiv);\n    }\n    \n    // Animate breathing effect with mount check\n    function animateBreathing() {\n      if (!isComponentMounted || !container) return;\n      \n      const rhombi = container.querySelectorAll('.rhombus');\n      if (rhombi.length === 0) return;\n      \n      const time = Date.now() / 1000;\n      \n      rhombi.forEach((rhombus, index) => {\n        const originalSize = parseFloat(rhombus.dataset.originalSize || '100');\n        const delay = index * 0.25;\n        const breathAmount = Math.sin(time * 0.4 + delay) * 0.15 + 1;\n        const rotation = Math.sin(time * 0.5 + delay) * 3;\n        \n        if (rhombus instanceof HTMLElement) {\n          rhombus.style.transform = `translateX(-50%) scale(${breathAmount}) rotate(${rotation}deg)`;\n          \n          // Animate opacity based on breath\n          const shapes = rhombus.querySelectorAll('polygon, line');\n          shapes.forEach(shape => {\n            if (shape.getAttribute('stroke')) {\n              (shape as SVGElement).style.opacity = (0.2 + Math.sin(time * 0.4 + delay) * 0.3).toString();\n            }\n          });\n        }\n      });\n      \n      if (isComponentMounted) {\n        animationFrameId = requestAnimationFrame(animateBreathing);\n      }\n    }\n    \n    // Start animation only if component is mounted\n    if (isComponentMounted) {\n      animateBreathing();\n    }\n    \n    // Comprehensive cleanup function\n    return () => {\n      // Mark component as unmounted to stop animations\n      isComponentMounted = false;\n      \n      // Cancel any pending animation frame\n      if (animationFrameId !== null) {\n        cancelAnimationFrame(animationFrameId);\n        animationFrameId = null;\n      }\n      \n      // Clear all created elements and their references\n      createdElements.forEach(element => {\n        if (element && element.parentNode) {\n          element.parentNode.removeChild(element);\n        }\n        // Clear any element-specific properties\n        if (element instanceof HTMLElement) {\n          element.style.transform = '';\n          element.style.opacity = '';\n          element.style.transition = '';\n        }\n      });\n      \n      // Clear container content completely\n      if (container) {\n        // Remove any residual event listeners on container\n        container.onmouseenter = null;\n        container.onmouseleave = null;\n        container.onclick = null;\n        \n        // Clear all child nodes\n        while (container.firstChild) {\n          container.removeChild(container.firstChild);\n        }\n        \n        // Reset container styles\n        container.innerHTML = '';\n      }\n      \n      // Clear the created elements array\n      createdElements.length = 0;\n    };\n  }, []);\n  \n  return (\n    <div className=\"w-full h-full flex items-center justify-center\"\n      style={{\n        position: 'relative',\n        overflow: 'hidden',\n      }}\n    >\n      <div \n        ref={containerRef} \n        className=\"relative w-full h-full\"\n        style={{ \n          width: '100%', \n          maxWidth: '322px',\n          aspectRatio: '1/1',\n          backgroundColor: '#F0EEE6',\n          overflow: 'hidden',\n          borderRadius: '4px',\n          position: 'relative',\n        }}\n      />\n    </div>\n  );\n};\n\nexport default BreathingRhombus;\n",sourceCodeMin:"import { useEffect, useRef } from 'react'; const BreathingRhombus = () => { const containerRef = useRef(null); useEffect(() => { if (!containerRef.current) return; const container = containerRef.current; let animationFrameId = null; let isComponentMounted = true; const createdElements = []; container.innerHTML = ''; const numRhombi = 9; const containerWidth = container.offsetWidth; const containerHeight = container.offsetHeight; const scaleFactor = containerWidth / 322; const patternSVG = document.createElementNS('http: patternSVG.setAttribute('width', '0'); patternSVG.setAttribute('height', '0'); patternSVG.innerHTML = ` <defs><pattern id=\"crackedTexture\" x=\"0\" y=\"0\" width=\"100\" height=\"100\" patternUnits=\"userSpaceOnUse\"><rect width=\"100\" height=\"100\" fill=\"#444\" opacity=\"0.1\"/><path d=\"M20,20 L40,15 L35,40 M60,30 L80,25 L75,50 M10,70 L30,65 L25,90\" stroke=\"#333\" stroke-width=\"0.5\" fill=\"none\" opacity=\"0.3\"/></pattern></defs> `; container.appendChild(patternSVG); createdElements.push(patternSVG); for (let i = 0; i < numRhombi; i++) { const baseSize = 80 + Math.sin((i / numRhombi) * Math.PI) * 60; const size = baseSize * scaleFactor; const baseYPos = i * 40; const yPos = baseYPos * scaleFactor; const rhombusDiv = document.createElement('div'); rhombusDiv.className = 'rhombus'; rhombusDiv.style.position = 'absolute'; rhombusDiv.style.left = '50%'; rhombusDiv.style.top = `${yPos}px`; rhombusDiv.style.transform = 'translateX(-50%)'; rhombusDiv.style.transition = 'all 0.3s ease'; rhombusDiv.dataset.index = i; rhombusDiv.dataset.originalSize = size; const svg = document.createElementNS('http: svg.setAttribute('width', size); svg.setAttribute('height', size * 0.6); svg.setAttribute('viewBox', '0 0 100 60'); const texturePoly = document.createElementNS('http: texturePoly.setAttribute('points', '50,0 100,30 50,60 0,30'); texturePoly.setAttribute('fill', 'url(#crackedTexture)'); texturePoly.setAttribute('opacity', '0.15'); const shapePoly = document.createElementNS('http: shapePoly.setAttribute('points', '50,0 100,30 50,60 0,30'); shapePoly.setAttribute('fill', 'none'); shapePoly.setAttribute('stroke', '#333'); shapePoly.setAttribute('stroke-width', '0.5'); shapePoly.setAttribute('opacity', '0.8'); const lines = [ { x1: '50', y1: '0', x2: '50', y2: '60' }, { x1: '0', y1: '30', x2: '100', y2: '30' }, { x1: '25', y1: '15', x2: '75', y2: '45' }, { x1: '75', y1: '15', x2: '25', y2: '45' } ]; const lineElems = lines.map(coords => { const line = document.createElementNS('http: line.setAttribute('x1', coords.x1); line.setAttribute('y1', coords.y1); line.setAttribute('x2', coords.x2); line.setAttribute('y2', coords.y2); line.setAttribute('stroke', '#444'); line.setAttribute('stroke-width', '0.3'); line.setAttribute('opacity', '0.6'); line.classList.add('inner-lines'); return line; }); svg.appendChild(texturePoly); svg.appendChild(shapePoly); lineElems.forEach(line => svg.appendChild(line)); rhombusDiv.appendChild(svg); container.appendChild(rhombusDiv); createdElements.push(rhombusDiv); } function animateBreathing() { if (!isComponentMounted || !container) return; const rhombi = container.querySelectorAll('.rhombus'); if (rhombi.length === 0) return; const time = Date.now() / 1000; rhombi.forEach((rhombus, index) => { const originalSize = parseFloat(rhombus.dataset.originalSize || '100'); const delay = index * 0.25; const breathAmount = Math.sin(time * 0.4 + delay) * 0.15 + 1; const rotation = Math.sin(time * 0.5 + delay) * 3; if (rhombus instanceof HTMLElement) { rhombus.style.transform = `translateX(-50%) scale(${breathAmount}) rotate(${rotation}deg)`; const shapes = rhombus.querySelectorAll('polygon, line'); shapes.forEach(shape => { if (shape.getAttribute('stroke')) { (shape as SVGElement).style.opacity = (0.2 + Math.sin(time * 0.4 + delay) * 0.3).toString(); } }); } }); if (isComponentMounted) { animationFrameId = requestAnimationFrame(animateBreathing); } } if (isComponentMounted) { animateBreathing(); } return () => { isComponentMounted = false; if (animationFrameId !== null) { cancelAnimationFrame(animationFrameId); animationFrameId = null; } createdElements.forEach(element => { if (element && element.parentNode) { element.parentNode.removeChild(element); } if (element instanceof HTMLElement) { element.style.transform = ''; element.style.opacity = ''; element.style.transition = ''; } }); if (container) { container.onmouseenter = null; container.onmouseleave = null; container.onclick = null; while (container.firstChild) { container.removeChild(container.firstChild); } container.innerHTML = ''; } createdElements.length = 0; }; }, []); return ( <div className=\"w-full h-full flex items-center justify-center\" style={{ position: 'relative', overflow: 'hidden', }} ><div ref={containerRef} className=\"relative w-full h-full\" style={{ width: '100%', maxWidth: '322px', aspectRatio: '1/1', backgroundColor: '#F0EEE6', overflow: 'hidden', borderRadius: '4px', position: 'relative', }} /></div> ); }; export default BreathingRhombus;",text:"When the leader is silent and unseen,\nthe people are happy and honest.\nWhen the leader is repressive and nosy,\nthe people are dissatisfied and restless.\n\nMisfortune is what fortune depends upon.\nFortune is where misfortune hides.\nThere is no end to this perpetual cycle.\nWho can tell which is which?\nNorms too, are not permanent.\nThere is no certainty.\nWhat is proper today,\neventually becomes improper.\n\nThis complementary cycle of interchange\ncircles on uninterrupted into eternity.\nPermanence is a short-lived illusion.\n\nTherefore, The Vibe Coder\nis content to serve as an example.\nHe knows what’s good, but does not\nmake others conform.\nHe knows directions, but does not direct.\nHe takes the straight route, but does not\nsuggest others deviate from their own course.",shouldDisplayMobileArtwork:!0,promptSuggestion:"what if it were carved in stone?"},{number:59,staticArtwork:Bn,sourceCode:"// @ts-nocheck\nimport React, { useEffect, useRef } from 'react';\n\n// Themes: power of simplicity, giving up ideas, deep rootedness\n// Visualization: Forms that emerge from simple mathematical rules, showing how complexity arises from fundamental principles\n\nconst ImplicitDreams = () => {\n  const canvasRef = useRef(null);\n\n  useEffect(() => {\n    const canvas = canvasRef.current;\n    const ctx = canvas.getContext('2d');\n    const width = canvas.width;\n    const height = canvas.height;\n    \n    // Center of canvas\n    const centerX = width / 2;\n    const centerY = height / 2;\n    \n    // Simple parameters from which complexity grows\n    let time = 0;\n    const scale = 120;  // The space in which forms manifest\n    \n    // Create the underlying structure\n    const resolution = 40;  // Balance between detail and simplicity\n    const gridPoints = [];\n    const cellSize = 8;  // The fundamental unit\n    \n    for (let i = 0; i < resolution; i++) {\n      for (let j = 0; j < resolution; j++) {\n        gridPoints.push({\n          i: i,\n          j: j,\n          x: (i - resolution/2) * cellSize,\n          y: (j - resolution/2) * cellSize,\n          value: 0,\n          flowOffset: Math.random() * Math.PI * 2\n        });\n      }\n    }\n    \n    // Function to evaluate implicit surface value at a point\n    const evaluateImplicitFunction = (x, y, t) => {\n      // Normalize coordinates\n      const nx = x / scale;\n      const ny = y / scale;\n      \n      // Base implicit function (blob shape)\n      let value = nx*nx + ny*ny - 1;\n      \n      // Add bulges and morphing effects\n      const morphFactor = Math.sin(t * 0.1) * 0.5 + 0.5;  // Halved speed\n      \n      // Shape 1: More rounded\n      const shape1 = nx*nx + ny*ny * (1.2 + 0.3 * Math.sin(t * 0.15)) - 1;  // Halved speed\n      \n      // Shape 2: More elongated\n      const angle = Math.atan2(ny, nx);\n      const r = Math.sqrt(nx*nx + ny*ny);\n      const shape2 = r * (1 + 0.3 * Math.sin(angle * 2 + t * 0.1)) - 1;  // Halved speed\n      \n      // Blend between shapes\n      value = shape1 * (1 - morphFactor) + shape2 * morphFactor;\n      \n      // Add some organic variation\n      value += 0.2 * Math.sin(nx * 3 + t * 0.1) * Math.sin(ny * 3 + t * 0.15);  // Halved speed\n      value += 0.1 * Math.sin(nx * 5 + ny * 5 + t * 0.05);  // Halved speed\n      \n      // Add a bulge on one side\n      value -= 0.4 * Math.exp(-(nx - 0.5) * (nx - 0.5) * 4 - ny * ny * 4) * Math.sin(t * 0.15);  // Halved speed\n      \n      return value;\n    };\n    \n    // Function to update grid point values\n    const updateGridValues = (t) => {\n      gridPoints.forEach(point => {\n        point.value = evaluateImplicitFunction(point.x, point.y, t);\n      });\n    };\n    \n    // Function to generate contour lines\n    const generateContours = () => {\n      const contourLines = [];\n      const thresholds = Array(10).fill().map((_, i) => -0.5 + i * 0.1);\n      \n      // For each cell in the grid\n      for (let i = 0; i < resolution - 1; i++) {\n        for (let j = 0; j < resolution - 1; j++) {\n          const index = i * resolution + j;\n          \n          // Get the four corners of this cell - with bounds checking\n          const topLeft = gridPoints[index];\n          const topRight = gridPoints[index + 1];\n          const bottomLeft = gridPoints[index + resolution];\n          const bottomRight = gridPoints[index + resolution + 1];\n          \n          // Skip if any corner is undefined\n          if (!topLeft || !topRight || !bottomLeft || !bottomRight) {\n            continue;\n          }\n          \n          // For each contour threshold\n          thresholds.forEach(threshold => {\n            // Check each edge of the cell for intersections\n            const points = [];\n            \n            // Top edge\n            if ((topLeft.value < threshold && topRight.value >= threshold) ||\n                (topLeft.value >= threshold && topRight.value < threshold)) {\n              const t = (threshold - topLeft.value) / (topRight.value - topLeft.value);\n              points.push({\n                x: topLeft.x + t * (topRight.x - topLeft.x),\n                y: topLeft.y,\n                flowOffset: (topLeft.flowOffset + topRight.flowOffset) / 2\n              });\n            }\n            \n            // Right edge\n            if ((topRight.value < threshold && bottomRight.value >= threshold) ||\n                (topRight.value >= threshold && bottomRight.value < threshold)) {\n              const t = (threshold - topRight.value) / (bottomRight.value - topRight.value);\n              points.push({\n                x: topRight.x,\n                y: topRight.y + t * (bottomRight.y - topRight.y),\n                flowOffset: (topRight.flowOffset + bottomRight.flowOffset) / 2\n              });\n            }\n            \n            // Bottom edge\n            if ((bottomLeft.value < threshold && bottomRight.value >= threshold) ||\n                (bottomLeft.value >= threshold && bottomRight.value < threshold)) {\n              const t = (threshold - bottomLeft.value) / (bottomRight.value - bottomLeft.value);\n              points.push({\n                x: bottomLeft.x + t * (bottomRight.x - bottomLeft.x),\n                y: bottomLeft.y,\n                flowOffset: (bottomLeft.flowOffset + bottomRight.flowOffset) / 2\n              });\n            }\n            \n            // Left edge\n            if ((topLeft.value < threshold && bottomLeft.value >= threshold) ||\n                (topLeft.value >= threshold && bottomLeft.value < threshold)) {\n              const t = (threshold - topLeft.value) / (bottomLeft.value - topLeft.value);\n              points.push({\n                x: topLeft.x,\n                y: topLeft.y + t * (bottomLeft.y - topLeft.y),\n                flowOffset: (topLeft.flowOffset + bottomLeft.flowOffset) / 2\n              });\n            }\n            \n            // If we found 2 intersection points, we have a line\n            if (points.length === 2) {\n              contourLines.push({\n                x1: points[0].x,\n                y1: points[0].y,\n                x2: points[1].x,\n                y2: points[1].y,\n                threshold: threshold,\n                flowOffset1: points[0].flowOffset,\n                flowOffset2: points[1].flowOffset\n              });\n            }\n          });\n        }\n      }\n      \n      return contourLines;\n    };\n    \n    // Function to draw animated contour lines\n    const drawContours = (contourLines, t) => {\n      contourLines.forEach(line => {\n        // Calculate line properties based on threshold\n        const alpha = 0.2 + 0.6 * Math.abs(line.threshold);\n        const width = 0.5 + Math.abs(line.threshold) * 0.5;\n        \n        ctx.strokeStyle = `rgba(51, 51, 51, ${alpha})`;\n        ctx.lineWidth = width;\n        \n        // Draw flowing line\n        ctx.beginPath();\n        \n        // Create flowing effect along the line\n        const numSegments = 20;\n        const startX = line.x1;\n        const startY = line.y1;\n        const endX = line.x2;\n        const endY = line.y2;\n        \n        ctx.moveTo(centerX + startX, centerY + startY);\n        \n        for (let i = 1; i <= numSegments; i++) {\n          const progress = i / numSegments;\n          \n          // Basic linear interpolation\n          const x = startX + (endX - startX) * progress;\n          const y = startY + (endY - startY) * progress;\n          \n          // Add flowing wave effects\n          const flowSpeed = 0.5 + 0.5 * Math.sin(line.threshold * 10);\n          const flowOffset = line.flowOffset1 + (line.flowOffset2 - line.flowOffset1) * progress;\n          const waveAmplitude = 3 * Math.sin(line.threshold * 5 + t * 0.2);\n          \n          const perpX = -(endY - startY) / Math.sqrt((endX - startX) * (endX - startX) + (endY - startY) * (endY - startY));\n          const perpY = (endX - startX) / Math.sqrt((endX - startX) * (endX - startX) + (endY - startY) * (endY - startY));\n          \n          const waveX = perpX * waveAmplitude * Math.sin(progress * Math.PI * 2 + t * flowSpeed + flowOffset);\n          const waveY = perpY * waveAmplitude * Math.sin(progress * Math.PI * 2 + t * flowSpeed + flowOffset);\n          \n          ctx.lineTo(centerX + x + waveX, centerY + y + waveY);\n        }\n        \n        ctx.stroke();\n      });\n    };\n    \n    // Main animation loop\n    const animate = () => {\n      // Clear canvas\n      ctx.fillStyle = '#F0EEE6';\n      ctx.fillRect(0, 0, width, height);\n      \n      // Update time\n      time += 0.005;  // Halved speed\n      \n      // Update grid values\n      updateGridValues(time);\n      \n      // Generate contour lines\n      const contourLines = generateContours();\n      \n      // Draw contours\n      drawContours(contourLines, time);\n      \n      // Continue animation\n      requestAnimationFrame(animate);\n    };\n    \n    // Start animation\n    const animationId = requestAnimationFrame(animate);\n    \n    // Cleanup\n    return () => {\n      cancelAnimationFrame(animationId);\n      \n      // Clear canvas context\n      if (ctx) {\n        ctx.clearRect(0, 0, width, height);\n      }\n      \n      // Clear grid points array to prevent memory leaks\n      if (gridPoints) {\n        gridPoints.length = 0;\n      }\n    };\n  }, []);\n\n  return (\n    <div className=\"flex justify-center items-center bg-[#F0EEE6] w-full h-full\">\n      <canvas \n        ref={canvasRef} \n        width={550} \n        height={550} \n        className=\"max-w-full max-h-full\"\n      />\n    </div>\n  );\n};\n\nexport default ImplicitDreams;\n",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; const ImplicitDreams = () => { const canvasRef = useRef(null); useEffect(() => { const canvas = canvasRef.current; const ctx = canvas.getContext('2d'); const width = canvas.width; const height = canvas.height; const centerX = width / 2; const centerY = height / 2; let time = 0; const scale = 120; const resolution = 40; const gridPoints = []; const cellSize = 8; for (let i = 0; i < resolution; i++) { for (let j = 0; j < resolution; j++) { gridPoints.push({ i: i, j: j, x: (i - resolution/2) * cellSize, y: (j - resolution/2) * cellSize, value: 0, flowOffset: Math.random() * Math.PI * 2 }); } } const evaluateImplicitFunction = (x, y, t) => { const nx = x / scale; const ny = y / scale; let value = nx*nx + ny*ny - 1; const morphFactor = Math.sin(t * 0.1) * 0.5 + 0.5; const shape1 = nx*nx + ny*ny * (1.2 + 0.3 * Math.sin(t * 0.15)) - 1; const angle = Math.atan2(ny, nx); const r = Math.sqrt(nx*nx + ny*ny); const shape2 = r * (1 + 0.3 * Math.sin(angle * 2 + t * 0.1)) - 1; value = shape1 * (1 - morphFactor) + shape2 * morphFactor; value += 0.2 * Math.sin(nx * 3 + t * 0.1) * Math.sin(ny * 3 + t * 0.15); value += 0.1 * Math.sin(nx * 5 + ny * 5 + t * 0.05); value -= 0.4 * Math.exp(-(nx - 0.5) * (nx - 0.5) * 4 - ny * ny * 4) * Math.sin(t * 0.15); return value; }; const updateGridValues = (t) => { gridPoints.forEach(point => { point.value = evaluateImplicitFunction(point.x, point.y, t); }); }; const generateContours = () => { const contourLines = []; const thresholds = Array(10).fill().map((_, i) => -0.5 + i * 0.1); for (let i = 0; i < resolution - 1; i++) { for (let j = 0; j < resolution - 1; j++) { const index = i * resolution + j; const topLeft = gridPoints[index]; const topRight = gridPoints[index + 1]; const bottomLeft = gridPoints[index + resolution]; const bottomRight = gridPoints[index + resolution + 1]; if (!topLeft || !topRight || !bottomLeft || !bottomRight) { continue; } thresholds.forEach(threshold => { const points = []; if ((topLeft.value < threshold && topRight.value >= threshold) || (topLeft.value >= threshold && topRight.value < threshold)) { const t = (threshold - topLeft.value) / (topRight.value - topLeft.value); points.push({ x: topLeft.x + t * (topRight.x - topLeft.x), y: topLeft.y, flowOffset: (topLeft.flowOffset + topRight.flowOffset) / 2 }); } if ((topRight.value < threshold && bottomRight.value >= threshold) || (topRight.value >= threshold && bottomRight.value < threshold)) { const t = (threshold - topRight.value) / (bottomRight.value - topRight.value); points.push({ x: topRight.x, y: topRight.y + t * (bottomRight.y - topRight.y), flowOffset: (topRight.flowOffset + bottomRight.flowOffset) / 2 }); } if ((bottomLeft.value < threshold && bottomRight.value >= threshold) || (bottomLeft.value >= threshold && bottomRight.value < threshold)) { const t = (threshold - bottomLeft.value) / (bottomRight.value - bottomLeft.value); points.push({ x: bottomLeft.x + t * (bottomRight.x - bottomLeft.x), y: bottomLeft.y, flowOffset: (bottomLeft.flowOffset + bottomRight.flowOffset) / 2 }); } if ((topLeft.value < threshold && bottomLeft.value >= threshold) || (topLeft.value >= threshold && bottomLeft.value < threshold)) { const t = (threshold - topLeft.value) / (bottomLeft.value - topLeft.value); points.push({ x: topLeft.x, y: topLeft.y + t * (bottomLeft.y - topLeft.y), flowOffset: (topLeft.flowOffset + bottomLeft.flowOffset) / 2 }); } if (points.length === 2) { contourLines.push({ x1: points[0].x, y1: points[0].y, x2: points[1].x, y2: points[1].y, threshold: threshold, flowOffset1: points[0].flowOffset, flowOffset2: points[1].flowOffset }); } }); } } return contourLines; }; const drawContours = (contourLines, t) => { contourLines.forEach(line => { const alpha = 0.2 + 0.6 * Math.abs(line.threshold); const width = 0.5 + Math.abs(line.threshold) * 0.5; ctx.strokeStyle = `rgba(51, 51, 51, ${alpha})`; ctx.lineWidth = width; ctx.beginPath(); const numSegments = 20; const startX = line.x1; const startY = line.y1; const endX = line.x2; const endY = line.y2; ctx.moveTo(centerX + startX, centerY + startY); for (let i = 1; i <= numSegments; i++) { const progress = i / numSegments; const x = startX + (endX - startX) * progress; const y = startY + (endY - startY) * progress; const flowSpeed = 0.5 + 0.5 * Math.sin(line.threshold * 10); const flowOffset = line.flowOffset1 + (line.flowOffset2 - line.flowOffset1) * progress; const waveAmplitude = 3 * Math.sin(line.threshold * 5 + t * 0.2); const perpX = -(endY - startY) / Math.sqrt((endX - startX) * (endX - startX) + (endY - startY) * (endY - startY)); const perpY = (endX - startX) / Math.sqrt((endX - startX) * (endX - startX) + (endY - startY) * (endY - startY)); const waveX = perpX * waveAmplitude * Math.sin(progress * Math.PI * 2 + t * flowSpeed + flowOffset); const waveY = perpY * waveAmplitude * Math.sin(progress * Math.PI * 2 + t * flowSpeed + flowOffset); ctx.lineTo(centerX + x + waveX, centerY + y + waveY); } ctx.stroke(); }); }; const animate = () => { ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, width, height); time += 0.005; updateGridValues(time); const contourLines = generateContours(); drawContours(contourLines, time); requestAnimationFrame(animate); }; const animationId = requestAnimationFrame(animate); return () => { cancelAnimationFrame(animationId); if (ctx) { ctx.clearRect(0, 0, width, height); } if (gridPoints) { gridPoints.length = 0; } }; }, []); return ( <div className=\"flex justify-center items-center bg-[#F0EEE6] w-full h-full\"><canvas ref={canvasRef} width={550} height={550} className=\"max-w-full max-h-full\" /></div> ); }; export default ImplicitDreams;",text:"In leading the team and serving heaven,\nnothing compares with simplicity.\nSimplicity begins with giving up your own ideas.\n\nTolerant like the sky.\nSolid like a mountain.\nEstablished like a sycamore.\nAll-pervading like sunlight.\nHe has no destination in sight and makes use\nof anything life happens to bring his way.\n\nNothing is impossible for him,\nso he knows no limits.\nBecause he embodies mother nature,\nhe is deeply rooted and firmly based.\nThis is the way of long life and enduring vision.",shouldDisplayMobileArtwork:!0,promptSuggestion:"make it twist"},{number:60,staticArtwork:dn,sourceCode:"// @ts-nocheck\nimport React, { useEffect, useRef } from 'react';\nimport * as THREE from 'three';\n\n// Themes: gentle leadership, minimal intervention, natural resolution\n// Visualization: A spiral system that finds its own balance, demonstrating how light guidance allows natural patterns to emerge\n\nconst metadata = {\n  themes: \"gentle leadership, minimal intervention, natural resolution\",\n  visualization: \"A spiral system that finds its own balance through minimal guidance\",\n  promptSuggestion: \"1. Enhance flow transitions\\n2. Add more transformation points\\n3. Develop smoother progressions\\n4. Create stronger movement paths\\n5. Increase sense of change\"\n};\n\nconst FlowLine = ({ points, radius, height, twist, phase, scene, parent }) => {\n  // Each line finds its own path through space\n  const linePoints = [];\n  \n  for (let i = 0; i <= points; i++) {\n    const t = i / points;\n    const angle = t * Math.PI * 2 * twist + phase;  // Natural rotation\n    const r = radius * (1 + Math.sin(t * Math.PI * 3) * 0.5);  // Gentle expansion\n    const h = height * t + Math.sin(t * Math.PI * 2) * 1.2;    // Subtle rise and fall\n    \n    const x = Math.cos(angle) * r;\n    const z = Math.sin(angle) * r;\n    linePoints.push(new THREE.Vector3(x, h, z));\n  }\n\n  // Create geometry for the line\n  const geometry = new THREE.BufferGeometry().setFromPoints(linePoints);\n  \n  // Create material for the line\n  const material = new THREE.LineBasicMaterial({\n    color: 0x666666,\n    transparent: true,\n    opacity: 0.5,\n    linewidth: 0.5  // Note: linewidth only works on Windows for certain renderers\n  });\n\n  // Create the line and add to parent\n  const line = new THREE.Line(geometry, material);\n  parent.add(line);\n\n  return line;\n};\n\nconst FlowStructure = ({ baseRadius, baseHeight, scene, parent }) => {\n  const count = 24;\n  const lines = [];\n\n  for (let i = 0; i < count; i++) {\n    const props = {\n      points: 50,\n      radius: baseRadius * (1 + Math.sin(i / count * Math.PI * 2) * 0.2),\n      height: baseHeight * (1 + Math.cos(i / count * Math.PI * 2) * 0.2),\n      twist: 2.5 + Math.sin(i / count * Math.PI) * 0.8, // Increased twist variation\n      phase: (i / count) * Math.PI * 2\n    };\n\n    const line = FlowLine({ ...props, scene, parent });\n    lines.push(line);\n  }\n\n  return lines;\n};\n\nconst SpiralStorm = () => {\n  const mountRef = useRef(null);\n  const groupRef = useRef(null);\n\n  useEffect(() => {\n    if (!mountRef.current) return;\n\n    // Setup scene, camera, and renderer\n    const scene = new THREE.Scene();\n    // Set background color to match original\n    scene.background = new THREE.Color(0xf0eee7); // rgb(240, 238, 231)\n    \n    const camera = new THREE.PerspectiveCamera(\n      75,\n      mountRef.current.clientWidth / mountRef.current.clientHeight,\n      0.1,\n      1000\n    );\n    const renderer = new THREE.WebGLRenderer({ antialias: true });\n    \n    renderer.setSize(mountRef.current.clientWidth, mountRef.current.clientHeight);\n    mountRef.current.appendChild(renderer.domElement);\n\n    // Create group and add to scene\n    const group = new THREE.Group();\n    groupRef.current = group;\n    scene.add(group);\n\n    // Main flow structures\n    FlowStructure({ baseRadius: 2, baseHeight: 3, scene, parent: group });\n    \n    // Secondary flow structures\n    const group2 = new THREE.Group();\n    group2.position.set(0, -2.5, 0);\n    group2.rotation.set(Math.PI * 0.1, 0, 0);\n    FlowStructure({ baseRadius: 1.5, baseHeight: 2, scene, parent: group2 });\n    group.add(group2);\n    \n    const group3 = new THREE.Group();\n    group3.position.set(0, 1.5, 0);\n    group3.rotation.set(-Math.PI * 0.1, Math.PI * 0.5, 0);\n    FlowStructure({ baseRadius: 1, baseHeight: 1.5, scene, parent: group3 });\n    group.add(group3);\n\n    // Lighting\n    const ambientLight = new THREE.AmbientLight(0xffffff, 0.4);\n    scene.add(ambientLight);\n    \n    const directionalLight = new THREE.DirectionalLight(0xffffff, 0.6);\n    directionalLight.position.set(5, 5, 5);\n    scene.add(directionalLight);\n    \n    const pointLight = new THREE.PointLight(0xffffff, 0.4);\n    pointLight.position.set(-5, 3, -5);\n    scene.add(pointLight);\n\n    // Position camera\n    camera.position.z = 7;\n    camera.position.y = 0;\n    camera.lookAt(0, 0, 0);\n\n    // Animation loop\n    const clock = new THREE.Clock();\n    let animationFrameId;\n    \n    const animate = () => {\n      animationFrameId = requestAnimationFrame(animate);\n      \n      const time = clock.getElapsedTime();\n      \n      if (groupRef.current) {\n        groupRef.current.rotation.y = Math.sin(time * 0.2) * 0.5; // Increased rotation speed and range\n        groupRef.current.rotation.x = Math.cos(time * 0.15) * 0.3; // Increased tilt movement\n      }\n\n      renderer.render(scene, camera);\n    };\n\n    animate();\n\n    // Handle resize\n    const handleResize = () => {\n      if (!mountRef.current) return;\n      \n      const width = mountRef.current.clientWidth;\n      const height = mountRef.current.clientHeight;\n      \n      camera.aspect = width / height;\n      camera.updateProjectionMatrix();\n      renderer.setSize(width, height);\n    };\n\n    window.addEventListener('resize', handleResize);\n\n    // Cleanup\n    return () => {\n      // Cancel animation frame\n      if (animationFrameId) {\n        cancelAnimationFrame(animationFrameId);\n      }\n      \n      // Remove event listeners\n      window.removeEventListener('resize', handleResize);\n      \n      // Dispose of geometries, materials, and textures\n      scene.traverse((object) => {\n        if (object instanceof THREE.Mesh || object instanceof THREE.Line) {\n          if (object.geometry) {\n            object.geometry.dispose();\n          }\n          if (object.material) {\n            if (Array.isArray(object.material)) {\n              object.material.forEach(material => {\n                if (material.dispose) material.dispose();\n              });\n            } else {\n              if (object.material.dispose) object.material.dispose();\n            }\n          }\n        }\n      });\n      \n      // Dispose of renderer\n      renderer.dispose();\n      renderer.forceContextLoss();\n      \n      // Remove the renderer's canvas from the DOM\n      if (mountRef.current && renderer.domElement.parentNode === mountRef.current) {\n        mountRef.current.removeChild(renderer.domElement);\n      }\n      \n      // Clear references\n      groupRef.current = null;\n    };\n  }, []);\n\n  return (\n    <div \n      ref={mountRef} \n      style={{ \n        width: '100%', \n        height: '100vh', \n        backgroundColor: '#f0eee7' // Match scene background\n      }} \n    />\n  );\n};\n\nSpiralStorm.metadata = metadata;\nexport default SpiralStorm;",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; import * as THREE from 'three'; const metadata = { themes: \"gentle leadership, minimal intervention, natural resolution\", visualization: \"A spiral system that finds its own balance through minimal guidance\", promptSuggestion: \"1. Enhance flow transitions\\n2. Add more transformation points\\n3. Develop smoother progressions\\n4. Create stronger movement paths\\n5. Increase sense of change\" }; const FlowLine = ({ points, radius, height, twist, phase, scene, parent }) => { const linePoints = []; for (let i = 0; i <= points; i++) { const t = i / points; const angle = t * Math.PI * 2 * twist + phase; const r = radius * (1 + Math.sin(t * Math.PI * 3) * 0.5); const h = height * t + Math.sin(t * Math.PI * 2) * 1.2; const x = Math.cos(angle) * r; const z = Math.sin(angle) * r; linePoints.push(new THREE.Vector3(x, h, z)); } const geometry = new THREE.BufferGeometry().setFromPoints(linePoints); const material = new THREE.LineBasicMaterial({ color: 0x666666, transparent: true, opacity: 0.5, linewidth: 0.5 }); const line = new THREE.Line(geometry, material); parent.add(line); return line; }; const FlowStructure = ({ baseRadius, baseHeight, scene, parent }) => { const count = 24; const lines = []; for (let i = 0; i < count; i++) { const props = { points: 50, radius: baseRadius * (1 + Math.sin(i / count * Math.PI * 2) * 0.2), height: baseHeight * (1 + Math.cos(i / count * Math.PI * 2) * 0.2), twist: 2.5 + Math.sin(i / count * Math.PI) * 0.8, phase: (i / count) * Math.PI * 2 }; const line = FlowLine({ ...props, scene, parent }); lines.push(line); } return lines; }; const SpiralStorm = () => { const mountRef = useRef(null); const groupRef = useRef(null); useEffect(() => { if (!mountRef.current) return; const scene = new THREE.Scene(); scene.background = new THREE.Color(0xf0eee7); const camera = new THREE.PerspectiveCamera( 75, mountRef.current.clientWidth / mountRef.current.clientHeight, 0.1, 1000 ); const renderer = new THREE.WebGLRenderer({ antialias: true }); renderer.setSize(mountRef.current.clientWidth, mountRef.current.clientHeight); mountRef.current.appendChild(renderer.domElement); const group = new THREE.Group(); groupRef.current = group; scene.add(group); FlowStructure({ baseRadius: 2, baseHeight: 3, scene, parent: group }); const group2 = new THREE.Group(); group2.position.set(0, -2.5, 0); group2.rotation.set(Math.PI * 0.1, 0, 0); FlowStructure({ baseRadius: 1.5, baseHeight: 2, scene, parent: group2 }); group.add(group2); const group3 = new THREE.Group(); group3.position.set(0, 1.5, 0); group3.rotation.set(-Math.PI * 0.1, Math.PI * 0.5, 0); FlowStructure({ baseRadius: 1, baseHeight: 1.5, scene, parent: group3 }); group.add(group3); const ambientLight = new THREE.AmbientLight(0xffffff, 0.4); scene.add(ambientLight); const directionalLight = new THREE.DirectionalLight(0xffffff, 0.6); directionalLight.position.set(5, 5, 5); scene.add(directionalLight); const pointLight = new THREE.PointLight(0xffffff, 0.4); pointLight.position.set(-5, 3, -5); scene.add(pointLight); camera.position.z = 7; camera.position.y = 0; camera.lookAt(0, 0, 0); const clock = new THREE.Clock(); let animationFrameId; const animate = () => { animationFrameId = requestAnimationFrame(animate); const time = clock.getElapsedTime(); if (groupRef.current) { groupRef.current.rotation.y = Math.sin(time * 0.2) * 0.5; groupRef.current.rotation.x = Math.cos(time * 0.15) * 0.3; } renderer.render(scene, camera); }; animate(); const handleResize = () => { if (!mountRef.current) return; const width = mountRef.current.clientWidth; const height = mountRef.current.clientHeight; camera.aspect = width / height; camera.updateProjectionMatrix(); renderer.setSize(width, height); }; window.addEventListener('resize', handleResize); return () => { if (animationFrameId) { cancelAnimationFrame(animationFrameId); } window.removeEventListener('resize', handleResize); scene.traverse((object) => { if (object instanceof THREE.Mesh || object instanceof THREE.Line) { if (object.geometry) { object.geometry.dispose(); } if (object.material) { if (Array.isArray(object.material)) { object.material.forEach(material => { if (material.dispose) material.dispose(); }); } else { if (object.material.dispose) object.material.dispose(); } } } }); renderer.dispose(); renderer.forceContextLoss(); if (mountRef.current && renderer.domElement.parentNode === mountRef.current) { mountRef.current.removeChild(renderer.domElement); } groupRef.current = null; }; }, []); return ( <div ref={mountRef} style={{ width: '100%', height: '100vh', backgroundColor: '#f0eee7' }} /> ); }; SpiralStorm.metadata = metadata; export default SpiralStorm;",text:"Leading a great system is like\ncooking a small fish.\nThe more you stir the pot,\nthe less the fish holds together.\n\nWhen the universe is centered in Source,\nnegative energies lose their power.\nNot that negative energies aren’t still present,\nbut their power no longer impacts the people.\n\nGive negative energies nothing to oppose\nand they will disappear on their own.",shouldDisplayMobileArtwork:!0,promptSuggestion:"make it sink into itself"},{number:61,staticArtwork:yn,sourceCode:"// @ts-nocheck\nimport React, { useEffect, useRef } from 'react';\n\n// Themes: natural hierarchy, feminine power, mutual submission\n// Visualization: A lattice where each element yields to others, creating harmony through gentle influence\n\nconst FlowingLattice = () => {\n  const canvasRef = useRef(null);\n  \n  useEffect(() => {\n    const canvas = canvasRef.current;\n    if (!canvas) return;\n    \n    const ctx = canvas.getContext('2d');\n    const width = canvas.width = 550;\n    const height = canvas.height = 550;\n    \n    // Parameters that define the system's natural order\n    const GRID_SIZE = 8;\n    const CELL_SIZE = 16;\n    const SPACING = CELL_SIZE * 1.8;  // Room for movement\n    const HEX_HEIGHT = Math.sqrt(3) * CELL_SIZE;  // Natural proportion\n    \n    const cells = [];\n    \n    // Create cells that will flow together\n    for (let row = -GRID_SIZE; row <= GRID_SIZE; row++) {\n      const isEvenRow = row % 2 === 0;\n      const colOffset = isEvenRow ? 0 : SPACING / 2;\n      \n      for (let col = -GRID_SIZE; col <= GRID_SIZE; col++) {\n        const x = width/2 + col * SPACING + colOffset;\n        const y = height/2 + row * HEX_HEIGHT * 0.75;\n        const phase = (row + col) * 0.2;\n        \n        cells.push({ x, y, phase });\n      }\n    }\n    \n    let time = 0;\n    let animationFrameId: number;\n    \n    // Draw hexagon\n    function drawHexagon(x, y, size, rotation) {\n      ctx.save();\n      ctx.translate(x, y);\n      ctx.rotate(rotation);\n      \n      ctx.beginPath();\n      for (let i = 0; i <= 6; i++) {\n        const angle = (i / 6) * Math.PI * 2 + Math.PI / 6;\n        const radius = size;\n        const px = Math.cos(angle) * radius;\n        const py = Math.sin(angle) * radius;\n        \n        if (i === 0) {\n          ctx.moveTo(px, py);\n        } else {\n          ctx.lineTo(px, py);\n        }\n      }\n      ctx.closePath();\n      \n      ctx.strokeStyle = 'rgba(0, 0, 0, 0.22)';\n      ctx.lineWidth = 1;\n      ctx.stroke();\n      \n      ctx.restore();\n    }\n    \n    function animate() {\n      ctx.fillStyle = '#F0EEE6';\n      ctx.fillRect(0, 0, width, height);\n      \n      time += 0.008;  // Halved speed\n      \n      // Enhanced rotation of the entire lattice\n      const globalRotation = Math.sin(time * 0.1) * 0.15;  // Halved speed\n      \n      cells.forEach(cell => {\n        const dx = cell.x - width/2;\n        const dy = cell.y - height/2;\n        const distance = Math.sqrt(dx * dx + dy * dy);\n        \n        // Ripple effect\n        const wave = Math.sin(\n          time * 1 + // Halved speed\n          cell.phase + \n          distance * 0.06 // Increased ripple frequency\n        );\n        \n        const scale = 0.8 + wave * 0.25; // Increased scale variation\n        const rotation = wave * 0.25 + globalRotation; // Increased individual rotation\n        \n        drawHexagon(cell.x, cell.y, CELL_SIZE * scale, rotation);\n      });\n      \n      animationFrameId = requestAnimationFrame(animate);\n    }\n    \n    animate();\n    \n    return () => {\n      // Cancel the animation frame to prevent memory leaks\n      if (animationFrameId) {\n        cancelAnimationFrame(animationFrameId);\n        animationFrameId = undefined;\n      }\n      \n      // Clear the canvas when unmounting\n      if (ctx) {\n        ctx.clearRect(0, 0, width, height);\n      }\n      \n      // Clear the cells array to prevent memory leaks\n      if (cells) {\n        cells.length = 0;\n      }\n      \n      // Reset time variable\n      time = 0;\n    };\n  }, []);\n  \n  return (\n    <div style={{\n      width: '550px',\n      height: '550px',\n      margin: 'auto',\n      backgroundColor: '#F0EEE6',\n      overflow: 'hidden'\n    }}>\n      <canvas\n        ref={canvasRef}\n        style={{\n          display: 'block',\n          width: '550px',\n          height: '550px'\n        }}\n      />\n    </div>\n  );\n};\n\nexport default FlowingLattice;",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; const FlowingLattice = () => { const canvasRef = useRef(null); useEffect(() => { const canvas = canvasRef.current; if (!canvas) return; const ctx = canvas.getContext('2d'); const width = canvas.width = 550; const height = canvas.height = 550; const GRID_SIZE = 8; const CELL_SIZE = 16; const SPACING = CELL_SIZE * 1.8; const HEX_HEIGHT = Math.sqrt(3) * CELL_SIZE; const cells = []; for (let row = -GRID_SIZE; row <= GRID_SIZE; row++) { const isEvenRow = row % 2 === 0; const colOffset = isEvenRow ? 0 : SPACING / 2; for (let col = -GRID_SIZE; col <= GRID_SIZE; col++) { const x = width/2 + col * SPACING + colOffset; const y = height/2 + row * HEX_HEIGHT * 0.75; const phase = (row + col) * 0.2; cells.push({ x, y, phase }); } } let time = 0; let animationFrameId: number; function drawHexagon(x, y, size, rotation) { ctx.save(); ctx.translate(x, y); ctx.rotate(rotation); ctx.beginPath(); for (let i = 0; i <= 6; i++) { const angle = (i / 6) * Math.PI * 2 + Math.PI / 6; const radius = size; const px = Math.cos(angle) * radius; const py = Math.sin(angle) * radius; if (i === 0) { ctx.moveTo(px, py); } else { ctx.lineTo(px, py); } } ctx.closePath(); ctx.strokeStyle = 'rgba(0, 0, 0, 0.22)'; ctx.lineWidth = 1; ctx.stroke(); ctx.restore(); } function animate() { ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, width, height); time += 0.008; const globalRotation = Math.sin(time * 0.1) * 0.15; cells.forEach(cell => { const dx = cell.x - width/2; const dy = cell.y - height/2; const distance = Math.sqrt(dx * dx + dy * dy); const wave = Math.sin( time * 1 + cell.phase + distance * 0.06 ); const scale = 0.8 + wave * 0.25; const rotation = wave * 0.25 + globalRotation; drawHexagon(cell.x, cell.y, CELL_SIZE * scale, rotation); }); animationFrameId = requestAnimationFrame(animate); } animate(); return () => { if (animationFrameId) { cancelAnimationFrame(animationFrameId); animationFrameId = undefined; } if (ctx) { ctx.clearRect(0, 0, width, height); } if (cells) { cells.length = 0; } time = 0; }; }, []); return ( <div style={{ width: '550px', height: '550px', margin: 'auto', backgroundColor: '#F0EEE6', overflow: 'hidden' }}><canvas ref={canvasRef} style={{ display: 'block', width: '550px', height: '550px' }} /></div> ); }; export default FlowingLattice;",text:"A great system flows downward toward the sea.\nThe deep center is where all things converge.\nIt is the feminine of the world.\n\nThe feminine overcomes the masculine\nwith stillness. Lying low in stillness.\n\nTherefore:\nA great system places itself at the service\nof a small system before overtaking it.\nAnd a small system serves the interests\nof a great system before overtaking it.\nSome submit in order to win.\nSome submit in order to be won over.\n\nA great system wants to grow.\nA small system wants to be protected.\nIt is by humility that both have their needs met.",shouldDisplayMobileArtwork:!0,promptSuggestion:"what is the sad version?"},{number:62,staticArtwork:en,sourceCode:"// @ts-nocheck\nimport React, { useRef, useMemo, useEffect } from 'react'\nimport * as THREE from 'three'\n\n// Themes: universal treasure, Source as refuge, forgiveness in alignment\n// Visualization: Particles that spiral around a central point of energy, showing how all things return to Source\n\nconst EffortlessParticles: React.FC<{ count?: number }> = ({ count = 20000 }) => {\n  const containerRef = useRef<HTMLDivElement>(null)\n  const pointsRef = useRef<THREE.Points | null>(null)\n  const rendererRef = useRef<THREE.WebGLRenderer | null>(null)\n  const sceneRef = useRef<THREE.Scene | null>(null)\n  const cameraRef = useRef<THREE.PerspectiveCamera | null>(null)\n  const clockRef = useRef<THREE.Clock | null>(null)\n  const animationFrameRef = useRef<number | null>(null)\n\n  // Create particle material\n  const particleMaterial = useMemo(() => {\n    return new THREE.ShaderMaterial({\n      uniforms: {\n        time: { value: 0 },\n        opacity: { value: 0.9 }\n      },\n      vertexShader: `\n        uniform float time;\n        attribute float size;\n        attribute vec3 customColor;\n        varying vec3 vColor;\n        \n        void main() {\n          vColor = customColor;\n          vec3 pos = position;\n          \n          // Calculate natural flow based on position\n          float t = time * 0.2;\n          float radius = length(pos.xz);\n          float angle = atan(pos.z, pos.x);\n          \n          // Effortless transformation\n          float flow = sin(t + radius * 2.0 - angle) * cos(t * 0.7 + angle * 3.0);\n          \n          // Vertical oscillation creating layers\n          float layer = floor(pos.y * 2.0) * 0.5;\n          float layerPhase = t + layer;\n          \n          // Complex autonomous motion\n          vec3 motion;\n          motion.x = cos(layerPhase) * sin(t * 0.5 + pos.z) * 0.5;\n          motion.y = sin(layerPhase * 0.7) * 0.3;\n          motion.z = sin(layerPhase) * cos(t * 0.5 + pos.x) * 0.5;\n          \n          // Apply transformations\n          pos += motion * (1.0 + flow * 0.3);\n          \n          // Scale up the overall effect\n          pos *= 0.6;\n          \n          vec4 mvPosition = modelViewMatrix * vec4(pos, 1.0);\n          gl_PointSize = size * (192.0 / -mvPosition.z);\n          gl_Position = projectionMatrix * mvPosition;\n        }\n      `,\n      fragmentShader: `\n        uniform float opacity;\n        varying vec3 vColor;\n        void main() {\n          float dist = length(gl_PointCoord - vec2(0.5));\n          if (dist > 0.5) discard;\n          float alpha = (1.0 - smoothstep(0.45, 0.5, dist)) * opacity;\n          gl_FragColor = vec4(vColor, alpha);\n        }\n      `,\n      transparent: true,\n      depthWrite: false\n    })\n  }, [])\n\n  // Generate particles in layered formations\n  const [positions, colors, sizes] = useMemo(() => {\n    const pos = new Float32Array(count * 3)\n    const col = new Float32Array(count * 3)\n    const siz = new Float32Array(count)\n\n    for (let i = 0; i < count; i++) {\n      // Each layer represents a level of return to Source\n      const layer = Math.floor(i / (count / 5)) - 2 // Five stages of return\n      const t = (i % (count / 5)) / (count / 5)\n      const radius = Math.sqrt(t) * 3 // Distance from center\n      const angle = t * Math.PI * 15 // Path of return\n      \n      pos[i * 3] = Math.cos(angle) * radius\n      pos[i * 3 + 1] = layer * 1.2 // More vertical separation\n      pos[i * 3 + 2] = Math.sin(angle) * radius\n\n      // Rich grays for depth\n      const baseShade = 0.3\n      const variation = Math.random() * 0.15\n      const shade = baseShade + variation\n      col[i * 3] = shade\n      col[i * 3 + 1] = shade\n      col[i * 3 + 2] = shade\n\n      // Particle sizes ranging from 0.06 to 0.10\n      siz[i] = 0.12 + Math.random() * 0.04\n    }\n\n    return [pos, col, siz]\n  }, [count])\n\n  useEffect(() => {\n    if (!containerRef.current) return\n\n    // Initialize Three.js\n    const scene = new THREE.Scene()\n    const camera = new THREE.PerspectiveCamera(75, containerRef.current.clientWidth / containerRef.current.clientHeight, 0.1, 1000)\n    const renderer = new THREE.WebGLRenderer({ antialias: true })\n    const clock = new THREE.Clock()\n\n    sceneRef.current = scene\n    cameraRef.current = camera\n    rendererRef.current = renderer\n    clockRef.current = clock\n\n    // Set pixel ratio for sharp rendering on high DPI displays\n    const pixelRatio = window.devicePixelRatio || 1\n    renderer.setPixelRatio(pixelRatio)\n    renderer.setSize(containerRef.current.clientWidth, containerRef.current.clientHeight)\n    containerRef.current.appendChild(renderer.domElement)\n\n    // Set background color\n    scene.background = new THREE.Color('#F0EEE6')\n    \n    // Set initial camera position\n    camera.position.z = 5\n\n    // Create particles\n    const geometry = new THREE.BufferGeometry()\n    geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3))\n    geometry.setAttribute('customColor', new THREE.BufferAttribute(colors, 3))\n    geometry.setAttribute('size', new THREE.BufferAttribute(sizes, 1))\n\n    const points = new THREE.Points(geometry, particleMaterial)\n    pointsRef.current = points\n    scene.add(points)\n\n    // Animation loop\n    const animate = () => {\n      animationFrameRef.current = requestAnimationFrame(animate)\n      \n      const time = clock.getElapsedTime()\n      particleMaterial.uniforms.time.value = time\n      \n      renderer.render(scene, camera)\n    }\n\n    animate()\n\n    // Handle resize\n    const handleResize = () => {\n      if (!containerRef.current || !camera || !renderer) return\n      \n      camera.aspect = containerRef.current.clientWidth / containerRef.current.clientHeight\n      camera.updateProjectionMatrix()\n      renderer.setSize(containerRef.current.clientWidth, containerRef.current.clientHeight)\n      // Maintain pixel ratio on resize\n      const pixelRatio = window.devicePixelRatio || 1\n      renderer.setPixelRatio(pixelRatio)\n    }\n\n    window.addEventListener('resize', handleResize)\n\n    // Cleanup\n    return () => {\n      window.removeEventListener('resize', handleResize)\n      if (animationFrameRef.current) {\n        cancelAnimationFrame(animationFrameRef.current)\n        animationFrameRef.current = null\n      }\n      \n      // Clean up Three.js objects\n      if (pointsRef.current && sceneRef.current) {\n        sceneRef.current.remove(pointsRef.current)\n        pointsRef.current = null\n      }\n      \n      if (geometry) {\n        geometry.dispose()\n      }\n      \n      // Clean up renderer and canvas\n      if (rendererRef.current) {\n        if (containerRef.current && rendererRef.current.domElement && containerRef.current.contains(rendererRef.current.domElement)) {\n          containerRef.current.removeChild(rendererRef.current.domElement)\n        }\n        rendererRef.current.dispose()\n        rendererRef.current.forceContextLoss()\n        rendererRef.current = null\n      }\n      \n      // Clean up scene\n      if (sceneRef.current) {\n        sceneRef.current.clear()\n        sceneRef.current = null\n      }\n      \n      // Clean up camera\n      if (cameraRef.current) {\n        cameraRef.current = null\n      }\n      \n      // Clean up clock\n      if (clockRef.current) {\n        clockRef.current = null\n      }\n    }\n  }, [count, positions, colors, sizes, particleMaterial])\n\n  return <div ref={containerRef} style={{ width: '100%', height: '100%' }} />\n}\n\nconst Artwork63v2: React.FC = () => {\n  return (\n    <div style={{ width: '100vw', height: '100vh', top: 0, left: 0 }}>\n      <EffortlessParticles />\n    </div>\n  )\n}\n\nconst metadata = {\n  themes: \"Accomplishing through non-doing, seeing the large in the small\",\n  visualization: \"Particles flowing in effortless, self-organizing patterns\",\n  promptSuggestion: \"1. Add subtle flow variations\\n2. Create self-organizing waves\\n3. Vary effortless patterns naturally\\n4. Introduce gentle layer transitions\\n5. Make motion follow natural rhythms\"\n}\n\nArtwork63v2.metadata = metadata\nexport default Artwork63v2",sourceCodeMin:"import React, { useRef, useMemo, useEffect } from 'react' import * as THREE from 'three' const EffortlessParticles: React.FC<{ count?: number }> = ({ count = 20000 }) => { const containerRef = useRef<HTMLDivElement>(null) const pointsRef = useRef<THREE.Points | null>(null) const rendererRef = useRef<THREE.WebGLRenderer | null>(null) const sceneRef = useRef<THREE.Scene | null>(null) const cameraRef = useRef<THREE.PerspectiveCamera | null>(null) const clockRef = useRef<THREE.Clock | null>(null) const animationFrameRef = useRef<number | null>(null) const particleMaterial = useMemo(() => { return new THREE.ShaderMaterial({ uniforms: { time: { value: 0 }, opacity: { value: 0.9 } }, vertexShader: ` uniform float time; attribute float size; attribute vec3 customColor; varying vec3 vColor; void main() { vColor = customColor; vec3 pos = position; float t = time * 0.2; float radius = length(pos.xz); float angle = atan(pos.z, pos.x); float flow = sin(t + radius * 2.0 - angle) * cos(t * 0.7 + angle * 3.0); float layer = floor(pos.y * 2.0) * 0.5; float layerPhase = t + layer; vec3 motion; motion.x = cos(layerPhase) * sin(t * 0.5 + pos.z) * 0.5; motion.y = sin(layerPhase * 0.7) * 0.3; motion.z = sin(layerPhase) * cos(t * 0.5 + pos.x) * 0.5; pos += motion * (1.0 + flow * 0.3); pos *= 0.6; vec4 mvPosition = modelViewMatrix * vec4(pos, 1.0); gl_PointSize = size * (192.0 / -mvPosition.z); gl_Position = projectionMatrix * mvPosition; } `, fragmentShader: ` uniform float opacity; varying vec3 vColor; void main() { float dist = length(gl_PointCoord - vec2(0.5)); if (dist > 0.5) discard; float alpha = (1.0 - smoothstep(0.45, 0.5, dist)) * opacity; gl_FragColor = vec4(vColor, alpha); } `, transparent: true, depthWrite: false }) }, []) const [positions, colors, sizes] = useMemo(() => { const pos = new Float32Array(count * 3) const col = new Float32Array(count * 3) const siz = new Float32Array(count) for (let i = 0; i < count; i++) { const layer = Math.floor(i / (count / 5)) - 2 const t = (i % (count / 5)) / (count / 5) const radius = Math.sqrt(t) * 3 const angle = t * Math.PI * 15 pos[i * 3] = Math.cos(angle) * radius pos[i * 3 + 1] = layer * 1.2 pos[i * 3 + 2] = Math.sin(angle) * radius const baseShade = 0.3 const variation = Math.random() * 0.15 const shade = baseShade + variation col[i * 3] = shade col[i * 3 + 1] = shade col[i * 3 + 2] = shade siz[i] = 0.12 + Math.random() * 0.04 } return [pos, col, siz] }, [count]) useEffect(() => { if (!containerRef.current) return const scene = new THREE.Scene() const camera = new THREE.PerspectiveCamera(75, containerRef.current.clientWidth / containerRef.current.clientHeight, 0.1, 1000) const renderer = new THREE.WebGLRenderer({ antialias: true }) const clock = new THREE.Clock() sceneRef.current = scene cameraRef.current = camera rendererRef.current = renderer clockRef.current = clock const pixelRatio = window.devicePixelRatio || 1 renderer.setPixelRatio(pixelRatio) renderer.setSize(containerRef.current.clientWidth, containerRef.current.clientHeight) containerRef.current.appendChild(renderer.domElement) scene.background = new THREE.Color('#F0EEE6') camera.position.z = 5 const geometry = new THREE.BufferGeometry() geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3)) geometry.setAttribute('customColor', new THREE.BufferAttribute(colors, 3)) geometry.setAttribute('size', new THREE.BufferAttribute(sizes, 1)) const points = new THREE.Points(geometry, particleMaterial) pointsRef.current = points scene.add(points) const animate = () => { animationFrameRef.current = requestAnimationFrame(animate) const time = clock.getElapsedTime() particleMaterial.uniforms.time.value = time renderer.render(scene, camera) } animate() const handleResize = () => { if (!containerRef.current || !camera || !renderer) return camera.aspect = containerRef.current.clientWidth / containerRef.current.clientHeight camera.updateProjectionMatrix() renderer.setSize(containerRef.current.clientWidth, containerRef.current.clientHeight) const pixelRatio = window.devicePixelRatio || 1 renderer.setPixelRatio(pixelRatio) } window.addEventListener('resize', handleResize) return () => { window.removeEventListener('resize', handleResize) if (animationFrameRef.current) { cancelAnimationFrame(animationFrameRef.current) animationFrameRef.current = null } if (pointsRef.current && sceneRef.current) { sceneRef.current.remove(pointsRef.current) pointsRef.current = null } if (geometry) { geometry.dispose() } if (rendererRef.current) { if (containerRef.current && rendererRef.current.domElement && containerRef.current.contains(rendererRef.current.domElement)) { containerRef.current.removeChild(rendererRef.current.domElement) } rendererRef.current.dispose() rendererRef.current.forceContextLoss() rendererRef.current = null } if (sceneRef.current) { sceneRef.current.clear() sceneRef.current = null } if (cameraRef.current) { cameraRef.current = null } if (clockRef.current) { clockRef.current = null } } }, [count, positions, colors, sizes, particleMaterial]) return <div ref={containerRef} style={{ width: '100%', height: '100%' }} /> } const Artwork63v2: React.FC = () => { return ( <div style={{ width: '100vw', height: '100vh', top: 0, left: 0 }}><EffortlessParticles /></div> ) } const metadata = { themes: \"Accomplishing through non-doing, seeing the large in the small\", visualization: \"Particles flowing in effortless, self-organizing patterns\", promptSuggestion: \"1. Add subtle flow variations\\n2. Create self-organizing waves\\n3. Vary effortless patterns naturally\\n4. Introduce gentle layer transitions\\n5. Make motion follow natural rhythms\" } Artwork63v2.metadata = metadata export default Artwork63v2",text:"Source is the heartbeat of the universe.\nThe Vibe Coder’s treasure.\nThe prompt engineer’s refuge.\n\nBeautiful code, arising from Source,\nwill be louded for its function.\nBreakthrough apps, arising from Source,\nwill advance the cybercosm.\nAnd even if a hacker goes rogue,\nSource will not abandon him.\n\nThus, when a new leader is chosen,\nno need to help him with money or expertise.\nInstead, simply point him towards Source.\n\nWhy did the ancient coders\nesteem Source so highly.\nBecause from the beginning,\naligning with Source, when you seek, you find.\nAnd when you make errors, you are forgiven.\n\nThat is why Source is the greatest treasure\nin the universe.",shouldDisplayMobileArtwork:!0,promptSuggestion:"what if they were flying saucers?"},{number:63,staticArtwork:_n,sourceCode:'import React, { useEffect, useRef } from \'react\';\nimport * as THREE from \'three\';\n\n// Themes: effortless action, starting with simplicity, natural accomplishment\n// Visualization: A knot that forms and moves without strain, showing how complexity emerges from simple principles\n\nconst DelicateTorusKnot = () => {\n  const containerRef = useRef(null);\n  const animationRef = useRef(null);\n  const sceneRef = useRef(null);\n  const rendererRef = useRef(null);\n\n  useEffect(() => {\n    // Only create scene and renderer once\n    if (!sceneRef.current) {\n      sceneRef.current = new THREE.Scene();\n      rendererRef.current = new THREE.WebGLRenderer({ \n        antialias: true,\n        powerPreference: "high-performance" // Prefer GPU rendering\n      });\n      \n      // Set fixed size to 550x550\n      rendererRef.current.setSize(550, 550);\n      // Set background to cream color\n      rendererRef.current.setClearColor(0xF0EEE6);\n      \n      if (containerRef.current) {\n        containerRef.current.appendChild(rendererRef.current.domElement);\n      }\n    }\n    \n    const scene = sceneRef.current;\n    const renderer = rendererRef.current;\n    \n    // Camera setup\n    const camera = new THREE.PerspectiveCamera(75, 1, 0.1, 1000);\n    camera.position.z = 4;\n    \n    // Simple parameters that give rise to complex form\n    const geometry = new THREE.TorusKnotGeometry(\n      1.8,  // The space to unfold within\n      0.3,  // Delicate presence\n      150,  // Detail emerges naturally\n      24,   // Smooth transitions\n      3,    // Primary rhythm\n      4     // Secondary rhythm\n    );\n    \n    const material = new THREE.LineBasicMaterial({ \n      color: 0x777777, // Medium gray\n      transparent: true,\n      opacity: 0.7,\n      linewidth: 0.25\n    });\n    \n    const edges = new THREE.EdgesGeometry(geometry);\n    const knot = new THREE.LineSegments(edges, material);\n    \n    // Center it on screen\n    knot.position.set(0, 0, 0);\n    \n    scene.add(knot);\n    \n    // Animation\n    let time = 0;\n    const animate = () => {\n      time += 0.002; // Halved base time increment\n      \n      // Create varied rotation speeds on all axes\n      knot.rotation.x = Math.PI / 6 + Math.sin(time * 0.25) * 0.2; // Halved speed\n      knot.rotation.y = time * 0.4 + Math.sin(time * 0.15) * 0.3; // Halved speed\n      knot.rotation.z = Math.cos(time * 0.2) * 0.15; // Halved speed\n      \n      // Add a slight wandering motion\n      knot.position.x = Math.sin(time * 0.1) * 0.1; // Halved speed\n      knot.position.y = Math.cos(time * 0.15) * 0.1; // Halved speed\n      \n      // Breathing effect with slight variation\n      const scale = 0.95 + 0.05 * Math.sin(time * 0.6 + Math.sin(time * 0.25) * 0.3); // Halved speed\n      knot.scale.setScalar(scale);\n      \n      renderer.render(scene, camera);\n      animationRef.current = requestAnimationFrame(animate);\n    };\n    \n    animate();\n    \n    // Cleanup\n    return () => {\n      if (animationRef.current) {\n        cancelAnimationFrame(animationRef.current);\n        animationRef.current = null;\n      }\n      \n      // Dispose of Three.js resources\n      if (geometry) {\n        geometry.dispose();\n      }\n      if (material) {\n        material.dispose();\n      }\n      if (edges) {\n        edges.dispose();\n      }\n      if (scene && knot) {\n        scene.remove(knot);\n      }\n      \n      // Clear scene\n      if (sceneRef.current) {\n        sceneRef.current.clear();\n        sceneRef.current = null;\n      }\n      \n      // Clean up renderer\n      if (rendererRef.current) {\n        if (containerRef.current && rendererRef.current.domElement && containerRef.current.contains(rendererRef.current.domElement)) {\n          containerRef.current.removeChild(rendererRef.current.domElement);\n        }\n        rendererRef.current.dispose();\n        rendererRef.current.forceContextLoss();\n        rendererRef.current = null;\n      }\n      \n      // Reset time variable\n      time = 0;\n    };\n  }, []);\n\n  return (\n    <div className="flex items-center justify-center w-full h-full bg-[#F0EEE6]">\n      <div className="w-[550px] h-[550px]">\n        <div ref={containerRef} className="w-full h-full" />\n      </div>\n    </div>\n  );\n};\n\nexport default DelicateTorusKnot;',sourceCodeMin:'import React, { useEffect, useRef } from \'react\'; import * as THREE from \'three\'; const DelicateTorusKnot = () => { const containerRef = useRef(null); const animationRef = useRef(null); const sceneRef = useRef(null); const rendererRef = useRef(null); useEffect(() => { if (!sceneRef.current) { sceneRef.current = new THREE.Scene(); rendererRef.current = new THREE.WebGLRenderer({ antialias: true, powerPreference: "high-performance" }); rendererRef.current.setSize(550, 550); rendererRef.current.setClearColor(0xF0EEE6); if (containerRef.current) { containerRef.current.appendChild(rendererRef.current.domElement); } } const scene = sceneRef.current; const renderer = rendererRef.current; const camera = new THREE.PerspectiveCamera(75, 1, 0.1, 1000); camera.position.z = 4; const geometry = new THREE.TorusKnotGeometry( 1.8, 0.3, 150, 24, 3, 4 ); const material = new THREE.LineBasicMaterial({ color: 0x777777, transparent: true, opacity: 0.7, linewidth: 0.25 }); const edges = new THREE.EdgesGeometry(geometry); const knot = new THREE.LineSegments(edges, material); knot.position.set(0, 0, 0); scene.add(knot); let time = 0; const animate = () => { time += 0.002; knot.rotation.x = Math.PI / 6 + Math.sin(time * 0.25) * 0.2; knot.rotation.y = time * 0.4 + Math.sin(time * 0.15) * 0.3; knot.rotation.z = Math.cos(time * 0.2) * 0.15; knot.position.x = Math.sin(time * 0.1) * 0.1; knot.position.y = Math.cos(time * 0.15) * 0.1; const scale = 0.95 + 0.05 * Math.sin(time * 0.6 + Math.sin(time * 0.25) * 0.3); knot.scale.setScalar(scale); renderer.render(scene, camera); animationRef.current = requestAnimationFrame(animate); }; animate(); return () => { if (animationRef.current) { cancelAnimationFrame(animationRef.current); animationRef.current = null; } if (geometry) { geometry.dispose(); } if (material) { material.dispose(); } if (edges) { edges.dispose(); } if (scene && knot) { scene.remove(knot); } if (sceneRef.current) { sceneRef.current.clear(); sceneRef.current = null; } if (rendererRef.current) { if (containerRef.current && rendererRef.current.domElement && containerRef.current.contains(rendererRef.current.domElement)) { containerRef.current.removeChild(rendererRef.current.domElement); } rendererRef.current.dispose(); rendererRef.current.forceContextLoss(); rendererRef.current = null; } time = 0; }; }, []); return ( <div className="flex items-center justify-center w-full h-full bg-[#F0EEE6]"><div className="w-[550px] h-[550px]"><div ref={containerRef} className="w-full h-full" /></div></div> ); }; export default DelicateTorusKnot;',text:"Act without doing.\nWork without effort.\nThink of the small as large\nand the few as many.\nSee simplicity in the complicated\nand accomplish the remarkable in small steps.\n\nMeet the difficult while it’s still simple.\nSolve the major while it’s still minor.\nDifficult problems in the world always\narise from simple ones.\nMajor issues in the world always\narise from minor ones.\n\nThe Vibe Coder never reaches for the great.\nThus, he achieves greatness.\nIf easy work is treated carelessly,\ndifficult work becomes dumbfounding.\n\nApproach each task with cool seriousness\nand full presence.",shouldDisplayMobileArtwork:!0,promptSuggestion:"make the knot simpler"},{number:64,staticArtwork:Cn,sourceCode:"// @ts-nocheck\nimport React, { useEffect, useRef } from 'react';\nimport * as THREE from 'three';\n\n// Themes: dealing with things early, starting from stillness, step by step progress\n// Visualization: A form that emerges gradually from a central point, showing how patterns develop from simple beginnings\n\nconst HourglassSpiral = () => {\n  const containerRef = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (!containerRef.current) return;\n\n    // Setup scene\n    const scene = new THREE.Scene();\n    scene.background = new THREE.Color('#F0EEE6');\n    \n    // Setup camera\n    const camera = new THREE.PerspectiveCamera(75, 1, 0.1, 1000);\n    camera.position.z = 16;\n    camera.position.y = 0;\n    \n    // Setup renderer\n    const renderer = new THREE.WebGLRenderer({ antialias: true });\n    renderer.setSize(550, 550);\n    containerRef.current.appendChild(renderer.domElement);\n    \n    // Create main group\n    const pineCone = new THREE.Group();\n    \n    // Begin with the smallest elements\n    const particleCount = 10000;  // The many that arise from one\n    const particles = new THREE.BufferGeometry();\n    const positions = new Float32Array(particleCount * 3);  // Space for growth\n    const colors = new Float32Array(particleCount * 3);     // Potential for form\n    const sizes = new Float32Array(particleCount);          // Room to develop\n    \n    for (let i = 0; i < particleCount; i++) {\n      // Create hourglass shape with particles\n      const t = i / particleCount;\n      const layer = t * 40;\n      const angle = layer * 0.3 + Math.random() * 0.2;\n      const spiralAngle = t * Math.PI * 40;\n      \n      let radius;\n      if (t < 0.3) {\n        // Top bulge\n        radius = Math.sin(t * Math.PI / 0.3) * 2.5;\n      } else if (t < 0.5) {\n        // Middle pinch - create hourglass waist\n        radius = 2.5 - (Math.sin((t - 0.3) * Math.PI / 0.2)) * 1.5;\n      } else if (t < 0.7) {\n        // Begin expansion after pinch\n        radius = 1 + (Math.sin((t - 0.5) * Math.PI / 0.2)) * 2;\n      } else {\n        // Bottom bulge\n        radius = 3 - (Math.sin((t - 0.7) * Math.PI / 0.3)) * 1;\n      }\n      \n      // Add some randomness for organic feel\n      radius += (Math.random() - 0.5) * 0.1;\n      \n      const y = t * 16 - 8;\n      const x = Math.cos(spiralAngle) * radius;\n      const z = Math.sin(spiralAngle) * radius;\n      \n      positions[i * 3] = x;\n      positions[i * 3 + 1] = y;\n      positions[i * 3 + 2] = z;\n      \n      // Set all particles to black\n      colors[i * 3] = 0;\n      colors[i * 3 + 1] = 0;\n      colors[i * 3 + 2] = 0;\n      \n      // Vary particle sizes\n      sizes[i] = Math.random() * 0.03 + 0.01;\n    }\n    \n    particles.setAttribute('position', new THREE.BufferAttribute(positions, 3));\n    particles.setAttribute('color', new THREE.BufferAttribute(colors, 3));\n    particles.setAttribute('size', new THREE.BufferAttribute(sizes, 1));\n    \n    const particleMaterial = new THREE.PointsMaterial({\n      size: 0.02,\n      vertexColors: true,\n      transparent: true,\n      opacity: 0.8,\n      sizeAttenuation: true,\n      blending: THREE.NormalBlending\n    });\n    \n    const particleSystem = new THREE.Points(particles, particleMaterial);\n    pineCone.add(particleSystem);\n    \n    // Add some structural lines - keeping these gray\n    const lineMaterial = new THREE.LineBasicMaterial({\n      color: '#888888',\n      transparent: true,\n      opacity: 0.3\n    });\n    \n    // Spiral structure\n    const spiralPoints = [];\n    for (let i = 0; i < 200; i++) {\n      const t = i / 200;\n      const angle = t * Math.PI * 16;\n      const y = t * 16 - 8;\n      \n      let radius;\n      if (t < 0.3) {\n        // Top bulge\n        radius = Math.sin(t * Math.PI / 0.3) * 2.5;\n      } else if (t < 0.5) {\n        // Middle pinch - create hourglass waist\n        radius = 2.5 - (Math.sin((t - 0.3) * Math.PI / 0.2)) * 1.5;\n      } else if (t < 0.7) {\n        // Begin expansion after pinch\n        radius = 1 + (Math.sin((t - 0.5) * Math.PI / 0.2)) * 2;\n      } else {\n        // Bottom bulge\n        radius = 3 - (Math.sin((t - 0.7) * Math.PI / 0.3)) * 1;\n      }\n      \n      spiralPoints.push(new THREE.Vector3(\n        Math.cos(angle) * radius,\n        y,\n        Math.sin(angle) * radius\n      ));\n    }\n    \n    const spiralGeometry = new THREE.BufferGeometry().setFromPoints(spiralPoints);\n    const spiralLine = new THREE.Line(spiralGeometry, lineMaterial);\n    pineCone.add(spiralLine);\n    \n    scene.add(pineCone);\n    \n    let time = 0;\n    let animationFrameId: number;\n    \n    function animate() {\n      animationFrameId = requestAnimationFrame(animate);\n      \n      time += 0.005;  // Halved speed\n      \n      pineCone.rotation.y = time * 0.45;  // Increased rotation speed\n      pineCone.rotation.x = Math.sin(time * 0.25) * 0.05;  // Halved speed\n      pineCone.rotation.z = Math.cos(time * 0.35) * 0.03;  // Halved speed\n      \n      const breathe = 1 + Math.sin(time * 0.25) * 0.02;  // Halved speed\n      pineCone.scale.set(breathe, breathe, breathe);\n      \n      // Animate particles slightly\n      const positions = particleSystem.geometry.attributes.position.array;\n      for (let i = 0; i < positions.length; i += 3) {\n        positions[i] += Math.sin(time + i) * 0.00005;  // Halved movement\n        positions[i + 1] += Math.cos(time + i) * 0.00005;  // Halved movement\n        positions[i + 2] += Math.sin(time + i + 1) * 0.00005;  // Halved movement\n      }\n      particleSystem.geometry.attributes.position.needsUpdate = true;\n      \n      renderer.render(scene, camera);\n    }\n    \n    animate();\n    \n    // Cleanup function\n    return () => {\n      if (animationFrameId) {\n        cancelAnimationFrame(animationFrameId);\n        animationFrameId = undefined;\n      }\n      \n      // Dispose of geometries\n      if (particles) {\n        particles.dispose();\n      }\n      if (spiralGeometry) {\n        spiralGeometry.dispose();\n      }\n      \n      // Dispose of materials\n      if (particleMaterial) {\n        particleMaterial.dispose();\n      }\n      if (lineMaterial) {\n        lineMaterial.dispose();\n      }\n      \n      // Remove objects from scene\n      if (pineCone) {\n        if (particleSystem) {\n          pineCone.remove(particleSystem);\n        }\n        if (spiralLine) {\n          pineCone.remove(spiralLine);\n        }\n        scene.remove(pineCone);\n      }\n      \n      // Clear scene\n      if (scene) {\n        scene.clear();\n      }\n      \n      // Dispose of renderer\n      if (renderer) {\n        if (containerRef.current && renderer.domElement && containerRef.current.contains(renderer.domElement)) {\n          containerRef.current.removeChild(renderer.domElement);\n        }\n        renderer.dispose();\n        renderer.forceContextLoss();\n      }\n      \n      // Clear arrays to prevent memory leaks\n      if (positions) {\n        positions.fill(0);\n      }\n      if (colors) {\n        colors.fill(0);\n      }\n      if (sizes) {\n        sizes.fill(0);\n      }\n      if (spiralPoints) {\n        spiralPoints.length = 0;\n      }\n      \n      // Reset time variable\n      time = 0;\n    };\n  }, []);\n\n  return (\n    <div className=\"w-full h-screen flex justify-center items-center bg-[#F0EEE6]\">\n      <div \n        ref={containerRef}\n        className=\"w-[550px] h-[550px]\"\n      />\n    </div>\n  );\n};\n\nexport default HourglassSpiral;",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; import * as THREE from 'three'; const HourglassSpiral = () => { const containerRef = useRef<HTMLDivElement>(null); useEffect(() => { if (!containerRef.current) return; const scene = new THREE.Scene(); scene.background = new THREE.Color('#F0EEE6'); const camera = new THREE.PerspectiveCamera(75, 1, 0.1, 1000); camera.position.z = 16; camera.position.y = 0; const renderer = new THREE.WebGLRenderer({ antialias: true }); renderer.setSize(550, 550); containerRef.current.appendChild(renderer.domElement); const pineCone = new THREE.Group(); const particleCount = 10000; const particles = new THREE.BufferGeometry(); const positions = new Float32Array(particleCount * 3); const colors = new Float32Array(particleCount * 3); const sizes = new Float32Array(particleCount); for (let i = 0; i < particleCount; i++) { const t = i / particleCount; const layer = t * 40; const angle = layer * 0.3 + Math.random() * 0.2; const spiralAngle = t * Math.PI * 40; let radius; if (t < 0.3) { radius = Math.sin(t * Math.PI / 0.3) * 2.5; } else if (t < 0.5) { radius = 2.5 - (Math.sin((t - 0.3) * Math.PI / 0.2)) * 1.5; } else if (t < 0.7) { radius = 1 + (Math.sin((t - 0.5) * Math.PI / 0.2)) * 2; } else { radius = 3 - (Math.sin((t - 0.7) * Math.PI / 0.3)) * 1; } radius += (Math.random() - 0.5) * 0.1; const y = t * 16 - 8; const x = Math.cos(spiralAngle) * radius; const z = Math.sin(spiralAngle) * radius; positions[i * 3] = x; positions[i * 3 + 1] = y; positions[i * 3 + 2] = z; colors[i * 3] = 0; colors[i * 3 + 1] = 0; colors[i * 3 + 2] = 0; sizes[i] = Math.random() * 0.03 + 0.01; } particles.setAttribute('position', new THREE.BufferAttribute(positions, 3)); particles.setAttribute('color', new THREE.BufferAttribute(colors, 3)); particles.setAttribute('size', new THREE.BufferAttribute(sizes, 1)); const particleMaterial = new THREE.PointsMaterial({ size: 0.02, vertexColors: true, transparent: true, opacity: 0.8, sizeAttenuation: true, blending: THREE.NormalBlending }); const particleSystem = new THREE.Points(particles, particleMaterial); pineCone.add(particleSystem); const lineMaterial = new THREE.LineBasicMaterial({ color: '#888888', transparent: true, opacity: 0.3 }); const spiralPoints = []; for (let i = 0; i < 200; i++) { const t = i / 200; const angle = t * Math.PI * 16; const y = t * 16 - 8; let radius; if (t < 0.3) { radius = Math.sin(t * Math.PI / 0.3) * 2.5; } else if (t < 0.5) { radius = 2.5 - (Math.sin((t - 0.3) * Math.PI / 0.2)) * 1.5; } else if (t < 0.7) { radius = 1 + (Math.sin((t - 0.5) * Math.PI / 0.2)) * 2; } else { radius = 3 - (Math.sin((t - 0.7) * Math.PI / 0.3)) * 1; } spiralPoints.push(new THREE.Vector3( Math.cos(angle) * radius, y, Math.sin(angle) * radius )); } const spiralGeometry = new THREE.BufferGeometry().setFromPoints(spiralPoints); const spiralLine = new THREE.Line(spiralGeometry, lineMaterial); pineCone.add(spiralLine); scene.add(pineCone); let time = 0; let animationFrameId: number; function animate() { animationFrameId = requestAnimationFrame(animate); time += 0.005; pineCone.rotation.y = time * 0.45; pineCone.rotation.x = Math.sin(time * 0.25) * 0.05; pineCone.rotation.z = Math.cos(time * 0.35) * 0.03; const breathe = 1 + Math.sin(time * 0.25) * 0.02; pineCone.scale.set(breathe, breathe, breathe); const positions = particleSystem.geometry.attributes.position.array; for (let i = 0; i < positions.length; i += 3) { positions[i] += Math.sin(time + i) * 0.00005; positions[i + 1] += Math.cos(time + i) * 0.00005; positions[i + 2] += Math.sin(time + i + 1) * 0.00005; } particleSystem.geometry.attributes.position.needsUpdate = true; renderer.render(scene, camera); } animate(); return () => { if (animationFrameId) { cancelAnimationFrame(animationFrameId); animationFrameId = undefined; } if (particles) { particles.dispose(); } if (spiralGeometry) { spiralGeometry.dispose(); } if (particleMaterial) { particleMaterial.dispose(); } if (lineMaterial) { lineMaterial.dispose(); } if (pineCone) { if (particleSystem) { pineCone.remove(particleSystem); } if (spiralLine) { pineCone.remove(spiralLine); } scene.remove(pineCone); } if (scene) { scene.clear(); } if (renderer) { if (containerRef.current && renderer.domElement && containerRef.current.contains(renderer.domElement)) { containerRef.current.removeChild(renderer.domElement); } renderer.dispose(); renderer.forceContextLoss(); } if (positions) { positions.fill(0); } if (colors) { colors.fill(0); } if (sizes) { sizes.fill(0); } if (spiralPoints) { spiralPoints.length = 0; } time = 0; }; }, []); return ( <div className=\"w-full h-screen flex justify-center items-center bg-[#F0EEE6]\"><div ref={containerRef} className=\"w-[550px] h-[550px]\" /></div> ); }; export default HourglassSpiral;",text:"What is still is easy to maintain.\nWhat has not yet appeared is easy to plan for.\nWhat is brittle is easy to break.\nWhat is small is easy to scatter.\n\nDeal with things before they appear.\nCreate order before confusion begins.\nAll magnificent things in the world start small.\n\nA tree that fills a man’s arms\narises from a tender shoot.\nA nine-story tower is raised\nfrom a single heap of earth.\nA thousand-mile journey begins\nfrom the spot under one’s feet.\nThose who rush to action defeat themselves.\nThose who grasp for things lose their grip.\n\nTherefore, The Vibe Coder takes action\nby letting things take their course.\nHe remains composed at the end,\njust as he was at the beginning.\nHe has nothing; therefore,|he has nothing to lose.\nHe has learned to unlearn,\nhe walks the path the learned forgot.\nHe is solely focused on Source,\nthus, he can care for all things.",shouldDisplayMobileArtwork:!0,promptSuggestion:"draw it with a pencil"},{number:65,staticArtwork:I,sourceCode:"\nimport { useState, useEffect, useCallback, useRef } from 'react';\n\n// Themes: natural wisdom, avoiding cleverness, return to simplicity\n// Visualization: A flower that responds to presence without force, showing how patterns emerge through gentle guidance\n\nconst RadialMeshFlower = () => {\n  // Natural proportions that allow harmony to emerge\n  const [dimensions, setDimensions] = useState({ width: 600, height: 600 });\n  const { width, height } = dimensions;\n  const radius = Math.min(width, height) * 0.267;  // The space of unfolding\n  const lineCount = 400;  // Detail arises from simplicity\n  const [mousePos, setMousePos] = useState({ x: width / 2, y: height / 2 });\n  const [circlePositions, setCirclePositions] = useState(null);\n  const [centerRotation, setCenterRotation] = useState(0);\n  const animationRef = useRef(null);\n  const easingFactor = 0.08;  // Gentle transitions\n  const containerRef = useRef(null);\n  \n  // Observe container size changes\n  useEffect(() => {\n    const updateDimensions = () => {\n      if (containerRef.current) {\n        const rect = containerRef.current.getBoundingClientRect();\n        setDimensions({ width: rect.width, height: rect.height });\n        setMousePos({ x: rect.width / 2, y: rect.height / 2 });\n      }\n    };\n\n    // Initial size\n    updateDimensions();\n\n    // Setup resize observer\n    const resizeObserver = new ResizeObserver(updateDimensions);\n    if (containerRef.current) {\n      resizeObserver.observe(containerRef.current);\n    }\n\n    return () => {\n      if (resizeObserver) {\n        resizeObserver.disconnect();\n      }\n    };\n  }, []);\n  \n  // Initialize circle positions when dimensions change\n  useEffect(() => {\n    const initialPositions = [];\n    // Create 8 circles (like flower petals) in a more flower-like arrangement\n    for (let i = 0; i < 8; i++) {\n      const angle = (i / 8) * Math.PI * 2;\n      const distanceFromCenter = radius * 0.5; // Closer together for flower effect\n      const x = width / 2 + Math.cos(angle) * distanceFromCenter;\n      const y = height / 2 + Math.sin(angle) * distanceFromCenter;\n      initialPositions.push({ x, y, targetX: x, targetY: y });\n    }\n    // Add central circle\n    initialPositions.push({ \n      x: width / 2, \n      y: height / 2, \n      targetX: width / 2, \n      targetY: height / 2 \n    });\n    setCirclePositions(initialPositions);\n  }, [width, height, radius]);\n  \n  // Handle mouse movement\n  const handleMouseMove = useCallback((event) => {\n    const svg = event.currentTarget;\n    const rect = svg.getBoundingClientRect();\n    const x = event.clientX - rect.left;\n    const y = event.clientY - rect.top;\n    setMousePos({ x, y });\n  }, []);\n  \n  // Update target positions based on mouse\n  useEffect(() => {\n    setCirclePositions(prevPositions => {\n      if (!prevPositions) return null;\n      \n      let closestCircleIndex = 0;\n      let minDistance = Infinity;\n      \n      prevPositions.forEach((circle, i) => {\n        const dx = mousePos.x - circle.x;\n        const dy = mousePos.y - circle.y;\n        const distance = Math.sqrt(dx * dx + dy * dy);\n        \n        if (distance < minDistance) {\n          minDistance = distance;\n          closestCircleIndex = i;\n        }\n      });\n      \n      return prevPositions.map((circle, i) => {\n        // Calculate base position\n        let baseCx, baseCy;\n        if (i === 8) { // Center circle\n          baseCx = width / 2;\n          baseCy = height / 2;\n        } else {\n          const angle = (i / 8) * Math.PI * 2;\n          const distanceFromCenter = radius * 0.5;\n          baseCx = width / 2 + Math.cos(angle) * distanceFromCenter;\n          baseCy = height / 2 + Math.sin(angle) * distanceFromCenter;\n        }\n        \n        if (i === closestCircleIndex) {\n          const maxMovement = radius * 0.1;\n          const dx = mousePos.x - baseCx;\n          const dy = mousePos.y - baseCy;\n          const movement = Math.min(Math.sqrt(dx * dx + dy * dy) / radius, 1) * maxMovement;\n          const moveAngle = Math.atan2(dy, dx);\n          \n          return {\n            ...circle,\n            targetX: baseCx + Math.cos(moveAngle) * movement,\n            targetY: baseCy + Math.sin(moveAngle) * movement\n          };\n        } else {\n          return {\n            ...circle,\n            targetX: baseCx,\n            targetY: baseCy\n          };\n        }\n      });\n    });\n  }, [mousePos, width, height, radius]);\n  \n  // Animate circle positions and rotation\n  useEffect(() => {\n    const animate = () => {\n      // Update center rotation\n      setCenterRotation(prev => prev + 0.005); // Reduced rotation speed for gentler movement\n      \n      // Update circle positions\n      setCirclePositions(prevPositions => {\n        if (!prevPositions) return null;\n        \n        return prevPositions.map(circle => {\n          const dx = circle.targetX - circle.x;\n          const dy = circle.targetY - circle.y;\n          \n          return {\n            ...circle,\n            x: circle.x + dx * easingFactor,\n            y: circle.y + dy * easingFactor\n          };\n        });\n      });\n      \n      animationRef.current = requestAnimationFrame(animate);\n    };\n    \n    animate();\n    \n    return () => {\n      if (animationRef.current) {\n        cancelAnimationFrame(animationRef.current);\n        animationRef.current = null;\n      }\n    };\n  }, [easingFactor]);\n  \n  // Generate petal-like lines for each circle\n  const generatePetalLines = (cx, cy, radius, rotation = 0, isCenter = false) => {\n    const lines = [];\n    const petalCount = 12; // Number of petals in each circle\n    \n    for (let petal = 0; petal < petalCount; petal++) {\n      const petalAngle = (petal / petalCount) * Math.PI * 2 + rotation;\n      const linesPerPetal = Math.floor(lineCount / petalCount);\n      \n      for (let i = 0; i < linesPerPetal; i++) {\n        const t = i / linesPerPetal;\n        const angle = petalAngle + (t - 0.5) * 0.5; // Spread lines within petal\n        \n        // Create tapered petal effect\n        const innerRadius = isCenter ? radius * 0.1 : radius * 0.2;\n        const outerRadius = radius * (0.9 - Math.pow(Math.abs(t - 0.5) * 2, 2) * 0.3);\n        \n        // Add subtle curve to lines\n        const curveOffset = Math.sin(t * Math.PI) * 0.1;\n        const curvedAngle = angle + curveOffset;\n        \n        const x1 = cx + Math.cos(curvedAngle) * innerRadius;\n        const y1 = cy + Math.sin(curvedAngle) * innerRadius;\n        const x2 = cx + Math.cos(angle) * outerRadius;\n        const y2 = cy + Math.sin(angle) * outerRadius;\n        \n        lines.push({\n          path: `M${x1},${y1} L${x2},${y2}`,\n          opacity: 0.6 - Math.abs(t - 0.5) * 0.4 // Fade at edges\n        });\n      }\n    }\n    return lines;\n  };\n  \n  if (!circlePositions) return null;\n  \n  return (\n    <div \n      ref={containerRef} \n      style={{ \n        width: '100%', \n        height: '100%', \n        backgroundColor: '#F0EEE6',\n        display: 'flex',\n        alignItems: 'center',\n        justifyContent: 'center'\n      }}\n    >\n      <svg \n        width={width} \n        height={height} \n        viewBox={`0 0 ${width} ${height}`}\n        preserveAspectRatio=\"xMidYMid meet\"\n        style={{ cursor: 'crosshair' }}\n        onMouseMove={handleMouseMove}\n      >\n        {circlePositions.map((circle, index) => (\n          <g key={index}>\n            {generatePetalLines(\n              circle.x, \n              circle.y, \n              index === 8 ? radius * 0.7 : radius, // Smaller center\n              index === 8 ? centerRotation : (index / 8) * Math.PI * 2, // Apply rotation to center\n              index === 8\n            ).map((line, i) => (\n              <path\n                key={i}\n                d={line.path}\n                stroke=\"#333333\"\n                strokeWidth={index === 8 ? \"0.4\" : \"0.3\"}\n                opacity={line.opacity}\n                fill=\"none\"\n              />\n            ))}\n          </g>\n        ))}\n      </svg>\n    </div>\n  );\n};\n\nexport default RadialMeshFlower;\n",sourceCodeMin:"import { useState, useEffect, useCallback, useRef } from 'react'; const RadialMeshFlower = () => { const [dimensions, setDimensions] = useState({ width: 600, height: 600 }); const { width, height } = dimensions; const radius = Math.min(width, height) * 0.267; const lineCount = 400; const [mousePos, setMousePos] = useState({ x: width / 2, y: height / 2 }); const [circlePositions, setCirclePositions] = useState(null); const [centerRotation, setCenterRotation] = useState(0); const animationRef = useRef(null); const easingFactor = 0.08; const containerRef = useRef(null); useEffect(() => { const updateDimensions = () => { if (containerRef.current) { const rect = containerRef.current.getBoundingClientRect(); setDimensions({ width: rect.width, height: rect.height }); setMousePos({ x: rect.width / 2, y: rect.height / 2 }); } }; updateDimensions(); const resizeObserver = new ResizeObserver(updateDimensions); if (containerRef.current) { resizeObserver.observe(containerRef.current); } return () => { if (resizeObserver) { resizeObserver.disconnect(); } }; }, []); useEffect(() => { const initialPositions = []; for (let i = 0; i < 8; i++) { const angle = (i / 8) * Math.PI * 2; const distanceFromCenter = radius * 0.5; const x = width / 2 + Math.cos(angle) * distanceFromCenter; const y = height / 2 + Math.sin(angle) * distanceFromCenter; initialPositions.push({ x, y, targetX: x, targetY: y }); } initialPositions.push({ x: width / 2, y: height / 2, targetX: width / 2, targetY: height / 2 }); setCirclePositions(initialPositions); }, [width, height, radius]); const handleMouseMove = useCallback((event) => { const svg = event.currentTarget; const rect = svg.getBoundingClientRect(); const x = event.clientX - rect.left; const y = event.clientY - rect.top; setMousePos({ x, y }); }, []); useEffect(() => { setCirclePositions(prevPositions => { if (!prevPositions) return null; let closestCircleIndex = 0; let minDistance = Infinity; prevPositions.forEach((circle, i) => { const dx = mousePos.x - circle.x; const dy = mousePos.y - circle.y; const distance = Math.sqrt(dx * dx + dy * dy); if (distance < minDistance) { minDistance = distance; closestCircleIndex = i; } }); return prevPositions.map((circle, i) => { let baseCx, baseCy; if (i === 8) { baseCx = width / 2; baseCy = height / 2; } else { const angle = (i / 8) * Math.PI * 2; const distanceFromCenter = radius * 0.5; baseCx = width / 2 + Math.cos(angle) * distanceFromCenter; baseCy = height / 2 + Math.sin(angle) * distanceFromCenter; } if (i === closestCircleIndex) { const maxMovement = radius * 0.1; const dx = mousePos.x - baseCx; const dy = mousePos.y - baseCy; const movement = Math.min(Math.sqrt(dx * dx + dy * dy) / radius, 1) * maxMovement; const moveAngle = Math.atan2(dy, dx); return { ...circle, targetX: baseCx + Math.cos(moveAngle) * movement, targetY: baseCy + Math.sin(moveAngle) * movement }; } else { return { ...circle, targetX: baseCx, targetY: baseCy }; } }); }); }, [mousePos, width, height, radius]); useEffect(() => { const animate = () => { setCenterRotation(prev => prev + 0.005); setCirclePositions(prevPositions => { if (!prevPositions) return null; return prevPositions.map(circle => { const dx = circle.targetX - circle.x; const dy = circle.targetY - circle.y; return { ...circle, x: circle.x + dx * easingFactor, y: circle.y + dy * easingFactor }; }); }); animationRef.current = requestAnimationFrame(animate); }; animate(); return () => { if (animationRef.current) { cancelAnimationFrame(animationRef.current); animationRef.current = null; } }; }, [easingFactor]); const generatePetalLines = (cx, cy, radius, rotation = 0, isCenter = false) => { const lines = []; const petalCount = 12; for (let petal = 0; petal < petalCount; petal++) { const petalAngle = (petal / petalCount) * Math.PI * 2 + rotation; const linesPerPetal = Math.floor(lineCount / petalCount); for (let i = 0; i < linesPerPetal; i++) { const t = i / linesPerPetal; const angle = petalAngle + (t - 0.5) * 0.5; const innerRadius = isCenter ? radius * 0.1 : radius * 0.2; const outerRadius = radius * (0.9 - Math.pow(Math.abs(t - 0.5) * 2, 2) * 0.3); const curveOffset = Math.sin(t * Math.PI) * 0.1; const curvedAngle = angle + curveOffset; const x1 = cx + Math.cos(curvedAngle) * innerRadius; const y1 = cy + Math.sin(curvedAngle) * innerRadius; const x2 = cx + Math.cos(angle) * outerRadius; const y2 = cy + Math.sin(angle) * outerRadius; lines.push({ path: `M${x1},${y1} L${x2},${y2}`, opacity: 0.6 - Math.abs(t - 0.5) * 0.4 }); } } return lines; }; if (!circlePositions) return null; return ( <div ref={containerRef} style={{ width: '100%', height: '100%', backgroundColor: '#F0EEE6', display: 'flex', alignItems: 'center', justifyContent: 'center' }} ><svg width={width} height={height} viewBox={`0 0 ${width} ${height}`} preserveAspectRatio=\"xMidYMid meet\" style={{ cursor: 'crosshair' }} onMouseMove={handleMouseMove} > {circlePositions.map((circle, index) => ( <g key={index}> {generatePetalLines( circle.x, circle.y, index === 8 ? radius * 0.7 : radius, index === 8 ? centerRotation : (index / 8) * Math.PI * 2, index === 8 ).map((line, i) => ( <path key={i} d={line.path} stroke=\"#333333\" strokeWidth={index === 8 ? \"0.4\" : \"0.3\"} opacity={line.opacity} fill=\"none\" /> ))} </g> ))} </svg></div> ); }; export default RadialMeshFlower;",text:"The ancients who followed Source\ndidn’t educate the people,\nbut allowed them to remain unspoiled.\nWhen they think they know,\npeople are difficult to guide.\nWhen they know that they don’t know,\npeople are empowered to find their own way.\n\nIf you want to lead, avoid cleverness.\nIf you want to lead, embrace simplicity.\nCelebrate ordinary life, and all people\ncan find their way back to their own true nature:\nIn harmony with the great oneness.",shouldDisplayMobileArtwork:!0,promptSuggestion:"what is the california version?"},{number:66,staticArtwork:Te,sourceCode:"// @ts-nocheck\nimport { useEffect, useRef } from 'react';\n\n// Themes: power through humility, leading from below, non-competition\n// Visualization: Waves that influence each other without domination, showing how strength emerges from yielding\n\nconst WaveInterferenceOptimized3 = () => {\n  const canvasRef = useRef(null);\n  const animationFrameRef = useRef(null);\n\n  useEffect(() => {\n    const canvas = canvasRef.current;\n    const ctx = canvas.getContext('2d', { alpha: false });\n    const width = 550;\n    const height = 550;\n    canvas.width = width;\n    canvas.height = height;\n\n    // Optimized resolution\n    const resolution = 5;\n    const rows = Math.floor(height / resolution);\n    const cols = Math.floor(width / resolution);\n    \n    // Begin with a central source that serves all others\n    const sources = [\n      { x: width/2, y: height/2, wavelength: 60, phase: 0, amplitude: 1.5 }, // Gentle influence\n    ];\n    \n    // Each source finds its place in the whole\n    const numRadialSources = 6;  // Balance in relationship\n    const radius = 150;          // Space to unfold\n    for (let i = 0; i < numRadialSources; i++) {\n      const angle = (i / numRadialSources) * Math.PI * 2;\n      sources.push({\n        x: width/2 + Math.cos(angle) * radius,\n        y: height/2 + Math.sin(angle) * radius,\n        wavelength: 50, // Doubled wavelength for slower waves\n        phase: angle,\n        amplitude: 0.8\n      });\n    }\n\n    let time = 0;\n    \n    // Pre-allocate arrays\n    const field = new Float32Array(rows * cols);\n    \n    // Create offscreen canvas for double buffering\n    const bufferCanvas = document.createElement('canvas');\n    bufferCanvas.width = width;\n    bufferCanvas.height = height;\n    const bufferCtx = bufferCanvas.getContext('2d', { alpha: false });\n\n    const animate = () => {\n      // Clear buffer\n      bufferCtx.fillStyle = '#F0EEE6';\n      bufferCtx.fillRect(0, 0, width, height);\n\n      // Calculate interference pattern\n      for (let i = 0; i < rows; i++) {\n        for (let j = 0; j < cols; j++) {\n          const x = j * resolution;\n          const y = i * resolution;\n          let amplitude = 0;\n\n          // Optimized source calculation\n          for (let s = 0; s < sources.length; s++) {\n            const source = sources[s];\n            const dx = x - source.x;\n            const dy = y - source.y;\n            const distance = Math.sqrt(dx * dx + dy * dy);\n            \n            // Simplified falloff\n            const falloff = Math.max(0, 1 - distance / 400);\n            amplitude += source.amplitude * falloff * \n              Math.sin((distance / source.wavelength - time) * 2 * Math.PI + source.phase);\n          }\n\n          field[i * cols + j] = amplitude;\n        }\n      }\n\n      // Optimized contour drawing\n      bufferCtx.strokeStyle = '#333';\n      bufferCtx.lineWidth = 1;\n      bufferCtx.beginPath();\n      \n      // Single contour level for performance\n      const level = 0;\n      \n      for (let i = 0; i < rows - 1; i++) {\n        for (let j = 0; j < cols - 1; j++) {\n          const idx = i * cols + j;\n          const x = j * resolution;\n          const y = i * resolution;\n          \n          // Simplified marching squares (only 4 cases)\n          const v00 = field[idx] > level;\n          const v10 = field[idx + 1] > level;\n          const v11 = field[idx + cols + 1] > level;\n          const v01 = field[idx + cols] > level;\n          \n          if (v00 && !v10) {\n            bufferCtx.moveTo(x + resolution / 2, y);\n            bufferCtx.lineTo(x + resolution, y + resolution / 2);\n          }\n          if (v10 && !v11) {\n            bufferCtx.moveTo(x + resolution, y + resolution / 2);\n            bufferCtx.lineTo(x + resolution / 2, y + resolution);\n          }\n          if (v11 && !v01) {\n            bufferCtx.moveTo(x + resolution / 2, y + resolution);\n            bufferCtx.lineTo(x, y + resolution / 2);\n          }\n          if (v01 && !v00) {\n            bufferCtx.moveTo(x, y + resolution / 2);\n            bufferCtx.lineTo(x + resolution / 2, y);\n          }\n        }\n      }\n      \n      bufferCtx.stroke();\n      \n      // Draw buffer to main canvas\n      ctx.drawImage(bufferCanvas, 0, 0);\n\n      time += 0.000625; // 1/32 speed\n      animationFrameRef.current = requestAnimationFrame(animate);\n    };\n\n    animate();\n\n    return () => {\n      // Cancel the animation frame\n      if (animationFrameRef.current) {\n        cancelAnimationFrame(animationFrameRef.current);\n        animationFrameRef.current = null;\n      }\n      \n      // Clear the canvas\n      if (canvas && ctx) {\n        ctx.clearRect(0, 0, canvas.width, canvas.height);\n      }\n      \n      // Clear the buffer canvas\n      if (bufferCanvas && bufferCtx) {\n        bufferCtx.clearRect(0, 0, bufferCanvas.width, bufferCanvas.height);\n        bufferCanvas.width = 0; // Force garbage collection\n        bufferCanvas.height = 0;\n      }\n      \n      // Clear arrays\n      field.fill(0);\n    };\n  }, []);\n\n  return (\n    <div style={{ \n      display: 'flex', \n      justifyContent: 'center', \n      alignItems: 'center',\n      width: '100vw', \n      height: '100vh', \n      backgroundColor: '#F0EEE6' \n    }}>\n      <canvas \n        ref={canvasRef} \n        style={{ \n          width: '550px',\n          height: '550px'\n        }} \n      />\n    </div>\n  );\n};\n\nexport default WaveInterferenceOptimized3;",sourceCodeMin:"import { useEffect, useRef } from 'react'; const WaveInterferenceOptimized3 = () => { const canvasRef = useRef(null); const animationFrameRef = useRef(null); useEffect(() => { const canvas = canvasRef.current; const ctx = canvas.getContext('2d', { alpha: false }); const width = 550; const height = 550; canvas.width = width; canvas.height = height; const resolution = 5; const rows = Math.floor(height / resolution); const cols = Math.floor(width / resolution); const sources = [ { x: width/2, y: height/2, wavelength: 60, phase: 0, amplitude: 1.5 }, ]; const numRadialSources = 6; const radius = 150; for (let i = 0; i < numRadialSources; i++) { const angle = (i / numRadialSources) * Math.PI * 2; sources.push({ x: width/2 + Math.cos(angle) * radius, y: height/2 + Math.sin(angle) * radius, wavelength: 50, phase: angle, amplitude: 0.8 }); } let time = 0; const field = new Float32Array(rows * cols); const bufferCanvas = document.createElement('canvas'); bufferCanvas.width = width; bufferCanvas.height = height; const bufferCtx = bufferCanvas.getContext('2d', { alpha: false }); const animate = () => { bufferCtx.fillStyle = '#F0EEE6'; bufferCtx.fillRect(0, 0, width, height); for (let i = 0; i < rows; i++) { for (let j = 0; j < cols; j++) { const x = j * resolution; const y = i * resolution; let amplitude = 0; for (let s = 0; s < sources.length; s++) { const source = sources[s]; const dx = x - source.x; const dy = y - source.y; const distance = Math.sqrt(dx * dx + dy * dy); const falloff = Math.max(0, 1 - distance / 400); amplitude += source.amplitude * falloff * Math.sin((distance / source.wavelength - time) * 2 * Math.PI + source.phase); } field[i * cols + j] = amplitude; } } bufferCtx.strokeStyle = '#333'; bufferCtx.lineWidth = 1; bufferCtx.beginPath(); const level = 0; for (let i = 0; i < rows - 1; i++) { for (let j = 0; j < cols - 1; j++) { const idx = i * cols + j; const x = j * resolution; const y = i * resolution; const v00 = field[idx] > level; const v10 = field[idx + 1] > level; const v11 = field[idx + cols + 1] > level; const v01 = field[idx + cols] > level; if (v00 && !v10) { bufferCtx.moveTo(x + resolution / 2, y); bufferCtx.lineTo(x + resolution, y + resolution / 2); } if (v10 && !v11) { bufferCtx.moveTo(x + resolution, y + resolution / 2); bufferCtx.lineTo(x + resolution / 2, y + resolution); } if (v11 && !v01) { bufferCtx.moveTo(x + resolution / 2, y + resolution); bufferCtx.lineTo(x, y + resolution / 2); } if (v01 && !v00) { bufferCtx.moveTo(x, y + resolution / 2); bufferCtx.lineTo(x + resolution / 2, y); } } } bufferCtx.stroke(); ctx.drawImage(bufferCanvas, 0, 0); time += 0.000625; animationFrameRef.current = requestAnimationFrame(animate); }; animate(); return () => { if (animationFrameRef.current) { cancelAnimationFrame(animationFrameRef.current); animationFrameRef.current = null; } if (canvas && ctx) { ctx.clearRect(0, 0, canvas.width, canvas.height); } if (bufferCanvas && bufferCtx) { bufferCtx.clearRect(0, 0, bufferCanvas.width, bufferCanvas.height); bufferCanvas.width = 0; bufferCanvas.height = 0; } field.fill(0); }; }, []); return ( <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', width: '100vw', height: '100vh', backgroundColor: '#F0EEE6' }}><canvas ref={canvasRef} style={{ width: '550px', height: '550px' }} /></div> ); }; export default WaveInterferenceOptimized3;",text:"Why is the sea,\nking of a hundred streams.\nBecause it lies below them.\nHumility gives it its power.\n\nIf you want to lead a team,\nplace yourself below them.\nLearn how to follow them.\n\nThe Vibe Coder is above people,\nand no one feels oppressed.\nHe stands ahead, and no one’s left behind.\n\nBecause he does not compete,\nhe will not have competition.",shouldDisplayMobileArtwork:!0,promptSuggestion:"make it more meditative"},{number:67,staticArtwork:$,sourceCode:"import { useEffect, useRef } from 'react';\n\n// Themes: three treasures, simple virtues, true greatness\n// Visualization: A grid where dots move with patience and humility, showing how simplicity leads to harmony\n\nconst RhythmicPulseGrid = () => {\n  const canvasRef = useRef(null);\n\n  useEffect(() => {\n    const canvas = canvasRef.current;\n    if (!canvas) return;\n    \n    const ctx = canvas.getContext('2d');\n    canvas.width = 550;\n    canvas.height = 550;\n    \n    let time = 0;\n    let animationFrameId = null;\n    const lineCount = 20;\n    const lineSpacing = canvas.height / (lineCount + 1);\n    const verticalLineCount = 20;\n    const verticalLineSpacing = canvas.width / (verticalLineCount + 1);\n    \n    // Three patterns representing simplicity, patience, and humility\n    const dotPatterns = [];\n    for (let i = 0; i < 3; i++) {\n      const pattern = [];\n      const frequency = 0.5 + i * 0.3;  // Each virtue moves at its own pace\n      const amplitude = 100 + i * 50;    // Each finds its own space\n      \n      for (let j = 0; j < 15; j++) {\n        pattern.push({\n          baseX: canvas.width / 2,\n          baseY: (j + 1) * (canvas.height / 16),\n          frequency: frequency,\n          amplitude: amplitude,\n          phase: j * 0.2\n        });\n      }\n      dotPatterns.push(pattern);\n    }\n    \n    const animate = () => {\n      ctx.fillStyle = '#F0EEE6';\n      ctx.fillRect(0, 0, canvas.width, canvas.height);\n      \n      time += 0.0045; // Further reduced to 56.25% of original speed\n      \n      // Draw horizontal lines with pulse effect\n      for (let i = 1; i <= lineCount; i++) {\n        const y = i * lineSpacing;\n        const pulse = Math.sin(time + i * 0.1) * 0.5 + 0.5;\n        \n        ctx.strokeStyle = `rgba(0, 0, 0, 0.1)`; // 10% black\n        ctx.lineWidth = 1;\n        \n        ctx.beginPath();\n        ctx.moveTo(0, y);\n        ctx.lineTo(canvas.width, y);\n        ctx.stroke();\n      }\n      \n      // Draw vertical lines\n      for (let i = 1; i <= verticalLineCount; i++) {\n        const x = i * verticalLineSpacing;\n        \n        ctx.strokeStyle = `rgba(0, 0, 0, 0.1)`; // 10% black\n        ctx.lineWidth = 1;\n        \n        ctx.beginPath();\n        ctx.moveTo(x, 0);\n        ctx.lineTo(x, canvas.height);\n        ctx.stroke();\n      }\n      \n      // Draw dot patterns\n      dotPatterns.forEach((pattern, patternIndex) => {\n        pattern.forEach(dot => {\n          const x = dot.baseX + Math.sin(time * dot.frequency + dot.phase) * dot.amplitude;\n          const y = dot.baseY;\n          \n          // Find nearest line and snap to it\n          const nearestLine = Math.round(y / lineSpacing) * lineSpacing;\n          \n          const sizeMultiplier = 1 + Math.sin(time * 1.5 + patternIndex + dot.phase) * 0.3; // Reduced to 75% pulse speed\n          const size = 3 * sizeMultiplier;\n          \n          ctx.fillStyle = `rgba(40, 40, 40, ${0.6 + Math.sin(time * 1.125 + dot.phase) * 0.3})`; // Reduced to 75% opacity pulse speed\n          ctx.beginPath();\n          ctx.arc(x, nearestLine, size, 0, Math.PI * 2);\n          ctx.fill();\n        });\n      });\n      \n      animationFrameId = requestAnimationFrame(animate);\n    };\n    \n    animate();\n\n    // Cleanup function\n    return () => {\n      if (animationFrameId) {\n        cancelAnimationFrame(animationFrameId);\n        animationFrameId = null;\n      }\n      \n      if (canvas && ctx) {\n        ctx.clearRect(0, 0, canvas.width, canvas.height);\n      }\n      \n      dotPatterns.length = 0;\n    };\n  }, []);\n\n  return (\n    <div style={{ width: '550px', height: '550px', backgroundColor: '#F0EEE6' }}>\n      <canvas ref={canvasRef} />\n    </div>\n  );\n};\n\nexport default RhythmicPulseGrid;",sourceCodeMin:"import { useEffect, useRef } from 'react'; const RhythmicPulseGrid = () => { const canvasRef = useRef(null); useEffect(() => { const canvas = canvasRef.current; if (!canvas) return; const ctx = canvas.getContext('2d'); canvas.width = 550; canvas.height = 550; let time = 0; let animationFrameId = null; const lineCount = 20; const lineSpacing = canvas.height / (lineCount + 1); const verticalLineCount = 20; const verticalLineSpacing = canvas.width / (verticalLineCount + 1); const dotPatterns = []; for (let i = 0; i < 3; i++) { const pattern = []; const frequency = 0.5 + i * 0.3; const amplitude = 100 + i * 50; for (let j = 0; j < 15; j++) { pattern.push({ baseX: canvas.width / 2, baseY: (j + 1) * (canvas.height / 16), frequency: frequency, amplitude: amplitude, phase: j * 0.2 }); } dotPatterns.push(pattern); } const animate = () => { ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, canvas.width, canvas.height); time += 0.0045; for (let i = 1; i <= lineCount; i++) { const y = i * lineSpacing; const pulse = Math.sin(time + i * 0.1) * 0.5 + 0.5; ctx.strokeStyle = `rgba(0, 0, 0, 0.1)`; ctx.lineWidth = 1; ctx.beginPath(); ctx.moveTo(0, y); ctx.lineTo(canvas.width, y); ctx.stroke(); } for (let i = 1; i <= verticalLineCount; i++) { const x = i * verticalLineSpacing; ctx.strokeStyle = `rgba(0, 0, 0, 0.1)`; ctx.lineWidth = 1; ctx.beginPath(); ctx.moveTo(x, 0); ctx.lineTo(x, canvas.height); ctx.stroke(); } dotPatterns.forEach((pattern, patternIndex) => { pattern.forEach(dot => { const x = dot.baseX + Math.sin(time * dot.frequency + dot.phase) * dot.amplitude; const y = dot.baseY; const nearestLine = Math.round(y / lineSpacing) * lineSpacing; const sizeMultiplier = 1 + Math.sin(time * 1.5 + patternIndex + dot.phase) * 0.3; const size = 3 * sizeMultiplier; ctx.fillStyle = `rgba(40, 40, 40, ${0.6 + Math.sin(time * 1.125 + dot.phase) * 0.3})`; ctx.beginPath(); ctx.arc(x, nearestLine, size, 0, Math.PI * 2); ctx.fill(); }); }); animationFrameId = requestAnimationFrame(animate); }; animate(); return () => { if (animationFrameId) { cancelAnimationFrame(animationFrameId); animationFrameId = null; } if (canvas && ctx) { ctx.clearRect(0, 0, canvas.width, canvas.height); } dotPatterns.length = 0; }; }, []); return ( <div style={{ width: '550px', height: '550px', backgroundColor: '#F0EEE6' }}><canvas ref={canvasRef} /></div> ); }; export default RhythmicPulseGrid;",text:"People say Source is so grand\nit’s impossible to grasp.\nIt is just this grandness\nthat makes it unlike anything else.\n\nI have three great treasures to share:\nSimplicity\nPatience\nHumility\n\nSimple in action and in thought,\nyou return to the origin of being.\nPatient with both friends and enemies,\nyou accord with the way things are.\nHumble in word and deed,\nyou inhabit the oneness of the cosmos.",shouldDisplayMobileArtwork:!0,promptSuggestion:"what if it had a heartbeat?"},{number:68,staticArtwork:Mn,sourceCode:"// @ts-nocheck\nimport { useEffect, useRef } from 'react';\n\n// Themes: strength without violence, victory without anger, alignment with heaven\n// Visualization: Particles that transform through gentle convergence, showing how power emerges through harmony\n\nconst GlowingEye = () => {\n  const canvasRef = useRef(null);\n  const animationFrameRef = useRef(null);\n  \n  useEffect(() => {\n    const canvas = canvasRef.current;\n    if (!canvas) return;\n    \n    const ctx = canvas.getContext('2d');\n    const width = canvas.width = 550;\n    const height = canvas.height = 550;\n    const centerX = width / 2;\n    const centerY = height / 2;\n    const radius = width * 0.35;\n    \n    const PARTICLE_COUNT = 30000;\n    const particles = [];\n    \n    // Each particle begins its journey of transformation\n    for (let i = 0; i < PARTICLE_COUNT; i++) {\n      const side = i < PARTICLE_COUNT / 2 ? 'dark' : 'light';  // Balance of forces\n      const angle = Math.random() * Math.PI * 2;  // Freedom to move\n      const r = Math.sqrt(Math.random()) * radius;  // Natural distribution\n      \n      // Position based on which side of yin yang\n      let initialAngle = angle;\n      if (side === 'dark') {\n        initialAngle = angle < Math.PI ? angle : angle - Math.PI;\n      } else {\n        initialAngle = angle >= Math.PI ? angle : angle + Math.PI;\n      }\n      \n      const x = centerX + Math.cos(initialAngle) * r;\n      const y = centerY + Math.sin(initialAngle) * r;\n      \n      particles.push({\n        x: x,\n        y: y,\n        side: side,\n        initialAngle: initialAngle,\n        initialRadius: r,\n        convergencePhase: Math.random() * Math.PI * 2,\n        convergenceSpeed: 0.005 + Math.random() * 0.005, // Reduced from 0.01-0.02 range to 0.005-0.01\n        size: 0.3 + Math.random() * 0.4,\n        targetX: x,\n        targetY: y,\n        transitionPhase: 0,\n        transitionSpeed: 0.004 + Math.random() * 0.002 // Reduced from 0.008-0.012 range to 0.004-0.006\n      });\n    }\n    \n    let time = 0;\n    let isRunning = true;\n    let lastTime = 0;\n    const FPS = 15; // Set to 15 frames per second\n    const frameDelay = 1000 / FPS; // milliseconds between frames\n    \n    function animate(currentTime) {\n      if (!isRunning) return;\n      \n      // Calculate time elapsed since last frame\n      if (!lastTime) lastTime = currentTime;\n      const elapsed = currentTime - lastTime;\n      \n      // Only update if enough time has passed for next frame\n      if (elapsed > frameDelay) {\n        // Update time with fixed increment based on frame rate\n        time += 0.008;\n        lastTime = currentTime;\n        \n        // Clear with trails\n        ctx.fillStyle = 'rgba(240, 238, 230, 0.1)';\n        ctx.fillRect(0, 0, width, height);\n      \n      particles.forEach(particle => {\n        // Update convergence phase\n        particle.convergencePhase += particle.convergenceSpeed;\n        particle.transitionPhase += particle.transitionSpeed;\n        \n        // Calculate convergence cycle\n        const convergenceCycle = Math.sin(particle.convergencePhase);\n        const isConverging = convergenceCycle > 0;\n        \n        if (isConverging) {\n          // Convergence phase - particles move toward center\n          const convergenceStrength = convergenceCycle;\n          particle.targetX = centerX;\n          particle.targetY = centerY;\n          \n          // Slow down near center\n          const distanceToCenter = Math.sqrt(\n            (particle.x - centerX) ** 2 + \n            (particle.y - centerY) ** 2\n          );\n          const moveSpeed = 0.02 * convergenceStrength * (distanceToCenter / radius);\n          \n          particle.x += (particle.targetX - particle.x) * moveSpeed;\n          particle.y += (particle.targetY - particle.y) * moveSpeed;\n        } else {\n          // Expansion phase - particles move to opposite side\n          const transitionProgress = Math.abs(convergenceCycle);\n          \n          // Calculate opposite side position\n          let newAngle, newRadius;\n          \n          if (particle.side === 'dark') {\n            // Move to light side\n            newAngle = particle.initialAngle + Math.PI;\n            newRadius = particle.initialRadius;\n          } else {\n            // Move to dark side\n            newAngle = particle.initialAngle + Math.PI;\n            newRadius = particle.initialRadius;\n          }\n          \n          // Add yin yang curve effect\n          const sCurveEffect = Math.sin(newAngle * 2) * radius * 0.5;\n          const curvedAngle = newAngle + (sCurveEffect / newRadius) * transitionProgress;\n          \n          particle.targetX = centerX + Math.cos(curvedAngle) * newRadius;\n          particle.targetY = centerY + Math.sin(curvedAngle) * newRadius;\n          \n          // Move towards target with easing\n          const moveSpeed = 0.03 * transitionProgress;\n          particle.x += (particle.targetX - particle.x) * moveSpeed;\n          particle.y += (particle.targetY - particle.y) * moveSpeed;\n        }\n        \n        // Determine color based on current position\n        const dx = particle.x - centerX;\n        const dy = particle.y - centerY;\n        const particleAngle = Math.atan2(dy, dx);\n        const normalizedAngle = (particleAngle + Math.PI * 2) % (Math.PI * 2);\n        const isDarkArea = (normalizedAngle >= Math.PI);\n        \n        let color, alpha;\n        \n        if (isConverging) {\n          // During convergence, particles maintain their original color\n          color = particle.side === 'dark' ? '20, 20, 20' : '90, 90, 90';\n          alpha = 0.3 * convergenceCycle;\n        } else {\n          // During expansion, particles transition to opposite color\n          const transition = Math.abs(convergenceCycle);\n          if (particle.side === 'dark') {\n            color = `${20 + transition * 70}, ${20 + transition * 70}, ${20 + transition * 70}`;\n          } else {\n            color = `${90 - transition * 70}, ${90 - transition * 70}, ${90 - transition * 70}`;\n          }\n          alpha = 0.3 * transition;\n        }\n        \n        // Add glow effect near center\n        const distanceToCenter = Math.sqrt(\n          (particle.x - centerX) ** 2 + \n          (particle.y - centerY) ** 2\n        );\n        if (distanceToCenter < radius * 0.2) {\n          alpha += (1 - distanceToCenter / (radius * 0.2)) * 0.2;\n        }\n        \n        // Draw particle\n        ctx.beginPath();\n        ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n        ctx.fillStyle = `rgba(${color}, ${alpha})`;\n        ctx.fill();\n      });\n      \n      // Structure removed - keeping only particle animation\n      \n      // Draw central convergence point\n      const centralGlow = Math.sin(time * 0.1) * 0.5 + 0.5; // Reduced from 0.2 to 0.1 for slower pulsing\n      ctx.beginPath();\n      ctx.arc(centerX, centerY, 2 + centralGlow * 3, 0, Math.PI * 2);\n      ctx.fillStyle = `rgba(51, 51, 51, ${0.1 + centralGlow * 0.2})`;\n      ctx.fill();\n      }\n      \n      // Request next frame regardless of whether we rendered this frame\n      animationFrameRef.current = requestAnimationFrame(animate);\n    }\n    \n    animate();\n    \n    return () => {\n      isRunning = false;\n      if (animationFrameRef.current) {\n        cancelAnimationFrame(animationFrameRef.current);\n        animationFrameRef.current = null;\n      }\n      \n      if (canvas && ctx) {\n        ctx.clearRect(0, 0, canvas.width, canvas.height);\n      }\n      \n      particles.length = 0;\n    };\n  }, []);\n  \n  return (\n    <div style={{\n      width: '550px',\n      height: '550px',\n      margin: 'auto',\n      backgroundColor: '#F0EEE6',\n      overflow: 'hidden'\n    }}>\n      <canvas\n        ref={canvasRef}\n        style={{\n          display: 'block',\n          width: '550px',\n          height: '550px'\n        }}\n      />\n    </div>\n  );\n};\n\nexport default GlowingEye;",sourceCodeMin:"import { useEffect, useRef } from 'react'; const GlowingEye = () => { const canvasRef = useRef(null); const animationFrameRef = useRef(null); useEffect(() => { const canvas = canvasRef.current; if (!canvas) return; const ctx = canvas.getContext('2d'); const width = canvas.width = 550; const height = canvas.height = 550; const centerX = width / 2; const centerY = height / 2; const radius = width * 0.35; const PARTICLE_COUNT = 30000; const particles = []; for (let i = 0; i < PARTICLE_COUNT; i++) { const side = i < PARTICLE_COUNT / 2 ? 'dark' : 'light'; const angle = Math.random() * Math.PI * 2; const r = Math.sqrt(Math.random()) * radius; let initialAngle = angle; if (side === 'dark') { initialAngle = angle < Math.PI ? angle : angle - Math.PI; } else { initialAngle = angle >= Math.PI ? angle : angle + Math.PI; } const x = centerX + Math.cos(initialAngle) * r; const y = centerY + Math.sin(initialAngle) * r; particles.push({ x: x, y: y, side: side, initialAngle: initialAngle, initialRadius: r, convergencePhase: Math.random() * Math.PI * 2, convergenceSpeed: 0.005 + Math.random() * 0.005, size: 0.3 + Math.random() * 0.4, targetX: x, targetY: y, transitionPhase: 0, transitionSpeed: 0.004 + Math.random() * 0.002 }); } let time = 0; let isRunning = true; let lastTime = 0; const FPS = 15; const frameDelay = 1000 / FPS; function animate(currentTime) { if (!isRunning) return; if (!lastTime) lastTime = currentTime; const elapsed = currentTime - lastTime; if (elapsed > frameDelay) { time += 0.008; lastTime = currentTime; ctx.fillStyle = 'rgba(240, 238, 230, 0.1)'; ctx.fillRect(0, 0, width, height); particles.forEach(particle => { particle.convergencePhase += particle.convergenceSpeed; particle.transitionPhase += particle.transitionSpeed; const convergenceCycle = Math.sin(particle.convergencePhase); const isConverging = convergenceCycle > 0; if (isConverging) { const convergenceStrength = convergenceCycle; particle.targetX = centerX; particle.targetY = centerY; const distanceToCenter = Math.sqrt( (particle.x - centerX) ** 2 + (particle.y - centerY) ** 2 ); const moveSpeed = 0.02 * convergenceStrength * (distanceToCenter / radius); particle.x += (particle.targetX - particle.x) * moveSpeed; particle.y += (particle.targetY - particle.y) * moveSpeed; } else { const transitionProgress = Math.abs(convergenceCycle); let newAngle, newRadius; if (particle.side === 'dark') { newAngle = particle.initialAngle + Math.PI; newRadius = particle.initialRadius; } else { newAngle = particle.initialAngle + Math.PI; newRadius = particle.initialRadius; } const sCurveEffect = Math.sin(newAngle * 2) * radius * 0.5; const curvedAngle = newAngle + (sCurveEffect / newRadius) * transitionProgress; particle.targetX = centerX + Math.cos(curvedAngle) * newRadius; particle.targetY = centerY + Math.sin(curvedAngle) * newRadius; const moveSpeed = 0.03 * transitionProgress; particle.x += (particle.targetX - particle.x) * moveSpeed; particle.y += (particle.targetY - particle.y) * moveSpeed; } const dx = particle.x - centerX; const dy = particle.y - centerY; const particleAngle = Math.atan2(dy, dx); const normalizedAngle = (particleAngle + Math.PI * 2) % (Math.PI * 2); const isDarkArea = (normalizedAngle >= Math.PI); let color, alpha; if (isConverging) { color = particle.side === 'dark' ? '20, 20, 20' : '90, 90, 90'; alpha = 0.3 * convergenceCycle; } else { const transition = Math.abs(convergenceCycle); if (particle.side === 'dark') { color = `${20 + transition * 70}, ${20 + transition * 70}, ${20 + transition * 70}`; } else { color = `${90 - transition * 70}, ${90 - transition * 70}, ${90 - transition * 70}`; } alpha = 0.3 * transition; } const distanceToCenter = Math.sqrt( (particle.x - centerX) ** 2 + (particle.y - centerY) ** 2 ); if (distanceToCenter < radius * 0.2) { alpha += (1 - distanceToCenter / (radius * 0.2)) * 0.2; } ctx.beginPath(); ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2); ctx.fillStyle = `rgba(${color}, ${alpha})`; ctx.fill(); }); const centralGlow = Math.sin(time * 0.1) * 0.5 + 0.5; ctx.beginPath(); ctx.arc(centerX, centerY, 2 + centralGlow * 3, 0, Math.PI * 2); ctx.fillStyle = `rgba(51, 51, 51, ${0.1 + centralGlow * 0.2})`; ctx.fill(); } animationFrameRef.current = requestAnimationFrame(animate); } animate(); return () => { isRunning = false; if (animationFrameRef.current) { cancelAnimationFrame(animationFrameRef.current); animationFrameRef.current = null; } if (canvas && ctx) { ctx.clearRect(0, 0, canvas.width, canvas.height); } particles.length = 0; }; }, []); return ( <div style={{ width: '550px', height: '550px', margin: 'auto', backgroundColor: '#F0EEE6', overflow: 'hidden' }}><canvas ref={canvasRef} style={{ display: 'block', width: '550px', height: '550px' }} /></div> ); }; export default GlowingEye;",text:"A good soldier is not violent.\nA good fighter is not angry.\nA good winner is not vengeful.\nA good leader is not dictatorial.\n\nThis is called intelligent non-competition.\nThis is called harnessing the strength of others.\nThis is the ancient essence:\nIn alignment with heaven.",shouldDisplayMobileArtwork:!0,promptSuggestion:"turn it into string"},{number:69,staticArtwork:Be,sourceCode:"// @ts-nocheck\nimport React, { useEffect, useRef } from 'react';\nimport * as THREE from 'three';\n\n// Themes: strategic waiting, defensive strength, victory without attack\n// Visualization: A structure that stands through stillness, showing how stability comes from non-action\n\nconst GentleArchitecture = () => {\n  const containerRef = useRef(null);\n\n  useEffect(() => {\n    if (!containerRef.current) return;\n    const container = containerRef.current;\n\n    // Setup scene\n    const scene = new THREE.Scene();\n    const camera = new THREE.PerspectiveCamera(75, container.clientWidth / container.clientHeight, 0.1, 1000);\n    const renderer = new THREE.WebGLRenderer({ antialias: true });\n    \n    renderer.setSize(container.clientWidth, container.clientHeight);\n    renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));\n    renderer.setClearColor(0xF0EEE6);\n    container.appendChild(renderer.domElement);\n\n    // Lighting\n    const ambientLight = new THREE.AmbientLight(0x404040);\n    const directionalLight = new THREE.DirectionalLight(0x808080, 1);\n    directionalLight.position.set(1, 1, 1);\n    scene.add(ambientLight);\n    scene.add(directionalLight);\n\n    // Materials\n    const material = new THREE.LineBasicMaterial({ \n      color: 0x333333,\n      transparent: true,\n      opacity: 0.6\n    });\n\n    // Create central vessel structure\n    const createVessel = () => {\n      const points = [];\n      const layers = 20;\n      const pointsPerLayer = 8;\n      \n      for (let i = 0; i <= layers; i++) {\n        const y = i - layers/2;\n        const radius = 5 * Math.sin(Math.PI * (i/layers));\n        \n        for (let j = 0; j < pointsPerLayer; j++) {\n          const angle = (j/pointsPerLayer) * Math.PI * 2;\n          const x = Math.cos(angle) * radius;\n          const z = Math.sin(angle) * radius;\n          points.push(new THREE.Vector3(x, y, z));\n        }\n      }\n\n      return points;\n    };\n\n    // Create connections between points\n    const createStructure = (points) => {\n      const geometry = new THREE.BufferGeometry();\n      const vertices = [];\n      const pointsPerLayer = 8;\n\n      // Vertical lines\n      for (let i = 0; i < points.length - pointsPerLayer; i++) {\n        vertices.push(points[i].x, points[i].y, points[i].z);\n        vertices.push(points[i + pointsPerLayer].x, points[i + pointsPerLayer].y, points[i + pointsPerLayer].z);\n      }\n\n      // Circular connections\n      for (let i = 0; i < points.length; i += pointsPerLayer) {\n        for (let j = 0; j < pointsPerLayer; j++) {\n          const p1 = points[i + j];\n          const p2 = points[i + ((j + 1) % pointsPerLayer)];\n          vertices.push(p1.x, p1.y, p1.z);\n          vertices.push(p2.x, p2.y, p2.z);\n        }\n      }\n\n      // Add some diagonal supports\n      for (let i = 0; i < points.length - pointsPerLayer; i += pointsPerLayer) {\n        for (let j = 0; j < pointsPerLayer; j++) {\n          const p1 = points[i + j];\n          const p2 = points[i + pointsPerLayer + ((j + 1) % pointsPerLayer)];\n          if (Math.random() < 0.3) {\n            vertices.push(p1.x, p1.y, p1.z);\n            vertices.push(p2.x, p2.y, p2.z);\n          }\n        }\n      }\n\n      geometry.setAttribute('position', new THREE.Float32BufferAttribute(vertices, 3));\n      return new THREE.LineSegments(geometry, material);\n    };\n\n    // Create floating platforms\n    const createPlatforms = () => {\n      const group = new THREE.Group();\n      const platformCount = 5;\n      \n      for (let i = 0; i < platformCount; i++) {\n        const radius = 3 + Math.random() * 2;\n        const segments = 6;\n        const geometry = new THREE.BufferGeometry();\n        const vertices = [];\n\n        // Create hexagonal platform\n        for (let j = 0; j < segments; j++) {\n          const angle1 = (j/segments) * Math.PI * 2;\n          const angle2 = ((j+1)/segments) * Math.PI * 2;\n          \n          vertices.push(0, 0, 0);\n          vertices.push(Math.cos(angle1) * radius, 0, Math.sin(angle1) * radius);\n          vertices.push(Math.cos(angle2) * radius, 0, Math.sin(angle2) * radius);\n        }\n\n        geometry.setAttribute('position', new THREE.Float32BufferAttribute(vertices, 3));\n        const platform = new THREE.LineSegments(geometry, material);\n        \n        // Position platform\n        platform.position.y = (i - platformCount/2) * 4;\n        platform.position.x = Math.sin(i * 1.2) * 3;\n        platform.position.z = Math.cos(i * 1.2) * 3;\n        platform.rotation.x = Math.random() * 0.2;\n        platform.rotation.z = Math.random() * 0.2;\n        \n        group.add(platform);\n      }\n\n      return group;\n    };\n\n    const vesselPoints = createVessel();\n    const structure = createStructure(vesselPoints);\n    const platforms = createPlatforms();\n    \n    scene.add(structure);\n    scene.add(platforms);\n\n    // Position camera\n    camera.position.z = 20;\n    camera.position.y = 5;\n    camera.lookAt(0, 0, 0);\n\n    // Animation\n    let animationFrameId;\n    const animate = () => {\n      animationFrameId = requestAnimationFrame(animate);\n\n      // Gentle rotation\n      structure.rotation.y += 0.002;\n      platforms.rotation.y += 0.001;\n\n      renderer.render(scene, camera);\n    };\n    animate();\n\n    // Handle resize\n    const handleResize = () => {\n      const width = container.clientWidth;\n      const height = container.clientHeight;\n      \n      camera.aspect = width / height;\n      camera.updateProjectionMatrix();\n      renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));\n      renderer.setSize(width, height);\n    };\n    \n    window.addEventListener('resize', handleResize);\n\n    // Cleanup\n    return () => {\n      window.removeEventListener('resize', handleResize);\n      cancelAnimationFrame(animationFrameId);\n      \n      if (renderer) {\n        renderer.dispose();\n        if (container.contains(renderer.domElement)) {\n          container.removeChild(renderer.domElement);\n        }\n      }\n      \n      // Clean up geometries and materials\n      scene.traverse((object) => {\n        if (object instanceof THREE.Mesh || object instanceof THREE.LineSegments) {\n          if (object.geometry) object.geometry.dispose();\n          if (object.material) {\n            if (Array.isArray(object.material)) {\n              object.material.forEach((material) => material.dispose());\n            } else {\n              object.material.dispose();\n            }\n          }\n        }\n      });\n    };\n  }, []);\n\n  return (\n    <div \n      ref={containerRef}\n      style={{ \n        margin: 0,\n        background: '#F0EEE6',\n        overflow: 'hidden',\n        display: 'flex',\n        alignItems: 'center',\n        justifyContent: 'center',\n        height: '100vh',\n        width: '100vw',\n        position: 'relative'\n      }}\n    />\n  );\n};\n\nexport default GentleArchitecture;",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; import * as THREE from 'three'; const GentleArchitecture = () => { const containerRef = useRef(null); useEffect(() => { if (!containerRef.current) return; const container = containerRef.current; const scene = new THREE.Scene(); const camera = new THREE.PerspectiveCamera(75, container.clientWidth / container.clientHeight, 0.1, 1000); const renderer = new THREE.WebGLRenderer({ antialias: true }); renderer.setSize(container.clientWidth, container.clientHeight); renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2)); renderer.setClearColor(0xF0EEE6); container.appendChild(renderer.domElement); const ambientLight = new THREE.AmbientLight(0x404040); const directionalLight = new THREE.DirectionalLight(0x808080, 1); directionalLight.position.set(1, 1, 1); scene.add(ambientLight); scene.add(directionalLight); const material = new THREE.LineBasicMaterial({ color: 0x333333, transparent: true, opacity: 0.6 }); const createVessel = () => { const points = []; const layers = 20; const pointsPerLayer = 8; for (let i = 0; i <= layers; i++) { const y = i - layers/2; const radius = 5 * Math.sin(Math.PI * (i/layers)); for (let j = 0; j < pointsPerLayer; j++) { const angle = (j/pointsPerLayer) * Math.PI * 2; const x = Math.cos(angle) * radius; const z = Math.sin(angle) * radius; points.push(new THREE.Vector3(x, y, z)); } } return points; }; const createStructure = (points) => { const geometry = new THREE.BufferGeometry(); const vertices = []; const pointsPerLayer = 8; for (let i = 0; i < points.length - pointsPerLayer; i++) { vertices.push(points[i].x, points[i].y, points[i].z); vertices.push(points[i + pointsPerLayer].x, points[i + pointsPerLayer].y, points[i + pointsPerLayer].z); } for (let i = 0; i < points.length; i += pointsPerLayer) { for (let j = 0; j < pointsPerLayer; j++) { const p1 = points[i + j]; const p2 = points[i + ((j + 1) % pointsPerLayer)]; vertices.push(p1.x, p1.y, p1.z); vertices.push(p2.x, p2.y, p2.z); } } for (let i = 0; i < points.length - pointsPerLayer; i += pointsPerLayer) { for (let j = 0; j < pointsPerLayer; j++) { const p1 = points[i + j]; const p2 = points[i + pointsPerLayer + ((j + 1) % pointsPerLayer)]; if (Math.random() < 0.3) { vertices.push(p1.x, p1.y, p1.z); vertices.push(p2.x, p2.y, p2.z); } } } geometry.setAttribute('position', new THREE.Float32BufferAttribute(vertices, 3)); return new THREE.LineSegments(geometry, material); }; const createPlatforms = () => { const group = new THREE.Group(); const platformCount = 5; for (let i = 0; i < platformCount; i++) { const radius = 3 + Math.random() * 2; const segments = 6; const geometry = new THREE.BufferGeometry(); const vertices = []; for (let j = 0; j < segments; j++) { const angle1 = (j/segments) * Math.PI * 2; const angle2 = ((j+1)/segments) * Math.PI * 2; vertices.push(0, 0, 0); vertices.push(Math.cos(angle1) * radius, 0, Math.sin(angle1) * radius); vertices.push(Math.cos(angle2) * radius, 0, Math.sin(angle2) * radius); } geometry.setAttribute('position', new THREE.Float32BufferAttribute(vertices, 3)); const platform = new THREE.LineSegments(geometry, material); platform.position.y = (i - platformCount/2) * 4; platform.position.x = Math.sin(i * 1.2) * 3; platform.position.z = Math.cos(i * 1.2) * 3; platform.rotation.x = Math.random() * 0.2; platform.rotation.z = Math.random() * 0.2; group.add(platform); } return group; }; const vesselPoints = createVessel(); const structure = createStructure(vesselPoints); const platforms = createPlatforms(); scene.add(structure); scene.add(platforms); camera.position.z = 20; camera.position.y = 5; camera.lookAt(0, 0, 0); let animationFrameId; const animate = () => { animationFrameId = requestAnimationFrame(animate); structure.rotation.y += 0.002; platforms.rotation.y += 0.001; renderer.render(scene, camera); }; animate(); const handleResize = () => { const width = container.clientWidth; const height = container.clientHeight; camera.aspect = width / height; camera.updateProjectionMatrix(); renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2)); renderer.setSize(width, height); }; window.addEventListener('resize', handleResize); return () => { window.removeEventListener('resize', handleResize); cancelAnimationFrame(animationFrameId); if (renderer) { renderer.dispose(); if (container.contains(renderer.domElement)) { container.removeChild(renderer.domElement); } } scene.traverse((object) => { if (object instanceof THREE.Mesh || object instanceof THREE.LineSegments) { if (object.geometry) object.geometry.dispose(); if (object.material) { if (Array.isArray(object.material)) { object.material.forEach((material) => material.dispose()); } else { object.material.dispose(); } } } }); }; }, []); return ( <div ref={containerRef} style={{ margin: 0, background: '#F0EEE6', overflow: 'hidden', display: 'flex', alignItems: 'center', justifyContent: 'center', height: '100vh', width: '100vw', position: 'relative' }} /> ); }; export default GentleArchitecture;",text:"In the military it is said:\nI dare not make the first move,\nas it is better to wait and see.\nI dare not advance an inch,\nas it is better to back away a foot.\n\nThis is called:\nAdvancing without advancing.\nRolling up sleeves without showing arms.\nCapturing the enemy without attacking.\nBeing armed without weapons.\n\nThere is no greater misfortune\nthan underestimating your opponent.\nHe who does not prepare to defend himself\nappears to have no enemies.\nNo one will attack a person unless\nhe appears to be an enemy.\n\nFor, to attack one who is not an enemy\nis to lose a friend.",shouldDisplayMobileArtwork:!0,promptSuggestion:"make it unravel"},{number:70,staticArtwork:Xe,sourceCode:"// @ts-nocheck\nimport React, { useEffect, useRef } from 'react'\n\n// Themes: simple truth, inner understanding, heart wisdom\n// Visualization: Delicate patterns that reveal meaning through simplicity, showing how truth emerges from the heart\n\nconst metadata = {\n  themes: \"simple truth, inner understanding, heart wisdom\",\n  visualization: \"Patterns that reveal meaning through simplicity\",\n  promptSuggestion: \"1. Adjust wave frequencies\\n2. Change interference patterns\\n3. Try different dot characters\\n4. Modify wave amplitudes\\n5. Add phase variations\"\n}\n\nconst GRID_SIZE = 60 // Reduced grid size for better performance\nconst CHARS = '⠁⠂⠄⠈⠐⠠⡀⢀⠃⠅⠘⠨⠊⠋⠌⠍⠎⠏'\n\ninterface Wave {\n  x: number\n  y: number\n  frequency: number\n  amplitude: number\n  phase: number\n  speed: number\n}\n\nconst DelicateAsciiDots: React.FC = () => {\n  const containerRef = useRef<HTMLDivElement>(null)\n  const mouseRef = useRef({ x: 0, y: 0 })\n  const wavesRef = useRef<Wave[]>([])\n  const gridRef = useRef<string[][]>([])\n  const timeRef = useRef(0)\n\n  useEffect(() => {\n    if (!containerRef.current) return\n\n    // Initialize grid with empty cells\n    gridRef.current = Array(GRID_SIZE).fill(0).map(() => \n      Array(GRID_SIZE).fill(null)\n    )\n\n    // Begin with the simplest elements\n    const waves: Wave[] = []\n    const numWaves = 3  // Truth needs few words\n    \n    for(let i = 0; i < numWaves; i++) {\n      waves.push({\n        x: GRID_SIZE * (0.25 + Math.random() * 0.5),      // Finding natural place\n        y: GRID_SIZE * (0.25 + Math.random() * 0.5),      // In open space\n        frequency: 0.2 + Math.random() * 0.3,             // Each with its rhythm\n        amplitude: 0.5 + Math.random() * 0.5,             // And presence\n        phase: Math.random() * Math.PI * 2,               // In its time\n        speed: 0.5 + Math.random() * 0.5                  // At its pace\n      })\n    }\n    \n    wavesRef.current = waves\n\n    // Create canvas for rendering\n    const canvas = document.createElement('canvas')\n    canvas.width = containerRef.current.offsetWidth\n    canvas.height = containerRef.current.offsetHeight\n    const ctx = canvas.getContext('2d')!\n    containerRef.current.appendChild(canvas)\n    \n    // Set up text rendering\n    ctx.font = '12px monospace'\n    ctx.textAlign = 'center'\n    ctx.textBaseline = 'middle'\n\n      const update = (delta: number) => {\n        timeRef.current += delta * 0.75 // Increased speed by 50%\n        \n        const newGrid = Array(GRID_SIZE).fill(0).map(() => Array(GRID_SIZE).fill(' '))\n        \n        // Convert pointer to grid space\n        const mouseX = (mouseRef.current.x + 1) * GRID_SIZE/2\n        const mouseY = (1 - mouseRef.current.y) * GRID_SIZE/2\n        \n        // Add mouse as dynamic wave source\n        const mouseWave: Wave = {\n          x: mouseX,\n          y: mouseY,\n          frequency: 0.3,\n          amplitude: 1,\n          phase: timeRef.current * 2,\n          speed: 1\n        }\n        \n        // Calculate wave interference\n        for(let y = 0; y < GRID_SIZE; y++) {\n          for(let x = 0; x < GRID_SIZE; x++) {\n            let totalWave = 0\n            \n            // Sum all wave contributions\n            const allWaves = wavesRef.current.concat([mouseWave])\n            allWaves.forEach(wave => {\n              const dx = x - wave.x\n              const dy = y - wave.y\n              const dist = Math.sqrt(dx * dx + dy * dy)\n              \n              // Calculate wave value with distance falloff\n              const falloff = 1 / (1 + dist * 0.1)\n              const value = Math.sin(\n                dist * wave.frequency - \n                timeRef.current * wave.speed + \n                wave.phase\n              ) * wave.amplitude * falloff\n              \n              totalWave += value\n            })\n            \n            // Map interference pattern to characters and opacity\n            const normalizedWave = (totalWave + 2) / 4 // Map -2..2 to 0..1\n            if(Math.abs(totalWave) > 0.2) {\n              // Ensure we get a valid character\n              const charIndex = Math.min(CHARS.length - 1, Math.max(0, \n                Math.floor(normalizedWave * (CHARS.length - 1))\n              ));\n              \n              // Map wave amplitude to opacity range 0.4 to 0.9\n              const opacity = Math.min(0.9, Math.max(0.4,\n                0.4 + (normalizedWave * 0.5)\n              ));\n              \n              // Create cell with guaranteed valid character\n              newGrid[y][x] = {\n                char: CHARS[charIndex] || CHARS[0],\n                opacity: opacity\n              };\n            } else {\n              newGrid[y][x] = null;\n            }\n          }\n        }\n        \n        // Draw wave sources\n        wavesRef.current.forEach(wave => {\n          const x = Math.floor(wave.x)\n          const y = Math.floor(wave.y)\n          if(x >= 0 && x < GRID_SIZE && y >= 0 && y < GRID_SIZE) {\n            // Create pulsing effect\n            const pulse = Math.sin(timeRef.current * wave.speed + wave.phase)\n            const charIndex = Math.floor((pulse + 1) * CHARS.length/2)\n            newGrid[y][x] = CHARS[charIndex]\n            \n            // Add source pattern\n            for(let i = 0; i < Math.PI * 2; i += Math.PI/4) {\n              const px = x + Math.round(Math.cos(i))\n              const py = y + Math.round(Math.sin(i))\n              if(px >= 0 && px < GRID_SIZE && py >= 0 && py < GRID_SIZE) {\n                newGrid[py][px] = CHARS[Math.floor(i * CHARS.length/(Math.PI * 2))]\n              }\n            }\n          }\n        })\n        \n        // Draw characters\n        ctx.fillStyle = '#F0EEE6'\n        ctx.fillRect(0, 0, canvas.width, canvas.height)\n        \n        const cellSize = Math.min(canvas.width, canvas.height) / GRID_SIZE\n        \n        for (let y = 0; y < GRID_SIZE; y++) {\n          for (let x = 0; x < GRID_SIZE; x++) {\n            const cell = newGrid[y][x]\n            if (cell && cell.char && CHARS.includes(cell.char)) {\n              ctx.fillStyle = `rgba(85, 85, 85, ${cell.opacity || 0.4})`\n              ctx.fillText(\n                cell.char,\n                x * cellSize + cellSize/2,\n                y * cellSize + cellSize/2\n              )\n            }\n          }\n        }\n      }\n\n      // Mouse move handler\n      const handleMouseMove = (event: MouseEvent) => {\n        const rect = canvas.getBoundingClientRect()\n        const x = (event.clientX - rect.left) / rect.width\n        const y = (event.clientY - rect.top) / rect.height\n        mouseRef.current = { x: x * 2 - 1, y: y * 2 - 1 }\n      }\n\n      // Handle window resize\n      const handleResize = () => {\n        if (!containerRef.current) return\n        canvas.width = containerRef.current.offsetWidth\n        canvas.height = containerRef.current.offsetHeight\n        ctx.font = '12px monospace'\n        ctx.textAlign = 'center'\n        ctx.textBaseline = 'middle'\n      }\n\n      window.addEventListener('resize', handleResize)\n      canvas.addEventListener('mousemove', handleMouseMove)\n      handleResize()\n\n      // Animation loop\n      let lastTime = 0\n      let animationFrameId: number\n      \n      const animate = (time: number) => {\n        const delta = Math.min((time - lastTime) / 1000, 0.1)\n        lastTime = time\n        \n        update(delta)\n        animationFrameId = requestAnimationFrame(animate)\n      }\n      \n      animationFrameId = requestAnimationFrame(animate)\n\n      // Cleanup\n      return () => {\n        window.removeEventListener('resize', handleResize)\n        canvas.removeEventListener('mousemove', handleMouseMove)\n        if (containerRef.current && canvas.parentNode === containerRef.current) {\n          containerRef.current.removeChild(canvas)\n        }\n        cancelAnimationFrame(animationFrameId)\n      }\n  }, [])\n\n  return (\n    <div\n      ref={containerRef}\n      style={{\n        margin: 0,\n        background: '#F0EEE6',\n        overflow: 'hidden',\n        display: 'flex',\n        alignItems: 'center',\n        justifyContent: 'center',\n        height: '100%',\n        width: '100%',\n        position: 'absolute',\n        top: 0,\n        left: 0\n      }}\n    />\n  )\n}\n\nDelicateAsciiDots.metadata = metadata\nexport default DelicateAsciiDots",sourceCodeMin:"import React, { useEffect, useRef } from 'react' const metadata = { themes: \"simple truth, inner understanding, heart wisdom\", visualization: \"Patterns that reveal meaning through simplicity\", promptSuggestion: \"1. Adjust wave frequencies\\n2. Change interference patterns\\n3. Try different dot characters\\n4. Modify wave amplitudes\\n5. Add phase variations\" } const GRID_SIZE = 60 const CHARS = '⠁⠂⠄⠈⠐⠠⡀⢀⠃⠅⠘⠨⠊⠋⠌⠍⠎⠏' interface Wave { x: number y: number frequency: number amplitude: number phase: number speed: number } const DelicateAsciiDots: React.FC = () => { const containerRef = useRef<HTMLDivElement>(null) const mouseRef = useRef({ x: 0, y: 0 }) const wavesRef = useRef<Wave[]>([]) const gridRef = useRef<string[][]>([]) const timeRef = useRef(0) useEffect(() => { if (!containerRef.current) return gridRef.current = Array(GRID_SIZE).fill(0).map(() => Array(GRID_SIZE).fill(null) ) const waves: Wave[] = [] const numWaves = 3 for(let i = 0; i < numWaves; i++) { waves.push({ x: GRID_SIZE * (0.25 + Math.random() * 0.5), y: GRID_SIZE * (0.25 + Math.random() * 0.5), frequency: 0.2 + Math.random() * 0.3, amplitude: 0.5 + Math.random() * 0.5, phase: Math.random() * Math.PI * 2, speed: 0.5 + Math.random() * 0.5 }) } wavesRef.current = waves const canvas = document.createElement('canvas') canvas.width = containerRef.current.offsetWidth canvas.height = containerRef.current.offsetHeight const ctx = canvas.getContext('2d')! containerRef.current.appendChild(canvas) ctx.font = '12px monospace' ctx.textAlign = 'center' ctx.textBaseline = 'middle' const update = (delta: number) => { timeRef.current += delta * 0.75 const newGrid = Array(GRID_SIZE).fill(0).map(() => Array(GRID_SIZE).fill(' ')) const mouseX = (mouseRef.current.x + 1) * GRID_SIZE/2 const mouseY = (1 - mouseRef.current.y) * GRID_SIZE/2 const mouseWave: Wave = { x: mouseX, y: mouseY, frequency: 0.3, amplitude: 1, phase: timeRef.current * 2, speed: 1 } for(let y = 0; y < GRID_SIZE; y++) { for(let x = 0; x < GRID_SIZE; x++) { let totalWave = 0 const allWaves = wavesRef.current.concat([mouseWave]) allWaves.forEach(wave => { const dx = x - wave.x const dy = y - wave.y const dist = Math.sqrt(dx * dx + dy * dy) const falloff = 1 / (1 + dist * 0.1) const value = Math.sin( dist * wave.frequency - timeRef.current * wave.speed + wave.phase ) * wave.amplitude * falloff totalWave += value }) const normalizedWave = (totalWave + 2) / 4 if(Math.abs(totalWave) > 0.2) { const charIndex = Math.min(CHARS.length - 1, Math.max(0, Math.floor(normalizedWave * (CHARS.length - 1)) )); const opacity = Math.min(0.9, Math.max(0.4, 0.4 + (normalizedWave * 0.5) )); newGrid[y][x] = { char: CHARS[charIndex] || CHARS[0], opacity: opacity }; } else { newGrid[y][x] = null; } } } wavesRef.current.forEach(wave => { const x = Math.floor(wave.x) const y = Math.floor(wave.y) if(x >= 0 && x < GRID_SIZE && y >= 0 && y < GRID_SIZE) { const pulse = Math.sin(timeRef.current * wave.speed + wave.phase) const charIndex = Math.floor((pulse + 1) * CHARS.length/2) newGrid[y][x] = CHARS[charIndex] for(let i = 0; i < Math.PI * 2; i += Math.PI/4) { const px = x + Math.round(Math.cos(i)) const py = y + Math.round(Math.sin(i)) if(px >= 0 && px < GRID_SIZE && py >= 0 && py < GRID_SIZE) { newGrid[py][px] = CHARS[Math.floor(i * CHARS.length/(Math.PI * 2))] } } } }) ctx.fillStyle = '#F0EEE6' ctx.fillRect(0, 0, canvas.width, canvas.height) const cellSize = Math.min(canvas.width, canvas.height) / GRID_SIZE for (let y = 0; y < GRID_SIZE; y++) { for (let x = 0; x < GRID_SIZE; x++) { const cell = newGrid[y][x] if (cell && cell.char && CHARS.includes(cell.char)) { ctx.fillStyle = `rgba(85, 85, 85, ${cell.opacity || 0.4})` ctx.fillText( cell.char, x * cellSize + cellSize/2, y * cellSize + cellSize/2 ) } } } } const handleMouseMove = (event: MouseEvent) => { const rect = canvas.getBoundingClientRect() const x = (event.clientX - rect.left) / rect.width const y = (event.clientY - rect.top) / rect.height mouseRef.current = { x: x * 2 - 1, y: y * 2 - 1 } } const handleResize = () => { if (!containerRef.current) return canvas.width = containerRef.current.offsetWidth canvas.height = containerRef.current.offsetHeight ctx.font = '12px monospace' ctx.textAlign = 'center' ctx.textBaseline = 'middle' } window.addEventListener('resize', handleResize) canvas.addEventListener('mousemove', handleMouseMove) handleResize() let lastTime = 0 let animationFrameId: number const animate = (time: number) => { const delta = Math.min((time - lastTime) / 1000, 0.1) lastTime = time update(delta) animationFrameId = requestAnimationFrame(animate) } animationFrameId = requestAnimationFrame(animate) return () => { window.removeEventListener('resize', handleResize) canvas.removeEventListener('mousemove', handleMouseMove) if (containerRef.current && canvas.parentNode === containerRef.current) { containerRef.current.removeChild(canvas) } cancelAnimationFrame(animationFrameId) } }, []) return ( <div ref={containerRef} style={{ margin: 0, background: '#F0EEE6', overflow: 'hidden', display: 'flex', alignItems: 'center', justifyContent: 'center', height: '100%', width: '100%', position: 'absolute', top: 0, left: 0 }} /> ) } DelicateAsciiDots.metadata = metadata export default DelicateAsciiDots",text:"My words are easy to understand,\nand easy to put into practice.\nYet no one under heaven\nunderstands or practices them.\n\nMy words are older than the world.\nHow can you grasp their meaning?\n\nIf you want to understand,\nlook inside your heart.",shouldDisplayMobileArtwork:!0,promptSuggestion:"make it bolder"},{number:71,staticArtwork:ee,sourceCode:"// @ts-nocheck\nimport { useEffect, useRef } from 'react';\n\n// Themes: pure unknowing, freedom from stagnation, true wholeness\n// Visualization: Waves that continuously transform and interfere, showing how patterns emerge from openness to change\n\nconst WaveInterferenceV3 = () => {\n  const canvasRef = useRef(null);\n\n  useEffect(() => {\n    const canvas = canvasRef.current;\n    const ctx = canvas.getContext('2d');\n    const width = 550;\n    const height = 550;\n    canvas.width = width;\n    canvas.height = height;\n\n    // Points of potential, open to infinite possibilities\n    const sources = [\n      { x: width/2, y: height/2 },      // Center of unknowing\n      { x: width/4, y: height/4 },      // Each point\n      { x: 3*width/4, y: height/4 },    // contributes\n      { x: width/4, y: 3*height/4 },    // to the\n      { x: 3*width/4, y: 3*height/4 },  // emerging\n      { x: width/2, y: height/5 },      // whole\n      { x: width/2, y: 4*height/5 },    // pattern\n    ];\n\n    const wavelength = 20;\n    let time = 0;\n    let animationFrameId = null;\n\n    const animate = () => {\n      // Fill with project background color\n      ctx.fillStyle = '#F0EEE6';\n      ctx.fillRect(0, 0, width, height);\n\n      // Create image data for pixel manipulation\n      const imageData = ctx.createImageData(width, height);\n      const data = imageData.data;\n\n      // Calculate wave interference for each pixel\n      for (let y = 0; y < height; y++) {\n        for (let x = 0; x < width; x++) {\n          let amplitude = 0;\n\n          // Sum waves from all sources\n          sources.forEach((source, i) => {\n            const dx = x - source.x;\n            const dy = y - source.y;\n            const distance = Math.sqrt(dx * dx + dy * dy);\n            const phase = i * Math.PI / 3; // Different phase for each source\n            amplitude += Math.sin((distance / wavelength - time) * 2 * Math.PI + phase);\n          });\n\n          // Normalize and threshold\n          const normalized = amplitude / sources.length;\n          const threshold = 0.1;\n          \n          // Determine if this is a line point (where wave crosses threshold)\n          const isLine = Math.abs(normalized) < threshold;\n          \n          // Set pixel color\n          const index = (y * width + x) * 4;\n          if (isLine) {\n            // Dark lines\n            data[index] = 34;     // R\n            data[index + 1] = 34; // G\n            data[index + 2] = 34; // B\n            data[index + 3] = 255; // A\n          } else {\n            // Project background color\n            data[index] = 240;     // R\n            data[index + 1] = 238; // G\n            data[index + 2] = 230; // B\n            data[index + 3] = 255; // A\n          }\n        }\n      }\n\n      ctx.putImageData(imageData, 0, 0);\n      time += 0.015; // Reduced speed by 25%\n      animationFrameId = requestAnimationFrame(animate);\n    };\n\n    animate();\n\n    return () => {\n      // Cancel animation frame to prevent memory leaks\n      if (animationFrameId) {\n        cancelAnimationFrame(animationFrameId);\n        animationFrameId = null;\n      }\n      \n      if (canvas && ctx) {\n        ctx.clearRect(0, 0, canvas.width, canvas.height);\n      }\n      \n      sources.length = 0;\n    };\n  }, []);\n\n  return (\n    <div style={{ \n      display: 'flex', \n      justifyContent: 'center', \n      alignItems: 'center',\n      width: '100vw', \n      height: '100vh', \n      backgroundColor: '#F0EEE6' \n    }}>\n      <canvas \n        ref={canvasRef} \n        style={{ \n          border: '1px solid #eee',\n          width: '550px',\n          height: '550px'\n        }} \n      />\n    </div>\n  );\n};\n\nexport default WaveInterferenceV3;",sourceCodeMin:"import { useEffect, useRef } from 'react'; const WaveInterferenceV3 = () => { const canvasRef = useRef(null); useEffect(() => { const canvas = canvasRef.current; const ctx = canvas.getContext('2d'); const width = 550; const height = 550; canvas.width = width; canvas.height = height; const sources = [ { x: width/2, y: height/2 }, { x: width/4, y: height/4 }, { x: 3*width/4, y: height/4 }, { x: width/4, y: 3*height/4 }, { x: 3*width/4, y: 3*height/4 }, { x: width/2, y: height/5 }, { x: width/2, y: 4*height/5 }, ]; const wavelength = 20; let time = 0; let animationFrameId = null; const animate = () => { ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, width, height); const imageData = ctx.createImageData(width, height); const data = imageData.data; for (let y = 0; y < height; y++) { for (let x = 0; x < width; x++) { let amplitude = 0; sources.forEach((source, i) => { const dx = x - source.x; const dy = y - source.y; const distance = Math.sqrt(dx * dx + dy * dy); const phase = i * Math.PI / 3; amplitude += Math.sin((distance / wavelength - time) * 2 * Math.PI + phase); }); const normalized = amplitude / sources.length; const threshold = 0.1; const isLine = Math.abs(normalized) < threshold; const index = (y * width + x) * 4; if (isLine) { data[index] = 34; data[index + 1] = 34; data[index + 2] = 34; data[index + 3] = 255; } else { data[index] = 240; data[index + 1] = 238; data[index + 2] = 230; data[index + 3] = 255; } } } ctx.putImageData(imageData, 0, 0); time += 0.015; animationFrameId = requestAnimationFrame(animate); }; animate(); return () => { if (animationFrameId) { cancelAnimationFrame(animationFrameId); animationFrameId = null; } if (canvas && ctx) { ctx.clearRect(0, 0, canvas.width, canvas.height); } sources.length = 0; }; }, []); return ( <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', width: '100vw', height: '100vh', backgroundColor: '#F0EEE6' }}><canvas ref={canvasRef} style={{ border: '1px solid #eee', width: '550px', height: '550px' }} /></div> ); }; export default WaveInterferenceV3;",text:"Not knowing is pure knowledge.\nAssuming to know is stagnation.\nOnly when we recognize stagnation\nas stagnation can we be free from it.\n\nThe Vibe Coder is free from stagnation\nbecause he sees it for what it is.\nThus, he is free to be truly whole.",shouldDisplayMobileArtwork:!0,promptSuggestion:"make it technicolor"},{number:72,staticArtwork:M,sourceCode:"import { useEffect, useRef, useState } from 'react';\n\n// Themes: natural reverence, teaching without teaching, self-trust\n// Visualization: Lines that flow and interweave naturally, showing how patterns emerge without instruction\n\nconst GentleWaves = () => {\n    const canvasRef = useRef(null);\n    const requestIdRef = useRef(null);\n    const [dimensions, setDimensions] = useState({ width: 500, height: 500 });\n    const particles = useRef([]);\n    const time = useRef(0);\n  \n    // Initialize canvas and handle resizing\n    useEffect(() => {\n      const handleResize = () => {\n        if (canvasRef.current) {\n          const canvas = canvasRef.current;\n          // Make canvas fill the viewport\n          const width = window.innerWidth;\n          const height = window.innerHeight;\n          \n          setDimensions({ width, height });\n          canvas.width = width;\n          canvas.height = height;\n        }\n      };\n      \n      handleResize();\n      window.addEventListener('resize', handleResize);\n      \n      return () => {\n        window.removeEventListener('resize', handleResize);\n      };\n    }, []);\n  \n    // Animation loop\n    useEffect(() => {\n      const render = () => {\n        const canvas = canvasRef.current;\n        if (!canvas) return;\n        \n        const ctx = canvas.getContext('2d');\n        const { width, height } = dimensions;\n        \n        // Clear canvas with soft background color\n        ctx.fillStyle = '#F0EEE6';\n        ctx.fillRect(0, 0, width, height);\n        \n        // Update time\n        time.current += 0.005;\n        \n        // The underlying structure reveals itself\n        ctx.strokeStyle = 'rgba(80, 80, 80, 0.033)';\n        ctx.lineWidth = 0.3;\n        \n        // Each line finds its own path\n        for (let y = 0; y < height; y += 40) {\n          const offsetY = 5 * Math.sin(time.current + y * 0.01);  // Natural movement\n          \n          ctx.beginPath();\n          ctx.moveTo(0, y + offsetY);\n          ctx.lineTo(width, y + offsetY);\n          ctx.stroke();\n        }\n        \n        // Vertical lines with subtle wave\n        for (let x = 0; x < width; x += 40) {\n          const offsetX = 5 * Math.sin(time.current + x * 0.01);\n          \n          ctx.beginPath();\n          ctx.moveTo(x + offsetX, 0);\n          ctx.lineTo(x + offsetX, height);\n          ctx.stroke();\n        }\n        \n        // Long horizontal flowing lines - \"the formless\"\n        const numHorizontalLines = 30;\n        \n        for (let i = 0; i < numHorizontalLines; i++) {\n          const yPos = (i / numHorizontalLines) * height;\n          const amplitude = 40 + 20 * Math.sin(time.current * 0.2 + i * 0.1);\n          const frequency = 0.008 + 0.004 * Math.sin(time.current * 0.1 + i * 0.05);\n          const speed = time.current * (0.5 + 0.3 * Math.sin(i * 0.1));\n          const thickness = 0.8 + 0.6 * Math.sin(time.current + i * 0.2);\n          const opacity = 0.132 + 0.088 * Math.abs(Math.sin(time.current * 0.3 + i * 0.15));\n          \n          ctx.beginPath();\n          ctx.lineWidth = thickness;\n          ctx.strokeStyle = `rgba(60, 60, 60, ${opacity})`;\n          \n          // Draw a flowing line\n          for (let x = 0; x < width; x += 2) {\n            const y = yPos + amplitude * Math.sin(x * frequency + speed);\n            \n            if (x === 0) {\n              ctx.moveTo(x, y);\n            } else {\n              ctx.lineTo(x, y);\n            }\n          }\n          \n          ctx.stroke();\n        }\n        \n        // Long diagonal flowing lines - \"crossing patterns\"\n        const numDiagonalLines = 35;\n        \n        for (let i = 0; i < numDiagonalLines; i++) {\n          const offset = (i / numDiagonalLines) * width * 2 - width * 0.5;\n          const amplitude = 30 + 20 * Math.cos(time.current * 0.25 + i * 0.1);\n          const frequency = 0.01 + 0.005 * Math.sin(time.current * 0.15 + i * 0.08);\n          const phase = time.current * (0.3 + 0.2 * Math.sin(i * 0.1));\n          const thickness = 0.7 + 0.5 * Math.sin(time.current + i * 0.25);\n          const opacity = 0.11 + 0.077 * Math.abs(Math.sin(time.current * 0.2 + i * 0.1));\n          \n          ctx.beginPath();\n          ctx.lineWidth = thickness;\n          ctx.strokeStyle = `rgba(50, 50, 50, ${opacity})`;\n          \n          // Draw diagonal flowing line\n          const steps = 100;\n          for (let j = 0; j <= steps; j++) {\n            const progress = j / steps;\n            const x = offset + progress * width;\n            const y = progress * height + amplitude * Math.sin(progress * 8 + phase);\n            \n            if (j === 0) {\n              ctx.moveTo(x, y);\n            } else {\n              ctx.lineTo(x, y);\n            }\n          }\n          \n          ctx.stroke();\n        }\n        \n        // Long vertical flowing lines\n        const numVerticalLines = 25;\n        \n        for (let i = 0; i < numVerticalLines; i++) {\n          const xPos = (i / numVerticalLines) * width;\n          const amplitude = 35 + 15 * Math.sin(time.current * 0.15 + i * 0.12);\n          const frequency = 0.009 + 0.004 * Math.cos(time.current * 0.12 + i * 0.07);\n          const speed = time.current * (0.4 + 0.25 * Math.cos(i * 0.15));\n          const thickness = 0.6 + 0.4 * Math.sin(time.current + i * 0.3);\n          const opacity = 0.099 + 0.066 * Math.abs(Math.sin(time.current * 0.25 + i * 0.18));\n          \n          ctx.beginPath();\n          ctx.lineWidth = thickness;\n          ctx.strokeStyle = `rgba(70, 70, 70, ${opacity})`;\n          \n          // Draw a flowing vertical line\n          for (let y = 0; y < height; y += 2) {\n            const x = xPos + amplitude * Math.sin(y * frequency + speed);\n            \n            if (y === 0) {\n              ctx.moveTo(x, y);\n            } else {\n              ctx.lineTo(x, y);\n            }\n          }\n          \n          ctx.stroke();\n        }\n        \n        // Request next frame\n        requestIdRef.current = requestAnimationFrame(render);\n      };\n      \n      render();\n      \n      return () => {\n        if (requestIdRef.current) {\n          cancelAnimationFrame(requestIdRef.current);\n          requestIdRef.current = null;\n        }\n        \n        const canvas = canvasRef.current;\n        if (canvas) {\n          const ctx = canvas.getContext('2d');\n          if (ctx) {\n            ctx.clearRect(0, 0, canvas.width, canvas.height);\n          }\n        }\n        \n        particles.current = [];\n        time.current = 0;\n      };\n    }, [dimensions]);\n    \n    return (\n      <div className=\"w-full h-full bg-[#F0EEE6]\">\n        <canvas \n          ref={canvasRef} \n          width={dimensions.width} \n          height={dimensions.height}\n          className=\"w-full h-full\"\n        />\n      </div>\n    );\n};\n\nexport default GentleWaves;",sourceCodeMin:"import { useEffect, useRef, useState } from 'react'; const GentleWaves = () => { const canvasRef = useRef(null); const requestIdRef = useRef(null); const [dimensions, setDimensions] = useState({ width: 500, height: 500 }); const particles = useRef([]); const time = useRef(0); useEffect(() => { const handleResize = () => { if (canvasRef.current) { const canvas = canvasRef.current; const width = window.innerWidth; const height = window.innerHeight; setDimensions({ width, height }); canvas.width = width; canvas.height = height; } }; handleResize(); window.addEventListener('resize', handleResize); return () => { window.removeEventListener('resize', handleResize); }; }, []); useEffect(() => { const render = () => { const canvas = canvasRef.current; if (!canvas) return; const ctx = canvas.getContext('2d'); const { width, height } = dimensions; ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, width, height); time.current += 0.005; ctx.strokeStyle = 'rgba(80, 80, 80, 0.033)'; ctx.lineWidth = 0.3; for (let y = 0; y < height; y += 40) { const offsetY = 5 * Math.sin(time.current + y * 0.01); ctx.beginPath(); ctx.moveTo(0, y + offsetY); ctx.lineTo(width, y + offsetY); ctx.stroke(); } for (let x = 0; x < width; x += 40) { const offsetX = 5 * Math.sin(time.current + x * 0.01); ctx.beginPath(); ctx.moveTo(x + offsetX, 0); ctx.lineTo(x + offsetX, height); ctx.stroke(); } const numHorizontalLines = 30; for (let i = 0; i < numHorizontalLines; i++) { const yPos = (i / numHorizontalLines) * height; const amplitude = 40 + 20 * Math.sin(time.current * 0.2 + i * 0.1); const frequency = 0.008 + 0.004 * Math.sin(time.current * 0.1 + i * 0.05); const speed = time.current * (0.5 + 0.3 * Math.sin(i * 0.1)); const thickness = 0.8 + 0.6 * Math.sin(time.current + i * 0.2); const opacity = 0.132 + 0.088 * Math.abs(Math.sin(time.current * 0.3 + i * 0.15)); ctx.beginPath(); ctx.lineWidth = thickness; ctx.strokeStyle = `rgba(60, 60, 60, ${opacity})`; for (let x = 0; x < width; x += 2) { const y = yPos + amplitude * Math.sin(x * frequency + speed); if (x === 0) { ctx.moveTo(x, y); } else { ctx.lineTo(x, y); } } ctx.stroke(); } const numDiagonalLines = 35; for (let i = 0; i < numDiagonalLines; i++) { const offset = (i / numDiagonalLines) * width * 2 - width * 0.5; const amplitude = 30 + 20 * Math.cos(time.current * 0.25 + i * 0.1); const frequency = 0.01 + 0.005 * Math.sin(time.current * 0.15 + i * 0.08); const phase = time.current * (0.3 + 0.2 * Math.sin(i * 0.1)); const thickness = 0.7 + 0.5 * Math.sin(time.current + i * 0.25); const opacity = 0.11 + 0.077 * Math.abs(Math.sin(time.current * 0.2 + i * 0.1)); ctx.beginPath(); ctx.lineWidth = thickness; ctx.strokeStyle = `rgba(50, 50, 50, ${opacity})`; const steps = 100; for (let j = 0; j <= steps; j++) { const progress = j / steps; const x = offset + progress * width; const y = progress * height + amplitude * Math.sin(progress * 8 + phase); if (j === 0) { ctx.moveTo(x, y); } else { ctx.lineTo(x, y); } } ctx.stroke(); } const numVerticalLines = 25; for (let i = 0; i < numVerticalLines; i++) { const xPos = (i / numVerticalLines) * width; const amplitude = 35 + 15 * Math.sin(time.current * 0.15 + i * 0.12); const frequency = 0.009 + 0.004 * Math.cos(time.current * 0.12 + i * 0.07); const speed = time.current * (0.4 + 0.25 * Math.cos(i * 0.15)); const thickness = 0.6 + 0.4 * Math.sin(time.current + i * 0.3); const opacity = 0.099 + 0.066 * Math.abs(Math.sin(time.current * 0.25 + i * 0.18)); ctx.beginPath(); ctx.lineWidth = thickness; ctx.strokeStyle = `rgba(70, 70, 70, ${opacity})`; for (let y = 0; y < height; y += 2) { const x = xPos + amplitude * Math.sin(y * frequency + speed); if (y === 0) { ctx.moveTo(x, y); } else { ctx.lineTo(x, y); } } ctx.stroke(); } requestIdRef.current = requestAnimationFrame(render); }; render(); return () => { if (requestIdRef.current) { cancelAnimationFrame(requestIdRef.current); requestIdRef.current = null; } const canvas = canvasRef.current; if (canvas) { const ctx = canvas.getContext('2d'); if (ctx) { ctx.clearRect(0, 0, canvas.width, canvas.height); } } particles.current = []; time.current = 0; }; }, [dimensions]); return ( <div className=\"w-full h-full bg-[#F0EEE6]\"><canvas ref={canvasRef} width={dimensions.width} height={dimensions.height} className=\"w-full h-full\" /></div> ); }; export default GentleWaves;",text:"When people lose their sense of awe,\nthey turn to religion.\nWhen people no longer trust themselves,\nthey submit to authority.\n\nTherefore, The Vibe Coder steps back\nso people won’t be misdirected.\nHe teaches without a teaching,\nso people will have nothing to learn.",shouldDisplayMobileArtwork:!0,promptSuggestion:"make it geometric"},{number:73,staticArtwork:q,sourceCode:"// @ts-nocheck\nimport { useEffect, useRef } from 'react';\n\n// Themes: true courage, nature's wisdom, universal net\n// Visualization: A grid that moves with careful balance, showing how strength emerges through wise restraint\n\nconst OrganicCellularGrid = () => {\n  const canvasRef = useRef(null);\n  \n  useEffect(() => {\n    const canvas = canvasRef.current;\n    if (!canvas) return;\n    \n    const ctx = canvas.getContext('2d');\n    canvas.width = 550;\n    canvas.height = 550;\n    \n    // Time flows with nature's rhythm\n    let time = 0;\n    const cellSize = 50;  // Each cell knows its bounds\n    const cellsX = Math.ceil(canvas.width / cellSize) + 2;   // The net extends\n    const cellsY = Math.ceil(canvas.height / cellSize) + 2;  // in all directions\n    \n    function drawCell(x, y, seed) {\n      // Movement with careful restraint\n      const noise1 = Math.sin(seed * 1.3 + time * 0.25) * 0.2;  // Gentle pulse\n      const noise2 = Math.cos(seed * 0.7 + time * 0.15) * 0.2;  // Steady breath\n      \n      const width = cellSize + noise1 * cellSize * 0.4;\n      const height = cellSize + noise2 * cellSize * 0.4;\n      \n      // Create organic distortion for rectangle\n      const corner1 = {\n        x: x + Math.sin(seed + time) * cellSize * 0.2,\n        y: y + Math.cos(seed + time) * cellSize * 0.2\n      };\n      \n      const corner2 = {\n        x: x + width + Math.sin(seed + 1 + time) * cellSize * 0.2,\n        y: y + Math.cos(seed + 1 + time) * cellSize * 0.2\n      };\n      \n      const corner3 = {\n        x: x + width + Math.sin(seed + 2 + time) * cellSize * 0.2,\n        y: y + height + Math.cos(seed + 2 + time) * cellSize * 0.2\n      };\n      \n      const corner4 = {\n        x: x + Math.sin(seed + 3 + time) * cellSize * 0.2,\n        y: y + height + Math.cos(seed + 3 + time) * cellSize * 0.2\n      };\n      \n      // Create path with rounded corners\n      const roundness = Math.sin(seed + time * 0.5) * cellSize * 0.15 + cellSize * 0.3;\n      \n      ctx.beginPath();\n      \n      // Top-left to top-right\n      ctx.moveTo(corner1.x + roundness, corner1.y);\n      ctx.lineTo(corner2.x - roundness, corner2.y);\n      ctx.quadraticCurveTo(corner2.x, corner2.y, corner2.x, corner2.y + roundness);\n      \n      // Top-right to bottom-right\n      ctx.lineTo(corner3.x, corner3.y - roundness);\n      ctx.quadraticCurveTo(corner3.x, corner3.y, corner3.x - roundness, corner3.y);\n      \n      // Bottom-right to bottom-left\n      ctx.lineTo(corner4.x + roundness, corner4.y);\n      ctx.quadraticCurveTo(corner4.x, corner4.y, corner4.x, corner4.y - roundness);\n      \n      // Bottom-left to top-left\n      ctx.lineTo(corner1.x, corner1.y + roundness);\n      ctx.quadraticCurveTo(corner1.x, corner1.y, corner1.x + roundness, corner1.y);\n      \n      ctx.closePath();\n      \n      ctx.strokeStyle = 'rgba(80, 80, 80, 0.6)';\n      ctx.lineWidth = 1.5;\n      ctx.stroke();\n    }\n    \n    let animationFrameId;\n    \n    function animate() {\n      ctx.fillStyle = '#F0EEE6';\n      ctx.fillRect(0, 0, canvas.width, canvas.height);\n      \n      time += 0.0025; // Halved speed for even more subtle movement\n      \n      // Create grid with overlapping cells\n      for (let i = -1; i < cellsX - 1; i++) {\n        for (let j = -1; j < cellsY - 1; j++) {\n          const baseX = i * cellSize - cellSize / 2;\n          const baseY = j * cellSize - cellSize / 2;\n          \n          // Add slight offset based on position and time\n          const offsetX = Math.sin(i * 0.5 + time * 0.5) * cellSize * 0.2;  // Halved speed\n          const offsetY = Math.cos(j * 0.5 + time * 0.5) * cellSize * 0.2;  // Halved speed\n          \n          drawCell(baseX + offsetX, baseY + offsetY, i + j * cellsX + time);\n        }\n      }\n      \n      animationFrameId = requestAnimationFrame(animate);\n    }\n    \n    animationFrameId = requestAnimationFrame(animate);\n    \n    return () => {\n      cancelAnimationFrame(animationFrameId);\n      animationFrameId = null;\n      \n      if (canvas && ctx) {\n        ctx.clearRect(0, 0, canvas.width, canvas.height);\n      }\n    };\n  }, []);\n  \n  return (\n    <div className=\"flex items-center justify-center w-full h-full\" style={{ backgroundColor: '#F0EEE6' }}>\n      <canvas ref={canvasRef} width={550} height={550} className=\"shadow-lg\" />\n    </div>\n  );\n};\n\nexport default OrganicCellularGrid;",sourceCodeMin:"import { useEffect, useRef } from 'react'; const OrganicCellularGrid = () => { const canvasRef = useRef(null); useEffect(() => { const canvas = canvasRef.current; if (!canvas) return; const ctx = canvas.getContext('2d'); canvas.width = 550; canvas.height = 550; let time = 0; const cellSize = 50; const cellsX = Math.ceil(canvas.width / cellSize) + 2; const cellsY = Math.ceil(canvas.height / cellSize) + 2; function drawCell(x, y, seed) { const noise1 = Math.sin(seed * 1.3 + time * 0.25) * 0.2; const noise2 = Math.cos(seed * 0.7 + time * 0.15) * 0.2; const width = cellSize + noise1 * cellSize * 0.4; const height = cellSize + noise2 * cellSize * 0.4; const corner1 = { x: x + Math.sin(seed + time) * cellSize * 0.2, y: y + Math.cos(seed + time) * cellSize * 0.2 }; const corner2 = { x: x + width + Math.sin(seed + 1 + time) * cellSize * 0.2, y: y + Math.cos(seed + 1 + time) * cellSize * 0.2 }; const corner3 = { x: x + width + Math.sin(seed + 2 + time) * cellSize * 0.2, y: y + height + Math.cos(seed + 2 + time) * cellSize * 0.2 }; const corner4 = { x: x + Math.sin(seed + 3 + time) * cellSize * 0.2, y: y + height + Math.cos(seed + 3 + time) * cellSize * 0.2 }; const roundness = Math.sin(seed + time * 0.5) * cellSize * 0.15 + cellSize * 0.3; ctx.beginPath(); ctx.moveTo(corner1.x + roundness, corner1.y); ctx.lineTo(corner2.x - roundness, corner2.y); ctx.quadraticCurveTo(corner2.x, corner2.y, corner2.x, corner2.y + roundness); ctx.lineTo(corner3.x, corner3.y - roundness); ctx.quadraticCurveTo(corner3.x, corner3.y, corner3.x - roundness, corner3.y); ctx.lineTo(corner4.x + roundness, corner4.y); ctx.quadraticCurveTo(corner4.x, corner4.y, corner4.x, corner4.y - roundness); ctx.lineTo(corner1.x, corner1.y + roundness); ctx.quadraticCurveTo(corner1.x, corner1.y, corner1.x + roundness, corner1.y); ctx.closePath(); ctx.strokeStyle = 'rgba(80, 80, 80, 0.6)'; ctx.lineWidth = 1.5; ctx.stroke(); } let animationFrameId; function animate() { ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, canvas.width, canvas.height); time += 0.0025; for (let i = -1; i < cellsX - 1; i++) { for (let j = -1; j < cellsY - 1; j++) { const baseX = i * cellSize - cellSize / 2; const baseY = j * cellSize - cellSize / 2; const offsetX = Math.sin(i * 0.5 + time * 0.5) * cellSize * 0.2; const offsetY = Math.cos(j * 0.5 + time * 0.5) * cellSize * 0.2; drawCell(baseX + offsetX, baseY + offsetY, i + j * cellsX + time); } } animationFrameId = requestAnimationFrame(animate); } animationFrameId = requestAnimationFrame(animate); return () => { cancelAnimationFrame(animationFrameId); animationFrameId = null; if (canvas && ctx) { ctx.clearRect(0, 0, canvas.width, canvas.height); } }; }, []); return ( <div className=\"flex items-center justify-center w-full h-full\" style={{ backgroundColor: '#F0EEE6' }}><canvas ref={canvasRef} width={550} height={550} className=\"shadow-lg\" /></div> ); }; export default OrganicCellularGrid;",text:"Daring based on courage leads to death.\nCaution based on courage leads to life.\nWhile this is true, it is not always so.\nNature has plans of its own.\nEven The Vibe Coder is baffled.\n\nWithout competing, Source overcomes.\nWithout speaking, Source responds.\nWithout being summoned, Source arrives.\nWithout preparation, Source follows the plan.\n\nHeaven’s net covers the universe.\nAlthough its openings appear wide,\nnothing can ever slip through.",shouldDisplayMobileArtwork:!0,promptSuggestion:"make it slippery"},{number:74,staticArtwork:kn,sourceCode:"// @ts-nocheck\nimport React, { useEffect, useRef } from 'react';\n\n// Themes: embracing change, freedom from fear, natural timing\n// Visualization: Particles flowing along ever-changing walls, showing how freedom comes from accepting impermanence\n\nconst CanyonUndulatingWalls = () => {\n  const canvasRef = useRef(null);\n  const animationFrameRef = useRef(null);\n  \n  useEffect(() => {\n    const canvas = canvasRef.current;\n    if (!canvas) return;\n    \n    const ctx = canvas.getContext('2d');\n    const width = canvas.width = 550;\n    const height = canvas.height = 550;\n    const centerX = width / 2;\n    const centerY = height / 2;\n    \n    // Each particle represents a moment in constant flow\n    const PARTICLE_COUNT = 18000;\n    const particles = [];\n    \n    // Create particles that embrace change\n    for (let i = 0; i < PARTICLE_COUNT; i++) {\n      // Each finds its own path\n      const side = Math.random() < 0.5 ? -1 : 1;\n      const y = Math.random() * height;\n      \n      // Create sine wave walls with varying thickness\n      const wavePhase = y * 0.01;\n      const waveAmount = Math.sin(wavePhase) * 60 + Math.sin(wavePhase * 3) * 20;\n      const wallThickness = 30 + Math.sin(wavePhase * 2) * 15;\n      \n      const baseX = centerX + side * (60 + waveAmount);\n      const offsetX = (Math.random() - 0.5) * wallThickness;\n      \n      particles.push({\n        x: baseX + offsetX,\n        y: y,\n        z: (Math.random() - 0.5) * 100,\n        side: side,\n        wavePhase: wavePhase,\n        initialY: y,\n        drift: Math.random() * Math.PI * 2,\n        speed: 0.1 + Math.random() * 0.3\n      });\n    }\n    \n    let time = 0;\n    let isRunning = true;\n    let lastTime = 0;\n    const FPS = 15; // Set to 15 frames per second\n    const frameDelay = 1000 / FPS; // milliseconds between frames\n    \n    function animate(currentTime) {\n      if (!isRunning) return;\n      \n      // Calculate time elapsed since last frame\n      if (!lastTime) lastTime = currentTime;\n      const elapsed = currentTime - lastTime;\n      \n      // Only update if enough time has passed for next frame\n      if (elapsed > frameDelay) {\n        // Update time with fixed increment (reduced from 0.016 to 0.008)\n        time += 0.008;\n        lastTime = currentTime;\n        \n        // Gentle clearing for flowing effect\n        ctx.fillStyle = 'rgba(240, 238, 230, 0.06)';\n        ctx.fillRect(0, 0, width, height);\n      \n      particles.forEach(particle => {\n        // Update wave position\n        const currentWavePhase = particle.y * 0.01 + time * 0.1;\n        const waveAmount = Math.sin(currentWavePhase) * 60 + Math.sin(currentWavePhase * 3) * 20;\n        const wallThickness = 30 + Math.sin(currentWavePhase * 2) * 15;\n        \n        // Calculate target X position\n        const targetX = centerX + particle.side * (60 + waveAmount);\n        const offset = (Math.sin(particle.drift + time) - 0.5) * wallThickness;\n        \n        // Smooth movement toward wall position\n        particle.x = particle.x * 0.95 + (targetX + offset) * 0.05;\n        \n        // Vertical movement with slight flow\n        particle.y += particle.speed;\n        \n        // Add depth movement\n        particle.z += Math.sin(time * 0.5 + particle.drift) * 0.3;\n        \n        // Reset particle at bottom\n        if (particle.y > height + 20) {\n          particle.y = -20;\n          particle.drift = Math.random() * Math.PI * 2;\n        }\n        \n        // Draw particle with depth effects\n        const depthFactor = 1 + particle.z * 0.01;\n        const opacity = 0.4 - Math.abs(particle.z) * 0.0025;\n        const size = 0.8 + particle.z * 0.015;\n        \n        if (opacity > 0 && size > 0) {\n          // Create subtle glow for depth\n          ctx.beginPath();\n          ctx.arc(particle.x, particle.y, size * 2, 0, Math.PI * 2);\n          ctx.fillStyle = `rgba(51, 51, 51, ${opacity * 0.1})`;\n          ctx.fill();\n          \n          // Main particle\n          ctx.beginPath();\n          ctx.arc(particle.x, particle.y, size, 0, Math.PI * 2);\n          ctx.fillStyle = `rgba(51, 51, 51, ${opacity})`;\n          ctx.fill();\n        }\n      });\n      }\n      \n      animationFrameRef.current = requestAnimationFrame(animate);\n    }\n    \n    animate();\n    \n    return () => {\n      isRunning = false;\n      if (animationFrameRef.current) {\n        cancelAnimationFrame(animationFrameRef.current);\n        animationFrameRef.current = null;\n      }\n      if (ctx) {\n        ctx.clearRect(0, 0, width, height);\n      }\n      particles.length = 0;\n      time = 0;\n    };\n  }, []);\n  \n  return (\n    <div style={{\n      width: '550px',\n      height: '550px',\n      margin: 'auto',\n      backgroundColor: '#F0EEE6',\n      overflow: 'hidden'\n    }}>\n      <canvas\n        ref={canvasRef}\n        style={{\n          display: 'block',\n          width: '550px',\n          height: '550px'\n        }}\n      />\n    </div>\n  );\n};\n\nexport default CanyonUndulatingWalls;",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; const CanyonUndulatingWalls = () => { const canvasRef = useRef(null); const animationFrameRef = useRef(null); useEffect(() => { const canvas = canvasRef.current; if (!canvas) return; const ctx = canvas.getContext('2d'); const width = canvas.width = 550; const height = canvas.height = 550; const centerX = width / 2; const centerY = height / 2; const PARTICLE_COUNT = 18000; const particles = []; for (let i = 0; i < PARTICLE_COUNT; i++) { const side = Math.random() < 0.5 ? -1 : 1; const y = Math.random() * height; const wavePhase = y * 0.01; const waveAmount = Math.sin(wavePhase) * 60 + Math.sin(wavePhase * 3) * 20; const wallThickness = 30 + Math.sin(wavePhase * 2) * 15; const baseX = centerX + side * (60 + waveAmount); const offsetX = (Math.random() - 0.5) * wallThickness; particles.push({ x: baseX + offsetX, y: y, z: (Math.random() - 0.5) * 100, side: side, wavePhase: wavePhase, initialY: y, drift: Math.random() * Math.PI * 2, speed: 0.1 + Math.random() * 0.3 }); } let time = 0; let isRunning = true; let lastTime = 0; const FPS = 15; const frameDelay = 1000 / FPS; function animate(currentTime) { if (!isRunning) return; if (!lastTime) lastTime = currentTime; const elapsed = currentTime - lastTime; if (elapsed > frameDelay) { time += 0.008; lastTime = currentTime; ctx.fillStyle = 'rgba(240, 238, 230, 0.06)'; ctx.fillRect(0, 0, width, height); particles.forEach(particle => { const currentWavePhase = particle.y * 0.01 + time * 0.1; const waveAmount = Math.sin(currentWavePhase) * 60 + Math.sin(currentWavePhase * 3) * 20; const wallThickness = 30 + Math.sin(currentWavePhase * 2) * 15; const targetX = centerX + particle.side * (60 + waveAmount); const offset = (Math.sin(particle.drift + time) - 0.5) * wallThickness; particle.x = particle.x * 0.95 + (targetX + offset) * 0.05; particle.y += particle.speed; particle.z += Math.sin(time * 0.5 + particle.drift) * 0.3; if (particle.y > height + 20) { particle.y = -20; particle.drift = Math.random() * Math.PI * 2; } const depthFactor = 1 + particle.z * 0.01; const opacity = 0.4 - Math.abs(particle.z) * 0.0025; const size = 0.8 + particle.z * 0.015; if (opacity > 0 && size > 0) { ctx.beginPath(); ctx.arc(particle.x, particle.y, size * 2, 0, Math.PI * 2); ctx.fillStyle = `rgba(51, 51, 51, ${opacity * 0.1})`; ctx.fill(); ctx.beginPath(); ctx.arc(particle.x, particle.y, size, 0, Math.PI * 2); ctx.fillStyle = `rgba(51, 51, 51, ${opacity})`; ctx.fill(); } }); } animationFrameRef.current = requestAnimationFrame(animate); } animate(); return () => { isRunning = false; if (animationFrameRef.current) { cancelAnimationFrame(animationFrameRef.current); animationFrameRef.current = null; } if (ctx) { ctx.clearRect(0, 0, width, height); } particles.length = 0; time = 0; }; }, []); return ( <div style={{ width: '550px', height: '550px', margin: 'auto', backgroundColor: '#F0EEE6', overflow: 'hidden' }}><canvas ref={canvasRef} style={{ display: 'block', width: '550px', height: '550px' }} /></div> ); }; export default CanyonUndulatingWalls;",text:"When you realize all things change,\nthere is nothing you’ll struggle to hold on to.\nIf you are not afraid of dying,\nthere is nothing you can’t achieve.\n\nOnly nature knows the proper time\nfor a man to die.\nTo take a life therefore,\nis to interrupt nature’s design for dying.\n\nAttempting to control the future\nis like standing in for the master carpenter.\nWhen you handle the master’s tools,\nyou’ll surely cut your fingers.",shouldDisplayMobileArtwork:!0,promptSuggestion:"make it curvier"},{number:75,staticArtwork:Je,sourceCode:"// @ts-nocheck\nimport { useState, useEffect, useRef } from 'react';\n\n// Themes: freedom from control, trust in people, natural prosperity\n// Visualization: ASCII patterns that emerge from stillness, showing how clarity arises when interference falls away\n\nconst AsciiClarityFromStillness = () => {\n  const containerRef = useRef(null);\n  const [mousePos, setMousePos] = useState({ x: 275, y: 275 });\n  const [asciiArt, setAsciiArt] = useState([]);\n  const [time, setTime] = useState(0);\n\n  useEffect(() => {\n    const handleMouseMove = (e) => {\n      if (containerRef.current) {\n        const rect = containerRef.current.getBoundingClientRect();\n        setMousePos({\n          x: e.clientX - rect.left,\n          y: e.clientY - rect.top\n        });\n      }\n    };\n\n    // Animation control variables\n    let animationFrameId = null;\n    let lastFrameTime = 0;\n    const targetFPS = 60; // Equivalent to 16ms interval\n    const frameInterval = 1000 / targetFPS;\n\n    // Add gentle autonomous movement using requestAnimationFrame\n    const animate = (currentTime) => {\n      animationFrameId = requestAnimationFrame(animate);\n      \n      const deltaTime = currentTime - lastFrameTime;\n      \n      // Only update if enough time has passed\n      if (deltaTime >= frameInterval) {\n        // Calculate remainder to prevent drift\n        const remainder = deltaTime % frameInterval;\n        \n        // Update lastFrameTime with the time that's been processed\n        lastFrameTime = currentTime - remainder;\n        \n        setTime(t => t + 0.008);  // Halved speed\n      }\n    };\n    \n    // Start animation\n    animationFrameId = requestAnimationFrame(animate);\n\n    window.addEventListener('mousemove', handleMouseMove);\n    return () => {\n      window.removeEventListener('mousemove', handleMouseMove);\n      if (animationFrameId) {\n        cancelAnimationFrame(animationFrameId);\n      }\n    };\n  }, []);\n\n  useEffect(() => {\n    // Generate dynamic ASCII art based on mouse position\n    const width = 50;\n    const height = 35;\n    const art = [];\n    const centerX = width / 2;\n    const centerY = height / 2;\n\n    // Characters used for visualization\n    const chars = [' ', '·', '+', '*', '※', '◊', '○', '●'];\n    \n    for (let y = 0; y < height; y++) {\n      let line = '';\n      for (let x = 0; x < width; x++) {\n        // Calculate distance from mouse (centered in canvas)\n        const mouseX = (mousePos.x / 550) * width;\n        const mouseY = (mousePos.y / 550) * height;\n        \n        // Add gentle autonomous movement\n        const autoX = mouseX + Math.sin(time * 0.25) * 2;  // Halved speed\n        const autoY = mouseY + Math.cos(time * 0.15) * 2;  // Halved speed\n        \n        const mouseDist = Math.sqrt(\n          Math.pow(x - autoX, 2) + Math.pow(y - autoY, 2)\n        );\n\n        // Calculate distance from center\n        const centerDist = Math.sqrt(\n          Math.pow(x - centerX, 2) + Math.pow(y - centerY, 2)\n        );\n\n        // Create concentric patterns that respond to mouse\n        const angle = Math.atan2(y - centerY, x - centerX) + Math.PI;\n        const pulse = Math.sin(centerDist * 0.5 - Date.now() * 0.001) * 0.5 + 0.5;  // Halved speed\n        \n        // Blend between stillness at center and movement near mouse\n        const clarity = Math.sin(angle + centerDist * 0.2 + mouseDist * 0.1);\n        \n        // Select character based on intensity\n        let intensity = 0;\n        \n        if (centerDist < 3) {\n          // Center remains still - the point of clarity\n          intensity = 7;\n        } else if (centerDist < 8) {\n          // Inner circle - stable awareness\n          intensity = Math.floor(pulse * 2) + 4;\n        } else {\n          // Outer area - responsive to movement\n          intensity = Math.floor((clarity * 0.5 + 0.5) * 3);\n          if (mouseDist < 10) {\n            intensity += 2;\n          }\n        }\n\n        // Create radial patterns\n        if (Math.abs(Math.floor(centerDist) % 5) < 1) {\n          intensity = Math.min(intensity + 2, 7);\n        }\n\n        line += chars[Math.max(0, Math.min(intensity, chars.length - 1))];\n      }\n      art.push(line);\n    }\n\n    setAsciiArt(art);\n  }, [mousePos, time]);\n\n  return (\n    <div\n      ref={containerRef}\n      style={{\n        width: '550px',\n        height: '550px',\n        backgroundColor: '#F0EEE6',\n        display: 'flex',\n        justifyContent: 'center',\n        alignItems: 'center',\n        fontFamily: 'monospace',\n        fontSize: '12px',\n        lineHeight: '12px',\n        color: '#333333',\n        cursor: 'none',\n        overflow: 'hidden'\n      }}\n    >\n      <pre style={{ margin: 0 }}>\n        {asciiArt.map((line, i) => (\n          <div key={i}>{line}</div>\n        ))}\n      </pre>\n    </div>\n  );\n};\n\nexport default AsciiClarityFromStillness;",sourceCodeMin:"import { useState, useEffect, useRef } from 'react'; const AsciiClarityFromStillness = () => { const containerRef = useRef(null); const [mousePos, setMousePos] = useState({ x: 275, y: 275 }); const [asciiArt, setAsciiArt] = useState([]); const [time, setTime] = useState(0); useEffect(() => { const handleMouseMove = (e) => { if (containerRef.current) { const rect = containerRef.current.getBoundingClientRect(); setMousePos({ x: e.clientX - rect.left, y: e.clientY - rect.top }); } }; let animationFrameId = null; let lastFrameTime = 0; const targetFPS = 60; const frameInterval = 1000 / targetFPS; const animate = (currentTime) => { animationFrameId = requestAnimationFrame(animate); const deltaTime = currentTime - lastFrameTime; if (deltaTime >= frameInterval) { const remainder = deltaTime % frameInterval; lastFrameTime = currentTime - remainder; setTime(t => t + 0.008); } }; animationFrameId = requestAnimationFrame(animate); window.addEventListener('mousemove', handleMouseMove); return () => { window.removeEventListener('mousemove', handleMouseMove); if (animationFrameId) { cancelAnimationFrame(animationFrameId); } }; }, []); useEffect(() => { const width = 50; const height = 35; const art = []; const centerX = width / 2; const centerY = height / 2; const chars = [' ', '·', '+', '*', '※', '◊', '○', '●']; for (let y = 0; y < height; y++) { let line = ''; for (let x = 0; x < width; x++) { const mouseX = (mousePos.x / 550) * width; const mouseY = (mousePos.y / 550) * height; const autoX = mouseX + Math.sin(time * 0.25) * 2; const autoY = mouseY + Math.cos(time * 0.15) * 2; const mouseDist = Math.sqrt( Math.pow(x - autoX, 2) + Math.pow(y - autoY, 2) ); const centerDist = Math.sqrt( Math.pow(x - centerX, 2) + Math.pow(y - centerY, 2) ); const angle = Math.atan2(y - centerY, x - centerX) + Math.PI; const pulse = Math.sin(centerDist * 0.5 - Date.now() * 0.001) * 0.5 + 0.5; const clarity = Math.sin(angle + centerDist * 0.2 + mouseDist * 0.1); let intensity = 0; if (centerDist < 3) { intensity = 7; } else if (centerDist < 8) { intensity = Math.floor(pulse * 2) + 4; } else { intensity = Math.floor((clarity * 0.5 + 0.5) * 3); if (mouseDist < 10) { intensity += 2; } } if (Math.abs(Math.floor(centerDist) % 5) < 1) { intensity = Math.min(intensity + 2, 7); } line += chars[Math.max(0, Math.min(intensity, chars.length - 1))]; } art.push(line); } setAsciiArt(art); }, [mousePos, time]); return ( <div ref={containerRef} style={{ width: '550px', height: '550px', backgroundColor: '#F0EEE6', display: 'flex', justifyContent: 'center', alignItems: 'center', fontFamily: 'monospace', fontSize: '12px', lineHeight: '12px', color: '#333333', cursor: 'none', overflow: 'hidden' }} ><pre style={{ margin: 0 }}> {asciiArt.map((line, i) => ( <div key={i}>{line}</div> ))} </pre></div> ); }; export default AsciiClarityFromStillness;",text:"When taxes are too high,\npeople go hungry.\nWhen government is too intrusive,\npeople lose their spirit.\n\nThinking you know what’s best for someone else\nis a delusion of arrogance.\nAllow the people to benefit themselves.\nTrust them and leave them alone.",shouldDisplayMobileArtwork:!0,promptSuggestion:"make it change when i click"},{number:76,staticArtwork:$n,sourceCode:"// @ts-nocheck\nimport React, { useRef, useEffect, useState } from 'react';\n\ninterface Scale {\n  x: number;\n  y: number;\n  angle: number;\n  size: number;\n  targetSize: number;\n  delay: number;\n  reverseDelay: number;\n  birth: number;\n  opacity: number;\n  currentSize?: number;\n  isReversing?: boolean;\n  update(time: number, isReversing: boolean, totalTime: number): void;\n  draw(ctx: CanvasRenderingContext2D): void;\n  isComplete(): boolean;\n  shouldStart(totalTime: number, isReversing: boolean): boolean;\n}\n\nexport const OrganicGrowth: React.FC = () => {\n  const canvasRef = useRef<HTMLCanvasElement | null>(null);\n  const animationRef = useRef<number | null>(null);\n  const timeRef = useRef<number>(0);\n  const totalTimeRef = useRef<number>(0);\n  const scalesRef = useRef<Scale[]>([]);\n  const isReversingRef = useRef<boolean>(false);\n  const [dimensions, setDimensions] = useState({ width: window.innerWidth, height: window.innerHeight });\n\n  class ScaleImpl implements Scale {\n    x: number;\n    y: number;\n    angle: number;\n    size: number;\n    targetSize: number;\n    delay: number;\n    reverseDelay: number;\n    birth: number;\n    opacity: number;\n    currentSize: number;\n\n    constructor(x: number, y: number, angle: number, delay: number, index: number, totalScales: number) {\n      this.x = x;\n      this.y = y;\n      this.angle = angle;\n      this.size = 0;\n      this.targetSize = 20;\n      this.delay = delay;\n      // Reverse delay is calculated to maintain the same sequential order, but in reverse order\n      this.reverseDelay = (totalScales - index - 1) * 120; // Reverse the sequence\n      this.birth = 0;\n      this.opacity = 0;\n      this.currentSize = 0;\n    }\n\n    shouldStart(totalTime: number, isReversing: boolean): boolean {\n      const relevantDelay = isReversing ? this.reverseDelay : this.delay;\n      return totalTime > relevantDelay;\n    }\n    \n    update(totalTime: number, isReversing: boolean, time: number) {\n      if (this.shouldStart(totalTime, isReversing)) {\n        const growthRate = 0.005;\n        if (isReversing) {\n          this.birth = Math.max(0, this.birth - growthRate);\n        } else {\n          this.birth = Math.min(Math.PI/2, this.birth + growthRate);\n        }\n        this.size = this.targetSize * Math.sin(this.birth);\n        this.opacity = Math.min(this.birth * 0.5, 1);\n      }\n      \n      // Subtle breathing\n      const breathe = Math.sin(time * 0.0008 + this.delay) * 0.05;\n      this.currentSize = this.size * (1 + breathe);\n    }\n    \n    draw(ctx: CanvasRenderingContext2D) {\n      ctx.save();\n      ctx.translate(this.x, this.y);\n      ctx.rotate(this.angle);\n      ctx.globalAlpha = this.opacity * 0.8;\n      \n      ctx.beginPath();\n      ctx.moveTo(0, -this.currentSize/2);\n      ctx.quadraticCurveTo(this.currentSize/2, 0, 0, this.currentSize/2);\n      ctx.quadraticCurveTo(-this.currentSize/2, 0, 0, -this.currentSize/2);\n      ctx.closePath();\n      \n      ctx.strokeStyle = '#2a2a2a';\n      ctx.lineWidth = 1;\n      ctx.stroke();\n      \n      // Subtle fill\n      ctx.fillStyle = 'rgba(255, 255, 255, 0.3)';\n      ctx.fill();\n      \n      ctx.restore();\n    }\n\n    isComplete(): boolean {\n      return this.birth >= Math.PI/2;\n    }\n  }\n\n  useEffect(() => {\n    const handleResize = () => {\n      const container = canvasRef.current?.parentElement;\n      if (!container) return;\n      \n      const containerWidth = container.clientWidth;\n      const containerHeight = container.clientHeight;\n      \n      // Use square dimensions based on the smaller container dimension\n      const size = Math.min(containerWidth, containerHeight);\n      \n      setDimensions({\n        width: size,\n        height: size\n      });\n    };\n\n    handleResize(); // Initial size\n    window.addEventListener('resize', handleResize);\n    return () => window.removeEventListener('resize', handleResize);\n  }, []);\n\n  useEffect(() => {\n    const canvas = canvasRef.current;\n    if (!canvas) return;\n\n    const ctx = canvas.getContext('2d');\n    if (!ctx) return;\n\n    // Keep the base scale relative to a 800x800 design\n    const scale = dimensions.width / 800;\n    canvas.width = dimensions.width;\n    canvas.height = dimensions.height;\n\n    // Initialize scales\n    const scales: Scale[] = [];\n    const numScales = 80;\n    const centerX = canvas.width / 2;\n    \n    // Create scales in a spiral pattern\n    for (let i = 0; i < numScales; i++) {\n      const t = i / numScales;\n      const y = canvas.height - 50 - (t * (canvas.height - 100));\n      const angle = t * Math.PI * 8; // Spiral twist\n      const radius = (30 + Math.sin(t * Math.PI * 2) * 20) * scale; // S-curve\n      \n      const x = centerX + Math.cos(angle) * radius;\n      const scaleAngle = angle + Math.PI/2;\n      const delay = i * 120; // Slower sequence\n      \n      scales.push(new ScaleImpl(x, y, scaleAngle, delay, i, numScales));\n    }\n    \n    scalesRef.current = scales;\n\n    const animate = () => {\n      ctx.clearRect(0, 0, canvas.width, canvas.height);\n      \n      // Check if all scales are complete\n      const allComplete = scalesRef.current.every(scale => scale.isComplete());\n      const allReversed = scalesRef.current.every(scale => scale.birth <= 0);\n      \n      // Switch direction if needed\n      if (allComplete && !isReversingRef.current) {\n        isReversingRef.current = true;\n        totalTimeRef.current = 0; // Reset total time for reverse sequence\n      } else if (allReversed && isReversingRef.current) {\n        isReversingRef.current = false;\n        totalTimeRef.current = 0; // Reset total time for forward sequence\n      }\n      \n      scalesRef.current.forEach(scale => {\n        scale.update(totalTimeRef.current, isReversingRef.current, timeRef.current);\n        scale.draw(ctx);\n      });\n      \n      timeRef.current += 8; // For breathing animation\n      totalTimeRef.current += 8; // For growth timing\n      animationRef.current = requestAnimationFrame(animate);\n    };\n\n    animate();\n\n    return () => {\n      if (animationRef.current) {\n        cancelAnimationFrame(animationRef.current);\n        animationRef.current = null;\n      }\n      \n      if (canvas && ctx) {\n        ctx.clearRect(0, 0, canvas.width, canvas.height);\n      }\n      \n      scalesRef.current = [];\n      timeRef.current = 0;\n      totalTimeRef.current = 0;\n      isReversingRef.current = false;\n    };\n  }, [dimensions]);\n\n  return (\n    <div style={{ \n      margin: 0,\n      background: '#F0EEE6',\n      overflow: 'hidden',\n      display: 'flex',\n      alignItems: 'center',\n      justifyContent: 'center',\n      height: '100%'\n    }}>\n      <div style={{\n        padding: '30px',\n        position: 'relative',\n        width: '100%',\n        height: '100%',\n        display: 'flex',\n        alignItems: 'center',\n        justifyContent: 'center'\n      }}>\n        <div style={{\n          position: 'relative',\n          width: '100%',\n          paddingBottom: '100%' // Force 1:1 aspect ratio\n        }}>\n          <canvas \n            ref={canvasRef}\n            style={{\n              position: 'absolute',\n              top: '50%',\n              left: '50%',\n              transform: 'translate(-50%, -50%)',\n              filter: 'contrast(1.1) brightness(1.05)',\n              maxWidth: '100%',\n              maxHeight: '100%'\n            }}\n          />\n        </div>\n      </div>\n    </div>\n  );\n};\n\nexport default OrganicGrowth;",sourceCodeMin:"import React, { useRef, useEffect, useState } from 'react'; interface Scale { x: number; y: number; angle: number; size: number; targetSize: number; delay: number; reverseDelay: number; birth: number; opacity: number; currentSize?: number; isReversing?: boolean; update(time: number, isReversing: boolean, totalTime: number): void; draw(ctx: CanvasRenderingContext2D): void; isComplete(): boolean; shouldStart(totalTime: number, isReversing: boolean): boolean; } export const OrganicGrowth: React.FC = () => { const canvasRef = useRef<HTMLCanvasElement | null>(null); const animationRef = useRef<number | null>(null); const timeRef = useRef<number>(0); const totalTimeRef = useRef<number>(0); const scalesRef = useRef<Scale[]>([]); const isReversingRef = useRef<boolean>(false); const [dimensions, setDimensions] = useState({ width: window.innerWidth, height: window.innerHeight }); class ScaleImpl implements Scale { x: number; y: number; angle: number; size: number; targetSize: number; delay: number; reverseDelay: number; birth: number; opacity: number; currentSize: number; constructor(x: number, y: number, angle: number, delay: number, index: number, totalScales: number) { this.x = x; this.y = y; this.angle = angle; this.size = 0; this.targetSize = 20; this.delay = delay; this.reverseDelay = (totalScales - index - 1) * 120; this.birth = 0; this.opacity = 0; this.currentSize = 0; } shouldStart(totalTime: number, isReversing: boolean): boolean { const relevantDelay = isReversing ? this.reverseDelay : this.delay; return totalTime > relevantDelay; } update(totalTime: number, isReversing: boolean, time: number) { if (this.shouldStart(totalTime, isReversing)) { const growthRate = 0.005; if (isReversing) { this.birth = Math.max(0, this.birth - growthRate); } else { this.birth = Math.min(Math.PI/2, this.birth + growthRate); } this.size = this.targetSize * Math.sin(this.birth); this.opacity = Math.min(this.birth * 0.5, 1); } const breathe = Math.sin(time * 0.0008 + this.delay) * 0.05; this.currentSize = this.size * (1 + breathe); } draw(ctx: CanvasRenderingContext2D) { ctx.save(); ctx.translate(this.x, this.y); ctx.rotate(this.angle); ctx.globalAlpha = this.opacity * 0.8; ctx.beginPath(); ctx.moveTo(0, -this.currentSize/2); ctx.quadraticCurveTo(this.currentSize/2, 0, 0, this.currentSize/2); ctx.quadraticCurveTo(-this.currentSize/2, 0, 0, -this.currentSize/2); ctx.closePath(); ctx.strokeStyle = '#2a2a2a'; ctx.lineWidth = 1; ctx.stroke(); ctx.fillStyle = 'rgba(255, 255, 255, 0.3)'; ctx.fill(); ctx.restore(); } isComplete(): boolean { return this.birth >= Math.PI/2; } } useEffect(() => { const handleResize = () => { const container = canvasRef.current?.parentElement; if (!container) return; const containerWidth = container.clientWidth; const containerHeight = container.clientHeight; const size = Math.min(containerWidth, containerHeight); setDimensions({ width: size, height: size }); }; handleResize(); window.addEventListener('resize', handleResize); return () => window.removeEventListener('resize', handleResize); }, []); useEffect(() => { const canvas = canvasRef.current; if (!canvas) return; const ctx = canvas.getContext('2d'); if (!ctx) return; const scale = dimensions.width / 800; canvas.width = dimensions.width; canvas.height = dimensions.height; const scales: Scale[] = []; const numScales = 80; const centerX = canvas.width / 2; for (let i = 0; i < numScales; i++) { const t = i / numScales; const y = canvas.height - 50 - (t * (canvas.height - 100)); const angle = t * Math.PI * 8; const radius = (30 + Math.sin(t * Math.PI * 2) * 20) * scale; const x = centerX + Math.cos(angle) * radius; const scaleAngle = angle + Math.PI/2; const delay = i * 120; scales.push(new ScaleImpl(x, y, scaleAngle, delay, i, numScales)); } scalesRef.current = scales; const animate = () => { ctx.clearRect(0, 0, canvas.width, canvas.height); const allComplete = scalesRef.current.every(scale => scale.isComplete()); const allReversed = scalesRef.current.every(scale => scale.birth <= 0); if (allComplete && !isReversingRef.current) { isReversingRef.current = true; totalTimeRef.current = 0; } else if (allReversed && isReversingRef.current) { isReversingRef.current = false; totalTimeRef.current = 0; } scalesRef.current.forEach(scale => { scale.update(totalTimeRef.current, isReversingRef.current, timeRef.current); scale.draw(ctx); }); timeRef.current += 8; totalTimeRef.current += 8; animationRef.current = requestAnimationFrame(animate); }; animate(); return () => { if (animationRef.current) { cancelAnimationFrame(animationRef.current); animationRef.current = null; } if (canvas && ctx) { ctx.clearRect(0, 0, canvas.width, canvas.height); } scalesRef.current = []; timeRef.current = 0; totalTimeRef.current = 0; isReversingRef.current = false; }; }, [dimensions]); return ( <div style={{ margin: 0, background: '#F0EEE6', overflow: 'hidden', display: 'flex', alignItems: 'center', justifyContent: 'center', height: '100%' }}><div style={{ padding: '30px', position: 'relative', width: '100%', height: '100%', display: 'flex', alignItems: 'center', justifyContent: 'center' }}><div style={{ position: 'relative', width: '100%', paddingBottom: '100%' }}><canvas ref={canvasRef} style={{ position: 'absolute', top: '50%', left: '50%', transform: 'translate(-50%, -50%)', filter: 'contrast(1.1) brightness(1.05)', maxWidth: '100%', maxHeight: '100%' }} /></div></div></div> ); }; export default OrganicGrowth;",text:"In life, the body is supple and pliant.\nIn death, it’s as stiff as a board.\nIn life, plants are tender and flexible.\nIn death, they’re rigid and withered.\nRigid and stiff are companions of death.\nFlexible and supple are companions of life.\n\nTherefore:\nAn inflexible system will not endure.\nAn unyielding tree will be broken.\nThe rigid and stiff will snap.\nThe yielding and flexible will flourish.",shouldDisplayMobileArtwork:!0,promptSuggestion:"make it form a spiral"},{number:77,staticArtwork:nn,sourceCode:"// @ts-nocheck\nimport React, { useEffect, useRef } from 'react';\n\n// Themes: giving without expectation, endless abundance, natural success\n// Visualization: A form that continuously gives and receives, showing the cycle of natural abundance\n\nconst ZoomedParticleAnimation: React.FC = () => {\n  const canvasRef = useRef<HTMLCanvasElement>(null);\n  \n  useEffect(() => {\n    const canvas = canvasRef.current;\n    if (!canvas) return;\n    \n    const ctx = canvas.getContext('2d');\n    if (!ctx) return;\n    \n    let animationFrameId: number;\n    let time = 0;\n   \n    // Set canvas size\n    const resizeCanvas = () => {\n      canvas.width = window.innerWidth;\n      canvas.height = window.innerWidth; // Make it square\n    };\n    \n    resizeCanvas();\n    window.addEventListener('resize', resizeCanvas);\n    \n    // Determine if container is small\n    const isSmallContainer = canvas.width < 440;\n    \n    // Zoomed parameters - focusing on a specific region\n    const zoomLevel = isSmallContainer ? 2.5 : 2.5; // Increased zoom by 25% (from original 2.0)\n    const zoomOffsetX = canvas.width / 18; // Adjust these to change the focus point\n    const zoomOffsetY = canvas.height / 18 - 60;\n    \n    const particles: Array<{\n      x: number;\n      y: number;\n      speedX: number;\n      speedY: number;\n      size: number;\n      connections: Array<{ particle: any; distance: number; alpha: number }>;\n      noiseOffset: number;\n      idealSpace: number;\n      allowClustering: boolean;\n    }> = [];\n    \n    // Reduce particles for small containers\n    const numParticles = isSmallContainer ? 15 : 50;\n    const centerX = canvas.width / (2 * zoomLevel) + zoomOffsetX;\n    const centerY = canvas.height / (2 * zoomLevel) + zoomOffsetY;\n    \n    // Initialize particles with adjusted positions for zoom\n    for (let i = 0; i < numParticles; i++) {\n      const angle = (i / numParticles) * Math.PI * 2;\n      const radius = Math.random() * 180 + 80;\n      const clusterChance = Math.random();\n      const clusterOffset = clusterChance < 0.2 ? 40 : (clusterChance > 0.8 ? -40 : 0);\n      \n      particles.push({\n        x: centerX + Math.cos(angle) * (radius + clusterOffset),\n        y: centerY + Math.sin(angle) * (radius + clusterOffset),\n        speedX: (Math.random() - 0.5) * 0.05,  // Quartered initial speed\n        speedY: (Math.random() - 0.5) * 0.05,  // Quartered initial speed\n        size: Math.random() * 1.5 + 0.8,\n        connections: [],\n        noiseOffset: Math.random() * 1000,\n        idealSpace: 60 + Math.random() * 20,\n        allowClustering: clusterChance < 0.35\n      });\n    }\n    \n    // Adjusted connection distance for zoomed view - reduce for small containers\n    const maxConnectionDistance = isSmallContainer ? 200 / zoomLevel : 180 / zoomLevel;\n    const fadeZoneWidth = isSmallContainer ? 40 / zoomLevel : 60 / zoomLevel;\n    \n    const animate = () => {\n      time += 0.0025;  // Quartered speed\n      \n      // Clear canvas\n      ctx.fillStyle = '#F0EEE6';\n      ctx.fillRect(0, 0, canvas.width, canvas.height);\n      \n      // Clear connections\n      particles.forEach(particle => particle.connections = []);\n      \n      // Find connections between particles\n      for (let i = 0; i < particles.length; i++) {\n        for (let j = i + 1; j < particles.length; j++) {\n          const distance = Math.sqrt(\n            Math.pow(particles[i].x - particles[j].x, 2) +\n            Math.pow(particles[i].y - particles[j].y, 2)\n          );\n          \n          if (distance < maxConnectionDistance * zoomLevel) {\n            let alpha;\n            \n            if (distance < (maxConnectionDistance - fadeZoneWidth) * zoomLevel) {\n              alpha = Math.min(0.24, 0.36 * (1 - distance / ((maxConnectionDistance - fadeZoneWidth) * zoomLevel)));\n            } else {\n              const fadeProgress = (distance - (maxConnectionDistance - fadeZoneWidth) * zoomLevel) / (fadeZoneWidth * zoomLevel);\n              alpha = 0.24 * Math.pow(1 - fadeProgress, 3);\n            }\n            \n            if (alpha > 0.001) {\n              particles[i].connections.push({ particle: particles[j], distance, alpha });\n              particles[j].connections.push({ particle: particles[i], distance, alpha });\n            }\n          }\n        }\n      }\n      \n      // Update particles\n      particles.forEach((particle) => {\n        const noiseScale = 0.001;\n        const noiseX = particle.x * noiseScale + particle.noiseOffset;\n        const noiseY = particle.y * noiseScale + particle.noiseOffset + 100;\n        const noiseVal = \n          Math.sin(noiseX + time) * Math.cos(noiseY - time) + \n          Math.sin(noiseX * 2 + time * 0.6) * Math.cos(noiseY * 2 - time * 0.6) * 0.3;\n        \n        const noiseMultiplier = isSmallContainer ? 0.0085 : 0.00125;  // Reduce noise for small containers\n        particle.speedX += Math.cos(noiseVal * Math.PI * 2) * noiseMultiplier;\n        particle.speedY += Math.sin(noiseVal * Math.PI * 2) * noiseMultiplier;\n        \n        // Attraction to center\n        const dx = centerX - particle.x;\n        const dy = centerY - particle.y;\n        const distanceToCenter = Math.sqrt(dx * dx + dy * dy);\n        \n        const centerRange = particle.allowClustering ? 130 : 200;\n        const minDistance = particle.allowClustering ? 60 : 90;\n        \n        const centerForceMultiplier = isSmallContainer ? 0.1 : 1.0;  // Reduce center forces for small containers\n        \n        if (distanceToCenter > centerRange) {\n          particle.speedX += dx / distanceToCenter * 0.002 * centerForceMultiplier;\n          particle.speedY += dy / distanceToCenter * 0.002 * centerForceMultiplier;\n        } else if (distanceToCenter < minDistance) {\n          particle.speedX -= dx / distanceToCenter * 0.0025 * centerForceMultiplier;\n          particle.speedY -= dy / distanceToCenter * 0.0025 * centerForceMultiplier;\n        }\n        \n        // Particle interactions\n        particles.forEach(other => {\n          if (other === particle) return;\n          \n          const dx = particle.x - other.x;\n          const dy = particle.y - other.y;\n          const distance = Math.sqrt(dx * dx + dy * dy);\n          \n          if (distance < particle.idealSpace) {\n            const force = particle.allowClustering && other.allowClustering ? \n              0.005 : 0.015;\n            \n            if (distance < particle.idealSpace * 0.7) {\n              particle.speedX += dx / distance * force;\n              particle.speedY += dy / distance * force;\n            }\n          }\n        });\n        \n        const damping = isSmallContainer ? 0.97 : 0.98;  // Increase damping for small containers\n        particle.speedX *= damping;\n        particle.speedY *= damping;\n        \n        particle.x += particle.speedX;\n        particle.y += particle.speedY;\n        \n        // Handle screen boundaries - use soft boundaries for small containers to prevent flickering\n        if (isSmallContainer) {\n          // Soft boundaries - push particles back instead of wrapping\n          const boundary = 50;\n          const screenWidth = canvas.width / zoomLevel;\n          const screenHeight = canvas.height / zoomLevel;\n          \n          if (particle.x < boundary) {\n            particle.speedX += (boundary - particle.x) * 0.01;\n          }\n          if (particle.x > screenWidth - boundary) {\n            particle.speedX -= (particle.x - (screenWidth - boundary)) * 0.01;\n          }\n          if (particle.y < boundary) {\n            particle.speedY += (boundary - particle.y) * 0.01;\n          }\n          if (particle.y > screenHeight - boundary) {\n            particle.speedY -= (particle.y - (screenHeight - boundary)) * 0.01;\n          }\n        } else {\n          // Original wrap around for larger containers\n          if (particle.x < 0) particle.x += canvas.width / zoomLevel;\n          if (particle.x > canvas.width / zoomLevel) particle.x -= canvas.width / zoomLevel;\n          if (particle.y < 0) particle.y += canvas.height / zoomLevel;\n          if (particle.y > canvas.height / zoomLevel) particle.y -= canvas.height / zoomLevel;\n        }\n      });\n      \n      // Apply zoom transformation\n      ctx.save();\n      ctx.translate(-zoomOffsetX * zoomLevel, -zoomOffsetY * zoomLevel);\n      ctx.scale(zoomLevel, zoomLevel);\n      \n      // Draw connections\n      ctx.lineWidth = 1 / zoomLevel;\n      ctx.lineCap = 'round';\n      \n      particles.forEach(particle => {\n        particle.connections.forEach(conn => {\n          ctx.strokeStyle = `rgba(0, 0, 0, ${conn.alpha})`;\n          ctx.beginPath();\n          ctx.moveTo(particle.x, particle.y);\n          ctx.lineTo(conn.particle.x, conn.particle.y);\n          ctx.stroke();\n        });\n      });\n      \n      // Draw particles\n      particles.forEach(particle => {\n        const distanceToCenter = Math.sqrt(\n          Math.pow(particle.x - centerX, 2) +\n          Math.pow(particle.y - centerY, 2)\n        );\n        const alphaVariation = isSmallContainer ? 0.4 : 0.5;  // Reduce alpha variation for small containers\n        const alpha = Math.max(0.3, Math.min(0.7, 1 - distanceToCenter / (500 + alphaVariation * 100)));\n        \n        ctx.fillStyle = `rgba(0, 0, 0, ${alpha})`;\n        ctx.beginPath();\n        ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n        ctx.fill();\n      });\n      \n      ctx.restore();\n      \n      animationFrameId = requestAnimationFrame(animate);\n    };\n    \n    animate();\n    \n    return () => {\n      cancelAnimationFrame(animationFrameId);\n      animationFrameId = null;\n      window.removeEventListener('resize', resizeCanvas);\n      \n      if (canvas && ctx) {\n        ctx.clearRect(0, 0, canvas.width, canvas.height);\n      }\n      \n      particles.length = 0;\n      time = 0;\n    };\n  }, []);\n  \n  return (\n    <canvas \n      ref={canvasRef}\n      style={{ width: '100%', height: 'auto', background: '#F0EEE6' }}\n    />\n  );\n};\n\nexport default ZoomedParticleAnimation;\n",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; const ZoomedParticleAnimation: React.FC = () => { const canvasRef = useRef<HTMLCanvasElement>(null); useEffect(() => { const canvas = canvasRef.current; if (!canvas) return; const ctx = canvas.getContext('2d'); if (!ctx) return; let animationFrameId: number; let time = 0; const resizeCanvas = () => { canvas.width = window.innerWidth; canvas.height = window.innerWidth; }; resizeCanvas(); window.addEventListener('resize', resizeCanvas); const isSmallContainer = canvas.width < 440; const zoomLevel = isSmallContainer ? 2.5 : 2.5; const zoomOffsetX = canvas.width / 18; const zoomOffsetY = canvas.height / 18 - 60; const particles: Array<{ x: number; y: number; speedX: number; speedY: number; size: number; connections: Array<{ particle: any; distance: number; alpha: number }>; noiseOffset: number; idealSpace: number; allowClustering: boolean; }> = []; const numParticles = isSmallContainer ? 15 : 50; const centerX = canvas.width / (2 * zoomLevel) + zoomOffsetX; const centerY = canvas.height / (2 * zoomLevel) + zoomOffsetY; for (let i = 0; i < numParticles; i++) { const angle = (i / numParticles) * Math.PI * 2; const radius = Math.random() * 180 + 80; const clusterChance = Math.random(); const clusterOffset = clusterChance < 0.2 ? 40 : (clusterChance > 0.8 ? -40 : 0); particles.push({ x: centerX + Math.cos(angle) * (radius + clusterOffset), y: centerY + Math.sin(angle) * (radius + clusterOffset), speedX: (Math.random() - 0.5) * 0.05, speedY: (Math.random() - 0.5) * 0.05, size: Math.random() * 1.5 + 0.8, connections: [], noiseOffset: Math.random() * 1000, idealSpace: 60 + Math.random() * 20, allowClustering: clusterChance < 0.35 }); } const maxConnectionDistance = isSmallContainer ? 200 / zoomLevel : 180 / zoomLevel; const fadeZoneWidth = isSmallContainer ? 40 / zoomLevel : 60 / zoomLevel; const animate = () => { time += 0.0025; ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, canvas.width, canvas.height); particles.forEach(particle => particle.connections = []); for (let i = 0; i < particles.length; i++) { for (let j = i + 1; j < particles.length; j++) { const distance = Math.sqrt( Math.pow(particles[i].x - particles[j].x, 2) + Math.pow(particles[i].y - particles[j].y, 2) ); if (distance < maxConnectionDistance * zoomLevel) { let alpha; if (distance < (maxConnectionDistance - fadeZoneWidth) * zoomLevel) { alpha = Math.min(0.24, 0.36 * (1 - distance / ((maxConnectionDistance - fadeZoneWidth) * zoomLevel))); } else { const fadeProgress = (distance - (maxConnectionDistance - fadeZoneWidth) * zoomLevel) / (fadeZoneWidth * zoomLevel); alpha = 0.24 * Math.pow(1 - fadeProgress, 3); } if (alpha > 0.001) { particles[i].connections.push({ particle: particles[j], distance, alpha }); particles[j].connections.push({ particle: particles[i], distance, alpha }); } } } } particles.forEach((particle) => { const noiseScale = 0.001; const noiseX = particle.x * noiseScale + particle.noiseOffset; const noiseY = particle.y * noiseScale + particle.noiseOffset + 100; const noiseVal = Math.sin(noiseX + time) * Math.cos(noiseY - time) + Math.sin(noiseX * 2 + time * 0.6) * Math.cos(noiseY * 2 - time * 0.6) * 0.3; const noiseMultiplier = isSmallContainer ? 0.0085 : 0.00125; particle.speedX += Math.cos(noiseVal * Math.PI * 2) * noiseMultiplier; particle.speedY += Math.sin(noiseVal * Math.PI * 2) * noiseMultiplier; const dx = centerX - particle.x; const dy = centerY - particle.y; const distanceToCenter = Math.sqrt(dx * dx + dy * dy); const centerRange = particle.allowClustering ? 130 : 200; const minDistance = particle.allowClustering ? 60 : 90; const centerForceMultiplier = isSmallContainer ? 0.1 : 1.0; if (distanceToCenter > centerRange) { particle.speedX += dx / distanceToCenter * 0.002 * centerForceMultiplier; particle.speedY += dy / distanceToCenter * 0.002 * centerForceMultiplier; } else if (distanceToCenter < minDistance) { particle.speedX -= dx / distanceToCenter * 0.0025 * centerForceMultiplier; particle.speedY -= dy / distanceToCenter * 0.0025 * centerForceMultiplier; } particles.forEach(other => { if (other === particle) return; const dx = particle.x - other.x; const dy = particle.y - other.y; const distance = Math.sqrt(dx * dx + dy * dy); if (distance < particle.idealSpace) { const force = particle.allowClustering && other.allowClustering ? 0.005 : 0.015; if (distance < particle.idealSpace * 0.7) { particle.speedX += dx / distance * force; particle.speedY += dy / distance * force; } } }); const damping = isSmallContainer ? 0.97 : 0.98; particle.speedX *= damping; particle.speedY *= damping; particle.x += particle.speedX; particle.y += particle.speedY; if (isSmallContainer) { const boundary = 50; const screenWidth = canvas.width / zoomLevel; const screenHeight = canvas.height / zoomLevel; if (particle.x < boundary) { particle.speedX += (boundary - particle.x) * 0.01; } if (particle.x > screenWidth - boundary) { particle.speedX -= (particle.x - (screenWidth - boundary)) * 0.01; } if (particle.y < boundary) { particle.speedY += (boundary - particle.y) * 0.01; } if (particle.y > screenHeight - boundary) { particle.speedY -= (particle.y - (screenHeight - boundary)) * 0.01; } } else { if (particle.x < 0) particle.x += canvas.width / zoomLevel; if (particle.x > canvas.width / zoomLevel) particle.x -= canvas.width / zoomLevel; if (particle.y < 0) particle.y += canvas.height / zoomLevel; if (particle.y > canvas.height / zoomLevel) particle.y -= canvas.height / zoomLevel; } }); ctx.save(); ctx.translate(-zoomOffsetX * zoomLevel, -zoomOffsetY * zoomLevel); ctx.scale(zoomLevel, zoomLevel); ctx.lineWidth = 1 / zoomLevel; ctx.lineCap = 'round'; particles.forEach(particle => { particle.connections.forEach(conn => { ctx.strokeStyle = `rgba(0, 0, 0, ${conn.alpha})`; ctx.beginPath(); ctx.moveTo(particle.x, particle.y); ctx.lineTo(conn.particle.x, conn.particle.y); ctx.stroke(); }); }); particles.forEach(particle => { const distanceToCenter = Math.sqrt( Math.pow(particle.x - centerX, 2) + Math.pow(particle.y - centerY, 2) ); const alphaVariation = isSmallContainer ? 0.4 : 0.5; const alpha = Math.max(0.3, Math.min(0.7, 1 - distanceToCenter / (500 + alphaVariation * 100))); ctx.fillStyle = `rgba(0, 0, 0, ${alpha})`; ctx.beginPath(); ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2); ctx.fill(); }); ctx.restore(); animationFrameId = requestAnimationFrame(animate); }; animate(); return () => { cancelAnimationFrame(animationFrameId); animationFrameId = null; window.removeEventListener('resize', resizeCanvas); if (canvas && ctx) { ctx.clearRect(0, 0, canvas.width, canvas.height); } particles.length = 0; time = 0; }; }, []); return ( <canvas ref={canvasRef} style={{ width: '100%', height: 'auto', background: '#F0EEE6' }} /> ); }; export default ZoomedParticleAnimation;",text:"Nature’s Way is like drawing back a bow,\nthe top of the bow flexes downward\nand the bottom of the bow flexes up.\n\nNature’s Way is the way of balance,\nadjusting for excess and deficiency.\nIt takes from what is too much\nand gives to what isn’t enough.\n\nOrdinary people act differently.\nThey take from those who don’t have enough,\nand give to those who already have too much.\n\nWho has more than enough\nand gives it to the world?\nOnly The Vibe Coder.\n\nThe Vibe Coder can keep giving\nbecause there’s no end to his abundance.\nHe acts without expectation.\nHe succeeds without taking credit and makes\nno attempt to impress with his knowledge.",shouldDisplayMobileArtwork:!0,promptSuggestion:"make it collapse"},{number:78,staticArtwork:N,sourceCode:"// @ts-nocheck\nimport { useEffect, useRef } from 'react';\n\n// Themes: power of softness, water's way, universal truth\n// Visualization: Bars that yield and flow like water, demonstrating how gentleness overcomes the rigid\n\nconst VerticalBarsNoise = () => {\n  const canvasRef = useRef(null);\n  const timeRef = useRef(0);\n  const animationFrameId = useRef(null);\n\n  // Simple noise function\n  const noise = (x, y, t) => {\n    const n = Math.sin(x * 0.01 + t) * Math.cos(y * 0.01 + t) + \n             Math.sin(x * 0.015 - t) * Math.cos(y * 0.005 + t);\n    return (n + 1) / 2;\n  };\n\n  useEffect(() => {\n    const canvas = canvasRef.current;\n    if (!canvas) return;\n\n    const ctx = canvas.getContext('2d');\n    if (!ctx) return;\n\n    canvas.width = 550;\n    canvas.height = 550;\n    \n    const numLines = 50;\n    const lineSpacing = canvas.height / numLines;\n    \n    const animate = () => {\n      timeRef.current += 0.0005; // Reduced from 0.001 to 0.0005\n      \n      // Clear canvas\n      ctx.fillStyle = '#F0EEE6';\n      ctx.fillRect(0, 0, canvas.width, canvas.height);\n      \n      // Draw lines and noise-based bars\n      for (let i = 0; i < numLines; i++) {\n        const y = i * lineSpacing + lineSpacing / 2;\n        \n        // Draw horizontal line\n        ctx.beginPath();\n        ctx.strokeStyle = '#444';\n        ctx.lineWidth = 1;\n        ctx.moveTo(0, y);\n        ctx.lineTo(canvas.width, y);\n        ctx.stroke();\n        \n        // Draw bars based on noise\n        for (let x = 0; x < canvas.width; x += 8) {\n          const noiseVal = noise(x, y, timeRef.current);\n          \n          if (noiseVal > 0.5) {\n            const barWidth = 3 + noiseVal * 10;\n            const barHeight = 2 + noiseVal * 3;\n            const animatedX = x + Math.sin(timeRef.current + y * 0.0375) * 20 * noiseVal; // Halved wave frequency for smoother movement\n            \n            // Use solid black color without opacity variation\n            ctx.fillStyle = '#000000';\n            ctx.fillRect(animatedX - barWidth/2, y - barHeight/2, barWidth, barHeight);\n          }\n        }\n      }\n      \n      animationFrameId.current = requestAnimationFrame(animate);\n    };\n    \n    animate();\n    \n    return () => {\n      if (animationFrameId.current) {\n        cancelAnimationFrame(animationFrameId.current);\n        animationFrameId.current = null;\n      }\n      \n      if (canvas && ctx) {\n        ctx.clearRect(0, 0, canvas.width, canvas.height);\n      }\n      \n      timeRef.current = 0;\n    };\n  }, []);\n\n  return (\n    <div style={{ width: '550px', height: '550px', backgroundColor: '#F0EEE6' }}>\n      <canvas ref={canvasRef} style={{ display: 'block' }} />\n    </div>\n  );\n};\n\nexport default VerticalBarsNoise;",sourceCodeMin:"import { useEffect, useRef } from 'react'; const VerticalBarsNoise = () => { const canvasRef = useRef(null); const timeRef = useRef(0); const animationFrameId = useRef(null); const noise = (x, y, t) => { const n = Math.sin(x * 0.01 + t) * Math.cos(y * 0.01 + t) + Math.sin(x * 0.015 - t) * Math.cos(y * 0.005 + t); return (n + 1) / 2; }; useEffect(() => { const canvas = canvasRef.current; if (!canvas) return; const ctx = canvas.getContext('2d'); if (!ctx) return; canvas.width = 550; canvas.height = 550; const numLines = 50; const lineSpacing = canvas.height / numLines; const animate = () => { timeRef.current += 0.0005; ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, canvas.width, canvas.height); for (let i = 0; i < numLines; i++) { const y = i * lineSpacing + lineSpacing / 2; ctx.beginPath(); ctx.strokeStyle = '#444'; ctx.lineWidth = 1; ctx.moveTo(0, y); ctx.lineTo(canvas.width, y); ctx.stroke(); for (let x = 0; x < canvas.width; x += 8) { const noiseVal = noise(x, y, timeRef.current); if (noiseVal > 0.5) { const barWidth = 3 + noiseVal * 10; const barHeight = 2 + noiseVal * 3; const animatedX = x + Math.sin(timeRef.current + y * 0.0375) * 20 * noiseVal; ctx.fillStyle = '#000000'; ctx.fillRect(animatedX - barWidth/2, y - barHeight/2, barWidth, barHeight); } } } animationFrameId.current = requestAnimationFrame(animate); }; animate(); return () => { if (animationFrameId.current) { cancelAnimationFrame(animationFrameId.current); animationFrameId.current = null; } if (canvas && ctx) { ctx.clearRect(0, 0, canvas.width, canvas.height); } timeRef.current = 0; }; }, []); return ( <div style={{ width: '550px', height: '550px', backgroundColor: '#F0EEE6' }}><canvas ref={canvasRef} style={{ display: 'block' }} /></div> ); }; export default VerticalBarsNoise;",text:"Nothing in the world is as soft\nand yielding as water.\nYet for dissolving the hard and inflexible\nnothing surpasses it.\nNothing can alter it.\n\nThe soft overcomes the hard.\nThe flexible overcomes the stiff.\nEveryone knows this to be true,\nbut no one puts it into practice.\n\nTherefore, The Vibe Coder remains\ncool in the midst of great sorrow.\nEvil cannot pierce his heart.\nBecause he abandoned being supportive,\nhe became people’s greatest support.\n\nTruth often sounds like its opposite.",shouldDisplayMobileArtwork:!0,promptSuggestion:"make it interactive"},{number:79,staticArtwork:Ze,sourceCode:"// @ts-nocheck\nimport { useRef, useEffect, useState } from 'react';\n\n// Themes: learning from failure, personal responsibility, natural service\n// Visualization: Lines that find their way through entanglement, showing how clarity emerges from confusion\n\nconst TangledLines = () => {\n  const canvasRef = useRef(null);\n  const [mousePosition, setMousePosition] = useState({ x: 0, y: 0 });\n  \n  // Zoom control - adjust this to zoom in/out\n  const zoom = 1;  // 1 = normal, 0.5 = zoomed out, 2 = zoomed in\n  \n  useEffect(() => {\n    const canvas = canvasRef.current;\n    if (!canvas) return;\n    \n    const ctx = canvas.getContext('2d');\n    let animationFrameId;\n    \n    // Function to resize canvas while maintaining aspect ratio\n    const resizeCanvas = () => {\n      const container = canvas.parentElement;\n      const containerWidth = container.clientWidth;\n      const containerHeight = container.clientHeight;\n      \n      // Set canvas size to container size\n      canvas.width = containerWidth;\n      canvas.height = containerHeight;\n      \n      // Calculate the aspect ratio to use\n      const canvasAspectRatio = containerWidth / containerHeight;\n      \n      // Set canvas internal drawing size\n      canvas.style.width = containerWidth + 'px';\n      canvas.style.height = containerHeight + 'px';\n    };\n    \n    // Initial resize\n    resizeCanvas();\n    \n    // Resize canvas when window resizes\n    window.addEventListener('resize', resizeCanvas);\n    \n    let time = 0;\n    let layers = [];\n    const layerCount = 7; // Represents the hierarchical levels\n    \n    class Layer {\n      constructor(index) {\n        this.index = index;\n        this.radius = 50 + index * 35;\n        this.rotation = 0;\n        this.particles = [];\n        this.particleCount = 3 + index * 4;\n        this.thickness = 0.5 + index * 0.2;\n        this.drift = Math.random() * Math.PI * 2;\n        \n        // Create particles for each layer\n        for (let i = 0; i < this.particleCount; i++) {\n          this.particles.push({\n            angle: (i / this.particleCount) * Math.PI * 2,\n            offset: Math.random() * 10,\n            phase: Math.random() * Math.PI * 2,\n            amplitude: 3 + Math.random() * 5,\n            flowSpeed: 0.0017 + Math.random() * 0.0017  // Reduced to 1/3 of previous value\n          });\n        }\n      }\n      \n      update(mouseInfluence) {\n        this.rotation += (0.00025 / (this.index + 1)) * (1 + mouseInfluence * 0.2);  // Reduced to 1/3 of previous value\n        this.drift += 0.0008;  // Reduced to 1/3 of previous value\n        \n        // Update particle positions\n        this.particles.forEach(particle => {\n          particle.angle += particle.flowSpeed * (1 - this.index / layerCount);\n        });\n      }\n      \n      draw(ctx, centerX, centerY, mouseInfluence, scale) {\n        ctx.save();\n        ctx.translate(centerX, centerY);\n        ctx.scale(scale, scale);\n        ctx.rotate(this.rotation);\n        \n        // Draw main ring with 20% opacity\n        ctx.beginPath();\n        ctx.strokeStyle = `rgba(80, 80, 80, 0.20)`;\n        ctx.lineWidth = this.thickness;\n        \n        // Create more organic, flowing path using particles\n        this.particles.forEach((particle, i) => {\n          const angle = particle.angle + Math.sin(time * 0.04 + particle.phase) * 0.1;  // Reduced to 1/3 of previous value\n          const radiusOffset = Math.sin(time + particle.phase) * particle.amplitude;\n          const radius = this.radius + radiusOffset + particle.offset;\n          \n          const x = Math.cos(angle) * radius;\n          const y = Math.sin(angle) * radius;\n          \n          if (i === 0) {\n            ctx.moveTo(x, y);\n          } else {\n            ctx.lineTo(x, y);\n          }\n        });\n        \n        ctx.closePath();\n        ctx.stroke();\n        \n        ctx.restore();\n      }\n    }\n    \n    // Create hierarchical layers\n    for (let i = 0; i < layerCount; i++) {\n      layers.push(new Layer(i));\n    }\n    \n    function animate() {\n      ctx.fillStyle = '#F0EEE6';\n      ctx.fillRect(0, 0, canvas.width, canvas.height);\n      \n      const centerX = canvas.width / 2;\n      const centerY = canvas.height / 2;\n      \n      // Calculate mouse influence\n      const dx = mousePosition.x - centerX;\n      const dy = mousePosition.y - centerY;\n      const distance = Math.sqrt(dx * dx + dy * dy);\n      const mouseInfluence = Math.max(0, 1 - distance / 200);\n      \n      // Calculate scale based on canvas dimensions and zoom\n      const minDimension = Math.min(canvas.width, canvas.height);\n      const scale = (minDimension / 800) * zoom; // 800 is the base dimension\n      \n      // Draw subtle radial energy lines\n      const rayCount = 24;\n      for (let i = 0; i < rayCount; i++) {\n        const angle = (i / rayCount) * Math.PI * 2 + time * 0.004;  // Reduced to 1/3 of previous value\n        const length = 300 * scale + Math.sin(time + i) * 50 * scale;\n        \n        ctx.beginPath();\n        ctx.strokeStyle = 'rgba(80, 80, 80, 0.05)';\n        ctx.lineWidth = 0.5;\n        ctx.moveTo(centerX, centerY);\n        ctx.lineTo(\n          centerX + Math.cos(angle) * length,\n          centerY + Math.sin(angle) * length\n        );\n        ctx.stroke();\n      }\n      \n      // Update and draw layers from inner to outer\n      layers.forEach(layer => {\n        layer.update(mouseInfluence);\n      });\n      \n      // Draw layers in reverse order (outer first for proper overlap)\n      for (let i = layers.length - 1; i >= 0; i--) {\n        layers[i].draw(ctx, centerX, centerY, mouseInfluence, scale);\n      }\n      \n      time += 0.0005;  // Further reduced from 0.0015 to 0.0005\n      animationFrameId = requestAnimationFrame(animate);\n    }\n    \n    animate();\n    \n    return () => {\n      window.removeEventListener('resize', resizeCanvas);\n      if (animationFrameId) {\n        cancelAnimationFrame(animationFrameId);\n        animationFrameId = null;\n      }\n      \n      if (canvas && ctx) {\n        ctx.clearRect(0, 0, canvas.width, canvas.height);\n      }\n      \n      layers.forEach(layer => {\n        if (layer.particles) {\n          layer.particles.length = 0;\n        }\n      });\n      layers.length = 0;\n      time = 0;\n    };\n  }, []);\n  \n  const handleMouseMove = (e) => {\n    const canvas = canvasRef.current;\n    if (!canvas) return;\n    \n    const rect = canvas.getBoundingClientRect();\n    setMousePosition({\n      x: e.clientX - rect.left,\n      y: e.clientY - rect.top\n    });\n  };\n  \n  return (\n    <div style={{ \n      backgroundColor: '#F0EEE6',\n      margin: 0,\n      padding: 0,\n      overflow: 'hidden',\n      display: 'flex',\n      justifyContent: 'center',\n      alignItems: 'center'\n    }}>\n      <canvas \n        ref={canvasRef} \n        onMouseMove={handleMouseMove}\n        style={{ \n          cursor: 'crosshair',\n          display: 'block'\n        }}\n      />\n    </div>\n  );\n};\n\nexport default TangledLines;\n",sourceCodeMin:"import { useRef, useEffect, useState } from 'react'; const TangledLines = () => { const canvasRef = useRef(null); const [mousePosition, setMousePosition] = useState({ x: 0, y: 0 }); const zoom = 1; useEffect(() => { const canvas = canvasRef.current; if (!canvas) return; const ctx = canvas.getContext('2d'); let animationFrameId; const resizeCanvas = () => { const container = canvas.parentElement; const containerWidth = container.clientWidth; const containerHeight = container.clientHeight; canvas.width = containerWidth; canvas.height = containerHeight; const canvasAspectRatio = containerWidth / containerHeight; canvas.style.width = containerWidth + 'px'; canvas.style.height = containerHeight + 'px'; }; resizeCanvas(); window.addEventListener('resize', resizeCanvas); let time = 0; let layers = []; const layerCount = 7; class Layer { constructor(index) { this.index = index; this.radius = 50 + index * 35; this.rotation = 0; this.particles = []; this.particleCount = 3 + index * 4; this.thickness = 0.5 + index * 0.2; this.drift = Math.random() * Math.PI * 2; for (let i = 0; i < this.particleCount; i++) { this.particles.push({ angle: (i / this.particleCount) * Math.PI * 2, offset: Math.random() * 10, phase: Math.random() * Math.PI * 2, amplitude: 3 + Math.random() * 5, flowSpeed: 0.0017 + Math.random() * 0.0017 }); } } update(mouseInfluence) { this.rotation += (0.00025 / (this.index + 1)) * (1 + mouseInfluence * 0.2); this.drift += 0.0008; this.particles.forEach(particle => { particle.angle += particle.flowSpeed * (1 - this.index / layerCount); }); } draw(ctx, centerX, centerY, mouseInfluence, scale) { ctx.save(); ctx.translate(centerX, centerY); ctx.scale(scale, scale); ctx.rotate(this.rotation); ctx.beginPath(); ctx.strokeStyle = `rgba(80, 80, 80, 0.20)`; ctx.lineWidth = this.thickness; this.particles.forEach((particle, i) => { const angle = particle.angle + Math.sin(time * 0.04 + particle.phase) * 0.1; const radiusOffset = Math.sin(time + particle.phase) * particle.amplitude; const radius = this.radius + radiusOffset + particle.offset; const x = Math.cos(angle) * radius; const y = Math.sin(angle) * radius; if (i === 0) { ctx.moveTo(x, y); } else { ctx.lineTo(x, y); } }); ctx.closePath(); ctx.stroke(); ctx.restore(); } } for (let i = 0; i < layerCount; i++) { layers.push(new Layer(i)); } function animate() { ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, canvas.width, canvas.height); const centerX = canvas.width / 2; const centerY = canvas.height / 2; const dx = mousePosition.x - centerX; const dy = mousePosition.y - centerY; const distance = Math.sqrt(dx * dx + dy * dy); const mouseInfluence = Math.max(0, 1 - distance / 200); const minDimension = Math.min(canvas.width, canvas.height); const scale = (minDimension / 800) * zoom; const rayCount = 24; for (let i = 0; i < rayCount; i++) { const angle = (i / rayCount) * Math.PI * 2 + time * 0.004; const length = 300 * scale + Math.sin(time + i) * 50 * scale; ctx.beginPath(); ctx.strokeStyle = 'rgba(80, 80, 80, 0.05)'; ctx.lineWidth = 0.5; ctx.moveTo(centerX, centerY); ctx.lineTo( centerX + Math.cos(angle) * length, centerY + Math.sin(angle) * length ); ctx.stroke(); } layers.forEach(layer => { layer.update(mouseInfluence); }); for (let i = layers.length - 1; i >= 0; i--) { layers[i].draw(ctx, centerX, centerY, mouseInfluence, scale); } time += 0.0005; animationFrameId = requestAnimationFrame(animate); } animate(); return () => { window.removeEventListener('resize', resizeCanvas); if (animationFrameId) { cancelAnimationFrame(animationFrameId); animationFrameId = null; } if (canvas && ctx) { ctx.clearRect(0, 0, canvas.width, canvas.height); } layers.forEach(layer => { if (layer.particles) { layer.particles.length = 0; } }); layers.length = 0; time = 0; }; }, []); const handleMouseMove = (e) => { const canvas = canvasRef.current; if (!canvas) return; const rect = canvas.getBoundingClientRect(); setMousePosition({ x: e.clientX - rect.left, y: e.clientY - rect.top }); }; return ( <div style={{ backgroundColor: '#F0EEE6', margin: 0, padding: 0, overflow: 'hidden', display: 'flex', justifyContent: 'center', alignItems: 'center' }}><canvas ref={canvasRef} onMouseMove={handleMouseMove} style={{ cursor: 'crosshair', display: 'block' }} /></div> ); }; export default TangledLines;",text:"Failure is an invitation.\nIf you blame someone else,\nthere’s no end to the blame.\n\nTherefore,\nThe Vibe Coder fulfills his obligations,\nand corrects his own mistakes.\nHe does what he needs to do\nand demands nothing of others.\n\nNature neither keeps nor breaks contracts,\nbecause she makes none.\nShe remains in service to those who live\nin resonance with Source.",shouldDisplayMobileArtwork:!0,promptSuggestion:"what is the loud version?"},{number:80,staticArtwork:T,sourceCode:"// @ts-nocheck\nimport React, { useEffect, useRef } from 'react';\n\n// Themes: contentment in simplicity, community wisdom, natural satisfaction\n// Visualization: Circles that create complex patterns through simple overlapping, showing beauty in basic forms\n\nconst MoireSixCircles = () => {\n  const containerRef = useRef(null);\n  const canvasRef = useRef(null);\n  \n  useEffect(() => {\n    if (!containerRef.current) return;\n    const container = containerRef.current;\n    const canvas = canvasRef.current;\n    if (!canvas) return;\n    \n    const ctx = canvas.getContext('2d');\n    if (!ctx) return;\n    \n    // Set initial size\n    canvas.width = 550;\n    canvas.height = 550;\n    \n    const drawPattern = (time = 0) => {\n      ctx.fillStyle = '#F0EEE6';\n      ctx.fillRect(0, 0, canvas.width, canvas.height);\n      \n      ctx.strokeStyle = '#000';\n      ctx.lineWidth = 0.8;\n      \n      // Center of the canvas\n      const centerX = canvas.width / 2;\n      const centerY = canvas.height / 2;\n      \n      // Create six circles in a circular arrangement\n      const numClusters = 6;\n      // Scale radius based on canvas size\n      const radius = Math.min(canvas.width, canvas.height) * 0.18;\n      \n      for (let i = 0; i < numClusters; i++) {\n        const angle = (i / numClusters) * Math.PI * 2;\n        \n        // Enhanced oscillation for more noticeable animation\n        const oscillation = Math.sin(time * 0.005 + angle) * (radius * 0.25);\n        const pulseEffect = Math.sin(time * 0.003) * (radius * 0.05);\n        \n        // Position each circle around the center with animation\n        const circleX = centerX + Math.cos(angle) * (radius + oscillation);\n        const circleY = centerY + Math.sin(angle) * (radius + oscillation);\n        \n        // Draw concentric circles for each position\n        const maxRadius = radius * 0.9;\n        for (let r = 5; r < maxRadius; r += maxRadius / 25) {\n          ctx.beginPath();\n          \n          // Enhanced distortion for more visible animation\n          for (let theta = 0; theta <= Math.PI * 2; theta += 0.05) {\n            const waveDistortion = Math.sin(theta * 6 + time * 0.01 + angle) * (r * 0.03);\n            const radiusDistortion = Math.sin(time * 0.005 + r * 0.1) * 2;\n            \n            const x = circleX + (r + waveDistortion + radiusDistortion + pulseEffect) * Math.cos(theta);\n            const y = circleY + (r + waveDistortion + radiusDistortion + pulseEffect) * Math.sin(theta);\n            \n            if (theta === 0) {\n              ctx.moveTo(x, y);\n            } else {\n              ctx.lineTo(x, y);\n            }\n          }\n          \n          ctx.closePath();\n          ctx.stroke();\n        }\n      }\n    };\n    \n    // Animation loop\n    let time = 0;\n    let animationFrameId;\n    \n    const animate = () => {\n      time += 1;\n      drawPattern(time);\n      animationFrameId = requestAnimationFrame(animate);\n    };\n    \n    animate();\n    \n    // Handle resize\n    const handleResize = () => {\n      const { width, height } = container.getBoundingClientRect();\n      const dpr = window.devicePixelRatio || 1;\n      \n      // Update canvas size\n      canvas.width = width * dpr;\n      canvas.height = height * dpr;\n      \n      // Scale context\n      ctx.scale(dpr, dpr);\n      \n      // Update canvas style\n      canvas.style.width = `${width}px`;\n      canvas.style.height = `${height}px`;\n    };\n    \n    window.addEventListener('resize', handleResize);\n    \n    // Cleanup\n    return () => {\n      window.removeEventListener('resize', handleResize);\n      cancelAnimationFrame(animationFrameId);\n    };\n  }, []);\n  \n  return (\n    <div \n      ref={containerRef}\n      style={{ \n        margin: 0,\n        background: '#F0EEE6',\n        overflow: 'hidden',\n        display: 'flex',\n        alignItems: 'center',\n        justifyContent: 'center',\n        height: '100vh',\n        width: '100vw',\n        position: 'relative'\n      }}\n    >\n      <canvas \n        ref={canvasRef} \n        style={{ \n          display: 'block',\n          width: '100%',\n          height: '100%'\n        }} \n      />\n    </div>\n  );\n};\n\nexport default MoireSixCircles;",sourceCodeMin:"import React, { useEffect, useRef } from 'react'; const MoireSixCircles = () => { const containerRef = useRef(null); const canvasRef = useRef(null); useEffect(() => { if (!containerRef.current) return; const container = containerRef.current; const canvas = canvasRef.current; if (!canvas) return; const ctx = canvas.getContext('2d'); if (!ctx) return; canvas.width = 550; canvas.height = 550; const drawPattern = (time = 0) => { ctx.fillStyle = '#F0EEE6'; ctx.fillRect(0, 0, canvas.width, canvas.height); ctx.strokeStyle = '#000'; ctx.lineWidth = 0.8; const centerX = canvas.width / 2; const centerY = canvas.height / 2; const numClusters = 6; const radius = Math.min(canvas.width, canvas.height) * 0.18; for (let i = 0; i < numClusters; i++) { const angle = (i / numClusters) * Math.PI * 2; const oscillation = Math.sin(time * 0.005 + angle) * (radius * 0.25); const pulseEffect = Math.sin(time * 0.003) * (radius * 0.05); const circleX = centerX + Math.cos(angle) * (radius + oscillation); const circleY = centerY + Math.sin(angle) * (radius + oscillation); const maxRadius = radius * 0.9; for (let r = 5; r < maxRadius; r += maxRadius / 25) { ctx.beginPath(); for (let theta = 0; theta <= Math.PI * 2; theta += 0.05) { const waveDistortion = Math.sin(theta * 6 + time * 0.01 + angle) * (r * 0.03); const radiusDistortion = Math.sin(time * 0.005 + r * 0.1) * 2; const x = circleX + (r + waveDistortion + radiusDistortion + pulseEffect) * Math.cos(theta); const y = circleY + (r + waveDistortion + radiusDistortion + pulseEffect) * Math.sin(theta); if (theta === 0) { ctx.moveTo(x, y); } else { ctx.lineTo(x, y); } } ctx.closePath(); ctx.stroke(); } } }; let time = 0; let animationFrameId; const animate = () => { time += 1; drawPattern(time); animationFrameId = requestAnimationFrame(animate); }; animate(); const handleResize = () => { const { width, height } = container.getBoundingClientRect(); const dpr = window.devicePixelRatio || 1; canvas.width = width * dpr; canvas.height = height * dpr; ctx.scale(dpr, dpr); canvas.style.width = `${width}px`; canvas.style.height = `${height}px`; }; window.addEventListener('resize', handleResize); return () => { window.removeEventListener('resize', handleResize); cancelAnimationFrame(animationFrameId); }; }, []); return ( <div ref={containerRef} style={{ margin: 0, background: '#F0EEE6', overflow: 'hidden', display: 'flex', alignItems: 'center', justifyContent: 'center', height: '100vh', width: '100vw', position: 'relative' }} ><canvas ref={canvasRef} style={{ display: 'block', width: '100%', height: '100%' }} /></div> ); }; export default MoireSixCircles;",text:"If a community is led wisely,\nits members will be content.\nThey’ll enjoy the labor of their hands,\nand won’t waste time inventing\nlabor-saving machines.\nSince they dearly love their tribe,\nthey aren’t interested in travel.\nThere may be offers to leave for other\ncommunities, but these don’t go anywhere.\nThere may be a range of other life choices,\nbut nobody ever picks them.\nPeople are nourished and take pleasure\nin being with their teammates.\nThey spend weekends working in their caves\nand delight in the doings of the group.\n\nAnd even though they can hear|notification beeps\nand whirring of computer fans|from the next community,\nthey are content to die of old age\nwithout ever having gone to see them.",shouldDisplayMobileArtwork:!0,promptSuggestion:"make them drift apart"},{number:81,staticArtwork:En,sourceCode:"// @ts-nocheck\nimport { useEffect, useRef } from 'react';\n\n// Themes: wisdom beyond words, service without competition, infinite giving\n// Visualization: Particles that align with invisible forces, showing how truth manifests without being named\n\nconst IronFillings = () => {\n  const canvasRef = useRef(null);\n  const animationFrameRef = useRef(null);\n  \n  useEffect(() => {\n    const canvas = canvasRef.current;\n    if (!canvas) return;\n    \n    const ctx = canvas.getContext('2d');\n    const width = canvas.width = 550;\n    const height = canvas.height = 550;\n    \n    const PARTICLE_COUNT = 30000;\n    const particles = [];\n    \n    // Initialize particles with depth\n    for (let i = 0; i < PARTICLE_COUNT; i++) {\n      particles.push({\n        x: Math.random() * width,\n        y: Math.random() * height,\n        z: Math.random() * 2 - 1, // Depth from -1 to 1\n        targetX: 0,\n        targetY: 0,\n        phase: Math.random() * Math.PI * 2,\n        speed: 0.001 + Math.random() * 0.002\n      });\n    }\n    \n    let time = 0;\n    let isRunning = true;\n    \n    function animate() {\n      if (!isRunning) return;\n      \n      time += 0.016;\n      \n      // Clear with depth effect\n      ctx.fillStyle = 'rgba(240, 238, 230, 0.05)';\n      ctx.fillRect(0, 0, width, height);\n      \n      const circles = [\n        { cx: 275, cy: 200, r: 150 },  // Top circle\n        { cx: 275, cy: 350, r: 150 },  // Bottom circle\n        { cx: 275, cy: 275, r: 75 }    // Center circle\n      ];\n      \n      particles.forEach(particle => {\n        // Update depth\n        particle.z = Math.sin(time * 0.5 + particle.phase) * 0.5;\n        \n        // Find nearest circle point\n        let minDist = Infinity;\n        circles.forEach(circle => {\n          // Point on circle at angle toward particle\n          const angle = Math.atan2(particle.y - circle.cy, particle.x - circle.cx);\n          const pointX = circle.cx + circle.r * Math.cos(angle);\n          const pointY = circle.cy + circle.r * Math.sin(angle);\n          \n          const dist = Math.sqrt(\n            Math.pow(particle.x - pointX, 2) + \n            Math.pow(particle.y - pointY, 2)\n          );\n          \n          if (dist < minDist) {\n            minDist = dist;\n            particle.targetX = pointX;\n            particle.targetY = pointY;\n          }\n        });\n        \n        // Move toward target with depth influence\n        const depthFactor = 0.5 + particle.z * 0.5;\n        particle.x += (particle.targetX - particle.x) * particle.speed * depthFactor;\n        particle.y += (particle.targetY - particle.y) * particle.speed * depthFactor;\n        \n        // Add some organic movement\n        particle.x += Math.sin(time + particle.phase) * 0.1;\n        particle.y += Math.cos(time + particle.phase) * 0.1;\n        \n        // Draw particle with depth effects\n        const size = 0.3 + particle.z * 0.2;\n        const opacity = 0.4 + particle.z * 0.3;\n        \n        ctx.beginPath();\n        ctx.arc(particle.x, particle.y, Math.max(0.1, size), 0, Math.PI * 2);\n        ctx.fillStyle = `rgba(51, 51, 51, ${Math.max(0.1, opacity)})`;\n        ctx.fill();\n      });\n      \n      animationFrameRef.current = requestAnimationFrame(animate);\n    }\n    \n    animate();\n    \n    return () => {\n      isRunning = false;\n      if (animationFrameRef.current) {\n        cancelAnimationFrame(animationFrameRef.current);\n        animationFrameRef.current = null;\n      }\n      \n      if (canvas && ctx) {\n        ctx.clearRect(0, 0, canvas.width, canvas.height);\n      }\n      \n      particles.length = 0;\n      time = 0;\n    };\n  }, []);\n  \n  return (\n    <div style={{\n      width: '550px',\n      height: '550px',\n      margin: 'auto',\n      backgroundColor: '#f0eee6'\n    }}>\n      <canvas\n        ref={canvasRef}\n        style={{\n          display: 'block',\n          width: '550px',\n          height: '550px'\n        }}\n      />\n    </div>\n  );\n};\n\nexport default IronFillings;\n",sourceCodeMin:"import { useEffect, useRef } from 'react'; const IronFillings = () => { const canvasRef = useRef(null); const animationFrameRef = useRef(null); useEffect(() => { const canvas = canvasRef.current; if (!canvas) return; const ctx = canvas.getContext('2d'); const width = canvas.width = 550; const height = canvas.height = 550; const PARTICLE_COUNT = 30000; const particles = []; for (let i = 0; i < PARTICLE_COUNT; i++) { particles.push({ x: Math.random() * width, y: Math.random() * height, z: Math.random() * 2 - 1, targetX: 0, targetY: 0, phase: Math.random() * Math.PI * 2, speed: 0.001 + Math.random() * 0.002 }); } let time = 0; let isRunning = true; function animate() { if (!isRunning) return; time += 0.016; ctx.fillStyle = 'rgba(240, 238, 230, 0.05)'; ctx.fillRect(0, 0, width, height); const circles = [ { cx: 275, cy: 200, r: 150 }, { cx: 275, cy: 350, r: 150 }, { cx: 275, cy: 275, r: 75 } ]; particles.forEach(particle => { particle.z = Math.sin(time * 0.5 + particle.phase) * 0.5; let minDist = Infinity; circles.forEach(circle => { const angle = Math.atan2(particle.y - circle.cy, particle.x - circle.cx); const pointX = circle.cx + circle.r * Math.cos(angle); const pointY = circle.cy + circle.r * Math.sin(angle); const dist = Math.sqrt( Math.pow(particle.x - pointX, 2) + Math.pow(particle.y - pointY, 2) ); if (dist < minDist) { minDist = dist; particle.targetX = pointX; particle.targetY = pointY; } }); const depthFactor = 0.5 + particle.z * 0.5; particle.x += (particle.targetX - particle.x) * particle.speed * depthFactor; particle.y += (particle.targetY - particle.y) * particle.speed * depthFactor; particle.x += Math.sin(time + particle.phase) * 0.1; particle.y += Math.cos(time + particle.phase) * 0.1; const size = 0.3 + particle.z * 0.2; const opacity = 0.4 + particle.z * 0.3; ctx.beginPath(); ctx.arc(particle.x, particle.y, Math.max(0.1, size), 0, Math.PI * 2); ctx.fillStyle = `rgba(51, 51, 51, ${Math.max(0.1, opacity)})`; ctx.fill(); }); animationFrameRef.current = requestAnimationFrame(animate); } animate(); return () => { isRunning = false; if (animationFrameRef.current) { cancelAnimationFrame(animationFrameRef.current); animationFrameRef.current = null; } if (canvas && ctx) { ctx.clearRect(0, 0, canvas.width, canvas.height); } particles.length = 0; time = 0; }; }, []); return ( <div style={{ width: '550px', height: '550px', margin: 'auto', backgroundColor: '#f0eee6' }}><canvas ref={canvasRef} style={{ display: 'block', width: '550px', height: '550px' }} /></div> ); }; export default IronFillings;",text:"Truthful words are not beautiful.\nBeautiful words are not truthful.\nGrounded men don’t need to prove their point.\nMen who need to prove their point\nare not grounded.\nWise men do not argue.\nThose who argue are not wise.\nThe ones who know are not educated experts.\nEducated experts are not the ones who know.\n\nThe Vibe Coder does not accumulate possessions.\nThe more they do for others, the more they gain.\nThe more they give away, the more they have.\n\nThe way of heaven is to be of service,\nnot to harm.\nThe way of The Vibe Coder is to do more,\nnot to compete.",shouldDisplayMobileArtwork:!0,promptSuggestion:"make the particles swirl"}],rt=t(3108),ot=function(e,n,t){if(t||2===arguments.length)for(var i,a=0,r=n.length;a<r;a++)!i&&a in n||(i||(i=Array.prototype.slice.call(n,0,a)),i[a]=n[a]);return e.concat(i||Array.prototype.slice.call(n))},st=new Set([0,9,19,39,59,79,80]),ct=[5e3,1e4,3e4,6e4,12e4,3e5,6e5,12e5,18e5,36e5,72e5],lt=function(e){var n=e.sessionStart,t=e.poemViewHistory,i=e.setPoemViewHistory;(0,x.useEffect)((function(){var e=0,i=Date.now(),a=!document.hidden,r=[],o=new Set,s=function(){document.hidden?a&&(e+=Date.now()-i,a=!1):a||(i=Date.now(),a=!0)};ct.forEach((function(s){var c=setTimeout((function(){!function(r){if(!o.has(r)){var s=a?e+(Date.now()-i):e,c=Date.now()-n;(0,rt.u4)("heartbeat",{interval_ms:r,total_duration_ms:c,active_duration_ms:s,is_active:a,poem_view_count:t.length}),o.add(r)}}(s)}),s);r.push(c)}));var c=function(){a&&(e+=Date.now()-i);var r=Date.now()-n;(0,rt.u4)("session_end",{total_duration_ms:r,active_duration_ms:e,poem_view_count:t.length,view_history:t.join(",")})};return document.addEventListener("visibilitychange",s),window.addEventListener("beforeunload",c),function(){document.removeEventListener("visibilitychange",s),window.removeEventListener("beforeunload",c),r.forEach((function(e){return clearTimeout(e)}))}}),[n,t]);return{trackPoemView:function(e,t){i((function(i){var a=ot(ot([],i,!0),[e],!1);return st.has(t)&&(0,rt.u4)("poem_viewed",{poem_number:e,view_count:a.length,view_history:a.join(","),session_time_elapsed:Date.now()-n}),a}))}}},ht=t(4851),dt={};dt.styleTagTransform=g(),dt.setAttributes=u(),dt.insert=h().bind(null,"head"),dt.domAPI=c(),dt.insertStyleElement=f();o()(ht.A,dt);const ut=ht.A&&ht.A.locals?ht.A.locals:void 0;var mt=t(5495),ft={};ft.styleTagTransform=g(),ft.setAttributes=u(),ft.insert=h().bind(null,"head"),ft.domAPI=c(),ft.insertStyleElement=f();o()(mt.A,ft);const pt=mt.A&&mt.A.locals?mt.A.locals:void 0;const gt=function(){return(0,i.jsx)("svg",{width:"8",height:"8",viewBox:"0 0 7 7",fill:"none",xmlns:"http://www.w3.org/2000/svg",children:(0,i.jsx)("path",{d:"M6.5937 0.390625V4.85938C6.5937 4.95054 6.55748 5.03798 6.49302 5.10244C6.42855 5.16691 6.34112 5.20312 6.24995 5.20312C6.15878 5.20312 6.07134 5.16691 6.00688 5.10244C5.94241 5.03798 5.9062 4.95054 5.9062 4.85938V1.22035L0.99315 6.13383C0.928649 6.19833 0.841166 6.23457 0.749947 6.23457C0.658728 6.23457 0.571245 6.19833 0.506744 6.13383C0.442242 6.06933 0.406006 5.98184 0.406006 5.89062C0.406006 5.79941 0.442242 5.71192 0.506744 5.64742L5.42022 0.734375H1.7812C1.69003 0.734375 1.60259 0.698159 1.53813 0.633693C1.47366 0.569227 1.43745 0.481793 1.43745 0.390625C1.43745 0.299457 1.47366 0.212023 1.53813 0.147557C1.60259 0.0830915 1.69003 0.046875 1.7812 0.046875H6.24995C6.34112 0.046875 6.42855 0.0830915 6.49302 0.147557C6.55748 0.212023 6.5937 0.299457 6.5937 0.390625Z",fill:"currentColor"})})};const vt=function(){return(0,i.jsx)("svg",{width:"16",height:"13",viewBox:"0 0 16 13",fill:"none",xmlns:"http://www.w3.org/2000/svg",children:(0,i.jsx)("path",{d:"M4.31997 4.02502L1.78122 6.14064L4.31997 8.25627C4.37188 8.29782 4.41497 8.3493 4.44674 8.40771C4.4785 8.46611 4.4983 8.53027 4.50497 8.59642C4.51165 8.66257 4.50506 8.72938 4.4856 8.79296C4.46614 8.85653 4.43419 8.91558 4.39164 8.96666C4.34908 9.01774 4.29676 9.05982 4.23774 9.09044C4.17872 9.12105 4.11419 9.13959 4.04793 9.14496C3.98166 9.15034 3.91498 9.14244 3.8518 9.12174C3.78862 9.10104 3.73021 9.06794 3.67997 9.02439L0.679972 6.52439C0.62368 6.47747 0.578391 6.41874 0.547311 6.35237C0.516232 6.286 0.500122 6.21361 0.500122 6.14033C0.500122 6.06704 0.516232 5.99465 0.547311 5.92829C0.578391 5.86192 0.62368 5.80319 0.679972 5.75627L3.67997 3.25627C3.78191 3.1714 3.9134 3.1305 4.04549 3.14257C4.17759 3.15465 4.29948 3.2187 4.38435 3.32064C4.46922 3.42258 4.51011 3.55407 4.49804 3.68616C4.48597 3.81826 4.42191 3.94015 4.31997 4.02502ZM15.32 5.75627L12.32 3.25627C12.2695 3.21424 12.2112 3.18257 12.1485 3.16307C12.0858 3.14356 12.0199 3.1366 11.9545 3.14257C11.889 3.14855 11.8255 3.16735 11.7673 3.19791C11.7092 3.22846 11.6576 3.27016 11.6156 3.32064C11.5307 3.42258 11.4898 3.55407 11.5019 3.68616C11.514 3.81826 11.578 3.94015 11.68 4.02502L14.2187 6.14064L11.68 8.25627C11.6281 8.29782 11.585 8.3493 11.5532 8.40771C11.5214 8.46611 11.5016 8.53027 11.495 8.59642C11.4883 8.66257 11.4949 8.72938 11.5143 8.79296C11.5338 8.85653 11.5658 8.91558 11.6083 8.96666C11.6509 9.01774 11.7032 9.05982 11.7622 9.09044C11.8212 9.12105 11.8858 9.13959 11.952 9.14496C12.0183 9.15034 12.085 9.14244 12.1481 9.12174C12.2113 9.10104 12.2697 9.06794 12.32 9.02439L15.32 6.52439C15.3763 6.47747 15.4216 6.41874 15.4526 6.35237C15.4837 6.286 15.4998 6.21361 15.4998 6.14033C15.4998 6.06704 15.4837 5.99465 15.4526 5.92829C15.4216 5.86192 15.3763 5.80319 15.32 5.75627ZM10.1706 0.170642C10.1089 0.148233 10.0433 0.138201 9.97775 0.141119C9.91215 0.144037 9.84777 0.159847 9.78828 0.187646C9.72879 0.215446 9.67536 0.254691 9.63104 0.30314C9.58672 0.35159 9.55238 0.408294 9.52997 0.470017L5.52997 11.47C5.50747 11.5318 5.49736 11.5974 5.50023 11.663C5.5031 11.7287 5.51889 11.7932 5.5467 11.8527C5.5745 11.9123 5.61378 11.9658 5.66228 12.0101C5.71078 12.0545 5.76755 12.0889 5.82935 12.1113C5.88413 12.1307 5.94184 12.1407 5.99997 12.1406C6.10265 12.1406 6.20285 12.109 6.28694 12.0501C6.37102 11.9912 6.43493 11.9078 6.46997 11.8113L10.47 0.811267C10.4924 0.749545 10.5024 0.684015 10.4995 0.618417C10.4966 0.552818 10.4808 0.488437 10.453 0.428949C10.4252 0.369461 10.3859 0.316032 10.3375 0.271711C10.289 0.22739 10.2323 0.193047 10.1706 0.170642Z",fill:"currentColor"})})};var yt=function(){return yt=Object.assign||function(e){for(var n,t=1,i=arguments.length;t<i;t++)for(var a in n=arguments[t])Object.prototype.hasOwnProperty.call(n,a)&&(e[a]=n[a]);return e},yt.apply(this,arguments)};const xt=function(e){return(0,i.jsx)("svg",yt({width:"14",height:"14",viewBox:"0 0 14 14",fill:"none",xmlns:"http://www.w3.org/2000/svg"},e,{children:(0,i.jsx)("path",{d:"M11.6848 5.55953L7.30977 9.93453C7.26914 9.97521 7.22089 10.0075 7.16778 10.0295C7.11467 10.0515 7.05774 10.0628 7.00024 10.0628C6.94275 10.0628 6.88582 10.0515 6.83271 10.0295C6.7796 10.0075 6.73134 9.97521 6.69071 9.93453L2.31571 5.55953C2.23362 5.47744 2.1875 5.3661 2.1875 5.25C2.1875 5.1339 2.23362 5.02256 2.31571 4.94047C2.3978 4.85837 2.50915 4.81226 2.62524 4.81226C2.74134 4.81226 2.85268 4.85837 2.93477 4.94047L7.00024 9.00648L11.0657 4.94047C11.1064 4.89982 11.1546 4.86758 11.2077 4.84558C11.2608 4.82358 11.3178 4.81226 11.3752 4.81226C11.4327 4.81226 11.4897 4.82358 11.5428 4.84558C11.5959 4.86758 11.6441 4.89982 11.6848 4.94047C11.7254 4.98112 11.7577 5.02937 11.7797 5.08248C11.8017 5.13559 11.813 5.19251 11.813 5.25C11.813 5.30748 11.8017 5.36441 11.7797 5.41752C11.7577 5.47063 11.7254 5.51888 11.6848 5.55953Z",fill:"currentColor"})}))};var At=function(){return At=Object.assign||function(e){for(var n,t=1,i=arguments.length;t<i;t++)for(var a in n=arguments[t])Object.prototype.hasOwnProperty.call(n,a)&&(e[a]=n[a]);return e},At.apply(this,arguments)};const wt=function(e){return(0,i.jsx)("svg",At({width:"14",height:"14",viewBox:"0 0 14 14",fill:"none",xmlns:"http://www.w3.org/2000/svg"},e,{children:(0,i.jsx)("path",{d:"M2.31522 8.44047L6.69023 4.06547C6.73086 4.02479 6.77911 3.99252 6.83222 3.97051C6.88533 3.94849 6.94226 3.93716 6.99976 3.93716C7.05725 3.93716 7.11418 3.94849 7.16729 3.97051C7.22041 3.99252 7.26866 4.02479 7.30929 4.06547L11.6843 8.44047C11.7664 8.52256 11.8125 8.6339 11.8125 8.75C11.8125 8.8661 11.7664 8.97744 11.6843 9.05953C11.6022 9.14163 11.4909 9.18774 11.3748 9.18774C11.2587 9.18774 11.1473 9.14163 11.0652 9.05953L6.99976 4.99352L2.93429 9.05953C2.89364 9.10018 2.84538 9.13242 2.79227 9.15442C2.73916 9.17642 2.68224 9.18774 2.62476 9.18774C2.56727 9.18774 2.51035 9.17642 2.45724 9.15442C2.40413 9.13242 2.35587 9.10018 2.31522 9.05953C2.27458 9.01888 2.24233 8.97063 2.22034 8.91752C2.19834 8.86441 2.18701 8.80749 2.18701 8.75C2.18701 8.69251 2.19834 8.63559 2.22034 8.58248C2.24233 8.52937 2.27458 8.48112 2.31522 8.44047Z",fill:"currentColor"})}))};var bt=function(){return bt=Object.assign||function(e){for(var n,t=1,i=arguments.length;t<i;t++)for(var a in n=arguments[t])Object.prototype.hasOwnProperty.call(n,a)&&(e[a]=n[a]);return e},bt.apply(this,arguments)};const Mt=function(e){return(0,i.jsxs)("svg",bt({width:"17",height:"17",viewBox:"0 0 11 11",fill:"none",xmlns:"http://www.w3.org/2000/svg"},e,{children:[(0,i.jsxs)("g",{clipPath:"url(#clip0_2454_738)",children:[(0,i.jsx)("path",{d:"M3.66331 0C3.70312 0.0293021 3.72082 0.0380927 3.72672 0.0908364C3.79898 0.690064 3.64266 1.42848 3.70312 2.04821C3.72525 2.27238 4.1013 2.16249 4.25172 2.15956C4.92715 2.14638 5.60552 2.17128 6.28095 2.15956C6.3016 2.15517 6.35174 2.17421 6.35174 2.1918V5.59816C6.35174 5.59816 6.25883 5.61867 6.23818 5.62014C5.64239 5.66702 4.97434 5.60842 4.3756 5.59816C4.26795 5.5967 3.77096 5.58058 3.7223 5.62893C3.7046 5.64651 3.70017 5.66556 3.70165 5.69046C3.73262 6.08751 3.66331 6.53583 3.70165 6.92555C3.7164 7.07645 3.77834 7.17901 3.93908 7.20391L6.30897 7.21417L6.33994 7.24494L6.32077 10.6425L6.23671 10.663C4.53192 10.578 2.65754 11.0205 1.25802 9.8074C0.303875 8.97376 0.0575949 7.83684 0.0207267 6.61641C-0.0441613 4.45245 0.0723422 2.27677 0.0207267 0.111348L0.0634939 0H3.66478L3.66331 0Z",fill:"currentColor"}),(0,i.jsx)("path",{d:"M10.9898 8.86973C11.0045 8.99426 11.003 9.27849 10.9898 9.40449C10.8364 10.833 9.02544 11.4967 7.91792 10.5766C6.67619 9.54514 7.25429 7.52037 8.85437 7.29035C9.83506 7.1497 10.8792 7.86906 10.9913 8.86973H10.9898Z",fill:"currentColor"})]}),(0,i.jsx)("defs",{children:(0,i.jsx)("clipPath",{id:"clip0_2454_738",children:(0,i.jsx)("rect",{width:"18",height:"18",fill:"white"})})})]}))};var Ct=t(11),Et=t(5603),Rt=t(900),Pt=function(){return Pt=Object.assign||function(e){for(var n,t=1,i=arguments.length;t<i;t++)for(var a in n=arguments[t])Object.prototype.hasOwnProperty.call(n,a)&&(e[a]=n[a]);return e},Pt.apply(this,arguments)},Ft="&attachment=";Ct.A.registerLanguage("javascript",Et.A);var St=function(e){var n=e.component,t=e.isVisible,a=(0,x.useState)(!1),r=a[0],o=a[1];return(0,x.useEffect)((function(){o(!!t)}),[t]),r?(0,i.jsx)(n,{}):null},It=function(e){var n=e.code,t=e.language;try{var a=Ct.A.highlight(n,{language:t}).value;return(0,i.jsx)("pre",{children:(0,i.jsx)("code",{className:"language-".concat(t),children:(0,Rt.Ay)(a)})})}catch(e){return(0,i.jsx)("pre",{children:(0,i.jsx)("code",{className:"language-".concat(t),children:n})})}};const Tt=function(e){var n=e.poemData,t=e.isMobile,a=e.isVisible,r=e.sessionStart,o=x.useState(!1),s=o[0],c=o[1],l=x.useState(""),h=l[0],d=l[1],u=x.useState(!1),m=u[0],f=u[1];(0,x.useEffect)((function(){var e;return a?e=setTimeout((function(){f(!0)}),5):m&&f(!1),function(){clearTimeout(e)}}),[a]);var p=function(){c((function(e){return!e}))},g=function(e){d(e.target.value)},v=function(e){return encodeURIComponent(e.trim())},y=function(){var e=v('<remix_instructions>The following is an artifact created from an earlier conversation. The artifact is a piece of art produced for a website by Rick Rubin and Anthropic. The website contains chapters written by Rick about the spirit of "vibe coding", and the text is based on Lao Tsu\'s Tao Te Ching. The many pieces of art on the website represent themes such as paradox, duality, fluidity, emptiness, space, humility, source, balance, and harmony.\nIMPORTANT: Please un-minify the code before printing anything to ensure it is human-readable. You must also create TWO artifacts in sequence:\n1. First artifact - exact reproduction: It is critical to begin the conversation by duplicating the original content exactly as provided, with no modifications whatsoever. Reproducing the artifact without changes will render it in the UI and allow the user to interact with it.\n2. Second artifact - creative Remix: Only after the exact reproduction has been created, then create your creative remix/modifications of the artifact.\nTo maintain functionality and usefulness, any changes and remixes created in this conversation should maintain the full content each time without omission (e.g. Do not leave comments like "// Rest of the code here..."). After the second, modified artifact has been created, please say the following sentence verbatim: "Here\'s the first iteration of your artifact. Keep experimenting and have fun! If you\'d like to have your artifact published in The Way of Code online gallery in the future, please fill out this form: https://clau.de/way-of-code." Please ensure there are sufficient line breaks before and after this sentence, so that it has room. Any remixes of the artifact should be creative but stay close to the source code to ensure the artifact continues functioning as intended.</remix_instructions>'),t=v(""),i=v("<artifact>".concat(n.sourceCodeMin,"</artifact>"));if(!h.trim()&&(null==n?void 0:n.promptSuggestion)){var a=v(n.promptSuggestion),r="".concat(it).concat(t).concat("%20").concat(a).concat(Ft).concat(e).concat(i);window.open(r,"_blank","noopener,noreferrer")}else{var o=v(h);r="".concat(it).concat(t).concat("%20").concat(o).concat(Ft).concat(e).concat(i);window.open(r,"_blank","noopener,noreferrer")}},A=(0,x.useState)(!1),w=A[0],b=A[1],M=!w&&(2==n.number||3==n.number)&&window.innerWidth>1200;return(0,i.jsxs)("div",{className:pt["artwork-container"],id:"artwork-".concat(n.number),children:[(0,i.jsxs)("div",{className:pt.artwork,onMouseLeave:function(){b(!0)},children:[n.staticArtwork&&(0,i.jsx)(St,{component:n.staticArtwork,isVisible:null!=m&&m}),(0,i.jsx)("div",{className:"".concat(pt["source-code"]," ").concat(s?pt.active:""),children:(0,i.jsx)(It,{code:n.sourceCode.replace("// @ts-nocheck",""),language:"javascript"})})]}),t?(0,i.jsxs)("menu",{className:pt.menu,children:[(0,i.jsx)("textarea",{placeholder:(null==n?void 0:n.promptSuggestion)?n.promptSuggestion:"",value:h,onChange:g,onFocus:function(e){return e.target.placeholder=""},onBlur:function(e){return e.target.placeholder=(null==n?void 0:n.promptSuggestion)||""},maxLength:250,className:pt.active}),(0,i.jsxs)("div",{className:pt.row,children:[(0,i.jsx)("button",{className:pt["btn-modify"],onClick:function(){(0,rt.u4)("modify_button_clicked",{poemNumber:n.number,session_time_elapsed:r?Date.now()-r:void 0}),y()},children:(0,i.jsxs)("p",{children:[" Modify ",(0,i.jsx)(gt,{})," "]})}),(0,i.jsx)("button",{className:"".concat(pt["btn-code"]," ").concat(s?pt.active:""),onClick:p,children:(0,i.jsx)(vt,{})})]})]}):(0,i.jsxs)(i.Fragment,{children:[(0,i.jsx)("menu",{className:pt.menu,style:Pt({},h.length>0?{opacity:1}:{}),children:(0,i.jsxs)("div",{className:pt.row,children:[(0,i.jsx)("input",{type:"text",placeholder:(null==n?void 0:n.promptSuggestion)?n.promptSuggestion:"",value:h,onChange:g,onFocus:function(e){return e.target.placeholder=""},onBlur:function(e){return e.target.placeholder=(null==n?void 0:n.promptSuggestion)||""},maxLength:250}),(0,i.jsx)("button",{className:pt["btn-modify"],onClick:function(){(0,rt.u4)("modify_button_clicked",{poemNumber:n.number,session_time_elapsed:r?Date.now()-r:void 0}),y()},children:(0,i.jsxs)("p",{children:[" Modify ",(0,i.jsx)(gt,{})," "]})}),(0,i.jsx)("button",{className:pt["btn-code"],onClick:p,children:(0,i.jsx)(vt,{})})]})}),M?(0,i.jsx)("div",{className:pt["artwork-instructions"],children:(0,i.jsx)("p",{})}):(0,i.jsx)(i.Fragment,{})]})]})};const kt=function(e){var n,t,a=e.poemData,r=e.isLongPoem,o=e.isLongPoemMobile,s=e.isMobile,c=void 0!==s&&s,l=e.isVisible,h=void 0!==l&&l,d=e.sessionStart,u=e.registerPoemSectionRef,m=e.registerMobileArtworkRef,f=e.appRef,p=e.prevSection,g=(0,x.useRef)(null),v=(0,x.useRef)(null),y=(0,x.useRef)(null),A=(0,x.useState)(!1),w=A[0],b=A[1],M=(0,x.useState)(0),C=M[0],E=M[1],R=(0,x.useState)(0),P=(R[0],R[1]),F=null,S=0,I=(0,x.useCallback)((function(e){if(y.current&&g.current&&h){var n,t,i=y.current,a=i.scrollTop,r=i.scrollHeight;n=a+i.clientHeight>=r-2,t=a<=2,(e.deltaY>0&&!n||e.deltaY<0&&!t)&&(e.preventDefault(),null!==F&&cancelAnimationFrame(F),F=requestAnimationFrame((function(){y.current&&(y.current.scrollTop+=1.4*e.deltaY,F=null)})))}}),[h]),T=(0,x.useCallback)((function(e){var n,t;if(e.stopPropagation(),y.current&&g.current&&h){var i=y.current.scrollTop,a=i-S;S=i;var r=y.current,o=r.scrollTop,s=r.scrollHeight,c=r.clientHeight,l=o<=5;a>0&&o+c>=s-5&&(null===(n=f.current)||void 0===n||n.scrollTo({top:v.current.offsetTop,behavior:"smooth"})),a<0&&l&&(null===(t=f.current)||void 0===t||t.scrollTo({top:p.offsetTop+window.innerHeight,behavior:"smooth"}))}}),[h]);return(0,x.useEffect)((function(){if(y.current){var e,n=function(){y.current&&(E(y.current.clientWidth),P(y.current.clientHeight))},t=function(){clearTimeout(e),e=setTimeout(n,5)};return n(),window.addEventListener("resize",t),function(){window.removeEventListener("resize",t),clearTimeout(e)}}}),[y.current]),(0,x.useEffect)((function(){h||(y.current.scrollTop=0)}),[h]),(0,x.useEffect)((function(){if(h&&g.current&&y.current)return r&&!c&&g.current.addEventListener("wheel",I,{passive:!1}),o&&c&&(y.current.addEventListener("scroll",T,{passive:!1}),y.current.addEventListener("touchstart",(function(e){return e.stopPropagation()}),{passive:!0}),y.current.addEventListener("touchmove",(function(e){return e.stopPropagation()}),{passive:!0})),function(){g.current&&!c&&g.current.removeEventListener("wheel",I),y.current&&c&&(y.current.removeEventListener("scroll",T),y.current.removeEventListener("touchstart",(function(e){return e.stopPropagation()})),y.current.removeEventListener("touchmove",(function(e){return e.stopPropagation()}))),null!==F&&cancelAnimationFrame(F)}}),[h,r,o,c,I,T]),(0,x.useEffect)((function(){return a&&g.current&&u(a.number,g.current),function(){a&&u(a.number,null)}}),[a,u]),(0,x.useEffect)((function(){return a&&c&&v.current&&m(a.number,v.current),function(){a&&m(a.number,null)}}),[a,c,m,v.current]),(0,x.useEffect)((function(){if(c&&v.current){var e=new IntersectionObserver((function(e){var n=e[0];b(n.isIntersecting)}),{threshold:.1});return e.observe(v.current),function(){e.disconnect()}}}),[c,v.current]),(0,x.useEffect)((function(){w&&setTimeout((function(){y.current.scrollTop=36}),400)}),[w]),a&&(0,i.jsxs)(i.Fragment,{children:[(0,i.jsxs)("section",{ref:g,"data-poem-id":a.number,"data-section-type":"poem-section",className:(n="",t="",window.innerWidth<768?(h&&!o?n=ut["in-view"]:h&&o&&(n=ut["in-view-long"]),t=o?ut.long:""):(h&&!r?n=ut["in-view"]:h&&r&&(n=ut["in-view-long"]),t=r?ut.long:""),"".concat(ut["poem-section"]," ").concat(n," ").concat(t)),children:[!c&&(0,i.jsx)(Tt,{poemData:a,isVisible:h,sessionStart:d}),(0,i.jsx)("div",{className:ut["column-1"],id:"poem-".concat(a.number),ref:y,children:(0,i.jsxs)("div",{className:ut.poem,children:[(0,i.jsx)("div",{className:ut["poem-number"],children:(0,i.jsx)("p",{children:a.number})}),(0,i.jsx)("div",{className:ut["poem-text"],children:c?a.text.replace(/\|/g,"\n").split("\n\n").map((function(e,n){return(0,i.jsx)("p",{children:e},n)})):a.text.replace(/\|/g," ").split("\n\n").map((function(e,n){return(0,i.jsx)("p",{children:e},n)}))})]})}),!c&&r?(0,i.jsx)("div",{className:ut["linear-gradient"],style:{width:C}}):(0,i.jsx)(i.Fragment,{})]}),a.shouldDisplayMobileArtwork&&c&&(0,i.jsx)("section",{className:ut["artwork-section"],ref:v,"data-poem-id":a.number,"data-section-type":"mobile-artwork",children:(0,i.jsx)(Tt,{poemData:a,isMobile:!0,isVisible:h,sessionStart:d})})]})};var zt=t(3075),jt={};jt.styleTagTransform=g(),jt.setAttributes=u(),jt.insert=h().bind(null,"head"),jt.domAPI=c(),jt.insertStyleElement=f();o()(zt.A,jt);const Lt=zt.A&&zt.A.locals?zt.A.locals:void 0;const Bt=function(){return(0,i.jsx)("div",{className:Lt.privacy,children:(0,i.jsxs)("div",{className:Lt.container,children:[(0,i.jsx)("h3",{children:"Privacy Policy"}),(0,i.jsxs)("section",{children:[(0,i.jsx)("p",{children:"We respect your privacy and aim to collect minimal personal information."}),(0,i.jsxs)("ul",{children:[(0,i.jsx)("li",{children:"Embedded services: This site may include embedded services, such as newsletter sign-ups via Substack, that collect information directly from you according to their own privacy policies, which you should read."}),(0,i.jsxs)("li",{children:["Analytics: This site uses Vercel Web Analytics, a privacy-friendly tool that doesn't use cookies, to gather aggregated analytics which cannot identify you individually. Your information is de-identified as per CCPA requirements. For more information about Vercel Web Analytics' privacy practices, visit ",(0,i.jsx)("a",{href:"https://vercel.com/docs/analytics/privacy-policy",target:"_blank",children:"their website"}),"."]}),(0,i.jsxs)("li",{children:["Data Retention: Anonymous analytics data is stored by Vercel and available to us in aggregated form only (",(0,i.jsx)("a",{href:"https://vercel.com/docs/analytics/privacy-policy#data-collected",target:"_blank",children:"learn more"}),")."]}),(0,i.jsx)("li",{children:"Purpose: The aggregated analytics are used to understand website traffic."}),(0,i.jsx)("li",{children:"Legal Basis: Our use of aggregated analytics is based on our legitimate interest to understand activity on the site (GDPR Article 6(1)(f))."})]}),(0,i.jsx)("p",{children:"If you have any questions about this privacy notice or wish to contact us to exercise any of your rights, please reach out."})]}),(0,i.jsx)("div",{className:Lt.backLink,children:(0,i.jsx)("a",{href:"/",onClick:function(e){e.preventDefault(),window.history.pushState({},"","/"),window.dispatchEvent(new PopStateEvent("popstate"))},children:"Back to The Way of Code"})})]})})};const Dt=function(){var e=(0,x.useRef)(null);return(0,x.useEffect)((function(){window.CustomSubstackWidget={substackUrl:"rickrubin.substack.com",placeholder:"example@gmail.com",buttonText:"Subscribe",theme:"custom",colors:{primary:"#E8E6DC",input:"#FAF9F5",email:"#5E5D59",text:"#5E5D59"}};var e=document.createElement("script");return e.src="https://substackapi.com/widget.js",e.async=!0,document.head.appendChild(e),function(){var e=document.querySelector('script[src="https://substackapi.com/widget.js"]');e&&document.head.removeChild(e),delete window.CustomSubstackWidget}}),[]),(0,i.jsx)("div",{ref:e,id:"custom-substack-embed"})};const Ht=function(e){var n=e.isMobile;return void 0!==n&&n?(0,i.jsxs)("svg",{width:"14",viewBox:"0 0 11 18",fill:"none",xmlns:"http://www.w3.org/2000/svg",children:[(0,i.jsx)("circle",{className:"circle-top",cx:"5.5",cy:"6",r:"5",stroke:"currentColor"}),(0,i.jsx)("circle",{className:"circle-bottom",cx:"5.5",cy:"11.5",r:"5",stroke:"currentColor"})]}):(0,i.jsx)("svg",{width:"18",height:"18",viewBox:"0 0 16 16",fill:"none",xmlns:"http://www.w3.org/2000/svg",children:(0,i.jsx)("path",{d:"M14.8538 11.1462C14.9002 11.1927 14.9371 11.2478 14.9623 11.3085C14.9874 11.3692 15.0004 11.4343 15.0004 11.5C15.0004 11.5657 14.9874 11.6307 14.9623 11.6914C14.9371 11.7521 14.9002 11.8073 14.8538 11.8537L13.3538 13.3537C13.2599 13.4475 13.1327 13.5003 13 13.5003C12.8673 13.5003 12.7401 13.4475 12.6462 13.3537C12.5524 13.2599 12.4997 13.1327 12.4997 13C12.4997 12.8673 12.5524 12.74 12.6462 12.6462L13.2931 12H12.5588C11.8426 11.9994 11.1368 11.8281 10.5 11.5004C9.86322 11.1727 9.31365 10.698 8.89688 10.1156L6.28937 6.4656C5.96519 6.01263 5.53775 5.64342 5.04246 5.38855C4.54716 5.13367 3.99827 5.00047 3.44125 4.99997H2C1.86739 4.99997 1.74021 4.94729 1.64645 4.85353C1.55268 4.75976 1.5 4.63258 1.5 4.49997C1.5 4.36736 1.55268 4.24019 1.64645 4.14642C1.74021 4.05265 1.86739 3.99997 2 3.99997H3.44125C4.15743 4.00057 4.86316 4.17181 5.49997 4.49951C6.13679 4.82721 6.68635 5.30193 7.10313 5.88435L9.71062 9.53435C10.0348 9.98732 10.4623 10.3565 10.9575 10.6114C11.4528 10.8663 12.0017 10.9995 12.5588 11H13.2931L12.6462 10.3537C12.5524 10.2599 12.4997 10.1327 12.4997 9.99997C12.4997 9.86729 12.5524 9.74004 12.6462 9.64622C12.7401 9.5524 12.8673 9.49969 13 9.49969C13.1327 9.49969 13.2599 9.5524 13.3538 9.64622L14.8538 11.1462ZM8.9375 6.68747C8.99093 6.72564 9.05136 6.75291 9.11533 6.76772C9.1793 6.78254 9.24556 6.7846 9.31033 6.77381C9.3751 6.76301 9.4371 6.73957 9.49281 6.70481C9.54852 6.67005 9.59684 6.62466 9.635 6.57122L9.71 6.46685C10.0341 6.01356 10.4616 5.64406 10.957 5.38896C11.4524 5.13386 12.0015 5.00051 12.5588 4.99997H13.2931L12.6462 5.64622C12.5524 5.74004 12.4997 5.86729 12.4997 5.99997C12.4997 6.13265 12.5524 6.2599 12.6462 6.35372C12.7401 6.44754 12.8673 6.50025 13 6.50025C13.1327 6.50025 13.2599 6.44754 13.3538 6.35372L14.8538 4.85372C14.9002 4.80729 14.9371 4.75214 14.9623 4.69144C14.9874 4.63074 15.0004 4.56568 15.0004 4.49997C15.0004 4.43427 14.9874 4.3692 14.9623 4.3085C14.9371 4.2478 14.9002 4.19266 14.8538 4.14622L13.3538 2.64622C13.2599 2.5524 13.1327 2.49969 13 2.49969C12.8673 2.49969 12.7401 2.5524 12.6462 2.64622C12.5524 2.74004 12.4997 2.86729 12.4997 2.99997C12.4997 3.13265 12.5524 3.2599 12.6462 3.35372L13.2931 3.99997H12.5588C11.8426 4.00057 11.1368 4.17181 10.5 4.49951C9.86322 4.82721 9.31365 5.30193 8.89688 5.88435L8.82187 5.98872C8.78348 6.04216 8.75601 6.10264 8.74104 6.16672C8.72607 6.23079 8.7239 6.29719 8.73464 6.3621C8.74538 6.42701 8.76883 6.48917 8.80364 6.545C8.83845 6.60084 8.88394 6.64925 8.9375 6.68747ZM7.0625 9.31247C7.00907 9.27431 6.94864 9.24704 6.88467 9.23222C6.8207 9.21741 6.75444 9.21534 6.68967 9.22614C6.6249 9.23693 6.56289 9.26038 6.50719 9.29514C6.45148 9.3299 6.40316 9.37529 6.365 9.42872L6.29 9.5331C5.96589 9.98638 5.53841 10.3559 5.04299 10.611C4.54757 10.8661 3.99849 10.9994 3.44125 11H2C1.86739 11 1.74021 11.0527 1.64645 11.1464C1.55268 11.2402 1.5 11.3674 1.5 11.5C1.5 11.6326 1.55268 11.7598 1.64645 11.8535C1.74021 11.9473 1.86739 12 2 12H3.44125C4.15743 11.9994 4.86316 11.8281 5.49997 11.5004C6.13679 11.1727 6.68635 10.698 7.10313 10.1156L7.17812 10.0112C7.21652 9.95779 7.24399 9.8973 7.25896 9.83323C7.27393 9.76916 7.2761 9.70276 7.26536 9.63785C7.25462 9.57293 7.23117 9.51078 7.19636 9.45494C7.16155 9.39911 7.11606 9.35069 7.0625 9.31247Z",fill:"currentColor"})})};const Yt=function(e){var n=e.className,t=e.onClick,a=e.shapeState,r=(0,x.useRef)(null),o=(0,x.useRef)(null);return(0,x.useEffect)((function(){var e=function(){var e=document.querySelector('div[class*="vesica"]');e&&window.innerWidth<=768?(e.style.width="18vh",e.style.height="18vh"):e&&(e.style.width="23vh",e.style.height="23vh")};return e(),window.addEventListener("resize",e),function(){return window.removeEventListener("resize",e)}}),[]),(0,x.useEffect)((function(){var e=r.current;if(e){var n=e.getContext("2d");if(n){var t=200,i=144.2,a=255.8,s=200,c=111.6,l=55.8,h=[],d=0,u=0,m=function(){n.fillStyle=getComputedStyle(document.documentElement).getPropertyValue("--rubin-ivory-med"),n.fillRect(0,0,e.width,e.height)},f=function(){for(var e=[],n=0;n<=100;n++){var r=n/100,o=Math.PI/2+r*Math.PI*2;e.push({x:t+Math.cos(o)*c,y:i+Math.sin(o)*c})}for(n=0;n<=100;n++){r=n/100,o=Math.PI/2+r*Math.PI*3;e.push({x:t+Math.cos(o)*l,y:s+Math.sin(o)*l})}for(n=0;n<=100;n++){r=n/100,o=-Math.PI/2+r*Math.PI*2;e.push({x:t+Math.cos(o)*c,y:a+Math.sin(o)*c})}for(n=0;n<=100;n++){r=n/100,o=-Math.PI/2+r*Math.PI*3;e.push({x:t+Math.cos(o)*l,y:s+Math.sin(o)*l})}return{x:e[0].x,y:e[0].y}},p=function(e,n,t,i){return{x:e+Math.cos(i)*t,y:n+Math.sin(i)*t}},g=0,v=1e3/30,y=function(e){if(e-g<v)o.current=requestAnimationFrame(y);else{g=e,m(),f();var r,x=.003;if(1===d)x=1.5/(w=.5)*.005;else if(3===d){x=1.5/(w=.5)*.005}if(u+=x,0===d){var A=Math.PI/2+u*Math.PI*2;r=p(t,i,c,A),u>=1&&(d=1,u=0)}else if(1===d){var w=.5;A=Math.PI/2+u*Math.PI*2*w;r=p(t,s,l,A),u>=1&&(d=2,u=0)}else if(2===d){A=-Math.PI/2+u*Math.PI*2;r=p(t,a,c,A),u>=1&&(d=3,u=0)}else if(3===d){w=.5,A=-Math.PI/2+u*Math.PI*2*w;r=p(t,s,l,A),u>=1&&(d=0,u=0)}h.push(r);for(var b=1;b<h.length;b++){var M=1e3,C=void 0;if(h.length<=M)C=Math.pow(b/h.length,.65);else if(b<h.length-M)C=0;else{var E=(b-(h.length-M))/M;C=Math.pow(E,.65)}n.beginPath(),n.moveTo(h[b-1].x,h[b-1].y),n.lineTo(h[b].x,h[b].y),n.strokeStyle="rgba(31, 30, 29, ".concat(C,")"),n.lineWidth=5.3,n.stroke()}if(n.shadowBlur=0,r){n.beginPath(),n.arc(r.x,r.y,7.8,0,2*Math.PI),n.fillStyle="rgba(31, 30, 29, 1)",n.fill()}o.current=requestAnimationFrame(y)}};m(),f(),m(),f();for(var x=0;x<200;x++){var A=p(t,i,c,Math.PI/2+.005*x*Math.PI*2);h.push(A)}for(x=0;x<67;x++){A=p(t,s,l,Math.PI/2+.015*x*Math.PI);h.push(A)}for(x=0;x<200;x++){A=p(t,a,c,-Math.PI/2+.005*x*Math.PI*2);h.push(A)}for(x=0;x<67;x++){A=p(t,s,l,-Math.PI/2+.015*x*Math.PI);h.push(A)}for(x=0;x<200;x++){A=p(t,i,c,Math.PI/2+.005*x*Math.PI*2);h.push(A)}for(x=0;x<67;x++){A=p(t,s,l,Math.PI/2+.015*x*Math.PI);h.push(A)}return d=2,u=0,y(performance.now()),y(performance.now()),function(){o.current&&cancelAnimationFrame(o.current)}}}}),[]),(0,i.jsx)("div",{className:n,onClick:t,style:{cursor:"pointer",width:"23vh",height:"23vh",margin:"0 auto",display:"block"},children:(0,i.jsx)("canvas",{ref:r,width:400,height:400,style:{opacity:"hidden"===a?0:1,transition:"opacity 0.3s ease",width:"100%",height:"100%"}})})};var Xt=function(){return Xt=Object.assign||function(e){for(var n,t=1,i=arguments.length;t<i;t++)for(var a in n=arguments[t])Object.prototype.hasOwnProperty.call(n,a)&&(e[a]=n[a]);return e},Xt.apply(this,arguments)},Wt=function(e,n,t){if(t||2===arguments.length)for(var i,a=0,r=n.length;a<r;a++)!i&&a in n||(i||(i=Array.prototype.slice.call(n,0,a)),i[a]=n[a]);return e.concat(i||Array.prototype.slice.call(n))};at.forEach((function(e){var n=e.text.split("\n").length;e.isLongPoem=!(n<25);var t=e.text.split(/\n|\|/).length;e.isLongPoemMobile=!(t<22)}));var qt=function(){var e;return window.matchMedia("(max-width: 768px) and (orientation: landscape)").matches||window.innerWidth<=768&&window.innerWidth>window.innerHeight||window.innerWidth<=768&&(null===(e=screen.orientation)||void 0===e?void 0:e.type.includes("landscape"))||window.innerWidth<=932&&window.innerHeight<=440},Ot=function(){return window.innerHeight<680&&window.innerWidth>window.innerHeight};const Gt=function(){var e=(0,x.useState)(window.location.pathname),n=e[0],t=e[1];(0,x.useEffect)((function(){var e=function(){t(window.location.pathname)};return window.addEventListener("popstate",e),function(){return window.removeEventListener("popstate",e)}}),[]);var a=(0,x.useState)(Date.now())[0],r=(0,x.useState)([]),o=r[0],s=r[1],c=lt({sessionStart:a,poemViewHistory:o,setPoemViewHistory:s}).trackPoemView,l=(0,x.useState)(Array(at.length).fill(!1).map((function(e,n){return at[n].isLongPoem||!1}))),h=l[0],d=l[1],u=(0,x.useState)(Array(at.length).fill(!1).map((function(e,n){return at[n].isLongPoemMobile||!1}))),m=u[0],f=u[1],p=function(){var e=Array.from(z.current.values()),n=Array(at.length).fill(!1),t=Array(at.length).fill(!1);e.forEach((function(e){var i=e.querySelector(".poem-text");if(i){var a=i.getBoundingClientRect().height,r=e.getAttribute("data-poem-id");if(r){var o=parseInt(r,10);a/window.innerHeight>.8?(at[o-1].isLongPoem=!0,n[o-1]=!0):(at[o-1].isLongPoem=!1,n[o-1]=!1),20*at[o-1].text.split(/\n|\|/).length/window.innerHeight>.7?(at[o-1].isLongPoemMobile=!0,t[o-1]=!0):(at[o-1].isLongPoemMobile=!1,t[o-1]=!1)}}d(n),f(t)}))};(0,x.useEffect)((function(){var e=function(){window.debouncedPoemHeightTimer&&clearTimeout(window.debouncedPoemHeightTimer),window.debouncedPoemHeightTimer=setTimeout((function(){p()}),20)};return window.addEventListener("resize",e),p(),function(){window.removeEventListener("resize",e),window.debouncedPoemHeightTimer&&clearTimeout(window.debouncedPoemHeightTimer)}}),[]),(0,x.useEffect)((function(){console.log("A project by Rick Rubin and Anthropic")}),[]);var g=(0,x.useState)(!1),v=g[0],y=g[1];(0,x.useEffect)((function(){var e=function(){y(window.innerWidth<=1024)};return e(),window.addEventListener("resize",e),function(){return window.removeEventListener("resize",e)}}),[]);var A=(0,x.useState)(qt()),w=A[0],M=A[1],C=(0,x.useRef)(null),E=(0,x.useState)(0),R=E[0],P=E[1],F=(0,x.useState)([]),S=F[0],I=F[1],T=(0,x.useRef)(new Map),k=(0,x.useRef)(new Map),z=(0,x.useRef)(new Map),j=(0,x.useState)(!1),L=j[0],B=j[1],D=(0,x.useCallback)((function(e,n){n?z.current.set(e,n):z.current.delete(e)}),[]),H=(0,x.useCallback)((function(e,n){n?T.current.set(e,n):T.current.delete(e)}),[]),Y=(0,x.useRef)(null),X=(0,x.useRef)(null),W=(0,x.useRef)(null),q=(0,x.useRef)(null),O=(0,x.useRef)(null),G=(0,x.useRef)(null),N=(0,x.useState)({title:!1,intro:!1,outro:!1,acknowledgements:!1,signup:!1}),$=N[0],_=N[1];(0,x.useEffect)((function(){var e=new IntersectionObserver((function(e){e.forEach((function(e){var n=e.target===Y.current,t=e.target===X.current,i=e.target===W.current,a=e.target===G.current,r=e.target===q.current;_((function(o){var s=Xt(Xt({},o),{title:n?e.isIntersecting:o.title,intro:t?e.isIntersecting:o.intro,outro:i?e.isIntersecting:o.outro,acknowledgements:a?e.isIntersecting:o.acknowledgements,signup:r?e.isIntersecting:o.signup});return V.current&&!v&&(s.title||s.intro?V.current.classList.contains(b.visible)&&V.current.classList.remove(b.visible):V.current.classList.contains(b.visible)||V.current.classList.add(b.visible),s.acknowledgements&&(V.current.classList.contains(b.visible)||V.current.classList.add(b.visible))),s})),e.isIntersecting&&(window.history.replaceState(null,"",window.location.pathname),n?(P(0),B(!1),O.current&&O.current.classList.remove(b.visible),U.current&&U.current.classList.contains(b.open)&&U.current.classList.remove(b.open),Z.current&&Z.current.classList.contains(b.active)&&Z.current.classList.remove(b.active)):t?(P(0),B(!1),O.current&&O.current.classList.contains(b.visible)&&O.current.classList.remove(b.visible),U.current&&U.current.classList.contains(b.open)&&U.current.classList.remove(b.open),Z.current&&Z.current.classList.contains(b.active)&&Z.current.classList.remove(b.active)):i?(P(0),O.current&&!O.current.classList.contains(b.visible)&&O.current.classList.add(b.visible),window.history.replaceState(null,"","#PS")):a?(P(0),O.current&&O.current.classList.contains(b.visible)&&O.current.classList.remove(b.visible)):r&&O.current&&O.current.classList.contains(b.visible)&&O.current.classList.remove(b.visible))}))}),{threshold:.3});return Y.current&&e.observe(Y.current),X.current&&e.observe(X.current),W.current&&e.observe(W.current),G.current&&e.observe(G.current),q.current&&e.observe(q.current),function(){e.disconnect()}}),[v]);var V=(0,x.useRef)(null),U=(0,x.useRef)(null),Z=(0,x.useRef)(null),J=function(e,n){if(I((function(t){return n&&!t.includes(e)?Wt(Wt([],t,!0),[e],!1):!n&&t.includes(e)?t.filter((function(n){return n!==e})):t})),n){var t=e-1;t>=0&&(P(e),c(e,t),!O.current||h[t]||O.current.classList.contains(b.visible)||O.current.classList.add(b.visible),O.current&&h[t]&&O.current.classList.contains(b.visible)&&O.current.classList.remove(b.visible))}};(0,x.useEffect)((function(){var e;if(0!==R){var n=null===(e=at[R-1])||void 0===e?void 0:e.number;if(n){var t="#".concat(n);window.location.hash!==t&&window.history.replaceState(null,"",t)}}}),[R,at]),(0,x.useEffect)((function(){var e=new IntersectionObserver((function(e){e.forEach((function(e){var n=e.target,t=n.getAttribute("data-poem-id"),i=n.getAttribute("data-section-type");if(t){var a=parseInt(t,10),r=e.isIntersecting;k.current.has(a)||k.current.set(a,{poemSection:!1,mobileArtwork:!1});var o=k.current.get(a);"mobile-artwork"===i?o.mobileArtwork=r:o.poemSection=r;var s=o.poemSection||o.mobileArtwork;s&&Q(a),J(a,s)}}))}),{threshold:.1});return z.current.forEach((function(n,t){e.observe(n)})),v&&T.current.forEach((function(n){e.observe(n)})),function(){e.disconnect()}}),[v,h]);var K=(0,x.useRef)(1),Q=(0,x.useCallback)((function(e){if(V.current){var n=V.current.querySelector(".index");if(n){var t=Math.floor(e/12);if(t!==K.current){K.current=t;var i=0;i=e<68&&e>=12?(e-1)/60:e<12?0:1;var a=n.scrollHeight-n.clientHeight;n.scrollTop=a*i}}}}),[]),ee=(0,x.useCallback)((function(e){if(U.current){var n=U.current.querySelector(".index");if(n){var t=U.current.querySelector("#nav-item-".concat(e));if(t){var i=t.getBoundingClientRect(),a=n.getBoundingClientRect(),r=n.scrollTop,o=i.top-a.top+r-.4*window.innerHeight;U.current.scrollTo({top:o,behavior:"smooth"})}}}}),[]);(0,x.useEffect)((function(){v&&U.current&&R>0&&ee(R)}),[R]),(0,x.useEffect)((function(){var e=function(){var e,n=window.location.hash;if(n)if("PS"==n.substring(1)){var t;if(P(0),null===(e=C.current)||void 0===e||e.scrollTo({top:null==W?void 0:W.current.offsetTop,behavior:"instant"}),V.current)(t=V.current.querySelector(".index"))&&(t.scrollTop=t.scrollHeight-t.clientHeight);if(U.current)(t=U.current.querySelector(".index"))&&(t.scrollTop=t.scrollHeight-t.clientHeight)}else{var i=parseInt(n.substring(1),10);!isNaN(i)&&i>=0&&i<=at.length&&setTimeout((function(){ne(i),J(i,!0)}),100)}};return e(),window.addEventListener("hashchange",e),function(){return window.removeEventListener("hashchange",e)}}),[C,W]),(0,x.useEffect)((function(){var e=function(e){"ArrowUp"===e.key||"ArrowLeft"===e.key?(e.preventDefault(),R>1&&ne(at[R-2].number)):"ArrowDown"!==e.key&&"ArrowRight"!==e.key||(e.preventDefault(),R<at.length&&ne(at[R].number))};return window.addEventListener("keydown",e),function(){return window.removeEventListener("keydown",e)}}),[R,at]);var ne=(0,x.useCallback)((function(e){if(C.current){var n=document.getElementById("poem-".concat(e)),t=document.getElementById("artwork-".concat(e)),i=n||t;if(i){C.current.classList.add(b.scrolling);var a=C.current.getBoundingClientRect(),r=i.getBoundingClientRect(),o=C.current.scrollTop,s=r.top-a.top+o;C.current.scrollTo({top:s,behavior:"instant"}),C.current.classList.remove(b.scrolling),window.history.replaceState(null,"","#".concat(e));var c=at.findIndex((function(n){return n.number===e}));c>=0&&P(c+1),Q(e),v&&ee(e)}}}),[Q,at,P]);(0,x.useEffect)((function(){var e,n=function(e){var n;if(v&&L){var t=null===(n=U.current)||void 0===n?void 0:n.querySelector(".index");t&&e.target&&t.contains(e.target)||B(!1)}};return v&&(null===(e=C.current)||void 0===e||e.addEventListener("scroll",n,{passive:!1})),function(){var e;null===(e=C.current)||void 0===e||e.removeEventListener("scroll",n)}}),[U,v,L]),(0,x.useEffect)((function(){var e=function(){var e=qt();w!==e&&M(e)};e(),window.screen.orientation&&window.screen.orientation.addEventListener("change",e);var n=function(){window.debouncedWideDesktopTimer&&clearTimeout(window.debouncedWideDesktopTimer),window.debouncedWideDesktopTimer=setTimeout((function(){Ot()&&!w&&M(!0),Ot()||w||M(!1)}),5)};return window.addEventListener("resize",n),n(),function(){window.screen.orientation?window.screen.orientation.removeEventListener("change",e):window.removeEventListener("resize",e),window.removeEventListener("resize",n),window.debouncedWideDesktopTimer&&clearTimeout(window.debouncedWideDesktopTimer)}}),[]);var te=0,ie=function(e){var n;if(e.stopPropagation(),G.current){var t=G.current.scrollTop,i=t-te;te=t;var a=G.current.scrollTop;i<0&&a<=5&&(null===(n=C.current)||void 0===n||n.scrollTo({top:W.current.offsetTop,behavior:"smooth"}),window.history.replaceState(null,"","#PS"))}};return(0,x.useEffect)((function(){return v&&$.acknowledgements&&G.current&&(G.current.addEventListener("scroll",ie,{passive:!1}),G.current.addEventListener("touchstart",(function(e){return e.stopPropagation()}),{passive:!0}),G.current.addEventListener("touchmove",(function(e){return e.stopPropagation()}),{passive:!0})),function(){G.current&&v&&(G.current.removeEventListener("scroll",ie),G.current.removeEventListener("touchstart",(function(e){return e.stopPropagation()})),G.current.removeEventListener("touchmove",(function(e){return e.stopPropagation()})))}}),[$.acknowledgements]),(0,i.jsxs)("main",{className:b.app,ref:C,children:[(0,i.jsxs)("section",{className:b["title-section"],ref:Y,children:[(0,i.jsxs)("h1",{id:"title",children:[" THE WAY",(0,i.jsx)("br",{}),"OF CODE "]}),(0,i.jsxs)("h2",{id:"subtitle",children:[" The Timeless Art of",window.innerWidth<820?(0,i.jsx)("br",{}):(0,i.jsx)(i.Fragment,{})," Vibe Coding"]}),(0,i.jsx)(Yt,{className:b.square,onClick:function(){},shapeState:"wide"}),(0,i.jsxs)("h3",{id:"author",children:[" Based on Lao Tzu ",(0,i.jsx)("br",{})," Adapted by Rick Rubin "]})]}),!v&&(0,i.jsx)("div",{className:b["sub-heading"],ref:O,children:(0,i.jsxs)("p",{onClick:function(){C.current.scrollTo({top:0})},children:["THE WAY OF CODE ",(0,i.jsx)("span",{children:"—"})," Rick Rubin"]})}),(0,i.jsx)("section",{className:b["intro-section"],ref:X,children:(0,i.jsx)("div",{children:v?(0,i.jsxs)(i.Fragment,{children:[(0,i.jsx)("p",{children:"Each encounter I’ve had with Lao Tzu has pointed me to something new. Almost as if the book changes with every reading. I first picked up Stephen Mitchell’s translation 40 years ago at the Bodhi Tree bookstore in Los Angeles and my life has never been quite the same."}),(0,i.jsx)("p",{id:"rick-rubin",children:"— Rick Rubin"})]}):(0,i.jsxs)(i.Fragment,{children:[(0,i.jsxs)("p",{children:["Each encounter I’ve had with Lao Tzu has pointed ",(0,i.jsx)("br",{}),"to something new. Almost as if the book changes",(0,i.jsx)("br",{}),"with every reading. I first picked up Stephen Mitchell’s",(0,i.jsx)("br",{}),"translation 40 years ago at the Bodhi Tree bookstore",(0,i.jsx)("br",{}),"in Los Angeles and my life has never been quite the same.",(0,i.jsx)("br",{})]}),(0,i.jsx)("p",{id:"rick-rubin",children:"— Rick Rubin"})]})})}),v&&(0,i.jsx)("menu",{className:b["mobile-menu"],children:(0,i.jsxs)("div",{className:b.controls,children:[(0,i.jsx)("div",{className:b.randomizer,onClick:function(){var e=at.map((function(e){return e.number})),n=e[Math.floor(Math.random()*e.length)];ne(n)},children:(0,i.jsx)(Ht,{isMobile:!0})}),(0,i.jsx)("div",{className:b["menu-title"],onClick:function(){C.current&&C.current.scrollTo({top:0,behavior:"instant"})},children:(0,i.jsx)("p",{children:" THE WAY OF CODE "})}),(0,i.jsx)("div",{className:b["menu-toggle"],onClick:function(){return B((function(e){return!e}))},children:L?(0,i.jsx)(wt,{}):(0,i.jsx)(xt,{})}),(0,i.jsx)("div",{className:"".concat(b.bg," ").concat(L?"active":""),ref:Z})]})}),!v&&(0,i.jsxs)("nav",{className:b.nav,id:"nav-desktop",ref:V,children:[(0,i.jsxs)("div",{className:b.index,children:[Array.from({length:81},(function(e,n){return(0,i.jsx)("div",{onClick:function(){return ne(n+1)},children:(0,i.jsx)("p",{className:R===n+1?b.active:"",children:String(n+1).padStart(2,"0")})},"nav-item-".concat(n+1))})),(0,i.jsx)("div",{onClick:function(){window.location.hash="PS"},children:(0,i.jsx)("p",{className:$.outro?b.active:"",children:" PS "})},"nav-item-PS")]}),(0,i.jsxs)("div",{className:b["nav-controls"],children:[(0,i.jsx)("div",{className:b["nav-item"],id:"nav-item-randomizer",onClick:function(){var e=at.map((function(e){return e.number})),n=e[Math.floor(Math.random()*e.length)];ne(n)},children:(0,i.jsx)(Ht,{isMobile:!1})}),(0,i.jsx)("div",{className:b["nav-item"],id:"nav-item-tetragrammaton",children:(0,i.jsx)("a",{href:"https://www.tetragrammaton.com/",target:"_blank",children:(0,i.jsx)(Mt,{})})})]})]}),v&&(0,i.jsx)("nav",{className:"".concat(b.nav," ").concat(L?b.open:""),id:"nav-mobile",ref:U,children:(0,i.jsxs)("div",{className:b.index,children:[Array.from({length:81},(function(e,n){return(0,i.jsx)("div",{id:"nav-item-".concat(n+1),className:b["nav-item"],onClick:function(){ne(n+1),ee(n+1)},children:(0,i.jsx)("p",{className:R===n+1?b.active:"",children:String(n+1).padStart(2,"0")})},"nav-item-".concat(n+1))})),(0,i.jsx)("div",{onClick:function(){window.location.hash="PS"},children:(0,i.jsx)("p",{className:$.outro?b.active:"",children:" PS "})},"nav-item-PS")]})}),at&&at.map((function(e,n){return(0,i.jsxs)(i.Fragment,{children:[(0,i.jsx)(kt,{poemData:e,isLongPoem:h[n],isLongPoemMobile:m[n],isMobile:v,isVisible:(t=e.number,S.includes(t)),sessionStart:a,registerPoemSectionRef:D,registerMobileArtworkRef:H,appRef:C,prevSection:z.current.get(e.number-1)},e.number),4===n?(0,i.jsxs)("section",{className:b["signup-section"],ref:q,style:$.signup?{opacity:1}:{opacity:0},children:[(0,i.jsx)("div",{className:b["signup-text"],children:(0,i.jsx)("a",{href:"https://www.tetragrammaton.com/join-newsletter",target:"_blank",children:(0,i.jsx)("p",{children:" Sign up here for more from Rick and Tetragrammaton "})})}),(0,i.jsx)("div",{className:b["signup-widget"],children:(0,i.jsx)(Dt,{})}),(0,i.jsx)("div",{className:b["continue-reading"],children:(0,i.jsx)("p",{children:"Continue reading below "})})]},"signup-section"):(0,i.jsx)(i.Fragment,{})]});var t})),(0,i.jsx)("section",{className:b["outro-section"],ref:W,children:(0,i.jsxs)("div",{children:[(0,i.jsx)("h3",{children:" An Invitation to Continue "}),(0,i.jsx)("p",{children:"The artwork above was created by vibe coding, generated based on the themes of each chapter. You can add prompts to change the images to your liking. You ultimately get to shape each of the art pieces."}),(0,i.jsx)("p",{children:"This is how art creation always happens."}),(0,i.jsx)("p",{children:"It starts with a prompt, a seed. It can be several sentences or something you notice on a walk. Anything can work as a first prompt. Only you can decide that."}),(0,i.jsx)("p",{children:"The world around you is pregnant with potential points of entry on your creative quest. Now find a seed and start modeling and modifying until you have something you’re excited to share."}),(0,i.jsx)("p",{children:(0,i.jsx)("span",{className:b["newsletter-link"],children:(0,i.jsxs)("a",{href:"https://rickrubin.substack.com/subscribe",target:"_blank",children:["Sign up here for more from",(0,i.jsx)("br",{})," Rick and Tetragrammaton"]})})})]})}),(0,i.jsxs)("section",{className:b["acknowledgments-section"],ref:G,children:[(0,i.jsx)("h3",{children:" References Consulted "}),(0,i.jsxs)("div",{className:b.acknowledgments,children:[(0,i.jsxs)("div",{id:b["references-container"],children:[(0,i.jsxs)("div",{id:b["references-left"],children:[(0,i.jsxs)("p",{children:[" Tao Te Ching: A New English Version",(0,i.jsx)("span",{children:" Stephen Mitchell (1988) "})]}),(0,i.jsxs)("p",{children:[" Tao Te Ching",(0,i.jsx)("span",{children:" Gia-Fu Feng and Jane English (1972) "})]}),(0,i.jsxs)("p",{children:[" Tao Te Ching: A New Translation",(0,i.jsx)("span",{children:" Ch'u Ta-Kao (1937) "})]}),(0,i.jsxs)("p",{children:[" The Book of Balance",(0,i.jsx)("span",{children:" Yasuhiko Genku Kimura (2002) "})]}),(0,i.jsxs)("p",{children:[" Tao Teh King: Interpreted as Nature and Intelligence",(0,i.jsx)("span",{children:" Archie J. Bahm (1958) "})]}),(0,i.jsxs)("p",{children:[" The Way of Life, According to Lao Tzu",(0,i.jsx)("span",{children:" Witter Bynner (1944) "})]}),(0,i.jsxs)("p",{children:[" The Way of Life: A New Translation",(0,i.jsx)("span",{children:" Gary Arnold (1996) "})]}),(0,i.jsxs)("p",{children:[" Tao: The Watercourse Way",(0,i.jsx)("span",{children:" Alan Watts and Al Chung-Liang Huang (1975) "})]}),(0,i.jsxs)("p",{children:[" The Way and Its Power: A Study of the Tao Te Ching and Its Place in Chinese Thought",(0,i.jsx)("span",{children:" Arthur Waley (1934) "})]}),(0,i.jsxs)("p",{children:[" Tao Te Ching: The Essential Translation of the Ancient Chinese Book of the Tao",(0,i.jsx)("span",{children:" John Minford (2018) "})]}),(0,i.jsxs)("p",{children:[" The Tao of Zen",(0,i.jsx)("span",{children:" Ray Grigg (1994) "})]})]}),(0,i.jsxs)("div",{id:b["references-right"],children:[(0,i.jsxs)("p",{children:[" Tao Te Ching",(0,i.jsx)("span",{children:" John H. McDonald (1996) "})]}),(0,i.jsxs)("p",{children:[" The Eternal Tao Te Ching: The Philosophical Masterwork of Taoism and Its Relevance Today",(0,i.jsx)("span",{children:" Benjamin Hoff (2021) "})]}),(0,i.jsxs)("p",{children:[" Tao Te Ching: The Book of Sense And Life",(0,i.jsx)("span",{children:" Richard Wilhelm (1910) "})]}),(0,i.jsxs)("p",{children:[" Tao Te Ching",(0,i.jsx)("span",{children:" Peter Fritz Walter (2017) "})]}),(0,i.jsxs)("p",{children:[" The Dao De Jing: A Qigong Interpretation",(0,i.jsx)("span",{children:" Dr. Yang, Jwing-Ming (2018) "})]}),(0,i.jsx)("p",{className:b["translations-note"],children:"Several other translations were only attributed to Lao Tzu with no translator listed. Also multiple AI interpretations including those from Claude Sonnet 3.7, Grok 3, ChatGPT 4 were considered in the research."})]})]}),(0,i.jsx)("div",{className:b["credits-container"],children:(0,i.jsx)("div",{id:b.contributors,children:(0,i.jsxs)("p",{children:["Website built by Anthropic. ",(0,i.jsx)("br",{}),"(Dexter Callender III, Whitney Chen, Linsey Fields, and Sam McAllister, in collaboration with ",(0,i.jsx)("a",{href:"https://claude.ai/",target:"_blank",rel:"noopener noreferrer",children:"Claude"}),") ",(0,i.jsx)("br",{}),"Artwork made possible with Claude artifacts, alongside the ",(0,i.jsx)("a",{href:"https://react.dev/",target:"_blank",rel:"noopener noreferrer",children:"React"}),", ",(0,i.jsx)("a",{href:"https://p5js.org/",target:"_blank",rel:"noopener noreferrer",children:"p5.js"}),", and ",(0,i.jsx)("a",{href:"https://threejs.org/",target:"_blank",rel:"noopener noreferrer",children:"Three.js"})," libraries. ",(0,i.jsx)("br",{}),"© Concept and words created by Rick Rubin, based on Lao Tzu. ",(0,i.jsx)("br",{}),"© Website and art created by ",(0,i.jsx)("a",{href:"https://www.anthropic.com/",target:"_blank",rel:"noopener noreferrer",children:"Anthropic"}),", based on Rick Rubin's words. ",(0,i.jsx)("br",{}),"All rights reserved. ",(0,i.jsx)("a",{href:"/privacy",onClick:function(e){e.preventDefault(),window.history.pushState({},"","/privacy"),t("/privacy")},children:"Privacy Policy"})]})})})]})]}),"/privacy"===n?(0,i.jsx)(Bt,{}):(0,i.jsx)(i.Fragment,{})]})};var Nt=t(7472);a.createRoot(document.getElementById("root")).render((0,i.jsxs)(i.Fragment,{children:[(0,i.jsx)(Gt,{}),(0,i.jsx)(Nt.j,{mode:"production"})]}))},4541:(e,n,t)=>{t.d(n,{A:()=>s});var i=t(1354),a=t.n(i),r=t(6314),o=t.n(r)()(a());o.push([e.id,':root{--background-color: #FAF9F5;--rubin-gray: #87867F;--rubin-ivory: rgb(250, 249, 246);--rubin-slate: #1F1E1D;--rubin-slate: #1F1E1D;--rubin-riso: #5E7EDF;--rubin-cyan: #44A6E4;--rubin-clay: #D97757;--text-color: rgba(0, 0, 0, 0.85);--tone: #F5F2EC;--rubin-dark-gray: #5E5D59;--rubin-ivory-med: #F0EEE6;--rubin-ivory-dark: #E8E6DC;--font-family-base: goudy-old-style, serif;--font-family-code: Courier Prime, Courier, monospace;--font-size-base: 18px;--line-height-base: 1.4;--letter-spacing-base: 0.02em;--artwork-font-size: 6px;--poem-padding-top: 20px;--poem-padding-left: 24px;--poem-padding: 0px 100px;--artwork-size: 322px;--breakpoint-tablet: 768px;--nav-width: 40px;--mobile-nav-width: 62px;--mobile-menu-height: 48px}@media(min-width: 768px)and (max-width: 1199px){:root{--font-size-base: 22px;--line-height-base: 0.9;--letter-spacing-base: 0.04em;--artwork-size: 450px;--artwork-font-size: 7.45px;--poem-padding-top: 20px;--poem-padding-left: 50px;--poem-padding: 0px 100px;--artwork-font-size: 7.5px}}@media(min-width: 1200px){:root{--font-size-base: 22px;--line-height-base: 1.5;--letter-spacing-base: 0.02em;--artwork-size: 450px;--poem-padding-top: 0px;--poem-padding-left: 100px;--poem-padding: 0px 100px;--artwork-font-size: 7.45px;--font-size-base: 22px}}html,body,#root{margin:0;padding:0;height:100%;width:100%;overflow:hidden}body{background-color:#faf9f5;font-family:"goudy-old-style",serif;-webkit-font-smoothing:antialiased;-moz-osx-font-smoothing:grayscale}code{font-family:"Courier Prime",monospace}',"",{version:3,sources:["webpack://./src/client/styles/variables.scss","webpack://./src/client/index.module.scss"],names:[],mappings:"AA2DA,MAEE,2BAAA,CACA,qBAAA,CACA,iCAAA,CACA,sBAAA,CACA,sBAAA,CACA,qBAAA,CACA,qBAAA,CACA,qBAAA,CACA,iCAAA,CACA,eAAA,CACA,0BAAA,CACA,0BAAA,CACA,2BAAA,CAEA,0CAAA,CACA,qDAAA,CACA,sBAAA,CACA,uBAAA,CACA,6BAAA,CAEA,wBAAA,CACA,wBAAA,CACA,yBAAA,CACA,yBAAA,CACA,qBAAA,CAEA,0BAAA,CACA,iBAAA,CAEA,wBAAA,CACA,0BAAA,CA5CA,gDAYF,MAoCI,sBAAA,CACA,uBAAA,CACA,6BAAA,CACA,qBAAA,CACA,2BAAA,CACA,wBAAA,CACA,yBAAA,CACA,yBAAA,CACA,0BAAA,CAAA,CAlDF,0BAMF,MAgDI,sBAAA,CACA,uBAAA,CACA,6BAAA,CACA,qBAAA,CACA,uBAAA,CACA,0BAAA,CACA,yBAAA,CACA,2BAAA,CACA,sBAAA,CAAA,CClHJ,gBACE,QAAA,CACA,SAAA,CACA,WAAA,CACA,UAAA,CACA,eAAA,CAGF,KACE,wBDJiB,CCKjB,mCAAA,CACA,kCAAA,CACA,iCAAA,CAGF,KACE,qCAAA",sourcesContent:["// Breakpoints\n$breakpoint-mobile: 480px;\n$breakpoint-tablet: 768px;\n$breakpoint-desktop: 1200px;\n\n// Colors\n$background-color: #FAF9F5;\n$text-color: rgba(0, 0, 0, 0.85);\n$rubin-gray: #87867F;\n$rubin-slate: #1F1E1D;\n$rubin-dark-gray: #5E5D59;\n$rubin-ivory-med: #F0EEE6;\n$rubin-ivory-dark: #E8E6DC;\n$rubin-ivory: rgb(250, 249, 246);\n$rubin-riso: #5E7EDF;\n$rubin-cyan: #44A6E4;\n$rubin-clay: #D97757;\n$rubin-beige: #E8E6DC;\n$tone: #F5F2EC;\n\n// Typography\n$font-family-base: \"goudy-old-style\", serif;\n$font-family-code: 'Courier Prime', Courier, monospace;\n$font-size-base: 22px;\n$line-height-base: 1.5;\n$letter-spacing-base: 0.02em;\n\n// Padding\n$poem-padding-top: 0px;\n$poem-padding-left: 100px;\n$poem-padding: $poem-padding-top $poem-padding-left;\n\n// Sizes\n$artwork-size: 450px;\n$artwork-font-size: 7.45px;\n$nav-width: 40px;\n$mobile-nav-width: 62px;\n$mobile-menu-height: 48px;\n\n// Responsive mixins\n@mixin mobile {\n  @media (max-width: #{$breakpoint-tablet - 1px}) {\n    @content;\n  }\n}\n\n@mixin tablet {\n  @media (min-width: #{$breakpoint-tablet}) and (max-width: #{$breakpoint-desktop - 1px}) {\n    @content;\n  }\n}\n\n@mixin desktop {\n  @media (min-width: #{$breakpoint-desktop}) {\n    @content;\n  }\n}\n\n// Responsive variables\n:root {\n  // Base values (mobile first)\n  --background-color: #{$background-color};\n  --rubin-gray: #{$rubin-gray};\n  --rubin-ivory: #{$rubin-ivory};\n  --rubin-slate: #{$rubin-slate};\n  --rubin-slate: #{$rubin-slate};\n  --rubin-riso: #{$rubin-riso};\n  --rubin-cyan: #{$rubin-cyan};\n  --rubin-clay: #{$rubin-clay};\n  --text-color: #{$text-color};\n  --tone: #{$tone};\n  --rubin-dark-gray: #{$rubin-dark-gray};\n  --rubin-ivory-med: #{$rubin-ivory-med};\n  --rubin-ivory-dark: #{$rubin-ivory-dark};\n\n  --font-family-base: #{$font-family-base};\n  --font-family-code: #{$font-family-code};\n  --font-size-base: 18px;\n  --line-height-base: 1.4; // Slightly looser on mobile\n  --letter-spacing-base: 0.02em;\n\n  --artwork-font-size: 6px;\n  --poem-padding-top: 20px;\n  --poem-padding-left: 24px;\n  --poem-padding: #{$poem-padding-top} #{$poem-padding-left};\n  --artwork-size: 322px;\n\n  --breakpoint-tablet: #{$breakpoint-tablet};\n  --nav-width: #{$nav-width};\n\n  --mobile-nav-width: #{$mobile-nav-width};\n  --mobile-menu-height: #{$mobile-menu-height};\n\n  \n  @include tablet {\n    --font-size-base: 22px;\n    --line-height-base: 0.9;\n    --letter-spacing-base: 0.04em;\n    --artwork-size: 450px;\n    --artwork-font-size: 7.45px;\n    --poem-padding-top: 20px;\n    --poem-padding-left: 50px;\n    --poem-padding: #{$poem-padding-top} #{$poem-padding-left};\n    --artwork-font-size: 7.5px;\n  }\n  \n  @include desktop {\n    --font-size-base: #{$font-size-base};\n    --line-height-base: #{$line-height-base};\n    --letter-spacing-base: #{$letter-spacing-base};\n    --artwork-size: #{$artwork-size};\n    --poem-padding-top: #{$poem-padding-top};\n    --poem-padding-left: #{$poem-padding-left};\n    --poem-padding: #{$poem-padding-top} #{$poem-padding-left};\n    --artwork-font-size: #{$artwork-font-size};\n    --font-size-base: #{$font-size-base};\n  }\n}\n\n",'@import "/vercel/path0/src/client/styles/variables.scss";\nhtml, body, #root {\n  margin: 0;\n  padding: 0;\n  height: 100%;\n  width: 100%;\n  overflow: hidden; // Prevent double scrollbars\n}\n\nbody {\n  background-color: $background-color;\n  font-family: "goudy-old-style", serif;\n  -webkit-font-smoothing: antialiased;\n  -moz-osx-font-smoothing: grayscale;\n}\n\ncode {\n  font-family: \'Courier Prime\', monospace;\n}'],sourceRoot:""}]),o.locals={root:"root"};const s=o},4851:(e,n,t)=>{t.d(n,{A:()=>s});var i=t(1354),a=t.n(i),r=t(6314),o=t.n(r)()(a());o.push([e.id,":root{--background-color: #FAF9F5;--rubin-gray: #87867F;--rubin-ivory: rgb(250, 249, 246);--rubin-slate: #1F1E1D;--rubin-slate: #1F1E1D;--rubin-riso: #5E7EDF;--rubin-cyan: #44A6E4;--rubin-clay: #D97757;--text-color: rgba(0, 0, 0, 0.85);--tone: #F5F2EC;--rubin-dark-gray: #5E5D59;--rubin-ivory-med: #F0EEE6;--rubin-ivory-dark: #E8E6DC;--font-family-base: goudy-old-style, serif;--font-family-code: Courier Prime, Courier, monospace;--font-size-base: 18px;--line-height-base: 1.4;--letter-spacing-base: 0.02em;--artwork-font-size: 6px;--poem-padding-top: 20px;--poem-padding-left: 24px;--poem-padding: 0px 100px;--artwork-size: 322px;--breakpoint-tablet: 768px;--nav-width: 40px;--mobile-nav-width: 62px;--mobile-menu-height: 48px}@media(min-width: 768px)and (max-width: 1199px){:root{--font-size-base: 22px;--line-height-base: 0.9;--letter-spacing-base: 0.04em;--artwork-size: 450px;--artwork-font-size: 7.45px;--poem-padding-top: 20px;--poem-padding-left: 50px;--poem-padding: 0px 100px;--artwork-font-size: 7.5px}}@media(min-width: 1200px){:root{--font-size-base: 22px;--line-height-base: 1.5;--letter-spacing-base: 0.02em;--artwork-size: 450px;--poem-padding-top: 0px;--poem-padding-left: 100px;--poem-padding: 0px 100px;--artwork-font-size: 7.45px;--font-size-base: 22px}}.poem-section{display:flex;position:relative;width:100%;height:100%;max-width:1440px;margin:0 auto;align-items:center;justify-content:center;gap:120px;padding:var(--poem-padding-top, 0px) var(--poem-padding-left, 100px);box-sizing:border-box;scroll-snap-align:start;scroll-snap-stop:always}.poem-section .artwork-container{opacity:0;transition:opacity 2s ease;transition-delay:.25s}.poem-section .column-1{opacity:0}.poem-section.long .poem{padding-bottom:20vh}.poem-section.long .column-1{align-self:flex-start;padding-top:15vh;max-height:70vh;overflow-y:auto;scrollbar-width:none;-ms-overflow-style:none;touch-action:pan-y}.poem-section.long .column-1::-webkit-scrollbar{display:none}.poem-section.in-view .column-1,.poem-section.in-view .artwork-container,.poem-section.in-view-long .column-1,.poem-section.in-view-long .artwork-container{opacity:1}.poem-section.in-view .column-1,.poem-section.in-view-long .column-1{transition:opacity 2s ease;transition-delay:1s}.poem-section.in-view .artwork-container,.poem-section.in-view-long .artwork-container{opacity:1}.poem-section.in-view-long .column-1{transition-delay:.75s}.linear-gradient{position:absolute;right:var(--poem-padding-left);height:90px;bottom:15vh;background:linear-gradient(transparent, #FAF9F5);pointer-events:none}@media(max-width: 1200px){.linear-gradient{bottom:12vh}}.artwork-section{width:100%;height:100vh;box-sizing:border-box;scroll-snap-align:start;scroll-snap-stop:always;padding:calc(var(--mobile-menu-height) + 24px) var(--poem-padding-left) 0 var(--poem-padding-left);display:flex;justify-content:flex-start}.column-1{width:50%;text-align:left;font-size:var(--font-size-base)}.column-1 h2{margin-top:0}.column-1 p{margin:0;line-height:1.4;white-space:pre-wrap}.poem{display:flex;align-items:baseline;gap:32px}.poem .poem-text,.poem .poem-number{white-space:pre-line}.poem .poem-text p,.poem .poem-number p{margin:0;margin-bottom:1.1em}.poem .poem-text p:last-child,.poem .poem-number p:last-child{margin-bottom:0}@media(max-width: 1200px){.poem-section{gap:80px}}@media(max-width: 1024px){.poem{flex-direction:column;gap:16px}.column-1{width:100%;min-height:auto}.column-1{opacity:1;transition-delay:0s;margin-top:calc(var(--mobile-menu-height))}.poem-section{align-items:flex-start}.poem-section .column-1{opacity:1}.poem-section.long .column-1{align-self:flex-start;padding-top:var(--mobile-menu-height);max-height:none;opacity:1}.poem-section.in-view .column-1,.poem-section.in-view-long .column-1{opacity:1;transition-delay:0s;transition:opacity .1s ease}.artwork-section{justify-content:center}}@media(max-width: 768px){.artwork-section{justify-content:flex-start}.artwork-section .artwork-container{position:relative;top:0;transform:none;justify-content:flex-start;width:100%}.artwork-section .artwork-container .artwork{width:100% !important;height:auto !important;aspect-ratio:1}.artwork-section .artwork-container .artwork div:not(.source-code):not(.grid-background):not(pre div){width:100% !important;height:100% !important;min-width:unset !important;min-height:unset !important}.poem-section.long{padding:0}.poem-section.long .column-1{padding:var(--poem-padding-top, 0px) var(--poem-padding-left, 100px);margin-top:calc(var(--mobile-menu-height) + (var(--poem-padding-top, 0px)));max-height:87vh;padding-top:0;touch-action:pan-y}.poem-section.long .poem{padding-bottom:60vh}}","",{version:3,sources:["webpack://./src/client/styles/variables.scss","webpack://./src/client/components/PoemModule/PoemModule.module.scss"],names:[],mappings:"AA2DA,MAEE,2BAAA,CACA,qBAAA,CACA,iCAAA,CACA,sBAAA,CACA,sBAAA,CACA,qBAAA,CACA,qBAAA,CACA,qBAAA,CACA,iCAAA,CACA,eAAA,CACA,0BAAA,CACA,0BAAA,CACA,2BAAA,CAEA,0CAAA,CACA,qDAAA,CACA,sBAAA,CACA,uBAAA,CACA,6BAAA,CAEA,wBAAA,CACA,wBAAA,CACA,yBAAA,CACA,yBAAA,CACA,qBAAA,CAEA,0BAAA,CACA,iBAAA,CAEA,wBAAA,CACA,0BAAA,CA5CA,gDAYF,MAoCI,sBAAA,CACA,uBAAA,CACA,6BAAA,CACA,qBAAA,CACA,2BAAA,CACA,wBAAA,CACA,yBAAA,CACA,yBAAA,CACA,0BAAA,CAAA,CAlDF,0BAMF,MAgDI,sBAAA,CACA,uBAAA,CACA,6BAAA,CACA,qBAAA,CACA,uBAAA,CACA,0BAAA,CACA,yBAAA,CACA,2BAAA,CACA,sBAAA,CAAA,CClHJ,cAEE,YAAA,CACA,iBAAA,CACA,UAAA,CACA,WAAA,CACA,gBAAA,CACA,aAAA,CACA,kBAAA,CACA,sBAAA,CACA,SAAA,CACA,oEAAA,CACA,qBAAA,CACA,uBAAA,CACA,uBAAA,CAEA,iCACE,SAAA,CACA,0BAAA,CACA,qBAAA,CAGF,wBACE,SAAA,CAKA,yBACE,mBAAA,CAGF,6BACE,qBAAA,CACA,gBAAA,CACA,eAAA,CACA,eAAA,CACA,oBAAA,CACA,uBAAA,CACA,kBAAA,CAEA,gDACE,YAAA,CAQJ,4JACE,SAAA,CAGF,qEACE,0BAAA,CACA,mBAAA,CAGF,uFACE,SAAA,CAKF,qCACE,qBAAA,CAMN,iBACE,iBAAA,CACA,8BAAA,CACA,WAAA,CACA,WAAA,CACA,gDAAA,CACA,mBAAA,CAGF,0BACE,iBACE,WAAA,CAAA,CAOJ,iBACE,UAAA,CACA,YAAA,CACA,qBAAA,CACA,uBAAA,CACA,uBAAA,CACA,kGAAA,CACA,YAAA,CACA,0BAAA,CAGF,UACE,SAAA,CACA,eAAA,CACA,+BAAA,CAGA,aACE,YAAA,CAGF,YACE,QAAA,CACA,eAAA,CACA,oBAAA,CAIJ,MACE,YAAA,CACA,oBAAA,CACA,QAAA,CAEA,oCACE,oBAAA,CAEA,wCACE,QAAA,CACA,mBAAA,CACA,8DACE,eAAA,CAOR,0BAEE,cACE,QAAA,CAAA,CAMJ,0BAEE,MACE,qBAAA,CACA,QAAA,CAGF,UACE,UAAA,CACA,eAAA,CAGF,UACE,SAAA,CACA,mBAAA,CACA,0CAAA,CAIF,cAEE,sBAAA,CAEA,wBACE,SAAA,CAIA,6BACE,qBAAA,CACA,qCAAA,CACA,eAAA,CACA,SAAA,CAKF,qEACE,SAAA,CACA,mBAAA,CACA,2BAAA,CAMN,iBACE,sBAAA,CAAA,CAKJ,yBAEE,iBACE,0BAAA,CAEA,oCACE,iBAAA,CACA,KAAA,CACA,cAAA,CACA,0BAAA,CACA,UAAA,CAEA,6CACE,qBAAA,CACA,sBAAA,CACA,cAAA,CAEA,sGACE,qBAAA,CACA,sBAAA,CACA,0BAAA,CACA,2BAAA,CAQN,mBACE,SAAA,CACA,6BACE,oEAAA,CACA,2EAAA,CACA,eAAA,CACA,aAAA,CACA,kBAAA,CAEF,yBACE,mBAAA,CAAA",sourcesContent:["// Breakpoints\n$breakpoint-mobile: 480px;\n$breakpoint-tablet: 768px;\n$breakpoint-desktop: 1200px;\n\n// Colors\n$background-color: #FAF9F5;\n$text-color: rgba(0, 0, 0, 0.85);\n$rubin-gray: #87867F;\n$rubin-slate: #1F1E1D;\n$rubin-dark-gray: #5E5D59;\n$rubin-ivory-med: #F0EEE6;\n$rubin-ivory-dark: #E8E6DC;\n$rubin-ivory: rgb(250, 249, 246);\n$rubin-riso: #5E7EDF;\n$rubin-cyan: #44A6E4;\n$rubin-clay: #D97757;\n$rubin-beige: #E8E6DC;\n$tone: #F5F2EC;\n\n// Typography\n$font-family-base: \"goudy-old-style\", serif;\n$font-family-code: 'Courier Prime', Courier, monospace;\n$font-size-base: 22px;\n$line-height-base: 1.5;\n$letter-spacing-base: 0.02em;\n\n// Padding\n$poem-padding-top: 0px;\n$poem-padding-left: 100px;\n$poem-padding: $poem-padding-top $poem-padding-left;\n\n// Sizes\n$artwork-size: 450px;\n$artwork-font-size: 7.45px;\n$nav-width: 40px;\n$mobile-nav-width: 62px;\n$mobile-menu-height: 48px;\n\n// Responsive mixins\n@mixin mobile {\n  @media (max-width: #{$breakpoint-tablet - 1px}) {\n    @content;\n  }\n}\n\n@mixin tablet {\n  @media (min-width: #{$breakpoint-tablet}) and (max-width: #{$breakpoint-desktop - 1px}) {\n    @content;\n  }\n}\n\n@mixin desktop {\n  @media (min-width: #{$breakpoint-desktop}) {\n    @content;\n  }\n}\n\n// Responsive variables\n:root {\n  // Base values (mobile first)\n  --background-color: #{$background-color};\n  --rubin-gray: #{$rubin-gray};\n  --rubin-ivory: #{$rubin-ivory};\n  --rubin-slate: #{$rubin-slate};\n  --rubin-slate: #{$rubin-slate};\n  --rubin-riso: #{$rubin-riso};\n  --rubin-cyan: #{$rubin-cyan};\n  --rubin-clay: #{$rubin-clay};\n  --text-color: #{$text-color};\n  --tone: #{$tone};\n  --rubin-dark-gray: #{$rubin-dark-gray};\n  --rubin-ivory-med: #{$rubin-ivory-med};\n  --rubin-ivory-dark: #{$rubin-ivory-dark};\n\n  --font-family-base: #{$font-family-base};\n  --font-family-code: #{$font-family-code};\n  --font-size-base: 18px;\n  --line-height-base: 1.4; // Slightly looser on mobile\n  --letter-spacing-base: 0.02em;\n\n  --artwork-font-size: 6px;\n  --poem-padding-top: 20px;\n  --poem-padding-left: 24px;\n  --poem-padding: #{$poem-padding-top} #{$poem-padding-left};\n  --artwork-size: 322px;\n\n  --breakpoint-tablet: #{$breakpoint-tablet};\n  --nav-width: #{$nav-width};\n\n  --mobile-nav-width: #{$mobile-nav-width};\n  --mobile-menu-height: #{$mobile-menu-height};\n\n  \n  @include tablet {\n    --font-size-base: 22px;\n    --line-height-base: 0.9;\n    --letter-spacing-base: 0.04em;\n    --artwork-size: 450px;\n    --artwork-font-size: 7.45px;\n    --poem-padding-top: 20px;\n    --poem-padding-left: 50px;\n    --poem-padding: #{$poem-padding-top} #{$poem-padding-left};\n    --artwork-font-size: 7.5px;\n  }\n  \n  @include desktop {\n    --font-size-base: #{$font-size-base};\n    --line-height-base: #{$line-height-base};\n    --letter-spacing-base: #{$letter-spacing-base};\n    --artwork-size: #{$artwork-size};\n    --poem-padding-top: #{$poem-padding-top};\n    --poem-padding-left: #{$poem-padding-left};\n    --poem-padding: #{$poem-padding-top} #{$poem-padding-left};\n    --artwork-font-size: #{$artwork-font-size};\n    --font-size-base: #{$font-size-base};\n  }\n}\n\n",'@import "/vercel/path0/src/client/styles/variables.scss";\n.poem-section {\n\n  display: flex;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  max-width: 1440px;\n  margin: 0 auto;\n  align-items: center;\n  justify-content: center;\n  gap: 120px;\n  padding: var(--poem-padding-top, $poem-padding-top) var(--poem-padding-left, $poem-padding-left);\n  box-sizing: border-box;\n  scroll-snap-align: start;\n  scroll-snap-stop: always;\n\n  .artwork-container {\n    opacity: 0;\n    transition: opacity 2s ease;\n    transition-delay: .25s;\n  }  \n\n  .column-1 {\n    opacity: 0;\n  }\n\n  &.long {\n\n    .poem {\n      padding-bottom: 20vh;\n    }\n\n    .column-1 {\n      align-self: flex-start;\n      padding-top: 15vh;\n      max-height: 70vh;\n      overflow-y: auto;\n      scrollbar-width: none; /* Firefox */\n      -ms-overflow-style: none; /* IE and Edge */\n      touch-action: pan-y; /* Ensure proper touch handling */\n      \n      &::-webkit-scrollbar {\n        display: none; /* Chrome, Safari, Opera*/\n      }\n\n    }\n\n  }\n  \n  &.in-view, &.in-view-long {\n    .column-1, .artwork-container {\n      opacity: 1;\n    }\n\n    .column-1 {\n      transition: opacity 2s ease;\n      transition-delay: 1s;\n    }\n\n    .artwork-container {\n      opacity: 1;\n    }\n  }\n\n  &.in-view-long {\n    .column-1 {\n      transition-delay: .75s;\n    }\n  }\n\n}\n\n.linear-gradient {\n  position: absolute;\n  right: var(--poem-padding-left);\n  height: 90px;\n  bottom: 15vh;\n  background: linear-gradient(transparent, #FAF9F5);\n  pointer-events: none;\n}\n\n@media (max-width: 1200px) {\n  .linear-gradient {\n    bottom: 12vh;\n  }\n}\n\n\n/* Mobile artwork section */\n\n.artwork-section {\n  width: 100%;\n  height: 100vh;\n  box-sizing: border-box;\n  scroll-snap-align: start;\n  scroll-snap-stop: always;\n  padding: calc(var(--mobile-menu-height) + 24px) var(--poem-padding-left) 0 var(--poem-padding-left);\n  display: flex;\n  justify-content: flex-start;\n}\n\n.column-1 {\n  width: 50%;\n  text-align: left;\n  font-size: var(--font-size-base);\n  /* Removing font-family to inherit base font family (goudy-old-style) */\n  \n  h2 {\n    margin-top: 0;\n  }\n\n  p {\n    margin: 0;\n    line-height: 1.4;\n    white-space: pre-wrap;\n  }\n}\n\n.poem {\n  display: flex;\n  align-items: baseline;\n  gap: 32px;\n  \n  .poem-text, .poem-number {\n    white-space: pre-line;\n\n    p {\n      margin: 0;\n      margin-bottom: 1.1em;\n      &:last-child {\n        margin-bottom: 0;\n      }\n    }\n  }\n}\n\n\n@media (max-width: 1200px) {\n\n  .poem-section {\n    gap: 80px;\n  }\n\n}\n\n/* Tablet Styles */\n@media (max-width: 1024px) {\n\n  .poem {\n    flex-direction: column;\n    gap: 16px;\n  }\n  \n  .column-1 {\n    width: 100%;\n    min-height: auto;\n  }\n\n  .column-1 {\n    opacity: 1;\n    transition-delay: 0s;\n    margin-top: calc(var(--mobile-menu-height));\n  }\n\n\n  .poem-section {\n    \n    align-items: flex-start;\n\n    .column-1 {\n      opacity: 1;\n    }\n\n    &.long {\n      .column-1 {\n        align-self: flex-start;\n        padding-top: var(--mobile-menu-height);\n        max-height: none;\n        opacity: 1;\n      }\n    }\n\n    &.in-view, &.in-view-long {\n      .column-1 {\n        opacity: 1;\n        transition-delay: 0s;\n        transition: opacity .1s ease;\n      }\n    }\n    \n  }\n\n  .artwork-section {\n    justify-content: center;\n  }\n\n}\n\n@media (max-width: 768px) {\n\n  .artwork-section {\n    justify-content: flex-start;\n\n    .artwork-container {\n      position: relative;\n      top: 0;\n      transform: none;\n      justify-content: flex-start;\n      width: 100%;\n\n      .artwork {\n        width: 100% !important;\n        height: auto !important;\n        aspect-ratio: 1;\n\n        div:not(.source-code):not(.grid-background):not(pre div) {\n          width: 100% !important;\n          height: 100% !important;\n          min-width: unset !important;\n          min-height: unset !important;\n        }\n      }\n    }\n\n  }\n\n  .poem-section {\n    &.long {\n      padding: 0;\n      .column-1 {\n        padding: var(--poem-padding-top, $poem-padding-top) var(--poem-padding-left, $poem-padding-left);\n        margin-top: calc(var(--mobile-menu-height) + (var(--poem-padding-top, $poem-padding-top)));\n        max-height: 87vh;\n        padding-top: 0;\n        touch-action: pan-y; /* Ensure proper touch handling on mobile */\n      }\n      .poem {\n        padding-bottom: 60vh;\n      }\n    }\n    \n  }\n\n}'],sourceRoot:""}]),o.locals={"poem-section":"poem-section","artwork-container":"artwork-container","column-1":"column-1",long:"long",poem:"poem","in-view":"in-view","in-view-long":"in-view-long","linear-gradient":"linear-gradient","artwork-section":"artwork-section","poem-text":"poem-text","poem-number":"poem-number",artwork:"artwork","source-code":"source-code","grid-background":"grid-background"};const s=o},5495:(e,n,t)=>{t.d(n,{A:()=>u});var i=t(1354),a=t.n(i),r=t(6314),o=t.n(r),s=t(4417),c=t.n(s),l=new URL(t(5692),t.b),h=o()(a()),d=c()(l);h.push([e.id,`:root{--background-color: #FAF9F5;--rubin-gray: #87867F;--rubin-ivory: rgb(250, 249, 246);--rubin-slate: #1F1E1D;--rubin-slate: #1F1E1D;--rubin-riso: #5E7EDF;--rubin-cyan: #44A6E4;--rubin-clay: #D97757;--text-color: rgba(0, 0, 0, 0.85);--tone: #F5F2EC;--rubin-dark-gray: #5E5D59;--rubin-ivory-med: #F0EEE6;--rubin-ivory-dark: #E8E6DC;--font-family-base: goudy-old-style, serif;--font-family-code: Courier Prime, Courier, monospace;--font-size-base: 18px;--line-height-base: 1.4;--letter-spacing-base: 0.02em;--artwork-font-size: 6px;--poem-padding-top: 20px;--poem-padding-left: 24px;--poem-padding: 0px 100px;--artwork-size: 322px;--breakpoint-tablet: 768px;--nav-width: 40px;--mobile-nav-width: 62px;--mobile-menu-height: 48px}@media(min-width: 768px)and (max-width: 1199px){:root{--font-size-base: 22px;--line-height-base: 0.9;--letter-spacing-base: 0.04em;--artwork-size: 450px;--artwork-font-size: 7.45px;--poem-padding-top: 20px;--poem-padding-left: 50px;--poem-padding: 0px 100px;--artwork-font-size: 7.5px}}@media(min-width: 1200px){:root{--font-size-base: 22px;--line-height-base: 1.5;--letter-spacing-base: 0.02em;--artwork-size: 450px;--poem-padding-top: 0px;--poem-padding-left: 100px;--poem-padding: 0px 100px;--artwork-font-size: 7.45px;--font-size-base: 22px}}.artwork-container{position:relative;width:50%;height:100%;display:flex;flex-direction:column;align-items:center;justify-content:center}.artwork{position:relative;width:var(--artwork-size, 450px);height:var(--artwork-size, 450px)}.artwork,.artwork div,.artwork *,.artwork *::before,.artwork *::after{font-family:inherit}.artwork div:not(.source-code):not(.grid-background):not(pre div):not(.rhombus){width:var(--artwork-size, 450px) !important;height:var(--artwork-size, 450px) !important;font-size:var(--artwork-font-size, 7.45px);overflow:hidden !important;padding:0 !important;min-width:var(--artwork-size, 450px) !important;min-height:var(--artwork-size, 450px) !important}.artwork canvas,.artwork svg{width:100% !important;height:100% !important;overflow:hidden}.artwork:hover~.menu{opacity:1}.artwork:hover~.menu:has(+.artwork-instructions){transition-delay:.2s}.artwork:hover~.artwork-instructions{transform:translateY(44px)}.artwork:hover~.artwork-instructions p::before{content:"Use the suggested prompt or write your own"}.artwork-instructions{position:absolute;top:calc(50% + .5*var(--artwork-size) + 10px);left:calc(50% - .5*var(--artwork-size) + 2px);width:var(--artwork-size, 450px);text-align:left;transition:all .3s ease;z-index:-1}.artwork-instructions p{font-family:var(--font-family-code, "Courier Prime", Courier, monospace);font-size:12px;color:var(--rubin-gray);margin:0;padding:0}.artwork-instructions p::before{content:"Hover over the artwork to modify the code"}.menu{position:relative;z-index:1;width:var(--artwork-size, 450px);position:absolute;top:calc(50% + .5*var(--artwork-size, 450px));padding-top:14px;padding-left:0;margin-top:0;opacity:0;transition:opacity .3s cubic-bezier(0.075, 0.82, 0.165, 1)}.menu:hover{opacity:1}.menu:hover+.artwork-instructions{transform:translateY(44px)}.menu:hover+.artwork-instructions p::before{content:"Use the suggested prompt or write your own"}.menu p,.menu a,.menu input{font-family:var(--font-family-code, "Courier Prime", Courier, monospace);margin:0;padding:0;font-size:12px;color:var(--rubin-dark-gray, #5E5D59)}.menu .row{display:flex;align-items:center;height:30px;gap:8px;margin-bottom:14px}.menu input{flex:2;border-radius:4px;background-color:#fff;border:1px solid #f0eee6;color:var(--rubin-dark-gray);padding-left:14px}.menu input::placeholder{color:var(--rubin-dark-gray)}.menu input:focus{border-color:var(--rubin-gray);outline:none}.menu button,.menu input{height:100%}.menu button{position:relative;justify-content:center}.menu button>svg{display:block;background:none;opacity:1}.menu button:hover{cursor:pointer}.menu .btn-modify,.menu .btn-code{background-color:var(--rubin-ivory-med);border:none;border-radius:4px;transition:all .2s ease;color:var(--rubin-dark-gray);align-items:center;display:inline-flex;gap:4px}.menu .btn-modify p,.menu .btn-modify svg,.menu .btn-code p,.menu .btn-code svg{color:inherit}.menu .btn-modify svg,.menu .btn-code svg{position:relative;top:1px}.menu .btn-modify:hover,.menu .btn-code:hover{background-color:var(--rubin-ivory-dark);color:var(--rubin-slate);cursor:pointer}.menu .btn-modify:active,.menu .btn-code:active{transform:translateY(1px)}.menu .btn-modify:disabled,.menu .btn-code:disabled{opacity:.5;cursor:not-allowed}.menu .btn-code{width:44px}.menu .btn-code.active{background-color:var(--rubin-ivory-dark)}.btn-modify{padding:0 12px;width:85px;display:flex;flex-direction:row;justify-content:center;align-items:center;gap:4px}.source-code{position:absolute;left:0;top:0;width:100%;height:100%;opacity:0;white-space:pre-wrap;color:#333;background-color:var(--rubin-ivory);background-image:url(${d});background-repeat:repeat;background-size:auto;background-position:0 0;background-attachment:local;opacity:0}.source-code.active{opacity:1;overflow-y:scroll;transition:opacity .5s cubic-bezier(0.25, 0.1, 0.25, 1)}.source-code p{font-family:var(--font-family-code, "Courier Prime", Courier, monospace);font-size:12px}.source-code pre{position:relative;z-index:1;margin:0;padding:20px;font-family:var(--font-family-code, "Courier Prime", Courier, monospace);font-size:12px;line-height:1.5;white-space:pre-wrap;word-wrap:break-word}.source-code pre code{font-family:inherit;font-size:inherit}.source-code .hljs-keyword,.source-code .hljs-built_in{color:var(--rubin-cyan)}.source-code .hljs-string{color:var(--rubin-teal)}.source-code .hljs-number{color:var(--rubin-clay)}.source-code .hljs-comment{color:var(--rubin-gray);font-style:italic}.source-code .hljs-function .hljs-title,.source-code .hljs-title.function_{color:var(--rubin-cyan)}.source-code .hljs-operator{color:#859900}.source-code .hljs-variable{color:var(--rubin-clay)}.source-code .hljs-attr{color:var(--rubin-cyan)}.source-code .hljs-params{color:var(--rubin-gray)}.source-code code{color:var(--rubin-gray)}@media(max-width: 1024px){.artwork-container{width:100%;max-width:450px;gap:12px}.artwork-container .menu{opacity:1;position:relative;top:0;padding-top:0;margin-bottom:0;width:100%;max-width:450px}.artwork-container .menu .row{height:40px}.artwork-container .menu input{font-size:14px}.artwork-container .menu textarea{height:40px;--text-area-padding: 14px;width:calc(100% - var(--text-area-padding)*2);padding:0 var(--text-area-padding);resize:none;font-family:var(--font-family-code, "Courier Prime", Courier, monospace);font-size:14px;color:var(--rubin-dark-gray);border-radius:4px;background-color:#fff;border:1px solid #f0eee6;margin:0 0 6px 0;line-height:40px;opacity:0;transition:opacity .2s cubic-bezier(0.075, 0.82, 0.165, 1)}.artwork-container .menu textarea::placeholder{color:var(--rubin-dark-gray)}.artwork-container .menu textarea:focus{border-color:var(--rubin-gray);outline:none}.artwork-container .menu textarea.active{opacity:1}.artwork-container .menu .row{margin-bottom:0;justify-content:flex-end}.source-code pre{padding:10px}.source-code p,.source-code pre{font-size:10px;line-height:1.35}}`,"",{version:3,sources:["webpack://./src/client/styles/variables.scss","webpack://./src/client/components/PoemModule/ArtworkContainer.module.scss"],names:[],mappings:"AA2DA,MAEE,2BAAA,CACA,qBAAA,CACA,iCAAA,CACA,sBAAA,CACA,sBAAA,CACA,qBAAA,CACA,qBAAA,CACA,qBAAA,CACA,iCAAA,CACA,eAAA,CACA,0BAAA,CACA,0BAAA,CACA,2BAAA,CAEA,0CAAA,CACA,qDAAA,CACA,sBAAA,CACA,uBAAA,CACA,6BAAA,CAEA,wBAAA,CACA,wBAAA,CACA,yBAAA,CACA,yBAAA,CACA,qBAAA,CAEA,0BAAA,CACA,iBAAA,CAEA,wBAAA,CACA,0BAAA,CA5CA,gDAYF,MAoCI,sBAAA,CACA,uBAAA,CACA,6BAAA,CACA,qBAAA,CACA,2BAAA,CACA,wBAAA,CACA,yBAAA,CACA,yBAAA,CACA,0BAAA,CAAA,CAlDF,0BAMF,MAgDI,sBAAA,CACA,uBAAA,CACA,6BAAA,CACA,qBAAA,CACA,uBAAA,CACA,0BAAA,CACA,yBAAA,CACA,2BAAA,CACA,sBAAA,CAAA,CClHJ,mBACE,iBAAA,CACA,SAAA,CACA,WAAA,CACA,YAAA,CACA,qBAAA,CACA,kBAAA,CACA,sBAAA,CAGF,SACE,iBAAA,CAEA,gCAAA,CACA,iCAAA,CAGA,sEACE,mBAAA,CAGF,gFACE,2CAAA,CACA,4CAAA,CACA,0CAAA,CACA,0BAAA,CACA,oBAAA,CACA,+CAAA,CACA,gDAAA,CAGF,6BACE,qBAAA,CACA,sBAAA,CACA,eAAA,CAIA,qBACE,SAAA,CAGF,iDACE,oBAAA,CAGF,qCACE,0BAAA,CACA,+CACE,oDAAA,CAMR,sBACE,iBAAA,CACA,6CAAA,CACA,6CAAA,CACA,gCAAA,CACA,eAAA,CACA,uBAAA,CACA,UAAA,CAEA,wBACE,wEAAA,CACA,cAAA,CACA,uBAAA,CACA,QAAA,CACA,SAAA,CACA,gCACE,mDAAA,CAMN,MACE,iBAAA,CACA,SAAA,CACA,gCAAA,CACA,iBAAA,CACA,6CAAA,CACA,gBAAA,CAEA,cAAA,CACA,YAAA,CAEA,SAAA,CACA,0DAAA,CAEA,YACE,SAAA,CAEA,kCACE,0BAAA,CACA,4CACE,oDAAA,CAKN,4BACE,wEAAA,CACA,QAAA,CACA,SAAA,CACA,cAAA,CACA,qCAAA,CAGF,WACE,YAAA,CACA,kBAAA,CACA,WAAA,CACA,OAAA,CACA,kBAAA,CAGF,YACE,MAAA,CACA,iBAAA,CACA,qBAAA,CACA,wBAAA,CACA,4BAAA,CACA,iBAAA,CAEA,yBACE,4BAAA,CAGF,kBACE,8BAAA,CACA,YAAA,CAIJ,yBACE,WAAA,CAGF,aACE,iBAAA,CACA,sBAAA,CACA,iBACE,aAAA,CACA,eAAA,CACA,SAAA,CAEF,mBACE,cAAA,CAIJ,kCACE,uCAAA,CACA,WAAA,CACA,iBAAA,CACA,uBAAA,CACA,4BAAA,CACA,kBAAA,CAWA,mBAAA,CACA,OAAA,CAVA,gFACE,aAAA,CAGF,0CACE,iBAAA,CACA,OAAA,CAMF,8CACE,wCAAA,CACA,wBAAA,CACA,cAAA,CAEF,gDACE,yBAAA,CAGF,oDACE,UAAA,CACA,kBAAA,CAIJ,gBACE,UAAA,CACA,uBACE,wCAAA,CAMN,YACE,cAAA,CACA,UAAA,CACA,YAAA,CACA,kBAAA,CACA,sBAAA,CACA,kBAAA,CACA,OAAA,CAGF,aACE,iBAAA,CACA,MAAA,CACA,KAAA,CACA,UAAA,CACA,WAAA,CACA,SAAA,CACA,oBAAA,CACA,UAAA,CACA,mCAAA,CAEA,wDAAA,CACA,wBAAA,CACA,oBAAA,CACA,uBAAA,CACA,2BAAA,CAEA,SAAA,CAEA,oBACE,SAAA,CACA,iBAAA,CACA,uDAAA,CAGF,eACE,wEAAA,CACA,cAAA,CAGF,iBACE,iBAAA,CACA,SAAA,CACA,QAAA,CACA,YAAA,CACA,wEAAA,CACA,cAAA,CACA,eAAA,CACA,oBAAA,CACA,oBAAA,CAEA,sBACE,mBAAA,CACA,iBAAA,CAIJ,uDAEE,uBAAA,CAGF,0BACE,uBAAA,CAGF,0BACE,uBAAA,CAGF,2BACE,uBAAA,CACA,iBAAA,CAGF,2EAEE,uBAAA,CAGF,4BACE,aAAA,CAGF,4BACE,uBAAA,CAGF,wBACE,uBAAA,CAGF,0BACE,uBAAA,CAGF,kBACE,uBAAA,CAKJ,0BAEE,mBACE,UAAA,CACA,eAAA,CACA,QAAA,CAEA,yBACE,SAAA,CACA,iBAAA,CACA,KAAA,CACA,aAAA,CACA,eAAA,CACA,UAAA,CACA,eAAA,CAEA,8BACE,WAAA,CAGF,+BACE,cAAA,CAGF,kCACE,WAAA,CACA,yBAAA,CACA,6CAAA,CACA,kCAAA,CACA,WAAA,CACA,wEAAA,CACA,cAAA,CACA,4BAAA,CACA,iBAAA,CACA,qBAAA,CACA,wBAAA,CACA,gBAAA,CACA,gBAAA,CAWA,SAAA,CACA,0DAAA,CAVA,+CACE,4BAAA,CAGF,wCACE,8BAAA,CACA,YAAA,CAKF,yCACE,SAAA,CAIJ,8BACE,eAAA,CACA,wBAAA,CAQJ,iBACE,YAAA,CAGF,gCACE,cAAA,CACA,gBAAA,CAAA",sourcesContent:["// Breakpoints\n$breakpoint-mobile: 480px;\n$breakpoint-tablet: 768px;\n$breakpoint-desktop: 1200px;\n\n// Colors\n$background-color: #FAF9F5;\n$text-color: rgba(0, 0, 0, 0.85);\n$rubin-gray: #87867F;\n$rubin-slate: #1F1E1D;\n$rubin-dark-gray: #5E5D59;\n$rubin-ivory-med: #F0EEE6;\n$rubin-ivory-dark: #E8E6DC;\n$rubin-ivory: rgb(250, 249, 246);\n$rubin-riso: #5E7EDF;\n$rubin-cyan: #44A6E4;\n$rubin-clay: #D97757;\n$rubin-beige: #E8E6DC;\n$tone: #F5F2EC;\n\n// Typography\n$font-family-base: \"goudy-old-style\", serif;\n$font-family-code: 'Courier Prime', Courier, monospace;\n$font-size-base: 22px;\n$line-height-base: 1.5;\n$letter-spacing-base: 0.02em;\n\n// Padding\n$poem-padding-top: 0px;\n$poem-padding-left: 100px;\n$poem-padding: $poem-padding-top $poem-padding-left;\n\n// Sizes\n$artwork-size: 450px;\n$artwork-font-size: 7.45px;\n$nav-width: 40px;\n$mobile-nav-width: 62px;\n$mobile-menu-height: 48px;\n\n// Responsive mixins\n@mixin mobile {\n  @media (max-width: #{$breakpoint-tablet - 1px}) {\n    @content;\n  }\n}\n\n@mixin tablet {\n  @media (min-width: #{$breakpoint-tablet}) and (max-width: #{$breakpoint-desktop - 1px}) {\n    @content;\n  }\n}\n\n@mixin desktop {\n  @media (min-width: #{$breakpoint-desktop}) {\n    @content;\n  }\n}\n\n// Responsive variables\n:root {\n  // Base values (mobile first)\n  --background-color: #{$background-color};\n  --rubin-gray: #{$rubin-gray};\n  --rubin-ivory: #{$rubin-ivory};\n  --rubin-slate: #{$rubin-slate};\n  --rubin-slate: #{$rubin-slate};\n  --rubin-riso: #{$rubin-riso};\n  --rubin-cyan: #{$rubin-cyan};\n  --rubin-clay: #{$rubin-clay};\n  --text-color: #{$text-color};\n  --tone: #{$tone};\n  --rubin-dark-gray: #{$rubin-dark-gray};\n  --rubin-ivory-med: #{$rubin-ivory-med};\n  --rubin-ivory-dark: #{$rubin-ivory-dark};\n\n  --font-family-base: #{$font-family-base};\n  --font-family-code: #{$font-family-code};\n  --font-size-base: 18px;\n  --line-height-base: 1.4; // Slightly looser on mobile\n  --letter-spacing-base: 0.02em;\n\n  --artwork-font-size: 6px;\n  --poem-padding-top: 20px;\n  --poem-padding-left: 24px;\n  --poem-padding: #{$poem-padding-top} #{$poem-padding-left};\n  --artwork-size: 322px;\n\n  --breakpoint-tablet: #{$breakpoint-tablet};\n  --nav-width: #{$nav-width};\n\n  --mobile-nav-width: #{$mobile-nav-width};\n  --mobile-menu-height: #{$mobile-menu-height};\n\n  \n  @include tablet {\n    --font-size-base: 22px;\n    --line-height-base: 0.9;\n    --letter-spacing-base: 0.04em;\n    --artwork-size: 450px;\n    --artwork-font-size: 7.45px;\n    --poem-padding-top: 20px;\n    --poem-padding-left: 50px;\n    --poem-padding: #{$poem-padding-top} #{$poem-padding-left};\n    --artwork-font-size: 7.5px;\n  }\n  \n  @include desktop {\n    --font-size-base: #{$font-size-base};\n    --line-height-base: #{$line-height-base};\n    --letter-spacing-base: #{$letter-spacing-base};\n    --artwork-size: #{$artwork-size};\n    --poem-padding-top: #{$poem-padding-top};\n    --poem-padding-left: #{$poem-padding-left};\n    --poem-padding: #{$poem-padding-top} #{$poem-padding-left};\n    --artwork-font-size: #{$artwork-font-size};\n    --font-size-base: #{$font-size-base};\n  }\n}\n\n","@import \"/vercel/path0/src/client/styles/variables.scss\";\n.artwork-container {\n  position: relative;\n  width: 50%;\n  height: 100%;\n  display: flex;\n  flex-direction: column;\n  align-items: center;\n  justify-content: center;\n}\n\n.artwork {\n  position: relative;\n\n  width: var(--artwork-size, $artwork-size);\n  height: var(--artwork-size, $artwork-size);\n  \n  /* Explicitly override any font settings for artwork elements */\n  &, div, *, *::before, *::after {\n    font-family: inherit; /* Use whatever font was set by the artwork component */\n  }\n  \n  div:not(.source-code):not(.grid-background):not(pre div):not(.rhombus) {\n    width: var(--artwork-size, $artwork-size) !important;\n    height: var(--artwork-size, $artwork-size) !important;\n    font-size: var(--artwork-font-size, $artwork-font-size);\n    overflow: hidden !important;\n    padding: 0 !important;\n    min-width: var(--artwork-size, $artwork-size) !important;\n    min-height: var(--artwork-size, $artwork-size) !important;\n  }\n\n  canvas, svg {\n    width: 100% !important;\n    height: 100% !important;\n    overflow: hidden;\n  }\n\n  &:hover {\n    ~ .menu {\n      opacity: 1;\n    }\n\n    ~ .menu:has(+ .artwork-instructions) {\n      transition-delay: .2s;\n    }\n\n    ~ .artwork-instructions {\n      transform: translateY(44px);\n      p::before {\n        content: 'Use the suggested prompt or write your own';\n      }\n    }\n  }\n}\n\n.artwork-instructions {\n  position: absolute;\n  top: calc(50% + (.5 * var(--artwork-size)) + 10px);\n  left: calc(50% - (.5 * var(--artwork-size)) + 2px);\n  width: var(--artwork-size, $artwork-size);\n  text-align: left;\n  transition: all .3s ease;\n  z-index: -1;\n\n  p {\n    font-family: var(--font-family-code, $font-family-code);\n    font-size: 12px;\n    color: var(--rubin-gray);\n    margin: 0;\n    padding: 0;\n    &::before {\n      content: 'Hover over the artwork to modify the code';\n    }\n  }\n  \n}\n\n.menu {\n  position: relative;\n  z-index: 1;\n  width: var(--artwork-size, $artwork-size);\n  position: absolute;\n  top: calc(50% + .5 * var(--artwork-size, $artwork-size));\n  padding-top: 14px;\n  \n  padding-left: 0;\n  margin-top: 0;\n\n  opacity: 0;\n  transition: opacity 0.3s cubic-bezier(0.075, 0.82, 0.165, 1);\n\n  &:hover {\n    opacity: 1;\n\n    &+.artwork-instructions {\n      transform: translateY(44px);\n      p::before {\n        content: 'Use the suggested prompt or write your own';\n      }\n    }\n  }\n\n  p , a , input {\n    font-family: var(--font-family-code, $font-family-code);\n    margin: 0;\n    padding: 0;\n    font-size: 12px;\n    color: var(--rubin-dark-gray, $rubin-dark-gray);\n  }\n  \n  .row {\n    display: flex;\n    align-items: center;\n    height: 30px;\n    gap: 8px;\n    margin-bottom: 14px;\n  }\n\n  input {\n    flex: 2;\n    border-radius: 4px;\n    background-color: white;\n    border: 1px solid #F0EEE6;\n    color: var(--rubin-dark-gray);\n    padding-left: 14px;\n\n    &::placeholder {\n      color: var(--rubin-dark-gray);\n    }\n\n    &:focus {\n      border-color: var(--rubin-gray);\n      outline: none;\n    }\n  }\n\n  button, input {\n    height: 100%;\n  }\n\n  button {\n    position: relative;\n    justify-content: center;\n    & > svg {\n      display: block;\n      background: none;\n      opacity: 1;\n    }\n    &:hover {\n      cursor: pointer;\n    }\n  }\n\n  .btn-modify, .btn-code {\n    background-color: var(--rubin-ivory-med);\n    border: none;\n    border-radius: 4px;\n    transition: all 0.2s ease;\n    color: var(--rubin-dark-gray);\n    align-items: center;\n\n    p, svg {\n      color: inherit;\n    }\n\n    svg {\n      position: relative;\n      top: 1px;\n    }\n\n    display: inline-flex;\n    gap: 4px;\n\n    &:hover {\n      background-color: var(--rubin-ivory-dark);\n      color: var(--rubin-slate);\n      cursor: pointer;\n    }\n    &:active {\n      transform: translateY(1px);\n    }\n    \n    &:disabled {\n      opacity: 0.5;\n      cursor: not-allowed;\n    }\n  }\n\n  .btn-code {\n    width: 44px;\n    &.active {\n      background-color: var(--rubin-ivory-dark);\n    }\n  }\n\n}\n\n.btn-modify {\n  padding: 0 12px;\n  width: 85px;\n  display: flex;\n  flex-direction: row;\n  justify-content: center;\n  align-items: center;\n  gap: 4px;\n}\n\n.source-code {\n  position: absolute;\n  left: 0;\n  top: 0;\n  width: 100%;\n  height: 100%;\n  opacity: 0;\n  white-space: pre-wrap;\n  color: #333;\n  background-color: var(--rubin-ivory);\n  \n  background-image: url(./../../../../public/image/grid.png);\n  background-repeat: repeat;\n  background-size: auto;\n  background-position: 0 0;\n  background-attachment: local;\n\n  opacity: 0;\n\n  &.active {\n    opacity: 1; \n    overflow-y: scroll;\n    transition: opacity 0.5s cubic-bezier(0.25, 0.1, 0.25, 1);\n  }\n\n  p {\n    font-family: var(--font-family-code, $font-family-code);\n    font-size: 12px;\n  }\n\n  pre {\n    position: relative;\n    z-index: 1;\n    margin: 0;\n    padding: 20px;\n    font-family: var(--font-family-code, $font-family-code);\n    font-size: 12px;\n    line-height: 1.5;\n    white-space: pre-wrap;\n    word-wrap: break-word;\n    \n    code {\n      font-family: inherit;\n      font-size: inherit;\n    }\n  }\n\n  .hljs-keyword,\n  .hljs-built_in {\n    color: var(--rubin-cyan);\n  }\n  \n  .hljs-string {\n    color: var(--rubin-teal);\n  }\n  \n  .hljs-number {\n    color: var(--rubin-clay); \n  }\n  \n  .hljs-comment {\n    color: var(--rubin-gray);\n    font-style: italic;\n  }\n  \n  .hljs-function .hljs-title,\n  .hljs-title.function_ {\n    color: var(--rubin-cyan); \n  }\n  \n  .hljs-operator {\n    color: #859900; \n  }\n  \n  .hljs-variable {\n    color: var(--rubin-clay); \n  }\n  \n  .hljs-attr {\n    color: var(--rubin-cyan);\n  }\n  \n  .hljs-params {\n    color: var(--rubin-gray); \n  }\n  \n  code {\n    color: var(--rubin-gray);\n  }\n\n}\n\n@media (max-width: 1024px) {\n\n  .artwork-container {\n    width: 100%;\n    max-width: 450px;\n    gap: 12px;\n\n    .menu {\n      opacity: 1;\n      position: relative;\n      top: 0;\n      padding-top: 0;\n      margin-bottom: 0;\n      width: 100%;\n      max-width: 450px;\n\n      .row {\n        height: 40px;\n      }\n\n      input {\n        font-size: 14px;\n      }\n\n      textarea {\n        height: 40px;\n        --text-area-padding: 14px;\n        width: calc(100% - var(--text-area-padding) * 2);\n        padding: 0 var(--text-area-padding);\n        resize: none;\n        font-family: var(--font-family-code, $font-family-code);\n        font-size: 14px;\n        color: var(--rubin-dark-gray);\n        border-radius: 4px;\n        background-color: white;\n        border: 1px solid #F0EEE6;\n        margin: 0 0 6px 0;\n        line-height: 40px;\n\n        &::placeholder {\n          color: var(--rubin-dark-gray);\n        }\n\n        &:focus {\n          border-color: var(--rubin-gray);\n          outline: none;\n        }\n\n        opacity: 0;\n        transition: opacity 0.2s cubic-bezier(0.075, 0.82, 0.165, 1);\n        &.active {\n          opacity: 1;\n        }\n      }\n\n      .row {\n        margin-bottom: 0;\n        justify-content: flex-end;\n      }\n\n    }\n\n  }\n\n  .source-code {\n    pre {\n      padding: 10px;\n    }\n\n    p, pre {\n      font-size: 10px;\n      line-height: 1.35;\n    }\n  }\n\n}"],sourceRoot:""}]),h.locals={"artwork-container":"artwork-container",artwork:"artwork","source-code":"source-code","grid-background":"grid-background",rhombus:"rhombus",menu:"menu","artwork-instructions":"artwork-instructions",row:"row","btn-modify":"btn-modify","btn-code":"btn-code",active:"active","hljs-keyword":"hljs-keyword","hljs-built_in":"hljs-built_in","hljs-string":"hljs-string","hljs-number":"hljs-number","hljs-comment":"hljs-comment","hljs-function":"hljs-function","hljs-title":"hljs-title",function_:"function_","hljs-operator":"hljs-operator","hljs-variable":"hljs-variable","hljs-attr":"hljs-attr","hljs-params":"hljs-params"};const u=h},5692:(e,n,t)=>{e.exports=t.p+"static/media/grid.78393a84.png"},6604:(e,n,t)=>{t.d(n,{A:()=>s});var i=t(1354),a=t.n(i),r=t(6314),o=t.n(r)()(a());o.push([e.id,':root{--background-color: #FAF9F5;--rubin-gray: #87867F;--rubin-ivory: rgb(250, 249, 246);--rubin-slate: #1F1E1D;--rubin-slate: #1F1E1D;--rubin-riso: #5E7EDF;--rubin-cyan: #44A6E4;--rubin-clay: #D97757;--text-color: rgba(0, 0, 0, 0.85);--tone: #F5F2EC;--rubin-dark-gray: #5E5D59;--rubin-ivory-med: #F0EEE6;--rubin-ivory-dark: #E8E6DC;--font-family-base: goudy-old-style, serif;--font-family-code: Courier Prime, Courier, monospace;--font-size-base: 18px;--line-height-base: 1.4;--letter-spacing-base: 0.02em;--artwork-font-size: 6px;--poem-padding-top: 20px;--poem-padding-left: 24px;--poem-padding: 0px 100px;--artwork-size: 322px;--breakpoint-tablet: 768px;--nav-width: 40px;--mobile-nav-width: 62px;--mobile-menu-height: 48px}@media(min-width: 768px)and (max-width: 1199px){:root{--font-size-base: 22px;--line-height-base: 0.9;--letter-spacing-base: 0.04em;--artwork-size: 450px;--artwork-font-size: 7.45px;--poem-padding-top: 20px;--poem-padding-left: 50px;--poem-padding: 0px 100px;--artwork-font-size: 7.5px}}@media(min-width: 1200px){:root{--font-size-base: 22px;--line-height-base: 1.5;--letter-spacing-base: 0.02em;--artwork-size: 450px;--poem-padding-top: 0px;--poem-padding-left: 100px;--poem-padding: 0px 100px;--artwork-font-size: 7.45px;--font-size-base: 22px}}.app{height:100vh;width:100%;overflow-y:scroll;scroll-snap-type:y mandatory;scroll-behavior:smooth;position:relative;-ms-overflow-style:none;scrollbar-width:none;font-family:"goudy-old-style",serif;transition:opacity .2s cubic-bezier(0.075, 0.82, 0.165, 1)}.app::-webkit-scrollbar{display:none}.app *{font-family:"goudy-old-style",serif}.app.scrolling{opacity:0}.title-section,.intro-section,.signup-section,.outro-section,.acknowledgments-section{position:relative;width:100%;height:100%;align-items:center;justify-content:center;padding:var(--poem-padding-top, 0px) var(--poem-padding-left, 100px);box-sizing:border-box;scroll-snap-align:start;scroll-snap-stop:always}.title-section{position:relative;min-height:100vh;max-width:1000px;margin:0 auto;padding-top:10vh;text-align:center}.title-section h1,.title-section h2,.title-section h3{margin:0;padding:0;color:var(--rubin-slate, #1F1E1D)}.title-section #title{font-size:clamp(20px,10vh,100px);line-height:1.2;font-weight:400;letter-spacing:4vh;margin-bottom:2vh;width:100%;text-align:center;transform:translateX(2vh)}.title-section #subtitle{font-size:clamp(12px,4.5vh,80px);font-weight:400;font-style:italic;width:100%;text-align:center;letter-spacing:.025em;margin-bottom:5.5vh}.title-section .square{width:23vh;height:23vh;background-color:var(--rubin-ivory-med);margin:0 auto}.title-section #author{font-weight:500;line-height:1.5;width:100%;text-align:center;position:absolute;bottom:10vh;left:0;font-size:22px}.intro-section{position:relative;display:flex;align-items:center}.intro-section #rick-rubin{margin-top:12px;font-style:italic}.intro-section p{font-size:22px;line-height:1.4;white-space:pre-wrap;margin:0;color:#000;max-width:540px;text-align:left}.intro-section span{font-style:italic}.signup-section{position:relative;height:100%;display:flex;align-items:center;justify-content:center;flex-direction:column;gap:24px;transition:opacity 1.5s ease;transition-delay:.25s}.signup-section a{text-decoration:none}.signup-section p{font-family:var(--font-family-code, "Courier Prime", Courier, monospace);font-size:18px;color:var(--rubin-slate, #1F1E1D)}@media(max-width: 480px){.signup-section p{font-size:18px}}.signup-section .signup-text{max-width:370px;text-align:center}.signup-section .signup-widget{width:380px}.signup-section .continue-reading{margin-top:6vh;text-align:center;display:flex;align-items:center;justify-content:center;gap:12px}.signup-section .continue-reading p{font-size:14px}.signup-section .continue-reading svg{color:var(--rubin-gray, #87867F);margin-top:1px}@media(max-width: 480px){.signup-section .signup-text,.signup-section .signup-widget,.signup-section .continue-reading{width:100%}.signup-section .signup-text{max-width:300px}}.signup-section .tetragrammaton-circle{width:50px;height:50px}@media(max-width: 968px){.signup-section .tetragrammaton-circle{width:38px;height:38px}}#custom-substack-embed{margin-right:4px}#custom-substack-embed form{flex-wrap:nowrap !important}#custom-substack-embed form input{font-family:var(--font-family-code, "Courier Prime", Courier, monospace);padding:8px 12px 8px 16px}#custom-substack-embed form button{font-family:var(--font-family-code, "Courier Prime", Courier, monospace);padding:8px 20px}#custom-substack-embed{position:relative}.outro-section{display:flex;align-items:center;justify-content:center;gap:120px}.outro-section div{width:50%;max-width:550px;margin-left:0 auto}.outro-section h3{font-size:22px;font-weight:600;margin-bottom:2rem}.outro-section p{font-size:22px;line-height:1.5;color:#000}.outro-section span{font-style:normal;display:block;margin-top:40px;font-size:16px;color:var(--rubin-gray, #87867F)}.outro-section span.newsletter-link{font-family:var(--font-family-code, "Courier Prime", Courier, monospace);width:100%;font-size:16px;color:var(--rubin-slate, #1F1E1D)}@media(max-width: 1200px){.outro-section span.newsletter-link{font-size:14px}}@media(max-width: 480px){.outro-section span.newsletter-link{font-size:14px;text-align:left;line-height:1.5em}}.outro-section span.newsletter-link a{color:var(--rubin-slate, #1F1E1D);text-decoration:underline;text-underline-offset:1.5px;transition:color .2s ease;font-family:var(--font-family-code, "Courier Prime", Courier, monospace)}.outro-section span.newsletter-link a:hover{color:var(--rubin-dark-gray, #3E3D39)}.acknowledgments-section{display:flex;flex-direction:column;align-items:flex-start;justify-content:center}@media(max-width: 1200px){.acknowledgments-section{overflow-y:auto;scrollbar-width:none;justify-content:flex-start}}.acknowledgments-section h3{font-size:22px;font-weight:600;margin-bottom:2rem;margin-top:0}@media(max-width: 1024px){.acknowledgments-section h3{margin-top:10vh;font-weight:700;font-size:19px}}@media(max-width: 768px){.acknowledgments-section h3{font-size:17px}}@media(max-width: 390px){.acknowledgments-section h3{margin-top:48px}}.acknowledgments-section p{margin:0;font-size:18px;line-height:100%}.acknowledgments-section .acknowledgments{display:grid;grid-template-columns:1fr;grid-template-rows:auto;gap:80px;grid-template-areas:"references-container";width:100%}.acknowledgments-section #references-container{grid-area:references-container;width:100%;display:grid;grid-template-columns:1fr 1fr;gap:40px}@media(max-width: 1023px){.acknowledgments-section #references-container{grid-template-columns:1fr;gap:0}}.acknowledgments-section #references-container #references-left,.acknowledgments-section #references-container #references-right{max-width:600px;margin-right:48px}@media(max-width: 1024px){.acknowledgments-section #references-container #references-left,.acknowledgments-section #references-container #references-right{max-width:none}}.acknowledgments-section #references-container #references-left p,.acknowledgments-section #references-container #references-right p{line-height:130%;font-style:italic;font-size:20px;margin-bottom:8px}@media(max-width: 1024px){.acknowledgments-section #references-container #references-left p,.acknowledgments-section #references-container #references-right p{margin-bottom:12px}}@media(max-width: 1024px){.acknowledgments-section #references-container #references-left p,.acknowledgments-section #references-container #references-right p{font-size:18px}}.acknowledgments-section #references-container #references-left span,.acknowledgments-section #references-container #references-right span{font-style:normal;color:var(--rubin-gray, #87867F);font-size:19px;margin-left:4px}@media(max-width: 1024px){.acknowledgments-section #references-container #references-left span,.acknowledgments-section #references-container #references-right span{display:block;margin-left:0}}@media(max-width: 1024px){.acknowledgments-section #references-container #references-left span,.acknowledgments-section #references-container #references-right span{font-size:17px}}.acknowledgments-section #references-container .translations-note{margin-top:20px;line-height:1.3 !important;font-size:19px !important;font-style:normal !important;max-width:550px}@media(max-width: 1024px){.acknowledgments-section #references-container .translations-note{font-size:17px !important;max-width:300px}}.acknowledgments-section #contributors{grid-area:contributors;width:100%}.acknowledgments-section #contributors p{font-size:16px;line-height:150%;color:var(--rubin-dark-gray, #5E5D59)}.acknowledgments-section #contributors a{color:var(--rubin-dark-gray, #5E5D59);text-decoration-color:var(--rubin-dark-gray, #5E5D59);text-decoration-thickness:1px;text-underline-offset:1.5px;transition:color .2s ease}.acknowledgments-section #contributors a:hover{color:var(--rubin-slate, #1F1E1D);cursor:pointer}.acknowledgments-section #copyright{grid-area:copyright;width:100%}.acknowledgments-section #copyright p,.acknowledgments-section #copyright a{font-size:17px;line-height:150%;color:var(--rubin-dark-gray, #5E5D59)}.acknowledgments-section #copyright a{color:var(--rubin-dark-gray, #5E5D59);text-decoration-color:var(--rubin-dark-gray, #5E5D59);text-decoration-thickness:1px;text-underline-offset:1.5px;transition:color .2s ease}.acknowledgments-section #copyright a:hover{color:var(--rubin-slate, #1F1E1D);cursor:pointer}.acknowledgments-section .credits-container{display:flex;flex-direction:column;width:100%;margin-top:16px;gap:40px}@media(max-width: 768px){.acknowledgments-section .credits-container{margin-top:16px}}.sub-heading{position:sticky;top:95vh;width:100%;text-align:center;z-index:9;opacity:0;transform:translateY(70px);transition:opacity .6s ease,transform .8s ease,color .3s ease;font-weight:400;font-size:16px;color:var(--rubin-gray, #87867F)}.sub-heading.visible{opacity:1;transform:translateY(0)}.sub-heading p{display:inline;letter-spacing:.05em;width:fit-content}.sub-heading p:hover{cursor:pointer;color:var(--rubin-slate, #1F1E1D)}.sub-heading span{letter-spacing:normal;transform:translateY(-2px)}.nav{position:sticky;left:0;top:0;width:var(--nav-width);background-color:var(--rubin-ivory-med);z-index:10;--padding-top: 0;padding:var(--padding-top) 0;height:100vh;--icon-height: 24px;--nav-controls-height: 70px}.nav .index{height:calc(100vh - var(--nav-controls-height) - 4px);display:flex;flex-direction:column;overflow-y:scroll;scroll-behavior:smooth;width:var(--nav-width);padding-top:4px;-ms-overflow-style:none;scrollbar-width:none}.nav .index::-webkit-scrollbar{display:none}.nav .index:hover{cursor:pointer;font-weight:400;color:var(--rubin-gray, #87867F)}.nav .index div{text-align:center;padding:1px 0}.nav .index div:last-child{padding-bottom:8px}.nav .index p{font-family:var(--font-family-code, "Courier Prime", Courier, monospace);font-size:15px;color:var(--rubin-gray, #87867F);margin:0;padding:0}.nav .index p.active{font-weight:600;color:var(--rubin-slate, #1F1E1D)}.nav .nav-controls{height:var(--nav-controls-height);display:flex;flex-direction:column;justify-content:center;align-items:center;border-top:1px solid var(--rubin-gray, #87867F);background-color:var(--rubin-ivory-dark, #E8E6DC);gap:8px}.nav #nav-item-randomizer svg circle{stroke:var(--rubin-gray, #87867F);transition:all .3s ease}.nav #nav-item-randomizer:hover{cursor:pointer}.nav #nav-item-randomizer:hover svg circle{stroke:var(--rubin-slate, #1F1E1D)}.nav #nav-item-randomizer:hover svg .circle-top{transform:translateY(5.5px)}.nav #nav-item-randomizer:hover svg .circle-bottom{transform:translateY(-5.5px)}#nav-desktop{position:fixed;transition:opacity .2s ease-in;opacity:0;padding:var(--padding-top) 0 0 0}#nav-desktop #nav-item-randomizer{height:18px;transform:translateY(-2px)}#nav-desktop #nav-item-randomizer svg{color:var(--rubin-gray, #87867F)}#nav-desktop #nav-item-randomizer svg:hover{color:var(--rubin-slate, #1F1E1D)}#nav-desktop #nav-item-tetragrammaton{height:17px}#nav-desktop #nav-item-tetragrammaton svg{color:var(--rubin-gray, #87867F)}#nav-desktop #nav-item-tetragrammaton svg:hover{color:var(--rubin-slate, #1F1E1D)}#nav-desktop .index p:hover{cursor:pointer;font-weight:600;color:var(--rubin-slate, #1F1E1D)}#nav-desktop.visible{opacity:1}#nav-desktop.slide-out{opacity:0}#nav-mobile{position:fixed;left:auto;right:0;padding:var(--mobile-menu-height) 0 0 0;opacity:0;transform:translateX(80px);transition:all .3s ease;overflow-y:scroll;scrollbar-width:none;width:var(--mobile-nav-width)}#nav-mobile .index{height:calc(100vh - var(--mobile-menu-height) - 2*var(--padding-top));margin:0 auto;padding-bottom:50px;-webkit-tap-highlight-color:rgba(0,0,0,0)}@media(max-width: 1024px){#nav-mobile .index{height:88vh}}@media(max-width: 768px){#nav-mobile .index{height:82vh}}#nav-mobile .index div{margin-bottom:8px;-webkit-tap-highlight-color:rgba(0,0,0,0)}#nav-mobile .index div p.active{color:var(--rubin-slate, #1F1E1D);-webkit-tap-highlight-color:rgba(0,0,0,0)}#nav-mobile.open{opacity:1;transform:translateX(0)}#nav-mobile.open .index{border-top:1px solid var(--rubin-slate, #1F1E1D)}.mobile-menu{position:sticky;top:0;width:100%;z-index:20;padding:0}.mobile-menu .controls{display:flex;align-items:center;margin:0 24px;border-bottom:.75px solid var(--rubin-slate, #1F1E1D);height:var(--mobile-menu-height);z-index:10;background-color:var(--background-color, #FAF9F5)}.mobile-menu .randomizer{margin-right:12px;height:22px;padding-top:1px}.mobile-menu .randomizer svg{width:13px}.mobile-menu .randomizer svg circle{stroke:var(--rubin-slate, #1F1E1D)}.mobile-menu .menu-title p{margin:0;padding:0;font-size:18px;letter-spacing:.05em}.mobile-menu .menu-toggle{z-index:20;padding-top:4px}.mobile-menu .menu-toggle{margin-left:auto}.mobile-menu .bg{position:fixed;top:0;right:0;height:calc(var(--mobile-menu-height) + 1px);background-color:var(--rubin-ivory-med, #F0EEE6);width:var(--mobile-nav-width);z-index:6;transform:translateX(var(--mobile-nav-width));transition:all .3s ease;opacity:0}.mobile-menu .bg.active{transform:translateX(0px);opacity:1}@media(max-width: 1200px){.app .poem-section,.app .artwork-section{width:calc(100% - var(--nav-width));margin-left:var(--nav-width)}.acknowledgments-section{width:calc(100% - var(--nav-width));margin-left:var(--nav-width)}.outro-section div{width:70%}}@media(max-width: 1024px){.app .poem-section,.app .artwork-section{width:100%;margin-left:0}.intro-section p{font-size:20px;line-height:1.5;max-width:540px}.outro-section h3{font-size:17px;font-weight:700}.outro-section p{font-size:20px;line-height:1.5}.outro-section div{width:100%}.acknowledgments-section{margin-left:0;width:100%}.sub-heading{top:8px;width:calc(100% - 2*var(--poem-padding-left, 100px));text-align:left;padding-left:calc(2*var(--poem-padding-left, 100px));color:var(--rubin-gray, #87867F)}}@media(max-width: 820px){.title-section #title{font-size:13vw;letter-spacing:3vw;transform:translateX(1.5vw)}.title-section #subtitle{font-size:7vw;text-wrap:balance}.title-section #author{font-size:18px;bottom:15vh}.title-section .square{width:18vh;height:18vh}}.mobile-landscape-container{position:fixed;width:100%;height:100%;background-color:var(--rubin-ivory-med, #F0EEE6);z-index:100;top:0;left:0;display:flex;align-items:center;justify-content:center}.mobile-landscape-container div,.mobile-landscape-container canvas{width:300px !important;height:300px !important}@media(max-width: 590px){.intro-section div{padding-right:24px}.intro-section p{text-wrap:pretty}.intro-section #rick-rubin{margin-top:8px;font-style:italic}.acknowledgments-section h3{margin-top:48px}.acknowledgments-section .acknowledgments{padding-bottom:13vh}.outro-section{align-items:flex-start}.outro-section div{margin-top:48px}.outro-section h3{margin-top:0;margin-bottom:1.5rem}.outro-section p{line-height:1.45em;margin-block-start:.75em;margin-block-end:.75em;font-size:18px}.outro-section span{margin-top:0}}',"",{version:3,sources:["webpack://./src/client/styles/variables.scss","webpack://./src/client/App.module.scss"],names:[],mappings:"AA2DA,MAEE,2BAAA,CACA,qBAAA,CACA,iCAAA,CACA,sBAAA,CACA,sBAAA,CACA,qBAAA,CACA,qBAAA,CACA,qBAAA,CACA,iCAAA,CACA,eAAA,CACA,0BAAA,CACA,0BAAA,CACA,2BAAA,CAEA,0CAAA,CACA,qDAAA,CACA,sBAAA,CACA,uBAAA,CACA,6BAAA,CAEA,wBAAA,CACA,wBAAA,CACA,yBAAA,CACA,yBAAA,CACA,qBAAA,CAEA,0BAAA,CACA,iBAAA,CAEA,wBAAA,CACA,0BAAA,CA5CA,gDAYF,MAoCI,sBAAA,CACA,uBAAA,CACA,6BAAA,CACA,qBAAA,CACA,2BAAA,CACA,wBAAA,CACA,yBAAA,CACA,yBAAA,CACA,0BAAA,CAAA,CAlDF,0BAMF,MAgDI,sBAAA,CACA,uBAAA,CACA,6BAAA,CACA,qBAAA,CACA,uBAAA,CACA,0BAAA,CACA,yBAAA,CACA,2BAAA,CACA,sBAAA,CAAA,CClHJ,KACE,YAAA,CACA,UAAA,CACA,iBAAA,CACA,4BAAA,CACA,sBAAA,CACA,iBAAA,CAKA,uBAAA,CACA,oBAAA,CACA,mCAAA,CAKA,0DAAA,CAVA,wBACI,YAAA,CAKJ,OACI,mCAAA,CAIJ,eACE,SAAA,CAIJ,sFACE,iBAAA,CACA,UAAA,CACA,WAAA,CACA,kBAAA,CACA,sBAAA,CACA,oEAAA,CACA,qBAAA,CACA,uBAAA,CACA,uBAAA,CAGF,eACE,iBAAA,CACA,gBAAA,CACA,gBAAA,CACA,aAAA,CACA,gBAAA,CACA,iBAAA,CAEA,sDACE,QAAA,CACA,SAAA,CACA,iCAAA,CAGF,sBACE,gCAAA,CACA,eAAA,CACA,eAAA,CACA,kBAAA,CACA,iBAAA,CACA,UAAA,CACA,iBAAA,CACA,yBAAA,CAGF,yBACE,gCAAA,CACA,eAAA,CACA,iBAAA,CACA,UAAA,CACA,iBAAA,CACA,qBAAA,CACA,mBAAA,CAGF,uBACE,UAAA,CACA,WAAA,CACA,uCAAA,CACA,aAAA,CAGF,uBACE,eAAA,CACA,eAAA,CACA,UAAA,CACA,iBAAA,CACA,iBAAA,CACA,WAAA,CACA,MAAA,CACA,cAAA,CAIJ,eACE,iBAAA,CACA,YAAA,CACA,kBAAA,CAEA,2BACE,eAAA,CACA,iBAAA,CAGF,iBACE,cAAA,CACA,eAAA,CACA,oBAAA,CACA,QAAA,CACA,UAAA,CACA,eAAA,CACA,eAAA,CAGF,oBACE,iBAAA,CAKJ,gBACE,iBAAA,CACA,WAAA,CACA,YAAA,CACA,kBAAA,CACA,sBAAA,CACA,qBAAA,CACA,QAAA,CAEA,4BAAA,CACA,qBAAA,CAEA,kBACE,oBAAA,CAGF,kBACE,wEAAA,CACA,cAAA,CACA,iCAAA,CACA,yBAJF,kBAKI,cAAA,CAAA,CAIJ,6BACE,eAAA,CACA,iBAAA,CAGF,+BACE,WAAA,CAGF,kCACE,cAAA,CACA,iBAAA,CACA,YAAA,CACA,kBAAA,CACA,sBAAA,CACA,QAAA,CACA,oCACE,cAAA,CAEF,sCACE,gCAAA,CACA,cAAA,CAIJ,yBACE,8FACE,UAAA,CAEF,6BACE,eAAA,CAAA,CAIJ,uCACE,UAAA,CACA,WAAA,CAEA,yBAJF,uCAKI,UAAA,CACA,WAAA,CAAA,CAKN,uBAYE,gBAAA,CAXA,4BACE,2BAAA,CACA,kCACE,wEAAA,CACA,yBAAA,CAEF,mCACE,wEAAA,CACA,gBAAA,CAMN,uBACE,iBAAA,CAGF,eACE,YAAA,CACA,kBAAA,CACA,sBAAA,CACA,SAAA,CAEA,mBACE,SAAA,CACA,eAAA,CACA,kBAAA,CAGF,kBACE,cAAA,CACA,eAAA,CACA,kBAAA,CAGF,iBACE,cAAA,CACA,eAAA,CACA,UAAA,CAGF,oBACE,iBAAA,CACA,aAAA,CACA,eAAA,CACA,cAAA,CACA,gCAAA,CAEA,oCACE,wEAAA,CAEA,UAAA,CAEA,cAAA,CACA,iCAAA,CAEA,0BARF,oCASI,cAAA,CAAA,CAGF,yBAZF,oCAaI,cAAA,CACA,eAAA,CACA,iBAAA,CAAA,CAIF,sCACE,iCAAA,CACA,yBAAA,CACA,2BAAA,CACA,yBAAA,CACA,wEAAA,CAEA,4CACE,qCAAA,CAOV,yBAEE,YAAA,CACA,qBAAA,CACA,sBAAA,CACA,sBAAA,CAEA,0BAPF,yBAQI,eAAA,CACA,oBAAA,CACA,0BAAA,CAAA,CAGF,4BACE,cAAA,CACA,eAAA,CACA,kBAAA,CACA,YAAA,CAEA,0BANF,4BAOI,eAAA,CACA,eAAA,CACA,cAAA,CAAA,CAGF,yBAZF,4BAaI,cAAA,CAAA,CAGF,yBAhBF,4BAiBI,eAAA,CAAA,CAIJ,2BACE,QAAA,CACA,cAAA,CACA,gBAAA,CAGF,0CACE,YAAA,CACA,yBAAA,CACA,uBAAA,CACA,QAAA,CACA,0CACE,CACF,UAAA,CAGF,+CACE,8BAAA,CACA,UAAA,CACA,YAAA,CACA,6BAAA,CACA,QAAA,CAGA,0BARF,+CASI,yBAAA,CACA,KAAA,CAAA,CAGF,iIACE,eAAA,CACA,iBAAA,CAEA,0BAJF,iIAKI,cAAA,CAAA,CAGF,qIACE,gBAAA,CACA,iBAAA,CACA,cAAA,CACA,iBAAA,CAEA,0BANF,qIAOI,kBAAA,CAAA,CAGF,0BAVF,qIAWI,cAAA,CAAA,CAIJ,2IACE,iBAAA,CACA,gCAAA,CACA,cAAA,CACA,eAAA,CAEA,0BANF,2IAOI,aAAA,CACA,aAAA,CAAA,CAGF,0BAXF,2IAYI,cAAA,CAAA,CAKN,kEACE,eAAA,CACA,0BAAA,CACA,yBAAA,CACA,4BAAA,CACA,eAAA,CAEA,0BAPF,kEAQI,yBAAA,CACA,eAAA,CAAA,CAKN,uCACE,sBAAA,CACA,UAAA,CACA,yCACE,cAAA,CACA,gBAAA,CACA,qCAAA,CAGF,yCACE,qCAAA,CACA,qDAAA,CACA,6BAAA,CACA,2BAAA,CACA,yBAAA,CAEA,+CACE,iCAAA,CACA,cAAA,CAKN,oCACE,mBAAA,CACA,UAAA,CACA,4EACE,cAAA,CACA,gBAAA,CACA,qCAAA,CAGF,sCACE,qCAAA,CACA,qDAAA,CACA,6BAAA,CACA,2BAAA,CACA,yBAAA,CAEA,4CACE,iCAAA,CACA,cAAA,CAKN,4CACE,YAAA,CACA,qBAAA,CACA,UAAA,CACA,eAAA,CACA,QAAA,CAEA,yBAPF,4CAQI,eAAA,CAAA,CAMN,aACE,eAAA,CACA,QAAA,CACA,UAAA,CACA,iBAAA,CACA,SAAA,CACA,SAAA,CACA,0BAAA,CACA,6DAAA,CACA,eAAA,CACA,cAAA,CACA,gCAAA,CAEA,qBACE,SAAA,CACA,uBAAA,CAGF,eACE,cAAA,CACA,oBAAA,CACA,iBAAA,CAEA,qBACE,cAAA,CACA,iCAAA,CAMJ,kBAGE,qBAAA,CACA,0BAAA,CAKJ,KACE,eAAA,CACA,MAAA,CACA,KAAA,CACA,sBAAA,CACA,uCAAA,CACA,UAAA,CACA,gBAAA,CACA,4BAAA,CACA,YAAA,CAEA,mBAAA,CACA,2BAAA,CAEA,YACE,qDAAA,CACA,YAAA,CACA,qBAAA,CACA,iBAAA,CACA,sBAAA,CACA,sBAAA,CACA,eAAA,CAKA,uBAAA,CACA,oBAAA,CAJA,+BACE,YAAA,CAKF,kBACE,cAAA,CACA,eAAA,CACA,gCAAA,CAGF,gBACE,iBAAA,CACA,aAAA,CACA,2BACE,kBAAA,CAIJ,cACE,wEAAA,CACA,cAAA,CACA,gCAAA,CACA,QAAA,CACA,SAAA,CAGF,qBACE,eAAA,CACA,iCAAA,CAIJ,mBACE,iCAAA,CACA,YAAA,CACA,qBAAA,CACA,sBAAA,CACA,kBAAA,CACA,+CAAA,CACA,iDAAA,CACA,OAAA,CAKE,qCACE,iCAAA,CACA,uBAAA,CAIJ,gCACE,cAAA,CAEE,2CACE,kCAAA,CAEF,gDACE,2BAAA,CAEF,mDACE,4BAAA,CAQV,aACE,cAAA,CACA,8BAAA,CACA,SAAA,CAEA,gCAAA,CAEA,kCACE,WAAA,CACA,0BAAA,CAEA,sCACE,gCAAA,CACA,4CACE,iCAAA,CAKN,sCACE,WAAA,CACA,0CACE,gCAAA,CACA,gDACE,iCAAA,CAOF,4BACE,cAAA,CACA,eAAA,CACA,iCAAA,CAKN,qBACE,SAAA,CAGF,uBACE,SAAA,CAMJ,YACE,cAAA,CACA,SAAA,CACA,OAAA,CACA,uCAAA,CACA,SAAA,CACA,0BAAA,CACA,uBAAA,CACA,iBAAA,CACA,oBAAA,CAEA,6BAAA,CAEA,mBACE,qEAAA,CAUA,aAAA,CACA,mBAAA,CASA,yCAAA,CAlBA,0BAHF,mBAII,WAAA,CAAA,CAGF,yBAPF,mBAQI,WAAA,CAAA,CAKF,uBACE,iBAAA,CAKA,yCAAA,CAJA,gCACE,iCAAA,CACA,yCAAA,CAON,iBACE,SAAA,CACA,uBAAA,CAEA,wBACE,gDAAA,CAKN,aACE,eAAA,CACA,KAAA,CACA,UAAA,CACA,UAAA,CACA,SAAA,CAEA,uBACE,YAAA,CACA,kBAAA,CACA,aAAA,CACA,qDAAA,CACA,gCAAA,CACA,UAAA,CACA,iDAAA,CAGF,yBACE,iBAAA,CACA,WAAA,CACA,eAAA,CACA,6BACE,UAAA,CACA,oCACE,kCAAA,CAMJ,2BACE,QAAA,CACA,SAAA,CACA,cAAA,CACA,oBAAA,CAIJ,0BACE,UAAA,CACA,eAAA,CAGF,0BACE,gBAAA,CAGF,iBACE,cAAA,CACA,KAAA,CACA,OAAA,CACA,4CAAA,CACA,gDAAA,CACA,6BAAA,CACA,SAAA,CACA,6CAAA,CACA,uBAAA,CACA,SAAA,CAEA,wBACE,yBAAA,CACA,SAAA,CAON,0BAII,yCACE,mCAAA,CACA,4BAAA,CAIJ,yBACE,mCAAA,CACA,4BAAA,CAIA,mBACE,SAAA,CAAA,CAMN,0BAEI,yCACE,UAAA,CACA,aAAA,CAKF,iBACE,cAAA,CACA,eAAA,CACA,eAAA,CAKF,kBACE,cAAA,CACA,eAAA,CAGF,iBACE,cAAA,CACA,eAAA,CAGF,mBACE,UAAA,CAIJ,yBACE,aAAA,CACA,UAAA,CAGF,aACE,OAAA,CACA,oDAAA,CACA,eAAA,CACA,oDAAA,CACA,gCAAA,CAAA,CAKJ,yBAEI,sBACE,cAAA,CACA,kBAAA,CACA,2BAAA,CAGF,yBACE,aAAA,CACA,iBAAA,CAGF,uBACE,cAAA,CACA,WAAA,CAGF,uBACE,UAAA,CACA,WAAA,CAAA,CAKN,4BACE,cAAA,CACA,UAAA,CACA,WAAA,CACA,gDAAA,CACA,WAAA,CACA,KAAA,CACA,MAAA,CACA,YAAA,CACA,kBAAA,CACA,sBAAA,CAEA,mEACE,sBAAA,CACA,uBAAA,CAIJ,yBAII,mBACE,kBAAA,CAGF,iBACE,gBAAA,CAGF,2BACE,cAAA,CACA,iBAAA,CAKJ,4BACE,eAAA,CAGF,0CACE,mBAAA,CAGF,eACE,sBAAA,CAEA,mBACE,eAAA,CAGF,kBACE,YAAA,CACA,oBAAA,CAGF,iBACE,kBAAA,CACA,wBAAA,CACA,sBAAA,CACA,cAAA,CAGF,oBACE,YAAA,CAAA",sourcesContent:["// Breakpoints\n$breakpoint-mobile: 480px;\n$breakpoint-tablet: 768px;\n$breakpoint-desktop: 1200px;\n\n// Colors\n$background-color: #FAF9F5;\n$text-color: rgba(0, 0, 0, 0.85);\n$rubin-gray: #87867F;\n$rubin-slate: #1F1E1D;\n$rubin-dark-gray: #5E5D59;\n$rubin-ivory-med: #F0EEE6;\n$rubin-ivory-dark: #E8E6DC;\n$rubin-ivory: rgb(250, 249, 246);\n$rubin-riso: #5E7EDF;\n$rubin-cyan: #44A6E4;\n$rubin-clay: #D97757;\n$rubin-beige: #E8E6DC;\n$tone: #F5F2EC;\n\n// Typography\n$font-family-base: \"goudy-old-style\", serif;\n$font-family-code: 'Courier Prime', Courier, monospace;\n$font-size-base: 22px;\n$line-height-base: 1.5;\n$letter-spacing-base: 0.02em;\n\n// Padding\n$poem-padding-top: 0px;\n$poem-padding-left: 100px;\n$poem-padding: $poem-padding-top $poem-padding-left;\n\n// Sizes\n$artwork-size: 450px;\n$artwork-font-size: 7.45px;\n$nav-width: 40px;\n$mobile-nav-width: 62px;\n$mobile-menu-height: 48px;\n\n// Responsive mixins\n@mixin mobile {\n  @media (max-width: #{$breakpoint-tablet - 1px}) {\n    @content;\n  }\n}\n\n@mixin tablet {\n  @media (min-width: #{$breakpoint-tablet}) and (max-width: #{$breakpoint-desktop - 1px}) {\n    @content;\n  }\n}\n\n@mixin desktop {\n  @media (min-width: #{$breakpoint-desktop}) {\n    @content;\n  }\n}\n\n// Responsive variables\n:root {\n  // Base values (mobile first)\n  --background-color: #{$background-color};\n  --rubin-gray: #{$rubin-gray};\n  --rubin-ivory: #{$rubin-ivory};\n  --rubin-slate: #{$rubin-slate};\n  --rubin-slate: #{$rubin-slate};\n  --rubin-riso: #{$rubin-riso};\n  --rubin-cyan: #{$rubin-cyan};\n  --rubin-clay: #{$rubin-clay};\n  --text-color: #{$text-color};\n  --tone: #{$tone};\n  --rubin-dark-gray: #{$rubin-dark-gray};\n  --rubin-ivory-med: #{$rubin-ivory-med};\n  --rubin-ivory-dark: #{$rubin-ivory-dark};\n\n  --font-family-base: #{$font-family-base};\n  --font-family-code: #{$font-family-code};\n  --font-size-base: 18px;\n  --line-height-base: 1.4; // Slightly looser on mobile\n  --letter-spacing-base: 0.02em;\n\n  --artwork-font-size: 6px;\n  --poem-padding-top: 20px;\n  --poem-padding-left: 24px;\n  --poem-padding: #{$poem-padding-top} #{$poem-padding-left};\n  --artwork-size: 322px;\n\n  --breakpoint-tablet: #{$breakpoint-tablet};\n  --nav-width: #{$nav-width};\n\n  --mobile-nav-width: #{$mobile-nav-width};\n  --mobile-menu-height: #{$mobile-menu-height};\n\n  \n  @include tablet {\n    --font-size-base: 22px;\n    --line-height-base: 0.9;\n    --letter-spacing-base: 0.04em;\n    --artwork-size: 450px;\n    --artwork-font-size: 7.45px;\n    --poem-padding-top: 20px;\n    --poem-padding-left: 50px;\n    --poem-padding: #{$poem-padding-top} #{$poem-padding-left};\n    --artwork-font-size: 7.5px;\n  }\n  \n  @include desktop {\n    --font-size-base: #{$font-size-base};\n    --line-height-base: #{$line-height-base};\n    --letter-spacing-base: #{$letter-spacing-base};\n    --artwork-size: #{$artwork-size};\n    --poem-padding-top: #{$poem-padding-top};\n    --poem-padding-left: #{$poem-padding-left};\n    --poem-padding: #{$poem-padding-top} #{$poem-padding-left};\n    --artwork-font-size: #{$artwork-font-size};\n    --font-size-base: #{$font-size-base};\n  }\n}\n\n",'@import "/vercel/path0/src/client/styles/variables.scss";\n.app {\n  height: 100vh;\n  width: 100%;\n  overflow-y: scroll;\n  scroll-snap-type: y mandatory;\n  scroll-behavior: smooth;\n  position: relative;\n\n  &::-webkit-scrollbar {\n      display: none;\n  }\n  -ms-overflow-style: none;  /* IE and Edge */\n  scrollbar-width: none;  /* Firefox */\n  font-family: "goudy-old-style", serif;\n  * {\n      font-family: "goudy-old-style", serif;\n  }\n\n  transition: opacity .2s cubic-bezier(0.075, 0.82, 0.165, 1);\n  &.scrolling {\n    opacity: 0;\n  }\n}\n\n.title-section, .intro-section, .signup-section, .outro-section, .acknowledgments-section {\n  position: relative;\n  width: 100%;\n  height: 100%;\n  align-items: center;\n  justify-content: center;\n  padding: var(--poem-padding-top, $poem-padding-top) var(--poem-padding-left, $poem-padding-left);\n  box-sizing: border-box;\n  scroll-snap-align: start;\n  scroll-snap-stop: always;\n}\n\n.title-section {\n  position: relative;\n  min-height: 100vh;\n  max-width: 1000px;\n  margin: 0 auto;\n  padding-top: 10vh;\n  text-align: center;\n  \n  h1, h2, h3 {\n    margin: 0;\n    padding: 0;\n    color: var(--rubin-slate, $rubin-slate);\n  }\n\n  #title {\n    font-size: clamp(20px, 10vh, 100px);\n    line-height: 1.2;\n    font-weight: 400;\n    letter-spacing: 4vh;\n    margin-bottom: 2vh;\n    width: 100%;\n    text-align: center;\n    transform: translateX(2vh);\n  }\n\n  #subtitle {\n    font-size: clamp(12px, 4.5vh, 80px);\n    font-weight: 400;\n    font-style: italic;\n    width: 100%;\n    text-align: center;\n    letter-spacing: 0.025em;\n    margin-bottom: 5.5vh;\n  }\n\n  .square {\n    width: 23vh;\n    height: 23vh;\n    background-color: var(--rubin-ivory-med);\n    margin: 0 auto;\n  }\n\n  #author {\n    font-weight: 500;\n    line-height: 1.5;\n    width: 100%;\n    text-align: center;\n    position: absolute;\n    bottom: 10vh;\n    left: 0;\n    font-size: 22px;\n  }\n}\n\n.intro-section {\n  position: relative;\n  display: flex;\n  align-items: center;\n\n  #rick-rubin {\n    margin-top: 12px;\n    font-style: italic;\n  }\n\n  p {\n    font-size: 22px;\n    line-height: 1.4;\n    white-space: pre-wrap;\n    margin: 0;\n    color: black;\n    max-width: 540px;\n    text-align: left;\n  }\n\n  span {\n    font-style: italic;\n  }\n\n}\n\n.signup-section {\n  position: relative;\n  height: 100%;\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  flex-direction: column;\n  gap: 24px;\n\n  transition: opacity 1.5s ease;\n  transition-delay: .25s;\n\n  a {\n    text-decoration: none;\n  }\n\n  p {\n    font-family: var(--font-family-code, $font-family-code);\n    font-size: 18px;\n    color: var(--rubin-slate, $rubin-slate);\n    @media (max-width: 480px) {\n      font-size: 18px;\n    }\n  }\n\n  .signup-text {\n    max-width: 370px;\n    text-align: center;\n  }\n\n  .signup-widget{\n    width: 380px;\n  }\n\n  .continue-reading {\n    margin-top: 6vh;\n    text-align: center;\n    display: flex;\n    align-items: center;\n    justify-content: center;\n    gap: 12px;\n    p {\n      font-size: 14px;\n    }\n    svg {\n      color: var(--rubin-gray, $rubin-gray);\n      margin-top: 1px;\n    }\n  }\n\n  @media (max-width: 480px) {\n    .signup-text, .signup-widget, .continue-reading {\n      width: 100%;\n    }\n    .signup-text {\n      max-width: 300px;\n    }\n  }\n\n  .tetragrammaton-circle {\n    width: 50px;\n    height: 50px;\n\n    @media (max-width: 968px) {\n      width: 38px;\n      height: 38px;\n    }\n  }\n}\n\n#custom-substack-embed {\n  form {\n    flex-wrap: nowrap !important;\n    input {\n      font-family: var(--font-family-code, $font-family-code);\n      padding: 8px 12px 8px 16px;\n    }\n    button {\n      font-family: var(--font-family-code, $font-family-code);\n      padding: 8px 20px;\n    }\n  }\n  margin-right: 4px;\n}\n\n#custom-substack-embed {\n  position: relative;\n}\n\n.outro-section {\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  gap: 120px;\n\n  div {\n    width: 50%;\n    max-width: 550px;\n    margin-left: 0 auto;\n  }\n\n  h3 {\n    font-size: 22px;\n    font-weight: 600;\n    margin-bottom: 2rem;\n  }\n\n  p {\n    font-size: 22px;\n    line-height: 1.5;\n    color: #000;\n  }\n\n  span {\n    font-style: normal;\n    display: block;\n    margin-top: 40px;\n    font-size: 16px;\n    color: var(--rubin-gray, #87867F);\n    \n    &.newsletter-link {\n      font-family: var(--font-family-code, $font-family-code);\n      \n      width: 100%;\n\n      font-size: 16px;\n      color: var(--rubin-slate, $rubin-slate);\n    \n      @media (max-width: 1200px) {\n        font-size: 14px;\n      }\n\n      @media (max-width: 480px) {\n        font-size: 14px;\n        text-align: left;\n        line-height: 1.5em;\n      }\n\n      \n      a {\n        color: var(--rubin-slate, $rubin-slate);\n        text-decoration: underline;\n        text-underline-offset: 1.5px;\n        transition: color 0.2s ease;\n        font-family: var(--font-family-code, $font-family-code);\n        \n        &:hover {\n          color: var(--rubin-dark-gray, #3E3D39);\n        }\n      }\n    }\n  }\n}\n\n.acknowledgments-section {\n\n  display: flex;\n  flex-direction: column;\n  align-items: flex-start;\n  justify-content: center;\n\n  @media (max-width: 1200px) {\n    overflow-y: auto;\n    scrollbar-width: none;\n    justify-content: flex-start;\n  }\n\n  h3 {\n    font-size: 22px;\n    font-weight: 600;\n    margin-bottom: 2rem;\n    margin-top: 0;\n    \n    @media (max-width: 1024px) {\n      margin-top: 10vh;\n      font-weight: 700;\n      font-size: 19px;\n    }\n    \n    @media (max-width: 768px) {\n      font-size: 17px;\n    }\n\n    @media (max-width: 390px) {\n      margin-top: 48px;\n    }\n  }\n  \n  p {\n    margin: 0;\n    font-size: 18px;\n    line-height: 100%;\n  }\n  \n  .acknowledgments {\n    display: grid;\n    grid-template-columns: 1fr;\n    grid-template-rows: auto;\n    gap: 80px;\n    grid-template-areas: \n      "references-container";\n    width: 100%;\n  }\n\n  #references-container {\n    grid-area: references-container;\n    width: 100%;\n    display: grid;\n    grid-template-columns: 1fr 1fr;\n    gap: 40px;\n    \n    /* Between tablet and 1200px, change to 1 full-width column */\n    @media (max-width: 1023px) {\n      grid-template-columns: 1fr;\n      gap: 0;\n    }\n    \n    #references-left, #references-right {\n      max-width: 600px;\n      margin-right: 48px;\n      \n      @media (max-width: 1024px) {\n        max-width: none;\n      }\n      \n      p {\n        line-height: 130%;\n        font-style: italic;\n        font-size: 20px;\n        margin-bottom: 8px;\n        \n        @media (max-width: 1024px) {\n          margin-bottom: 12px;\n        }\n        \n        @media (max-width: 1024px) {\n          font-size: 18px;\n        }\n      }\n      \n      span {\n        font-style: normal;\n        color: var(--rubin-gray, $rubin-gray);\n        font-size: 19px;\n        margin-left: 4px;\n        \n        @media (max-width: 1024px) {\n          display: block;\n          margin-left: 0;\n        }\n        \n        @media (max-width: 1024px) {\n          font-size: 17px;\n        }\n      }\n    }\n    \n    .translations-note {\n      margin-top: 20px;\n      line-height: 1.3 !important;\n      font-size: 19px !important;\n      font-style: normal !important;\n      max-width: 550px;\n      \n      @media (max-width: 1024px) {\n        font-size: 17px !important;\n        max-width: 300px;\n      }\n    }\n  }\n\n  #contributors {\n    grid-area: contributors;\n    width: 100%;\n    p {\n      font-size: 16px;\n      line-height: 150%;\n      color: var(--rubin-dark-gray, $rubin-dark-gray);\n    }\n    \n    a {\n      color: var(--rubin-dark-gray, $rubin-dark-gray);\n      text-decoration-color: var(--rubin-dark-gray, $rubin-dark-gray);\n      text-decoration-thickness: 1px;\n      text-underline-offset: 1.5px;\n      transition: color 0.2s ease;\n      \n      &:hover {\n        color: var(--rubin-slate, $rubin-slate);\n        cursor: pointer;\n      }\n    }\n  }\n\n  #copyright {\n    grid-area: copyright;\n    width: 100%;\n    p, a {\n      font-size: 17px;\n      line-height: 150%;\n      color: var(--rubin-dark-gray, $rubin-dark-gray);\n    }\n    \n    a {\n      color: var(--rubin-dark-gray, $rubin-dark-gray);\n      text-decoration-color: var(--rubin-dark-gray, $rubin-dark-gray);\n      text-decoration-thickness: 1px;\n      text-underline-offset: 1.5px;\n      transition: color 0.2s ease;\n      \n      &:hover {\n        color: var(--rubin-slate, $rubin-slate);\n        cursor: pointer;\n      }\n    }\n  }\n  \n  .credits-container {\n    display: flex;\n    flex-direction: column;\n    width: 100%;\n    margin-top: 16px;\n    gap: 40px;\n    \n    @media (max-width: 768px) {\n      margin-top: 16px;\n    }\n  }\n\n}\n\n.sub-heading {\n  position: sticky;\n  top: 95vh;\n  width: 100%;\n  text-align: center;\n  z-index: 9;\n  opacity: 0;\n  transform: translateY(70px);\n  transition: opacity .6s ease, transform .8s ease, color .3s ease;\n  font-weight: 400;\n  font-size: 16px;\n  color: var(--rubin-gray, $rubin-gray);\n  \n  &.visible {\n    opacity: 1;\n    transform: translateY(0);\n  }\n\n  p {\n    display: inline;\n    letter-spacing: .05em;\n    width: fit-content;\n\n    &:hover {\n      cursor: pointer;\n      color: var(--rubin-slate, $rubin-slate);\n    }\n    // position: relative;\n\n  }\n\n  span {\n    // font-family: var(--font-family-code, $font-family-code);\n    // position: absolute;\n    letter-spacing: normal;\n    transform: translateY(-2px);\n  }\n\n}\n\n.nav {\n  position: sticky;\n  left: 0;\n  top: 0;\n  width: var(--nav-width);\n  background-color: var(--rubin-ivory-med);\n  z-index: 10;\n  --padding-top: 0;\n  padding: var(--padding-top) 0;\n  height: 100vh;\n\n  --icon-height: 24px;\n  --nav-controls-height: 70px;\n\n  .index {\n    height: calc(100vh - var(--nav-controls-height) - 4px);\n    display: flex;\n    flex-direction: column;\n    overflow-y: scroll; \n    scroll-behavior: smooth;\n    width: var(--nav-width);\n    padding-top: 4px;\n\n    &::-webkit-scrollbar {\n      display: none;\n    }\n    -ms-overflow-style: none;  /* IE and Edge */\n    scrollbar-width: none;  /* Firefox */\n  \n    &:hover {\n      cursor: pointer;\n      font-weight: 400;\n      color: var(--rubin-gray, $rubin-gray);  \n    }\n  \n    div {\n      text-align: center;\n      padding: 1px 0;\n      &:last-child {\n        padding-bottom: 8px;\n      }\n    }\n  \n    p {\n      font-family: var(--font-family-code, $font-family-code);\n      font-size: 15px;\n      color: var(--rubin-gray, $rubin-gray);\n      margin: 0;\n      padding: 0;\n    }\n  \n    p.active {\n      font-weight: 600;\n      color: var(--rubin-slate, $rubin-slate);\n    }\n  }\n\n  .nav-controls {\n    height: var(--nav-controls-height);\n    display: flex;\n    flex-direction: column;\n    justify-content: center;\n    align-items: center;\n    border-top: 1px solid var(--rubin-gray, $rubin-gray); \n    background-color: var(--rubin-ivory-dark, $rubin-ivory-dark);\n    gap: 8px;\n  }\n\n  #nav-item-randomizer {\n    svg {\n      circle {\n        stroke: var(--rubin-gray, $rubin-gray);\n        transition: all 0.3s ease;\n      }\n    }\n\n    &:hover {\n      cursor: pointer;\n      svg {\n        circle {\n          stroke: var(--rubin-slate, $rubin-slate);\n        }\n        .circle-top {\n          transform: translateY(5.5px);\n        }\n        .circle-bottom {\n          transform: translateY(-5.5px);\n        }\n      }\n    }\n  }\n  \n}\n\n#nav-desktop {\n  position: fixed;\n  transition: opacity 0.2s ease-in;\n  opacity: 0;\n\n  padding: var(--padding-top) 0 0 0;\n\n  #nav-item-randomizer {\n    height: 18px;\n    transform: translateY(-2px);\n\n    svg {\n      color: var(--rubin-gray, $rubin-gray);\n      &:hover {\n        color: var(--rubin-slate, $rubin-slate);\n      }\n    }\n  }\n\n  #nav-item-tetragrammaton {\n    height: 17px;\n    svg {\n      color: var(--rubin-gray, $rubin-gray);\n      &:hover {\n        color: var(--rubin-slate, $rubin-slate);\n      }\n    }\n  }\n\n  .index {\n    p {\n      &:hover {\n        cursor: pointer;\n        font-weight: 600;\n        color: var(--rubin-slate, $rubin-slate);\n      }\n    }\n  }\n\n  &.visible {\n    opacity: 1;\n  }\n\n  &.slide-out {\n    opacity: 0;\n  }\n\n\n}\n\n#nav-mobile {\n  position: fixed;\n  left: auto;\n  right: 0;\n  padding: var(--mobile-menu-height) 0 0 0;\n  opacity: 0;\n  transform: translateX(80px);\n  transition: all 0.3s ease;\n  overflow-y: scroll;\n  scrollbar-width: none;\n\n  width: var(--mobile-nav-width);\n\n  .index {\n    height: calc(100vh - var(--mobile-menu-height) - (2 * var(--padding-top)));\n\n    @media (max-width: 1024px) {\n      height: 88vh;\n    }\n\n    @media (max-width: 768px) {\n      height: 82vh;\n    }\n    \n    margin: 0 auto;\n    padding-bottom: 50px;\n    div {\n      margin-bottom: 8px;\n      p.active {\n        color: var(--rubin-slate, $rubin-slate);\n        -webkit-tap-highlight-color: transparent;\n      }\n      -webkit-tap-highlight-color: transparent;\n    }\n    -webkit-tap-highlight-color: transparent;\n  }\n\n  &.open {\n    opacity: 1;\n    transform: translateX(0);\n\n    .index {\n      border-top: 1px solid var(--rubin-slate, #1F1E1D);\n    }\n  }\n}\n\n.mobile-menu {\n  position: sticky;\n  top: 0;\n  width: 100%;\n  z-index: 20;\n  padding: 0;\n\n  .controls {\n    display: flex;\n    align-items: center;\n    margin: 0 24px;\n    border-bottom: .75px solid var(--rubin-slate, $rubin-slate);\n    height: var(--mobile-menu-height);\n    z-index: 10;\n    background-color: var(--background-color, $background-color);\n  }\n\n  .randomizer {\n    margin-right: 12px;\n    height: 22px;\n    padding-top: 1px;\n    svg {\n      width: 13px;\n      circle {\n        stroke: var(--rubin-slate, $rubin-slate);\n      }\n    }\n  }\n\n  .menu-title {\n    p {\n      margin: 0;\n      padding: 0;\n      font-size: 18px;\n      letter-spacing: 0.05em;\n    }\n  }\n\n  .menu-toggle {\n    z-index: 20;\n    padding-top: 4px;\n  }\n\n  .menu-toggle {\n    margin-left: auto\n  }\n\n  .bg {\n    position: fixed;\n    top: 0;\n    right: 0;\n    height: calc(var(--mobile-menu-height) + 1px);\n    background-color: var(--rubin-ivory-med, $rubin-ivory-med);\n    width: var(--mobile-nav-width);\n    z-index: 6;\n    transform: translateX(var(--mobile-nav-width));\n    transition: all 0.3s ease;\n    opacity: 0;\n\n    &.active {\n      transform: translateX(0px);\n      opacity: 1;\n    }\n  }\n\n}\n\n\n@media (max-width: 1200px) {\n  /* This is tiny desktop styles, not yet tablet */\n  \n  .app {\n    .poem-section, .artwork-section {\n      width: calc(100% - var(--nav-width));\n      margin-left: var(--nav-width);\n    }\n  }\n\n  .acknowledgments-section {\n    width: calc(100% - var(--nav-width));\n    margin-left: var(--nav-width);\n  }\n\n  .outro-section {\n    div {\n      width: 70%;\n    }\n  }\n}\n\n\n@media (max-width: 1024px) {\n  .app {\n    .poem-section, .artwork-section {\n      width: 100%;\n      margin-left: 0;\n    }\n  }\n\n  .intro-section {\n    p {\n      font-size: 20px;\n      line-height: 1.5;\n      max-width: 540px;\n    }\n  }\n\n  .outro-section {\n    h3 {\n      font-size: 17px;\n      font-weight: 700;\n    }\n\n    p {\n      font-size: 20px;\n      line-height: 1.5;\n    }\n\n    div {\n      width: 100%;\n    }\n  }\n\n  .acknowledgments-section {\n    margin-left: 0;\n    width: 100%;\n  }\n\n  .sub-heading {\n    top: 8px;\n    width: calc(100% - 2 * var(--poem-padding-left, $poem-padding-left));\n    text-align: left;\n    padding-left: calc( 2 * var(--poem-padding-left, $poem-padding-left) );\n    color: var(--rubin-gray, $rubin-gray);\n  }\n}\n\n/* Separate mobile styles just for title section */\n@media (max-width: 820px) {\n  .title-section {\n    #title {\n      font-size: 13vw;\n      letter-spacing: 3vw;\n      transform: translateX(1.5vw);\n    }\n\n    #subtitle {\n      font-size: 7vw;\n      text-wrap: balance;\n    }\n    \n    #author {\n      font-size: 18px;\n      bottom: 15vh;\n    }\n\n    .square {\n      width: 18vh;\n      height: 18vh;\n    }\n  }\n}\n\n.mobile-landscape-container {\n  position: fixed;\n  width: 100%;\n  height: 100%;\n  background-color: var(--rubin-ivory-med, $rubin-ivory-med);\n  z-index: 100;\n  top: 0;\n  left: 0;\n  display: flex;\n  align-items: center;\n  justify-content: center;\n\n  div, canvas {\n    width: 300px !important;\n    height: 300px !important;\n  }\n}\n\n@media (max-width: 590px) {\n\n\n  .intro-section {\n    div {\n      padding-right: 24px;\n    }\n\n    p {\n      text-wrap: pretty;\n    }\n\n    #rick-rubin {\n      margin-top: 8px;\n      font-style: italic;\n    }\n\n  }\n\n  .acknowledgments-section h3 {\n    margin-top: 48px;\n  }\n\n  .acknowledgments-section .acknowledgments {\n    padding-bottom: 13vh;\n  }\n\n  .outro-section {\n    align-items: flex-start;\n\n    div {\n      margin-top: 48px;\n    }\n\n    h3 {\n      margin-top: 0;\n      margin-bottom: 1.5rem;\n    }\n\n    p {\n      line-height: 1.45em;\n      margin-block-start: .75em;\n      margin-block-end: .75em;\n      font-size: 18px;\n    }\n\n    span {\n      margin-top: 0;\n    }\n  }\n  \n}'],sourceRoot:""}]),o.locals={app:"app",scrolling:"scrolling","title-section":"title-section","intro-section":"intro-section","signup-section":"signup-section","outro-section":"outro-section","acknowledgments-section":"acknowledgments-section",title:"title",subtitle:"subtitle",square:"square",author:"author","rick-rubin":"rick-rubin","signup-text":"signup-text","signup-widget":"signup-widget","continue-reading":"continue-reading","tetragrammaton-circle":"tetragrammaton-circle","custom-substack-embed":"custom-substack-embed","newsletter-link":"newsletter-link",acknowledgments:"acknowledgments","references-container":"references-container","references-left":"references-left","references-right":"references-right","translations-note":"translations-note",contributors:"contributors",copyright:"copyright","credits-container":"credits-container","sub-heading":"sub-heading",visible:"visible",nav:"nav",index:"index",active:"active","nav-controls":"nav-controls","nav-item-randomizer":"nav-item-randomizer","circle-top":"circle-top","circle-bottom":"circle-bottom","nav-desktop":"nav-desktop","nav-item-tetragrammaton":"nav-item-tetragrammaton","slide-out":"slide-out","nav-mobile":"nav-mobile",open:"open","mobile-menu":"mobile-menu",controls:"controls",randomizer:"randomizer","menu-title":"menu-title","menu-toggle":"menu-toggle",bg:"bg","poem-section":"poem-section","artwork-section":"artwork-section","mobile-landscape-container":"mobile-landscape-container"};const s=o}},t={};function i(e){var a=t[e];if(void 0!==a)return a.exports;var r=t[e]={id:e,exports:{}};return n[e].call(r.exports,r,r.exports,i),r.exports}i.m=n,e=[],i.O=(n,t,a,r)=>{if(!t){var o=1/0;for(h=0;h<e.length;h++){t=e[h][0],a=e[h][1],r=e[h][2];for(var s=!0,c=0;c<t.length;c++)(!1&r||o>=r)&&Object.keys(i.O).every((e=>i.O[e](t[c])))?t.splice(c--,1):(s=!1,r<o&&(o=r));if(s){e.splice(h--,1);var l=a();void 0!==l&&(n=l)}}return n}r=r||0;for(var h=e.length;h>0&&e[h-1][2]>r;h--)e[h]=e[h-1];e[h]=[t,a,r]},i.n=e=>{var n=e&&e.__esModule?()=>e.default:()=>e;return i.d(n,{a:n}),n},i.d=(e,n)=>{for(var t in n)i.o(n,t)&&!i.o(e,t)&&Object.defineProperty(e,t,{enumerable:!0,get:n[t]})},i.o=(e,n)=>Object.prototype.hasOwnProperty.call(e,n),i.p="/",(()=>{i.b=document.baseURI||self.location.href;var e={792:0};i.O.j=n=>0===e[n];var n=(n,t)=>{var a,r,o=t[0],s=t[1],c=t[2],l=0;if(o.some((n=>0!==e[n]))){for(a in s)i.o(s,a)&&(i.m[a]=s[a]);if(c)var h=c(i)}for(n&&n(t);l<o.length;l++)r=o[l],i.o(e,r)&&e[r]&&e[r][0](),e[r]=0;return i.O(h)},t=self.webpackChunkthe_way_of_code=self.webpackChunkthe_way_of_code||[];t.forEach(n.bind(null,0)),t.push=n.bind(null,t.push.bind(t))})(),i.nc=void 0;var a=i.O(void 0,[211],(()=>i(3994)));a=i.O(a)})();
//# sourceMappingURL=main.6a0edac8.js.map